---
phase: 13-audio-foundation-sound-polish
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - Assets/Resources/AudioManager.prefab (Unity 메뉴 실행으로 생성)
  - Assets/Editor/AudioManagerPrefabBuilder.cs (클립 교체 시)
  - Assets/Scripts/Enemy/EnemyDeathEffect.cs (SFX-06 판정 시)
  - Assets/Editor/HitSparkBuilder.cs (SFX-06 판정 시)
  - Assets/Scripts/World/FloorTransitionEffect.cs (SFX-06 판정 시, duration 조정 2순위)
autonomous: false
requirements: [SFX-01, SFX-02, SFX-03, SFX-04, SFX-06]

must_haves:
  truths:
    - "AudioManager 싱글턴이 MainMenu→AttackSelect→SampleScene→사망→재시작 전 구간에서 1개 인스턴스로 유지된다 (SC1)"
    - "포탈 진입 시 상승음, 새 층 등장 시 하강음이 들린다 (SC2)"
    - "대시 처치 순간 슬래시음이 들리고, 슬로우모션/HitFreeze 중에도 피치/타이밍이 깨지지 않는다 (SC3)"
    - "적 사망 시 글리치 노이즈가 슬래시와 인과적 시간차로 들린다 (SC4)"
    - "포탈/히트/사망 연출의 타이밍·피드백 어색함이 플레이테스트 기준 개선 완료로 판정된다 (SC5/SFX-06)"
  artifacts:
    - path: "Assets/Resources/AudioManager.prefab"
      provides: "부트스트랩이 로드하는 AudioManager 프리팹 — 클립 4개 연결 상태"
  key_links:
    - from: "Assets/Resources/AudioManager.prefab"
      to: "Assets/Audio/ 선별 클립 4개"
      via: "SerializeField 참조 (프리팹 YAML의 guid 참조)"
      pattern: "_portalEnter|_slash"
---

<objective>
Unity 에디터에서 프리팹 빌더를 실행해 인프라를 완성하고, 성공 기준 5개 전항목을 플레이테스트로 검증하며, 발견된 타이밍·피드백 어색함(SFX-06)을 정해진 폴리싱 레시피로 수정한다.

Purpose: Phase 13의 최종 통합·검수 게이트. 사운드는 인간 청각 판정이 유일한 검증 수단 (13-RESEARCH.md Validation Architecture의 manual-only 정당화).
Output: 클립 연결된 AudioManager.prefab, SFX-06 폴리싱 적용, 성공 기준 5개 통과 기록
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-audio-foundation-sound-polish/13-CONTEXT.md
@.planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md
@Assets/Scripts/Audio/AudioManager.cs
@Assets/Editor/AudioManagerPrefabBuilder.cs
@Assets/Scripts/Enemy/EnemyDeathEffect.cs
@Assets/Editor/HitSparkBuilder.cs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Unity 에디터 — 컴파일 확인 + 프리팹 빌더 실행 + 임포트 설정 확인</name>
  <files>Assets/Resources/AudioManager.prefab</files>
  <read_first>
    - Assets/Editor/AudioManagerPrefabBuilder.cs (실행할 메뉴와 선별 클립 경로 확인)
  </read_first>
  <action>
    Unity 에디터 내부 조작(메뉴 실행/Inspector 확인)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 사용자가 문제를 보고하면(클립 없음 에러, 임포트 설정 미적용 등) 해당 원인을 수정(경로 상수 교정, Reimport 안내)한 뒤 재실행을 요청한다.
  </action>
  <what-built>AudioManager 인프라(13-01) + CC0 팩·프리팹 빌더(13-02) + 연출 훅 4곳(13-03) — Unity 에디터 안에서의 마지막 조립 단계만 남음</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서 프로젝트를 열고 순서대로:
    1. **컴파일 확인**: Console에 컴파일 에러 0건 (경고는 허용)
    2. **임포트 설정 확인**: Assets/Audio/Kenney_SciFiSounds의 아무 클립이나 선택 → Inspector에서 "Force To Mono" 체크 + Compression Format "ADPCM"인지 확인.
       - 적용 안 되어 있으면: Assets/Audio 폴더 우클릭 → Reimport (AudioImportSettings.cs가 팩 복사와 같은 배치로 임포트된 경우 발생 가능)
    3. **DSP 확인**: Edit → Project Settings → Audio → DSP Buffer Size가 "Good latency"인지 확인. 아니면 드롭다운에서 Good latency 선택
    4. **프리팹 생성**: 메뉴 Tools → Audio → Build AudioManager Prefab 실행 → Console에 "[AudioManagerPrefabBuilder] Assets/Resources/AudioManager.prefab 생성 완료" 로그 + "클립 없음" 에러 0건 확인
    5. **프리팹 확인**: Assets/Resources/AudioManager.prefab 선택 → Inspector의 Clips 4개 슬롯(_portalEnter/_portalExit/_slash/_enemyDeathGlitch)이 전부 None이 아님
  </how-to-verify>
  <verify>
    <automated>ls "Assets/Resources/AudioManager.prefab" && grep -c "_portalEnter\|_slash" "Assets/Resources/AudioManager.prefab"</automated>
  </verify>
  <acceptance_criteria>
    - Console 컴파일 에러 0건 (사용자 확인)
    - Assets/Resources/AudioManager.prefab 파일이 디스크에 존재하고 YAML 내 _portalEnter/_portalExit/_slash/_enemyDeathGlitch 필드에 guid 참조(`{fileID: 0}` 아님)가 기록되어 있다
    - Assets/Audio 클립의 임포트 설정이 Force To Mono + ADPCM (사용자 확인)
    - DSP Buffer Size = Good latency (사용자 확인)
  </acceptance_criteria>
  <done>부트스트랩이 로드할 AudioManager.prefab이 클립 4개 연결 상태로 존재하고, 임포트/DSP 설정이 의도대로 적용됨</done>
  <resume-signal>"approved" 입력 또는 문제 항목 설명 (예: "빌더 실행 시 클립 없음 에러 2건")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 — 성공 기준 5개 검증 + SFX-06 어색함 리포트</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §Architecture Patterns Pattern 4 (SFX-06 폴리싱 후보 4개)
  </read_first>
  <action>
    사운드 재생/타이밍은 인간 청각 판정만 가능(13-RESEARCH.md manual-only 정당화) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고, SC1~SC4 통과 여부와 SFX-06 항목 A~D 리포트를 수집한다. 리포트가 Task 3의 입력이 된다.
  </action>
  <what-built>사운드 4종이 연출 3곳에 배선된 전체 오디오 파이프라인</what-built>
  <how-to-verify>
    에디터 Play (GameBootstrapper가 MainMenu로 리다이렉트) 후 체크리스트:

    **[SC1 — AudioManager 생존/단일성]**
    1. MainMenu → AttackSelect → SampleScene 진입 후, Hierarchy의 DontDestroyOnLoad 섹션에 AudioManager가 정확히 **1개** 존재
    2. 사망 → AttackSelect 복귀 → SampleScene 재진입 후에도 여전히 1개 (2개 이상이면 Pitfall 4 — 사운드 이중 재생 확인 가능)

    **[SC2 — 포탈 사운드]**
    3. EXIT 포탈 진입 시 상승 워프음이 마스크 연출과 함께 들린다
    4. 새 층 등장 시 하강음이 포탈 성장 연출과 함께 들린다

    **[SC3 — 히트 사운드 / 슬로우모션 무결성]**
    5. 공격 버튼을 눌러 슬로우모션을 유지한 채 적을 처치 → 슬래시음이 대시 도착 순간 들리고, 피치가 늘어지거나 뭉개지지 않는다 (정상 속도 처치와 동일하게 들려야 함)
    6. 연속 2~3킬 시 소리가 "기관총"처럼 겹치거나 볼륨이 튀지 않는다

    **[SC4 — 사망 사운드]**
    7. 적 처치 시 슬래시 후 글리치 노이즈가 이어진다 — 두 소리가 하나의 사건("벤다→깨진다")으로 들리는가?

    **[SFX-06 어색함 점검 — 아래 항목별로 "OK" 또는 어색한 내용 기술]**
    A. 슬래시→글리치 간격: 인과로 들리는가, 아니면 무관한 두 소리처럼 분리되는가 (0.8s 이상 벌어지면 대체로 분리감)
    B. 처치 순간 히트 스파크가 HitFreeze 75ms 동안 정지하는 것이 "임팩트 프레임"으로 느껴지는가, 어색한 멈칫임인가
    C. 포탈음 길이가 마스크 연출 구간과 맞는가 (소리가 먼저 끝나거나 연출 후에도 남는가)
    D. 클립 자체의 톤이 어색한 것이 있는가 (교체 대상 지목 — 예: "슬래시가 너무 총소리 같음")
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 오디오 청각 판정은 자동화 불가 (13-RESEARCH.md Validation Architecture 정당화). 통과 기준: 사용자 리포트에서 체크리스트 1~7 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - SC1~SC4 체크리스트 1~7번 전부 통과 보고 수신
    - SFX-06 항목 A~D 각각에 대해 OK 또는 구체 어색함 리포트 수신
  </acceptance_criteria>
  <done>성공 기준 1~4 통과 확인 + SFX-06 폴리싱 대상 목록(A~D 리포트) 확보</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved, A~D: OK", 문제가 있으면 번호별로 기술 (예: "A: 간격이 너무 길어 분리됨, D: 포탈 진입음 교체 필요")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: SFX-06 폴리싱 적용 — 리포트 기반 레시피 실행</name>
  <files>Assets/Scripts/Enemy/EnemyDeathEffect.cs, Assets/Editor/HitSparkBuilder.cs, Assets/Editor/AudioManagerPrefabBuilder.cs, Assets/Scripts/World/FloorTransitionEffect.cs</files>
  <read_first>
    - Task 2 리포트 (어떤 항목이 어색한지 — 이 태스크의 유일한 입력)
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs (레시피 A 적용 대상 — PlayDeathSequence 구조)
    - Assets/Editor/HitSparkBuilder.cs (레시피 B 적용 대상 — ParticleSystem main 모듈 설정 위치)
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §Pitfall 8, §Architecture Patterns Pattern 4
  </read_first>
  <action>
    Task 2 리포트에서 어색하다고 판정된 항목**만** 아래 레시피로 수정한다 (OK 항목은 절대 건드리지 않는다 — 정밀 변경 원칙). 전 항목 OK면 이 태스크는 "폴리싱 불요 — 훅 배치 자체로 싱크 확보됨"을 SUMMARY에 기록하고 종료한다.

    **레시피 A — 슬래시→글리치 간격 과다 (Pitfall 8):**
    EnemyDeathEffect.cs에서 글리치 호출을 Die 애니메이션 완주 대기 앞으로 이동:
    1. `[SerializeField] private float _glitchSfxDelay = 0.3f;` 필드 추가 (튜닝 노출)
    2. PlayDeathSequence 시작부(첫 `yield return null;` 직후)에 `StartCoroutine(PlayGlitchDelayed());` 추가
    3. 새 private 코루틴 추가:
       ```csharp
       private IEnumerator PlayGlitchDelayed()
       {
           yield return new WaitForSecondsRealtime(_glitchSfxDelay); // D-00b: 반드시 Realtime — HitFreeze 면역
           AudioManager.PlaySfx(Sfx.EnemyDeathGlitch);
       }
       ```
    4. 기존 step 2의 `AudioManager.PlaySfx(Sfx.EnemyDeathGlitch);` 줄 제거 (이중 재생 방지)

    **레시피 B — 히트 스파크 HitFreeze 정지 어색:**
    HitSparkBuilder.cs의 ParticleSystem main 모듈 설정부에 `main.useUnscaledTime = true;` 1줄 추가 → 재검증 시 Tools 메뉴로 HitSpark 프리팹 재생성 요청을 재청취 안내에 포함.

    **레시피 C — 포탈음-연출 길이 불일치:**
    1순위: AudioManagerPrefabBuilder.cs의 해당 경로 상수를 길이가 맞는 다른 후보 클립으로 교체 (13-02 선별 가이드의 후보 패턴에서 재선별) → 빌더 재실행 필요 플래그.
    2순위(클립 교체로 부족할 때만): FloorTransitionEffect.cs의 duration SerializeField 기본값 조정 — 진입은 `_entryMaskDuration`(현 0.4f)·`_portalShrinkDuration`(현 0.3f), 퇴장은 `_exitPortalGrowDuration`(현 0.4f)·`_exitMaskDuration`(현 0.5f)·`_portalFadeDuration`(현 0.3f)을 클립 길이에 ±0.2f 이내로 근접시킨다.

    **레시피 D — 클립 톤 어색:**
    AudioManagerPrefabBuilder.cs의 해당 경로 상수를 지목된 톤에 맞는 다른 후보로 교체 (예: 슬래시가 총소리 같으면 `laserRetro*` → `zap*` 계열) → 빌더 재실행 필요 플래그.

    수정 후: 레시피 B/C/D처럼 Unity 메뉴 재실행이 필요한 변경이 있으면, 완료 보고에 "재실행 필요: Tools → Audio → Build AudioManager Prefab (또는 HitSpark 빌더)"를 명시하고 사용자에게 해당 항목 재청취 확인을 요청한다 (checkpoint 동적 생성). 코드만 바뀐 경우(레시피 A)도 해당 항목 재청취 1회를 요청한다.
  </action>
  <verify>
    <automated>git diff --stat HEAD -- Assets/Scripts/Enemy/EnemyDeathEffect.cs Assets/Editor/HitSparkBuilder.cs Assets/Editor/AudioManagerPrefabBuilder.cs Assets/Scripts/World/FloorTransitionEffect.cs</automated>
  </verify>
  <acceptance_criteria>
    - Task 2 리포트의 어색 판정 항목마다 대응 레시피 커밋이 존재한다 (OK 항목 대응 파일은 diff 0)
    - 레시피 A 적용 시: EnemyDeathEffect.cs에 `WaitForSecondsRealtime(_glitchSfxDelay)`가 존재하고, `PlaySfx(Sfx.EnemyDeathGlitch)` 호출이 파일 내 정확히 1곳이다
    - 레시피 B 적용 시: HitSparkBuilder.cs에 `useUnscaledTime = true`가 존재한다
    - 전 항목 OK인 경우: 4개 파일 diff 0 + SUMMARY에 "폴리싱 불요" 판정 근거 기록
    - 어떤 경우에도 `WaitForSeconds(`(Realtime 아닌 것)나 `Time.deltaTime`이 새로 추가되지 않았다
  </acceptance_criteria>
  <done>플레이테스트에서 지목된 타이밍·피드백 어색함이 전부 대응 레시피로 수정되고 재청취 확인 요청까지 전달됨 — SFX-06 충족</done>
</task>

</tasks>

<verification>
- 성공 기준 5개(SC1~SC5) 플레이테스트 체크리스트 전항목 통과 (Task 2 + Task 3 재청취)
- Hierarchy DontDestroyOnLoad 아래 AudioManager 정확히 1개 (사망 재시작 루프 포함)
- Console 에러 0건
- 폴리싱 변경분이 리포트 항목과 1:1 대응 (과잉 수정 없음)
</verification>

<success_criteria>
- Phase 13 성공 기준 5개 전부 TRUE: 싱글턴 3씬 생존+재생 / 포탈 진입·퇴장음 / 슬로우모션 무결 히트음 / 사망음 / 타이밍 어색함 개선
- `/gsd:verify-work 13` 진입 가능 상태
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-foundation-sound-polish/13-04-SUMMARY.md` (플레이테스트 리포트 요지 + 적용한 레시피 + 미적용 사유 포함)
</output>
