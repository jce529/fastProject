---
phase: 13-audio-foundation-sound-polish
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - Assets/Scripts/Player/CombatController.cs
  - Assets/Scripts/Enemy/EnemyDeathEffect.cs
  - Assets/Scripts/World/FloorTransitionEffect.cs
autonomous: true
requirements: [SFX-02, SFX-03, SFX-04]

must_haves:
  truths:
    - "대시 처치 확정 순간(OnDashHit 직후, HitFreeze 이전)에 슬래시 사운드가 트리거된다"
    - "적 사망 연출의 파티클 재생 직전에 글리치 노이즈가 트리거된다 — 슬래시와 자연 시간차 레이어 (D-05)"
    - "포탈 진입 마스크 성장 시작과 동시에 상승음, 새 층 포탈 성장 시작과 동시에 하강음이 트리거된다 (D-06)"
  artifacts:
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "SFX-03 슬래시 훅 (기존 파일 1줄 추가)"
      contains: "AudioManager.PlaySfx(Sfx.Slash)"
    - path: "Assets/Scripts/Enemy/EnemyDeathEffect.cs"
      provides: "SFX-04 글리치 훅 (기존 파일 1줄 추가)"
      contains: "AudioManager.PlaySfx(Sfx.EnemyDeathGlitch)"
    - path: "Assets/Scripts/World/FloorTransitionEffect.cs"
      provides: "SFX-02 포탈 진입/퇴장 훅 (기존 파일 2줄 추가)"
      contains: "AudioManager.PlaySfx(Sfx.PortalEnter)"
  key_links:
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/Audio/AudioManager.cs"
      via: "ExecuteDash step 6 처치 분기의 static 호출"
      pattern: "AudioManager\\.PlaySfx\\(Sfx\\.Slash\\)"
    - from: "Assets/Scripts/Enemy/EnemyDeathEffect.cs"
      to: "Assets/Scripts/Audio/AudioManager.cs"
      via: "PlayDeathSequence step 2 파티클 직전 static 호출"
      pattern: "AudioManager\\.PlaySfx\\(Sfx\\.EnemyDeathGlitch\\)"
    - from: "Assets/Scripts/World/FloorTransitionEffect.cs"
      to: "Assets/Scripts/Audio/AudioManager.cs"
      via: "PlayEntry/PlayExit 첫 구간의 static 호출"
      pattern: "AudioManager\\.PlaySfx\\(Sfx\\.Portal(Enter|Exit)\\)"
---

<objective>
Phase 12 연출 컴포넌트 3곳에 AudioManager.PlaySfx 호출을 배선한다 — 총 4줄 추가, 리팩토링 0건 (CLAUDE.md 정밀 변경 원칙).

Purpose: SFX-02(포탈), SFX-03(히트), SFX-04(사망)가 실제 연출 타이밍에 소리를 내도록 하는 유일한 코드 연결점.
Output: 기존 파일 3개에 각각 1~2줄 추가된 훅
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-audio-foundation-sound-polish/13-CONTEXT.md
@.planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md
@Assets/Scripts/Player/CombatController.cs
@Assets/Scripts/Enemy/EnemyDeathEffect.cs
@Assets/Scripts/World/FloorTransitionEffect.cs

<interfaces>
<!-- 13-01이 생성한 호출 대상 API — 탐색 불필요. -->

From Assets/Scripts/Audio/AudioManager.cs (13-01 산출물):
```csharp
public enum Sfx { PortalEnter, PortalExit, Slash, EnemyDeathGlitch }
public static void PlaySfx(Sfx id, float volume = 1f); // null 안전 — 부트스트랩 실패 시 조용히 무시
```

훅 지점 현재 코드 (2026-07-09 소스 확인 — 라인 번호는 참고용, 패턴으로 위치 확인할 것):

CombatController.cs ExecuteDash() step 6 (~line 306-311):
```csharp
// 6. Kill and effects
target.OnDashHit();
SpawnHitSpark(destination);
_cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
ScoreManager.AddKillScore();
yield return StartCoroutine(HitFreeze(hitFreezeDuration));
```

EnemyDeathEffect.cs PlayDeathSequence() step 2 (~line 43-44):
```csharp
// 2. 파티클 재생
SpawnDeathParticles();
```

FloorTransitionEffect.cs PlayEntry() 시그니처와 첫 줄 (~line 27-29):
```csharp
public IEnumerator PlayEntry(Transform portal)
{
    float portalX = portal.position.x;
```

FloorTransitionEffect.cs PlayExit() 시그니처와 첫 구간 (~line 61-66):
```csharp
public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)
{
    // X1: 퇴장 포탈 이펙트 성장.
    GameObject portalEffect = null;
    if (portalEffectPrefab != null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 처치 체인 훅 — 슬래시(CombatController) + 사망 글리치(EnemyDeathEffect) (SFX-03, SFX-04, D-05)</name>
  <files>Assets/Scripts/Player/CombatController.cs, Assets/Scripts/Enemy/EnemyDeathEffect.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (ExecuteDash 코루틴 전체 — step 6 "Kill and effects" 주석 블록 위치 확인)
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs (PlayDeathSequence — "// 2. 파티클 재생" 주석 위치 확인)
    - Assets/Scripts/Audio/AudioManager.cs (PlaySfx 시그니처 확인)
  </read_first>
  <action>
    **CombatController.cs** — ExecuteDash()의 step 6 처치 분기에서 `target.OnDashHit();` 바로 다음 줄에 1줄 추가:
    ```csharp
    target.OnDashHit();
    AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
    SpawnHitSpark(destination);
    ```
    위치가 중요한 이유: HitFreeze(timeScale=0) 이전에 호출해야 임팩트 프레임과 사운드 어택이 일치한다. Unity DSP 클럭은 timeScale 독립이므로 HitFreeze 75ms 동안에도 클립은 정상 재생된다 (RESEARCH.md 검증).

    **EnemyDeathEffect.cs** — PlayDeathSequence()의 `// 2. 파티클 재생` 주석 직후, `SpawnDeathParticles();` 바로 앞에 1줄 추가:
    ```csharp
    // 2. 파티클 재생
    AudioManager.PlaySfx(Sfx.EnemyDeathGlitch); // SFX-04/D-05: 시뮬레이션 NPC 붕괴 노이즈 — Die 애니메이션 완주 후라 슬래시와 자연 시간차
    SpawnDeathParticles();
    ```
    슬래시→글리치 시간차(HitFreeze 75ms + Die 애니메이션 길이)는 D-05의 "벤다→깨진다" 레이어를 자연 형성한다. 간격이 0.8s를 넘어 어색하면 13-04 Task 3(SFX-06)에서 조정한다 — 이 플랜에서는 이동하지 않는다.

    두 파일 모두 다른 어떤 줄도 수정/리팩토링하지 않는다.
  </action>
  <verify>
    <automated>grep -n "AudioManager.PlaySfx(Sfx.Slash)" "Assets/Scripts/Player/CombatController.cs" && grep -n "AudioManager.PlaySfx(Sfx.EnemyDeathGlitch)" "Assets/Scripts/Enemy/EnemyDeathEffect.cs"</automated>
  </verify>
  <acceptance_criteria>
    - CombatController.cs에서 `AudioManager.PlaySfx(Sfx.Slash);`가 `target.OnDashHit();` 다음 줄, `SpawnHitSpark(destination);` 이전 줄에 위치한다
    - EnemyDeathEffect.cs에서 `AudioManager.PlaySfx(Sfx.EnemyDeathGlitch);`가 `SpawnDeathParticles();` 직전 줄에 위치한다
    - 두 파일의 git diff가 각각 정확히 +1줄이다 (기존 코드 변경 0)
  </acceptance_criteria>
  <done>대시 처치 순간 슬래시, 적 붕괴 시작 순간 글리치 노이즈가 트리거되는 배선 완료 — D-05 2단 콤보 구조 성립</done>
</task>

<task type="auto">
  <name>Task 2: 포탈 전환 훅 — 진입 상승음 / 퇴장 하강음 (SFX-02, D-06)</name>
  <files>Assets/Scripts/World/FloorTransitionEffect.cs</files>
  <read_first>
    - Assets/Scripts/World/FloorTransitionEffect.cs (PlayEntry/PlayExit 코루틴 구조와 타이밍 SerializeField 확인 — _entryMaskDuration 0.4f, _exitPortalGrowDuration 0.4f 등)
    - Assets/Scripts/Audio/AudioManager.cs (PlaySfx 시그니처 확인)
  </read_first>
  <action>
    **FloorTransitionEffect.cs** — 2줄 추가:

    1. `PlayEntry(Transform portal)` 본문 첫 줄(`float portalX = portal.position.x;` 앞)에 추가:
    ```csharp
    public IEnumerator PlayEntry(Transform portal)
    {
        AudioManager.PlaySfx(Sfx.PortalEnter); // SFX-02/D-06: 진입 = 상승 워프음 — E2 마스크 성장(0.4s)과 동시 시작
        float portalX = portal.position.x;
    ```

    2. `PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)` 본문 첫 줄(`// X1` 주석 앞)에 추가:
    ```csharp
    public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)
    {
        AudioManager.PlaySfx(Sfx.PortalExit); // SFX-02/D-06: 퇴장 = 하강 마무리음 — X1 포탈 성장(0.4s)과 동시 시작
        // X1: 퇴장 포탈 이펙트 성장.
    ```

    사운드-비주얼 정합 근거: PlayEntry 총 연출 0.7s(마스크 0.4 + 수축 0.3) ↔ 진입 클립 0.5–0.8s, PlayExit 총 ~1.2s(성장 0.4 + 마스크 0.5 + 페이드 0.3) ↔ 퇴장 클립 0.8–1.2s (13-02 선별 기준과 대응). 싱크가 어긋나면 13-04 Task 3에서 클립 교체 1순위, duration SerializeField 조정 2순위로 처리한다.

    다른 어떤 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "AudioManager.PlaySfx(Sfx.PortalEnter)" "Assets/Scripts/World/FloorTransitionEffect.cs" && grep -n "AudioManager.PlaySfx(Sfx.PortalExit)" "Assets/Scripts/World/FloorTransitionEffect.cs"</automated>
  </verify>
  <acceptance_criteria>
    - FloorTransitionEffect.cs에서 `AudioManager.PlaySfx(Sfx.PortalEnter);`가 PlayEntry 본문 첫 실행문이다
    - FloorTransitionEffect.cs에서 `AudioManager.PlaySfx(Sfx.PortalExit);`가 PlayExit 본문 첫 실행문이다
    - 파일의 git diff가 정확히 +2줄이다 (기존 코드 변경 0)
  </acceptance_criteria>
  <done>포탈 진입 시 상승음, 새 층 등장 시 하강음이 연출 시작과 동시에 트리거되는 배선 완료 — D-06 방향감 리듬 성립</done>
</task>

</tasks>

<verification>
- 4개 PlaySfx 호출이 정확한 위치에 존재 (위 grep 4건 통과)
- `git diff --stat`에서 3개 파일 합계 +4줄 외 변경 없음
- 청취 검증은 13-04 체크포인트에서 수행 (클립 연결이 선행 조건이므로 이 플랜 단독으로는 무음이 정상 — PlaySfx는 null 안전)
</verification>

<success_criteria>
- SFX-02/03/04의 코드 배선 100% 완료 — 남은 것은 프리팹 생성(13-04 체크포인트)뿐
- 기존 연출 코드 무변경 (추가 4줄 외 diff 0)
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-foundation-sound-polish/13-03-SUMMARY.md`
</output>
