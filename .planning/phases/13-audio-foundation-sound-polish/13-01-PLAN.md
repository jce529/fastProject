---
phase: 13-audio-foundation-sound-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Audio/AudioManager.cs
  - Assets/Editor/AudioImportSettings.cs
  - ProjectSettings/AudioManager.asset
autonomous: true
requirements: [SFX-01]

must_haves:
  truths:
    - "AudioManager.PlaySfx(Sfx.Slash) 형태의 static 호출이 어느 스크립트에서든 컴파일된다 (Sfx enum 4종 존재)"
    - "Assets/Audio/ 하위로 임포트되는 모든 오디오 파일에 Force To Mono + ADPCM + Decompress On Load가 자동 적용된다"
    - "DSP 버퍼 사이즈가 512(Good latency)로 설정되어 히트 사운드 지연이 최소화된다"
  artifacts:
    - path: "Assets/Scripts/Audio/AudioManager.cs"
      provides: "MonoBehaviour 싱글턴 + 2채널(액션/배경) AudioSource 풀 + enum 키 API + RuntimeInitializeOnLoadMethod 부트스트랩"
      exports: ["Sfx", "AudioManager", "PlaySfx"]
      min_lines: 80
    - path: "Assets/Editor/AudioImportSettings.cs"
      provides: "Assets/Audio/ 대상 모바일 임포트 설정 자동화 (AssetPostprocessor)"
      contains: "OnPreprocessAudio"
    - path: "ProjectSettings/AudioManager.asset"
      provides: "DSP Buffer 512 설정"
      contains: "m_RequestedDSPBufferSize: 512"
  key_links:
    - from: "Assets/Scripts/Audio/AudioManager.cs"
      to: "Resources/AudioManager.prefab (Plan 13-02가 생성)"
      via: "Bootstrap()의 Resources.Load"
      pattern: "Resources\\.Load<AudioManager>\\(\"AudioManager\"\\)"
    - from: "Assets/Editor/AudioImportSettings.cs"
      to: "Assets/Audio/ (Plan 13-02가 팩 복사)"
      via: "assetPath.StartsWith 경로 필터"
      pattern: "Assets/Audio/"
---

<objective>
프로젝트 최초의 오디오 재생 인프라를 신설한다: AudioManager 싱글턴(2채널 풀 + enum API + 부트스트랩), CC0 팩 임포트 전에 반드시 존재해야 하는 AssetPostprocessor(모바일 임포트 설정), DSP 버퍼 512 설정.

Purpose: SFX-01의 코드 기반 전체. 이 플랜이 Wave 1인 이유 — AudioImportSettings.cs가 오디오 팩 복사(Plan 13-02) **이전에** 커밋되어 있어야 최초 임포트부터 설정이 적용된다 (RESEARCH.md Pitfall 5).
Output: AudioManager.cs, AudioImportSettings.cs, DSP 512 설정된 ProjectSettings/AudioManager.asset
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-audio-foundation-sound-polish/13-CONTEXT.md
@.planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md
@Assets/Scripts/World/GameBootstrapper.cs
@Assets/Scripts/Player/InputManager.cs

<interfaces>
<!-- 기존 코드베이스에서 재사용할 검증된 패턴. 탐색 불필요 — 아래를 그대로 따를 것. -->

From Assets/Scripts/World/GameBootstrapper.cs (부트스트랩 어트리뷰트 선례):
```csharp
[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
private static void EnsureMainMenu() { ... }
```

From Assets/Scripts/Player/InputManager.cs:9-13 (싱글턴 중복 가드 선례 — Pitfall 4 대응):
```csharp
public static InputManager Instance { get; private set; }
private void Awake()
{
    if (Instance != null && Instance != this) { Destroy(gameObject); return; }
    Instance = this;
}
```

ProjectSettings/AudioManager.asset 현재 상태 (수정 대상 라인):
```yaml
m_DSPBufferSize: 1024
m_RequestedDSPBufferSize: 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudioImportSettings.cs — Assets/Audio/ 모바일 임포트 설정 자동화</name>
  <files>Assets/Editor/AudioImportSettings.cs</files>
  <read_first>
    - Assets/Editor/HitSparkBuilder.cs (기존 Editor 스크립트 네임스페이스/스타일 확인 — 이 프로젝트 Editor 스크립트는 네임스페이스 없이 파일당 1클래스)
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §Pitfall 5, §Code Examples/임포트 설정 자동화
  </read_first>
  <action>
    `Assets/Editor/AudioImportSettings.cs`를 아래 코드로 신규 생성한다 (RESEARCH.md 검증 코드 그대로 — Pitfall 5 대응):

    ```csharp
    using UnityEditor;
    using UnityEngine;

    /// <summary>
    /// Assets/Audio/ 하위로 임포트되는 모든 오디오 파일에 모바일(Android ARM64) 최적
    /// 임포트 설정을 일괄 적용한다. CC0 팩 통째 임포트(D-02, ~130파일)의 수작업을 대체.
    /// 반드시 오디오 팩 복사 이전에 이 스크립트가 커밋/컴파일되어 있어야 한다 (Pitfall 5).
    /// </summary>
    public class AudioImportSettings : AssetPostprocessor
    {
        private void OnPreprocessAudio()
        {
            if (!assetPath.StartsWith("Assets/Audio/")) return;

            var importer = (AudioImporter)assetImporter;
            importer.forceToMono = true;               // 2D 게임 SFX — 메모리 50% 절감

            var settings = importer.defaultSampleSettings;
            settings.loadType          = AudioClipLoadType.DecompressOnLoad; // 짧은 SFX — 재생 시 디코딩 CPU 0
            settings.compressionFormat = AudioCompressionFormat.ADPCM;       // 임팩트성 SFX 모바일 표준 (~3.5:1)
            settings.sampleRateSetting = AudioSampleRateSetting.OptimizeSampleRate;
            importer.defaultSampleSettings = settings;
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "OnPreprocessAudio" "Assets/Editor/AudioImportSettings.cs" && grep -c "AudioCompressionFormat.ADPCM" "Assets/Editor/AudioImportSettings.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/AudioImportSettings.cs 파일이 존재한다
    - 파일에 `class AudioImportSettings : AssetPostprocessor` 선언이 포함된다
    - 파일에 `assetPath.StartsWith("Assets/Audio/")` 경로 필터가 포함된다
    - 파일에 `forceToMono = true`, `AudioClipLoadType.DecompressOnLoad`, `AudioCompressionFormat.ADPCM`, `AudioSampleRateSetting.OptimizeSampleRate` 4개 설정이 모두 포함된다
  </acceptance_criteria>
  <done>Assets/Audio/ 하위로 임포트되는 오디오 파일 전체에 Force To Mono + ADPCM + Decompress On Load + Optimize Sample Rate가 자동 적용될 준비 완료</done>
</task>

<task type="auto">
  <name>Task 2: AudioManager.cs — 싱글턴 + 2채널 풀 + enum API + 부트스트랩 (SFX-01, D-00a, D-07, D-08)</name>
  <files>Assets/Scripts/Audio/AudioManager.cs</files>
  <read_first>
    - Assets/Scripts/World/GameBootstrapper.cs (RuntimeInitializeOnLoadMethod 선례 — 동일 어트리뷰트 사용)
    - Assets/Scripts/Player/InputManager.cs 1-13행 (싱글턴 중복 가드 선례 — 동일 패턴 사용)
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §Code Examples/AudioManager 핵심 골격
  </read_first>
  <action>
    `Assets/Scripts/Audio/` 폴더를 만들고 `AudioManager.cs`를 아래 코드로 신규 생성한다 (RESEARCH.md 검증 골격 그대로. D-00a: MonoBehaviour 싱글턴+풀링, D-07: 2채널, D-08: 이번 페이즈 클립은 전부 액션 채널):

    ```csharp
    using UnityEngine;

    public enum Sfx
    {
        PortalEnter,      // D-06: 상승 워프음
        PortalExit,       // D-06: 하강 마무리음
        Slash,            // D-05: 대시 처치 슬래시
        EnemyDeathGlitch, // D-05: 사망 글리치 노이즈
    }

    /// <summary>
    /// SFX-01: 프로젝트 유일 오디오 재생 진입점. MonoBehaviour 싱글턴 + 2채널 AudioSource 풀 (D-00a/D-07).
    /// - 액션 채널(8보이스): timeScale 완전 독립 — Unity DSP 클럭은 timeScale 영향을 받지 않으므로 추가 코드 없음.
    /// - 배경 채널(2보이스): LateUpdate에서 pitch = Time.timeScale 추종 (슬로우모션 피치다운 인프라, 이번 페이즈 소비 클립 없음 — D-08).
    /// GameBootstrapper와 동일한 RuntimeInitializeOnLoadMethod 부트스트랩 — 씬 수정 0회로 3개 씬 전부에서 생존.
    /// </summary>
    public class AudioManager : MonoBehaviour
    {
        public static AudioManager Instance { get; private set; }

        [Header("Channel Pools (D-07)")]
        [SerializeField] private int _actionVoices = 8;
        [SerializeField] private int _ambientVoices = 2;
        [SerializeField, Range(0.05f, 1f)] private float _minAmbientPitch = 0.3f; // Pitfall 1: pitch=0 정지 방지

        [Header("Clips — 플레이테스트 중 Inspector에서 교체 (D-03)")]
        [SerializeField] private AudioClip _portalEnter;
        [SerializeField] private AudioClip _portalExit;
        [SerializeField] private AudioClip _slash;
        [SerializeField] private AudioClip _enemyDeathGlitch;

        private AudioSource[] _actionPool;
        private AudioSource[] _ambientPool;
        private int _actionCursor;
        private AudioClip _lastClip;
        private double _lastClipDspTime; // double 필수 — dspTime은 누적 증가

        // GameBootstrapper.cs와 동일 패턴 — 씬 수정 없이 3개 씬 전부에서 생존 보장
        [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
        private static void Bootstrap()
        {
            if (Instance != null) return;
            var prefab = Resources.Load<AudioManager>("AudioManager");
            if (prefab == null) { Debug.LogError("[Audio] Resources/AudioManager.prefab 없음"); return; }
            DontDestroyOnLoad(Instantiate(prefab.gameObject));
        }

        private void Awake()
        {
            // InputManager.cs:11 기존 중복 가드 패턴 (Pitfall 4: DontDestroyOnLoad 중복 인스턴스)
            if (Instance != null && Instance != this) { Destroy(gameObject); return; }
            Instance = this;
            _actionPool  = CreatePool("Action", _actionVoices);
            _ambientPool = CreatePool("Ambient", _ambientVoices);
        }

        private AudioSource[] CreatePool(string label, int size)
        {
            var pool = new AudioSource[size];
            for (int i = 0; i < size; i++)
            {
                var child = new GameObject($"{label}Voice{i}");
                child.transform.SetParent(transform, false);
                var src = child.AddComponent<AudioSource>();
                src.playOnAwake  = false;
                src.spatialBlend = 0f; // 2D — AudioListener 위치 무관
                pool[i] = src;
            }
            return pool;
        }

        private void LateUpdate()
        {
            // D-07 배경 채널: timeScale 추종 피치다운. 액션 채널은 절대 건드리지 않음.
            // AudioSource DSP 재생은 timeScale 독립이므로 액션 채널 코드는 0줄 (RESEARCH.md 검증).
            float pitch = Mathf.Max(_minAmbientPitch, Time.timeScale);
            for (int i = 0; i < _ambientPool.Length; i++)
                _ambientPool[i].pitch = pitch;
        }

        /// <summary>모든 연출 훅의 단일 진입점. null 안전 — 부트스트랩 실패 시 조용히 무시.</summary>
        public static void PlaySfx(Sfx id, float volume = 1f) => Instance?.PlayInternal(id, volume);

        private void PlayInternal(Sfx id, float volume)
        {
            AudioClip clip = id switch
            {
                Sfx.PortalEnter      => _portalEnter,
                Sfx.PortalExit       => _portalExit,
                Sfx.Slash            => _slash,
                Sfx.EnemyDeathGlitch => _enemyDeathGlitch,
                _ => null,
            };
            if (clip == null) return;

            // Pitfall 3: 동일 클립 30ms 내 재트리거 스킵 (연속 처치 위상 중첩 클리핑 방지)
            if (clip == _lastClip && AudioSettings.dspTime - _lastClipDspTime < 0.03) return;
            _lastClip = clip;
            _lastClipDspTime = AudioSettings.dspTime;

            var src = _actionPool[_actionCursor];
            _actionCursor = (_actionCursor + 1) % _actionPool.Length;
            src.pitch = Random.Range(0.95f, 1.05f); // 반복 재생 기계음 방지 — timeScale 연동 아님
            src.PlayOneShot(clip, volume);
        }
    }
    ```

    주의사항 (Anti-Patterns, RESEARCH.md):
    - AudioListener를 절대 추가하지 않는다 — 각 씬 Main Camera에 이미 존재
    - `WaitForSeconds`/`Time.deltaTime` 사용 금지 — 이 파일에는 시간 대기 코드 자체가 없어야 한다 (LateUpdate pitch 추종은 프레임 단위라 timeScale 무관)
    - AudioMixer 도입 금지 — per-source volume/pitch로 충분 (오버엔지니어링)
  </action>
  <verify>
    <automated>grep -c "public static void PlaySfx" "Assets/Scripts/Audio/AudioManager.cs" && grep -c "RuntimeInitializeOnLoadMethod" "Assets/Scripts/Audio/AudioManager.cs" && grep -c "EnemyDeathGlitch" "Assets/Scripts/Audio/AudioManager.cs" && ! grep -n "WaitForSeconds\b\|Time.deltaTime" "Assets/Scripts/Audio/AudioManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Audio/AudioManager.cs가 존재하고 `public enum Sfx` 안에 PortalEnter, PortalExit, Slash, EnemyDeathGlitch 4개 값이 정의된다
    - `public static void PlaySfx(Sfx id, float volume = 1f)` 시그니처가 존재한다
    - `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]` + `Resources.Load<AudioManager>("AudioManager")` 부트스트랩이 존재한다
    - Awake에 `if (Instance != null && Instance != this) { Destroy(gameObject); return; }` 중복 가드가 존재한다
    - LateUpdate에 `Mathf.Max(_minAmbientPitch, Time.timeScale)` 배경 채널 피치 추종이 존재한다
    - `AudioSettings.dspTime` 기반 30ms 재트리거 가드가 존재한다
    - 파일 어디에도 `WaitForSeconds`(Realtime 아닌 것), `Time.deltaTime`, `AudioListener`, `AudioMixer` 문자열이 없다
  </acceptance_criteria>
  <done>어느 스크립트에서든 `AudioManager.PlaySfx(Sfx.Slash)` 호출이 가능한 싱글턴 인프라 완성 — 클립 연결(13-02)과 훅 배선(13-03)이 병렬로 이어질 수 있는 상태</done>
</task>

<task type="auto">
  <name>Task 3: DSP Buffer Size 512 설정 (Pitfall 2 — 히트 사운드 지연 제거)</name>
  <files>ProjectSettings/AudioManager.asset</files>
  <read_first>
    - ProjectSettings/AudioManager.asset (현재 값 확인: m_DSPBufferSize: 1024, m_RequestedDSPBufferSize: 0)
  </read_first>
  <action>
    `ProjectSettings/AudioManager.asset` YAML을 직접 수정한다 (2개 라인만 변경, 다른 라인 유지):

    - `m_DSPBufferSize: 1024` → `m_DSPBufferSize: 512`
    - `m_RequestedDSPBufferSize: 0` → `m_RequestedDSPBufferSize: 512`

    이유: 현재 1024(Best Performance)는 모바일에서 수십 ms 지연 — "대시 도착 순간 슬래시"라는 코어 손맛에 치명적. 256(Best latency)은 에디터 스터터/저사양 Android 크래클 보고가 있어 512(Good latency)가 안전한 중간값 (RESEARCH.md Pitfall 2).

    주의: Unity 에디터가 열려 있으면 에디터가 종료 시 이 파일을 덮어쓸 수 있다. 수정 후 13-04 체크포인트에서 Project Settings → Audio → DSP Buffer Size가 "Good latency"로 표시되는지 확인하는 항목이 있다.
  </action>
  <verify>
    <automated>grep -c "m_RequestedDSPBufferSize: 512" "ProjectSettings/AudioManager.asset" && grep -c "m_DSPBufferSize: 512" "ProjectSettings/AudioManager.asset"</automated>
  </verify>
  <acceptance_criteria>
    - ProjectSettings/AudioManager.asset에 `m_DSPBufferSize: 512` 라인이 존재한다
    - ProjectSettings/AudioManager.asset에 `m_RequestedDSPBufferSize: 512` 라인이 존재한다
    - 파일의 다른 필드(m_Volume, m_VirtualVoiceCount 등)는 변경되지 않았다 (git diff가 2라인만 표시)
  </acceptance_criteria>
  <done>DSP 버퍼가 Good latency(512)로 요청되어 히트 임팩트 사운드의 체감 지연이 제거될 준비 완료</done>
</task>

</tasks>

<verification>
- `Assets/Editor/AudioImportSettings.cs`, `Assets/Scripts/Audio/AudioManager.cs` 존재 + 위 grep 전부 통과
- `ProjectSettings/AudioManager.asset` git diff가 DSP 2라인만 변경
- 컴파일 확인은 Unity 에디터가 필요한 작업이므로 13-04 체크포인트에서 수행 (콘솔 에러 0)
</verification>

<success_criteria>
- SFX-01 코드 기반 완성: PlaySfx enum API + 2채널 풀 + 부트스트랩 + 중복 가드 전부 존재
- 오디오 팩 임포트 전 필수 선행물(AssetPostprocessor) 커밋 완료 — Plan 13-02 진행 가능
- DSP 512 설정 완료
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-foundation-sound-polish/13-01-SUMMARY.md`
</output>
