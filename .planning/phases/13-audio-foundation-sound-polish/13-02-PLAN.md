---
phase: 13-audio-foundation-sound-polish
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - Assets/Audio/Kenney_SciFiSounds/ (신규 폴더, ~70 ogg)
  - Assets/Audio/Kenney_DigitalAudio/ (신규 폴더, ~60 ogg)
  - Assets/Editor/AudioManagerPrefabBuilder.cs
autonomous: true
requirements: [SFX-01, SFX-02, SFX-03, SFX-04]

must_haves:
  truths:
    - "Kenney Sci-Fi Sounds + Digital Audio 두 CC0 팩이 Assets/Audio/ 하위에 통째로 존재한다 (D-01, D-02)"
    - "포탈진입/포탈퇴장/슬래시/사망글리치 4개 용도에 각각 구체 클립 파일이 D-05/D-06 기준으로 선별되어 빌더에 기록된다 (D-03)"
    - "Unity 메뉴 1회 실행으로 Resources/AudioManager.prefab이 클립 4개 연결 상태로 생성될 수 있다"
  artifacts:
    - path: "Assets/Audio/Kenney_SciFiSounds/"
      provides: "SF 톤 SFX 라이브러리 (워프/레이저/포스필드 계열, CC0)"
    - path: "Assets/Audio/Kenney_DigitalAudio/"
      provides: "디지털 톤 SFX 라이브러리 (상승·하강 톤/전자음 계열, CC0)"
    - path: "Assets/Editor/AudioManagerPrefabBuilder.cs"
      provides: "AudioManager.prefab 생성 + 선별 클립 4개 SerializedObject 연결 에디터 도구"
      contains: "MenuItem"
  key_links:
    - from: "Assets/Editor/AudioManagerPrefabBuilder.cs"
      to: "Assets/Audio/ 내 선별 클립 4개"
      via: "AssetDatabase.LoadAssetAtPath<AudioClip> 경로 상수"
      pattern: "AssetDatabase\\.LoadAssetAtPath<AudioClip>"
    - from: "Assets/Editor/AudioManagerPrefabBuilder.cs"
      to: "Assets/Resources/AudioManager.prefab"
      via: "PrefabUtility.SaveAsPrefabAsset"
      pattern: "SaveAsPrefabAsset.*Resources/AudioManager"
---

<objective>
Kenney CC0 사운드 팩 2종을 다운로드해 Assets/Audio/에 통째로 임포트하고(D-01/D-02), D-05/D-06 기준으로 클립 4개를 선별해(D-03/D-04) Resources/AudioManager.prefab을 생성하는 에디터 빌더를 작성한다.

Purpose: SFX-02/03/04가 실제로 소리날 클립 자산과, AudioManager 부트스트랩이 로드할 프리팹의 생성 경로를 확보한다.
Output: Assets/Audio/ 팩 2종(~130파일), AudioManagerPrefabBuilder.cs (13-04 체크포인트에서 실행)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-audio-foundation-sound-polish/13-CONTEXT.md
@.planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md
@Assets/Editor/PortalEffectBuilder.cs
@Assets/Scripts/Audio/AudioManager.cs

<interfaces>
<!-- Plan 13-01이 생성한 AudioManager의 SerializeField 필드명 — 빌더의 SerializedObject 연결 키. -->

From Assets/Scripts/Audio/AudioManager.cs (13-01 산출물):
```csharp
[SerializeField] private AudioClip _portalEnter;
[SerializeField] private AudioClip _portalExit;
[SerializeField] private AudioClip _slash;
[SerializeField] private AudioClip _enemyDeathGlitch;
```
SerializedObject 프로퍼티 경로: "_portalEnter", "_portalExit", "_slash", "_enemyDeathGlitch"

부트스트랩이 기대하는 프리팹 경로 (13-01 Bootstrap 코드 기준):
`Resources.Load<AudioManager>("AudioManager")` → 프리팹은 반드시 `Assets/Resources/AudioManager.prefab`, 루트 GameObject에 AudioManager 컴포넌트 부착.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kenney CC0 팩 2종 다운로드 & Assets/Audio/ 통째 임포트 (D-01, D-02)</name>
  <files>Assets/Audio/Kenney_SciFiSounds/, Assets/Audio/Kenney_DigitalAudio/</files>
  <read_first>
    - Assets/Editor/AudioImportSettings.cs (13-01 산출물 — 존재 확인. 없으면 중단하고 13-01 먼저 완료할 것: 이 파일이 없는 상태에서 오디오를 복사하면 임포트 설정이 적용되지 않는다)
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §Standard Stack/Installation, §Open Questions 2
  </read_first>
  <action>
    1. 임시 폴더에 두 팩의 zip을 다운로드한다. kenney.nl 다운로드 URL은 해시 경로라 사전 확정 불가 — 에셋 페이지 HTML에서 zip 링크를 추출한다:
       ```bash
       curl -sL "https://kenney.nl/assets/sci-fi-sounds" -o scifi_page.html
       grep -oE 'https://kenney\.nl/media/pages/assets/[^"]+\.zip' scifi_page.html | head -1
       # 추출된 URL로:
       curl -L -o kenney_scifi.zip "<추출된 URL>"

       curl -sL "https://kenney.nl/assets/digital-audio" -o digital_page.html
       grep -oE 'https://kenney\.nl/media/pages/assets/[^"]+\.zip' digital_page.html | head -1
       curl -L -o kenney_digital.zip "<추출된 URL>"
       ```
       (링크 패턴이 다르면 `grep -oE '[^"]+\.zip'`으로 완화해 재시도)
    2. PowerShell `Expand-Archive`로 각각 압축 해제한다.
    3. 압축 내용물 전체(오디오 파일들 + License.txt — 통째 임포트가 D-02 결정)를 다음 위치로 복사한다. 팩 내부에 `Audio/` 서브폴더가 있으면 그 안의 파일들을 대상 폴더 바로 아래로 평탄화 복사:
       - Sci-Fi Sounds → `Assets/Audio/Kenney_SciFiSounds/`
       - Digital Audio → `Assets/Audio/Kenney_DigitalAudio/`
    4. 복사 후 각 폴더의 오디오 파일 개수를 세어 보고한다 (Sci-Fi ~70개, Digital ~60개 기대).

    **폴백 (다운로드 실패 시):** curl 추출/다운로드가 2회 시도에도 실패하면 진행을 멈추고 checkpoint:human-action을 동적으로 생성한다 — 사용자에게 브라우저로 https://kenney.nl/assets/sci-fi-sounds 와 https://kenney.nl/assets/digital-audio 에서 zip 2개를 받아 지정 임시 폴더에 넣어 달라고 요청 후 3번부터 재개.
  </action>
  <verify>
    <automated>ls "Assets/Audio/Kenney_SciFiSounds" | grep -c -i "\.ogg\|\.wav" && ls "Assets/Audio/Kenney_DigitalAudio" | grep -c -i "\.ogg\|\.wav"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Audio/Kenney_SciFiSounds/에 오디오 파일(.ogg 또는 .wav)이 50개 이상 존재한다
    - Assets/Audio/Kenney_DigitalAudio/에 오디오 파일이 40개 이상 존재한다
    - 각 폴더에 License.txt(CC0 명시)가 함께 복사되어 있다
  </acceptance_criteria>
  <done>CC0 SF/디지털 사운드 라이브러리 ~130파일이 프로젝트에 상비되어 이번 페이즈 및 Phase 16(보스 스폰 사운드)에서 재다운로드 없이 사용 가능</done>
</task>

<task type="auto">
  <name>Task 2: 클립 4종 선별 + AudioManagerPrefabBuilder.cs 작성 (D-03, D-04, D-05, D-06)</name>
  <files>Assets/Editor/AudioManagerPrefabBuilder.cs</files>
  <read_first>
    - Assets/Audio/ 하위 전체 파일 목록 (Task 1 산출물 — 실제 파일명 확인 후 선별. RESEARCH.md의 파일명 패턴은 추정이므로 반드시 실목록 기준으로 결정)
    - Assets/Scripts/Audio/AudioManager.cs (SerializeField 필드명 4개 확인)
    - Assets/Editor/PortalEffectBuilder.cs (기존 빌더 스크립트의 MenuItem/프리팹 저장 스타일)
    - .planning/phases/13-audio-foundation-sound-polish/13-RESEARCH.md §클립 선별 가이드
  </read_first>
  <action>
    1. `Assets/Audio/` 하위 실제 파일명을 나열하고, RESEARCH.md 선별 가이드 기준으로 4개 용도별 클립을 1개씩 확정한다 (후보가 여럿이면 파일명 의미가 가장 부합하는 것 — 플레이테스트에서 교체 가능하므로 과도하게 고민하지 않는다, D-03):
       | 용도 | 기준 (D-05/D-06) | 후보 파일명 패턴 |
       |------|------------------|------------------|
       | _portalEnter | 상승 워프/텔레포트음, ≈0.5–0.8s | `*Up*`, `phaserUp*`, `powerUp*`, `forceField*`, `doorOpen*` |
       | _portalExit | 하강 마무리음, ≈0.8–1.2s | `*Down*`, `phaserDown*`, `doorClose*` |
       | _slash | 날카로운 즉각 어택, ≤0.3s | `laserSmall*`, `laserRetro*`, `laser*`, `zap*` |
       | _enemyDeathGlitch | 지글거리는 디지털 붕괴 노이즈, ≈0.3–0.6s | `spaceTrash*`, `computerNoise*`, 저음 재프 계열 |
    2. `Assets/Editor/AudioManagerPrefabBuilder.cs`를 아래 구조로 신규 생성한다. `// [선별]` 상수 4개에 1번에서 확정한 **실제 경로**를 기입한다:

    ```csharp
    using UnityEditor;
    using UnityEngine;

    /// <summary>
    /// Resources/AudioManager.prefab을 생성하고 선별된 클립 4개(D-03/D-05/D-06)를 연결한다.
    /// 실행: Tools > Audio > Build AudioManager Prefab. 재실행 시 기존 프리팹을 덮어쓴다(클립 교체 루프 지원).
    /// </summary>
    public static class AudioManagerPrefabBuilder
    {
        // [선별] D-06 진입 = 상승 워프음
        private const string PortalEnterPath = "Assets/Audio/<실제 선별 경로>.ogg";
        // [선별] D-06 퇴장 = 하강 마무리음
        private const string PortalExitPath = "Assets/Audio/<실제 선별 경로>.ogg";
        // [선별] D-05 대시 처치 = 날카로운 슬래시
        private const string SlashPath = "Assets/Audio/<실제 선별 경로>.ogg";
        // [선별] D-05 사망 = 글리치/디지털 노이즈
        private const string GlitchPath = "Assets/Audio/<실제 선별 경로>.ogg";

        private const string PrefabPath = "Assets/Resources/AudioManager.prefab";

        [MenuItem("Tools/Audio/Build AudioManager Prefab")]
        public static void Build()
        {
            var go = new GameObject("AudioManager");
            var mgr = go.AddComponent<AudioManager>();

            var so = new SerializedObject(mgr);
            AssignClip(so, "_portalEnter", PortalEnterPath);
            AssignClip(so, "_portalExit", PortalExitPath);
            AssignClip(so, "_slash", SlashPath);
            AssignClip(so, "_enemyDeathGlitch", GlitchPath);
            so.ApplyModifiedPropertiesWithoutUndo();

            if (!AssetDatabase.IsValidFolder("Assets/Resources"))
                AssetDatabase.CreateFolder("Assets", "Resources");
            PrefabUtility.SaveAsPrefabAsset(go, PrefabPath);
            Object.DestroyImmediate(go);
            AssetDatabase.SaveAssets();
            Debug.Log($"[AudioManagerPrefabBuilder] {PrefabPath} 생성 완료 — 클립 4개 연결");
        }

        private static void AssignClip(SerializedObject so, string field, string path)
        {
            var clip = AssetDatabase.LoadAssetAtPath<AudioClip>(path);
            if (clip == null) Debug.LogError($"[AudioManagerPrefabBuilder] 클립 없음: {path}");
            so.FindProperty(field).objectReferenceValue = clip;
        }
    }
    ```

    3. 선별 결과(4개 경로 + 선정 사유 1줄씩)를 SUMMARY에 기록할 수 있도록 커밋 메시지/작업 노트에 남긴다.

    주의: 이 태스크는 파일 작성까지만 — 메뉴 실행(프리팹 실제 생성)은 Unity 에디터가 필요하므로 13-04 체크포인트에서 수행한다.
  </action>
  <verify>
    <automated>grep -c "MenuItem(\"Tools/Audio/Build AudioManager Prefab\")" "Assets/Editor/AudioManagerPrefabBuilder.cs" && ! grep -n "<실제 선별 경로>" "Assets/Editor/AudioManagerPrefabBuilder.cs" && for f in $(grep -oE 'Assets/Audio/[^"]+' "Assets/Editor/AudioManagerPrefabBuilder.cs"); do ls "$f" > /dev/null || echo "MISSING $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/AudioManagerPrefabBuilder.cs가 존재하고 `[MenuItem("Tools/Audio/Build AudioManager Prefab")]`이 포함된다
    - 경로 상수 4개(PortalEnterPath/PortalExitPath/SlashPath/GlitchPath)가 전부 실존하는 Assets/Audio/ 하위 파일을 가리킨다 (플레이스홀더 `<실제 선별 경로>` 문자열이 남아있지 않음)
    - SerializedObject 프로퍼티 키가 "_portalEnter", "_portalExit", "_slash", "_enemyDeathGlitch" 4개와 정확히 일치한다
    - `PrefabUtility.SaveAsPrefabAsset(go, "Assets/Resources/AudioManager.prefab")` 저장 경로가 부트스트랩의 Resources.Load("AudioManager")와 일치한다
  </acceptance_criteria>
  <done>Unity 메뉴 1회 실행으로 클립 연결된 AudioManager.prefab이 생성 가능한 상태 — 클립 교체(D-03 플레이테스트 루프)는 상수 수정 + 재실행으로 반복 가능</done>
</task>

</tasks>

<verification>
- Assets/Audio/ 두 폴더에 오디오 파일 합계 90개 이상
- AudioManagerPrefabBuilder.cs의 경로 상수 4개가 전부 실존 파일
- 프리팹 실제 생성/청취 검증은 13-04 체크포인트에서 수행
</verification>

<success_criteria>
- D-01/D-02 이행: CC0 팩 2종 통째 임포트 완료
- D-03/D-04/D-05/D-06 이행: SF/디지털 톤 기준 클립 4종 선별 완료, 근거 기록
- 13-04에서 Tools 메뉴 실행만으로 프리팹 생성 가능
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-foundation-sound-polish/13-02-SUMMARY.md` (선별한 4개 클립 경로와 선정 사유 포함)
</output>
