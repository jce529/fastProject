---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 04
type: execute
wave: 3
depends_on: ["999.2-03"]
files_modified:
  - Assets/Editor/RoomRespawnTool.cs
  - Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_EdgeRun/Room_EdgeRun.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_LastStand/Room_LastStand.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_RiskCrossing/Room_RiskCrossing.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_Vertical_Gauntlet/Room_Vertical_Gauntlet.prefab
autonomous: false
requirements: [D-01, D-01a, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "Complex_Room 6종 프리팹 루트에 RoomClearCondition+RoomRespawnGate가 실제로 부착되어 디스크에 저장된다"
    - "클리어한 룸을 벗어났다가 체인 안에서 재진입하면 원래 마커 위치에 적이 다시 스폰VFX와 함께 등장한다 (D-01, D-01a, D-07, D-08)"
    - "클리어 전에 벗어났다가 재진입하면 리스폰이 트리거되지 않는다 (D-02)"
    - "체인 밖으로 밀려나 Destroy된 뒤 재생성된 룸은 리스폰이 아닌 최초 스폰으로 동작한다 (D-01a Case B 경계 확인)"
    - "Corridor와 보스 룸(아직 미구현)에는 리스폰이 적용되지 않는다 (D-04, D-06)"
    - "리스폰된 적을 처치하면 일반 처치보다 낮은 점수가 누적된다 (D-10)"
    - "10회 이상 반복 왕복해도 콘솔 에러나 프레임 저하 없이 무제한 리스폰이 동작한다 (D-09)"
  artifacts:
    - path: "Assets/Editor/RoomRespawnTool.cs"
      provides: "Complex_Room 6종에 RoomClearCondition+RoomRespawnGate를 멱등적으로 부착하는 에디터 도구"
      contains: "MenuItem(\"Fast/Phase999.2/Add Room Respawn Gates\")"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab"
      provides: "RoomClearCondition+RoomRespawnGate 부착 완료"
      contains: "RoomRespawnGate"
  key_links:
    - from: "Assets/Editor/RoomRespawnTool.cs"
      to: "Assets/Prefabs/Rooms/Complex_Room/*/*.prefab"
      via: "PrefabUtility.LoadPrefabContents + AddComponent<RoomClearCondition>/<RoomRespawnGate> + SaveAsPrefabAsset"
      pattern: "LoadPrefabContents"
---

<objective>
Unity 에디터에서 신규 `RoomRespawnTool`을 실행해 Complex_Room 6종 프리팹 루트에 `RoomClearCondition`+`RoomRespawnGate`를 실제로 부착하고, 999.2-01~03에서 작성한 코드 배선(구조적 리스폰 게이트 + 리스폰 지원 EnemySpawner + 감소 점수 + WorldGenerator 통합)이 실제 플레이에서 D-01~D-10(999.2-CONTEXT.md, D-01a 포함)을 만족하는지 플레이테스트로 검증한다.

Purpose: 이 Phase의 최종 통합·검수 게이트. 룸 재진입/스폰 VFX 타이밍/무제한 반복 안정성은 인간 시각 판정+프로파일러 관찰이 유일한 검증 수단(999.2-VALIDATION.md — 이 프로젝트에 자동화 테스트 프레임워크 없음, Phase 13/14와 동일한 manual-only 컨벤션).
Output: `RoomRespawnGate`/`RoomClearCondition`가 부착된 Complex_Room 프리팹 6종, D-01~D-10 플레이테스트 통과 기록.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-CONTEXT.md
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-VALIDATION.md
@Assets/Editor/RoomMarkerTool.cs
@Assets/Editor/CorridorEnemySpawnerTool.cs
@Assets/Scripts/World/WorldGenerator.cs

<interfaces>
Complex_Room 6종 프리팹 경로 (2026-07-10 확인, `Assets/Prefabs/Rooms/Complex_Room/{Name}/{Name}.prefab`):
- Room_AllInOne, Room_EdgeRun, Room_GaugeOutpost, Room_LastStand, Room_RiskCrossing, Room_Vertical_Gauntlet

`Assets/Editor/CorridorEnemySpawnerTool.cs` 참고 패턴 (멱등적 PrefabUtility 편집 — 그대로 모방):
```csharp
var root = PrefabUtility.LoadPrefabContents(path);
if (root.GetComponent<EnemySpawner>() == null)
    marker.gameObject.AddComponent<EnemySpawner>();
PrefabUtility.SaveAsPrefabAsset(root, path);
PrefabUtility.UnloadPrefabContents(root);
```

999.2-01 산출물 — `RoomRespawnGate`는 `[RequireComponent(typeof(RoomClearCondition))]`이므로 `RoomClearCondition`을 먼저(또는 함께) 부착해야 한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomRespawnTool 에디터 도구 신설 — Complex_Room 6종 리스폰 게이트 부착 도구 작성 (D-04, D-06)</name>
  <files>Assets/Editor/RoomRespawnTool.cs</files>
  <read_first>
    - Assets/Editor/CorridorEnemySpawnerTool.cs (멱등적 PrefabUtility 편집 패턴 — 그대로 모방)
    - Assets/Scripts/Room/RoomRespawnGate.cs (999.2-01 산출물 — RequireComponent 확인)
  </read_first>
  <action>
    신규 파일 `Assets/Editor/RoomRespawnTool.cs` 생성:

    ```csharp
    using UnityEditor;
    using UnityEngine;

    /// <summary>
    /// Menu: Fast/Phase999.2/Add Room Respawn Gates
    /// D-01/D-02/D-04/D-06(999.2): Complex_Room 6종 프리팹 루트에 RoomClearCondition+RoomRespawnGate를
    /// 멱등적으로 부착한다. Corridor/보스 룸은 리스폰 대상이 아니므로(D-04/D-06) 이 배열에 포함하지
    /// 않는다. RoomMarkerTool.cs/CorridorEnemySpawnerTool.cs와 동일한
    /// LoadPrefabContents/SaveAsPrefabAsset 패턴.
    /// </summary>
    public static class RoomRespawnTool
    {
        private static readonly string[] RoomNames =
        {
            "Room_AllInOne",
            "Room_EdgeRun",
            "Room_GaugeOutpost",
            "Room_LastStand",
            "Room_RiskCrossing",
            "Room_Vertical_Gauntlet",
        };

        [MenuItem("Fast/Phase999.2/Add Room Respawn Gates")]
        public static void AddRoomRespawnGates()
        {
            foreach (var roomName in RoomNames)
            {
                string path = $"Assets/Prefabs/Rooms/Complex_Room/{roomName}/{roomName}.prefab";

                var root = PrefabUtility.LoadPrefabContents(path);
                if (root == null)
                {
                    Debug.LogError($"[RoomRespawnTool] Prefab not found: {path}");
                    continue;
                }

                if (root.GetComponent<RoomClearCondition>() == null)
                    root.AddComponent<RoomClearCondition>();

                if (root.GetComponent<RoomRespawnGate>() == null)
                    root.AddComponent<RoomRespawnGate>();

                PrefabUtility.SaveAsPrefabAsset(root, path);
                PrefabUtility.UnloadPrefabContents(root);

                Debug.Log($"[RoomRespawnTool] RoomClearCondition+RoomRespawnGate applied to {roomName}");
            }

            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();

            Debug.Log("[RoomRespawnTool] Done. 6 Complex_Room prefabs updated with respawn gates.");
        }
    }
    ```

    이 태스크는 코드만 작성한다 — 메뉴 실행(Unity 에디터 조작)은 Task 2의 checkpoint:human-action에서 수행한다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase999.2/Add Room Respawn Gates\")" Assets/Editor/RoomRespawnTool.cs &amp;&amp; grep -c "Room_AllInOne\|Room_EdgeRun\|Room_GaugeOutpost\|Room_LastStand\|Room_RiskCrossing\|Room_Vertical_Gauntlet" Assets/Editor/RoomRespawnTool.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/RoomRespawnTool.cs`가 존재하고 `[MenuItem("Fast/Phase999.2/Add Room Respawn Gates")]`를 포함한다
    - `RoomNames` 배열에 정확히 6개 항목이 존재한다 (Complex_Room 전체, Corridor/보스 룸 없음 — D-04/D-06)
    - `root.GetComponent<RoomClearCondition>() == null`/`root.GetComponent<RoomRespawnGate>() == null` 멱등성 체크가 각각 존재한다 (재실행 안전)
    - 아직 Unity 메뉴가 실행되지 않았으므로 6개 프리팹 파일의 git diff는 이 태스크 시점에 0줄이다
  </acceptance_criteria>
  <done>Complex_Room 6종에 리스폰 게이트를 부착하는 멱등적 에디터 도구가 코드로 존재 — 실행은 Task 2에서 수행.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Unity 에디터 — 컴파일 확인 + Room Respawn Gate 부착 도구 실행</name>
  <files>Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab, Assets/Prefabs/Rooms/Complex_Room/Room_EdgeRun/Room_EdgeRun.prefab, Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab, Assets/Prefabs/Rooms/Complex_Room/Room_LastStand/Room_LastStand.prefab, Assets/Prefabs/Rooms/Complex_Room/Room_RiskCrossing/Room_RiskCrossing.prefab, Assets/Prefabs/Rooms/Complex_Room/Room_Vertical_Gauntlet/Room_Vertical_Gauntlet.prefab</files>
  <read_first>
    - Assets/Editor/RoomRespawnTool.cs (Task 1 산출물 — 실행할 메뉴 경로와 대상 프리팹 확인)
  </read_first>
  <action>
    Unity 에디터 내부 조작(메뉴 실행)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 컴파일 에러가 보고되면 999.2-01/02/03에서 작성한 코드의 문법/시그니처 불일치를 수정한 뒤 재확인을 요청한다.
  </action>
  <what-built>RoomClearCondition.IsCleared/ResetForRespawn + RoomRespawnGate(999.2-01), EnemySpawner.ResetForRespawn/Spawn 반환형 + RespawnedEnemyMarker + ScoreManager 감소점수(999.2-02), WorldGenerator MarkRoomLeft/TryRespawnRoom 배선(999.2-03), RoomRespawnTool 코드(이 Plan Task 1) — Unity 에디터 안에서의 마지막 조립 단계만 남음</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서 프로젝트를 열고 순서대로:
    1. **컴파일 확인**: Console에 컴파일 에러 0건 (경고는 허용)
    2. **Room Respawn Gate 부착**: 메뉴 Fast → Phase999.2 → Add Room Respawn Gates 실행 → Console에 "[RoomRespawnTool] Done. 6 Complex_Room prefabs updated with respawn gates." 로그 확인, 에러("Prefab not found") 0건 확인
    3. **프리팹 확인**: `Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab`을 선택해 Inspector에서 루트 GameObject에 RoomClearCondition + RoomRespawnGate 컴포넌트가 보이는지 확인 (나머지 5개 중 최소 1개 추가 샘플 확인)
    4. **재실행 안전성 확인**: 메뉴를 한 번 더 실행 — 중복 컴포넌트가 추가되지 않고 동일 로그가 다시 출력되는지 확인 (멱등성)
  </how-to-verify>
  <verify>
    <automated>grep -l "RoomRespawnGate" "Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab" "Assets/Prefabs/Rooms/Complex_Room/Room_EdgeRun/Room_EdgeRun.prefab" "Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab" "Assets/Prefabs/Rooms/Complex_Room/Room_LastStand/Room_LastStand.prefab" "Assets/Prefabs/Rooms/Complex_Room/Room_RiskCrossing/Room_RiskCrossing.prefab" "Assets/Prefabs/Rooms/Complex_Room/Room_Vertical_Gauntlet/Room_Vertical_Gauntlet.prefab"</automated>
  </verify>
  <acceptance_criteria>
    - Console 컴파일 에러 0건 (사용자 확인)
    - 6개 Complex_Room 프리팹 파일 모두에서 `grep -l "RoomRespawnGate"`가 매치를 반환한다 (파일 YAML에 컴포넌트 참조 존재)
    - 메뉴 재실행 시 경고/에러 없이 멱등적으로 동작한다 (사용자 확인)
  </acceptance_criteria>
  <done>Complex_Room 6종 프리팹에 RoomClearCondition+RoomRespawnGate가 실제로 부착되어 디스크에 저장됨 — D-01~D-10 코드 배선이 실제 콘텐츠로 완성.</done>
  <resume-signal>"approved" 입력 또는 문제 항목 설명 (예: "컴파일 에러 2건" 또는 "Room_LastStand에서 Prefab not found 에러 발생")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 플레이테스트 — D-01~D-10 (D-01a 포함) 전체 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-CONTEXT.md §Implementation Decisions (D-01~D-10, D-01a)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <action>
    룸 재진입/리스폰 타이밍/무제한 반복 안정성은 인간 시각 판정+프로파일러 관찰만 가능(999.2-RESEARCH.md Validation Architecture manual-only 정당화, 자동화 테스트 프레임워크 없음) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 원인 파일(WorldGenerator.cs 노드 전이 지점, RoomRespawnGate.cs 상태 전이, EnemySpawner.cs 리셋 순서 등)을 특정해 후속 수정 여부를 사용자와 논의한다.
  </action>
  <what-built>Case A 전용 룸 재진입 리스폰 파이프라인 — RoomClearCondition/RoomRespawnGate(클리어+이탈 감지) → WorldGenerator.MarkRoomLeft/TryRespawnRoom(체인-인덱스 전이 트리거) → EnemySpawner.ResetForRespawn+Spawn(안전한 재생성) → 기존 ActivateStaggered/EnemySpawnEffect(스폰 VFX 재사용) → RespawnedEnemyMarker+ScoreManager(감소 점수)</what-built>
  <how-to-verify>
    에디터 Play 후 체크리스트:

    **[D-01/D-01a/D-07 — 재진입 리스폰 기본 동작]**
    1. Complex_Room 하나에 진입해 배치된 적을 전부 처치(클리어)한다
    2. 오른쪽(또는 왼쪽) 인접 Room/Corridor로 이동해 방금 클리어한 Room에서 벗어난다 (체인에서 Destroy되지 않는 범위 내 — 1~2개 룸 정도만 이동)
    3. 다시 클리어했던 Room으로 돌아온다 → 원래 EnemySpawner 마커 위치에 동일 타입(근접/원거리)의 적이 포탈 VFX와 함께 다시 등장하는지 확인

    **[D-02 — 클리어 전 재진입은 리스폰 아님]**
    4. 새로운 Room에 진입해 적을 전멸시키지 않은 채(일부만 처치) 다시 뒤로 물러났다가 재진입 → 리스폰이 트리거되지 않고(이미 살아있는 나머지 적만 그대로) 기존 1회성 스폰 동작 그대로인지 확인

    **[D-03/D-09 — 쿨다운 없음 + 무제한 반복]**
    5. 3번에서 리스폰된 적을 다시 전멸시키고, 같은 Room을 2번 이상 반복해서 나갔다가 들어오기(총 3회 이상 왕복) → 매번 즉시 리스폰이 트리거되는지(쿨다운 대기 없음), 콘솔 에러나 프레임 저하가 없는지 확인

    **[D-01a — Case B 경계(체인 이탈 후 재생성)]**
    6. 클리어한 Room에서 충분히 멀리(3개 이상 Room/Corridor) 전진해 그 Room이 체인에서 Destroy되도록 만든 뒤, 다시 그 위치로 되돌아온다 → 새로 생성된 Room은 리스폰이 아닌 최초 스폰(HasActivated=false 상태)으로 동작하는지 확인 — 즉, 이전 Room과 동일한 프리팹이 아닐 수도 있고, 적이 리스폰 태그 없이 정상 스폰되는지 확인

    **[D-04/D-06 — 범위 제외 확인]**
    7. Corridor 구간에서는 적을 전멸시킨 뒤 나갔다가 들어와도 리스폰이 트리거되지 않는지 확인 (Corridor에는 RoomRespawnGate가 없음)

    **[D-05 — 적 타입 유지]**
    8. 근접형 마커 위치에서는 항상 근접형이, 원거리형 마커 위치에서는 항상 원거리형이 리스폰되는지 확인

    **[D-08 — 스폰 VFX 동일성]**
    9. 리스폰 시 포탈 성장+걸어나오기+마스크 수축 연출이 최초 스폰 때와 동일하게 재생되는지, 스폰 연출 중에는 감지/타겟팅되지 않는지 확인 (여러 마리면 스태거 순차 등장도 확인)

    **[D-10 — 감소 점수]**
    10. 리스폰된 적을 처치했을 때 점수 증가량이 일반 처치(100점)보다 적은지(30점) HUD 점수 표시로 확인 — 처치 직전/직후 점수를 비교

    **[일반 안정성]**
    11. 2분간 자유 플레이 + 반복 리스폰 유도 중 콘솔 에러 0건, 적이 벽에 끼거나 사라지는 등의 비정상 동작 없음
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 룸 재진입/리스폰 타이밍/무제한 반복 안정성은 시각 판정+프로파일러 관찰만 가능 (999.2-RESEARCH.md Validation Architecture 정당화, 이 프로젝트에 자동화 테스트 프레임워크 없음). 통과 기준: 사용자 리포트에서 체크리스트 1~11 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~11번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(어떤 Room, 어떤 적 타입, 재현 방법)이 함께 기록된다
  </acceptance_criteria>
  <done>D-01~D-10(D-01a 포함) 전부 플레이테스트로 통과 확인.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "6번: 체인 이탈 후 재생성된 Room에서도 리스폰 태그가 남아있음")</resume-signal>
</task>

</tasks>

<verification>
- 999.2-CONTEXT.md D-01~D-10(D-01a 포함) 전항목 플레이테스트 통과
- Complex_Room 6종 프리팹에 RoomClearCondition+RoomRespawnGate 존재 (grep 확인)
- Console 에러 0건 (2분 자유 플레이 + 3회 이상 반복 리스폰 기준)
</verification>

<success_criteria>
- 999.2-CONTEXT.md D-01~D-10(D-01a 스코프 명확화 포함)이 Room 재진입 시 실제 플레이로 검증됨
- 이 리스폰 파이프라인이 Case A(체인 내 재진입)로 명확히 한정되고, Case B(체인 이탈 후 재생성)에는 부작용 없이 최초 스폰으로 자연 회귀함이 확인됨
- Corridor/보스 룸(D-04/D-06)에는 영향이 없음이 확인됨
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-04-SUMMARY.md` (플레이테스트 체크리스트 결과 + 발견된 이슈 + 대응 여부 포함)
</output>
