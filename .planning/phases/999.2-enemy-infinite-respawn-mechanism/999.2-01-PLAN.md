---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Room/RoomClearCondition.cs
  - Assets/Scripts/Room/RoomRespawnGate.cs
autonomous: true
requirements: [D-01, D-01a, D-02, D-03]

must_haves:
  truths:
    - "RoomClearCondition.IsCleared는 감시 대상 적이 모두 죽은 뒤에만 true를 반환한다 (D-02)"
    - "RoomRespawnGate.MarkLeft()는 RoomClearCondition.IsCleared가 true일 때만 리스폰을 대기 상태로 표시한다 — 클리어 전 이탈은 대기 상태를 만들지 않는다 (D-02)"
    - "RoomRespawnGate.ConsumeRespawn() 호출 전까지는 별도 쿨다운 타이머 없이 재진입 즉시 리스폰이 트리거될 수 있는 상태가 유지된다 (D-03)"
    - "RoomRespawnGate는 GameObject마다 독립된 인스턴스 상태를 가지므로, 체인 이탈로 Destroy()된 뒤 재생성된 룸의 새 인스턴스는 항상 리스폰 미대기(false) 상태로 시작한다 (D-01a Case B 구조적 배제)"
    - "RoomClearCondition.ResetForRespawn() 호출 후에는 재클리어 판정이 다시 정상 동작한다 — 즉시 Update()가 재클리어를 감지할 수 있는 상태로 복귀한다 (D-02 재사용)"
  artifacts:
    - path: "Assets/Scripts/Room/RoomClearCondition.cs"
      provides: "IsCleared 읽기 전용 노출 + ResetForRespawn() 재무장 API"
      contains: "public bool IsCleared"
    - path: "Assets/Scripts/Room/RoomRespawnGate.cs"
      provides: "룸별 재진입 리스폰 게이트 (Case A 전용, Pitfall 2 신호 소비자)"
      contains: "public class RoomRespawnGate"
  key_links:
    - from: "Assets/Scripts/Room/RoomRespawnGate.cs"
      to: "Assets/Scripts/Room/RoomClearCondition.cs"
      via: "GetComponent<RoomClearCondition>() 후 IsCleared 읽기"
      pattern: "ClearCondition\\.IsCleared"
    - from: "Assets/Scripts/Room/RoomRespawnGate.cs"
      to: "Assets/Scripts/Room/RoomClearCondition.cs"
      via: "[RequireComponent] 강제 동반 부착"
      pattern: "RequireComponent\\(typeof\\(RoomClearCondition\\)\\)"
---

<objective>
Case A(체인 안에서 룸 인스턴스가 파괴되지 않고 살아있는 동안의 재진입) 전용 리스폰 게이트의 구조적 기반을 만든다. `RoomClearCondition`이 "완전 클리어" 여부를 외부에 읽기 전용으로 노출하고, 리스폰 직후 재클리어 판정을 재무장할 수 있게 하며, 새 `RoomRespawnGate` 컴포넌트가 "클리어된 상태에서 나갔다가 돌아옴"이라는 D-01 트리거 조건을 GameObject 인스턴스 수명에 종속된 상태로 추적한다.

이 컴포넌트는 룸 GameObject가 Destroy()되면 상태도 함께 사라지므로, WorldGenerator의 체인 이탈(`RemoveTail`/`RemoveHead`) 이후 재생성된 새 룸 인스턴스는 항상 리스폰 미대기 상태로 시작한다 — 999.2-CONTEXT.md D-01a가 요구하는 Case A 한정 스코프가 별도의 조건 분기 없이 구조적으로 성립한다.

Purpose: WorldGenerator(999.2-03)가 실제 트리거 신호(플레이어의 체인-노드 전환)를 이 게이트에 위임할 수 있는 안정된 계약을 먼저 확립한다.
Output: `RoomClearCondition.cs`(IsCleared/ResetForRespawn 추가), `RoomRespawnGate.cs`(신규).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-CONTEXT.md
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md
@Assets/Scripts/Room/RoomClearCondition.cs

<interfaces>
현재 RoomClearCondition.cs 전체 (2026-07-10 확인, 변경 대상) — 이 Phase 이전까지 어떤 프리팹에도
부착되지 않은 상태(999.2-RESEARCH.md Pattern 3). 실제 프리팹 부착은 999.2-04에서 수행한다.

```csharp
public class RoomClearCondition : MonoBehaviour
{
    [Header("감시할 적")]
    public GameObject[] enemies;

    [Header("전멸 시 활성화할 오브젝트")]
    public GameObject targetObject;

    private IEnemy[] _enemyCache;
    private bool _activated;

    private void Start()
    {
        if (targetObject != null)
            targetObject.SetActive(false);

        if (enemies != null && enemies.Length > 0)
        {
            _enemyCache = new IEnemy[enemies.Length];
            for (int i = 0; i < enemies.Length; i++)
            {
                if (enemies[i] != null)
                    _enemyCache[i] = enemies[i].GetComponent<IEnemy>();
            }
        }
        else
        {
            var found = new List<IEnemy>();
            foreach (MonoBehaviour mb in GetComponentsInChildren<MonoBehaviour>(true))
            {
                if (mb is IEnemy e)
                    found.Add(e);
            }
            _enemyCache = found.ToArray();
        }

        if (_enemyCache == null || _enemyCache.Length == 0)
            Activate();
    }

    private void Update()
    {
        if (_activated || _enemyCache == null || _enemyCache.Length == 0) return;
        foreach (var enemy in _enemyCache)
        {
            if (enemy != null && enemy.IsAlive) return;
        }
        Activate();
    }

    private void Activate()
    {
        _activated = true;
        if (targetObject != null)
            targetObject.SetActive(true);
    }
}
```

`IEnemy`는 `bool IsAlive { get; }` / `void OnDashHit()` / `void ClearHighlight()` 3-member 계약(잠금, 확장 금지) — 이 Plan은 `IEnemy`를 전혀 건드리지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomClearCondition에 IsCleared 노출 + ResetForRespawn() 추가 (D-02)</name>
  <files>Assets/Scripts/Room/RoomClearCondition.cs</files>
  <read_first>
    - Assets/Scripts/Room/RoomClearCondition.cs (현재 전체 — 위 interfaces 블록과 동일)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md 의 "Pattern 3: RoomClearCondition wiring" 절 (재무장 필요성 근거)
  </read_first>
  <action>
    `Assets/Scripts/Room/RoomClearCondition.cs` 전체를 아래로 교체한다:

    ```csharp
    using System.Collections.Generic;
    using UnityEngine;

    /// <summary>
    /// 지정한 적이 모두 죽으면 targetObject를 활성화한다.
    /// 씬 설정:
    ///   - enemies: 감시할 적 GameObject를 에디터에서 드래그 (IEnemy 구현체여야 함)
    ///   - targetObject: 전멸 시 나타낼 오브젝트 (문, 사다리, 출구 트리거 등)
    /// targetObject는 게임 시작 시 자동으로 비활성화된다.
    /// D-02/RoomRespawnGate(999.2): IsCleared로 "완전 클리어" 여부를 외부에 노출하고,
    /// ResetForRespawn()으로 리스폰 직후 재클리어 판정을 재활성화한다.
    /// </summary>
    public class RoomClearCondition : MonoBehaviour
    {
        [Header("감시할 적")]
        public GameObject[] enemies;

        [Header("전멸 시 활성화할 오브젝트")]
        public GameObject targetObject;

        private IEnemy[] _enemyCache;
        private bool _activated;

        /// <summary>D-02: RoomRespawnGate가 "완전히 클리어됐는지"를 확인하는 데 사용하는 읽기 전용 노출.</summary>
        public bool IsCleared => _activated;

        private void Start()
        {
            if (targetObject != null)
                targetObject.SetActive(false);

            DiscoverEnemies();

            // 적이 없으면 즉시 활성화
            if (_enemyCache == null || _enemyCache.Length == 0)
                Activate();
        }

        private void Update()
        {
            if (_activated || _enemyCache == null || _enemyCache.Length == 0) return;

            foreach (var enemy in _enemyCache)
            {
                if (enemy != null && enemy.IsAlive) return;
            }

            Activate();
        }

        private void Activate()
        {
            _activated = true;
            if (targetObject != null)
                targetObject.SetActive(true);
        }

        /// <summary>Start()의 적 탐색 로직을 분리 — ResetForRespawn()에서도 동일 로직을 재사용한다 (999.2 신규).</summary>
        private void DiscoverEnemies()
        {
            if (enemies != null && enemies.Length > 0)
            {
                // 정적 캐싱 (에디터에서 직접 연결한 경우)
                _enemyCache = new IEnemy[enemies.Length];
                for (int i = 0; i < enemies.Length; i++)
                {
                    if (enemies[i] != null)
                        _enemyCache[i] = enemies[i].GetComponent<IEnemy>();
                }
            }
            else
            {
                // 동적 탐색 (WorldGenerator/RoomRespawnGate가 스폰한 적 — includeInactive:true 필수)
                var found = new List<IEnemy>();
                foreach (MonoBehaviour mb in GetComponentsInChildren<MonoBehaviour>(true))
                {
                    if (mb is IEnemy e)
                        found.Add(e);
                }
                _enemyCache = found.ToArray();
            }
        }

        /// <summary>
        /// D-02/999.2-RESEARCH.md Pitfall/Pattern 3: RoomRespawnGate가 리스폰 스폰(EnemySpawner.Spawn()
        /// 전부 완료) 직후 호출한다. _activated 영구 래치를 재무장하고, 새로 생성된(비활성) 적 자식들을
        /// 다시 스캔해 재클리어 판정이 다시 동작하도록 한다 — 호출하지 않으면 리스폰 이후 재클리어가
        /// 영원히 감지되지 않는다.
        /// </summary>
        public void ResetForRespawn()
        {
            _activated = false;
            if (targetObject != null)
                targetObject.SetActive(false);
            DiscoverEnemies();
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "public bool IsCleared" Assets/Scripts/Room/RoomClearCondition.cs &amp;&amp; grep -n "public void ResetForRespawn" Assets/Scripts/Room/RoomClearCondition.cs &amp;&amp; grep -n "private void DiscoverEnemies" Assets/Scripts/Room/RoomClearCondition.cs</automated>
  </verify>
  <acceptance_criteria>
    - `RoomClearCondition.cs`에 `public bool IsCleared => _activated;`가 존재한다
    - `public void ResetForRespawn()`이 `_activated = false;`와 `DiscoverEnemies();` 호출을 포함한다
    - `Start()`와 `ResetForRespawn()` 양쪽 모두 `DiscoverEnemies()`를 호출한다 (로직 중복 없음)
    - 기존 `enemies`/`targetObject` public 필드와 `Update()`의 클리어 판정 로직은 변경되지 않는다 (surgical change)
  </acceptance_criteria>
  <done>RoomClearCondition이 외부에서 클리어 여부를 읽고(IsCleared) 리스폰 이후 재무장(ResetForRespawn)할 수 있는 API를 갖춘다.</done>
</task>

<task type="auto">
  <name>Task 2: RoomRespawnGate 신설 — 룸별 재진입 리스폰 게이트 (D-01, D-01a, D-03)</name>
  <files>Assets/Scripts/Room/RoomRespawnGate.cs</files>
  <read_first>
    - Assets/Scripts/Room/RoomClearCondition.cs (Task 1 완료 후 상태 — IsCleared/ResetForRespawn 시그니처)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md 의 "Pattern 1: Room-level re-entry gate" 및 "Pitfall 2" 절
  </read_first>
  <action>
    신규 파일 `Assets/Scripts/Room/RoomRespawnGate.cs` 생성:

    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-01/D-01a/D-02/D-03/D-04/D-09(999.2): Room 재진입 리스폰 게이트. Case A(체인 안에서 룸 인스턴스가
    /// Destroy되지 않고 살아있는 동안의 재진입) 전용 — GameObject 자체가 이 상태를 들고 있으므로,
    /// WorldGenerator.RemoveTail()/RemoveHead()로 Destroy()되면 이 컴포넌트와 _pendingRespawn 상태도
    /// 함께 사라져, Case B(완전 체인 이탈 후 재생성)는 구조적으로 respawn을 트리거하지 않는다(D-01a).
    /// WorldGenerator.UpdatePlayerIndex()가 계산하는 체인-인덱스 전이 신호(999.2-RESEARCH.md Pitfall 2)를
    /// 트리거로 사용한다 — 별도의 카메라 경계 트리거 콜라이더를 새로 만들지 않는다.
    /// D-04: Corridor/보스 룸 프리팹에는 이 컴포넌트를 부착하지 않는다(999.2-04에서 Complex_Room 6종에만
    /// 부착) — WorldGenerator가 GetComponent로 조회하므로 미부착 오브젝트에서는 자연히 no-op된다.
    /// </summary>
    [RequireComponent(typeof(RoomClearCondition))]
    public class RoomRespawnGate : MonoBehaviour
    {
        private bool _pendingRespawn;

        public RoomClearCondition ClearCondition { get; private set; }

        /// <summary>true면 "클리어된 상태에서 나갔다가 아직 재진입하지 않음" — WorldGenerator가 이 값을 보고
        /// 실제 리스폰 실행 여부를 결정한다.</summary>
        public bool IsPendingRespawn => _pendingRespawn;

        private void Awake()
        {
            ClearCondition = GetComponent<RoomClearCondition>();
        }

        /// <summary>D-01: 플레이어가 이 room 노드를 떠날 때 WorldGenerator가 호출한다.
        /// D-02: 클리어된 상태에서 떠났을 때만 리스폰을 대기시킨다 — 클리어 전 이탈은 기존 1회성 동작 유지.</summary>
        public void MarkLeft()
        {
            if (ClearCondition.IsCleared)
                _pendingRespawn = true;
        }

        /// <summary>D-03: WorldGenerator가 실제 리스폰을 실행하기 직전 호출해 게이트를 소비한다 — 별도
        /// 쿨다운 타이머 없이 "나갔다가 다시 들어옴" 자체가 게이트 역할을 한다. 다음 클리어+이탈 사이클에서
        /// MarkLeft()가 다시 이 값을 true로 세팅할 수 있으므로 D-09(무제한 리스폰)를 그대로 지원한다.</summary>
        public void ConsumeRespawn()
        {
            _pendingRespawn = false;
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "RequireComponent(typeof(RoomClearCondition))" Assets/Scripts/Room/RoomRespawnGate.cs &amp;&amp; grep -n "public void MarkLeft" Assets/Scripts/Room/RoomRespawnGate.cs &amp;&amp; grep -n "public void ConsumeRespawn" Assets/Scripts/Room/RoomRespawnGate.cs &amp;&amp; grep -n "public bool IsPendingRespawn" Assets/Scripts/Room/RoomRespawnGate.cs</automated>
  </verify>
  <acceptance_criteria>
    - `RoomRespawnGate.cs`가 존재하고 `[RequireComponent(typeof(RoomClearCondition))]` 속성을 갖는다
    - `MarkLeft()` 본문이 `ClearCondition.IsCleared`를 확인한 뒤에만 `_pendingRespawn = true`를 설정한다 (D-02 게이팅)
    - `ConsumeRespawn()`은 별도 타이머/코루틴 없이 `_pendingRespawn = false;` 한 줄로 구성된다 (D-03: 쿨다운 없음)
    - `IEnemy`, `EnemySpawner`, `WorldGenerator` 등 다른 기존 파일에 대한 참조/수정이 없다 (Task 2는 이 신규 파일만 생성)
  </acceptance_criteria>
  <done>RoomRespawnGate가 Case A 전용 재진입 리스폰 게이트로 완성 — Destroy 시 상태가 함께 사라지는 GameObject 수명 종속 설계로 D-01a Case B를 구조적으로 배제한다.</done>
</task>

</tasks>

<verification>
- `RoomClearCondition.cs`/`RoomRespawnGate.cs` 컴파일 성공 (Unity 컴파일은 999.2-04에서 최종 확인)
- `RoomRespawnGate`가 `RoomClearCondition` 없이는 부착될 수 없다 (`RequireComponent`)
- 이 Plan은 어떤 씬/프리팹 파일도 수정하지 않는다 (코드 계약만 정의 — 프리팹 부착은 999.2-04)
</verification>

<success_criteria>
- D-01/D-01a/D-02/D-03이 요구하는 "클리어 후 이탈 → 재진입 시 리스폰 대기 소비, 쿨다운 없음, Case A 한정" 구조가 코드 계약으로 확립됨
- 999.2-03(WorldGenerator 통합)이 `RoomRespawnGate.MarkLeft()`/`IsPendingRespawn`/`ConsumeRespawn()`/`ClearCondition.ResetForRespawn()`을 그대로 호출할 수 있는 안정된 API 확보
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-01-SUMMARY.md`
</output>
