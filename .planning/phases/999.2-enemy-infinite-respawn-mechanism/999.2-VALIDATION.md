---
phase: 999.2
slug: enemy-infinite-respawn-mechanism
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-10
---

# Phase 999.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — `com.unity.test-framework` 1.6.0 is installed but no EditMode/PlayMode test assembly or `[Test]`/`[UnityTest]` files exist anywhere in `Assets/`. |
| **Config file** | none |
| **Quick run command** | n/a — no automated harness |
| **Full suite command** | n/a |
| **Estimated runtime** | n/a |

This project has never introduced automated Unity tests. Phases 13 and 14 were both verified via manual playtest checklists (see STATE.md), which this phase continues rather than introducing test infra as an unplanned side quest (see CLAUDE.md scope discipline).

---

## Sampling Rate

- **After every task commit:** n/a — no automated quick-run exists
- **After every plan wave:** manual playtest checklist covering the decision table below
- **Before `/gsd:verify-work`:** full manual checklist pass, consistent with Phase 14 precedent
- **Max feedback latency:** n/a (manual-only phase)

---

## Per-Task Verification Map

No automated per-task commands exist for this phase (manual-only, see below). Task-level acceptance criteria (grep/file-existence checks on code structure) are defined per-task in PLAN.md files; end-to-end behavior is verified via the Manual-Only Verifications table below.

---

## Wave 0 Requirements

None — no test framework exists project-wide, and continuing the manual-playtest pattern is the correct, established approach rather than a gap to fill.

*Existing infrastructure covers all phase requirements (manual playtest, per project precedent).*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|--------------------|
| Respawn only after full clear + Case-A re-entry; no respawn on pre-clear re-entry | D-01/D-02/D-01a | Physics/coroutine/timing-driven Unity gameplay behavior; no automated Unity test infra exists project-wide | Clear a room, exit and re-enter while the room is still resident in the chain → enemies respawn. Enter, partially fight, retreat, re-enter → no respawn (pre-clear case keeps existing one-shot `HasActivated` semantics). |
| No respawn in Corridor or Boss Room | D-04/D-06 | Same as above | Clear a Corridor's enemies (if any), leave/return → no respawn. Boss Room not yet built (Phase 16) — verify no respawn hook is wired to boss markers once they exist. |
| Respawn appears at original `EnemySpawner` marker position | D-07 | Visual/positional confirmation | Visually confirm respawned enemy spawns at the same marker transform, not a random position. |
| Respawn VFX matches first-spawn VFX exactly | D-08 | Visual/timing confirmation | Side-by-side comparison of portal grow/walk-out/fade timing on first spawn vs respawn. |
| Unlimited respawn repetitions, no leak/perf degradation | D-09 | Requires Unity Profiler observation, not a scriptable assertion | 10+ rapid back-and-forth loops in one room while Unity Profiler is attached; watch GC alloc/instance count trend for growth. |
| Respawn kills award reduced score | D-10 | Runtime score readout comparison | Compare `ScoreManager.Score` delta on a first-kill vs a respawn-kill of the same enemy type. |
| Case A boundary — no respawn attempt when room was evicted from chain (Case B, out of scope) | D-01a | Requires exercising WorldGenerator chain eviction/regeneration in a live playtest | Walk far enough forward that a cleared room falls outside `_lookaheadCount`/`_lookbehindCount` (2) and gets destroyed, then walk back → confirm the newly-generated room behaves as first-entry (normal one-shot spawn), not respawn. |

---

## Validation Sign-Off

- [x] All tasks have manual-only verification mapped (no automated framework exists project-wide)
- [x] Sampling continuity: n/a — manual-only phase, no automated task chain to break
- [x] Wave 0 covers all MISSING references — none required
- [x] No watch-mode flags — n/a
- [x] Feedback latency < N/A — manual playtest only
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
