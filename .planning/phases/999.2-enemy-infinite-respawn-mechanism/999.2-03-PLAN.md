---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 03
type: execute
wave: 2
depends_on: ["999.2-01", "999.2-02"]
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements: [D-01, D-01a, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "플레이어가 클리어된 룸을 떠났다가 체인 안에서 다시 들어오면 원래 EnemySpawner 마커 위치에 동일 타입(근접/원거리)의 적이 다시 스폰된다 (D-01, D-01a, D-05, D-07)"
    - "클리어되기 전에 룸을 나갔다가 돌아와도 리스폰이 트리거되지 않는다 — 기존 HasActivated 1회성 스폰 동작이 그대로 유지된다 (D-02)"
    - "리스폰은 별도 쿨다운 타이머 없이 재진입 자체로 즉시 트리거된다 (D-03)"
    - "Corridor 노드에서는 RoomRespawnGate가 없어 MarkRoomLeft()/TryRespawnRoom() 호출이 안전하게 no-op된다 (D-04)"
    - "리스폰된 적은 기존 ActivateStaggered()/EnemySpawnEffect 파이프라인을 그대로 통해 스태거 간격을 두고 포탈 VFX와 함께 순차 등장한다 (D-08)"
    - "리스폰 횟수에 대한 카운터/제한이 코드 어디에도 존재하지 않는다 (D-09)"
  artifacts:
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "MarkRoomLeft()/TryRespawnRoom() + UpdatePlayerIndex() 배선"
      contains: "private void TryRespawnRoom"
  key_links:
    - from: "Assets/Scripts/World/WorldGenerator.cs"
      to: "Assets/Scripts/Room/RoomRespawnGate.cs"
      via: "GetComponent<RoomRespawnGate>() 후 MarkLeft()/IsPendingRespawn/ConsumeRespawn() 호출"
      pattern: "GetComponent<RoomRespawnGate>"
    - from: "Assets/Scripts/World/WorldGenerator.cs"
      to: "Assets/Scripts/World/EnemySpawner.cs"
      via: "ResetForRespawn() 후 Spawn() 재호출"
      pattern: "spawner\\.ResetForRespawn\\(\\)"
    - from: "Assets/Scripts/World/WorldGenerator.cs"
      to: "Assets/Scripts/World/WorldGenerator.cs (ActivateStaggered)"
      via: "기존 스태거+포탈VFX 코루틴 재사용 (신규 코루틴 작성 없음)"
      pattern: "StartCoroutine\\(ActivateStaggered"
---

<objective>
`WorldGenerator`가 매 프레임 계산하는 체인-인덱스 전이 신호(`UpdatePlayerIndex()`)에 룸 재진입 리스폰 트리거를 얹는다. 999.2-01의 `RoomRespawnGate`(어디서 상태를 들고 있을지)와 999.2-02의 `EnemySpawner.ResetForRespawn()`/`RespawnedEnemyMarker`(어떻게 다시 스폰하고 태깅할지)를 실제 게임플레이 이벤트에 연결하는 최종 통합 Plan이다.

새 트리거 콜라이더나 별도 룸 경계 감지 시스템을 만들지 않는다 — 999.2-RESEARCH.md Pitfall 2가 명시한 대로, `WorldGenerator.UpdatePlayerIndex()`가 이미 매 프레임 계산하는 forward/backward 노드 전환 지점(`_playerCurrentNode` 전진/후퇴)에 `MarkRoomLeft()`/`TryRespawnRoom()` 호출만 추가한다.

Purpose: D-01~D-10(999.2-CONTEXT.md)이 요구하는 "룸을 나갔다가 다시 들어오면 리스폰"을 실제 플레이 경로에서 동작하게 만든다.
Output: `WorldGenerator.cs`에 `MarkRoomLeft()`/`TryRespawnRoom()` 메서드 추가 + `UpdatePlayerIndex()` 배선.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-CONTEXT.md
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md
@Assets/Scripts/World/WorldGenerator.cs

<interfaces>
999.2-01 산출물 — `Assets/Scripts/Room/RoomRespawnGate.cs` (탐색 불필요, 아래 계약 그대로 사용):
```csharp
[RequireComponent(typeof(RoomClearCondition))]
public class RoomRespawnGate : MonoBehaviour
{
    public RoomClearCondition ClearCondition { get; private set; }
    public bool IsPendingRespawn => _pendingRespawn;
    public void MarkLeft();       // D-01/D-02: 클리어된 상태에서 이 room을 떠날 때 호출
    public void ConsumeRespawn(); // D-03: 실제 리스폰 실행 직전 게이트 소비
}
```

999.2-01 산출물 — `Assets/Scripts/Room/RoomClearCondition.cs` (탐색 불필요):
```csharp
public class RoomClearCondition : MonoBehaviour
{
    public bool IsCleared { get; } // _activated 읽기 전용 노출
    public void ResetForRespawn(); // 재클리어 판정 재활성화 (적 자식 재스캔 포함)
}
```

999.2-02 산출물 — `Assets/Scripts/World/EnemySpawner.cs` (탐색 불필요):
```csharp
public class EnemySpawner : MonoBehaviour
{
    public enum EnemyType { Melee, Ranged }
    public EnemyType Type { get; }
    public bool HasActivated { get; }
    public GameObject Spawn(GameObject meleePrefab, GameObject rangedPrefab); // 반환값 = 새로 생성된 인스턴스(또는 null)
    public void Activate(GameObject portalEffectPrefab = null);
    public void ResetForRespawn(); // HasActivated=false + _spawned=null (댕글링 참조 제거)
}
```

999.2-02 산출물 — `Assets/Scripts/Enemy/RespawnedEnemyMarker.cs` (탐색 불필요, 빈 마커):
```csharp
public class RespawnedEnemyMarker : MonoBehaviour { }
```

현재 `Assets/Scripts/World/WorldGenerator.cs`의 관련 부분 (2026-07-10 확인, 변경 대상):

이미 존재하는 필드(재사용, 신규 필드 추가 불필요):
```csharp
[SerializeField] private GameObject _meleeEnemyPrefab;
[SerializeField] private GameObject _rangedEnemyPrefab;
[SerializeField] private GameObject _portalEffectPrefab;
```

이미 존재하는 메서드(그대로 재사용, 신규 코루틴 작성 불필요):
```csharp
private void TryActivateSection(GameObject section)
{
    if (section == null || _activatedSections.Contains(section)) return;
    _activatedSections.Add(section);
    if (_pendingSpawns.TryGetValue(section, out var list))
    {
        _pendingSpawns.Remove(section);
        StartCoroutine(ActivateStaggered(list));
    }
}

private IEnumerator ActivateStaggered(List<EnemySpawner> spawners)
{
    foreach (var spawner in spawners)
    {
        if (spawner != null) spawner.Activate(_portalEffectPrefab);
        yield return new WaitForSecondsRealtime(_spawnStaggerInterval);
    }
}
```

`UpdatePlayerIndex()` 현재 전체 (배선 대상 — 정확히 이 형태를 아래 Task 2에서 교체한다):
```csharp
private void UpdatePlayerIndex()
{
    while (_playerCurrentNode.Next != null)
    {
        var nextEntryConnector = FindConnector(_playerCurrentNode.Next.Value.room, RoomConnector.Direction.Left);
        if (nextEntryConnector == null) break;
        if (_playerTransform.position.x > nextEntryConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Next;
            _playerCurrentIndex++;
            TryActivateSection(_playerCurrentNode.Value.room); // D-01: 새 Room 진입 시 활성화
        }
        else
            break;
    }

    while (_playerCurrentNode.Previous != null)
    {
        var prevExitConnector = FindConnector(_playerCurrentNode.Previous.Value.room, RoomConnector.Direction.Right);
        if (prevExitConnector == null) break;
        if (_playerTransform.position.x < prevExitConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Previous;
            _playerCurrentIndex--;
            TryActivateSection(_playerCurrentNode.Value.room); // D-01: 뒤로 이동해 Room 재진입 시에도 확인(이미 활성화됐으면 D-02로 스킵)
        }
        else
            break;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorldGenerator에 MarkRoomLeft()/TryRespawnRoom() 메서드 추가 (D-01, D-05, D-07, D-08, D-09)</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs 의 `TryActivateSection()`/`ActivateStaggered()`/`_meleeEnemyPrefab`/`_rangedEnemyPrefab`/`_portalEffectPrefab` 선언부 (위 interfaces 블록 참고)
    - Assets/Scripts/Room/RoomRespawnGate.cs (999.2-01 산출물)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md 의 "Pattern 1"/"Pitfall 2" 절
  </read_first>
  <action>
    `Assets/Scripts/World/WorldGenerator.cs`에서 `ActivateStaggered()` 메서드 바로 다음, `Update()` 메서드 바로 앞에 아래 두 private 메서드를 삽입한다:

    ```csharp
        /// <summary>
        /// D-01/999.2: 플레이어가 이 room 노드를 떠날 때 UpdatePlayerIndex()가 호출한다. Corridor에는
        /// RoomRespawnGate가 없으므로(D-04) GetComponent가 null을 반환해 안전하게 no-op된다.
        /// </summary>
        private void MarkRoomLeft(GameObject room)
        {
            var gate = room.GetComponent<RoomRespawnGate>();
            gate?.MarkLeft();
        }

        /// <summary>
        /// D-01/D-02/D-03/D-05/D-07/D-08/D-09(999.2): room 노드에 재진입했을 때 RoomRespawnGate가 대기 중인
        /// 리스폰이 있으면 실행한다. EnemySpawner.ResetForRespawn()으로 댕글링 참조를 비운 뒤 Spawn()을
        /// 재호출해 동일 마커 위치(D-07)에 동일 타입(D-05)으로 재생성하고, 기존 ActivateStaggered()
        /// 파이프라인을 그대로 재사용해 스폰 VFX(D-08)까지 동일하게 재생한다. 새로 생성된 인스턴스에는
        /// RespawnedEnemyMarker를 태깅해 D-10 감소 점수 판정에 사용한다. D-09: 별도 리스폰 횟수 제한을
        /// 두지 않는다 — 클리어+이탈+재진입 사이클을 몇 번이든 반복할 수 있다.
        /// </summary>
        private void TryRespawnRoom(GameObject room)
        {
            var gate = room.GetComponent<RoomRespawnGate>();
            if (gate == null || !gate.IsPendingRespawn) return;
            gate.ConsumeRespawn(); // D-03: 별도 쿨다운 없이 이번 재진입 1회로 게이트 소비

            var pending = new List<EnemySpawner>();
            foreach (EnemySpawner spawner in room.GetComponentsInChildren<EnemySpawner>(true))
            {
                spawner.ResetForRespawn();
                GameObject spawned = spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab); // D-05/D-07: 동일 마커 위치, 동일 타입
                if (spawned != null) spawned.AddComponent<RespawnedEnemyMarker>(); // D-10: 감소 점수 태깅
                pending.Add(spawner);
            }

            gate.ClearCondition.ResetForRespawn(); // 재클리어 판정 재활성화 (Pitfall 3)

            if (pending.Count > 0) StartCoroutine(ActivateStaggered(pending)); // D-08: 기존 스태거+포탈VFX 파이프라인 재사용
        }
    ```
  </action>
  <verify>
    <automated>grep -n "private void MarkRoomLeft" Assets/Scripts/World/WorldGenerator.cs &amp;&amp; grep -n "private void TryRespawnRoom" Assets/Scripts/World/WorldGenerator.cs &amp;&amp; grep -n "gate.ClearCondition.ResetForRespawn" Assets/Scripts/World/WorldGenerator.cs &amp;&amp; grep -n "AddComponent<RespawnedEnemyMarker>" Assets/Scripts/World/WorldGenerator.cs</automated>
  </verify>
  <acceptance_criteria>
    - `TryRespawnRoom()`은 `gate == null || !gate.IsPendingRespawn`일 때 즉시 `return`하는 가드로 시작한다
    - `gate.ConsumeRespawn()`이 스폰 루프보다 먼저 호출된다 (D-03: 게이트를 먼저 소비)
    - 루프 내부에서 `spawner.ResetForRespawn()`이 `spawner.Spawn(...)`보다 먼저 호출된다 (D-09 Pitfall 1 순서)
    - `gate.ClearCondition.ResetForRespawn()`이 스폰 루프가 끝난 뒤(모든 `Spawn()` 완료 후) 호출된다 — 새로 생성된 자식들이 `DiscoverEnemies()` 재스캔에 포함되도록
    - 리스폰 카운터/제한 변수(예: 최대 리스폰 횟수)가 추가되지 않는다 (D-09)
    - 신규 코루틴이 작성되지 않는다 — 기존 `ActivateStaggered(pending)`을 그대로 호출한다 (D-08)
  </acceptance_criteria>
  <done>WorldGenerator가 룸별 리스폰 실행 로직(MarkRoomLeft/TryRespawnRoom)을 갖추되, 아직 실제 트리거 지점(UpdatePlayerIndex)에는 연결되지 않은 상태.</done>
</task>

<task type="auto">
  <name>Task 2: UpdatePlayerIndex()에 MarkRoomLeft/TryRespawnRoom 배선 — 실제 트리거 연결 (D-01, D-01a, D-02, D-04)</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs 의 `UpdatePlayerIndex()` 전체 (Task 1 완료 후 상태, 위 interfaces 블록의 "현재" 버전과 동일)
  </read_first>
  <action>
    `UpdatePlayerIndex()` 메서드 전체를 아래로 교체한다 (forward while 루프와 backward while 루프 각각에 `MarkRoomLeft`/`TryRespawnRoom` 호출을 추가하는 것 외에는 기존 로직 변경 없음):

    ```csharp
        private void UpdatePlayerIndex()
        {
            // 플레이어 X > 다음 룸의 ENT X → 다음 룸에 실제로 들어간 시점에만 인덱스 진행.
            // (기존 룸의 EXIT 기준이면 복도 중간에서 되돌아가도 인덱스가 이미 넘어가 생성/삭제가
            // 먼저 발생함 — 생성(GEN-01 등)과 동일하게 "다음 룸 진입" 시점으로 통일)
            // LinkedList는 인덱스 임의접근이 없으므로 _playerCurrentNode를 인덱스와 함께 전진/후퇴시킨다.
            while (_playerCurrentNode.Next != null)
            {
                var nextEntryConnector = FindConnector(_playerCurrentNode.Next.Value.room, RoomConnector.Direction.Left);
                if (nextEntryConnector == null) break;
                if (_playerTransform.position.x > nextEntryConnector.transform.position.x)
                {
                    MarkRoomLeft(_playerCurrentNode.Value.room); // D-01(999.2): 현재 room을 떠남 — 클리어됐다면 리스폰 대기 등록
                    _playerCurrentNode = _playerCurrentNode.Next;
                    _playerCurrentIndex++;
                    TryActivateSection(_playerCurrentNode.Value.room); // D-01: 새 Room 진입 시 활성화
                    TryRespawnRoom(_playerCurrentNode.Value.room); // D-01/D-02(999.2): 재진입 리스폰 체크
                }
                else
                    break;
            }

            // 플레이어 X < 이전 룸의 EXIT X → 이전 룸에 실제로 들어간 시점에만 인덱스 후퇴 (좌우 대칭)
            while (_playerCurrentNode.Previous != null)
            {
                var prevExitConnector = FindConnector(_playerCurrentNode.Previous.Value.room, RoomConnector.Direction.Right);
                if (prevExitConnector == null) break;
                if (_playerTransform.position.x < prevExitConnector.transform.position.x)
                {
                    MarkRoomLeft(_playerCurrentNode.Value.room); // D-01(999.2): 현재 room을 떠남 — 좌우 대칭
                    _playerCurrentNode = _playerCurrentNode.Previous;
                    _playerCurrentIndex--;
                    TryActivateSection(_playerCurrentNode.Value.room); // D-01: 뒤로 이동해 Room 재진입 시에도 확인(이미 활성화됐으면 D-02로 스킵)
                    TryRespawnRoom(_playerCurrentNode.Value.room); // D-01/D-02(999.2): 재진입 리스폰 체크
                }
                else
                    break;
            }
        }
    ```

    이 배선만으로 D-01a(Case A 한정)가 구조적으로 성립하는 이유: `MarkRoomLeft`/`TryRespawnRoom`은 항상 `_chain`에 현재 살아있는 `LinkedListNode`의 `.room`(즉 아직 `Destroy()`되지 않은 GameObject)에 대해서만 호출된다. `RemoveTail()`/`RemoveHead()`가 room을 `Destroy()`하면 그 GameObject와 부착된 `RoomRespawnGate` 인스턴스 자체가 사라지므로, 이후 같은 위치에 새로 생성되는 room은 항상 새 `RoomRespawnGate`(초기 상태 `_pendingRespawn=false`)를 갖는다 — Case B를 감지하거나 배제하는 별도 조건문이 필요 없다.
  </action>
  <verify>
    <automated>grep -n "MarkRoomLeft(_playerCurrentNode.Value.room)" Assets/Scripts/World/WorldGenerator.cs | wc -l && grep -n "TryRespawnRoom(_playerCurrentNode.Value.room)" Assets/Scripts/World/WorldGenerator.cs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `MarkRoomLeft(_playerCurrentNode.Value.room)` 호출이 정확히 2곳(forward/backward 루프 각각)에 존재하며, 둘 다 `_playerCurrentNode = ...Next/Previous;` 대입보다 앞줄이다
    - `TryRespawnRoom(_playerCurrentNode.Value.room)` 호출이 정확히 2곳에 존재하며, 둘 다 해당 루프의 `TryActivateSection(_playerCurrentNode.Value.room)` 호출보다 뒷줄이다
    - `_playerCurrentIndex`/`_playerCurrentNode` 갱신 로직, `FindConnector` 호출, `if/else break` 분기 구조는 기존과 100% 동일하다 (surgical change — 두 함수 호출 추가 외 다른 diff 없음)
  </acceptance_criteria>
  <done>플레이어가 클리어된 룸을 벗어났다가 체인 안에서 다시 진입하면 TryRespawnRoom()이 실제로 트리거되는 완전한 통합이 완성된다.</done>
</task>

</tasks>

<verification>
- `WorldGenerator.cs` 컴파일 성공 (Unity 컴파일은 999.2-04에서 최종 확인 — Complex_Room 프리팹에 RoomRespawnGate가 아직 부착되지 않았으므로 이 시점에는 GetComponent가 항상 null을 반환해 안전하게 no-op)
- Corridor 노드 통과 시 `GetComponent<RoomRespawnGate>()`가 항상 null → D-04(Corridor 제외)가 자동으로 성립
- `SpawnNextPair()`/`SpawnPrevPair()`/`RemoveTail()`/`RemoveHead()`/`FloorTransitionSequence()` 등 기존 체인 생성/정리/층전환 로직은 이 Plan에서 전혀 수정되지 않는다
</verification>

<success_criteria>
- D-01/D-01a/D-02/D-03/D-04/D-05/D-07/D-08/D-09가 실제 WorldGenerator 트리거 경로에 완전히 배선됨
- 999.2-04에서 Complex_Room 6종 프리팹에 RoomRespawnGate를 부착하기만 하면 즉시 실제 플레이로 검증 가능한 상태
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-03-SUMMARY.md`
</output>
