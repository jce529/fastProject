---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 01
subsystem: gameplay-room
tags: [unity, monobehaviour, room-lifecycle, respawn-gate]

# Dependency graph
requires: []
provides:
  - "RoomClearCondition.IsCleared read-only exposure of the completion latch"
  - "RoomClearCondition.ResetForRespawn() to re-arm clear detection after a respawn spawn"
  - "RoomRespawnGate component: Case A re-entry respawn gate (MarkLeft/IsPendingRespawn/ConsumeRespawn)"
affects: [999.2-02, 999.2-03, 999.2-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "GameObject-lifetime-bound gate state: RoomRespawnGate holds _pendingRespawn as an instance field with no persistence, so Destroy() of the room GameObject structurally clears the flag (Case B exclusion by construction, no branching)"
    - "RequireComponent contract wiring: RoomRespawnGate requires RoomClearCondition and caches it via GetComponent in Awake(), giving downstream callers (WorldGenerator) a single ClearCondition accessor instead of a second GetComponent call"

key-files:
  created:
    - Assets/Scripts/Room/RoomRespawnGate.cs
  modified:
    - Assets/Scripts/Room/RoomClearCondition.cs

key-decisions:
  - "DiscoverEnemies() extracted from Start() so ResetForRespawn() can reuse identical enemy-cache rebuild logic without duplicating the static/dynamic discovery branch (surgical change, no behavior change to Start()/Update())"
  - "ConsumeRespawn() implemented as a single-line flag clear with no cooldown timer/coroutine, per D-03 — enables unlimited repeat respawns (D-09) driven purely by leave/re-enter transitions"

patterns-established:
  - "Pattern: gate components expose IsPendingRespawn as read-only bool + explicit Mark/Consume verbs, keeping all respawn timing decisions in the caller (WorldGenerator, 999.2-03) rather than inside the gate itself"

requirements-completed: [D-01, D-01a, D-02, D-03]

# Metrics
duration: ~5min
completed: 2026-07-14
---

# Phase 999.2 Plan 01: Room Respawn Gate Foundation Summary

**RoomClearCondition gains IsCleared/ResetForRespawn API; new RoomRespawnGate component tracks "cleared then left, not yet re-entered" state bound to GameObject lifetime, structurally excluding Case B (chain-eviction Destroy) from re-triggering respawn.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-07-14T11:44:00+09:00 (approx, first task commit 11:44:55)
- **Completed:** 2026-07-14T11:45:23+09:00
- **Tasks:** 2
- **Files modified:** 2 (1 modified, 1 created)

## Accomplishments
- `RoomClearCondition.IsCleared` read-only accessor exposes the internal `_activated` latch to external callers without breaking encapsulation
- `RoomClearCondition.ResetForRespawn()` re-arms the clear latch and re-scans children for newly spawned (inactive) enemies, reusing the same `DiscoverEnemies()` logic as `Start()`
- New `RoomRespawnGate` component (`[RequireComponent(typeof(RoomClearCondition))]`) establishes the Case A re-entry gate contract: `MarkLeft()` gated on `IsCleared`, `IsPendingRespawn` read-only flag, `ConsumeRespawn()` with no cooldown timer

## Task Commits

Each task was committed atomically:

1. **Task 1: RoomClearCondition IsCleared + ResetForRespawn (D-02)** - `5c1e11e` (feat)
2. **Task 2: RoomRespawnGate reentry respawn gate (D-01, D-01a, D-03)** - `09f38e8` (feat)

**Plan metadata:** (this commit, to follow)

## Files Created/Modified
- `Assets/Scripts/Room/RoomClearCondition.cs` - Added `IsCleared` property, extracted `DiscoverEnemies()`, added `ResetForRespawn()`
- `Assets/Scripts/Room/RoomRespawnGate.cs` - New component: Case A room re-entry respawn gate

## Decisions Made
- Extracted `DiscoverEnemies()` as a private helper reused by both `Start()` and `ResetForRespawn()` to avoid logic duplication (plan-mandated, verified via acceptance criteria)
- `ConsumeRespawn()` intentionally has no cooldown/timer mechanism — "leave and re-enter" itself is the gate, matching D-03 exactly as specified in the plan

## Deviations from Plan

None - plan executed exactly as written. Both files match the plan's `<action>` code blocks verbatim, and all `<verify>` grep checks and `<acceptance_criteria>` passed.

## Issues Encountered

None. No scene/prefab files were touched (plan scope was code-contract only, per `<verification>`). No `.meta` file was generated for `RoomRespawnGate.cs` since Unity Editor was not running during this session — this is expected and will auto-generate on next Editor open, consistent with prior plans in this repo that add new scripts outside the Editor.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 999.2-03 (WorldGenerator integration) can now call `RoomRespawnGate.MarkLeft()` / `IsPendingRespawn` / `ConsumeRespawn()` and `RoomClearCondition.ResetForRespawn()` against a stable, tested API contract
- 999.2-04 (prefab attachment) can attach `RoomRespawnGate` to the Complex_Room 6 prefabs — `RequireComponent(RoomClearCondition)` will auto-add the dependency if missing
- No blockers. Actual prefab wiring and Unity compilation verification deferred to 999.2-04 as planned.

---
*Phase: 999.2-enemy-infinite-respawn-mechanism*
*Completed: 2026-07-14*

## Self-Check: PASSED

- FOUND: Assets/Scripts/Room/RoomClearCondition.cs
- FOUND: Assets/Scripts/Room/RoomRespawnGate.cs
- FOUND commit: 5c1e11e
- FOUND commit: 09f38e8
