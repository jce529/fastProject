---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/EnemySpawner.cs
  - Assets/Scripts/Enemy/RespawnedEnemyMarker.cs
  - Assets/Scripts/World/ScoreManager.cs
  - Assets/Scripts/Player/CombatController.cs
autonomous: true
requirements: [D-05, D-07, D-09, D-10]

must_haves:
  truths:
    - "EnemySpawner.Spawn()은 새로 생성한 GameObject를 반환하며, 반환값을 무시하는 기존 호출자(DebugRoomTeleporter, WorldGenerator.TrySpawnEnemies)는 수정 없이 그대로 컴파일된다"
    - "EnemySpawner.ResetForRespawn() 호출 후 Spawn()을 다시 호출하면 HasActivated/_spawned가 모두 초기화되어 Activate()가 다시 정상 실행된다 — 댕글링 참조로 인한 영구 no-op이 발생하지 않는다 (D-09, Pitfall 1)"
    - "리스폰된 적을 처치하면 ScoreManager.Score가 KillScore(100)가 아닌 RespawnKillScore(30)만큼만 증가한다 (D-10)"
    - "리스폰이 아닌 일반(첫 스폰) 적을 처치하면 기존과 동일하게 KillScore(100)만큼 증가한다 — 회귀 없음"
  artifacts:
    - path: "Assets/Scripts/World/EnemySpawner.cs"
      provides: "ResetForRespawn() + Spawn()의 GameObject 반환"
      contains: "public void ResetForRespawn"
    - path: "Assets/Scripts/Enemy/RespawnedEnemyMarker.cs"
      provides: "리스폰 개체 식별용 additive 마커 컴포넌트 (IEnemy 미확장)"
      contains: "public class RespawnedEnemyMarker"
    - path: "Assets/Scripts/World/ScoreManager.cs"
      provides: "RespawnKillScore 상수 + AddKillScore(bool) 오버로드"
      contains: "RespawnKillScore"
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "처치 순간 RespawnedEnemyMarker 판정 후 감소 점수 호출"
      contains: "RespawnedEnemyMarker"
  key_links:
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/Enemy/RespawnedEnemyMarker.cs"
      via: "GetComponent<RespawnedEnemyMarker>() 판정"
      pattern: "GetComponent<RespawnedEnemyMarker>"
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/World/ScoreManager.cs"
      via: "AddKillScore(bool isRespawn) 호출"
      pattern: "AddKillScore\\(isRespawnKill\\)"
---

<objective>
리스폰 사이클에서 `EnemySpawner`가 안전하게 재사용될 수 있도록 댕글링 참조 문제(999.2-RESEARCH.md Pitfall 1)를 해소하고, 리스폰된 적을 처치했을 때 감소된 점수(D-10)를 지급하는 경제 시스템을 완성한다. `IEnemy`(3-member 계약, 잠금)를 확장하지 않고, 프로젝트 자체 선례인 `ISpawnGatable` additive-interface 패턴과 동일한 방식(빈 마커 컴포넌트)으로 "이 적은 리스폰된 개체다"를 태깅한다.

Purpose: 999.2-03(WorldGenerator 통합)이 리스폰 시 `EnemySpawner.ResetForRespawn()`→`Spawn()`을 안전하게 재호출하고, 새로 생성된 인스턴스에 `RespawnedEnemyMarker`를 부착해 점수 차등을 자동으로 만들 수 있게 한다.
Output: `EnemySpawner.cs`(리스폰 지원 추가), `RespawnedEnemyMarker.cs`(신규), `ScoreManager.cs`(감소 점수 API), `CombatController.cs`(판정 배선).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-CONTEXT.md
@.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md
@Assets/Scripts/World/EnemySpawner.cs
@Assets/Scripts/World/ScoreManager.cs
@Assets/Scripts/Player/CombatController.cs

<interfaces>
현재 `Assets/Scripts/World/EnemySpawner.cs` 전체 (변경 대상):
```csharp
public class EnemySpawner : MonoBehaviour
{
    public enum EnemyType { Melee, Ranged }
    [SerializeField] private EnemyType _type = EnemyType.Melee;
    public EnemyType Type => _type;
    private GameObject _spawned;
    public bool HasActivated { get; private set; }

    public void Spawn(GameObject meleePrefab, GameObject rangedPrefab)
    {
        GameObject prefab = _type == EnemyType.Melee ? meleePrefab : rangedPrefab;
        if (prefab == null) return;
        _spawned = Instantiate(prefab, transform.position, Quaternion.identity, transform);
        _spawned.SetActive(false);
    }

    public void Activate(GameObject portalEffectPrefab = null)
    {
        if (HasActivated || _spawned == null) return;
        HasActivated = true;
        var gate = _spawned.GetComponent<ISpawnGatable>();
        gate?.SetSpawnGate(true);
        _spawned.SetActive(true);
        var effect = _spawned.GetComponent<EnemySpawnEffect>();
        if (effect == null) effect = _spawned.AddComponent<EnemySpawnEffect>();
        effect.StartCoroutine(effect.PlaySpawnSequence(portalEffectPrefab, gate));
    }
}
```

기존 `Spawn()` 호출자들 — 반환값을 캡처하지 않으므로 시그니처를 `void`→`GameObject`로 바꿔도 컴파일에 영향 없음 (surgical, files_modified에 포함하지 않음):
- `Assets/Scripts/World/WorldGenerator.cs` §`TrySpawnEnemies()`: `spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);`
- `Assets/Scripts/World/DebugRoomTeleporter.cs` §`TeleportToRoom()`: `spawner.Spawn(_meleePrefab, _rangedPrefab);`

현재 `Assets/Scripts/World/ScoreManager.cs`의 관련 부분:
```csharp
public const int   KillScore        = 100;
...
public static void AddKillScore()
{
    Score += KillScore;
}
```
호출부는 단일 지점 — `Assets/Scripts/Player/CombatController.cs` §`ExecuteDash()`:
```csharp
target.OnDashHit();
AudioManager.PlaySfx(Sfx.Slash);
SpawnHitSpark(destination);
_cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
ScoreManager.AddKillScore();
yield return StartCoroutine(HitFreeze(hitFreezeDuration));
```
`target`은 `IEnemy` 타입이지만, 같은 메서드 내 다른 곳에서 이미 `((MonoBehaviour)target).transform.position`처럼 `MonoBehaviour`로 캐스팅해 사용하는 기존 패턴이 있다 — 동일 캐스팅 패턴을 재사용한다.

`ISpawnGatable.cs` (Phase 14, IEnemy 미확장 additive 패턴의 기존 선례):
```csharp
public interface ISpawnGatable
{
    void SetSpawnGate(bool isSpawning);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EnemySpawner에 ResetForRespawn() 추가 + Spawn() GameObject 반환 (D-09 Pitfall 1)</name>
  <files>Assets/Scripts/World/EnemySpawner.cs</files>
  <read_first>
    - Assets/Scripts/World/EnemySpawner.cs (현재 전체 — 위 interfaces 블록과 동일)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md 의 "Pitfall 1: Dangling `_spawned` reference after death" 절
  </read_first>
  <action>
    `Assets/Scripts/World/EnemySpawner.cs` 전체를 아래로 교체한다:

    ```csharp
    using UnityEngine;

    /// <summary>
    /// Room/Corridor 프리팹의 각 스폰 포인트에 부착하는 마커 컴포넌트.
    /// D-01/D-02: Spawn()은 사전 생성(비활성 인스턴스) 전용 — 화면 밖 미리 생성 시점에 호출된다.
    /// Activate()는 플레이어가 실제로 이 구간에 도달했을 때만 호출되며, 스폰 VFX 트리거의 유일한
    /// 지점이다(STATE.md 제약 — Awake/OnEnable에서 트리거 금지). HasActivated로 1회성 스폰을 보장한다.
    /// 룸이 Destroy()될 때 자식인 적도 함께 제거된다.
    /// D-01/D-09(999.2): ResetForRespawn()으로 HasActivated 래치를 재무장하고 _spawned 댕글링 참조를
    /// 명시적으로 비운다 — 999.2-RESEARCH.md Pitfall 1(재무장만 하고 _spawned를 안 비우면 Activate()가
    /// 영구 no-op됨) 방지.
    /// </summary>
    public class EnemySpawner : MonoBehaviour
    {
        public enum EnemyType { Melee, Ranged }

        [SerializeField] private EnemyType _type = EnemyType.Melee;
        public EnemyType Type => _type;

        private GameObject _spawned;

        /// <summary>D-02: 이미 활성화된 마커는 재호출해도 안전한 no-op — 1회성 스폰.</summary>
        public bool HasActivated { get; private set; }

        /// <summary>D-01(999.2): 리스폰 직후 새로 Instantiate된 인스턴스를 상위 호출자(WorldGenerator)가
        /// 필요 시 태깅(RespawnedEnemyMarker)할 수 있도록 반환한다. 기존 호출자(DebugRoomTeleporter,
        /// WorldGenerator.TrySpawnEnemies)는 반환값을 무시해도 그대로 컴파일된다.</summary>
        public GameObject Spawn(GameObject meleePrefab, GameObject rangedPrefab)
        {
            GameObject prefab = _type == EnemyType.Melee ? meleePrefab : rangedPrefab;
            if (prefab == null) return null;

            _spawned = Instantiate(prefab, transform.position, Quaternion.identity, transform);
            _spawned.SetActive(false);
            return _spawned;
        }

        /// <summary>
        /// D-01: 플레이어가 실제로 이 구간에 진입했을 때만 호출한다. portalEffectPrefab이 null이면
        /// 포탈 VFX 없이 걸어나오기+게이팅만 재생한다(DebugRoomTeleporter 등 호환).
        /// SPWN-02: SetActive(true) 이전에 스폰 게이트를 닫아, 같은 프레임 다른 스크립트의 Update()가
        /// 이 적을 감지/타겟팅하지 못하도록 한다 (Pitfall 1).
        /// </summary>
        public void Activate(GameObject portalEffectPrefab = null)
        {
            if (HasActivated || _spawned == null) return;
            HasActivated = true;

            var gate = _spawned.GetComponent<ISpawnGatable>();
            gate?.SetSpawnGate(true);

            _spawned.SetActive(true);

            var effect = _spawned.GetComponent<EnemySpawnEffect>();
            if (effect == null) effect = _spawned.AddComponent<EnemySpawnEffect>();
            effect.StartCoroutine(effect.PlaySpawnSequence(portalEffectPrefab, gate));
        }

        /// <summary>
        /// D-01/D-09(999.2): RoomRespawnGate/WorldGenerator가 재진입 리스폰을 트리거할 때, Spawn()을
        /// 다시 호출하기 직전에 호출한다. HasActivated만 리셋하고 _spawned를 그대로 두면 이전에
        /// Destroy()된 적 인스턴스를 가리키는 댕글링 참조가 남아 Activate()의 `_spawned == null` 가드가
        /// 예측 불가능하게 동작한다(999.2-RESEARCH.md Pitfall 1) — 반드시 _spawned도 함께 null로 비운다.
        /// </summary>
        public void ResetForRespawn()
        {
            HasActivated = false;
            _spawned = null;
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "public GameObject Spawn" Assets/Scripts/World/EnemySpawner.cs &amp;&amp; grep -n "public void ResetForRespawn" Assets/Scripts/World/EnemySpawner.cs &amp;&amp; grep -n "_spawned = null;" Assets/Scripts/World/EnemySpawner.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Spawn()`의 반환 타입이 `GameObject`이고, `null` 프리팹일 때 `null`을 반환하며 성공 시 `_spawned`를 반환한다
    - `ResetForRespawn()` 본문이 정확히 `HasActivated = false;`와 `_spawned = null;` 두 줄로 구성된다 (댕글링 참조 방지)
    - `Assets/Scripts/World/WorldGenerator.cs`와 `Assets/Scripts/World/DebugRoomTeleporter.cs`의 git diff가 0줄이다 (반환값 무시 호환)
    - `Activate()`의 기존 로직(HasActivated 가드, ISpawnGatable 배선, EnemySpawnEffect AddComponent)은 변경되지 않는다
  </acceptance_criteria>
  <done>EnemySpawner가 댕글링 참조 없이 안전하게 리스폰될 수 있는 ResetForRespawn() API와, 리스폰 태깅에 필요한 Spawn()의 GameObject 반환을 갖춘다.</done>
</task>

<task type="auto">
  <name>Task 2: RespawnedEnemyMarker 신설 + ScoreManager 감소 점수 API (D-10)</name>
  <files>Assets/Scripts/Enemy/RespawnedEnemyMarker.cs, Assets/Scripts/World/ScoreManager.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/ISpawnGatable.cs (IEnemy 미확장 additive 패턴의 기존 선례)
    - Assets/Scripts/World/ScoreManager.cs (현재 전체 — 상수 스타일 확인)
    - .planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-RESEARCH.md 의 "Pattern 2: Reduced-score respawn kill" 절
  </read_first>
  <action>
    1. 신규 파일 `Assets/Scripts/Enemy/RespawnedEnemyMarker.cs` 생성:

    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-10(999.2): 리스폰된 적 인스턴스에만 부착되는 빈 마커 컴포넌트 — IEnemy(3-member 계약, 잠금됨)를
    /// 확장하지 않고 "이 적은 리스폰된 개체다"를 태깅하기 위한 additive 컴포넌트. ISpawnGatable(Phase 14)이
    /// 같은 문제(IEnemy 미확장)를 해결한 방식과 동일한 프로젝트 컨벤션을 따른다.
    /// WorldGenerator가 리스폰 경로에서만 AddComponent하고, CombatController.ExecuteDash()가
    /// GetComponent&lt;RespawnedEnemyMarker&gt;()로 감소 점수 여부를 판정한다. 적 GameObject가 죽으면
    /// (EnemyDeathEffect.PlayDeathSequence()의 Destroy(gameObject)) 이 마커도 함께 사라진다 — 별도 정리 불필요.
    /// </summary>
    public class RespawnedEnemyMarker : MonoBehaviour
    {
    }
    ```

    2. `Assets/Scripts/World/ScoreManager.cs`에서 상수 선언부와 `AddKillScore()`를 아래로 교체한다 (다른 부분은 변경하지 않음):

    ```csharp
    public const int   KillScore        = 100;
    public const int   RespawnKillScore = 30;   // D-10(999.2): 리스폰 킬 감소 점수 — KillScore의 약 30%. 파밍을 점수 관점에서 비효율적으로 만들되 완전히 막지는 않는다(D-09: 무제한 리스폰과 일관).
    public const int   FastClearBonus   = 300;
    public const int   NormalClearBonus = 150;
    public const int   SlowClearBonus   = 50;
    public const float FastClearTime    = 10f; // 초 이내 클리어 시 FastClearBonus
    public const float NormalClearTime  = 25f; // 초 이내 클리어 시 NormalClearBonus
    public const int   TimeBonusPerSecond = 10; // D-02(Phase 11): 남은 초 × 10점
    ```

    그리고 `AddKillScore()` 메서드를:
    ```csharp
    /// <summary>CombatController.ExecuteDash() — target.OnDashHit() 직후 호출.
    /// D-10(999.2): isRespawn=true면 RespawnKillScore(감소된 점수)를, 기본값 false면 기존 KillScore를 더한다.
    /// 기본 매개변수로 인자 없는 기존 호출 형태(AddKillScore())도 그대로 컴파일된다.</summary>
    public static void AddKillScore(bool isRespawn = false)
    {
        Score += isRespawn ? RespawnKillScore : KillScore;
    }
    ```
    로 교체한다.
  </action>
  <verify>
    <automated>grep -n "public class RespawnedEnemyMarker" Assets/Scripts/Enemy/RespawnedEnemyMarker.cs &amp;&amp; grep -n "RespawnKillScore" Assets/Scripts/World/ScoreManager.cs &amp;&amp; grep -n "AddKillScore(bool isRespawn = false)" Assets/Scripts/World/ScoreManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - `RespawnedEnemyMarker.cs`가 `MonoBehaviour`를 상속하는 빈 클래스로 존재한다 (필드/메서드 없음)
    - `ScoreManager.cs`에 `public const int RespawnKillScore = 30;`이 존재한다
    - `AddKillScore(bool isRespawn = false)` 본문이 정확히 `Score += isRespawn ? RespawnKillScore : KillScore;` 한 줄이다
    - `ScoreManager.cs`의 다른 상수(`FastClearBonus`, `TimeBonusPerSecond` 등)와 `AddTimeBonus()`/`AddRoomClearBonus()`/`Reset()`/`StartRoomTimer()`는 변경되지 않는다
  </acceptance_criteria>
  <done>리스폰 개체를 태깅할 수 있는 마커 컴포넌트와, 감소된 점수를 지급하는 ScoreManager API가 완성된다.</done>
</task>

<task type="auto">
  <name>Task 3: CombatController에서 리스폰 판정 후 감소 점수 호출 배선 (D-10)</name>
  <files>Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs 의 `ExecuteDash()` 메서드 전체 (킬/점수 호출 지점)
    - Assets/Scripts/Enemy/RespawnedEnemyMarker.cs (Task 2 산출물)
  </read_first>
  <action>
    `Assets/Scripts/Player/CombatController.cs`의 `ExecuteDash(IEnemy target)` 메서드에서 아래 블록을:

    ```csharp
        // 6. Kill and effects
        target.OnDashHit();
        AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
        SpawnHitSpark(destination);
        _cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
        ScoreManager.AddKillScore();
        yield return StartCoroutine(HitFreeze(hitFreezeDuration));
    ```

    아래로 교체한다:

    ```csharp
        // 6. Kill and effects
        bool isRespawnKill = ((MonoBehaviour)target).GetComponent<RespawnedEnemyMarker>() != null; // D-10(999.2): 리스폰 개체 여부로 감소 점수 판정 — IEnemy 미확장(ISpawnGatable과 동일 패턴)
        target.OnDashHit();
        AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
        SpawnHitSpark(destination);
        _cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
        ScoreManager.AddKillScore(isRespawnKill);
        yield return StartCoroutine(HitFreeze(hitFreezeDuration));
    ```

    `((MonoBehaviour)target)` 캐스팅은 같은 메서드 상단에서 이미 `Vector2 destination = (Vector2)((MonoBehaviour)target).transform.position;`로 사용 중인 기존 패턴을 그대로 재사용한 것이다 — 신규 캐스팅 기법 도입 아님.
  </action>
  <verify>
    <automated>grep -n "GetComponent<RespawnedEnemyMarker>" Assets/Scripts/Player/CombatController.cs &amp;&amp; grep -n "AddKillScore(isRespawnKill)" Assets/Scripts/Player/CombatController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `isRespawnKill` 판정이 `target.OnDashHit()` 호출보다 앞줄에 위치한다 (죽은 뒤에도 컴포넌트는 남아있어 순서 자체는 기능적으로 안전하지만, 판정 의도를 명확히 하기 위함)
    - `ScoreManager.AddKillScore(isRespawnKill)` 호출로 기존 `ScoreManager.AddKillScore();` 무인자 호출이 대체된다
    - `ExecuteDash()`의 나머지 로직(대시 이동, 애니메이션, HitFreeze, 쿨다운/게이지 보너스)은 변경되지 않는다
  </acceptance_criteria>
  <done>리스폰된 적을 처치했을 때만 ScoreManager.RespawnKillScore가 지급되고, 일반 적 처치는 기존 KillScore 그대로 유지된다.</done>
</task>

</tasks>

<verification>
- `EnemySpawner.cs`/`ScoreManager.cs`/`CombatController.cs`/`RespawnedEnemyMarker.cs` 컴파일 성공 (Unity 컴파일은 999.2-04에서 최종 확인)
- `WorldGenerator.cs`, `DebugRoomTeleporter.cs`의 git diff 0줄 (Spawn() 반환형 변경에 영향받지 않음)
- 이 Plan은 어떤 씬/프리팹 파일도 수정하지 않는다
</verification>

<success_criteria>
- D-09(무제한 리스폰 시 댕글링 참조로 인한 영구 no-op 방지)와 D-10(감소 점수)이 코드 레벨에서 완성됨
- 999.2-03(WorldGenerator 통합)이 `spawner.ResetForRespawn()`→`spawner.Spawn(...)`→반환된 GameObject에 `AddComponent<RespawnedEnemyMarker>()`를 그대로 호출할 수 있는 안정된 API 확보
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-enemy-infinite-respawn-mechanism/999.2-02-SUMMARY.md`
</output>
