---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 02
subsystem: gameplay
tags: [unity, c#, enemy-spawn, scoring, respawn]

# Dependency graph
requires:
  - phase: 999.2-enemy-infinite-respawn-mechanism (plan 01)
    provides: baseline respawn mechanism groundwork (if any) — this plan is self-contained on current EnemySpawner/ScoreManager/CombatController
provides:
  - "EnemySpawner.ResetForRespawn() safely re-arms HasActivated + clears dangling _spawned reference"
  - "EnemySpawner.Spawn() returns the instantiated GameObject for caller tagging"
  - "RespawnedEnemyMarker empty additive marker component (IEnemy-unextended tagging pattern)"
  - "ScoreManager.RespawnKillScore (30) + AddKillScore(bool isRespawn = false) overload"
  - "CombatController.ExecuteDash() detects RespawnedEnemyMarker and grants reduced score"
affects: [999.2-03-worldgenerator-integration, 999.2-04-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive marker component (ISpawnGatable precedent) used again to tag respawned enemies without touching the locked 3-member IEnemy contract"

key-files:
  created:
    - Assets/Scripts/Enemy/RespawnedEnemyMarker.cs
  modified:
    - Assets/Scripts/World/EnemySpawner.cs
    - Assets/Scripts/World/ScoreManager.cs
    - Assets/Scripts/Player/CombatController.cs

key-decisions:
  - "ResetForRespawn() clears both HasActivated and _spawned in the same call to eliminate the dangling-reference no-op documented in 999.2-RESEARCH.md Pitfall 1"
  - "RespawnKillScore = 30 (~30% of KillScore) makes respawn farming score-inefficient without blocking it outright, consistent with D-09's unlimited-respawn design"
  - "AddKillScore(bool isRespawn = false) uses a default parameter so the sole existing call site would have compiled unchanged even before Task 3 rewired it"

patterns-established:
  - "Empty additive marker components for IEnemy-unextended tagging (RespawnedEnemyMarker following ISpawnGatable)"

requirements-completed: [D-05, D-07, D-09, D-10]

# Metrics
duration: 5min
completed: 2026-07-14
---

# Phase 999.2 Plan 02: EnemySpawner Respawn Safety + Reduced Respawn-Kill Scoring Summary

**EnemySpawner gains a dangling-reference-safe ResetForRespawn() API and GameObject-returning Spawn(), paired with a new RespawnedEnemyMarker tag and ScoreManager.AddKillScore(bool) overload that pays 30 points (vs. 100) for respawned kills.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-07-14T11:45:00+09:00
- **Completed:** 2026-07-14T11:46:05+09:00
- **Tasks:** 3
- **Files modified:** 3 modified, 1 created

## Accomplishments
- EnemySpawner can now be safely reused across a respawn cycle without leaving Activate() permanently no-op'd (Pitfall 1 from 999.2-RESEARCH.md resolved)
- New RespawnedEnemyMarker additive component lets WorldGenerator (999.2-03) tag respawned instances without extending the locked IEnemy interface
- ScoreManager now distinguishes respawn kills (30 pts) from normal kills (100 pts), wired end-to-end through CombatController.ExecuteDash()

## Task Commits

Each task was committed atomically:

1. **Task 1: EnemySpawner ResetForRespawn() + Spawn() GameObject return** - `ebe9f2a` (feat)
2. **Task 2: RespawnedEnemyMarker + ScoreManager reduced-score API** - `7cba019` (feat)
3. **Task 3: CombatController respawn-kill detection wiring** - `4a12928` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `Assets/Scripts/World/EnemySpawner.cs` - Spawn() returns GameObject; added ResetForRespawn() clearing HasActivated + _spawned
- `Assets/Scripts/Enemy/RespawnedEnemyMarker.cs` - New empty MonoBehaviour marker for respawned enemy instances
- `Assets/Scripts/World/ScoreManager.cs` - Added RespawnKillScore=30 constant; AddKillScore(bool isRespawn = false) overload
- `Assets/Scripts/Player/CombatController.cs` - ExecuteDash() checks target for RespawnedEnemyMarker before scoring, passes result to AddKillScore

## Decisions Made
- ResetForRespawn() body is exactly `HasActivated = false; _spawned = null;` — no additional logic, matching the plan's precise Pitfall-1 fix
- Reused the existing `((MonoBehaviour)target)` cast pattern already present in ExecuteDash() rather than introducing a new casting technique
- No changes made to WorldGenerator.cs or DebugRoomTeleporter.cs — confirmed 0-line diff since Spawn()'s signature change (void → GameObject) doesn't affect callers that ignore the return value

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. This plan does not touch any scene or prefab files.

## Next Phase Readiness

- 999.2-03 (WorldGenerator integration) can now safely call `spawner.ResetForRespawn()` → `spawner.Spawn(...)` → `AddComponent<RespawnedEnemyMarker>()` on the returned GameObject to complete the respawn + reduced-score economy end-to-end
- Unity compile verification of these 4 files is deferred to 999.2-04 per plan's verification section
- No blockers identified

---
*Phase: 999.2-enemy-infinite-respawn-mechanism*
*Completed: 2026-07-14*

## Self-Check: PASSED

All created/modified files confirmed present on disk; all 3 task commits (ebe9f2a, 7cba019, 4a12928) confirmed in git history.
