---
phase: 999.2-enemy-infinite-respawn-mechanism
plan: 03
subsystem: gameplay
tags: [unity, c#, world-generation, enemy-respawn, room-lifecycle]

# Dependency graph
requires:
  - phase: 999.2-enemy-infinite-respawn-mechanism (plan 01)
    provides: RoomRespawnGate (MarkLeft/IsPendingRespawn/ConsumeRespawn) + RoomClearCondition (IsCleared/ResetForRespawn)
  - phase: 999.2-enemy-infinite-respawn-mechanism (plan 02)
    provides: EnemySpawner.ResetForRespawn()/Spawn() GameObject return + RespawnedEnemyMarker + reduced respawn-kill scoring
provides:
  - "WorldGenerator.MarkRoomLeft(GameObject) — notifies RoomRespawnGate when player leaves a room node (no-op on Corridor)"
  - "WorldGenerator.TryRespawnRoom(GameObject) — consumes the gate, resets+respawns EnemySpawners at original marker positions, tags new instances with RespawnedEnemyMarker, resets RoomClearCondition, reuses ActivateStaggered() for spawn VFX"
  - "UpdatePlayerIndex() forward/backward loops wired to call MarkRoomLeft/TryRespawnRoom on every chain-index transition"
affects: [999.2-04-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "GetComponent<T>() + null-conditional (?.) as the standard no-op guard for optional per-room components (RoomRespawnGate absent on Corridor)"

key-files:
  created: []
  modified:
    - Assets/Scripts/World/WorldGenerator.cs

key-decisions:
  - "TryRespawnRoom() calls gate.ConsumeRespawn() before the spawn loop (D-03: gate consumed on this reentry regardless of spawn outcome)"
  - "spawner.ResetForRespawn() is called before spawner.Spawn(...) in every loop iteration, matching 999.2-RESEARCH.md Pitfall 1 ordering"
  - "gate.ClearCondition.ResetForRespawn() is called after the spawn loop completes so newly spawned children are included in the re-scan"
  - "No respawn counter/limit variable introduced anywhere (D-09: unlimited respawn cycles)"
  - "Reused the existing ActivateStaggered() coroutine — no new coroutine written for spawn VFX (D-08)"

patterns-established:
  - "Room-node-leave/enter events piggyback on the existing UpdatePlayerIndex() chain-index transition instead of new trigger colliders (999.2-RESEARCH.md Pitfall 2 avoidance)"

requirements-completed: [D-01, D-01a, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]

# Metrics
duration: 6min
completed: 2026-07-14
---

# Phase 999.2 Plan 03: WorldGenerator Room-Reentry Respawn Integration Summary

**WorldGenerator's existing per-frame chain-index transition signal (UpdatePlayerIndex) now drives room-reentry enemy respawn end-to-end, with zero new trigger colliders or coroutines.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-07-14T11:59:00+09:00
- **Completed:** 2026-07-14T11:59:58+09:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `WorldGenerator` gained `MarkRoomLeft()`/`TryRespawnRoom()` — the final integration point tying together 999.2-01's `RoomRespawnGate` state machine and 999.2-02's `EnemySpawner.ResetForRespawn()`/`RespawnedEnemyMarker` tagging
- `UpdatePlayerIndex()`'s forward and backward chain-transition loops now call these two methods on every room-node crossing, requiring no new trigger geometry (999.2-RESEARCH.md Pitfall 2 avoided)
- Corridor nodes automatically no-op (no `RoomRespawnGate` component attached), structurally satisfying D-04 without any conditional branching
- D-01a (Case A vs Case B distinction) holds structurally: `RoomRespawnGate`'s state lives on the room `GameObject` itself, so `RemoveTail()`/`RemoveHead()`-driven `Destroy()` erases the gate along with any pending-respawn state — no explicit Case B exclusion code needed

## Task Commits

Each task was committed atomically:

1. **Task 1: WorldGenerator에 MarkRoomLeft()/TryRespawnRoom() 메서드 추가** - `f3280a5` (feat)
2. **Task 2: UpdatePlayerIndex()에 MarkRoomLeft/TryRespawnRoom 배선** - `b9304d7` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `Assets/Scripts/World/WorldGenerator.cs` - Added `MarkRoomLeft(GameObject)`/`TryRespawnRoom(GameObject)` private methods; wired both into `UpdatePlayerIndex()`'s forward/backward chain-transition loops

## Decisions Made
- Followed the plan's exact method bodies and call-site ordering verbatim (gate consumed before spawn loop, `ResetForRespawn()` before `Spawn()`, `ClearCondition.ResetForRespawn()` after the loop, existing `ActivateStaggered()` reused)
- No new fields, coroutines, or counters added — matches D-09 (no respawn limit) and the plan's "surgical change" acceptance criteria

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. This plan touches only `WorldGenerator.cs`; no scene or prefab changes.

## Next Phase Readiness

- 999.2-04 can now attach `RoomRespawnGate` to the Complex_Room 6 prefabs and verify the full respawn loop in actual play (per this plan's success criteria)
- Full Unity compile verification is deferred to 999.2-04, consistent with this plan's `<verification>` section, since `Complex_Room` prefabs don't yet carry `RoomRespawnGate` (GetComponent returns null everywhere until then, which is safe no-op behavior, not a compile blocker)
- No blockers identified

---
*Phase: 999.2-enemy-infinite-respawn-mechanism*
*Completed: 2026-07-14*

## Self-Check: PASSED

All modified files confirmed present on disk; both task commits (f3280a5, b9304d7) confirmed in git history.
