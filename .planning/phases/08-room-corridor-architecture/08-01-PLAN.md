---
phase: 08-room-corridor-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/RoomConnector.cs
  - Assets/Editor/RoomMarkerTool.cs
autonomous: true
requirements: [ARCH-01, ARCH-03]

must_haves:
  truths:
    - "Room 프리팹(Room_Combat, Room_Fall, Room_Gap, Room_Stair) 씬 뷰에서 파란/초록 Gizmo 구체가 좌우 가장자리에 각각 표시된다"
    - "RoomConnector 인스펙터에서 Direction(Left/Right)과 ConnectedObject 필드가 직렬화되어 노출된다"
    - "Phase 9 WorldGenerator가 GetComponentsInChildren<RoomConnector>()로 Left/Right 커넥터를 탐색할 수 있다"
  artifacts:
    - path: "Assets/Scripts/World/RoomConnector.cs"
      provides: "연결 마커 컴포넌트 + Gizmo 시각화"
      exports: ["RoomConnector", "RoomConnector.Direction"]
    - path: "Assets/Editor/RoomMarkerTool.cs"
      provides: "4개 Room 프리팹에 RoomConnector 자동 부착 에디터 도구"
      contains: "MenuItem(\"Fast/Phase8/Add Room Connectors\")"
  key_links:
    - from: "RoomMarkerTool.cs"
      to: "Room_Combat/Room_Combat.prefab, Room_Fall, Room_Gap, Room_Stair"
      via: "PrefabUtility.LoadPrefabContents → Transform.Find(\"ENT\"/\"EXIT\") → AddComponent<RoomConnector>"
      pattern: "PrefabUtility\\.LoadPrefabContents"
    - from: "RoomConnector.OnDrawGizmos"
      to: "씬 뷰 시각화"
      via: "Gizmos.DrawSphere(transform.position, 0.4f)"
      pattern: "OnDrawGizmos"
---

<objective>
Room 프리팹에 방향 정보를 가진 연결 마커 컴포넌트(RoomConnector)를 도입하고, 기존 4개 Room 프리팹의 ENT/EXIT 자식 오브젝트에 자동으로 부착하는 에디터 도구를 작성한다.

Purpose: Phase 9 WorldGenerator가 GetComponentsInChildren<RoomConnector>()로 Left/Right 커넥터를 탐색해 Room→Corridor 체인을 자동 생성할 수 있는 아키텍처 기반 마련.
Output: RoomConnector.cs (런타임 마커), RoomMarkerTool.cs (에디터 설정 도구)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/새 폴더/Fast/.planning/PROJECT.md
@D:/새 폴더/Fast/.planning/ROADMAP.md
@D:/새 폴더/Fast/.planning/STATE.md

@D:/새 폴더/Fast/.planning/phases/08-room-corridor-architecture/08-CONTEXT.md
</context>

<interfaces>
<!-- 참고: 기존 마커 패턴 (RoomEntry.cs) -->

```csharp
// Assets/Scripts/World/RoomEntry.cs — 경량 마커 MonoBehaviour 패턴
public class RoomEntry : MonoBehaviour { }
```

<!-- 참고: RoomPrefabBuilder.cs의 SavePrefab 패턴 — RoomMarkerTool이 동일 방식으로 프리팹 저장 -->
```csharp
// 기존 패턴: PrefabUtility.SaveAsPrefabAsset + DestroyImmediate
// 신규 패턴: PrefabUtility.LoadPrefabContents + SaveAsPrefabAsset(root, path) + UnloadPrefabContents
// — 기존 프리팹 수정에는 LoadPrefabContents/UnloadPrefabContents 사용

// 각 Room 프리팹 구조 (RoomPrefabBuilder 기준):
// root(Room_Combat)
//   ├─ Geometry/
//   │    ├─ Floor
//   │    ├─ WallLeft   ← (-14.5, 4, 0)
//   │    └─ WallRight  ← ( 14.5, 4, 0)
//   ├─ EnemySpawns/
//   ├─ ENT             ← (-14, 0, 0)  ★ RoomConnector(Direction.Left) 추가 대상
//   └─ EXIT            ← ( 14, 0, 0)  ★ RoomConnector(Direction.Right) 추가 대상

// Room별 ENT/EXIT 위치 (Phase 9 WorldGenerator 참고용):
// Room_Combat: ENT(-14,0)   EXIT(14,0)    — 같은 높이
// Room_Fall:   ENT(-11,0)   EXIT(11,2)    — EXIT가 2 높음
// Room_Gap:    ENT(-14,0)   EXIT(14,1)    — EXIT가 1 높음
// Room_Stair:  ENT(-12,0)   EXIT(12,9)    — EXIT가 9 높음
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RoomConnector.cs 작성</name>
  <files>Assets/Scripts/World/RoomConnector.cs</files>
  <action>
    경량 마커 MonoBehaviour. RoomEntry.cs와 동일한 최소 패턴을 따르되 Direction 정보와 Gizmo를 추가한다.

    구현 사항:
    - `public enum Direction { Left, Right }` — 룸 기준 좌/우 연결 방향
    - `[SerializeField] public Direction direction;` — 인스펙터 노출
    - `[SerializeField] public GameObject connectedObject;` — Phase 9에서 WorldGenerator가 채움; Phase 8에서는 null 허용
    - `OnDrawGizmos()`: `direction == Direction.Left` → `Color.blue`, `Direction.Right` → `Color.green`, `Gizmos.DrawSphere(transform.position, 0.4f)`
    - 클래스 주석: "Phase 9 WorldGenerator가 GetComponentsInChildren<RoomConnector>()로 탐색한다."

    금지: Update/Start/Awake 불필요 — 마커 전용이므로 생명주기 메서드 추가하지 않음.
  </action>
  <verify>
    <automated>grep -n "Direction\|OnDrawGizmos\|DrawSphere" "D:/새 폴더/Fast/Assets/Scripts/World/RoomConnector.cs"</automated>
  </verify>
  <done>RoomConnector.cs에 Direction enum, direction/connectedObject 직렬화 필드, OnDrawGizmos가 포함된다.</done>
</task>

<task type="auto">
  <name>Task 2: RoomMarkerTool.cs 에디터 스크립트 작성</name>
  <files>Assets/Editor/RoomMarkerTool.cs</files>
  <action>
    메뉴 "Fast/Phase8/Add Room Connectors" 실행 시 4개 Room 프리팹의 ENT/EXIT 자식에 RoomConnector를 부착하는 에디터 전용 스크립트.

    대상 Room 목록 (per D-02, D-CONTEXT):
    - Room_Combat, Room_Fall, Room_Gap, Room_Stair

    로직:
    1. 각 roomName에 대해 `path = $"Assets/Prefabs/Rooms/{roomName}/{roomName}.prefab"`
    2. `var root = PrefabUtility.LoadPrefabContents(path)` — null이면 LogError + continue
    3. `AddConnector(root, "ENT", RoomConnector.Direction.Left)`
    4. `AddConnector(root, "EXIT", RoomConnector.Direction.Right)`
    5. `PrefabUtility.SaveAsPrefabAsset(root, path)`
    6. `PrefabUtility.UnloadPrefabContents(root)`
    7. 루프 완료 후 `AssetDatabase.SaveAssets(); AssetDatabase.Refresh();`

    `AddConnector(GameObject root, string childName, RoomConnector.Direction dir)` 헬퍼:
    - `root.transform.Find(childName)` — null이면 LogWarning + return
    - `child.GetComponent<RoomConnector>() != null` → 이미 있으면 return (멱등성)
    - `var rc = child.gameObject.AddComponent<RoomConnector>()`
    - `rc.direction = dir`

    using: UnityEditor, UnityEngine
    클래스: `public static class RoomMarkerTool`
  </action>
  <verify>
    <automated>grep -n "LoadPrefabContents\|AddConnector\|MenuItem\|Phase8" "D:/새 폴더/Fast/Assets/Editor/RoomMarkerTool.cs"</automated>
  </verify>
  <done>RoomMarkerTool.cs에 [MenuItem("Fast/Phase8/Add Room Connectors")]가 있고 4개 Room에 RoomConnector를 멱등적으로 추가하는 로직이 포함된다.</done>
</task>

</tasks>

<verification>
- RoomConnector.cs 컴파일 오류 없음 (Direction enum, 직렬화 필드, Gizmos)
- RoomMarkerTool.cs 컴파일 오류 없음 (PrefabUtility API 사용)
- 파일 2개 모두 생성됨: grep 명령 결과 확인
</verification>

<success_criteria>
- Assets/Scripts/World/RoomConnector.cs 존재, Direction enum과 OnDrawGizmos 포함
- Assets/Editor/RoomMarkerTool.cs 존재, Fast/Phase8 메뉴 항목 포함
- Unity 컴파일 후 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/08-room-corridor-architecture/08-01-SUMMARY.md`
</output>
