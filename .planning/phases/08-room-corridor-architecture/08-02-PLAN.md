---
phase: 08-room-corridor-architecture
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Assets/Editor/CorridorBuilder.cs
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "Corridor_Flat, Corridor_Up, Corridor_Down 프리팹 3개가 Assets/Prefabs/Corridors/ 에 존재한다"
    - "각 Corridor에 EnemySpawnPoint 태그를 가진 자식 오브젝트가 최소 1개 있다"
    - "각 Corridor의 좌우 끝에 RoomConnector(Left/Right) 컴포넌트가 부착된 자식 마커가 있다"
  artifacts:
    - path: "Assets/Editor/CorridorBuilder.cs"
      provides: "3종 Corridor 프리팹 생성 에디터 도구"
      contains: "MenuItem(\"Fast/Phase8/Build Corridors\")"
    - path: "Assets/Prefabs/Corridors/Corridor_Flat/Corridor_Flat.prefab"
      provides: "직진 전투 구간"
    - path: "Assets/Prefabs/Corridors/Corridor_Up/Corridor_Up.prefab"
      provides: "상승(저→고) 전투 구간"
    - path: "Assets/Prefabs/Corridors/Corridor_Down/Corridor_Down.prefab"
      provides: "하강(고→저) 전투 구간"
  key_links:
    - from: "Corridor ENT child"
      to: "RoomConnector(Direction.Left)"
      via: "AddComponent<RoomConnector> + direction = Left"
      pattern: "Direction\\.Left"
    - from: "Corridor EXIT child"
      to: "RoomConnector(Direction.Right)"
      via: "AddComponent<RoomConnector> + direction = Right"
      pattern: "Direction\\.Right"
---

<objective>
Corridor 3종(상승/직진/하강) 프리팹을 에디터 스크립트로 생성한다. 각 Corridor는 계단 플랫폼과 EnemySpawnPoint 태그 자식을 포함하며, 양 끝에 RoomConnector 마커가 붙어 Phase 9 WorldGenerator가 Room과 정렬할 수 있다.

Purpose: ARCH-02 충족 — 전투 구간인 Corridor 3종이 에셋 폴더에 존재해야 Phase 9 무한 생성 로직이 랜덤 선택할 수 있다.
Output: Assets/Editor/CorridorBuilder.cs + 실행 시 생성되는 Corridor 3종 프리팹
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/새 폴더/Fast/.planning/phases/08-room-corridor-architecture/08-CONTEXT.md
@D:/새 폴더/Fast/.planning/phases/08-room-corridor-architecture/08-01-SUMMARY.md
</context>

<interfaces>
<!-- 08-01에서 생성된 RoomConnector (이 계획의 CorridorBuilder가 참조) -->
```csharp
// Assets/Scripts/World/RoomConnector.cs
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    [SerializeField] public Direction direction;
    [SerializeField] public GameObject connectedObject;
}
```

<!-- RoomPrefabBuilder.cs 패턴 재사용: CreatePlatform, CreateMarker, SavePrefab -->
```csharp
// PLATFORM_LAYER = 9  (TagManager의 "Platform" 레이어)
// CreatePlatform: BoxCollider2D(size) + SpriteRenderer(UISprite, Sliced, size) + layer=9
// CreateMarker: 빈 GameObject, Transform.localPosition만 설정
// SavePrefab: PrefabUtility.SaveAsPrefabAsset(root, path) + DestroyImmediate(root)
```

<!-- Corridor 치수 설계 (D-07, D-08, D-09 기반) -->
<!--
  Corridor_Flat (직진):
    플랫폼: Floor at (0, 0), size (12, 1)
    ENT child: (-6, 0)  → RoomConnector(Left)   — Y=0 (저→저)
    EXIT child: (6, 0)  → RoomConnector(Right)
    EnemySpawnPoint: (0, 1)

  Corridor_Up (상승 저→고, 높이차 4):
    Step_A: (-4, 0), size (5, 1)
    Step_B: ( 1, 2), size (5, 1)
    Step_C: ( 5, 4), size (5, 1)
    ENT child: (-6, 0)  → RoomConnector(Left)
    EXIT child: ( 7, 4) → RoomConnector(Right)  — Y=4 (저→고)
    EnemySpawnPoint: (5, 5)  (Step_C 위)

  Corridor_Down (하강 고→저, 높이차 4):
    Step_A: (-5, 4), size (5, 1)
    Step_B: (-1, 2), size (5, 1)
    Step_C: ( 4, 0), size (5, 1)
    ENT child: (-7, 4)  → RoomConnector(Left)   — Y=4 (고→저)
    EXIT child: ( 6, 0) → RoomConnector(Right)
    EnemySpawnPoint: (-5, 5)  (Step_A 위)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CorridorBuilder.cs 에디터 스크립트 작성</name>
  <files>Assets/Editor/CorridorBuilder.cs</files>
  <action>
    메뉴 "Fast/Phase8/Build Corridors" 실행 시 Corridor 3종 프리팹을 생성/갱신하는 에디터 전용 스크립트.

    구조:
    - `public static class CorridorBuilder`
    - `[MenuItem("Fast/Phase8/Build Corridors")]` → `Run()` 메서드
    - Run(): `BuildCorridor_Flat()`, `BuildCorridor_Up()`, `BuildCorridor_Down()` 순서 호출 후 `AssetDatabase.SaveAssets(); AssetDatabase.Refresh();`

    저장 경로: `Assets/Prefabs/Corridors/{corridorName}/{corridorName}.prefab`
    디렉토리 없으면 `System.IO.Directory.CreateDirectory(Application.dataPath + "/Prefabs/Corridors/..." )` 또는 `AssetDatabase.CreateFolder`로 생성.

    헬퍼 (RoomPrefabBuilder 패턴 재사용):
    - `CreatePlatform(GameObject parent, string name, Vector3 pos, float w, float h)` — BoxCollider2D + SpriteRenderer(UISprite, Sliced) + layer=9
    - `CreateMarker(GameObject parent, string name, Vector3 localPos)` — 빈 GameObject
    - `AddConnector(GameObject markerGO, RoomConnector.Direction dir)` — AddComponent<RoomConnector> + direction 설정
    - `CreateSpawnPoint(GameObject parent, string name, Vector3 localPos)` — 빈 GO + `go.tag = "EnemySpawnPoint"`
    - `SavePrefab(GameObject root, string corridorName)` — 기존 에셋 있으면 DeleteAsset 후 SaveAsPrefabAsset + DestroyImmediate

    **BuildCorridor_Flat()**:
    ```
    root("Corridor_Flat")
      ├─ Geometry/
      │    └─ Floor: pos(0,0,0), size(12,1)
      ├─ ENT:  localPos(-6, 0, 0) → AddConnector(Left)
      ├─ EXIT: localPos( 6, 0, 0) → AddConnector(Right)
      └─ EnemySpawn_0: localPos(0, 1, 0), tag="EnemySpawnPoint"
    ```

    **BuildCorridor_Up()**:
    ```
    root("Corridor_Up")
      ├─ Geometry/
      │    ├─ Step_A: pos(-4, 0, 0), size(5,1)
      │    ├─ Step_B: pos( 1, 2, 0), size(5,1)
      │    └─ Step_C: pos( 5, 4, 0), size(5,1)
      ├─ ENT:  localPos(-6, 0, 0) → AddConnector(Left)
      ├─ EXIT: localPos( 7, 4, 0) → AddConnector(Right)
      └─ EnemySpawn_0: localPos(5, 5, 0), tag="EnemySpawnPoint"
    ```

    **BuildCorridor_Down()**:
    ```
    root("Corridor_Down")
      ├─ Geometry/
      │    ├─ Step_A: pos(-5, 4, 0), size(5,1)
      │    ├─ Step_B: pos(-1, 2, 0), size(5,1)
      │    └─ Step_C: pos( 4, 0, 0), size(5,1)
      ├─ ENT:  localPos(-7, 4, 0) → AddConnector(Left)
      ├─ EXIT: localPos( 6, 0, 0) → AddConnector(Right)
      └─ EnemySpawn_0: localPos(-5, 5, 0), tag="EnemySpawnPoint"
    ```

    using: UnityEditor, UnityEngine, System.IO
    주의: `go.tag = "EnemySpawnPoint"` — Unity TagManager에 이 태그가 이미 정의되어 있음. 없으면 LogWarning만 출력하고 계속 진행.
  </action>
  <verify>
    <automated>grep -n "BuildCorridor_Flat\|BuildCorridor_Up\|BuildCorridor_Down\|EnemySpawnPoint\|MenuItem" "D:/새 폴더/Fast/Assets/Editor/CorridorBuilder.cs"</automated>
  </verify>
  <done>CorridorBuilder.cs에 Fast/Phase8/Build Corridors 메뉴 항목과 3종 Corridor 빌더 메서드가 있다. Unity 컴파일 오류 없음.</done>
</task>

</tasks>

<verification>
- CorridorBuilder.cs 파일 존재 확인: grep 명령 결과
- 3개 BuildCorridor_* 메서드 모두 정의됨
- EnemySpawnPoint 태그 설정 코드 포함됨
- RoomConnector.Direction 참조 포함됨 (08-01 의존성 활용)
</verification>

<success_criteria>
- Assets/Editor/CorridorBuilder.cs 생성됨
- Unity 컴파일 후 에러 없음
- "Fast/Phase8/Build Corridors" 메뉴 항목 Unity에서 확인 가능
</success_criteria>

<output>
After completion, create `.planning/phases/08-room-corridor-architecture/08-02-SUMMARY.md`
</output>
