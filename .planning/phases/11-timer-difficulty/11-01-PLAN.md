---
phase: 11-timer-difficulty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/FloorTimer.cs
  - Assets/Scripts/World/ScoreManager.cs
autonomous: true
requirements:
  - TIMER-01
  - TIMER-02
  - SCORE-01

must_haves:
  truths:
    - "FloorTimer.RemainingSeconds가 60에서 시작해 Time.unscaledTime 기반으로 감소한다 (D-01, D-07)"
    - "FloorTimer.Tick()은 RemainingSeconds가 0 이하가 된 최초 1프레임에만 PlayerController.OnPlayerDeath를 발동하고, 이후로는 재호출해도 다시 발동하지 않는다 (D-06, TIMER-02)"
    - "ScoreManager.AddTimeBonus(remainingSeconds)는 remainingSeconds × 10점을 Score에 더한다 (D-02)"
  artifacts:
    - path: "Assets/Scripts/World/FloorTimer.cs"
      provides: "정적 클래스 — Duration, RemainingSeconds, Reset(), Tick()"
      contains: "public static class FloorTimer"
    - path: "Assets/Scripts/World/ScoreManager.cs"
      provides: "AddTimeBonus(float remainingSeconds) 메서드 + TimeBonusPerSecond 상수"
      contains: "public static void AddTimeBonus"
  key_links:
    - from: "FloorTimer.Tick()"
      to: "PlayerController.TriggerDeath()"
      via: "직접 정적 메서드 호출 (RemainingSeconds <= 0f 최초 1회)"
      pattern: "PlayerController\\.TriggerDeath\\(\\)"
    - from: "ScoreManager.AddTimeBonus(remainingSeconds)"
      to: "ScoreManager.Score"
      via: "Score += Mathf.RoundToInt(remainingSeconds) * TimeBonusPerSecond"
      pattern: "Score \\+= Mathf\\.RoundToInt"
---

<objective>
`FloorTimer` 정적 클래스를 신규 생성하고(`FloorManager`/`ScoreManager` 패턴 준수, D-07), 기존 `ScoreManager.cs`에 남은 시간 기반 점수 계산 API(`AddTimeBonus`)를 추가한다. 이 Plan은 Phase 11의 기초 데이터 계약만 만든다 — `WorldGenerator`/`HUDController` 통합은 Plan 02/03에서 진행한다.

Purpose: Plan 02(WorldGenerator 통합)와 Plan 03(HUD 표시)이 참조할 정적 API의 정확한 시그니처를 먼저 확정해, 후속 Plan이 코드 탐색 없이 바로 사용할 수 있게 한다.
Output: `FloorTimer.cs` 신규 파일 1개, `ScoreManager.cs` 확장 (컴파일 성공, 아직 호출부 없음 — 이 Plan 단독으로는 게임플레이에 영향 없음).
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-timer-difficulty/11-CONTEXT.md

<interfaces>
Existing project conventions this plan ports directly:

From Assets/Scripts/World/FloorManager.cs (정적 클래스, data-only, 전체):
```csharp
public static class FloorManager
{
    public static int CurrentFloor = 1;
}
```

From Assets/Scripts/World/ScoreManager.cs (현재 55줄 전체 — Time.unscaledTime 기반 타이머 패턴, 이 Plan이 확장하는 대상):
```csharp
public static class ScoreManager
{
    public const int   KillScore        = 100;
    public const int   FastClearBonus   = 300;
    public const int   NormalClearBonus = 150;
    public const int   SlowClearBonus   = 50;
    public const float FastClearTime    = 10f;
    public const float NormalClearTime  = 25f;

    public static int Score { get; private set; }
    private static float _roomStartTime;

    public static void Reset() { Score = 0; _roomStartTime = Time.unscaledTime; }
    public static void AddKillScore() { Score += KillScore; }
    public static void StartRoomTimer() { _roomStartTime = Time.unscaledTime; }
    public static void AddRoomClearBonus() { /* elapsed 기반 보너스 분기 */ }
}
```

From Assets/Scripts/Player/PlayerController.cs (line 58-60, 이 Plan이 호출하는 대상 — 수정 금지):
```csharp
public static event Action OnPlayerDeath;
public static void TriggerDeath() => OnPlayerDeath?.Invoke();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FloorTimer.cs — 정적 타이머 클래스</name>
  <files>Assets/Scripts/World/FloorTimer.cs</files>
  <read_first>
    - Assets/Scripts/World/FloorManager.cs (가장 단순한 정적 클래스 패턴)
    - Assets/Scripts/World/ScoreManager.cs (Time.unscaledTime 기반 타이머 패턴 — StartRoomTimer/AddRoomClearBonus)
    - Assets/Scripts/Player/PlayerController.cs (OnPlayerDeath/TriggerDeath 정의부, line 58-60)
  </read_first>
  <action>
`Assets/Scripts/World/FloorTimer.cs`를 신규 생성한다:

```csharp
using UnityEngine;

/// <summary>
/// Phase 11 — 층 제한시간 정적 클래스. FloorManager/ScoreManager 패턴 준수 (data-only, no scene lifecycle).
/// D-01: 모든 층 60초 고정. D-07: Time.unscaledTime 기반 — 슬로우모션(Time.timeScale) 면역.
/// D-06/TIMER-02: RemainingSeconds가 0에 도달하면 Tick()이 1회만 PlayerController.OnPlayerDeath를 발동한다.
/// </summary>
public static class FloorTimer
{
    public const float Duration = 60f; // D-01: 층당 고정 제한시간(초)

    private static float _floorStartTime;
    private static bool  _expired;

    /// <summary>층 진입마다 호출 — 60초로 리셋한다 (D-08). WorldGenerator.Start()/FloorTransitionSequence()에서 호출.</summary>
    public static void Reset()
    {
        _floorStartTime = Time.unscaledTime;
        _expired = false;
    }

    /// <summary>남은 시간(초). Time.unscaledTime 기반 — 슬로우모션 중에도 실시간으로 감소한다.</summary>
    public static float RemainingSeconds => Mathf.Max(0f, Duration - (Time.unscaledTime - _floorStartTime));

    /// <summary>
    /// 매 프레임 호출 — 남은 시간이 0에 도달하면 1회만 PlayerController.OnPlayerDeath를 발동한다 (D-06, TIMER-02).
    /// WorldGenerator.Update()에서 호출 (Plan 02).
    /// </summary>
    public static void Tick()
    {
        if (_expired) return;
        if (RemainingSeconds <= 0f)
        {
            _expired = true;
            PlayerController.TriggerDeath();
        }
    }
}
```

주의:
- `Time.deltaTime`/`WaitForSeconds` 사용 금지 — 반드시 `Time.unscaledTime` (ROADMAP Stack Constraints: "Timer: Time.unscaledDeltaTime only")
- `Reset()`을 호출하는 지점(WorldGenerator)은 Plan 02에서 연결한다 — 이 Plan에서는 아무도 호출하지 않으므로 컴파일만 통과하면 된다
  </action>
  <verify>
    <automated>grep -n "public static class FloorTimer\|public const float Duration = 60f\|public static void Reset\|public static float RemainingSeconds\|public static void Tick\|PlayerController.TriggerDeath()" "Assets/Scripts/World/FloorTimer.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "public static class FloorTimer" 라인이 존재한다
    - "public const float Duration = 60f;" 라인이 존재한다 (D-01)
    - "public static void Reset()" 메서드가 존재하고 본문에 "_floorStartTime = Time.unscaledTime;"과 "_expired = false;"를 포함한다
    - "public static float RemainingSeconds" 프로퍼티가 존재하고 "Time.unscaledTime"을 사용한다 ("Time.deltaTime"은 사용하지 않는다)
    - "public static void Tick()" 메서드가 존재하고 본문에 "if (_expired) return;" 가드와 "PlayerController.TriggerDeath();" 호출을 포함한다
  </acceptance_criteria>
  <done>FloorTimer.cs가 컴파일 에러 없이 존재하며, Duration/RemainingSeconds/Reset()/Tick() API가 D-01/D-06/D-07/D-08 결정과 일치한다. 아직 아무 곳에서도 호출되지 않는다(Plan 02에서 연결).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ScoreManager.cs — AddTimeBonus(remainingSeconds) 추가</name>
  <files>Assets/Scripts/World/ScoreManager.cs</files>
  <read_first>
    - Assets/Scripts/World/ScoreManager.cs (수정 대상 — 현재 55줄 전체 상태 확인 필수)
  </read_first>
  <action>
`Assets/Scripts/World/ScoreManager.cs`를 다음과 같이 수정한다.

**1. 상수 추가** — 기존 `public const float NormalClearTime  = 25f; // 초 이내 클리어 시 NormalClearBonus` 라인 바로 뒤에 삽입:

```csharp
    public const int   TimeBonusPerSecond = 10; // D-02(Phase 11): 남은 초 × 10점
```

**2. 메서드 추가** — 기존 `AddKillScore()` 메서드 바로 뒤에 삽입:

```csharp
    /// <summary>
    /// SCORE-01(Phase 11): EXIT 포탈 진입 순간 남은 제한시간(초)에 비례한 점수를 더한다 (D-02).
    /// WorldGenerator.FloorTransitionSequence() 시작 시점(D-02b)에서 FloorTimer.RemainingSeconds를 인자로 호출.
    /// </summary>
    public static void AddTimeBonus(float remainingSeconds)
    {
        Score += Mathf.RoundToInt(remainingSeconds) * TimeBonusPerSecond;
    }
```

금지 사항:
- 기존 `Score`/`_roomStartTime`/`Reset()`/`AddRoomClearBonus()` 로직은 변경하지 않는다 (surgical change — 이 Task는 추가만 한다)
  </action>
  <verify>
    <automated>grep -n "TimeBonusPerSecond\|public static void AddTimeBonus" "Assets/Scripts/World/ScoreManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "public const int   TimeBonusPerSecond = 10;" (또는 동등한 공백의) 라인이 존재한다
    - "public static void AddTimeBonus(float remainingSeconds)" 메서드가 존재한다
    - AddTimeBonus 본문에 "Score += Mathf.RoundToInt(remainingSeconds) * TimeBonusPerSecond;"가 존재한다
    - 기존 KillScore/FastClearBonus/NormalClearBonus/SlowClearBonus/AddKillScore/AddRoomClearBonus/StartRoomTimer/Reset 심볼이 모두 그대로 남아있다 (삭제되지 않음)
  </acceptance_criteria>
  <done>ScoreManager.AddTimeBonus(remainingSeconds)가 D-02 공식(남은 초 × 10점)대로 Score에 정확히 가산하며, 기존 킬/클리어 보너스 로직은 영향받지 않는다.</done>
</task>

</tasks>

<verification>
`Assets/Scripts/World/FloorTimer.cs`가 신규 생성되어 컴파일 에러 없이 존재한다.
`Assets/Scripts/World/ScoreManager.cs`에 `AddTimeBonus` 메서드가 추가되고 기존 메서드/상수가 모두 보존된다.
Unity Editor에서 두 파일을 열었을 때 컴파일 에러가 0개여야 한다 (이 Plan은 호출부가 없으므로 신규 미사용 경고(CS0414 등)는 발생하지 않는다 — static 메서드/프로퍼티는 미호출이어도 경고 없음).
</verification>

<success_criteria>
- FloorTimer.cs가 D-01(60초 고정)/D-06(만료 시 1회 TriggerDeath)/D-07(정적 클래스, unscaledTime)/D-08(Reset API) 결정을 모두 반영한다
- ScoreManager.AddTimeBonus가 D-02 공식(남은 초 × 10점)을 정확히 구현한다
- 두 파일 모두 컴파일 에러 없이 존재하며, Plan 02/03이 즉시 참조 가능한 상태다
</success_criteria>

<output>
완료 후 `.planning/phases/11-timer-difficulty/11-01-SUMMARY.md` 생성:
- FloorTimer.cs 신규 생성 내용 (API 목록: Duration, RemainingSeconds, Reset(), Tick())
- ScoreManager.cs에 추가된 TimeBonusPerSecond 상수 및 AddTimeBonus() 메서드
- Task 1/2 verify 결과 (grep 출력 요약)
- Plan 02/03이 참조할 정적 API 시그니처 재확인
</output>
