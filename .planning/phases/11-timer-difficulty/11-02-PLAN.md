---
phase: 11-timer-difficulty
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
  - Assets/Scripts/World/EnemySpawner.cs
autonomous: true
requirements:
  - TIMER-01
  - TIMER-02
  - DIFF-01
  - SCORE-01

must_haves:
  truths:
    - "게임 시작(WorldGenerator.Start()) 시 FloorTimer가 60초로 시작된다 (D-08)"
    - "EXIT 포탈 진입(FloorTransitionSequence) 시 이전 층의 남은 시간이 ScoreManager.Score에 누적된 뒤 FloorTimer가 60초로 리셋된다 (D-02b, D-08)"
    - "타이머가 0에 도달하면 다음 프레임 내에 PlayerController.OnPlayerDeath가 발동한다 (TIMER-02, WorldGenerator.Update()가 매 프레임 FloorTimer.Tick() 호출)"
    - "3층 이상에서 스폰되는 (근접+원거리) 적 수가 1층보다 많다 — 기존 FloorSpawner.GetEnemyCount() 계단식 테이블을 그대로 이식 (D-03, DIFF-01)"
    - "새로 Instantiate된 Room의 EnemySpawner 마커가 타입별로 앞에서부터 카운트만큼 즉시 Spawn()+Activate()된다 — 마커 수보다 카운트가 많으면 있는 만큼만 활성화하고 에러를 내지 않는다 (D-04, D-04b)"
    - "EXIT 포탈의 다음 층 대기룸(StandbyRoom)도 Instantiate 시점에 즉시 적이 스폰/활성화된다 — 난이도는 그 방이 실제로 활성화될 미래 층 번호(FloorManager.CurrentFloor + 1) 기준이다"
  artifacts:
    - path: "Assets/Scripts/World/EnemySpawner.cs"
      provides: "_type 필드를 읽을 수 있는 public Type 게터"
      contains: "public EnemyType Type => _type;"
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "GetEnemyCount(floor) + TrySpawnEnemies(room, floor) + FloorTimer/ScoreManager 호출이 통합된 완성 WorldGenerator"
      contains: "private void TrySpawnEnemies(GameObject room, int floor)"
  key_links:
    - from: "WorldGenerator.Start() / SpawnNextPair() / SpawnPrevPair()"
      to: "WorldGenerator.TrySpawnEnemies(room, FloorManager.CurrentFloor)"
      via: "직접 메서드 호출 — TrySpawnExitPortal(room) 호출 직후"
      pattern: "TrySpawnEnemies\\(room, FloorManager\\.CurrentFloor\\)"
    - from: "WorldGenerator.TrySpawnExitPortal()"
      to: "WorldGenerator.TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1)"
      via: "직접 메서드 호출 — standbyRoom Instantiate 직후, SetActive(false) 이전"
      pattern: "TrySpawnEnemies\\(standbyRoom, FloorManager\\.CurrentFloor \\+ 1\\)"
    - from: "WorldGenerator.TrySpawnEnemies()"
      to: "EnemySpawner.Spawn()/Activate()"
      via: "GetComponentsInChildren<EnemySpawner>(true) 순회 + spawner.Type 필터"
      pattern: "GetComponentsInChildren<EnemySpawner>"
    - from: "WorldGenerator.Update()"
      to: "FloorTimer.Tick()"
      via: "매 프레임 첫 줄에서 직접 호출"
      pattern: "FloorTimer\\.Tick\\(\\)"
    - from: "WorldGenerator.FloorTransitionSequence()"
      to: "ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds) → FloorTimer.Reset()"
      via: "코루틴 시작 직후 점수 가산, 새 층 셋업 직후 리셋"
      pattern: "ScoreManager\\.AddTimeBonus\\(FloorTimer\\.RemainingSeconds\\)"
---

<objective>
`Assets/Scripts/World/WorldGenerator.cs`에 Phase 11의 3가지 통합 지점을 추가한다:
1. DIFF-01 — Room이 Instantiate되는 즉시(lookahead/lookbehind/대기룸 포함) 층 번호 기반 난이도 테이블(D-03, FloorSpawner.GetEnemyCount 이식)로 EnemySpawner 마커를 활성화한다(D-04/D-04b).
2. TIMER-01/TIMER-02 — 게임 시작과 층 전환마다 `FloorTimer.Reset()`을 호출하고(D-08), `Update()`에서 매 프레임 `FloorTimer.Tick()`을 호출해 시간 초과 시 사망 이벤트가 발동되게 한다.
3. SCORE-01 — 포탈 진입 순간 `ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds)`를 호출해 남은 시간을 점수로 환산한다(D-02b).

`EnemySpawner.cs`에는 타입 필터링에 필요한 `public EnemyType Type` 게터를 추가한다(기존 `_type`은 private이라 WorldGenerator가 타입별 개수를 셀 방법이 없었다).

Purpose: Plan 01에서 만든 `FloorTimer`/`ScoreManager.AddTimeBonus` API와 이미 완성되어 있던 `EnemySpawner.Spawn()/Activate()` API(현재 아무도 호출하지 않음)를 실제 게임플레이 루프에 연결하는 핵심 통합 작업.
Output: `WorldGenerator.cs`가 TIMER-01/02, DIFF-01, SCORE-01 요구사항을 모두 충족하도록 완성됨.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-timer-difficulty/11-CONTEXT.md

<interfaces>
From Assets/Scripts/World/FloorTimer.cs (Plan 01 output — this plan's consumer contract):
```csharp
public const float Duration = 60f;
public static void Reset();
public static float RemainingSeconds { get; } // Time.unscaledTime 기반
public static void Tick(); // 만료 시 1회 PlayerController.TriggerDeath() 호출
```

From Assets/Scripts/World/ScoreManager.cs (Plan 01 output):
```csharp
public static void AddTimeBonus(float remainingSeconds); // Score += RoundToInt(remainingSeconds) * 10
```

From Assets/Scripts/World/EnemySpawner.cs (현재 전체 — 이 Plan이 Type 게터만 추가):
```csharp
public class EnemySpawner : MonoBehaviour
{
    public enum EnemyType { Melee, Ranged }
    [SerializeField] private EnemyType _type = EnemyType.Melee;
    private GameObject _spawned;
    public void Spawn(GameObject meleePrefab, GameObject rangedPrefab) { ... _spawned.SetActive(false); }
    public void Activate() { if (_spawned != null) _spawned.SetActive(true); }
}
```

From Assets/Scripts/World/FloorSpawner.cs (line 208-217, D-03 이식 원본 — 로직만 복제, 파일 자체는 수정하지 않음):
```csharp
private (int melee, int ranged) GetEnemyCount(int floor)
{
    if (floor <= 5)   return (Random.Range(2, 4), Random.Range(0, 2));
    if (floor <= 10)  return (2, Random.Range(1, 3));
    return (2, Random.Range(2, 4));
}
```

Current Assets/Scripts/World/WorldGenerator.cs relevant excerpt (Phase 10 완성 후, 493줄 — 읽고 전체 상태 확인 필수):
```csharp
    [Header("References (Phase 10 추가)")]
    [SerializeField] private PlayerController _player;
    [SerializeField] private CombatController _combatController;
    // -- 이 Plan이 바로 뒤에 새 Header 블록 삽입 --

    private void Start()
    {
        if (_roomPrefabs == null || _roomPrefabs.Length == 0) { ...; return; }
        // -- 이 Plan이 여기 바로 뒤에 FloorTimer.Reset() 삽입 --
        // 1. 시작 룸 스폰 ...
        TrySpawnExitPortal(startRoom); // EXIT-01
        // -- 이 Plan이 바로 뒤에 TrySpawnEnemies(startRoom, FloorManager.CurrentFloor) 삽입 --
        ...
    }

    private void SpawnNextPair()
    {
        ...
        TrySpawnExitPortal(room);
        // -- 이 Plan이 바로 뒤에 TrySpawnEnemies(room, FloorManager.CurrentFloor) 삽입 --
        _chain.AddLast((room, corridor));
    }

    private void SpawnPrevPair()
    {
        ...
        TrySpawnExitPortal(room);
        // -- 이 Plan이 바로 뒤에 TrySpawnEnemies(room, FloorManager.CurrentFloor) 삽입 --
        ...
    }

    private void TrySpawnExitPortal(GameObject room)
    {
        ...
        var standbyRoom = Instantiate(standbyPrefab, standbyPos, Quaternion.identity);
        // -- 이 Plan이 여기 바로 뒤, SetActive(false) 이전에 TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1) 삽입 --
        standbyRoom.SetActive(false);
        portal.StandbyRoom = standbyRoom;
        ...
    }

    private void Update()
    {
        // -- 이 Plan이 메서드 첫 줄에 FloorTimer.Tick() 삽입 --
        if (_playerTransform == null || _chain.Count == 0) return;
        ...
    }

    private IEnumerator FloorTransitionSequence(ExitPortal portal)
    {
        Debug.Log($"[WorldGenerator] EnterPortal → Floor {FloorManager.CurrentFloor}");
        // -- 이 Plan이 바로 뒤에 ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds) 삽입 --
        _combatController?.ForceExitCombatState();
        _player.LockInput();
        FloorManager.CurrentFloor++;
        ...
        _currentYDrift = 0f; // 새 층은 드리프트 예산 초기화
        // -- 이 Plan이 바로 뒤에 FloorTimer.Reset() 삽입 --
        // Step 2 — ExitSpawnPoint 텔레포트
        ...
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DIFF-01 — EnemySpawner.Type 게터 + WorldGenerator 난이도 스폰 로직</name>
  <files>Assets/Scripts/World/EnemySpawner.cs, Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/EnemySpawner.cs (수정 대상 — 현재 32줄 전체)
    - Assets/Scripts/World/WorldGenerator.cs (수정 대상 — 현재 493줄 전체, 특히 Start()/SpawnNextPair()/SpawnPrevPair()/TrySpawnExitPortal())
    - Assets/Scripts/World/FloorSpawner.cs (GetEnemyCount 이식 원본, line 208-217)
  </read_first>
  <action>
**1. EnemySpawner.cs — public Type 게터 추가** — 기존 `[SerializeField] private EnemyType _type = EnemyType.Melee;` 라인 바로 뒤에 삽입:

```csharp
    public EnemyType Type => _type;
```

**2. WorldGenerator.cs — Inspector 필드 추가** — 기존 `[Header("References (Phase 10 추가)")]` 블록(`_combatController` 필드) 바로 뒤에 삽입:

```csharp

    [Header("Enemy Spawning (Phase 11)")]
    [SerializeField] private GameObject _meleeEnemyPrefab;
    [SerializeField] private GameObject _rangedEnemyPrefab;
```

**3. WorldGenerator.cs — GetEnemyCount() + TrySpawnEnemies() 메서드 추가** — 기존 `TrySpawnExitPortal()` 메서드가 끝나는 닫힘 브레이스(`}`) 바로 뒤, `private void Update()` 바로 앞에 삽입:

```csharp

    /// <summary>
    /// D-03: FloorSpawner.GetEnemyCount(int floor) 원본 로직을 그대로 이식한 계단식 난이도 테이블.
    /// 1~5층: 근접 Random.Range(2,4) + 원거리 Random.Range(0,2)
    /// 6~10층: 근접 2 + 원거리 Random.Range(1,3)
    /// 11층+: 근접 2 + 원거리 Random.Range(2,4)
    /// </summary>
    private (int melee, int ranged) GetEnemyCount(int floor)
    {
        if (floor <= 5)   return (Random.Range(2, 4), Random.Range(0, 2));
        if (floor <= 10)  return (2, Random.Range(1, 3));
        return (2, Random.Range(2, 4));
    }

    /// <summary>
    /// D-04/D-04b: room이 Instantiate된 직후 즉시 호출한다. room의 EnemySpawner 마커를 타입별로
    /// 필터링해 GetEnemyCount(floor) 카운트만큼 앞에서부터 Spawn()+Activate()한다.
    /// 마커 수보다 카운트가 많으면 있는 만큼만 활성화한다(에러 없음, D-04b).
    /// </summary>
    private void TrySpawnEnemies(GameObject room, int floor)
    {
        (int meleeCount, int rangedCount) = GetEnemyCount(floor);
        int meleeSpawned = 0;
        int rangedSpawned = 0;

        foreach (EnemySpawner spawner in room.GetComponentsInChildren<EnemySpawner>(true))
        {
            if (spawner.Type == EnemySpawner.EnemyType.Melee && meleeSpawned < meleeCount)
            {
                spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
                spawner.Activate();
                meleeSpawned++;
            }
            else if (spawner.Type == EnemySpawner.EnemyType.Ranged && rangedSpawned < rangedCount)
            {
                spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
                spawner.Activate();
                rangedSpawned++;
            }
        }
    }
```

**4. Start()에 호출 추가** — 기존 `TrySpawnExitPortal(startRoom); // EXIT-01: 시작 룸도 포탈 스폰 대상 (예외 없음)` 라인 바로 뒤에 추가:

```csharp

        TrySpawnEnemies(startRoom, FloorManager.CurrentFloor); // DIFF-01: 시작 룸 적 스폰
```

**5. SpawnNextPair()에 호출 추가** — 기존 `TrySpawnExitPortal(room);` 라인 바로 뒤에 추가:

```csharp
        TrySpawnEnemies(room, FloorManager.CurrentFloor); // DIFF-01
```

**6. SpawnPrevPair()에 호출 추가** — 기존 `TrySpawnExitPortal(room);` 라인 바로 뒤에 추가:

```csharp
        TrySpawnEnemies(room, FloorManager.CurrentFloor); // DIFF-01
```

**7. TrySpawnExitPortal()의 대기룸 생성부 수정** — 기존:

```csharp
        var standbyRoom = Instantiate(standbyPrefab, standbyPos, Quaternion.identity);
        standbyRoom.SetActive(false);
        portal.StandbyRoom = standbyRoom;
```

다음으로 교체:

```csharp
        var standbyRoom = Instantiate(standbyPrefab, standbyPos, Quaternion.identity);
        TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1); // DIFF-01: 대기룸은 활성화될 미래 층 기준 난이도
        standbyRoom.SetActive(false);
        portal.StandbyRoom = standbyRoom;
```

금지 사항:
- `FindObjectsOfType` 금지 (ROADMAP Stack Constraints)
- `GetComponentsInChildren<EnemySpawner>()` 호출 시 `includeInactive: true` 생략 금지 (방어적 일관성 — Pitfall 2 컨벤션)
- `GetEnemyCount()`의 숫자 테이블(2,4 / 0,2 / 2 / 1,3 / 2,4 / 5 / 10)을 임의로 바꾸지 말 것 — D-03이 명시적으로 "신규 설계 없음, 그대로 재사용"을 요구한다
  </action>
  <verify>
    <automated>grep -n "public EnemyType Type => _type;" "Assets/Scripts/World/EnemySpawner.cs"; grep -n "_meleeEnemyPrefab\|_rangedEnemyPrefab\|private (int melee, int ranged) GetEnemyCount\|private void TrySpawnEnemies\|TrySpawnEnemies(startRoom\|TrySpawnEnemies(room, FloorManager.CurrentFloor)\|TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor \+ 1)" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "public EnemyType Type => _type;" 라인이 EnemySpawner.cs에 존재한다
    - "[SerializeField] private GameObject _meleeEnemyPrefab;"과 "[SerializeField] private GameObject _rangedEnemyPrefab;"이 WorldGenerator.cs에 존재한다
    - "private (int melee, int ranged) GetEnemyCount(int floor)" 메서드가 존재하고 "floor <= 5", "floor <= 10" 분기와 Random.Range 인자값(2,4 / 0,2 / 1,3 / 2,4)이 FloorSpawner.cs 원본과 동일하다
    - "private void TrySpawnEnemies(GameObject room, int floor)" 메서드가 존재하고 "GetComponentsInChildren<EnemySpawner>(true)"를 사용한다
    - "TrySpawnEnemies(startRoom, FloorManager.CurrentFloor);" 호출이 Start() 안에 존재한다
    - "TrySpawnEnemies(room, FloorManager.CurrentFloor);" 호출이 SpawnNextPair()와 SpawnPrevPair() 양쪽에 각각 존재한다 (총 2회)
    - "TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1);" 호출이 TrySpawnExitPortal() 안에서 "standbyRoom.SetActive(false);" 라인보다 먼저 실행된다
  </acceptance_criteria>
  <done>WorldGenerator가 Room을 Instantiate하는 4개 지점(시작 룸/lookahead/lookbehind/대기룸) 모두에서 즉시 층 번호 기반 난이도 테이블(D-03)로 EnemySpawner 마커를 Spawn()+Activate()한다. 대기룸은 자신이 실제로 활성화될 미래 층(FloorManager.CurrentFloor + 1) 기준으로 난이도가 계산된다. DIFF-01이 코드 레벨에서 충족된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TIMER-01/TIMER-02/SCORE-01 — FloorTimer 리셋/틱 + ScoreManager 점수 가산 배선</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (Task 1 완료 후 현재 상태 — 삽입 위치 확인)
    - Assets/Scripts/World/FloorTimer.cs (Plan 01 산출물 — Reset()/RemainingSeconds/Tick() 시그니처)
    - Assets/Scripts/World/ScoreManager.cs (Plan 01 산출물 — AddTimeBonus(float) 시그니처)
  </read_first>
  <action>
**1. Start() — 게임 시작 시 60초 타이머 시작 (D-08)** — 기존:

```csharp
    private void Start()
    {
        if (_roomPrefabs == null || _roomPrefabs.Length == 0)
        {
            Debug.LogError("[WorldGenerator] _roomPrefabs is empty — Inspector에서 할당 필요");
            return;
        }

        // 1. 시작 룸 스폰 — 반드시 Vector3.zero 기준 (Pitfall 2: AlignByEntry 공식 성립 조건)
```

다음으로 교체 (null 체크와 "1. 시작 룸 스폰" 주석 사이에 한 줄 삽입):

```csharp
    private void Start()
    {
        if (_roomPrefabs == null || _roomPrefabs.Length == 0)
        {
            Debug.LogError("[WorldGenerator] _roomPrefabs is empty — Inspector에서 할당 필요");
            return;
        }

        FloorTimer.Reset(); // D-08(TIMER-01): 게임 시작 시 60초 타이머 시작

        // 1. 시작 룸 스폰 — 반드시 Vector3.zero 기준 (Pitfall 2: AlignByEntry 공식 성립 조건)
```

**2. Update() — 매 프레임 타이머 만료 체크 (TIMER-02)** — 기존:

```csharp
    private void Update()
    {
        if (_playerTransform == null || _chain.Count == 0) return;
```

다음으로 교체 (메서드 첫 줄에 삽입 — chain/player 상태와 무관하게 항상 체크되어야 하므로 early-return 가드보다 앞에 위치):

```csharp
    private void Update()
    {
        FloorTimer.Tick(); // TIMER-02: 만료 시 1회 PlayerController.OnPlayerDeath 발동

        if (_playerTransform == null || _chain.Count == 0) return;
```

**3. FloorTransitionSequence() — 점수 가산 + 타이머 리셋 (SCORE-01, TIMER-01)** — 기존:

```csharp
    private IEnumerator FloorTransitionSequence(ExitPortal portal)
    {
        Debug.Log($"[WorldGenerator] EnterPortal → Floor {FloorManager.CurrentFloor}");

        // Step 1 — 입력 잠금 (D-04 6단계 중 1단계). ForceExitCombatState는 LockInput 이전에 호출해야 한다.
```

다음으로 교체 (Debug.Log 직후, Step 1 주석 이전에 삽입 — 층이 바뀌기 전, 이전 층의 남은 시간을 캡처해야 하므로 반드시 이 위치):

```csharp
    private IEnumerator FloorTransitionSequence(ExitPortal portal)
    {
        Debug.Log($"[WorldGenerator] EnterPortal → Floor {FloorManager.CurrentFloor}");

        ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds); // D-02b(SCORE-01): 진입 순간 남은 시간을 점수로 환산

        // Step 1 — 입력 잠금 (D-04 6단계 중 1단계). ForceExitCombatState는 LockInput 이전에 호출해야 한다.
```

그리고 같은 코루틴 내 기존:

```csharp
        _currentYDrift = 0f; // 새 층은 드리프트 예산 초기화

        // Step 2 — ExitSpawnPoint 텔레포트 (10-TRANSITION-DESIGN.md 결정 — 포탈 스폰과 동일 마커 재사용)
```

다음으로 교체 (드리프트 초기화 직후, Step 2 텔레포트 이전에 삽입 — 새 층의 60초 카운트다운이 텔레포트/카메라 스냅보다 먼저 시작되어야 함):

```csharp
        _currentYDrift = 0f; // 새 층은 드리프트 예산 초기화
        FloorTimer.Reset(); // D-08(TIMER-01): 새 층 60초 리셋

        // Step 2 — ExitSpawnPoint 텔레포트 (10-TRANSITION-DESIGN.md 결정 — 포탈 스폰과 동일 마커 재사용)
```

금지 사항:
- `ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds)` 호출을 `FloorTimer.Reset()`보다 뒤에 두지 말 것 — 순서가 바뀌면 이미 리셋된(60에 가까운) 값이 점수화되어 D-02b 위반
- `FloorTimer.Tick()`을 `if (_playerTransform == null || _chain.Count == 0) return;` 가드 뒤에 두지 말 것 — 체인이 비어있는 특수 상황에서도 타이머 만료 체크는 항상 동작해야 한다
  </action>
  <verify>
    <automated>grep -n "FloorTimer.Reset()\|FloorTimer.Tick()\|ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds)" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "FloorTimer.Reset();" 호출이 Start() 안에서 "_roomPrefabs is empty" 에러 반환 이후, "1. 시작 룸 스폰" 주석 이전에 위치한다
    - "FloorTimer.Tick();" 호출이 Update() 메서드의 첫 실행문이다 (early-return 가드보다 앞)
    - "ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds);" 호출이 FloorTransitionSequence() 안에서 "EnterPortal → Floor" Debug.Log 직후, "FloorManager.CurrentFloor++;" 이전에 위치한다
    - "FloorTimer.Reset();" 호출이 FloorTransitionSequence() 안에서 "_currentYDrift = 0f;" 직후, ExitSpawnPoint 텔레포트(Step 2) 이전에 위치한다 (Start()의 Reset() 호출과 합쳐 총 2회 등장)
  </acceptance_criteria>
  <done>게임 시작과 층 전환마다 FloorTimer가 60초로 리셋되고(TIMER-01), Update()가 매 프레임 만료를 체크해 시간 초과 시 사망 이벤트를 발동하며(TIMER-02), 포탈 진입 순간 이전 층의 남은 시간이 정확히 점수로 환산된다(SCORE-01, D-02b 순서 보존).</done>
</task>

</tasks>

<verification>
Unity Editor에서 WorldGenerator.cs/EnemySpawner.cs를 열었을 때 컴파일 에러가 0개여야 한다 (Plan 01의 FloorTimer.cs/ScoreManager.AddTimeBonus가 이미 존재하므로 이 시점부터 전체 빌드가 통과해야 정상).
grep으로 "TrySpawnEnemies\(" 호출이 WorldGenerator.cs 안에 정확히 4회(Start/SpawnNextPair/SpawnPrevPair/TrySpawnExitPortal) 등장해야 한다.
</verification>

<success_criteria>
- WorldGenerator.cs가 컴파일 에러 없이 DIFF-01/TIMER-01/TIMER-02/SCORE-01을 모두 구현한다
- Room이 Instantiate되는 모든 지점(시작/lookahead/lookbehind/대기룸)에서 즉시 난이도 테이블 기반 적 스폰이 일어난다
- FloorTimer가 게임 시작과 층 전환마다 정확히 리셋되고, 매 프레임 만료가 체크된다
- 층 전환 시 이전 층의 남은 시간이 리셋 전에 정확히 점수화된다
</success_criteria>

<output>
완료 후 `.planning/phases/11-timer-difficulty/11-02-SUMMARY.md` 생성:
- EnemySpawner.cs에 추가된 Type 게터
- WorldGenerator.cs에 추가된 필드/메서드 목록 (_meleeEnemyPrefab, _rangedEnemyPrefab, GetEnemyCount, TrySpawnEnemies)
- 4개 호출 지점(Start/SpawnNextPair/SpawnPrevPair/TrySpawnExitPortal) 확인 결과
- FloorTimer.Reset()/Tick(), ScoreManager.AddTimeBonus() 배선 위치 및 순서 확인 결과 (Task 1/2 verify 출력 요약)
</output>
