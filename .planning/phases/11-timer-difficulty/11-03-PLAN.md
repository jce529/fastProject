---
phase: 11-timer-difficulty
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - Assets/Scripts/UI/HUDController.cs
autonomous: true
requirements:
  - TIMER-01
  - SCORE-02

must_haves:
  truths:
    - "HUD에 남은 시간이 정수 초 단위로 실시간 표시된다 (TIMER-01)"
    - "남은 시간이 임계값(20초) 이하로 줄어들면 타이머 텍스트가 흰색→빨간색으로 점멸하기 시작하고, 0초에 가까워질수록 점멸 간격이 짧아진다(D-05)"
    - "HUD에 ScoreManager.Score가 실시간으로 표시된다 — 기존 _scoreLabel 슬롯이 이미 이 역할을 수행 중이며 이 Plan에서 추가 변경이 필요 없음을 확인한다 (SCORE-02)"
  artifacts:
    - path: "Assets/Scripts/UI/HUDController.cs"
      provides: "_timerLabel 슬롯 + FloorTimer 표시 Update() 로직 + TimerFlickerLoop() 코루틴"
      contains: "private IEnumerator TimerFlickerLoop()"
  key_links:
    - from: "HUDController.Update()"
      to: "FloorTimer.RemainingSeconds"
      via: "_timerLabel.SetText(\"{0}\", Mathf.CeilToInt(FloorTimer.RemainingSeconds))"
      pattern: "FloorTimer\\.RemainingSeconds"
    - from: "HUDController.TimerFlickerLoop()"
      to: "_timerLabel.color"
      via: "코루틴 루프 — WaitForSecondsRealtime(interval) 간격이 FloorTimer.RemainingSeconds에 반비례"
      pattern: "TimerFlickerLoop"
---

<objective>
`HUDController.cs`에 남은 층 제한시간을 표시하는 `_timerLabel` 슬롯을 추가하고(TIMER-01), `InvincibilityHandler.cs`의 코루틴 플리커 패턴을 참고해 남은 시간이 줄어들수록 점점 빠르게 흰색↔빨간색으로 점멸하는 경고 연출을 구현한다(D-05). 기존 `_scoreLabel`이 이미 `ScoreManager.Score`를 실시간 표시하고 있음을 확인해 SCORE-02를 충족 상태로 마감한다.

Purpose: Plan 01(FloorTimer)이 노출한 `RemainingSeconds` 정적 프로퍼티를 HUD에 연결해 플레이어가 남은 시간을 인지하고, 임박 시 시각적 긴장감(D-05, 사용자 원문: "남은 시간이 적을수록 점점 빠르게 점멸했으면 좋겠어 빨간색으로")을 제공한다.
Output: `HUDController.cs`가 TIMER-01/SCORE-02 요구사항을 모두 충족하도록 확장됨. Plan 02(WorldGenerator)와 독립적으로 동작 — 이 Plan은 `FloorTimer.RemainingSeconds`만 읽고 `FloorTimer.Tick()`은 호출하지 않는다(사망 발동은 WorldGenerator.Update()의 책임, UI는 표시만 담당).
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-timer-difficulty/11-CONTEXT.md

<interfaces>
From Assets/Scripts/World/FloorTimer.cs (Plan 01 output — this plan only reads RemainingSeconds, never calls Tick()/Reset()):
```csharp
public const float Duration = 60f;
public static float RemainingSeconds { get; } // Time.unscaledTime 기반, 0 이하로 내려가지 않음(Mathf.Max clamp)
```

From Assets/Scripts/Player/InvincibilityHandler.cs (코루틴 플리커 패턴 — 참고용, 이 Plan이 그대로 복사하지 않고 "가변 간격" 버전으로 각색):
```csharp
private IEnumerator InvincibilityCoroutine(float duration)
{
    float elapsed = 0f;
    while (elapsed < duration)
    {
        _spriteRenderer.enabled = !_spriteRenderer.enabled;
        yield return new WaitForSecondsRealtime(flickerInterval);
        elapsed += flickerInterval;
    }
}
```

Current Assets/Scripts/UI/HUDController.cs (전체, 35줄 — 수정 대상):
```csharp
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class HUDController : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI _floorLabel;
    [SerializeField] private TextMeshProUGUI _scoreLabel;
    [SerializeField] private Image           _gaugeFill;
    [SerializeField] private TextMeshProUGUI _attackTypeLabel;
    [SerializeField] private ChronoGaugeController _gauge;

    private AttackType _lastType = (AttackType)(-1);

    private void Start()
    {
        if (_gauge == null)
            _gauge = FindFirstObjectByType<ChronoGaugeController>();
    }

    private void Update()
    {
        _floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
        _scoreLabel?.SetText("{0}", ScoreManager.Score);
        if (_gauge != null)
            _gaugeFill.fillAmount = _gauge.Value;

        AttackType t = AttackTypeSelector.Selected;
        if (t != _lastType)
        {
            _lastType = t;
            _attackTypeLabel.SetText(t == AttackType.Linear ? "LINEAR" : "FAN");
        }
    }
}
```

Note: `_scoreLabel?.SetText("{0}", ScoreManager.Score);`가 이미 존재한다 (SCORE-02는 이 한 줄로 이미 충족됨 — 이 Plan에서 이 줄은 변경하지 않는다).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: _timerLabel 슬롯 + Update() 표시 로직 (TIMER-01) + SCORE-02 확인</name>
  <files>Assets/Scripts/UI/HUDController.cs</files>
  <read_first>
    - Assets/Scripts/UI/HUDController.cs (수정 대상 — 현재 35줄 전체 상태 확인 필수)
    - Assets/Scripts/World/FloorTimer.cs (Plan 01 산출물 — RemainingSeconds 시그니처)
  </read_first>
  <action>
`Assets/Scripts/UI/HUDController.cs`를 다음과 같이 수정한다.

**1. using 추가** — 파일 최상단, 기존 `using TMPro;` 바로 앞에 삽입:

```csharp
using System.Collections;
```

**2. 필드 추가** — 기존 `[SerializeField] private ChronoGaugeController _gauge;` 라인 바로 뒤에 삽입:

```csharp
    [SerializeField] private TextMeshProUGUI _timerLabel;
```

**3. Update()에 타이머 표시 추가** — 기존:

```csharp
    private void Update()
    {
        _floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
        _scoreLabel?.SetText("{0}", ScoreManager.Score);
```

다음으로 교체 (기존 두 줄은 그대로 두고, 그 사이에 타이머 표시 한 줄만 추가 — SCORE-02 담당 줄인 `_scoreLabel?.SetText` 자체는 절대 수정하지 않는다):

```csharp
    private void Update()
    {
        _floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
        _timerLabel?.SetText("{0}", Mathf.CeilToInt(FloorTimer.RemainingSeconds)); // TIMER-01
        _scoreLabel?.SetText("{0}", ScoreManager.Score); // SCORE-02: 기존 코드 — 변경 없음
```

금지 사항:
- `_scoreLabel?.SetText("{0}", ScoreManager.Score);` 라인의 위치/내용을 변경하지 말 것 (SCORE-02는 이미 충족되어 있으므로 순수 확인 대상)
- `FloorTimer.Tick()`을 이 파일 어디에도 호출하지 말 것 — 사망 발동은 WorldGenerator.Update()의 책임(Plan 02)이며, HUD는 순수 표시 전용이다
  </action>
  <verify>
    <automated>grep -n "using System.Collections;\|_timerLabel\|FloorTimer.RemainingSeconds\|_scoreLabel?.SetText" "Assets/Scripts/UI/HUDController.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "using System.Collections;" 라인이 파일 상단 using 블록에 존재한다
    - "[SerializeField] private TextMeshProUGUI _timerLabel;" 라인이 존재한다
    - "_timerLabel?.SetText(\"{0}\", Mathf.CeilToInt(FloorTimer.RemainingSeconds));" 라인이 Update() 안에 존재한다
    - "_scoreLabel?.SetText(\"{0}\", ScoreManager.Score);" 라인이 변경 없이 그대로 존재한다 (SCORE-02 이미 충족 확인)
    - "FloorTimer.Tick()" 문자열이 이 파일에 존재하지 않는다 (HUD는 표시 전용, 사망 발동 로직 없음 확인)
  </acceptance_criteria>
  <done>HUD가 FloorTimer.RemainingSeconds를 정수 초 단위로 실시간 표시한다(TIMER-01). 기존 _scoreLabel 표시 로직이 그대로 보존되어 SCORE-02가 이미 충족 상태임을 코드로 확인했다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TimerFlickerLoop() — 남은 시간 반비례 점멸 경고 (D-05)</name>
  <files>Assets/Scripts/UI/HUDController.cs</files>
  <read_first>
    - Assets/Scripts/UI/HUDController.cs (Task 1 완료 후 현재 상태 — 삽입 위치 확인)
    - Assets/Scripts/Player/InvincibilityHandler.cs (코루틴 + WaitForSecondsRealtime 간격 토글 패턴, 참고 원본)
  </read_first>
  <action>
**1. 필드 추가** — 기존 `[SerializeField] private TextMeshProUGUI _timerLabel;` 라인 바로 뒤에 삽입:

```csharp

    [Header("Timer Flicker (D-05)")]
    [SerializeField] private float _flickerThreshold  = 20f;  // 이 값(초) 이하로 남았을 때만 점멸 시작
    [SerializeField] private float _minFlickerInterval = 0.08f; // 0초에 가까울 때 (가장 빠름)
    [SerializeField] private float _maxFlickerInterval = 0.4f;  // threshold 진입 시점 (가장 느림)

    private bool _flickerRed;
```

**2. Start()에 코루틴 시작 추가** — 기존:

```csharp
    private void Start()
    {
        if (_gauge == null)
            _gauge = FindFirstObjectByType<ChronoGaugeController>();
    }
```

다음으로 교체:

```csharp
    private void Start()
    {
        if (_gauge == null)
            _gauge = FindFirstObjectByType<ChronoGaugeController>();

        StartCoroutine(TimerFlickerLoop());
    }
```

**3. TimerFlickerLoop() 코루틴 추가** — 클래스 닫힘 브레이스(`}`) 바로 앞, `Update()` 메서드 뒤에 삽입:

```csharp

    /// <summary>
    /// D-05: 남은 시간이 _flickerThreshold 이하로 줄어들면 _timerLabel 색상을 흰색↔빨간색으로 토글한다.
    /// 토글 간격은 남은 시간에 비례(초반엔 느리게, 0에 가까워질수록 빠르게) — InvincibilityHandler의
    /// 코루틴 + WaitForSecondsRealtime 패턴을 참고하되, 간격을 매 사이클 재계산하는 가변 버전이다.
    /// </summary>
    private IEnumerator TimerFlickerLoop()
    {
        while (true)
        {
            if (_timerLabel == null)
            {
                yield return new WaitForSecondsRealtime(0.2f);
                continue;
            }

            float remaining = FloorTimer.RemainingSeconds;

            if (remaining > _flickerThreshold)
            {
                _timerLabel.color = Color.white;
                yield return new WaitForSecondsRealtime(0.2f);
                continue;
            }

            _flickerRed = !_flickerRed;
            _timerLabel.color = _flickerRed ? Color.red : Color.white;

            float interval = Mathf.Lerp(_minFlickerInterval, _maxFlickerInterval, Mathf.Clamp01(remaining / _flickerThreshold));
            yield return new WaitForSecondsRealtime(interval);
        }
    }
```

금지 사항:
- `WaitForSeconds` 사용 금지 — 반드시 `WaitForSecondsRealtime` (슬로우모션 Time.timeScale 면역 필수, ROADMAP Stack Constraints)
- 코루틴이 `while (true)`로 영구 루프이므로 별도 정지 로직을 추가하지 말 것 — HUD GameObject는 씬 전체 수명 동안 유지되므로 자연 종료(OnDestroy 시 코루틴 자동 중단)로 충분하다
  </action>
  <verify>
    <automated>grep -n "private IEnumerator TimerFlickerLoop\|StartCoroutine(TimerFlickerLoop())\|WaitForSecondsRealtime\|Mathf.Lerp(_minFlickerInterval, _maxFlickerInterval" "Assets/Scripts/UI/HUDController.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "private IEnumerator TimerFlickerLoop()" 메서드가 존재한다
    - "StartCoroutine(TimerFlickerLoop());"가 Start() 안에 존재한다
    - TimerFlickerLoop() 본문에 "remaining > _flickerThreshold" 분기와 "_timerLabel.color = Color.white;"가 존재한다 (임계값 이상에서는 점멸 없음)
    - TimerFlickerLoop() 본문에 "_flickerRed = !_flickerRed;"와 "_timerLabel.color = _flickerRed ? Color.red : Color.white;"가 존재한다
    - "Mathf.Lerp(_minFlickerInterval, _maxFlickerInterval, Mathf.Clamp01(remaining / _flickerThreshold))"가 존재한다 (간격이 남은 시간에 비례 — remaining이 작을수록 interval이 _minFlickerInterval에 가까워짐)
    - "WaitForSeconds(" (Realtime 아닌 버전) 문자열이 이 파일에 존재하지 않는다
  </acceptance_criteria>
  <done>남은 시간이 20초(기본값) 이하로 줄어들면 타이머 텍스트가 흰색↔빨간색으로 점멸하기 시작하고, 0초에 가까워질수록 점멸 간격이 0.4초에서 0.08초로 점점 짧아진다(D-05). 20초 초과 구간에서는 항상 흰색으로 고정된다.</done>
</task>

</tasks>

<verification>
Unity Editor에서 HUDController.cs를 열었을 때 컴파일 에러가 0개여야 한다 (Plan 01의 FloorTimer.cs가 이미 존재하므로 이 시점부터 빌드가 통과해야 정상 — 이 Plan은 Plan 02와 독립적으로 실행 가능하다).
grep으로 "_scoreLabel?.SetText" 라인이 정확히 1회, 변경 없이 존재해야 한다 (SCORE-02 회귀 방지).
</verification>

<success_criteria>
- HUDController.cs가 컴파일 에러 없이 TIMER-01(타이머 표시)과 D-05(점멸 경고)를 구현한다
- SCORE-02(점수 실시간 표시)가 기존 코드 그대로 보존되어 회귀 없음을 확인했다
- HUD 표시 로직이 WorldGenerator(Plan 02)의 사망 발동 로직과 완전히 분리되어 있다 (UI는 FloorTimer.RemainingSeconds만 읽는다)
</success_criteria>

<output>
완료 후 `.planning/phases/11-timer-difficulty/11-03-SUMMARY.md` 생성:
- HUDController.cs에 추가된 필드/메서드 목록 (_timerLabel, _flickerThreshold/_minFlickerInterval/_maxFlickerInterval, TimerFlickerLoop)
- Task 1/2 verify 결과 (grep 출력 요약)
- SCORE-02 기존 코드 보존 확인 결과
</output>
