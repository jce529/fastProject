---
phase: 11-timer-difficulty
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified:
  - Assets/Editor/HUDTimerLabelBuilder.cs
  - Assets/Scenes/SampleScene.unity
autonomous: false
requirements:
  - TIMER-01
  - TIMER-02
  - DIFF-01
  - SCORE-01
  - SCORE-02

must_haves:
  truths:
    - "SampleScene의 HUDController GameObject에 _timerLabel이 연결되어 있고 Game 뷰에 남은 시간이 표시된다"
    - "SampleScene의 WorldGenerator GameObject에 _meleeEnemyPrefab(MeleeEnemy.prefab), _rangedEnemyPrefab(RangedEnemy.prefab)이 연결되어 있다"
    - "층 진입 시 타이머가 60에서 카운트다운하고, 슬로우모션 중에도 실시간으로 감소한다 (TIMER-01)"
    - "남은 시간 20초 이하부터 타이머 텍스트가 빨간색으로 점점 빠르게 점멸한다 (D-05)"
    - "타이머가 0에 도달하면 사망 화면이 표시된다 (TIMER-02)"
    - "3층 이상에서 스폰되는 적 수가 1층보다 육안으로 확인될 만큼 많다 (DIFF-01)"
    - "EXIT 포탈 진입 시 HUD 점수가 남은 시간 × 10만큼 증가한다 (SCORE-01, SCORE-02)"
  artifacts:
    - path: "Assets/Editor/HUDTimerLabelBuilder.cs"
      provides: "Fast/Phase11/Add Timer Label To HUD 메뉴 — TimerLabel TextMeshProUGUI 생성 + HUDController._timerLabel 자동 연결"
      contains: "MenuItem(\"Fast/Phase11/Add Timer Label To HUD\")"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "HUDController._timerLabel + WorldGenerator._meleeEnemyPrefab/_rangedEnemyPrefab이 모두 연결된 씬"
      contains: "HUDController"
  key_links:
    - from: "SampleScene HUDController GameObject"
      to: "TimerLabel TextMeshProUGUI (신규 생성)"
      via: "HUDTimerLabelBuilder.Run() 메뉴 실행 → SerializedObject로 _timerLabel 자동 연결"
      pattern: "TimerLabel"
    - from: "SampleScene WorldGenerator GameObject"
      to: "Assets/Prefabs/Enemies/MeleeEnemy.prefab, Assets/Prefabs/Enemies/RangedEnemy.prefab"
      via: "Inspector _meleeEnemyPrefab/_rangedEnemyPrefab 필드 드래그 연결"
      pattern: "Inspector에서 MeleeEnemy.prefab/RangedEnemy.prefab 드래그"
---

<objective>
`HUDTimerLabelBuilder.cs` 에디터 도구를 신규 생성해 SampleScene의 기존 `HUDController` HUD Canvas에 `TimerLabel` TextMeshProUGUI를 자동으로 생성하고 `_timerLabel` 필드에 연결한다(TIMER-01 화면 배선). 이어서 사용자가 Unity Editor에서 해당 메뉴를 실행하고, `WorldGenerator`의 신규 필드 `_meleeEnemyPrefab`/`_rangedEnemyPrefab`을 기존 `Assets/Prefabs/Enemies/MeleeEnemy.prefab`/`RangedEnemy.prefab`으로 수동 연결한 뒤, TIMER-01/TIMER-02/DIFF-01/SCORE-01/SCORE-02 성공 기준을 플레이테스트로 확인한다.

Purpose: Plan 01~03에서 완성된 코드가 실제 씬에서 의도대로 동작하는지 검증하는 최종 게이트. Phase 10의 10-03/10-04 플랜과 동일한 검증 스타일(에디터 도구 자동 생성 → Inspector 연결 checkpoint → 플레이테스트 checkpoint)을 따른다.
Output: TIMER-01/TIMER-02/DIFF-01/SCORE-01/SCORE-02가 모두 충족된 상태로 확인된 SampleScene. Phase 11 완료 선언 가능 상태.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-timer-difficulty/11-CONTEXT.md

<interfaces>
From Assets/Scripts/UI/HUDController.cs (Plan 03 output — this task's SerializedProperty target):
```csharp
[SerializeField] private TextMeshProUGUI _scoreLabel;   // 기존 필드 — 템플릿으로 복제할 대상
[SerializeField] private TextMeshProUGUI _timerLabel;   // Plan 03이 추가한 신규 필드 — 이 Plan이 연결
```

From Assets/Scripts/World/WorldGenerator.cs (Plan 02 output — Inspector 연결 대상):
```csharp
[SerializeField] private GameObject _meleeEnemyPrefab;
[SerializeField] private GameObject _rangedEnemyPrefab;
```

Existing enemy prefab paths (Assets/Prefabs/Enemies/ — 이미 존재, FloorSpawner Inspector에서도 동일 프리팹 사용 중):
- Assets/Prefabs/Enemies/MeleeEnemy.prefab
- Assets/Prefabs/Enemies/RangedEnemy.prefab

From Assets/Editor/ExitPortalBuilder.cs (SavePrefab/MenuItem 컨벤션 — 이 Plan이 SerializedObject 연결 방식으로 확장):
```csharp
[MenuItem("Fast/Phase10/Build Exit Portal Prefab")]
public static void Run() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HUDTimerLabelBuilder.cs — TimerLabel 자동 생성 + HUDController 연결 에디터 도구</name>
  <files>Assets/Editor/HUDTimerLabelBuilder.cs</files>
  <read_first>
    - Assets/Scripts/UI/HUDController.cs (Plan 03 완료 후 — _scoreLabel/_timerLabel 필드 확인)
    - Assets/Editor/ExitPortalBuilder.cs (MenuItem + Debug.Log 컨벤션)
  </read_first>
  <action>
`Assets/Editor/HUDTimerLabelBuilder.cs`를 신규 생성한다. 이 메뉴 실행 시 현재 열려 있는 씬에서 `HUDController`를 찾고, 기존 `_scoreLabel` GameObject를 템플릿으로 복제해 `TimerLabel`을 만든 뒤 `_timerLabel` 필드에 자동 연결한다.

```csharp
using TMPro;
using UnityEditor;
using UnityEditor.SceneManagement;
using UnityEngine;

/// <summary>
/// Menu: Fast/Phase11/Add Timer Label To HUD
/// 현재 열려 있는 씬의 HUDController를 찾아 기존 _scoreLabel을 템플릿으로 TimerLabel
/// TextMeshProUGUI를 같은 부모 아래 복제 생성하고, HUDController._timerLabel 필드에
/// SerializedObject로 자동 연결한다 (TIMER-01).
/// </summary>
public static class HUDTimerLabelBuilder
{
    [MenuItem("Fast/Phase11/Add Timer Label To HUD")]
    public static void Run()
    {
        var hud = Object.FindFirstObjectByType<HUDController>(FindObjectsInactive.Include);
        if (hud == null)
        {
            Debug.LogError("[HUDTimerLabelBuilder] 씬에서 HUDController를 찾을 수 없습니다.");
            return;
        }

        var so = new SerializedObject(hud);
        var scoreLabelProp = so.FindProperty("_scoreLabel");
        var timerLabelProp = so.FindProperty("_timerLabel");

        if (timerLabelProp == null)
        {
            Debug.LogError("[HUDTimerLabelBuilder] HUDController에 _timerLabel 필드가 없습니다. Plan 11-03 완료 후 실행하세요.");
            return;
        }

        var scoreLabel = scoreLabelProp.objectReferenceValue as TextMeshProUGUI;
        if (scoreLabel == null)
        {
            Debug.LogError("[HUDTimerLabelBuilder] _scoreLabel이 연결되어 있지 않습니다. 먼저 ScoreLabel을 연결하세요.");
            return;
        }

        if (timerLabelProp.objectReferenceValue != null)
        {
            Debug.Log("[HUDTimerLabelBuilder] _timerLabel이 이미 연결되어 있습니다 — 건너뜁니다.");
            return;
        }

        var timerGO = Object.Instantiate(scoreLabel.gameObject, scoreLabel.transform.parent);
        timerGO.name = "TimerLabel";

        var timerRect = timerGO.GetComponent<RectTransform>();
        var scoreRect  = scoreLabel.GetComponent<RectTransform>();
        timerRect.anchoredPosition = scoreRect.anchoredPosition + new Vector2(0f, -50f);

        var timerTmp = timerGO.GetComponent<TextMeshProUGUI>();
        timerTmp.SetText("{0}", (int)FloorTimer.Duration);
        timerTmp.color = Color.white;

        timerLabelProp.objectReferenceValue = timerTmp;
        so.ApplyModifiedProperties();

        EditorUtility.SetDirty(hud);
        EditorSceneManager.MarkSceneDirty(hud.gameObject.scene);

        Debug.Log("[HUDTimerLabelBuilder] TimerLabel 생성 및 HUDController._timerLabel 연결 완료.");
    }
}
```

Note: 이 Task는 스크립트 생성만 한다 — 실제로 메뉴를 실행해 씬을 변경하는 작업은 Task 2(사용자 수동 실행)에서 진행한다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(.Fast/Phase11/Add Timer Label To HUD.)\|FindFirstObjectByType<HUDController>\|_scoreLabel\|_timerLabel\|SerializedObject\|MarkSceneDirty" "Assets/Editor/HUDTimerLabelBuilder.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/HUDTimerLabelBuilder.cs가 존재한다
    - "[MenuItem(\"Fast/Phase11/Add Timer Label To HUD\")]"가 존재한다
    - "var so = new SerializedObject(hud);"와 "so.FindProperty(\"_timerLabel\")"이 존재한다
    - "timerLabelProp.objectReferenceValue = timerTmp;"와 "so.ApplyModifiedProperties();"가 존재한다
    - "EditorSceneManager.MarkSceneDirty"가 존재한다 (씬 변경사항이 저장 가능 상태로 표시됨)
  </acceptance_criteria>
  <done>HUDTimerLabelBuilder.cs가 존재하며, Unity Editor에서 메뉴를 실행하면 TimerLabel이 자동 생성되고 HUDController._timerLabel에 연결될 준비가 되었다 (Task 2에서 실제 실행).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: SampleScene — TimerLabel 생성 실행 + WorldGenerator 적 프리팹 Inspector 연결</name>
  <read_first>
    - Assets/Editor/HUDTimerLabelBuilder.cs (Task 1에서 생성 — 메뉴 경로 확인)
    - Assets/Scripts/World/WorldGenerator.cs (Plan 02 산출물 — _meleeEnemyPrefab/_rangedEnemyPrefab 필드 확인)
  </read_first>
  <what-built>
Task 1에서 `HUDTimerLabelBuilder.cs` 에디터 스크립트가 생성되었다. Plan 02가 `WorldGenerator`에 `_meleeEnemyPrefab`/`_rangedEnemyPrefab` Inspector 필드를 추가했다. 이제 Unity Editor에서 메뉴를 실행해 HUD에 타이머 라벨을 만들고, WorldGenerator에 적 프리팹을 연결한다.
  </what-built>
  <how-to-verify>
1. `Assets/Scenes/SampleScene.unity`를 연다. 컴파일이 완료될 때까지 기다린다 (Console에 에러가 없어야 한다 — Plan 01~03이 모두 완료되어 있어야 정상)
2. 상단 메뉴에서 `Fast > Phase11 > Add Timer Label To HUD` 클릭
3. Console 창(Window > General > Console)에서 다음 로그를 확인한다:
   - `[HUDTimerLabelBuilder] TimerLabel 생성 및 HUDController._timerLabel 연결 완료.`
   - Error가 없어야 한다
4. Hierarchy에서 HUDController가 붙은 Canvas 하위에 `TimerLabel` GameObject가 새로 생성되었는지 확인한다
5. HUDController GameObject를 선택 → Inspector에서 `_timerLabel` 필드에 `TimerLabel`이 자동 연결되어 있는지 확인한다
6. Hierarchy에서 `WorldGenerator` GameObject를 선택한다
7. Inspector의 `Enemy Spawning (Phase 11)` 헤더 아래에서:
   - **_meleeEnemyPrefab:** Project 창 `Assets/Prefabs/Enemies/MeleeEnemy.prefab`을 드래그
   - **_rangedEnemyPrefab:** Project 창 `Assets/Prefabs/Enemies/RangedEnemy.prefab`을 드래그
8. 기존 Phase 10 필드(`_exitPortalPrefab`, `_exitSpawnChance`, `_maxExitsActive`, `_player`, `_combatController` 등)는 변경하지 않는다
9. `Ctrl+S`로 씬 저장
  </how-to-verify>
  <resume-signal>
"완료" — TimerLabel 생성 확인 + WorldGenerator 적 프리팹 2개 연결 확인. 또는 문제 설명 (메뉴가 보이지 않음, 컴파일 에러, TimerLabel이 화면 밖에 생성됨 등).
  </resume-signal>
  <acceptance_criteria>
    - Console에 "[HUDTimerLabelBuilder] TimerLabel 생성 및 HUDController._timerLabel 연결 완료." 로그가 출력되었다
    - HUDController Inspector의 _timerLabel 필드에 TimerLabel이 연결되어 있다
    - WorldGenerator Inspector의 _meleeEnemyPrefab에 MeleeEnemy.prefab이 연결되어 있다
    - WorldGenerator Inspector의 _rangedEnemyPrefab에 RangedEnemy.prefab이 연결되어 있다
    - 기존 Phase 10 WorldGenerator 필드가 그대로 유지되어 있다 (덮어쓰이지 않음)
    - Console에 컴파일 에러가 없다
  </acceptance_criteria>
  <done>SampleScene의 HUDController와 WorldGenerator가 Phase 11에서 추가된 모든 필드를 연결한 상태로 저장되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 플레이테스트 — TIMER-01 / TIMER-02 / DIFF-01 / SCORE-01 / SCORE-02 성공 기준 확인</name>
  <read_first>
    - .planning/phases/11-timer-difficulty/11-CONTEXT.md (도메인/성공 기준 재확인)
  </read_first>
  <what-built>
WorldGenerator가 층 진입마다 FloorTimer를 60초로 리셋하고(TIMER-01), 매 프레임 만료를 체크해 시간 초과 시 PlayerController.OnPlayerDeath를 발동한다(TIMER-02). Room이 Instantiate되는 즉시 층 번호 기반 난이도 테이블로 적을 스폰한다(DIFF-01). EXIT 포탈 진입 시 남은 시간이 점수로 환산되고(SCORE-01), HUD가 타이머와 점수를 실시간으로 표시한다(TIMER-01 화면, SCORE-02).
  </what-built>
  <how-to-verify>

**테스트 준비:**
- Unity Editor 상단 Play 버튼 클릭 전, Console 창과 Game 뷰를 함께 연다

**TIMER-01 확인 — 카운트다운 표시 + 슬로우모션 면역:**

1. Play 모드 시작 직후 HUD에 타이머가 "60" 근처 값에서 시작해 매초 감소하는지 확인한다
2. 공격 버튼을 눌러 슬로우모션(Time.timeScale ≈ 0.2)을 발동한 상태로 몇 초 유지한다 — 타이머가 게임 화면 속도와 무관하게 실제 시계와 같은 속도로 계속 감소하는지 확인한다 (슬로우모션 중에도 타이머가 느려지지 않아야 한다)

**D-05 확인 — 점멸 경고:**

3. 타이머가 20초 이하로 내려갈 때까지 대기(또는 방치)한다 — 텍스트가 흰색에서 빨간색으로 점멸하기 시작하는지 확인한다
4. 시간이 더 줄어들수록(예: 10초 → 3초) 점멸 속도가 눈에 띄게 빨라지는지 확인한다

**TIMER-02 확인 — 시간 초과 사망:**

5. 아무것도 하지 않고 타이머가 0에 도달할 때까지 기다린다 (또는 테스트 편의를 위해 Inspector에서 `FloorTimer` 관련 값을 일시적으로 조정하지 않고 실제로 60초 대기)
6. 타이머가 0에 도달하는 순간 사망 화면(DeathScreenController)이 표시되는지 확인한다
7. Console에 "Player died" 로그(PlayerDeathHandler.HandleDeath)가 출력되는지 확인한다

**DIFF-01 확인 — 난이도 스케일링:**

8. Play를 재시작하고 EXIT 포탈을 통해(또는 `_exitSpawnChance`를 1.0f로 임시 상향해) 3층까지 진행한다
9. 1층과 3층에서 각각 Hierarchy 또는 육안으로 스폰된 적(MeleeEnemy/RangedEnemy) 수를 비교한다 — 3층 쪽이 눈에 띄게 많아야 한다(D-03 테이블: 1~5층 근접 2~3+원거리 0~1 vs 6층 이상 원거리 비중 증가)

**SCORE-01 / SCORE-02 확인 — 시간 비례 점수:**

10. EXIT 포탈에 진입하기 직전 HUD의 타이머 값(예: 남은 45초)과 점수 값을 기록한다
11. 포탈 진입 직후 HUD 점수가 "이전 점수 + (기록한 남은 초 × 10)"만큼 증가했는지 확인한다 (예: 45초 남았다면 +450점)
12. 포탈 진입 직후 타이머가 다시 60 근처로 리셋되는지 확인한다

**오류 확인:**

13. Console에 "[WorldGenerator]"/"[HUDTimerLabelBuilder]"로 시작하는 LogError가 없어야 한다
  </how-to-verify>
  <resume-signal>
다음 형식으로 결과를 입력:
- "TIMER-01: 통과, TIMER-02: 통과, DIFF-01: 통과, SCORE-01: 통과, SCORE-02: 통과" — 모두 성공
- 또는 미통과 항목과 관찰된 현상 설명 (예: "TIMER-02 미통과 — 타이머가 0에 도달해도 사망 화면이 뜨지 않음")
  </resume-signal>
  <acceptance_criteria>
    - HUD 타이머가 60에서 시작해 실시간으로 감소하고, 슬로우모션 중에도 감소 속도가 변하지 않는다
    - 남은 시간 20초 이하부터 타이머 텍스트가 빨간색으로 점멸하며, 시간이 줄어들수록 점멸이 빨라진다
    - 타이머가 0에 도달하면 사망 화면이 표시되고 Console에 "Player died"가 출력된다
    - 3층에서 스폰된 적 수가 1층보다 육안으로 확인 가능할 만큼 많다
    - EXIT 포탈 진입 시 HUD 점수가 정확히 (남은 초 × 10)만큼 증가한다
    - 포탈 진입 후 타이머가 60 근처로 다시 리셋된다
    - Console에 "[WorldGenerator]"/"[HUDTimerLabelBuilder]" LogError가 없다
  </acceptance_criteria>
  <done>
TIMER-01, TIMER-02, DIFF-01, SCORE-01, SCORE-02 성공 기준이 모두 충족되었다:
1. 층 진입마다 60초 타이머가 실시간(슬로우모션 면역)으로 카운트다운되고 임박 시 점멸 경고가 발동한다
2. 시간 초과 시 기존 사망 플로우가 그대로 재사용되어 게임오버가 발생한다
3. 층 번호가 높아질수록 스폰 적 수가 증가한다
4. EXIT 포탈 진입 시 남은 시간에 비례한 점수가 정확히 누적되고 HUD에 실시간 반영된다
  </done>
</task>

</tasks>

<verification>
Phase 11 성공 기준 4항목(ROADMAP.md)을 모두 Task 3 플레이테스트에서 확인한다:
1. TIMER-01: 층 진입 시 HUD 타이머가 60초부터 즉시 카운트다운을 시작하고, 슬로우모션 중에도 실시간으로 감소한다
2. TIMER-02: 타이머가 0에 도달하는 순간 PlayerController.OnPlayerDeath가 발동해 기존 사망 화면이 표시된다
3. DIFF-01: 3층에서 스폰되는 총 몬스터 수가 1층보다 눈에 띄게 많다
4. SCORE-01/SCORE-02: EXIT 포탈 진입 시 남은 시간(초) × 10점이 ScoreManager.Score에 누적되고 HUD에 실시간 반영된다
</verification>

<success_criteria>
- Task 1 완료: HUDTimerLabelBuilder.cs 생성됨
- Task 2 완료: SampleScene에 TimerLabel 생성 + HUDController/WorldGenerator Inspector 필드 전부 연결됨
- Task 3 완료: TIMER-01/02, DIFF-01, SCORE-01/02 성공 기준 모두 플레이테스트에서 확인됨
- Console에 컴파일 오류 및 런타임 오류 없음
- 위 조건 충족 후 `/gsd:verify-work 11` 실행 가능 상태
</success_criteria>

<output>
완료 후 `.planning/phases/11-timer-difficulty/11-04-SUMMARY.md` 생성:
- Task 1 결과 (HUDTimerLabelBuilder.cs 생성 내용)
- Task 2 결과 (TimerLabel 생성 + Inspector 연결 내용)
- Task 3 결과 (TIMER-01/02, DIFF-01, SCORE-01/02 각 항목 통과 여부 및 관찰 내용)
- 발견된 문제 및 해결 방법 (있다면)
- Phase 11 완료 여부 선언
</output>
