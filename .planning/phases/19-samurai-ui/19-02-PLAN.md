---
phase: 19-samurai-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/CombatModuleRegistry.cs
  - Assets/Scripts/UI/CombatModuleSelector.cs
  - Assets/Scripts/UI/AttackSelectController.cs
  - Assets/Editor/AttackSelectUIBuilder.cs
autonomous: true
requirements: [UNLOCK-02, UNLOCK-03]

must_haves:
  truths:
    - "AttackSelect 화면에 3개 모듈 슬롯(기본전투모듈/Overclock/사무라이 전투형 모듈)이 표시된다"
    - "기본전투모듈 슬롯은 항상 클릭 가능하다 (requiredBossId=null)"
    - "Overclock/사무라이 전투형 모듈 슬롯은 BossUnlockManager.IsUnlocked(해당 bossId)가 false이면 버튼이 비활성화되고 자물쇠 아이콘이 표시된다"
    - "잠긴 슬롯을 클릭해도 아무 반응이 없다 (Button.interactable=false로 클릭 자체가 차단됨)"
    - "해금된 슬롯을 클릭하면 CombatModuleSelector.SelectedIndex가 그 슬롯의 인덱스로 설정되고 SampleScene이 로드된다"
  artifacts:
    - path: "Assets/Scripts/UI/CombatModuleRegistry.cs"
      provides: "D-12 배열 기반 모듈 목록 — DisplayName/ModuleId/RequiredBossId, 향후 DeadEye/MAX/NOVA 슬롯을 한 줄만 추가하면 되는 구조"
      contains: "public static readonly CombatModuleEntry[] All"
    - path: "Assets/Scripts/UI/CombatModuleSelector.cs"
      provides: "AttackTypeSelector와 동일한 static 선택 상태 컨벤션"
      contains: "public static class CombatModuleSelector"
    - path: "Assets/Scripts/UI/AttackSelectController.cs"
      provides: "D-12/D-13 데이터 기반 N-way 루프 — 하드코딩된 OnLinearClicked/OnFanClicked 제거"
      contains: "CombatModuleRegistry.All"
    - path: "Assets/Editor/AttackSelectUIBuilder.cs"
      provides: "AttackSelect.unity에 3개 버튼+라벨+자물쇠 아이콘을 절차적으로 배치하는 에디터 도구"
      contains: "MenuItem(\"Fast/Phase19/Build AttackSelect UI\")"
  key_links:
    - from: "Assets/Scripts/UI/AttackSelectController.cs"
      to: "Assets/Scripts/UI/CombatModuleRegistry.cs"
      via: "Start()가 CombatModuleRegistry.All을 순회"
      pattern: "CombatModuleRegistry\\.All"
    - from: "Assets/Scripts/UI/AttackSelectController.cs"
      to: "Assets/Scripts/Progression/BossUnlockManager.cs"
      via: "entry.IsUnlocked가 BossUnlockManager.IsUnlocked(RequiredBossId) 호출"
      pattern: "BossUnlockManager\\.IsUnlocked"
---

<objective>
UNLOCK-02/UNLOCK-03을 구현한다. 기존 2-way(Linear/Fan) `AttackSelectController`를 D-12가 요구하는 배열/레지스트리 기반 N-way(현재 3-way: 기본전투모듈/Overclock/사무라이 전투형 모듈) 모듈 선택 화면으로 확장하고, D-13(잠금 표시)을 구현한다. 기존 Linear/Fan 공격 형태 선택은 이 화면의 책임이 아니게 된다 — `AttackTypeSelector`(19-RESEARCH.md §4 file layout 주석: "여전히 Overclock 자체의 Linear/Fan 형태를 결정, 모듈 선택과 직교") 자체는 이 Plan에서 전혀 건드리지 않는다.

이 Plan은 실제 전투 모듈 클래스(`BasicCombatModule`/`OverclockModule`/`SamuraiParryModule`)에 전혀 의존하지 않는다 — `CombatModuleId` enum과 문자열 `RequiredBossId`만으로 완전히 decoupled되어 있어 Plan 19-01과 병렬로 진행 가능하다.

Purpose: 로비 화면에서 해금 상태에 따라 모듈을 선택하는 실제 UI(D-12/D-13)를 완성해, 이후 CombatController(19-03)가 읽어들일 선택 상태를 확정한다.
Output: `Assets/Scripts/UI/`에 신규 2파일 + `AttackSelectController.cs` 재작성, `Assets/Editor/AttackSelectUIBuilder.cs` 신규 에디터 도구.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md

<interfaces>
<!-- 기존 static 선택 컨벤션 (재작성 대상 아님 — CombatModuleSelector가 이 패턴을 그대로 모방) -->
Assets/Scripts/UI/AttackTypeSelector.cs:
```csharp
public enum AttackType { Linear, Fan }
public class AttackTypeSelector : MonoBehaviour
{
    public static AttackType Selected { get; private set; } = AttackType.Linear;
    public static void SetType(AttackType type) { ... }
}
```

<!-- 현재 AttackSelectController.cs 전체 (17 lines, 완전 교체 대상) -->
Assets/Scripts/UI/AttackSelectController.cs:
```csharp
using UnityEngine;
using UnityEngine.SceneManagement;

public class AttackSelectController : MonoBehaviour
{
    public void OnLinearClicked()
    {
        AttackTypeSelector.SetType(AttackType.Linear);
        SceneManager.LoadScene("SampleScene");
    }

    public void OnFanClicked()
    {
        AttackTypeSelector.SetType(AttackType.Fan);
        SceneManager.LoadScene("SampleScene");
    }
}
```

<!-- 해금 조회 API (이미 구현/검증됨, 그대로 재사용) -->
Assets/Scripts/Progression/BossUnlockManager.cs:
```csharp
public static class BossUnlockManager
{
    public static bool IsUnlocked(string bossId);
    public static void Unlock(string bossId);
}
```

<!-- Assets/Scenes/AttackSelect.unity 현재 GameObject 계층 (직접 grep 확인) — Task 2가 이 씬을 수정한다 -->
루트: Canvas(RenderMode=ScreenSpaceOverlay, CanvasScaler ReferenceResolution 1920x1080) > Panel > Linear(Button, RectTransform anchoredPosition=(0,80) size=(400,120)) + Fan(Button) 형제, 각각 자식 "Text (TMP)" 보유.
"AttackSelectorController"라는 이름의 GameObject가 `AttackSelectController` 컴포넌트를 보유하며, Linear/Fan 버튼의 `m_OnClick.m_PersistentCalls`가 각각 `OnLinearClicked`/`OnFanClicked`를 Inspector에서 직접 참조 중이다(Persistent Listener). 이 Plan에서 그 메서드들을 삭제하므로 이 Persistent Listener 참조는 컴파일 후 무효가 되지만, Task 2에서 `AttackSelectController.Start()`가 런타임에 `Button.onClick.RemoveAllListeners()`를 호출한 뒤 새 리스너를 등록하므로 Play 시 안전하게 대체된다(씬 YAML의 stale 참조는 별도 정리 불필요 — Unity가 무효 메서드 참조를 콘솔 경고만 내고 무시함, 필요 시 Task 3의 에디터 도구가 버튼을 재구성하며 함께 정리된다).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CombatModuleRegistry + CombatModuleSelector (데이터/상태)</name>
  <files>Assets/Scripts/UI/CombatModuleRegistry.cs, Assets/Scripts/UI/CombatModuleSelector.cs</files>
  <read_first>
    - Assets/Scripts/UI/AttackTypeSelector.cs (static 선택 상태 컨벤션 — CombatModuleSelector가 동일 패턴 모방)
    - Assets/Scripts/Progression/BossUnlockManager.cs (IsUnlocked 시그니처)
    - .planning/phases/19-samurai-ui/19-CONTEXT.md D-12(개정)/D-17 (정확한 3개 엔트리 내용 — requiredBossId 값)
  </read_first>
  <action>
    **1) `CombatModuleRegistry.cs`** (신규):
    ```csharp
    /// <summary>D-12: 모듈 선택 UI가 순회하는 배열 기반 레지스트리. 향후 DeadEye(Phase 20)/
    /// MAX(Phase 22)/NOVA(Phase 23)는 이 배열에 엔트리 한 줄만 추가하면 된다(하드코딩 나열 금지).
    /// 실제 IPlayerCombatModule 구현 클래스를 전혀 참조하지 않는다 — CombatModuleId enum + 문자열
    /// requiredBossId만으로 UI 레이어가 전투 모듈 레이어와 완전히 decoupled된다.</summary>
    public enum CombatModuleId { Basic, Overclock, Samurai }

    public readonly struct CombatModuleEntry
    {
        public readonly string DisplayName;
        public readonly CombatModuleId ModuleId;
        public readonly string RequiredBossId; // null/"" = 상시 해금

        public CombatModuleEntry(string displayName, CombatModuleId moduleId, string requiredBossId)
        {
            DisplayName = displayName;
            ModuleId = moduleId;
            RequiredBossId = requiredBossId;
        }

        public bool IsUnlocked => string.IsNullOrEmpty(RequiredBossId) || BossUnlockManager.IsUnlocked(RequiredBossId);
    }

    public static class CombatModuleRegistry
    {
        public static readonly CombatModuleEntry[] All =
        {
            new CombatModuleEntry("기본전투모듈", CombatModuleId.Basic, requiredBossId: null),           // D-16: 상시 해금
            new CombatModuleEntry("Overclock", CombatModuleId.Overclock, requiredBossId: "Fiora"),         // D-17: 기존 게이팅 그대로 유지 (BossUnlockManager.IsUnlocked("Fiora"))
            new CombatModuleEntry("사무라이 전투형 모듈", CombatModuleId.Samurai, requiredBossId: "Samurai"), // SAMURAI-01
        };
    }
    ```

    **2) `CombatModuleSelector.cs`** (신규 — `AttackTypeSelector`와 동일한 static 선택 컨벤션):
    ```csharp
    /// <summary>UNLOCK-02: 선택된 전투 모듈의 static 런타임 상태. AttackTypeSelector.Selected와
    /// 동일한 static-선택 패턴. 기본값 index 0(기본전투모듈)은 항상 해금 상태이므로 안전한 폴백이다(D-16).</summary>
    public static class CombatModuleSelector
    {
        public static int SelectedIndex { get; private set; } = 0;
        public static CombatModuleId SelectedModuleId => CombatModuleRegistry.All[SelectedIndex].ModuleId;
        public static void SetSelected(int index) => SelectedIndex = index;
    }
    ```
  </action>
  <verify>
    <automated>grep -n "enum CombatModuleId" Assets/Scripts/UI/CombatModuleRegistry.cs; grep -c "new CombatModuleEntry" Assets/Scripts/UI/CombatModuleRegistry.cs; grep -n "requiredBossId: null" Assets/Scripts/UI/CombatModuleRegistry.cs; grep -n "\"Fiora\"" Assets/Scripts/UI/CombatModuleRegistry.cs; grep -n "\"Samurai\"" Assets/Scripts/UI/CombatModuleRegistry.cs; grep -n "static class CombatModuleSelector" Assets/Scripts/UI/CombatModuleSelector.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new CombatModuleEntry" Assets/Scripts/UI/CombatModuleRegistry.cs` → 3 (정확히 3개 엔트리)
    - 기본전투모듈 엔트리는 `requiredBossId: null` (grep 매치), Overclock 엔트리는 `"Fiora"` (D-17 게이팅 유지), 사무라이 전투형 모듈 엔트리는 `"Samurai"`
    - `CombatModuleSelector.SelectedIndex`의 기본값이 `0`이다 (기본전투모듈 — 항상 해금 상태라 안전한 폴백)
    - 두 파일 모두 `OverclockModule`/`BasicCombatModule`/`SamuraiParryModule` 클래스명을 문자열/타입 참조 그 어느 형태로도 포함하지 않는다 (decoupling 확인) — `grep -c "OverclockModule\|BasicCombatModule\|SamuraiParryModule" Assets/Scripts/UI/CombatModuleRegistry.cs Assets/Scripts/UI/CombatModuleSelector.cs` → 0
  </acceptance_criteria>
  <done>레지스트리 3엔트리 + 선택 상태 static 클래스가 존재하고, 구체적 전투 모듈 클래스와 완전히 decoupled 상태다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AttackSelectController N-way 재작성</name>
  <files>Assets/Scripts/UI/AttackSelectController.cs</files>
  <read_first>
    - Assets/Scripts/UI/AttackSelectController.cs (현재 전체 — 위 interfaces 블록 참고, 완전 교체 대상)
    - Assets/Scripts/UI/CombatModuleRegistry.cs, CombatModuleSelector.cs (Task 1 산출물)
  </read_first>
  <action>
    `AttackSelectController.cs`를 아래 내용으로 완전히 교체한다. `OnLinearClicked`/`OnFanClicked`는 삭제한다(D-12: Linear/Fan 공격 형태 선택은 이 화면의 책임이 아니게 됨 — `AttackTypeSelector` 자체는 무변경, 기본값 `AttackType.Linear`가 그대로 유지됨).
    ```csharp
    using UnityEngine;
    using UnityEngine.UI;
    using UnityEngine.SceneManagement;
    using TMPro;

    /// <summary>UNLOCK-02/UNLOCK-03/D-12/D-13: 배열 기반 N-way 모듈 선택 화면. Inspector 배열
    /// 3개(_moduleButtons/_lockIcons/_labels)는 CombatModuleRegistry.All과 인덱스 정렬되어야 한다
    /// (AttackSelectUIBuilder.cs가 절차적으로 배치+배선한다).</summary>
    public class AttackSelectController : MonoBehaviour
    {
        [SerializeField] private Button[] _moduleButtons;
        [SerializeField] private Image[] _lockIcons;
        [SerializeField] private TMP_Text[] _labels;

        private void Start()
        {
            var entries = CombatModuleRegistry.All;
            for (int i = 0; i < entries.Length; i++)
            {
                int index = i; // 클로저 캡처
                bool unlocked = entries[i].IsUnlocked;

                if (_labels != null && i < _labels.Length && _labels[i] != null)
                    _labels[i].text = entries[i].DisplayName;

                if (_moduleButtons != null && i < _moduleButtons.Length && _moduleButtons[i] != null)
                {
                    _moduleButtons[i].interactable = unlocked; // D-13: 클릭 자체 차단
                    _moduleButtons[i].onClick.RemoveAllListeners(); // 씬에 남아있을 수 있는 구 Persistent Listener 대체
                    _moduleButtons[i].onClick.AddListener(() => OnModuleClicked(index));
                }

                if (_lockIcons != null && i < _lockIcons.Length && _lockIcons[i] != null)
                    _lockIcons[i].enabled = !unlocked; // D-13: 자물쇠 아이콘
            }
        }

        private void OnModuleClicked(int index)
        {
            CombatModuleSelector.SetSelected(index);
            SceneManager.LoadScene("SampleScene");
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "OnLinearClicked\|OnFanClicked" Assets/Scripts/UI/AttackSelectController.cs; grep -n "CombatModuleRegistry.All" Assets/Scripts/UI/AttackSelectController.cs; grep -n "CombatModuleSelector.SetSelected" Assets/Scripts/UI/AttackSelectController.cs; grep -n "\.interactable = unlocked" Assets/Scripts/UI/AttackSelectController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OnLinearClicked\|OnFanClicked" Assets/Scripts/UI/AttackSelectController.cs` → 0 (완전 삭제 확인)
    - `Start()`가 `CombatModuleRegistry.All`을 순회하며 각 엔트리의 `IsUnlocked`로 `Button.interactable`과 lock icon `enabled`를 설정한다 (grep 매치 확인)
    - 클릭 핸들러가 `CombatModuleSelector.SetSelected(index)` 호출 후 `SceneManager.LoadScene("SampleScene")`을 호출한다
    - Unity Editor 컴파일 에러 0건 수동 확인
  </acceptance_criteria>
  <done>AttackSelectController.cs가 데이터 기반 N-way 로직으로 완전히 교체되었고 컴파일 에러가 없다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AttackSelectUIBuilder 에디터 도구 — 3버튼 씬 구성</name>
  <files>Assets/Editor/AttackSelectUIBuilder.cs</files>
  <read_first>
    - Assets/Scenes/AttackSelect.unity (grep으로 확인한 현재 구조 — 위 interfaces 블록 참고: Canvas > Panel > Linear/Fan Button, 각 자식 "Text (TMP)")
    - Assets/Editor/DebugSceneBuilder.cs (99-126줄 — Button+Image+TextMeshProUGUI 절차적 생성 코드 패턴을 그대로 재사용)
    - Assets/Scripts/UI/AttackSelectController.cs (Task 2 산출물 — `_moduleButtons`/`_lockIcons`/`_labels` 배열 필드에 SerializedObject로 값을 채워야 함)
    - Assets/Scripts/UI/CombatModuleRegistry.cs (3개 엔트리 순서 — 버튼 인덱스와 정렬)
  </read_first>
  <action>
    `Assets/Editor/AttackSelectUIBuilder.cs`를 신규 작성한다. 메뉴: `Fast/Phase19/Build AttackSelect UI`.

    구현 순서:
    1. `EditorSceneManager.OpenScene("Assets/Scenes/AttackSelect.unity", OpenSceneMode.Single)`로 씬을 연다.
    2. 씬에서 `GameObject.Find("Panel")`로 기존 Panel(Linear/Fan 버튼의 부모)을 찾는다. 없으면 `GameObject.Find("Canvas")`를 부모로 사용.
    3. 기존 `GameObject.Find("Linear")`/`GameObject.Find("Fan")` 버튼 오브젝트가 있으면 `Object.DestroyImmediate()`로 제거한다(재구성 대상 — D-12: 이 화면은 이제 Linear/Fan이 아니라 모듈을 선택한다).
    4. `CombatModuleRegistry.All` 3개 엔트리마다 버튼 GameObject를 절차적으로 생성한다. DebugSceneBuilder.cs의 버튼 생성 패턴(RectTransform+Image+Button+자식 TextMeshProUGUI)을 그대로 따르되:
       - RectTransform: `anchorMin=anchorMax=(0.5,0.5)`, `sizeDelta=(360,120)`, `anchoredPosition = new Vector2(0, 140 - i * 150)` (3개 버튼을 세로로 균등 배치, i=0,1,2)
       - Button 자식으로 "Label" TextMeshProUGUI (entry.DisplayName, fontSize=32, 중앙 정렬)
       - Button 자식으로 "LockIcon" Image (작은 정사각형, `anchoredPosition=(150,0)`, `sizeDelta=(36,36)`, 기본 `enabled=false`, 색상은 진한 회색 `new Color(0.2f,0.2f,0.2f,0.9f)` — 실제 자물쇠 스프라이트 없이 단색 사각형으로 충분, Claude's Discretion)
    5. 생성한 3개 Button/Image/TMP_Text 배열을 `AttackSelectController` 컴포넌트(씬에서 `GameObject.Find`로 찾거나, `AttackSelectController` 타입으로 `Object.FindFirstObjectByType`)에 `SerializedObject` + `FindProperty("_moduleButtons")`/`FindProperty("_lockIcons")`/`FindProperty("_labels")`로 배열 크기 3 설정 후 각 인덱스에 대입한다(DebugSceneBuilder.cs의 `camFollowSO.FindProperty(...).objectReferenceValue = ...; camFollowSO.ApplyModifiedProperties();` 패턴 재사용).
    6. `EditorSceneManager.SaveScene(EditorSceneManager.GetActiveScene())`으로 저장, `AssetDatabase.SaveAssets()`.
    7. 재실행 시 멱등적이어야 한다 — 4번 단계 시작 전 기존에 이 도구가 생성한 버튼(이름 규칙: "ModuleButton_0"/"ModuleButton_1"/"ModuleButton_2")이 있으면 먼저 제거 후 재생성한다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase19/Build AttackSelect UI\")" Assets/Editor/AttackSelectUIBuilder.cs; grep -n "CombatModuleRegistry.All" Assets/Editor/AttackSelectUIBuilder.cs; grep -n "_moduleButtons\|_lockIcons\|_labels" Assets/Editor/AttackSelectUIBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/AttackSelectUIBuilder.cs`가 존재하고 `[MenuItem("Fast/Phase19/Build AttackSelect UI")]` 메뉴를 정의한다
    - `CombatModuleRegistry.All`을 순회해 정확히 3개 버튼을 생성하는 루프가 존재한다
    - `AttackSelectController`의 `_moduleButtons`/`_lockIcons`/`_labels` 3개 SerializedProperty에 값을 대입하는 코드가 존재한다
    - 재실행 멱등성을 위한 기존 오브젝트 제거 로직이 존재한다("ModuleButton_" 이름 규칙 검색+제거)
    - 실제 메뉴 실행(씬 저장 포함)은 이 Task 범위 밖이다 — 19-06의 checkpoint에서 일괄 실행한다(이 Task는 도구 작성만, `git diff Assets/Scenes/AttackSelect.unity` → 빈 출력이어야 정상)
  </acceptance_criteria>
  <done>AttackSelectUIBuilder.cs가 멱등적인 씬 구성 도구로 존재하며, 아직 실행되지 않아 AttackSelect.unity는 무변경 상태다.</done>
</task>

</tasks>

<verification>
- `CombatModuleRegistry.All`이 정확히 3개 엔트리(기본전투모듈=null, Overclock="Fiora", 사무라이 전투형 모듈="Samurai")를 갖는다.
- `AttackSelectController.cs`가 하드코딩된 2-way 메서드 없이 레지스트리를 순회하는 데이터 기반 N-way 로직으로 교체되었다.
- `AttackSelectUIBuilder.cs`가 멱등적인 에디터 도구로 존재한다(19-06에서 실행).
</verification>

<success_criteria>
ROADMAP.md Phase 19 Success Criteria #6("게임 시작 전 선택 화면에서 해금된 모듈은 선택 가능하게, 아직 해금되지 않은 모듈은 잠금 상태로 표시된다")의 코드 기반이 완성된다. 실제 씬 반영/플레이테스트는 19-06에서 수행.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-02-SUMMARY.md`
</output>
</content>
