---
phase: 19-samurai-ui
plan: 06
type: execute
wave: 4
depends_on: ["19-02", "19-05"]
files_modified:
  - Assets/Scenes/AttackSelect.unity
  - Assets/Scenes/DebugScene.unity
  - Assets/Prefabs/Enemies/SamuraiBoss.prefab
  - Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab
autonomous: false
requirements: [SAMURAI-01, SAMURAI-02, SAMURAI-03, SAMURAI-04, SAMURAI-05, UNLOCK-02, UNLOCK-03]

must_haves:
  truths:
    - "AttackSelect 화면에서 기본전투모듈은 항상 선택 가능하고, Overclock/사무라이 전투형 모듈은 각각 보스 격파 전엔 잠금(자물쇠 아이콘+비활성 버튼)으로 표시된다"
    - "DebugScene에서 SAMURAI 룸으로 이동해 기본전투모듈로 전투 시 패링 없이 구르기만으로 평시 콤보/패링 전용 타이밍을 모두 회피할 수 있다"
    - "사무라이 전투형 모듈로 패링 전용 타이밍에 정확한 타이밍+방향으로 탭하면 패링이 성공하고 즉사하지 않는다"
    - "그로기 상태에서 7회 처치 진행이 누적되면 SAMURAI가 처치되고 PlayerPrefs boss_unlock_Samurai가 1로 영속된다"
    - "SAMURAI 격파 후 AttackSelect 화면에서 사무라이 전투형 모듈 슬롯 잠금이 해제된다"
    - "Overclock 선택 시 기존 hold-slowmo→release-resolve 흐름이 Phase 18 검증 당시와 동일하게 동작한다 (INFRA-01 무회귀)"
  artifacts:
    - path: "Assets/Scenes/AttackSelect.unity"
      provides: "실제 3-way 모듈 선택 화면 (AttackSelectUIBuilder 실행 결과)"
      contains: "ModuleButton_0"
    - path: "Assets/Prefabs/Enemies/SamuraiBoss.prefab"
      provides: "실제 스폰 가능한 SAMURAI 보스 프리팹"
      contains: "SamuraiBoss"
  key_links:
    - from: "Assets/Scenes/DebugScene.unity"
      to: "Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab"
      via: "ToSamuraiRoom_Teleporter의 DebugRoomTeleporter.targetRoomPrefab"
      pattern: "Room_SamuraiFsmTest"
---

<objective>
19-01~19-05가 작성한 코드/에디터 도구를 실제로 실행해 씬/프리팹 애셋을 생성하고, ROADMAP.md Phase 19의 6개 Success Criteria 전부를 실제 플레이로 검증한다. 이 프로젝트는 자동화된 테스트 프레임워크가 없으므로(19-VALIDATION.md 확정 컨벤션) Phase 15/18/18.1과 동일하게 에디터 메뉴 실행 + 수동 플레이테스트 체크리스트로 마무리한다.

Purpose: SAMURAI-01~05, UNLOCK-02/03을 실제 플레이로 최종 승인하고 Phase 19를 완료 처리할 수 있는 근거를 만든다.
Output: `AttackSelect.unity`/`DebugScene.unity` 갱신, `SamuraiBoss.prefab`/`Room_SamuraiFsmTest.prefab` 신규 생성, 통과된 플레이테스트 체크리스트.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 에디터 메뉴 4종 실행 — 프리팹/씬 애셋 실제 생성</name>
  <files>Assets/Scenes/AttackSelect.unity, Assets/Scenes/DebugScene.unity, Assets/Prefabs/Enemies/SamuraiBoss.prefab, Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab</files>
  <read_first>
    - Assets/Editor/AttackSelectUIBuilder.cs (19-02 Task 3 산출물)
    - Assets/Editor/SamuraiBossPrefabBuilder.cs, RoomSamuraiFsmTestBuilder.cs (19-05 Task 1 산출물)
    - Assets/Editor/DebugSceneBuilder.cs (19-05 Task 2가 확장한 최종 버전)
  </read_first>
  <action>
    Unity Editor를 열어 컴파일 에러가 0건임을 콘솔에서 먼저 확인한 뒤, 다음 순서로 메뉴를 실행한다(의존 순서 고정 — 순서를 바꾸면 각 도구의 사전조건 null 체크에 걸려 에러 로그와 함께 중단된다):
    1. `Fast/Phase19/Build SamuraiBoss Prefab` — `Assets/Prefabs/Enemies/SamuraiBoss.prefab` 생성 확인
    2. `Fast/Phase19/Build Room_SamuraiFsmTest` — `Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab` 생성 확인 (nested SamuraiBoss 인스턴스 포함)
    3. `Fast/Debug/Build DebugScene` — 기존 Room_BossFsmTest(FioraBoss) + 신규 ToSamuraiRoom_Teleporter + DebugCombatModuleSwitcher가 배선된 `DebugScene.unity` 재생성 확인
    4. `Fast/Phase19/Build AttackSelect UI` — `AttackSelect.unity`에 3개 모듈 버튼(ModuleButton_0/1/2)이 배치되고 `AttackSelectController`의 `_moduleButtons`/`_lockIcons`/`_labels` 배열이 채워졌는지 Inspector에서 확인
  </action>
  <what-built>SAMURAI 보스/룸 프리팹, DebugScene의 SAMURAI 테스트 경로, AttackSelect의 3-way UI — 전부 코드/도구는 준비되었으나 아직 실제 애셋으로 구체화되지 않은 상태였다.</what-built>
  <how-to-verify>
    각 메뉴 실행 후 Project 창에서 해당 애셋이 생성/갱신되었는지, Console에 에러가 없는지 확인한다. `AttackSelect.unity`를 열어 Hierarchy에서 3개 버튼(기본전투모듈/Overclock/사무라이 전투형 모듈)이 보이는지, `DebugScene.unity`를 열어 Room_BossFsmTest 옆에 새 텔레포터 pad(Scene 뷰 시안색 Gizmo)가 보이는지 확인한다.
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — Unity 에디터 메뉴 실행은 CLI 경로가 없다(이 프로젝트의 기존 컨벤션, Phase 15/18 선례와 동일). 통과 기준: 4개 메뉴 전부 에러 없이 완료되고 대상 애셋이 실제로 존재</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Prefabs/Enemies/SamuraiBoss.prefab`이 존재하고 `SamuraiBoss` 컴포넌트를 포함한다
    - `Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab`이 존재하고 nested `SamuraiBoss` 인스턴스를 포함한다
    - `Assets/Scenes/DebugScene.unity`가 저장되었고 `ToSamuraiRoom_Teleporter` GameObject를 포함한다
    - `Assets/Scenes/AttackSelect.unity`가 저장되었고 3개 모듈 버튼을 포함하며 `AttackSelectController`의 3개 배열 필드가 모두 채워져 있다
    - Unity 콘솔에 에러/예외 0건
  </acceptance_criteria>
  <done>4개 에디터 메뉴가 순서대로 에러 없이 실행되어 필요한 모든 씬/프리팹 애셋이 실제로 생성되었다.</done>
  <resume-signal>4개 메뉴 모두 에러 없이 완료되면 "approved", 특정 메뉴에서 에러가 발생하면 어느 메뉴/어떤 에러 메시지인지 알려주세요.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 19 통합 플레이테스트 — SAMURAI-01~05, UNLOCK-02/03, D-15~D-18</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/19-samurai-ui/19-VALIDATION.md (Manual-Only Verifications 표, Per-Task Verification Map)
    - .planning/ROADMAP.md §Phase 19 Success Criteria 6개
    - .planning/phases/19-samurai-ui/19-CONTEXT.md D-01~D-18 전체
  </read_first>
  <action>
    자동화 테스트 프레임워크가 없으므로(19-VALIDATION.md 확정 컨벤션) 아래 체크리스트를 실제 플레이로 확인하고 결과를 수집한다. 실패 항목이 있으면 어느 Plan(19-01~19-05)의 어느 파일이 원인인지 특정해 재조정한다(타이밍 튜너블은 `[SerializeField]`로 노출되어 있으므로 대부분 Inspector 값 조정으로 해결 가능 — SAMURAI-05가 명시적으로 요구하는 실측 튜닝 과정이다).
  </action>
  <what-built>기본전투모듈/Overclock/사무라이 전투형 모듈 3종 + SAMURAI 보스 FSM(평시/패링/그로기) + 3-way 모듈 선택 UI — Phase 19 전체 기능.</what-built>
  <how-to-verify>
    **A. 모듈 선택 UI (UNLOCK-02/03) — `AttackSelect.unity`를 Play:**
    1. 신규 세이브(PlayerPrefs에 `boss_unlock_Fiora`/`boss_unlock_Samurai` 없음) 기준, 기본전투모듈 버튼만 클릭 가능하고 Overclock/사무라이 전투형 모듈 버튼에는 자물쇠 아이콘이 보이며 클릭이 먹히지 않는지 확인.
    2. 기본전투모듈을 클릭 — SampleScene으로 정상 로드되는지 확인.

    **B. 기본전투모듈 + Overclock 회귀 확인 — `SampleScene.unity` 또는 `DebugScene.unity`를 Play:**
    3. 기본전투모듈 장착 상태에서 Attack 탭 — `Time.timeScale`이 1을 유지한 채 즉시 스윙이 나가고 근처 MeleeEnemy/RangedEnemy가 원샷 처치되는지 확인.
    4. (DebugScene) 숫자키 2로 Overclock 전환 — 기존 홀드 슬로우모션→릴리즈 대시 흐름이 Phase 18 검증 당시와 완전히 동일하게 동작하는지 확인(INFRA-01 무회귀).

    **C. SAMURAI 보스 FSM — `DebugScene.unity`를 Play, ToSamuraiRoom_Teleporter로 이동(진입 후 Up Arrow):**
    5. 숫자키 1(기본전투모듈)로 전환 — SAMURAI의 평시 콤보(예고→공격) 판정에 맞으면 즉사, 구르기로 회피하면 생존하는지 확인(D-07).
    6. 기본전투모듈 상태로 패링 전용 타이밍(투사체 발사) 구간에서 무입력 — 투사체가 명중해 즉사하는지, 구르기로는 회피 가능한지 확인(D-05/D-08).
    7. 숫자키 3(사무라이 전투형 모듈)로 전환 — 패링 전용 타이밍에 투사체를 향해 정확한 타이밍+방향으로 Attack 탭 — 패링이 성공해 즉사하지 않고 투사체가 소멸/반사되는지 확인(SAMURAI-03).
    8. 평시 구간에서 사무라이 전투형 모듈로 보스를 여러 번 타격 + 패링을 반복 성공 — 그로기 상태(색상 변화)에 진입하는지 확인(D-09).
    9. 그로기 상태에서 보스를 타격 — 처치 진행이 1 증가하는지(육안으로는 확인 불가하므로 그로기→평시 반복을 통해 최종 7회째에 실제로 처치되는지로 간접 확인, D-10/D-11).
    10. 7번째 그로기 히트로 SAMURAI가 처치되는지, 사망 연출(파티클/카메라 쉐이크)이 재생되는지 확인(D-11).
    11. Unity RunCommand 또는 Inspector로 `PlayerPrefs.GetInt("boss_unlock_Samurai")`가 1인지 확인, Play를 재시작해도 유지되는지 확인(SAMURAI-01, Phase 18-02 선례와 동일 검증 방식).

    **D. UI 재확인 — `AttackSelect.unity`를 다시 Play:**
    12. SAMURAI 격파 후 사무라이 전투형 모듈 슬롯의 자물쇠가 사라지고 클릭 가능해지는지 확인. Overclock은 `boss_unlock_Fiora`가 여전히 0이면 계속 잠금 상태여야 한다(D-17 — Overclock 게이팅이 기본전투모듈/사무라이 전투형 모듈 해금과 무관하게 독립적으로 유지되는지 확인).

    **E. 타이밍 튜닝 (SAMURAI-05):**
    13. 패링 판정이 "너무 관대해서 아무 때나 성공"하지도, "너무 빡빡해서 정확한 타이밍에도 실패"하지도 않는지 여러 차례 반복 확인 — 필요 시 `SamuraiBoss.parryTelegraphDuration`/`SamuraiParryModule`의 `ctx.SwingHalfAngleDeg`(CombatController Inspector) 값을 조정한다.
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 이 프로젝트는 자동화 테스트 프레임워크가 없다(19-VALIDATION.md 확정). 통과 기준: 체크리스트 1~13 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~13번 전부 통과 보고 수신
    - 실패 항목이 있다면 어느 번호에서 어떤 증상으로 실패했는지 구체적으로 기록되고, 원인 Plan/파일이 특정된다
  </acceptance_criteria>
  <done>ROADMAP.md Phase 19의 6개 Success Criteria 전부가 실제 플레이로 확인되고, SAMURAI-01~05/UNLOCK-02/UNLOCK-03 7개 요구사항이 실측 통과한다.</done>
  <resume-signal>모두 통과하면 "approved", 문제가 있으면 어느 체크리스트 항목이 실패했는지 구체적으로 설명해 주세요.</resume-signal>
</task>

</tasks>

<verification>
- 4개 에디터 메뉴가 순서대로 실행되어 필요한 모든 씬/프리팹 애셋이 실제로 존재한다.
- 체크리스트 1~13번 전부 통과.
- `PlayerPrefs.GetInt("boss_unlock_Samurai")`가 Play 재시작 후에도 1로 유지된다.
</verification>

<success_criteria>
ROADMAP.md Phase 19 Success Criteria 1~6 전부가 실제 플레이로 검증 완료된다. SAMURAI-01~05, UNLOCK-02, UNLOCK-03 7개 요구사항이 실측 통과 상태로 REQUIREMENTS.md에 반영 가능해진다.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-06-SUMMARY.md`
</output>
</content>
