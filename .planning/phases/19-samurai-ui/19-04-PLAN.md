---
phase: 19-samurai-ui
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - Assets/Scripts/Enemy/ParryableProjectile.cs
  - Assets/Scripts/Enemy/Boss/SamuraiBoss.cs
autonomous: true
requirements: [SAMURAI-01, SAMURAI-03, SAMURAI-04, SAMURAI-05]

must_haves:
  truths:
    - "SAMURAI 보스는 평시 콤보(예고→공격)와 패링 전용 타이밍(투사체 발사)을 번갈아 반복한다 (D-07/D-08)"
    - "평시 콤보의 공격 판정에 맞으면 플레이어는 즉사한다 — 단, 구르기(i-frame)로 회피 가능하다 (D-07)"
    - "패링 전용 타이밍에 무입력이거나 패링에 실패하면 투사체가 플레이어에게 명중해 즉사시킨다 — 단, 구르기로 회피 가능하다 (D-05/D-08, Pitfall 2: 트리거 경유 사망 전달)"
    - "평시 콤보 중 보스를 타격하면 그로기 게이지가 채워질 뿐 처치 카운트는 증가하지 않는다 (D-09, Pitfall 4 방지)"
    - "패링 성공 시에도 그로기 게이지가 채워진다 (D-09)"
    - "그로기 게이지가 임계치에 도달하면 그로기 상태에 진입하고, 그 상태에서의 피격 1회가 처치 진행 1회로 카운트된다 (D-10)"
    - "처치 진행이 정확히 7회 누적되면 보스가 처치되고 BossUnlockManager.Unlock(\"Samurai\")가 호출되어 사무라이 전투형 모듈이 해금된다 (D-11, SAMURAI-01)"
  artifacts:
    - path: "Assets/Scripts/Enemy/ParryableProjectile.cs"
      provides: "D-03/D-06/Pitfall 2 — 패링 가능한 물리 투사체, IParryable 구현"
      contains: "class ParryableProjectile : MonoBehaviour, IParryable"
    - path: "Assets/Scripts/Enemy/Boss/SamuraiBoss.cs"
      provides: "BossEnemyBase 상속 SAMURAI FSM — 평시/패링/그로기 3단계 루프"
      contains: "class SamuraiBoss : BossEnemyBase"
  key_links:
    - from: "Assets/Scripts/Enemy/Boss/SamuraiBoss.cs"
      to: "Assets/Scripts/Enemy/ParryableProjectile.cs"
      via: "ParryTimingSegment()가 절차적으로 GameObject 생성 후 ParryableProjectile.Init() 호출"
      pattern: "AddComponent<ParryableProjectile>"
    - from: "Assets/Scripts/Enemy/ParryableProjectile.cs"
      to: "Assets/Scripts/Player/PlayerController.cs"
      via: "OnTriggerEnter2D가 PlayerController.TriggerDeath() 호출 (Pitfall 2 — 트리거 경유만)"
      pattern: "PlayerController\\.TriggerDeath"
    - from: "Assets/Scripts/Enemy/Boss/SamuraiBoss.cs"
      to: "Assets/Scripts/Enemy/Boss/BossEnemyBase.cs"
      via: "OnDashHit()이 그로기 상태에서만 Die(...)를 호출 (BossId=\"Samurai\")"
      pattern: "Die\\(.*BossId\\)"
---

<objective>
SAMURAI-01/03/04/05를 구현한다. `BossEnemyBase`를 상속하는 `SamuraiBoss`가 D-07(평시 콤보)/D-08(패링 전용 타이밍)을 번갈아 반복하고, D-09(그로기 게이지 2단계 누적)/D-10(그로기 중 피격만 처치 카운트)/D-11(7회 처치) 구조로 처치된다. 패링 전용 타이밍의 공격은 실제 물리 투사체(`ParryableProjectile`)로 전달되어, D-05(구르기 회피 가능)가 `RollController`의 기존 `PlayerInvincible` 레이어 교체만으로 자동 성립한다(Pitfall 2 — 코드로 직접 사망 판정하지 않는다).

**중요 — 19-RESEARCH.md Pitfall 4의 전제와 다르다**: 19-CONTEXT.md D-09 원문("평시에 때릴 때와 패링을 통해서 그로기 게이지를 채우고, 그로기 시에 한번씩 공격")은 보스가 평시 구간에도 `IsAlive=true`(타겟 가능) 상태를 유지함을 의미한다 — FioraBoss처럼 평시=타겟불가/빈틈=타겟가능의 이분법이 아니다. `OnDashHit()`이 현재 `_state`(Normal vs Groggy)로 내부 분기해 "게이지만 채움" vs "처치 카운트"를 나눈다. 이 Plan은 CONTEXT.md를 authoritative로 따른다.

Purpose: SAMURAI 보스 FSM을 완성해 SAMURAI-01(격파 시 패링 모듈 최초 해금)의 트리거 조건을 만든다.
Output: `ParryableProjectile.cs`, `SamuraiBoss.cs` 신규 2파일.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md

<interfaces>
<!-- Plan 19-01 산출물 — 그대로 사용 -->
Assets/Scripts/Player/Combat/IParryable.cs:
```csharp
public interface IParryable
{
    Vector2 Position { get; }
    void OnParried(Vector2 reflectDirection);
}
```

<!-- BossEnemyBase 계약 (변경 금지 — 그대로 상속) -->
Assets/Scripts/Enemy/Boss/BossEnemyBase.cs:
```csharp
public abstract class BossEnemyBase : MonoBehaviour, IEnemy, ISpawnGatable
{
    public bool IsAlive { get; protected set; } = true;
    protected abstract IEnumerator PatternLoop();
    protected abstract void OnPlayerDiedCleanup();
    protected abstract Color GetHighlightColor();
    protected virtual void Die(float maskRiseDuration, Color particleColor, int particleBurstCount,
        float shakeDuration, float shakeAmplitude, string bossId); // ScoreManager.AddBossKillScore() + BossUnlockManager.Unlock(bossId) 내장
    public abstract void OnDashHit();
}
```

<!-- ProjectileController.cs 기존 패턴 — ParryableProjectile이 동일한 Rigidbody2D/Collider2D 물리 구성을 따른다 (신규 파일이지만 재작성 아님) -->
Assets/Scripts/Enemy/ProjectileController.cs:
```csharp
[RequireComponent(typeof(Rigidbody2D))]
[RequireComponent(typeof(Collider2D))]
public class ProjectileController : MonoBehaviour
{
    public void Init(Vector2 direction);          // _rb.linearVelocity = direction.normalized * speed
    private void FixedUpdate();                    // 거리 기반 자기소멸
    private void OnTriggerEnter2D(Collider2D other); // Player 태그 → PlayerController.TriggerDeath()
}
```

<!-- FioraBoss.cs 참고 패턴 (완전히 별개 파일 — SamuraiBoss.cs가 참고할 구조적 선례일 뿐, FioraBoss.cs 자체는 수정하지 않는다) -->
- `HitReactionAndReset()` (271-301줄): 색상 플래시+넉백+정지 후 `PatternLoop()` 재시작 — 동일 구조를 SamuraiBoss에도 적용
- `OnDashHit()` (251-267줄): defeat-guard(`if (_isDefeated) return;`) + 코루틴 중단 후 재시작 패턴
- `GetHighlightColor()` override로 상태별 색상 반환 (Vulnerable 틴트가 ClearHighlight()에서 하드코딩 흰색으로 덮이는 것을 방지)

<!-- MeleeEnemy.cs TelegraphAndAttack() 패턴 (참고용, 수정 대상 아님) — D-07 평시 콤보의 텔레그래프+히트박스 구조 선례 -->
Assets/Scripts/Enemy/MeleeEnemy.cs 143-200줄: `_exclamationIcon.enabled=true` → `WaitForSecondsRealtime` 루프(매 프레임 `!IsAlive` 가드) → 히트박스 활성화 → `WaitForSecondsRealtime(hitboxActiveDuration)` → 비활성화.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ParryableProjectile — 패링 가능한 물리 투사체 (D-03/D-06, Pitfall 2)</name>
  <files>Assets/Scripts/Enemy/ParryableProjectile.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/ProjectileController.cs (전체 — Rigidbody2D/Collider2D 구성 및 OnTriggerEnter2D 패턴 확인, 신규 파일이 따라야 할 물리 컨벤션)
    - Assets/Scripts/Player/Combat/IParryable.cs (Plan 19-01 산출물 — 구현할 계약)
    - Assets/Scripts/Player/PlayerController.cs (`TriggerDeath()` static 메서드 확인)
    - .planning/phases/19-samurai-ui/19-RESEARCH.md Common Pitfalls #2 (트리거 경유 사망 전달의 이유)
  </read_first>
  <action>
    `Assets/Scripts/Enemy/ParryableProjectile.cs`를 신규 작성한다:
    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-03/D-06/Pitfall 2: SAMURAI의 패링 전용 타이밍 공격을 물리 트리거로 전달하는 투사체.
    /// ProjectileController.cs와 동일한 Rigidbody2D(Dynamic, Gravity=0, Continuous+Interpolate)
    /// 패턴을 따르되 IParryable을 구현해 SamuraiParryModule(19-03)이 스윙 범위 안에서 검출한다.
    /// 죽음 전달은 오직 OnTriggerEnter2D뿐이다(Pitfall 2) — RollController의 PlayerInvincible
    /// 레이어 교체가 이 트리거를 자동으로 무시하므로 구르기 회피(D-05)가 별도 코드 없이 성립한다.
    /// </summary>
    [RequireComponent(typeof(Rigidbody2D))]
    [RequireComponent(typeof(Collider2D))]
    public class ParryableProjectile : MonoBehaviour, IParryable
    {
        private Rigidbody2D _rb;
        private Vector2 _startPosition;
        private float _maxDistance;
        private System.Action<Vector2> _onParried;
        private bool _consumed;

        public Vector2 Position => transform.position;

        private void Awake() => _rb = GetComponent<Rigidbody2D>();

        /// <summary>SamuraiBoss.ParryTimingSegment()가 인스턴스 생성 직후(첫 FixedUpdate 이전) 호출한다.</summary>
        public void Init(Vector2 direction, float speed, float maxDistance, System.Action<Vector2> onParried)
        {
            _startPosition = _rb.position;
            _maxDistance = maxDistance;
            _onParried = onParried;
            _rb.linearVelocity = direction.normalized * speed;
        }

        private void FixedUpdate()
        {
            if ((_rb.position - _startPosition).sqrMagnitude >= _maxDistance * _maxDistance)
                Destroy(gameObject);
        }

        /// <summary>D-06: 패링 성공 — 순수 방어, 보스에게 데미지 없음. 투사체 소멸 + 그로기 게이지
        /// 콜백만 수행한다.</summary>
        public void OnParried(Vector2 reflectDirection)
        {
            if (_consumed) return;
            _consumed = true;
            _onParried?.Invoke(reflectDirection);
            Destroy(gameObject);
        }

        private void OnTriggerEnter2D(Collider2D other)
        {
            if (_consumed) return;
            if (other.CompareTag("Player"))
            {
                _consumed = true;
                // Pitfall 2: 오직 이 트리거 경유만 — PlayerInvincible 레이어(구르기 i-frame)가
                // Physics2D 충돌 매트릭스에서 자동으로 이 트리거와의 충돌을 배제해 D-05를 보장한다.
                PlayerController.TriggerDeath();
                Destroy(gameObject);
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "class ParryableProjectile" Assets/Scripts/Enemy/ParryableProjectile.cs; grep -n "IParryable" Assets/Scripts/Enemy/ParryableProjectile.cs; grep -n "PlayerController.TriggerDeath" Assets/Scripts/Enemy/ParryableProjectile.cs; grep -c "PlayerController.TriggerDeath" Assets/Scripts/Enemy/ParryableProjectile.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ParryableProjectile" Assets/Scripts/Enemy/ParryableProjectile.cs` → `class ParryableProjectile : MonoBehaviour, IParryable` 매치
    - `grep -c "PlayerController.TriggerDeath" Assets/Scripts/Enemy/ParryableProjectile.cs` → 정확히 1 (오직 `OnTriggerEnter2D` 안에서만 호출 — 타이머/코루틴에서 직접 호출하는 코드가 없음을 확인, Pitfall 2 준수)
    - `OnParried()`와 `OnTriggerEnter2D()` 모두 `_consumed` 가드로 시작해 중복 처리를 방지한다
    - Unity Editor 컴파일 에러 0건 수동 확인
  </acceptance_criteria>
  <done>ParryableProjectile.cs가 IParryable을 구현하고, 사망 전달이 오직 물리 트리거 경유로만 이루어진다(Pitfall 2 준수).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SamuraiBoss — 평시/패링/그로기 3단계 FSM (D-07~D-11)</name>
  <files>Assets/Scripts/Enemy/Boss/SamuraiBoss.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/Boss/BossEnemyBase.cs (전체 — 상속할 abstract 멤버 확인: `PatternLoop()`/`OnPlayerDiedCleanup()`/`GetHighlightColor()`/`OnDashHit()`, `Die(...)` 시그니처)
    - Assets/Scripts/Enemy/Boss/FioraBoss.cs (전체 — `HitReactionAndReset()`/`OnDashHit()`/색상 틴트 패턴의 구조적 선례, 이 파일 자체는 수정하지 않는다)
    - Assets/Scripts/Enemy/MeleeEnemy.cs (143-200줄 — 텔레그래프+히트박스 구조 선례)
    - Assets/Scripts/Enemy/ParryableProjectile.cs (Task 1 산출물 — `Init()` 시그니처)
    - .planning/phases/19-samurai-ui/19-CONTEXT.md D-07~D-11 (정확한 규칙 재확인)
  </read_first>
  <action>
    `Assets/Scripts/Enemy/Boss/SamuraiBoss.cs`를 신규 작성한다. `BossEnemyBase`를 상속하며, `[SerializeField] private Collider2D _meleeHitbox;`와 `[SerializeField] private SpriteRenderer _exclamationIcon;`은 FioraBoss와 동일한 컨벤션으로 선언(SamuraiBossPrefabBuilder가 19-05에서 배선).

    **상수/튜너블 (정확한 값)**:
    - `public const string BossId = "Samurai";`
    - `private const int RequiredCycles = 7;` (D-11)
    - `[SerializeField] private float normalTelegraphDuration = 0.5f;` (D-07)
    - `[SerializeField] private float hitboxActiveDuration = 0.15f;`
    - `[SerializeField] private float parryTelegraphDuration = 0.3f;` (D-08 windup)
    - `[SerializeField] private float parryProjectileSpeed = 8f;`
    - `[SerializeField] private float parryProjectileMaxDistance = 15f;`
    - `[SerializeField] private float groggyThreshold = 3f;` (D-09, Claude's Discretion — 플레이테스트 튜닝 대상)
    - `[SerializeField] private float groggyPerHit = 1f;` (D-09)
    - `[SerializeField] private float groggyPerParry = 1f;` (D-09)
    - `[SerializeField] private float groggyWindowDuration = 2.5f;` (D-10 그로기 취약 창)
    - `[SerializeField] private Color groggyTintColor = new Color(0.3f, 0.8f, 1f);`
    - `[SerializeField] private Color hitFlashColor = Color.white;`
    - `[SerializeField] private float hitFlashDuration = 0.15f;`
    - `[SerializeField] private float knockbackForce = 4f;`
    - `[SerializeField] private float resetPauseDuration = 0.5f;`
    - `[SerializeField] private float bossMaskRiseDuration = 1.2f;`
    - `[SerializeField] private Color bossParticleColor = new Color(0.25f, 0.5f, 0.9f);` (Fiora의 주황과 구분되는 파란색 계열)
    - `[SerializeField] private int bossParticleBurstCount = 30;`
    - `[SerializeField] private float bossDeathShakeDuration = 0.4f;`
    - `[SerializeField] private float bossDeathShakeAmplitude = 0.5f;`

    **FSM 상태**: `private enum BossState { Normal, ParryWindow, Groggy, HitReaction, Dead }` — `BossState _state = BossState.Normal;`, `private float _groggyGauge;`, `private int _hitCount;`.

    **핵심 설계 원칙 (D-09 재확인)**: 평시 구간(Normal/ParryWindow) 내내 `IsAlive = true`(항상 타겟 가능 — FioraBoss와 달리 Dash-phase 타겟불가 개념이 없다). 즉, 이 보스는 별도 chase/patrol 로직 없이(1v1 보스룸 전제, Claude's Discretion — 범위 최소화) 고정된 로컬 히트박스로 텔레그래프→공격을 반복한다.

    **`PatternLoop()`**:
    ```
    while (true) {
        yield return NormalComboSegment();  // D-07
        if (_isDefeated) yield break;
        yield return ParryTimingSegment();  // D-08
        if (_isDefeated) yield break;

        if (_groggyGauge >= groggyThreshold) {
            _state = BossState.Groggy;
            _sr.color = groggyTintColor;
            float g = 0f;
            while (g < groggyWindowDuration) {
                g += Time.unscaledDeltaTime;
                if (_isDefeated) yield break;
                yield return null;
            }
            // 그로기 창이 히트 없이 종료 -> 게이지 리셋, 평시 복귀 (D-09 재도전 허용)
            _groggyGauge = 0f;
            _state = BossState.Normal;
            _sr.color = _baseColor;
        }
    }
    ```

    **`NormalComboSegment()`**: `_state = BossState.Normal;` → `_exclamationIcon.enabled = true` → `normalTelegraphDuration`초 대기(매 프레임 `Time.unscaledDeltaTime` 누적 + `if (_isDefeated) yield break;` 가드, MeleeEnemy 패턴과 동일) → 아이콘 끄고 `_meleeHitbox.enabled = true` → `WaitForSecondsRealtime(hitboxActiveDuration)` → `_meleeHitbox.enabled = false`.

    **`ParryTimingSegment()`**: `_state = BossState.ParryWindow;` → `_exclamationIcon.enabled = true` → `parryTelegraphDuration`초 대기(동일 가드 패턴) → 아이콘 끔 → `_playerTransform` 방향으로 단위벡터 계산(널이면 `Vector2.left` 폴백) → 절차적으로 투사체 GameObject 생성:
    ```csharp
    var projGO = new GameObject("SamuraiParryProjectile");
    projGO.layer = LayerMask.NameToLayer("Enemy"); // SamuraiParryModule(19-03)이 이 레이어로 조회한다
    projGO.transform.position = transform.position;
    var sr = projGO.AddComponent<SpriteRenderer>();
    sr.color = new Color(0.9f, 0.9f, 1f);
    var rb = projGO.AddComponent<Rigidbody2D>();
    rb.gravityScale = 0f;
    rb.collisionDetectionMode = CollisionDetectionMode2D.Continuous;
    rb.interpolation = RigidbodyInterpolation2D.Interpolate;
    var col = projGO.AddComponent<CircleCollider2D>();
    col.isTrigger = true;
    col.radius = 0.3f;
    var parryable = projGO.AddComponent<ParryableProjectile>();
    parryable.Init(dirToPlayer, parryProjectileSpeed, parryProjectileMaxDistance, OnParryProjectileParried);
    ```
    이후 `parryProjectileMaxDistance / parryProjectileSpeed + 0.5f`초만큼(투사체 비행 시간 + 여유) 매 프레임 `Time.unscaledDeltaTime` 누적하며 대기(`if (_isDefeated) yield break;` 가드) — 이 대기 동안 투사체 자체가 자신의 `OnTriggerEnter2D`/`OnParried()`로 결과를 처리하므로 `SamuraiBoss`는 결과를 폴링하지 않는다.

    `SpriteRenderer sr` 필드명이 `_sr`(BossEnemyBase 소유)과 충돌하지 않도록 지역 변수명은 `projSr` 등으로 구분할 것.

    **`OnParryProjectileParried(Vector2 reflectDir)`**: `_groggyGauge += groggyPerParry;` (D-09) — 그 외 아무것도 하지 않는다(D-06: 순수 방어).

    **`public override void OnDashHit()`**:
    ```csharp
    public override void OnDashHit()
    {
        if (_isDefeated) return;
        if (_patternCoroutine != null) { StopCoroutine(_patternCoroutine); _patternCoroutine = null; }

        if (_state == BossState.Groggy)
        {
            _hitCount++;
            _groggyGauge = 0f;
            if (_hitCount >= RequiredCycles)
            {
                Die(bossMaskRiseDuration, bossParticleColor, bossParticleBurstCount,
                    bossDeathShakeDuration, bossDeathShakeAmplitude, BossId);
                return;
            }
        }
        else
        {
            _groggyGauge += groggyPerHit; // D-09: 평시 타격 성공 -> 게이지만 채움, 처치 카운트 아님 (Pitfall 4 방지)
        }

        _patternCoroutine = StartCoroutine(HitReactionAndReset());
    }
    ```

    **`HitReactionAndReset()`**: FioraBoss.cs 271-301줄과 동일 구조(색상 플래시 `hitFlashColor`/`hitFlashDuration`, `_playerTransform` 반대 방향 넉백 `knockbackForce`, `resetPauseDuration`초 `WaitForSecondsRealtime` 후 `_patternCoroutine = StartCoroutine(PatternLoop());`) — `_state = BossState.HitReaction;`을 시작 시 설정하고 아이콘/히트박스를 끈다.

    **`OnPlayerDiedCleanup()`**: 아이콘/히트박스 비활성화, 애니메이터 `isMoving` false.
    **`GetHighlightColor()`**: `_state == BossState.Groggy ? groggyTintColor : _baseColor`.
  </action>
  <verify>
    <automated>grep -n "class SamuraiBoss" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs; grep -n "BossId = \"Samurai\"" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs; grep -n "RequiredCycles = 7" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs; grep -n "BossState.Groggy" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs; grep -c "Time.deltaTime\|WaitForSeconds(" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs; grep -n "AddComponent<ParryableProjectile>" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class SamuraiBoss" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs` → `class SamuraiBoss : BossEnemyBase` 매치
    - `grep -n "BossId = \"Samurai\""` → 매치 (SAMURAI-01의 BossUnlockManager 키)
    - `grep -n "RequiredCycles = 7"` → 매치 (D-11 정확한 7회)
    - `OnDashHit()`이 `_state == BossState.Groggy`일 때만 `_hitCount++`와 `Die(...)` 호출 경로를 갖고, 그 외에는 `_groggyGauge += groggyPerHit`만 수행한다(D-09/Pitfall 4 방지 — 코드 순서로 확인)
    - `grep -c "Time.deltaTime\|WaitForSeconds(" Assets/Scripts/Enemy/Boss/SamuraiBoss.cs` → 0 (Pitfall 3: 전 구간 `Time.unscaledDeltaTime`/`WaitForSecondsRealtime`만 사용)
    - `grep -n "AddComponent<ParryableProjectile>"` → 매치, `Init(dirToPlayer, parryProjectileSpeed, parryProjectileMaxDistance, OnParryProjectileParried)` 호출 확인
    - `PlayerController.TriggerDeath()`를 `SamuraiBoss.cs`가 직접 호출하는 곳이 `_meleeHitbox`의 `OnTriggerEnter2D` 1곳(평시 콤보 물리 히트박스, FioraBoss와 동일 패턴)뿐인지 확인 — 패링 전용 타이밍의 사망은 전적으로 `ParryableProjectile`이 담당하므로 `ParryTimingSegment()`/`OnParryProjectileParried()` 안에는 `TriggerDeath` 호출이 없어야 한다
    - Unity Editor 컴파일 에러 0건 수동 확인
  </acceptance_criteria>
  <done>SamuraiBoss.cs가 평시/패링/그로기 3단계 루프와 7회 처치 조건을 구현하고, BossEnemyBase.Die()를 통해 BossUnlockManager.Unlock("Samurai")를 트리거할 수 있다.</done>
</task>

</tasks>

<verification>
- `ParryableProjectile`이 `IParryable`을 구현하고 사망 전달이 오직 `OnTriggerEnter2D` 경유로만 이루어진다.
- `SamuraiBoss`의 `OnDashHit()`이 그로기 상태와 평시 상태를 명확히 분기해 D-09/D-10/D-11을 구현한다.
- 전 구간 `Time.unscaledDeltaTime`/`WaitForSecondsRealtime`만 사용한다(Pitfall 3).
</verification>

<success_criteria>
SAMURAI-01(격파 시 최초 해금)/SAMURAI-03(패링 성공 시 무효화+반사)/SAMURAI-04(평시/패링 교차, 실패 시 즉사)/SAMURAI-05(타이밍 튜너블 노출)의 코드 기반이 완성된다. 실측 타이밍 튜닝과 실플레이 검증은 19-06에서 수행.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-04-SUMMARY.md`
</output>
</content>
