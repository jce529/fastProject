---
phase: 19-samurai-ui
plan: 05
type: execute
wave: 3
depends_on: ["19-03", "19-04"]
files_modified:
  - Assets/Editor/SamuraiBossPrefabBuilder.cs
  - Assets/Editor/RoomSamuraiFsmTestBuilder.cs
  - Assets/Editor/DebugSceneBuilder.cs
  - Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs
  - Assets/Scripts/Player/CombatController.cs
autonomous: true
requirements: [SAMURAI-01, SAMURAI-05]

must_haves:
  truths:
    - "Fast/Phase19/Build SamuraiBoss Prefab 메뉴 실행 시 Assets/Prefabs/Enemies/SamuraiBoss.prefab이 생성된다"
    - "Fast/Phase19/Build Room_SamuraiFsmTest 메뉴 실행 시 nested SamuraiBoss 인스턴스를 포함한 룸 프리팹이 생성된다"
    - "Fast/Debug/Build DebugScene 재실행 시 기존 Room_BossFsmTest(FioraBoss)에 더해 Room_SamuraiFsmTest로 이동 가능한 텔레포터 트리거가 추가된다"
    - "DebugScene Play 중 숫자키 1/2/3으로 기본전투모듈/Overclock/사무라이 전투형 모듈을 즉시 전환할 수 있다 (D-18 대체 검증 경로)"
  artifacts:
    - path: "Assets/Editor/SamuraiBossPrefabBuilder.cs"
      provides: "MeleeEnemy.prefab 구조를 복제해 SamuraiBoss.prefab 생성 (BossEnemyPrefabBuilder.cs D-10 패턴 재사용)"
      contains: "MenuItem(\"Fast/Phase19/Build SamuraiBoss Prefab\")"
    - path: "Assets/Editor/RoomSamuraiFsmTestBuilder.cs"
      provides: "Room_SamuraiFsmTest.prefab — RoomBossFsmTestBuilder.cs와 동일 컨벤션"
      contains: "MenuItem(\"Fast/Phase19/Build Room_SamuraiFsmTest\")"
    - path: "Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs"
      provides: "D-18 대체 — 실제 로비 플로우 없이 DebugScene에서 모듈을 즉시 전환하는 테스트 전용 컴포넌트"
      contains: "class DebugCombatModuleSwitcher : MonoBehaviour"
  key_links:
    - from: "Assets/Editor/DebugSceneBuilder.cs"
      to: "Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab"
      via: "Build()가 Room_BossFsmTest 인스턴스 근처에 DebugRoomTeleporter 트리거 pad를 추가 배치"
      pattern: "Room_SamuraiFsmTest"
    - from: "Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs"
      to: "Assets/Scripts/Player/CombatController.cs"
      via: "숫자키 입력 시 CombatController.DebugSetActiveModule(CombatModuleId) 호출"
      pattern: "DebugSetActiveModule"
---

<objective>
D-14(Phase 18/18.1 선례대로 DebugScene 확장)와 D-18(실제 튜토리얼→로비 씬 전환 배선은 범위 밖 — DebugScene 내 대체 검증)을 만족하는 에디터 도구 2종 + DebugSceneBuilder 확장 + 런타임 테스트 헬퍼 1종을 만든다. 이 Plan은 도구를 **작성만** 한다 — 실제 메뉴 실행과 씬/프리팹 저장은 19-06의 checkpoint에서 일괄 수행한다(git diff로 이 Plan 완료 시점엔 씬/프리팹 애셋이 아직 변경되지 않았음을 확인 가능해야 한다).

Purpose: SAMURAI-01~05를 DebugScene에서 실제로 플레이 검증할 수 있는 환경을 갖춘다. 19-VALIDATION.md의 Manual-Only Verifications 표가 요구하는 "DebugScene-side toggle(실제 로비 UI 아님)"을 `DebugCombatModuleSwitcher`로 구현한다.
Output: 신규 에디터 도구 2개 + `DebugSceneBuilder.cs` 확장 + 신규 런타임 디버그 컴포넌트 1개 + `CombatController.cs`에 디버그 전용 public 메서드 1개 추가.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md

<interfaces>
<!-- Plan 19-04 산출물 — SamuraiBossPrefabBuilder가 배선할 대상 -->
Assets/Scripts/Enemy/Boss/SamuraiBoss.cs:
```csharp
public class SamuraiBoss : BossEnemyBase
{
    public const string BossId = "Samurai";
    [SerializeField] private SpriteRenderer _exclamationIcon;
    [SerializeField] private Collider2D _meleeHitbox;
}
```

<!-- Plan 19-02 산출물 -->
Assets/Scripts/UI/CombatModuleRegistry.cs: `public enum CombatModuleId { Basic, Overclock, Samurai }`

<!-- Plan 19-03 산출물 — CombatController가 이미 갖고 있는 private 팩토리 (재사용 대상, 이 Plan이 public 래퍼만 추가) -->
Assets/Scripts/Player/CombatController.cs:
```csharp
private IPlayerCombatModule BuildModule(CombatModuleId id) { /* switch: Overclock/Samurai/Basic */ }
```

<!-- Assets/Editor/BossEnemyPrefabBuilder.cs 전체 (Task 1이 그대로 미러링) — MeleeEnemy.prefab 클론 -> 컴포넌트 교체 -> compound trigger collider 변환 -> scale/tint 패턴.
`SourcePrefabPath = "Assets/Prefabs/Enemies/MeleeEnemy.prefab"`, `PrefabUtility.LoadPrefabContents`/`SaveAsPrefabAsset` 사용. `MeleeHitbox` 자식 오브젝트를 제거하고 루트에
`CapsuleCollider2D(isTrigger=true)`를 body collider 크기의 1.08배로 compound 추가하는 정렬 버그 회피 패턴(18.1-01 교훈)을 그대로 반복한다. -->

<!-- Assets/Editor/RoomBossFsmTestBuilder.cs 전체 (Task 1이 그대로 미러링) — 평평한 바닥 Tilemap + RoomEntry/ExitSpawnPoint + Door/ENT+EXIT RoomConnector + CameraBound + nested boss instance -->

<!-- Assets/Editor/DebugSceneBuilder.cs 전체 (Task 2가 확장) — 현재 Room_BossFsmTest(FioraBoss)를 원점에 배치, 플레이어 클론, 정적 카메라, Canvas+return 버튼까지 구성 후 저장.
DebugRoomTeleporter는 static `s_lastDebugRoom`/`s_nextSpawnPos`로 "직전 텔레포트 도착 룸"만 추적하므로, DebugSceneBuilder가 직접 Instantiate한 Room_BossFsmTest는 그 static 추적 대상이 아니어서 텔레포트로 파괴되지 않는다. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SamuraiBossPrefabBuilder + RoomSamuraiFsmTestBuilder</name>
  <files>Assets/Editor/SamuraiBossPrefabBuilder.cs, Assets/Editor/RoomSamuraiFsmTestBuilder.cs</files>
  <read_first>
    - Assets/Editor/BossEnemyPrefabBuilder.cs (전체 — Task 1의 SamuraiBossPrefabBuilder가 그대로 미러링할 정확한 클론/컴포넌트교체/compound-collider 로직)
    - Assets/Editor/RoomBossFsmTestBuilder.cs (전체 — Task 1의 RoomSamuraiFsmTestBuilder가 그대로 미러링할 정확한 룸 구성 로직)
    - Assets/Scripts/Enemy/Boss/SamuraiBoss.cs (Plan 19-04 산출물 — `_exclamationIcon`/`_meleeHitbox` SerializedProperty 이름 확인)
  </read_first>
  <action>
    **1) `SamuraiBossPrefabBuilder.cs`** — `BossEnemyPrefabBuilder.cs`의 `BuildBossEnemyPrefab()`을 그대로 미러링하되:
    - `SourcePrefabPath = "Assets/Prefabs/Enemies/MeleeEnemy.prefab"` (동일)
    - `TargetPrefabPath = "Assets/Prefabs/Enemies/SamuraiBoss.prefab"`
    - `Object.DestroyImmediate(clone.GetComponent<MeleeEnemy>())` 후 `clone.AddComponent<SamuraiBoss>()` (FioraBoss 대신)
    - `BossScale = new Vector3(1.5f, 1.5f, 1f)` (Fiora의 1.6배와 살짝 다르게 — 시각적 구분, Claude's Discretion)
    - `BossTint = new Color(0.25f, 0.35f, 0.6f)` (청회색 — Fiora의 진한 붉은색과 구분)
    - `MeleeHitbox` 자식 제거 후 루트 compound `CapsuleCollider2D(isTrigger=true, size = bodyCapsule.size * 1.08f)` 추가하는 로직은 BossEnemyPrefabBuilder.cs와 동일하게 반복
    - `SerializedObject(boss).FindProperty("_exclamationIcon")`/`FindProperty("_meleeHitbox")`에 각각 배선
    - 메뉴: `[MenuItem("Fast/Phase19/Build SamuraiBoss Prefab")]`

    **2) `RoomSamuraiFsmTestBuilder.cs`** — `RoomBossFsmTestBuilder.cs`의 `Build()`를 그대로 미러링하되:
    - `RoomName = "Room_SamuraiFsmTest"`
    - `PrefabPath = "Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab"`
    - `BossPrefabPath = "Assets/Prefabs/Enemies/SamuraiBoss.prefab"`
    - 나머지(평평한 바닥 x:-14~14, RoomEntry+ExitSpawnPoint at (0,1,0), Door/ENT(Left)+EXIT(Right) RoomConnector, CameraBound size=(30,40), nested boss instance at (6,1,0))는 완전히 동일한 구성
    - 메뉴: `[MenuItem("Fast/Phase19/Build Room_SamuraiFsmTest")]`
    - `SamuraiBossPrefabBuilder`가 먼저 실행되어야 함을 알리는 동일한 null 체크 + 에러 로그 패턴 유지(`RoomBossFsmTestBuilder.cs`의 `BossPrefabPath` null 체크 그대로)
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase19/Build SamuraiBoss Prefab\")" Assets/Editor/SamuraiBossPrefabBuilder.cs; grep -n "AddComponent<SamuraiBoss>" Assets/Editor/SamuraiBossPrefabBuilder.cs; grep -n "MenuItem(\"Fast/Phase19/Build Room_SamuraiFsmTest\")" Assets/Editor/RoomSamuraiFsmTestBuilder.cs; grep -n "Room_SamuraiFsmTest.prefab" Assets/Editor/RoomSamuraiFsmTestBuilder.cs; git diff --stat Assets/Prefabs/</automated>
  </verify>
  <acceptance_criteria>
    - `SamuraiBossPrefabBuilder.cs`가 `[MenuItem("Fast/Phase19/Build SamuraiBoss Prefab")]`를 정의하고 `AddComponent<SamuraiBoss>()`를 호출한다
    - `RoomSamuraiFsmTestBuilder.cs`가 `[MenuItem("Fast/Phase19/Build Room_SamuraiFsmTest")]`를 정의하고 `Room_SamuraiFsmTest.prefab` 경로를 사용한다
    - `git diff --stat Assets/Prefabs/` → 빈 출력 (이 Task는 도구 작성만, 아직 어떤 프리팹 애셋도 생성/수정되지 않음 — 실제 실행은 19-06)
    - Unity Editor 컴파일 에러 0건 수동 확인
  </acceptance_criteria>
  <done>두 에디터 도구가 기존 Fiora용 도구와 동일한 컨벤션으로 존재하며, 아직 실행되지 않아 프리팹 애셋에 변화가 없다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DebugSceneBuilder 확장 + DebugCombatModuleSwitcher + CombatController 디버그 훅</name>
  <files>Assets/Editor/DebugSceneBuilder.cs, Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs, Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Editor/DebugSceneBuilder.cs (전체 — 정확한 삽입 지점 확인, 특히 54-63줄 Room_BossFsmTest 배치 이후 ~ 86줄 Canvas 구성 이전 사이)
    - Assets/Scripts/World/DebugRoomTeleporter.cs (전체 — `targetRoomPrefab`/`_bossPrefab` SerializedProperty 이름, static `s_lastDebugRoom` 추적 범위 확인)
    - Assets/Scripts/Player/CombatController.cs (Plan 19-03이 추가한 `BuildModule(CombatModuleId)` private 메서드 — 이 Task는 이를 감싸는 public 래퍼만 추가)
    - Assets/Scripts/UI/CombatModuleRegistry.cs (`CombatModuleId` enum)
  </read_first>
  <action>
    **1) `CombatController.cs`에 디버그 전용 public 메서드 추가** — 기존 `BuildModule(CombatModuleId id)` private 메서드 바로 다음 줄에:
    ```csharp
        /// <summary>D-18 대체 — 실제 로비 UI 없이 DebugScene에서 즉시 모듈을 바꿔 테스트하기 위한
        /// 디버그 전용 훅. DebugCombatModuleSwitcher가 호출한다. 정식 플로우(Awake 시점 결정)는
        /// 변경하지 않는다.</summary>
        public void DebugSetActiveModule(CombatModuleId id) => _activeModule = BuildModule(id);
    ```
    이 외 `CombatController.cs`의 어떤 라인도 수정하지 않는다.

    **2) `DebugCombatModuleSwitcher.cs`** (신규):
    ```csharp
    using UnityEngine;
    using UnityEngine.InputSystem;

    /// <summary>D-18: 실제 튜토리얼→로비 플로우 배선은 이번 Phase 범위 밖 — 대신 DebugScene에서
    /// 숫자키로 즉시 모듈을 바꿔 SAMURAI-02/03/04를 기본전투모듈/사무라이 전투형 모듈/Overclock
    /// 각각에 대해 반복 검증할 수 있게 하는 테스트 전용 컴포넌트.</summary>
    public class DebugCombatModuleSwitcher : MonoBehaviour
    {
        [SerializeField] private CombatController _combatController;

        private void Update()
        {
            if (Keyboard.current == null || _combatController == null) return;
            if (Keyboard.current.digit1Key.wasPressedThisFrame) _combatController.DebugSetActiveModule(CombatModuleId.Basic);
            if (Keyboard.current.digit2Key.wasPressedThisFrame) _combatController.DebugSetActiveModule(CombatModuleId.Overclock);
            if (Keyboard.current.digit3Key.wasPressedThisFrame) _combatController.DebugSetActiveModule(CombatModuleId.Samurai);
        }
    }
    ```

    **3) `DebugSceneBuilder.cs` 확장** — 기존 `Build()` 메서드의 4번 단계(Room_BossFsmTest 배치, 54-62줄) 완료 직후, 5번 단계(Canvas 구성, 86줄~) 시작 전에 새 단계를 삽입한다:
    - `Room_SamuraiFsmTest.prefab`을 `AssetDatabase.LoadAssetAtPath<GameObject>("Assets/Prefabs/Rooms/Room_SamuraiFsmTest/Room_SamuraiFsmTest.prefab")`로 로드. null이면 기존 `roomPrefab` null 체크와 동일한 패턴으로 에러 로그 후 `return`("Fast/Phase19/Build Room_SamuraiFsmTest 먼저 실행" 안내).
    - 텔레포터 pad GameObject `"ToSamuraiRoom_Teleporter"`를 생성: `BoxCollider2D(isTrigger=true, size=(1,1))`, 위치는 `entryPos + new Vector3(10f, 0f, 0f)`(Room_BossFsmTest의 ENT/EXIT 스팬 28유닛 안쪽, 플레이어 스폰 지점에서 도보 접근 가능한 거리), `DebugRoomTeleporter` 컴포넌트 추가 후 `SerializedObject`로 `targetRoomPrefab` = Room_SamuraiFsmTest.prefab만 설정(`_bossPrefab`은 비워둠 — SamuraiBoss는 룸 프리팹에 이미 nested되어 있으므로 불필요, Room_BossFsmTest와 동일 컨벤션).
    - `playerCopy`(이미 존재하는 클론 플레이어 GameObject)에 `DebugCombatModuleSwitcher` 컴포넌트를 추가하고 `SerializedObject`로 `_combatController` 필드에 `playerCopy.GetComponent<CombatController>()`를 대입.
    - 마지막 `Debug.Log(...)` 메시지(147줄)에 "+ Room_SamuraiFsmTest 텔레포터 pad + DebugCombatModuleSwitcher(숫자키 1/2/3)" 문구를 추가.
    - 기존 Room_BossFsmTest 배치/카메라/Canvas/return 버튼 로직은 단 한 글자도 바꾸지 않는다 — 순수 추가 삽입이다.
  </action>
  <verify>
    <automated>grep -n "DebugSetActiveModule" Assets/Scripts/Player/CombatController.cs; grep -n "class DebugCombatModuleSwitcher" Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs; grep -n "digit1Key\|digit2Key\|digit3Key" Assets/Scripts/Debug/DebugCombatModuleSwitcher.cs; grep -n "Room_SamuraiFsmTest" Assets/Editor/DebugSceneBuilder.cs; grep -n "ToSamuraiRoom_Teleporter" Assets/Editor/DebugSceneBuilder.cs; git diff --stat Assets/Scenes/DebugScene.unity</automated>
  </verify>
  <acceptance_criteria>
    - `CombatController.cs`에 `public void DebugSetActiveModule(CombatModuleId id) => BuildModule(id);` 형태의 1줄 래퍼가 존재하고, 그 외 라인은 무변경이다(`git diff Assets/Scripts/Player/CombatController.cs`에서 추가 라인 1~2줄만 확인되어야 함)
    - `DebugCombatModuleSwitcher.cs`가 숫자키 1/2/3 각각을 `CombatModuleId.Basic`/`Overclock`/`Samurai`에 매핑한다
    - `DebugSceneBuilder.cs`가 `Room_SamuraiFsmTest.prefab`을 로드하고 `"ToSamuraiRoom_Teleporter"` GameObject + `DebugRoomTeleporter` 컴포넌트를 생성하는 코드를 포함한다
    - `git diff --stat Assets/Scenes/DebugScene.unity` → 빈 출력 (이 Task는 도구 코드만 작성 — 씬 파일 자체의 실제 재생성/저장은 19-06의 checkpoint에서 메뉴 실행으로 수행)
    - Unity Editor 컴파일 에러 0건 수동 확인
  </acceptance_criteria>
  <done>DebugSceneBuilder.cs가 SAMURAI 테스트 룸 연결 코드를 포함하도록 확장되었고, DebugCombatModuleSwitcher와 CombatController의 디버그 훅이 존재하며, CombatController.cs의 기존 로직은 1줄 추가 외 무변경이다.</done>
</task>

</tasks>

<verification>
- `SamuraiBossPrefabBuilder.cs`/`RoomSamuraiFsmTestBuilder.cs`가 기존 Fiora용 도구와 동일한 컨벤션(멱등적 재실행, 명확한 사전조건 에러 로그)으로 작성되었다.
- `DebugSceneBuilder.cs` 확장이 기존 Room_BossFsmTest 배치 로직을 전혀 건드리지 않는 순수 추가다.
- `CombatController.cs`는 1줄짜리 public 디버그 래퍼 외 무변경이다.
</verification>

<success_criteria>
D-14/D-18이 요구하는 DebugScene 확장 도구 일체가 준비된다. 19-06에서 실제 메뉴 실행 + 통합 플레이테스트를 수행한다.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-05-SUMMARY.md`
</output>
</content>
