---
phase: 19-samurai-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs
  - Assets/Scripts/Player/Combat/IParryable.cs
  - Assets/Scripts/Player/Combat/AimUtil.cs
  - Assets/Scripts/Player/Combat/CombatContext.cs
  - Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs
  - Assets/Scripts/Player/Combat/BasicCombatModule.cs
autonomous: true
requirements: [SAMURAI-02]

must_haves:
  truths:
    - "기본전투모듈이 CombatController에 장착되면 Attack 탭 시 슬로우모션 없이(Time.timeScale이 절대 1에서 벗어나지 않고) 즉시 제자리 스윙이 나간다"
    - "스윙 부채꼴 안에 있는 IEnemy(MeleeEnemy/RangedEnemy/보스 공통)는 탭 1회로 원샷 처치된다"
    - "탭 직후 짧은 고정 락아웃 동안에는 재입력이 무시된다"
  artifacts:
    - path: "Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs"
      provides: "CombatController가 Overclock 방식 hold-slowmo 루프를 우회하는 additive marker 인터페이스"
      contains: "interface IRealtimeCombatModule"
    - path: "Assets/Scripts/Player/Combat/IParryable.cs"
      provides: "패링 가능한 오브젝트(투사체)의 side-channel 계약 — IEnemy 확장 금지 원칙 준수"
      contains: "interface IParryable"
    - path: "Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs"
      provides: "D-01~D-03 공유 탭 스윙 로직 — 기본전투모듈/사무라이 전투형 모듈의 공통 베이스"
      contains: "abstract class TapSwingCombatModuleBase : IPlayerCombatModule, IRealtimeCombatModule"
    - path: "Assets/Scripts/Player/Combat/BasicCombatModule.cs"
      provides: "D-15/D-16 기본전투모듈 — 패링 오버라이드 없는 순정 스윙"
      contains: "class BasicCombatModule : TapSwingCombatModuleBase"
  key_links:
    - from: "Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs"
      to: "Assets/Scripts/Player/Combat/AimUtil.cs"
      via: "ResolveSwing()이 AimUtil.GetMouseWorldDirection() 호출"
      pattern: "AimUtil\\.GetMouseWorldDirection"
    - from: "Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs"
      to: "Assets/Scripts/Player/Combat/CombatContext.cs"
      via: "ctx.SwingRadius/SwingHalfAngleDeg/TapLockout 신규 필드 사용"
      pattern: "ctx\\.(SwingRadius|SwingHalfAngleDeg|TapLockout)"
---

<objective>
SAMURAI-02(패링 없이 실시간으로 동작, 탭=즉시 방향성 스윙)의 공유 기반을 만든다. Overclock의 hold-slowmo→release-resolve 상태머신을 절대 거치지 않는 새 모듈 카테고리(`IRealtimeCombatModule`)를 정의하고, D-01~D-03(제자리 방향성 스윙/원샷킬/짧은 고정 락아웃)을 구현하는 공유 베이스 클래스를 만든 뒤, 그 위에 패링이 없는 D-15/D-16 "기본전투모듈"을 얹는다. 사무라이 전투형 모듈(패링 포함)은 이 베이스를 상속하는 후속 Plan(19-03)의 몫이다.

Purpose: 19-RESEARCH.md §1(host-hook 패턴)이 권장한 additive marker interface를 실제로 만들어, `CombatController.cs`를 건드리지 않고도(이 Plan은 CombatController.cs를 전혀 수정하지 않는다) 다음 Plan들이 독립적으로 병렬 진행할 수 있는 계약을 먼저 확정한다(Interface-First 순서).
Output: `Assets/Scripts/Player/Combat/` 폴더에 신규 파일 5개 + `CombatContext.cs`에 3개 필드 추가.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md

<interfaces>
<!-- 현재 IPlayerCombatModule 계약 (변경 금지 — closed contract, 이 Plan은 이 파일을 수정하지 않는다) -->
Assets/Scripts/Player/Combat/IPlayerCombatModule.cs:
```csharp
public interface IPlayerCombatModule
{
    IEnemy FindTarget(UnityEngine.Vector2 origin, CombatContext ctx);
    IEnumerator Resolve(IEnemy target, CombatContext ctx);
    IEnumerator Whiff(CombatContext ctx);
}
```

<!-- InputManager 탭 감지 — 이미 존재, 신규 입력 액션 불필요 (D-04) -->
Assets/Scripts/Player/InputManager.cs:
```csharp
public bool AttackHeld => _attackHeldThisFrame;      // 좌클릭이 눌린 프레임 단 1회 true
public bool IsAttackDown => _attackAction.IsPressed();
```

<!-- IEnemy 계약 (변경 금지, 3-member 고정) -->
Assets/Scripts/Enemy/IEnemy.cs:
```csharp
public interface IEnemy
{
    bool IsAlive { get; }
    void OnDashHit();
    void ClearHighlight();
}
```

<!-- CombatContext.cs 현재 전체 (31 lines) — Task 1이 여기에 3개 필드를 추가한다 -->
Assets/Scripts/Player/Combat/CombatContext.cs:
```csharp
public class CombatContext
{
    public Rigidbody2D Rb;
    public SpriteRenderer SpriteRenderer;
    public Animator Animator;
    public TrailRenderer TrailRenderer;
    public InvincibilityHandler Invincibility;
    public CameraFollow CameraFollow;
    public ChronoGaugeController Gauge;
    public Camera MainCamera;

    public GameObject HitSparkPrefab;
    public float DashDuration;
    public float HitFreezeDuration;
    public float PostKillLockout;
    public float WhiffLockout;
    public float CameraShakeDuration;
    public float CameraShakeAmplitude;
    public float SearchRadius;
    public float FanRadius;
    public float FanHalfAngleDeg;
    public float LinearHalfAngleDeg;

    public ContactFilter2D EnemyFilter;
    public Collider2D[] HitBuffer;
    public int ObstacleMask;

    public System.Action<float> SetAttackCooldown;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 계약 정의 — IRealtimeCombatModule + IParryable + AimUtil + CombatContext 확장</name>
  <files>Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs, Assets/Scripts/Player/Combat/IParryable.cs, Assets/Scripts/Player/Combat/AimUtil.cs, Assets/Scripts/Player/Combat/CombatContext.cs</files>
  <read_first>
    - Assets/Scripts/Player/Combat/CombatContext.cs (전체 — 추가할 3개 필드의 정확한 삽입 지점 확인)
    - Assets/Scripts/Player/Combat/OverclockModule.cs (154-162줄 `GetMouseWorldDirection` — AimUtil이 이 로직과 동일한 계산을 하되 OverclockModule.cs 자체는 절대 건드리지 않는다, INFRA-01 회귀 방지)
    - Assets/Scripts/Player/Combat/IPlayerCombatModule.cs (닫힌 계약 확인 — 수정 금지)
  </read_first>
  <action>
    4개 파일을 작성한다. **`OverclockModule.cs`는 이 Task에서 절대 열거나 수정하지 않는다** — INFRA-01(이미 실플레이 검증 완료)의 회귀 위험을 원천 차단하기 위해 기존 private `GetMouseWorldDirection`은 그대로 두고, `AimUtil.cs`는 완전히 새로운 파일로 별도 존재한다(두 곳에 동일 로직이 잠시 중복되는 것을 허용 — Don't Hand-Roll 항목이지만 이미 검증된 Overclock 경로를 건드리지 않는 것이 우선).

    **1) `IRealtimeCombatModule.cs`** (신규):
    ```csharp
    using UnityEngine;

    /// <summary>
    /// SAMURAI-02: CombatController.Update()의 Overclock 전용 hold-slowmo→release-resolve
    /// 상태머신을 완전히 우회하는 additive marker 인터페이스. IPlayerCombatModule을 대체하지 않고
    /// 나란히 구현된다 — IEnemy를 닫힌 계약으로 유지하고 ISpawnGatable을 별도로 얹은 프로젝트의
    /// 기존 컨벤션과 동일하다(19-RESEARCH.md §1).
    /// </summary>
    public interface IRealtimeCombatModule
    {
        /// <summary>CombatController.Update()가 Overclock 분기 대신 매 프레임 호출한다.
        /// 이 모듈은 자신의 입력 폴링/타이밍/판정을 전부 스스로 소유한다.</summary>
        void Tick(CombatContext ctx);
    }
    ```

    **2) `IParryable.cs`** (신규):
    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-04/D-06: 패링 가능한 오브젝트(SAMURAI의 패링 전용 타이밍 투사체)의 side-channel 계약.
    /// IEnemy를 4번째 멤버로 확장하지 않는다(ARCHITECTURE.md Anti-Pattern 2, 19-CONTEXT.md canonical_refs).
    /// </summary>
    public interface IParryable
    {
        Vector2 Position { get; }

        /// <summary>패링 성공 시 호출된다. reflectDirection은 플레이어의 조준 방향(D-06: 반사 방향).</summary>
        void OnParried(Vector2 reflectDirection);
    }
    ```

    **3) `AimUtil.cs`** (신규 — OverclockModule의 기존 private `GetMouseWorldDirection`과 동일한 계산을 공용 static 헬퍼로 제공, OverclockModule.cs 자체는 무변경):
    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-01: 마우스→월드 방향 계산 공용 헬퍼. TapSwingCombatModuleBase(기본전투모듈/사무라이 전투형 모듈)가
    /// 사용한다. OverclockModule.cs의 기존 private GetMouseWorldDirection과 동일한 계산이지만 별도
    /// 파일로 존재한다 — 이미 실플레이 검증된 Overclock 경로(INFRA-01)를 리팩토링 대상에서 제외하기 위함.
    /// </summary>
    public static class AimUtil
    {
        public static Vector2 GetMouseWorldDirection(Vector2 origin, Camera mainCamera)
        {
            UnityEngine.InputSystem.Mouse mouse = UnityEngine.InputSystem.Mouse.current;
            Vector2 mousePos = mouse != null ? mouse.position.ReadValue() : (Vector2)mainCamera.WorldToScreenPoint(origin);
            Vector3 mouseWorld = mainCamera.ScreenToWorldPoint(new Vector3(mousePos.x, mousePos.y, Mathf.Abs(mainCamera.transform.position.z)));
            Vector2 dir = (Vector2)mouseWorld - origin;
            return dir.sqrMagnitude > 0.001f ? dir.normalized : Vector2.right;
        }
    }
    ```

    **4) `CombatContext.cs` 수정** — 기존 21개 필드는 한 글자도 바꾸지 않고, `public int ObstacleMask;` 필드 선언 바로 다음 줄에 3개 필드를 추가한다(순수 추가, D-01~D-03/D-15/D-16 튜너블 — Overclock은 이 필드들을 전혀 참조하지 않으므로 회귀 없음):
    ```csharp
        public float SwingRadius;       // D-01: 기본/사무라이 전투형 모듈 스윙 반경 (Overclock의 SearchRadius/FanRadius와 독립 — Open Question 2)
        public float SwingHalfAngleDeg; // D-01: 스윙 부채꼴 절반각
        public float TapLockout;        // D-03: 탭 공격 사이 짧은 고정 락아웃
    ```
  </action>
  <verify>
    <automated>grep -n "interface IRealtimeCombatModule" Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs; grep -n "interface IParryable" Assets/Scripts/Player/Combat/IParryable.cs; grep -n "static class AimUtil" Assets/Scripts/Player/Combat/AimUtil.cs; grep -c "SwingRadius\|SwingHalfAngleDeg\|TapLockout" Assets/Scripts/Player/Combat/CombatContext.cs; git diff --stat Assets/Scripts/Player/Combat/OverclockModule.cs</automated>
  </verify>
  <acceptance_criteria>
    - `IRealtimeCombatModule.cs`가 존재하고 정확히 1개 멤버(`Tick(CombatContext ctx)`)를 가진 인터페이스를 선언한다
    - `IParryable.cs`가 존재하고 `Position` 프로퍼티 + `OnParried(Vector2)` 메서드 2개 멤버를 선언한다
    - `AimUtil.cs`가 `public static class AimUtil`을 선언하고 `GetMouseWorldDirection(Vector2, Camera)` static 메서드를 갖는다
    - `grep -c "SwingRadius\|SwingHalfAngleDeg\|TapLockout" Assets/Scripts/Player/Combat/CombatContext.cs` → 3 (신규 필드 3개, 기존 21개 필드는 diff 없음)
    - `git diff --stat Assets/Scripts/Player/Combat/OverclockModule.cs` → 빈 출력 (이 Task에서 무변경 확인)
  </acceptance_criteria>
  <done>4개 계약/헬퍼 파일이 정확한 시그니처로 존재하고, CombatContext.cs는 3개 필드만 순수 추가되었으며, OverclockModule.cs는 완전히 무변경이다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TapSwingCombatModuleBase + BasicCombatModule 구현</name>
  <files>Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs, Assets/Scripts/Player/Combat/BasicCombatModule.cs</files>
  <read_first>
    - Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs, IPlayerCombatModule.cs, AimUtil.cs, CombatContext.cs (Task 1 산출물 — 이 시그니처/필드를 그대로 사용)
    - Assets/Scripts/Player/Combat/OverclockModule.cs (11-51줄 `IsInAttackShape`/`FindTarget` — 유사한 dot-product 부채꼴 판정을 참고하되 별도 구현, ctx.EnemyFilter/ctx.HitBuffer 재사용 패턴 확인)
    - Assets/Scripts/Enemy/IEnemy.cs (`OnDashHit()` — 원샷킬 트리거)
  </read_first>
  <action>
    **1) `TapSwingCombatModuleBase.cs`** (신규 — D-01~D-03):
    ```csharp
    using System.Collections;
    using UnityEngine;

    /// <summary>
    /// D-01~D-03: 기본전투모듈/사무라이 전투형 모듈이 공유하는 탭 스윙 로직.
    /// IRealtimeCombatModule.Tick()에서 매 프레임 락아웃을 감소시키고, 탭(AttackHeld 1프레임)
    /// 시 ResolveSwing()을 실행한다. 슬로우모션/게이지/HitFreeze를 절대 건드리지 않는다(SAMURAI-02).
    /// </summary>
    public abstract class TapSwingCombatModuleBase : IPlayerCombatModule, IRealtimeCombatModule
    {
        private float _lockoutRemaining;

        public void Tick(CombatContext ctx)
        {
            if (_lockoutRemaining > 0f)
                _lockoutRemaining -= Time.unscaledDeltaTime; // D-03: 슬로우모션 무관 — 항상 실시간

            var input = InputManager.Instance;
            if (input.AttackHeld && _lockoutRemaining <= 0f)
            {
                ResolveSwing(ctx);
                _lockoutRemaining = ctx.TapLockout;
            }
        }

        /// <summary>D-01: 제자리 방향성 스윙(마우스 방향) — D-02: 부채꼴 안 가장 가까운 IEnemy 원샷킬.
        /// SamuraiParryModule(19-03)이 이 메서드를 오버라이드해 패링 판정을 먼저 검사하고,
        /// 패링 대상이 없으면 base.ResolveSwing(ctx)로 폴백한다.</summary>
        protected virtual void ResolveSwing(CombatContext ctx)
        {
            Vector2 origin = ctx.Rb.position;
            Vector2 dir = AimUtil.GetMouseWorldDirection(origin, ctx.MainCamera);
            ctx.SpriteRenderer.flipX = dir.x < 0f;

            int count = Physics2D.OverlapCircle(origin, ctx.SwingRadius, ctx.EnemyFilter, ctx.HitBuffer);
            IEnemy nearest = null;
            float bestSqDist = float.MaxValue;
            float cosHalf = Mathf.Cos(ctx.SwingHalfAngleDeg * Mathf.Deg2Rad);

            for (int i = 0; i < count; i++)
            {
                var enemy = ctx.HitBuffer[i].GetComponent<IEnemy>();
                if (enemy == null || !enemy.IsAlive) continue;

                Vector2 targetPos = (Vector2)ctx.HitBuffer[i].transform.position;
                Vector2 toTarget = targetPos - origin;
                float dist = toTarget.magnitude;
                if (dist > ctx.SwingRadius) continue;
                if (Vector2.Dot(dir, toTarget.normalized) < cosHalf) continue;

                float sqDist = dist * dist;
                if (sqDist < bestSqDist) { bestSqDist = sqDist; nearest = enemy; }
            }

            if (nearest != null)
            {
                nearest.OnDashHit(); // D-02: 원샷킬, 모든 적(MeleeEnemy/RangedEnemy/보스) 공통
                AudioManager.PlaySfx(Sfx.Slash); // 기존 SFX 자산 재사용 — 전용 SFX 없음(Claude's Discretion)
            }
        }

        // IPlayerCombatModule — CombatController의 IRealtimeCombatModule 조기 반환(19-03에서 배선)으로
        // 인해 실행 도달 불가능한 dead stub. _activeModule 필드의 정적 타입을 만족시키기 위해서만 존재한다.
        public IEnemy FindTarget(Vector2 origin, CombatContext ctx) => null;
        public IEnumerator Resolve(IEnemy target, CombatContext ctx) { yield break; }
        public IEnumerator Whiff(CombatContext ctx) { yield break; }
    }
    ```

    **2) `BasicCombatModule.cs`** (신규 — D-15/D-16, 오버라이드 없음):
    ```csharp
    /// <summary>
    /// D-15/D-16: 기본전투모듈 — TapSwingCombatModuleBase의 스윙 로직을 그대로 사용, 패링 없음.
    /// 튜토리얼 진입 시점부터 상시 해금(CombatModuleRegistry에서 requiredBossId=null로 등록됨,
    /// 19-02 참고), 사무라이 전투형 모듈이 해금된 이후에도 계속 선택 가능하다(대체되지 않음).
    /// </summary>
    public class BasicCombatModule : TapSwingCombatModuleBase
    {
    }
    ```
  </action>
  <verify>
    <automated>grep -n "class TapSwingCombatModuleBase" Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs; grep -n "class BasicCombatModule" Assets/Scripts/Player/Combat/BasicCombatModule.cs; grep -c "Time.timeScale\|WaitForSeconds(" Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class TapSwingCombatModuleBase" Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs` → `abstract class TapSwingCombatModuleBase : IPlayerCombatModule, IRealtimeCombatModule` 매치
    - `grep -n "class BasicCombatModule" Assets/Scripts/Player/Combat/BasicCombatModule.cs` → `class BasicCombatModule : TapSwingCombatModuleBase` 매치, 본문에 오버라이드 없음(빈 클래스)
    - `grep -c "Time.timeScale\|WaitForSeconds(" Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs` → 0 (SAMURAI-02: 슬로우모션 관련 API를 절대 호출하지 않음, `WaitForSeconds(`는 unscaled가 아닌 스케일드 버전이므로 사용 금지 — Pitfall 3와 동일 취지)
    - Unity Editor 컴파일 에러 0건 수동 확인 (자동 빌드 파이프라인 없음, 이 프로젝트 컨벤션)
    - 수동 플레이테스트(19-06에서 통합 검증): `BasicCombatModule`을 임시로 `CombatController.Awake()`에 `new BasicCombatModule()`로 하드코딩해 Play 후 Attack 탭 시 `Time.timeScale`이 1을 유지한 채 근처 MeleeEnemy가 즉시 처치되는지 확인 — 이 임시 하드코딩은 19-03이 실제 배선을 완료하면 되돌린다(이 Plan은 CombatController.cs를 수정하지 않으므로 이 항목은 19-03/19-06에서 최종 확인)
  </acceptance_criteria>
  <done>TapSwingCombatModuleBase.cs가 D-01~D-03을 구현하고, BasicCombatModule.cs가 D-15/D-16 그대로의 순정 스윙을 제공하며, 둘 다 컴파일 에러 없이 존재한다.</done>
</task>

</tasks>

<verification>
- `Assets/Scripts/Player/Combat/` 폴더에 `IRealtimeCombatModule.cs`/`IParryable.cs`/`AimUtil.cs`/`TapSwingCombatModuleBase.cs`/`BasicCombatModule.cs` 5개 신규 파일이 존재한다.
- `CombatContext.cs`는 3개 필드만 순수 추가되고 기존 필드/OverclockModule.cs는 완전히 무변경이다(git diff로 확인 가능).
- `TapSwingCombatModuleBase`는 `Time.timeScale`/`WaitForSeconds(`(scaled)를 전혀 사용하지 않는다.
</verification>

<success_criteria>
19-CONTEXT.md D-01~D-03, D-15, D-16이 코드로 구현되어 있고(CombatController 배선은 19-03의 몫), 다음 Plan(19-02/19-03/19-04)이 이 계약들을 참조해 병렬/순차로 진행할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-01-SUMMARY.md`
</output>
</content>
