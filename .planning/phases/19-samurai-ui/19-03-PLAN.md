---
phase: 19-samurai-ui
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - Assets/Scripts/Player/Combat/SamuraiParryModule.cs
  - Assets/Scripts/Player/CombatController.cs
autonomous: true
requirements: [SAMURAI-02, SAMURAI-03]

must_haves:
  truths:
    - "사무라이 전투형 모듈이 장착된 상태에서 Attack을 탭하면 슬로우모션 없이 즉시 스윙이 나간다 (SAMURAI-02)"
    - "스윙 범위+방향 안에 IParryable 투사체가 있으면 패링이 발동해 투사체가 소멸/반사되고 보스에게 처치 판정을 주지 않는다 (SAMURAI-03, D-06)"
    - "패링 대상이 없으면 동일한 탭이 일반 적/보스 원샷킬 스윙으로 처리된다 (D-02 폴백)"
    - "CombatController.Awake()가 CombatModuleSelector.SelectedModuleId에 따라 실제 장착 모듈을 구성한다 (UNLOCK-02 배선)"
    - "OverclockModule이 선택된 경우 기존 hold-slowmo→release-resolve 흐름이 한 글자도 다르지 않게 동작한다 (INFRA-01 회귀 없음)"
  artifacts:
    - path: "Assets/Scripts/Player/Combat/SamuraiParryModule.cs"
      provides: "D-04~D-06 패링 판정 — TapSwingCombatModuleBase.ResolveSwing() 오버라이드"
      contains: "class SamuraiParryModule : TapSwingCombatModuleBase"
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "IRealtimeCombatModule 조기 반환 host-hook + CombatModuleSelector 기반 모듈 구성"
      contains: "is IRealtimeCombatModule"
  key_links:
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/Player/Combat/IRealtimeCombatModule.cs"
      via: "Update() 최상단 early-return 분기"
      pattern: "_activeModule is IRealtimeCombatModule"
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/UI/CombatModuleSelector.cs"
      via: "Awake()가 SelectedModuleId를 읽어 모듈 인스턴스 생성"
      pattern: "CombatModuleSelector\\.SelectedModuleId"
    - from: "Assets/Scripts/Player/Combat/SamuraiParryModule.cs"
      to: "Assets/Scripts/Player/Combat/IParryable.cs"
      via: "ResolveSwing() 오버라이드가 IParryable 컴포넌트를 OverlapCircle로 탐색"
      pattern: "GetComponent<IParryable>"
---

<objective>
D-04~D-06(패링 판정, 순수 방어, 투사체 반사)을 구현하는 `SamuraiParryModule`을 완성하고, `CombatController`를 실제로 새 모듈 3종(기본전투모듈/Overclock/사무라이 전투형 모듈)을 host할 수 있도록 배선한다. 19-RESEARCH.md §1이 권장한 host-hook 패턴(additive early-return)을 적용해 **Overclock의 기존 코드 경로는 1바이트도 변경되지 않는다**(INFRA-01 회귀 방지 최우선).

Purpose: UNLOCK-02(선택된 모듈로 실제 게임 시작)를 완성하고, SAMURAI-02/03의 플레이어 측 절반(패링 캐치 로직)을 완결한다. 보스 측(투사체 발사/그로기 게이지)은 19-04가 병렬로 만든다.
Output: `SamuraiParryModule.cs` 신규 + `CombatController.cs`에 host-hook과 모듈 팩토리 추가.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-samurai-ui/19-CONTEXT.md
@.planning/phases/19-samurai-ui/19-RESEARCH.md
@.planning/phases/19-samurai-ui/19-VALIDATION.md

<interfaces>
<!-- Plan 19-01 산출물 — 그대로 사용 -->
Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs:
```csharp
public abstract class TapSwingCombatModuleBase : IPlayerCombatModule, IRealtimeCombatModule
{
    protected virtual void ResolveSwing(CombatContext ctx) { /* D-01~D-03 순정 스윙 */ }
}
```
Assets/Scripts/Player/Combat/IParryable.cs:
```csharp
public interface IParryable
{
    Vector2 Position { get; }
    void OnParried(Vector2 reflectDirection);
}
```
Assets/Scripts/Player/Combat/CombatContext.cs (Plan 19-01이 추가한 필드): `SwingRadius`, `SwingHalfAngleDeg`, `TapLockout`.

<!-- Plan 19-02 산출물 — 그대로 사용 -->
Assets/Scripts/UI/CombatModuleRegistry.cs:
```csharp
public enum CombatModuleId { Basic, Overclock, Samurai }
public static class CombatModuleSelector { public static CombatModuleId SelectedModuleId => ...; }
```

<!-- CombatController.cs 현재 전체 구조 (317 lines, 직접 읽은 현재 소스 기준) -->
Assets/Scripts/Player/CombatController.cs:
- `Awake()` (86-133줄): 컴포넌트 캐싱 후 `_activeModule = new OverclockModule();` (106줄), `_ctx = new CombatContext { ... };` (107-132줄)
- `Update()` (141-200줄): 141-145줄이 `if (_player != null && _player.InputLocked) return; if (_isBusy) return;` — 이 직후가 host-hook 삽입 지점. 146줄부터 Overclock 전용 hold-slowmo→release-resolve 로직 시작(변경 금지).
- `[SerializeField]` 튜너블 필드 (26-45줄) — `searchRadius`/`fanRadius`/`fanHalfAngleDeg`/`linearHalfAngleDeg` 등 기존 필드 옆에 신규 필드 3개(`swingRadius`/`swingHalfAngleDeg`/`tapLockout`) 추가.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SamuraiParryModule — 패링 판정 (D-04~D-06)</name>
  <files>Assets/Scripts/Player/Combat/SamuraiParryModule.cs</files>
  <read_first>
    - Assets/Scripts/Player/Combat/TapSwingCombatModuleBase.cs (Plan 19-01 산출물 — `ResolveSwing()` 시그니처와 base 구현 확인, 오버라이드 대상)
    - Assets/Scripts/Player/Combat/IParryable.cs, AimUtil.cs (Plan 19-01 산출물)
    - .planning/phases/19-samurai-ui/19-CONTEXT.md D-04~D-06
  </read_first>
  <action>
    `SamuraiParryModule.cs`를 신규 작성한다:
    ```csharp
    using UnityEngine;

    /// <summary>
    /// D-04~D-06: 사무라이 전투형 모듈(패링 포함). TapSwingCombatModuleBase의 스윙을 그대로 쓰되,
    /// 스윙 범위 안에 IParryable(투사체)이 있으면 먼저 패링을 시도한다 — 있으면 패링(순수 방어,
    /// 처치 카운트 없음, D-06), 없으면 base.ResolveSwing()으로 폴백해 일반 원샷킬을 그대로 수행한다(D-02).
    /// 투사체 레이어는 "Enemy"(SamuraiBoss.cs가 생성 시 설정, 19-04) — CombatController의 기존
    /// ctx.EnemyFilter와 같은 레이어를 재사용하되 이 클래스 전용 ContactFilter2D/버퍼로 별도 조회한다.
    /// </summary>
    public class SamuraiParryModule : TapSwingCombatModuleBase
    {
        private readonly Collider2D[] _parryBuffer = new Collider2D[4];
        private ContactFilter2D _parryFilter;
        private bool _filterInitialized;

        private void EnsureFilter()
        {
            if (_filterInitialized) return;
            _parryFilter.SetLayerMask(LayerMask.GetMask("Enemy"));
            _parryFilter.useTriggers = true;
            _filterInitialized = true;
        }

        protected override void ResolveSwing(CombatContext ctx)
        {
            EnsureFilter();
            Vector2 origin = ctx.Rb.position;
            Vector2 dir = AimUtil.GetMouseWorldDirection(origin, ctx.MainCamera);
            float cosHalf = Mathf.Cos(ctx.SwingHalfAngleDeg * Mathf.Deg2Rad);

            int count = Physics2D.OverlapCircle(origin, ctx.SwingRadius, _parryFilter, _parryBuffer);
            for (int i = 0; i < count; i++)
            {
                var parryable = _parryBuffer[i].GetComponent<IParryable>();
                if (parryable == null) continue;

                Vector2 toTarget = parryable.Position - origin;
                float dist = toTarget.magnitude;
                if (dist > ctx.SwingRadius) continue;
                if (Vector2.Dot(dir, toTarget.normalized) < cosHalf) continue;

                // D-04: 타이밍(탭 시점) + 방향(스윙 부채꼴) 동시 충족 -> 패링 성공
                parryable.OnParried(dir); // D-06: 순수 방어 — 보스 OnDashHit 호출 없음
                ctx.SpriteRenderer.flipX = dir.x < 0f;
                AudioManager.PlaySfx(Sfx.Slash); // 전용 SFX 없음(Claude's Discretion) — 기존 자산 재사용
                return; // 패링이 이 탭을 전부 소비 — 동시에 다른 적을 베지 않는다
            }

            base.ResolveSwing(ctx); // 패링 대상 없음 -> 일반 스윙(D-02, 원샷킬)으로 폴백
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "class SamuraiParryModule" Assets/Scripts/Player/Combat/SamuraiParryModule.cs; grep -n "GetComponent<IParryable>" Assets/Scripts/Player/Combat/SamuraiParryModule.cs; grep -n "base.ResolveSwing" Assets/Scripts/Player/Combat/SamuraiParryModule.cs; grep -c "OnDashHit" Assets/Scripts/Player/Combat/SamuraiParryModule.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class SamuraiParryModule" Assets/Scripts/Player/Combat/SamuraiParryModule.cs` → `class SamuraiParryModule : TapSwingCombatModuleBase` 매치
    - `GetComponent<IParryable>` 호출이 존재해 패링 대상을 탐색한다
    - 패링 성공 경로(`parryable.OnParried(dir)` 호출 후 `return`)가 `base.ResolveSwing(ctx)` 호출 이전에 위치해 두 경로가 상호 배타적임을 코드 순서로 확인 가능
    - `grep -c "OnDashHit" Assets/Scripts/Player/Combat/SamuraiParryModule.cs` → 0 (D-06: 패링 자체는 보스에게 `OnDashHit()`을 직접 호출하지 않음 — 원샷킬 폴백 경로는 base 클래스에 있으므로 이 파일 안에서는 0회가 정상)
  </acceptance_criteria>
  <done>SamuraiParryModule.cs가 D-04~D-06 패링 우선 판정 + D-02 폴백을 구현하고 컴파일 에러 없이 존재한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CombatController — host-hook + 모듈 선택 배선</name>
  <files>Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (전체 — Awake()/Update() 정확한 현재 줄 번호 확인, 특히 86-133줄 Awake, 141-145줄 Update 진입부, 106줄 `_activeModule = new OverclockModule();`)
    - Assets/Scripts/Player/Combat/SamuraiParryModule.cs, BasicCombatModule.cs (Task 1, Plan 19-01 산출물 — 생성자 확인)
    - Assets/Scripts/UI/CombatModuleRegistry.cs (Plan 19-02 산출물 — `CombatModuleId`/`CombatModuleSelector.SelectedModuleId`)
  </read_first>
  <action>
    **1) 신규 `[SerializeField]` 튜너블 3개 추가** — 기존 26-45줄 필드 블록 끝(`_cameraShakeAmplitude` 다음 줄)에:
    ```csharp
        [SerializeField] private float swingRadius = 3f;         // D-01: 기본/사무라이 전투형 모듈 스윙 반경 (Overclock의 searchRadius/fanRadius와 독립)
        [SerializeField] private float swingHalfAngleDeg = 50f;  // D-01: 스윙 부채꼴 절반각
        [SerializeField] private float tapLockout = 0.12f;       // D-03: 탭 공격 사이 짧은 고정 락아웃
    ```

    **2) `Awake()` 수정** — 기존 106줄 `_activeModule = new OverclockModule();`를 `_activeModule = BuildModule(CombatModuleSelector.SelectedModuleId);`로 교체하고, `_ctx = new CombatContext { ... }` 이니셜라이저(107-132줄)의 `ObstacleMask = _obstacleMask,` 다음 줄에 3개 필드를 추가:
    ```csharp
            SwingRadius          = swingRadius,
            SwingHalfAngleDeg    = swingHalfAngleDeg,
            TapLockout           = tapLockout,
    ```
    `Awake()` 메서드 이후(private 헬퍼 영역)에 팩토리 메서드를 추가:
    ```csharp
        private IPlayerCombatModule BuildModule(CombatModuleId id)
        {
            switch (id)
            {
                case CombatModuleId.Overclock: return new OverclockModule();
                case CombatModuleId.Samurai:   return new SamuraiParryModule();
                case CombatModuleId.Basic:
                default:                       return new BasicCombatModule();
            }
        }
    ```

    **3) `Update()` 수정** — 기존 143-145줄:
    ```csharp
        if (_player != null && _player.InputLocked) return;
        if (_isBusy) return;
    ```
    바로 다음 줄(147줄 `var input = InputManager.Instance;` 이전)에 host-hook을 삽입한다:
    ```csharp
        // SAMURAI-02: 실시간 모듈은 Overclock의 hold-slowmo→release-resolve 상태머신을 완전히 우회한다.
        // OverclockModule은 IRealtimeCombatModule을 구현하지 않으므로 이 분기는 항상 false — Overclock
        // 경로는 아래 기존 로직으로 그대로 흘러간다(19-RESEARCH.md §1, zero behavior change).
        if (_activeModule is IRealtimeCombatModule realtimeModule)
        {
            realtimeModule.Tick(_ctx);
            return;
        }
    ```
    **`Update()`의 나머지 146줄 이후(공격 쿨다운/슬로우모션/릴리즈 처리)는 단 한 글자도 수정하지 않는다** — Overclock 경로는 100% byte-identical로 유지되어야 한다(INFRA-01).
  </action>
  <verify>
    <automated>grep -n "BuildModule(CombatModuleSelector.SelectedModuleId)" Assets/Scripts/Player/CombatController.cs; grep -n "is IRealtimeCombatModule" Assets/Scripts/Player/CombatController.cs; grep -n "private IPlayerCombatModule BuildModule" Assets/Scripts/Player/CombatController.cs; grep -c "new OverclockModule()" Assets/Scripts/Player/CombatController.cs; grep -n "SwingRadius          = swingRadius" Assets/Scripts/Player/CombatController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BuildModule(CombatModuleSelector.SelectedModuleId)" Assets/Scripts/Player/CombatController.cs` → `Awake()` 안에서 1개 매치
    - `grep -n "is IRealtimeCombatModule" Assets/Scripts/Player/CombatController.cs` → `Update()` 최상단(입력락/`_isBusy` 체크 직후, `var input = InputManager.Instance;` 이전)에서 1개 매치
    - `grep -c "new OverclockModule()"` → 1 (여전히 `BuildModule()` 스위치 안에서만 생성됨, 하드코딩 직접 대입은 사라짐)
    - `BuildModule()` 스위치문이 `CombatModuleId.Overclock`/`CombatModuleId.Samurai`/기본값(Basic) 3갈래를 모두 처리한다
    - `Update()`의 host-hook `return` 이후 기존 146번째 줄부터 끝까지(슬로우모션/게이지/릴리즈 로직)는 git diff 상 무변경이다 — `git diff Assets/Scripts/Player/CombatController.cs`를 확인해 host-hook 삽입 블록과 Awake()의 3곳(필드 3개/BuildModule 호출/ctx 필드 3개/BuildModule 메서드 정의) 외 다른 라인이 바뀌지 않았는지 수동 확인
    - Unity Editor 컴파일 에러 0건 수동 확인
    - 수동 플레이테스트(19-06에서 최종 통합 검증): Overclock 선택 상태로 Play 시 기존 hold-slowmo→release-resolve 흐름이 100% 동일하게 동작(INFRA-01 무회귀), 기본전투모듈/사무라이 전투형 모듈 선택 상태로 Play 시 Time.timeScale이 1을 유지한 채 탭 즉시 스윙이 나가는지 확인
  </acceptance_criteria>
  <done>CombatController가 host-hook과 모듈 팩토리를 통해 3종 모듈을 구성할 수 있고, Overclock 코드 경로는 host-hook 삽입 외에는 완전히 무변경이다.</done>
</task>

</tasks>

<verification>
- `SamuraiParryModule`이 패링 우선 판정 + `TapSwingCombatModuleBase.ResolveSwing()` 폴백 구조를 갖는다.
- `CombatController.Update()`는 host-hook 삽입 5줄 외 기존 코드가 100% 동일하다(INFRA-01 회귀 없음).
- `CombatController.Awake()`가 `CombatModuleSelector.SelectedModuleId`로 실제 장착 모듈을 결정한다(UNLOCK-02 배선 완료).
</verification>

<success_criteria>
SAMURAI-02/03의 플레이어 측 로직이 완성되고, UNLOCK-02(선택된 모듈로 게임 시작)가 실제로 동작한다. 19-06에서 통합 플레이테스트로 최종 확인.
</success_criteria>

<output>
After completion, create `.planning/phases/19-samurai-ui/19-03-SUMMARY.md`
</output>
</content>
