---
phase: 01-foundation-movement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ProjectSettings/TagManager.asset
  - Assets/Scenes/SampleScene.unity
  - Assets/Scripts/Camera/CameraFollow.cs
autonomous: true
requirements:
  - MOVE-01
  - MOVE-02

must_haves:
  truths:
    - "SampleScene contains one wide platform and two invisible fall-trigger zones, one on each side"
    - "Physics layer matrix has Platform, PlayerHurtbox, and PlayerInvincible layers defined"
    - "Camera follows the player position every LateUpdate with no lead-ahead"
  artifacts:
    - path: "ProjectSettings/TagManager.asset"
      provides: "Layer definitions for Platform, PlayerHurtbox, PlayerInvincible"
      contains: "Platform"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "Test scene with platform + fall triggers"
      contains: "FallZone"
    - path: "Assets/Scripts/Camera/CameraFollow.cs"
      provides: "LateUpdate direct camera follow"
      exports: ["CameraFollow"]
  key_links:
    - from: "Assets/Scripts/Camera/CameraFollow.cs"
      to: "Player GameObject"
      via: "public Transform target field assigned in Inspector"
      pattern: "target\\.position"
---

<objective>
Set up the Unity project foundation that all player code depends on: physics layers, test scene layout, and camera follow.

Purpose: Nothing can be tested without a scene. The layer matrix (Platform, PlayerHurtbox, PlayerInvincible) must exist before PlayerController or FallDetector use them. Camera follow must be in LateUpdate per D-11/D-13 decisions.

Output: SampleScene with one wide platform, two fall trigger zones, and a CameraFollow script attached to Main Camera.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-movement/01-CONTEXT.md

<interfaces>
<!-- Input System action map already defined in project -->
<!-- Assets/InputSystem_Actions.inputactions defines: -->
<!-- Player action map: Move (Vector2), Jump (Button), Attack (Button) -->
<!-- Phase 1 uses only Move and Jump -->

<!-- Required layer names (must match exactly in code) -->
<!-- Layer 6: Platform -->
<!-- Layer 7: PlayerHurtbox -->
<!-- Layer 8: PlayerInvincible -->
<!-- These will be referenced by name in subsequent plans -->

<!-- Physics2D layer collision matrix requirements: -->
<!-- Platform collides with: PlayerHurtbox, PlayerInvincible -->
<!-- PlayerHurtbox does NOT collide with PlayerInvincible (same object) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure physics layers and build SampleScene test layout</name>
  <files>ProjectSettings/TagManager.asset, Assets/Scenes/SampleScene.unity</files>
  <read_first>
    - ProjectSettings/TagManager.asset (current layer definitions — must not overwrite existing layers 0-5)
    - Assets/Scenes/SampleScene.unity (existing scene content to clear safely)
  </read_first>
  <action>
Step 1 — Add physics layers to ProjectSettings/TagManager.asset.
Open TagManager.asset and add three named layers in slots 6, 7, 8:
  - Layer 6: "Platform"
  - Layer 7: "PlayerHurtbox"
  - Layer 8: "PlayerInvincible"

Layers 0-5 are Unity built-ins (Default, TransparentFX, Ignore Raycast, Water, UI, PostProcessing) — do not modify them. The YAML key is `layers` and each entry is a quoted string. Slots 3 and 4 may show as empty strings; preserve them.

Step 2 — Configure Physics2D layer collision matrix in ProjectSettings/Physics2DSettings.asset.
Ensure Platform (6) collides with PlayerHurtbox (7) and PlayerInvincible (8).
Ensure PlayerHurtbox (7) does NOT collide with PlayerInvincible (8) — same player object, no self-collision.

Step 3 — Build SampleScene layout via Unity Editor (Scene view).
Clear any default objects from the template. Create the following hierarchy:

  Environment/
    Platform           — Main floor
      Sprite Renderer: 1920x64 px white square sprite, Color #808080 (gray)
      Box Collider 2D: size (24, 0.8), offset (0, 0), Layer = Platform
      Transform: position (0, -3, 0)
    FallZone_Left      — Left fall trigger
      Box Collider 2D: size (4, 20), Is Trigger = true, Layer = Default
      Transform: position (-14, -10, 0)
    FallZone_Right     — Right fall trigger
      Box Collider 2D: size (4, 20), Is Trigger = true, Layer = Default
      Transform: position (14, -10, 0)
  Player             — Placeholder; full implementation in Plan 02
    Sprite Renderer: 64x64 px white square sprite, Color #FFFFFF
    Transform: position (0, -2, 0)
    Tag: "Player"
  Main Camera
    Transform: position (0, 0, -10)
    Camera: orthographic, size 5.4 (fits 1920x1080 at ~178 px/unit)

The Platform should be wide enough that the player can run left/right freely. 24 units wide at the default Unity 1 unit = 1 meter scale gives ~4 seconds of running before hitting a fall zone.

Do NOT add any scripts to GameObjects in this task — scripts are added in Task 2 and Plan 02.
  </action>
  <verify>
    <automated>grep -c "Platform" "ProjectSettings/TagManager.asset"</automated>
  </verify>
  <acceptance_criteria>
    - ProjectSettings/TagManager.asset contains the string "Platform" in a layers entry
    - ProjectSettings/TagManager.asset contains the string "PlayerHurtbox" in a layers entry
    - ProjectSettings/TagManager.asset contains the string "PlayerInvincible" in a layers entry
    - Assets/Scenes/SampleScene.unity contains a GameObject named "Platform"
    - Assets/Scenes/SampleScene.unity contains a GameObject named "FallZone_Left"
    - Assets/Scenes/SampleScene.unity contains a GameObject named "FallZone_Right"
    - Assets/Scenes/SampleScene.unity contains a GameObject with Tag "Player"
  </acceptance_criteria>
  <done>Layer matrix is configured. SampleScene has a wide gray platform at y=-3 with two invisible trigger colliders flanking it, and a placeholder Player object at center.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CameraFollow script (LateUpdate, no lead-ahead)</name>
  <files>Assets/Scripts/Camera/CameraFollow.cs, Assets/Scenes/SampleScene.unity</files>
  <read_first>
    - Assets/Scenes/SampleScene.unity (to confirm Main Camera path and Player object name before wiring)
    - .planning/phases/01-foundation-movement/01-CONTEXT.md (D-11, D-12, D-13 camera decisions)
  </read_first>
  <action>
Create directory Assets/Scripts/Camera/ if it does not exist.

Create Assets/Scripts/Camera/CameraFollow.cs:

```csharp
using UnityEngine;

/// <summary>
/// Direct LateUpdate camera follow — no Cinemachine, no lead-ahead (per D-11, D-12, D-13).
/// Attach to Main Camera. Assign target = Player Transform in Inspector.
/// </summary>
public class CameraFollow : MonoBehaviour
{
    [SerializeField] private Transform target;
    [SerializeField] private Vector3 offset = new Vector3(0f, 1f, -10f);

    private void LateUpdate()
    {
        if (target == null) return;
        transform.position = target.position + offset;
    }
}
```

Key implementation notes:
- LateUpdate runs after all Update/FixedUpdate calls in the frame — eliminates camera jitter from physics (per Pitfall 6 in research)
- offset.z must remain -10 (Camera depth in 2D Unity scenes)
- offset.y = 1 gives a slight upward bias so the player is not at the very bottom of the screen
- No smoothing/lerp — follows precisely as per D-12 ("정밀 추적")
- No Cinemachine dependency per D-13

After creating the script:
1. In Unity Editor, select Main Camera in SampleScene
2. Add Component: CameraFollow
3. Set target field = Player Transform (drag Player object)
4. Confirm offset values: (0, 1, -10)
5. Save scene
  </action>
  <verify>
    <automated>grep -c "LateUpdate" "Assets/Scripts/Camera/CameraFollow.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Camera/CameraFollow.cs exists
    - CameraFollow.cs contains "LateUpdate" (correct update loop)
    - CameraFollow.cs contains "target.position + offset" (direct follow, no lerp)
    - CameraFollow.cs does NOT contain "Cinemachine" (per D-13)
    - CameraFollow.cs does NOT contain "Lerp" or "SmoothDamp" (per D-12, no smoothing)
    - Assets/Scenes/SampleScene.unity references "CameraFollow" (script attached to Main Camera)
  </acceptance_criteria>
  <done>CameraFollow.cs is attached to Main Camera in SampleScene. Camera tracks Player position in LateUpdate with a fixed (0, 1, -10) offset. No smoothing. No Cinemachine.</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. Open Unity Editor — SampleScene loads without errors
2. Confirm layer names in Edit > Project Settings > Tags and Layers: slot 6 = Platform, 7 = PlayerHurtbox, 8 = PlayerInvincible
3. Confirm Physics 2D layer matrix: Platform collides with PlayerHurtbox and PlayerInvincible
4. Press Play — camera follows the (stationary) Player placeholder
5. No console errors on scene load
</verification>

<success_criteria>
- Layer matrix configured with Platform (6), PlayerHurtbox (7), PlayerInvincible (8)
- SampleScene has one wide platform + two fall trigger zones + Player placeholder
- CameraFollow attached to Main Camera, using LateUpdate, no Cinemachine
- Zero console errors on Play
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-movement/01-01-SUMMARY.md`
</output>
