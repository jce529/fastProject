---
phase: 16-boss-room-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/TestWorldGenerator.cs (deleted)
  - Assets/Scripts/World/TestWorldGenerator.cs.meta (deleted)
  - Assets/Scripts/World/FloorSpawner.cs (deleted)
  - Assets/Scripts/World/FloorSpawner.cs.meta (deleted)
  - Assets/Scripts/World/RoomExit.cs (deleted)
  - Assets/Scripts/World/RoomExit.cs.meta (deleted)
  - Assets/Scripts/Enemy/MeleeEnemy.cs
  - Assets/Scripts/Enemy/RangedEnemy.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "TestWorldGenerator.cs, FloorSpawner.cs, RoomExit.cs no longer exist anywhere in the repository (100% dead code confirmed via GUID cross-check in 16-CONTEXT.md)"
    - "MeleeEnemy.cs and RangedEnemy.cs no longer declare the unused LayerPlayerHurtbox/LayerPlayerInvincible constants (superseded by Physics2D collision matrix)"
    - "No remaining code file references FloorSpawner, RoomExit, or TestWorldGenerator by type name (WorldGenerator.cs only ever referenced them in comments, never in code)"
  artifacts:
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "MeleeEnemy without dead layer constants"
      contains: "public class MeleeEnemy"
    - path: "Assets/Scripts/Enemy/RangedEnemy.cs"
      provides: "RangedEnemy without dead layer constants"
      contains: "public class RangedEnemy"
  key_links: []
---

<objective>
Phase 16 리팩토링 배치의 첫 단계 — 100% 죽은 것으로 확인된 3개 파일(`TestWorldGenerator.cs`, `FloorSpawner.cs`, `RoomExit.cs`)을 삭제하고, `MeleeEnemy.cs`/`RangedEnemy.cs`에 남아있는 죽은 레이어 상수(`LayerPlayerHurtbox`/`LayerPlayerInvincible`)를 제거한다.

Purpose: 16-CONTEXT.md D-01~D-04 — WorldGenerator(Phase 9)/ExitPortal(Phase 10)로 이미 완전히 대체되어 죽은 코드를 제거하고, Physics2D 충돌 매트릭스로 이미 대체된 죽은 상수를 정리한다. 이후 Wave 2(16-03)의 EnemyBase 추출 작업이 깨끗한 MeleeEnemy.cs/RangedEnemy.cs를 기반으로 진행되도록 먼저 처리한다.
Output: 3개 죽은 스크립트 파일 삭제 완료, MeleeEnemy.cs/RangedEnemy.cs에서 죽은 상수 2줄씩 제거.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-boss-room-lifecycle/16-CONTEXT.md
@Assets/Scripts/World/TestWorldGenerator.cs
@Assets/Scripts/World/FloorSpawner.cs
@Assets/Scripts/World/RoomExit.cs
@Assets/Scripts/Enemy/MeleeEnemy.cs
@Assets/Scripts/Enemy/RangedEnemy.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 죽은 파일 3종 삭제 (D-01, D-02, D-03)</name>
  <files>Assets/Scripts/World/TestWorldGenerator.cs, Assets/Scripts/World/TestWorldGenerator.cs.meta, Assets/Scripts/World/FloorSpawner.cs, Assets/Scripts/World/FloorSpawner.cs.meta, Assets/Scripts/World/RoomExit.cs, Assets/Scripts/World/RoomExit.cs.meta</files>
  <read_first>
    - .planning/phases/16-boss-room-lifecycle/16-CONTEXT.md `<decisions>` D-01/D-02/D-03 및 `<code_context>` "죽은 코드 조사 방법론"
  </read_first>
  <action>
    아래 6개 파일을 삭제한다 (스크립트 + 대응하는 .meta 파일 — Unity는 .meta 파일이 남아있으면 GUID 충돌 경고를 낼 수 있으므로 반드시 함께 삭제):
    - `Assets/Scripts/World/TestWorldGenerator.cs` (D-01: 씬/프리팹 어디에도 GUID 참조 없음, 100% 죽은 코드)
    - `Assets/Scripts/World/TestWorldGenerator.cs.meta`
    - `Assets/Scripts/World/FloorSpawner.cs` (D-02: WorldGenerator(Phase 9)로 대체됨. SampleScene.unity의 남은 참조는 비활성 GameObject 하나뿐이며, 그 GameObject 자체의 씬 제거는 사용자가 Unity 에디터에서 직접 처리하기로 확정됨 — 이 태스크는 .cs/.cs.meta 파일 삭제만 수행한다)
    - `Assets/Scripts/World/FloorSpawner.cs.meta`
    - `Assets/Scripts/World/RoomExit.cs` (D-03: ExitPortal(Phase 10)로 대체됨. `FloorSpawner.Instance?.AdvanceFloor()`가 FloorSpawner 항상 비활성이라 영구 no-op이었음. 구형 Room_*.prefab 14종+Room_Debug.prefab에 부착된 컴포넌트 참조는 사용자가 그 프리팹들을 통째로 Unity 에디터에서 삭제하기로 확정됨 — 이 태스크는 .cs/.cs.meta 파일 삭제만 수행한다)
    - `Assets/Scripts/World/RoomExit.cs.meta`

    삭제 방법: `git rm` 사용 (Bash 도구로 `git rm Assets/Scripts/World/TestWorldGenerator.cs Assets/Scripts/World/TestWorldGenerator.cs.meta Assets/Scripts/World/FloorSpawner.cs Assets/Scripts/World/FloorSpawner.cs.meta Assets/Scripts/World/RoomExit.cs Assets/Scripts/World/RoomExit.cs.meta`).

    다른 코드 파일은 이 3개 타입을 컴파일 타임에 참조하지 않는다 — `WorldGenerator.cs`의 주석(§7-8)이 "FloorSpawner/TestWorldGenerator 대신 배치한다"고 언급하지만 이는 문서화 주석일 뿐 실제 코드 참조가 아니므로 삭제해도 컴파일에 영향 없다. `CameraFollow.cs`(§7,41), `PlayerController.cs`(§202), `ScoreManager.cs`(§50,57)의 XML 문서 주석에도 "FloorSpawner"/"RoomExit" 언급이 있으나 마찬가지로 주석뿐이다 — 이 태스크에서는 그 주석들을 수정하지 않는다(정밀한 변경 원칙 — 이번 삭제로 인해 실제로 깨지는 것은 없으므로 주석까지 건드릴 필요 없음).
  </action>
  <verify>
    <automated>test ! -f "Assets/Scripts/World/TestWorldGenerator.cs" && test ! -f "Assets/Scripts/World/FloorSpawner.cs" && test ! -f "Assets/Scripts/World/RoomExit.cs" && test ! -f "Assets/Scripts/World/TestWorldGenerator.cs.meta" && test ! -f "Assets/Scripts/World/FloorSpawner.cs.meta" && test ! -f "Assets/Scripts/World/RoomExit.cs.meta" && echo "ALL_DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/World/TestWorldGenerator.cs`, `Assets/Scripts/World/FloorSpawner.cs`, `Assets/Scripts/World/RoomExit.cs`와 각각의 `.meta` 파일이 파일시스템에 존재하지 않는다
    - `git status`에 이 6개 파일이 `deleted:` 상태로 표시된다
    - `grep -rl "class TestWorldGenerator\|class FloorSpawner\|class RoomExit" Assets/Scripts` 실행 결과가 비어 있다 (0 matches)
  </acceptance_criteria>
  <done>3개 죽은 스크립트 파일(+ .meta)이 저장소에서 완전히 제거됨.</done>
</task>

<task type="auto">
  <name>Task 2: MeleeEnemy.cs/RangedEnemy.cs 죽은 레이어 상수 제거 (D-04)</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs, Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/MeleeEnemy.cs (§29-31 근처 — 상수 선언부)
    - Assets/Scripts/Enemy/RangedEnemy.cs (§26-29 근처 — 상수 선언부)
    - .planning/phases/16-boss-room-lifecycle/16-CONTEXT.md `<decisions>` D-04
  </read_first>
  <action>
    두 파일에서 아래 2줄을 각각 삭제한다 — 어디서도 참조되지 않는 죽은 상수(D-16 주석에 이미 "Physics2D 충돌 매트릭스로 처리되어 코드 체크 불필요"라고 명시되어 있음):

    **`Assets/Scripts/Enemy/MeleeEnemy.cs`** — 아래 블록(현재 §29-31, `// -- Layer constants` 주석 포함 3줄) 전체 삭제:
    ```csharp
    // -- Layer constants (hardcoded — matches TagManager.asset, established pattern) --
    private const int LayerPlayerHurtbox    = 7;
    private const int LayerPlayerInvincible = 8;
    ```

    **`Assets/Scripts/Enemy/RangedEnemy.cs`** — 아래 블록(현재 §26-28, `// -- Layer constants` 주석 포함) 중 `LayerPlayerHurtbox`/`LayerPlayerInvincible` 두 줄만 삭제한다 (같은 블록의 `TelegraphDuration` 상수는 실제로 사용 중이므로 유지):
    ```csharp
    // -- Layer constants ------------------------------------------------------------
    private const int LayerPlayerHurtbox    = 7;
    private const int LayerPlayerInvincible = 8;
    private const float TelegraphDuration   = 0.8f;
    ```
    위 블록을 아래로 교체 (주석은 유지, 두 상수만 제거):
    ```csharp
    private const float TelegraphDuration   = 0.8f;
    ```

    다른 코드는 건드리지 않는다 — 두 상수는 현재 파일 어디에서도 읽히지 않으므로 삭제해도 동작 변화 없음.
  </action>
  <verify>
    <automated>! grep -n "LayerPlayerHurtbox\|LayerPlayerInvincible" Assets/Scripts/Enemy/MeleeEnemy.cs Assets/Scripts/Enemy/RangedEnemy.cs && grep -n "TelegraphDuration" Assets/Scripts/Enemy/RangedEnemy.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LayerPlayerHurtbox\|LayerPlayerInvincible" Assets/Scripts/Enemy/MeleeEnemy.cs Assets/Scripts/Enemy/RangedEnemy.cs` 결과가 0 matches
    - `RangedEnemy.cs`의 `TelegraphDuration` 상수(`private const float TelegraphDuration = 0.8f;`)는 그대로 남아있다
    - 두 파일의 다른 필드/메서드는 변경되지 않는다
  </acceptance_criteria>
  <done>MeleeEnemy.cs/RangedEnemy.cs에서 미사용 레이어 상수가 제거되고, 실제 사용 중인 TelegraphDuration은 그대로 유지됨.</done>
</task>

</tasks>

<verification>
- 6개 파일(3개 .cs + 3개 .meta) 삭제 완료, `git status`에 deleted로 표시
- `LayerPlayerHurtbox`/`LayerPlayerInvincible` 문자열이 Assets/Scripts 전체에서 검색되지 않음
- MeleeEnemy.cs/RangedEnemy.cs 나머지 코드는 변경 없음 (surgical change)
</verification>

<success_criteria>
- D-01, D-02, D-03, D-04가 코드 레벨에서 완료됨
- Wave 2(16-03)가 깨끗한 MeleeEnemy.cs/RangedEnemy.cs를 기반으로 EnemyBase 추출을 진행할 수 있는 상태가 됨
</success_criteria>

<output>
After completion, create `.planning/phases/16-boss-room-lifecycle/16-01-SUMMARY.md`
</output>
