---
phase: 16-boss-room-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "근접/원거리 적을 대시로 처치하면 여전히 즉시 처치되고 ScoreManager.Score가 정확히 1회 증가한다 (D-08 회귀 없음)"
    - "리스폰된 적(999.2 파이프라인)을 처치하면 RespawnKillScore(30점, 일반 KillScore 100점의 약 30%)가 적립된다 — 각 적이 스스로 판정하는 경로로도 999.2 동작이 그대로 유지된다"
    - "Linear/Fan 두 공격 타입 모두 정상적으로 타겟을 탐지하고 대시가 발동한다 (D-06 회귀 없음)"
    - "콘솔에 [Combat] ExecuteDash/DashOrWhiff 계열의 디버그 로그 스팸이 더 이상 출력되지 않는다 (D-07)"
    - "게임이 콘솔 에러 없이 정상적으로 컴파일/실행된다 (D-01~D-05 삭제/리팩토링으로 인한 컴파일 에러 없음)"
  artifacts: []
  key_links: []
---

<objective>
16-01/16-02/16-03에서 완료된 리팩토링 배치(D-01~D-08) 전체를 실제 플레이로 검증한다 — 죽은 파일 삭제, CombatController 정리, EnemyBase 추출과 점수 시점 재설계가 기존 전투 루프(근접/원거리 적 처치, Linear/Fan 공격, 리스폰 킬)를 회귀 없이 그대로 유지하는지 확인한다.

Purpose: D-05(EnemyBase 추출)와 D-08(점수 시점 재설계)은 `OnDashHit()`이라는 게임의 핵심 처치 경로를 두 개의 병렬 Plan(16-02: CombatController 측 제거, 16-03: Enemy 측 이동)에 걸쳐 재작성했다 — 코드 리뷰만으로는 잡기 어려운 통합 회귀(예: 점수가 아예 적립되지 않거나 중복 적립되는 문제)를 플레이로 확인하는 것이 이 Plan의 유일한 목적이다.
Output: Phase 16 리팩토링 배치(D-01~D-08) 전체에 대한 플레이테스트 검증 기록.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-boss-room-lifecycle/16-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 플레이테스트 — 리팩토링 배치(D-01~D-08) 전체 회귀 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/16-boss-room-lifecycle/16-CONTEXT.md `<decisions>` D-01~D-08 전체
  </read_first>
  <action>
    Unity 에디터에서 SampleScene을 Play하여 아래 체크리스트를 확인한다. 이 리팩토링 배치는 씬/프리팹을 건드리지 않는 순수 코드 변경이지만, `OnDashHit()`(적 처치의 핵심 경로)이 EnemyBase 추출+점수 재배선으로 크게 재작성되었으므로 자동화 불가능한 실제 게임플레이 확인이 필요하다.
  </action>
  <what-built>
    D-01/D-02/D-03: 죽은 파일 3종(TestWorldGenerator.cs, FloorSpawner.cs, RoomExit.cs) 삭제.
    D-04: MeleeEnemy.cs/RangedEnemy.cs의 죽은 레이어 상수 제거.
    D-05: EnemyBase.cs 신설 — MeleeEnemy/RangedEnemy 공통 로직(OnDashHit 공통부, ClearHighlight, IsPlayerInRange, SetSpawnGate, OnEnable/OnDisable) 추출.
    D-06: CombatController.FindNearestEnemyInRange()의 Linear/Fan 마우스 방향 계산 헬퍼 통합.
    D-07: CombatController.DashOrWhiff()/ExecuteDash()의 디버그 로그 12줄 제거.
    D-08: 점수 적립 시점을 "공격 시(CombatController.ExecuteDash())"에서 "사망 시(각 적의 OnDashHit(), EnemyBase 공통부)"로 이동.
  </what-built>
  <how-to-verify>
    에디터 Play 후 체크리스트:

    **[기본 컴파일/실행 확인]**
    1. Unity 콘솔에 컴파일 에러가 0건인지 확인 (D-01~D-05 삭제/리팩토링으로 인한 참조 깨짐 없음)

    **[D-08 — 점수 시점 재설계 회귀 확인]**
    2. 근접 적(MeleeEnemy)을 대시로 처치 → HUD 점수가 정확히 100점(KillScore) 증가하는지 확인 (한 번만 증가, 중복/누락 없음)
    3. 원거리 적(RangedEnemy)을 대시로 처치 → 동일하게 100점 증가 확인
    4. 리스폰된 적(999.2 파이프라인 — Complex_Room을 클리어 후 벗어났다가 재진입해 리스폰된 개체)을 처치 → 30점(RespawnKillScore)만 증가하는지 확인 (일반 킬과 구분됨)
    5. 처치 순간 히트프리즈/카메라 쉐이크/사망 이펙트(파티클+마스크 페이드)가 기존과 동일하게 재생되는지 확인 (OnDashHit() 순서 보존 회귀 없음)

    **[D-06 — 마우스 방향 헬퍼 통합 회귀 확인]**
    6. Linear 공격 타입 선택 후 마우스를 다양한 방향으로 이동시키며 대시 타겟팅이 정상 동작하는지 확인
    7. Fan 공격 타입 선택 후 동일하게 확인

    **[D-07 — 콘솔 노이즈 제거 확인]**
    8. 대시/헛베기를 여러 번 실행하는 동안 콘솔에 `[Combat] ExecuteDash:`/`[Combat] DashOrWhiff:` 계열의 Debug.Log 스팸이 더 이상 출력되지 않는지 확인 (`[Combat] Attack released...`와 `[Combat] Executing Whiff...`는 범위 밖이라 그대로 출력됨 — 정상)

    **[전반 회귀]**
    9. 헛베기(타겟 없이 공격) 시 기존과 동일하게 페널티 락아웃이 적용되는지 확인
    10. 근접 적의 텔레그래프 이동/공격, 원거리 적의 조준선/발사/카이팅이 리팩토링 전과 동일하게 동작하는지 확인
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — OnDashHit()/점수 적립/콘솔 로그는 실시간 플레이 관찰과 HUD 점수 확인이 필요하며 프로젝트에 자동화 테스트 프레임워크가 구성되어 있지 않음(선행 999.4-01-PLAN.md와 동일 컨벤션). 통과 기준: 체크리스트 1~10 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~10번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(어느 항목, 어떤 조건)이 함께 기록된다
  </acceptance_criteria>
  <done>Phase 16 리팩토링 배치(D-01~D-08)가 실제 플레이로 검증되어 회귀 없음이 확인됨.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "4번: 리스폰 킬도 100점이 적립됨")</resume-signal>
</task>

</tasks>

<verification>
- Phase 16의 Success Criteria #6("기존 코드 안의 중복 로직/이상 패턴이 식별되어 정리된다")이 D-01~D-08 전체 완료로 충족됨
- 근접/원거리 적 처치, Linear/Fan 공격, 리스폰 킬 점수 구분 — 전부 리팩토링 전과 동일하게 동작
</verification>

<success_criteria>
- D-01~D-08 전체가 실제 플레이로 검증됨
- 이번 세션에서는 보스 룸 기능 자체(BOSS-01/02/07/09/10, SFX-05)를 다루지 않는다 — 별도 discuss-phase 세션 이후 후속 Phase 16 계획에서 진행
</success_criteria>

<output>
After completion, create `.planning/phases/16-boss-room-lifecycle/16-04-SUMMARY.md`
</output>
