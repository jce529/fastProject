---
phase: 16-boss-room-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/CombatController.cs
autonomous: true
requirements: [D-06, D-07, D-08]

must_haves:
  truths:
    - "FindNearestEnemyInRange()의 Linear/Fan 분기가 더 이상 마우스→월드 방향 계산 코드를 각자 복붙하지 않고 공유 헬퍼를 호출한다 (D-06)"
    - "DashOrWhiff()/ExecuteDash()에 남아있던 콘솔 노이즈용 Debug.Log 호출이 모두 제거되었다 (D-07)"
    - "CombatController.ExecuteDash()는 더 이상 ScoreManager.AddKillScore()를 호출하지 않고, RespawnedEnemyMarker도 참조하지 않는다 — 점수 적립은 각 적의 OnDashHit()(16-03에서 EnemyBase에 구현)로 이동했다 (D-08)"
  artifacts:
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "마우스 방향 헬퍼 통합 + 디버그 로그 정리 + 점수 호출 제거"
      contains: "private Vector2 GetMouseWorldDirection"
  key_links: []
---

<objective>
`CombatController.cs`의 `FindNearestEnemyInRange()`에서 Linear/Fan 분기가 복붙하던 마우스→월드 방향 계산을 헬퍼로 통합하고(D-06), `DashOrWhiff()`/`ExecuteDash()`에 남은 디버깅용 `Debug.Log` 호출을 정리하며(D-07), `ExecuteDash()`에서 `ScoreManager.AddKillScore()` 호출과 `RespawnedEnemyMarker` 판정 로직을 제거한다(D-08 — 이 로직은 16-03에서 각 적의 `OnDashHit()`으로 이동한다).

Purpose: 16-CONTEXT.md D-06/D-07/D-08 — CombatController의 콘솔 노이즈와 중복 코드를 정리하고, "공격 시" 점수 적립을 "사망 시" 점수 적립으로 재설계하기 위한 CombatController 측 절반(호출 제거)을 완료한다. 나머지 절반(각 적의 자기 판정+점수 호출)은 16-03(Wave 2)에서 완료된다 — 이 Plan은 CombatController.cs만 건드리므로 16-03(MeleeEnemy.cs/RangedEnemy.cs)과 파일 충돌 없이 병렬 실행 가능하다.
Output: `CombatController.cs` — `GetMouseWorldDirection()` 헬퍼 신설, Debug.Log 12줄 제거, `isRespawnKill`/`ScoreManager.AddKillScore()` 호출 제거.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-boss-room-lifecycle/16-CONTEXT.md
@Assets/Scripts/Player/CombatController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: FindNearestEnemyInRange() 마우스 방향 계산 헬퍼 통합 (D-06)</name>
  <files>Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (현재 §364-425 `FindNearestEnemyInRange()` 전체)
    - .planning/phases/16-boss-room-lifecycle/16-CONTEXT.md `<decisions>` D-06
  </read_first>
  <action>
    `FindNearestEnemyInRange()` 메서드 바로 위에 새 private 헬퍼 메서드를 추가한다:
    ```csharp
    /// <summary>D-06(Phase 16): FindNearestEnemyInRange()의 Linear/Fan 두 분기가 거의 동일하게
    /// 복붙하던 마우스→월드 방향 계산을 통합. 마우스가 origin과 거의 겹치는 예외 상황
    /// (sqrMagnitude 낮음)에는 Vector2.right로 폴백 — 기존 Fan 분기에만 있던 안전장치를
    /// Linear 분기에도 동일하게 적용한다(정상 플레이 동작 변경 없음, 극단적 edge case만 보강).</summary>
    private Vector2 GetMouseWorldDirection(Vector2 origin)
    {
        UnityEngine.InputSystem.Mouse mouse = UnityEngine.InputSystem.Mouse.current;
        Vector2 mousePos = mouse != null ? mouse.position.ReadValue() : (Vector2)_mainCamera.WorldToScreenPoint(origin);
        Vector3 mouseWorld = _mainCamera.ScreenToWorldPoint(new Vector3(mousePos.x, mousePos.y, Mathf.Abs(_mainCamera.transform.position.z)));
        Vector2 dir = (Vector2)mouseWorld - origin;
        return dir.sqrMagnitude > 0.001f ? dir.normalized : Vector2.right;
    }
    ```

    그 다음 `FindNearestEnemyInRange()`의 앞부분(현재 §364-389)을 아래로 교체한다:

    기존:
    ```csharp
    private IEnemy FindNearestEnemyInRange()
    {
        Vector2 origin = (Vector2)transform.position;
        Vector2 attackDir = Vector2.right;
        float currentMaxDist = searchRadius;

        // [D-01/D-02 Integration] Calculate direction and specific radius once per search
        if (AttackTypeSelector.Selected == AttackType.Linear)
        {
            // Linear attack uses mouse-driven direction (matches RangeDisplay)
            UnityEngine.InputSystem.Mouse mouse = UnityEngine.InputSystem.Mouse.current;
            Vector2 mousePos = mouse != null ? mouse.position.ReadValue() : (Vector2)_mainCamera.WorldToScreenPoint(origin);
            Vector3 mouseWorld = _mainCamera.ScreenToWorldPoint(new Vector3(mousePos.x, mousePos.y, Mathf.Abs(_mainCamera.transform.position.z)));
            attackDir = ((Vector2)mouseWorld - origin).normalized;
            currentMaxDist = searchRadius; // matches linearLength=10
        }
        else
        {
            // Fan attack uses mouse-driven direction (matches RangeDisplay)
            UnityEngine.InputSystem.Mouse mouse = UnityEngine.InputSystem.Mouse.current;
            Vector2 mousePos2 = mouse != null ? mouse.position.ReadValue() : (Vector2)_mainCamera.WorldToScreenPoint(origin);
            Vector3 mouseWorld2 = _mainCamera.ScreenToWorldPoint(new Vector3(mousePos2.x, mousePos2.y, Mathf.Abs(_mainCamera.transform.position.z)));
            Vector2 dir = (Vector2)mouseWorld2 - origin;
            attackDir = dir.sqrMagnitude > 0.001f ? dir.normalized : Vector2.right;
            currentMaxDist = fanRadius;
        }

        // Pre-allocated buffer — no GC (ROADMAP Stack Constraint)
        int count = Physics2D.OverlapCircle(origin, searchRadius, _enemyFilter, _hitBuffer);
        ...
    ```

    신규:
    ```csharp
    private IEnemy FindNearestEnemyInRange()
    {
        Vector2 origin = (Vector2)transform.position;

        // D-06(Phase 16): Linear/Fan 분기가 거의 동일했던 마우스→월드 방향 계산을 헬퍼로 통합.
        Vector2 attackDir = GetMouseWorldDirection(origin);
        float currentMaxDist = (AttackTypeSelector.Selected == AttackType.Linear) ? searchRadius : fanRadius;

        // Pre-allocated buffer — no GC (ROADMAP Stack Constraint)
        int count = Physics2D.OverlapCircle(origin, searchRadius, _enemyFilter, _hitBuffer);
        ...
    ```

    (`...` 이하 나머지 `FindNearestEnemyInRange()` 본문은 변경 없음 — `nearest`/`bestSqDist` 루프, `IsInAttackShape()` 호출부는 그대로 유지)
  </action>
  <verify>
    <automated>grep -n "private Vector2 GetMouseWorldDirection" Assets/Scripts/Player/CombatController.cs && ! grep -n "mousePos2\|mouseWorld2" Assets/Scripts/Player/CombatController.cs && grep -c "GetMouseWorldDirection(origin)" Assets/Scripts/Player/CombatController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `GetMouseWorldDirection(Vector2 origin)` private 메서드가 존재한다
    - `FindNearestEnemyInRange()`가 `attackDir = GetMouseWorldDirection(origin);` 한 줄로 방향을 계산한다
    - `mousePos2`/`mouseWorld2` 변수명(구 Fan 분기 전용)이 파일에 더 이상 존재하지 않는다
    - `IsInAttackShape()` 호출부 및 루프 로직은 변경되지 않는다
  </acceptance_criteria>
  <done>Linear/Fan 마우스 방향 계산이 단일 헬퍼로 통합됨.</done>
</task>

<task type="auto">
  <name>Task 2: DashOrWhiff()/ExecuteDash() 디버그 로그 정리 + 점수 호출 제거 (D-07, D-08)</name>
  <files>Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (현재 §229-318 `DashOrWhiff()`/`ExecuteDash()` 전체)
    - .planning/phases/16-boss-room-lifecycle/16-CONTEXT.md `<decisions>` D-07, D-08
  </read_first>
  <action>
    **(A) D-07 — `DashOrWhiff()`에서 Debug.Log 5줄 삭제** (아래 5줄을 각각 제거, 나머지 로직은 그대로):
    - `Debug.Log("[Combat] Coroutine: DashOrWhiff started.");`
    - `Debug.Log($"[Combat] DashOrWhiff: Target '{((MonoBehaviour)target).name}' confirmed. Jumping to ExecuteDash.");`
    - `Debug.Log("[Combat] DashOrWhiff: ExecuteDash yield returned.");`
    - `Debug.Log("[Combat] DashOrWhiff: No target. Jumping to ExecuteWhiff.");`
    - `Debug.Log("[Combat] DashOrWhiff: Setting _isBusy = false and exiting.");`

    결과 `DashOrWhiff()`:
    ```csharp
    private IEnumerator DashOrWhiff(IEnemy cachedTarget = null)
    {
        _isBusy = true;
        _animator?.SetBool("IsAttacking", true);

        // 하이라이트된 적을 우선 사용, 없으면 재탐색
        var target = (cachedTarget != null && cachedTarget.IsAlive)
            ? cachedTarget
            : FindNearestEnemyInRange();
        if (target != null)
        {
            yield return ExecuteDash(target);
        }
        else
        {
            yield return ExecuteWhiff();
        }

        _animator?.SetBool("IsAttacking", false);
        _isBusy = false;
    }
    ```

    **(B) D-07 — `ExecuteDash()`에서 Debug.Log 7줄 삭제** (`Debug.LogError("[Combat] ExecuteDash: TARGET IS NULL at start! Aborting.");`는 실제 null 가드용 에러 로그이므로 유지 — D-07은 "Debug.Log" 호출만 대상으로 함):
    - `Debug.Log("[Combat] ExecuteDash: ENTRANCE - Coroutine effectively started.");`
    - `Debug.Log("[Combat] ExecuteDash: Calling ExitSlowMotion.");`
    - `Debug.Log("[Combat] ExecuteDash: ExitSlowMotion finished.");`
    - `Debug.Log($"[Combat] ExecuteDash: StartPos={startPos}, Dest={destination}, Dir={dirToTarget}");`
    - `Debug.Log("[Combat] ExecuteDash: Path clear. Preparing movement.");`
    - `Debug.Log("[Combat] ExecuteDash: Arrived at target");`
    - `Debug.Log("[Combat] ExecuteDash: Sequence Complete");`

    **(C) D-08 — "Kill and effects" 섹션에서 점수 호출 제거**. 기존:
    ```csharp
        // 6. Kill and effects
        bool isRespawnKill = ((MonoBehaviour)target).GetComponent<RespawnedEnemyMarker>() != null; // D-10(999.2): 리스폰 개체 여부로 감소 점수 판정 — IEnemy 미확장(ISpawnGatable과 동일 패턴)
        target.OnDashHit();
        AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
        SpawnHitSpark(destination);
        _cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
        ScoreManager.AddKillScore(isRespawnKill);
        yield return StartCoroutine(HitFreeze(hitFreezeDuration));
    ```
    신규 (isRespawnKill 계산과 ScoreManager.AddKillScore 호출 제거 — D-08: 이 로직은 16-03에서 각 적의 OnDashHit()으로 이동):
    ```csharp
        // 6. Kill and effects — D-08(Phase 16): 점수 적립은 각 적의 OnDashHit()(EnemyBase 공통 부분,
        // 16-03에서 구현)로 이동했다 — 여기서는 더 이상 ScoreManager.AddKillScore()를 호출하지 않는다.
        target.OnDashHit();
        AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
        SpawnHitSpark(destination);
        _cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
        yield return StartCoroutine(HitFreeze(hitFreezeDuration));
    ```

    최종 `ExecuteDash()` 전체 (A/B/C 적용 결과):
    ```csharp
    private IEnumerator ExecuteDash(IEnemy target)
    {
        if (target == null)
        {
            Debug.LogError("[Combat] ExecuteDash: TARGET IS NULL at start! Aborting.");
            yield break;
        }

        // 1. ExitSlowMotion BEFORE first yield — ensures MovePosition runs at timeScale=1
        ExitSlowMotion();

        Vector2 startPos    = _rb.position;
        Vector2 destination = (Vector2)((MonoBehaviour)target).transform.position;
        Vector2 dirToTarget = (destination - startPos).normalized;

        // 3. 대상 방향으로 스프라이트 전환
        _spriteRenderer.flipX = destination.x < startPos.x;

        // 4. Setup visual, animation, invincibility
        _animator?.SetBool("IsDashing", true);
        _invincibilityHandler.StartInvincibility(dashDuration + 0.05f);
        if (_trailRenderer != null) _trailRenderer.emitting = true;
        _rb.linearVelocity = Vector2.zero;

        // 5. 대시 이동 (smoothstep 보간으로 가속-감속 느낌)
        float elapsed = 0f;
        while (elapsed < dashDuration)
        {
            float t = elapsed / dashDuration;
            float smooth = t * t * (3f - 2f * t); // smoothstep
            _rb.MovePosition(Vector2.Lerp(startPos, destination, smooth));
            elapsed += Time.fixedDeltaTime;
            yield return new WaitForFixedUpdate();
        }
        _rb.MovePosition(destination);

        // 6. Cleanup visual and animation
        _animator?.SetBool("IsDashing", false);
        if (_trailRenderer != null) _trailRenderer.emitting = false;

        // 6. Kill and effects — D-08(Phase 16): 점수 적립은 각 적의 OnDashHit()(EnemyBase 공통 부분,
        // 16-03에서 구현)로 이동했다 — 여기서는 더 이상 ScoreManager.AddKillScore()를 호출하지 않는다.
        target.OnDashHit();
        AudioManager.PlaySfx(Sfx.Slash); // SFX-03/D-05: 처치 확정 순간 슬래시 — HitFreeze 이전 호출, DSP는 timeScale=0 중에도 계속 재생
        SpawnHitSpark(destination);
        _cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
        yield return StartCoroutine(HitFreeze(hitFreezeDuration));

        _attackCooldown = postKillLockout;
        _gauge.AddKillBonus();
    }
    ```

    이 태스크에서 건드리지 않는 것 (정밀한 변경 원칙):
    - `Update()`의 `Debug.Log("[Combat] Attack released. Checking dash condition.");` (§160) — D-07 결정 범위가 `DashOrWhiff()`/`ExecuteDash()`로 명시적으로 한정되어 있어 그대로 둔다.
    - `ExecuteWhiff()`의 `Debug.Log("[Combat] Executing Whiff (Penalty)");` (§322) — 마찬가지로 D-07 범위 밖.
    - `HitFreeze()`, `IsInAttackShape()`, `UpdateHighlight()` 등 나머지 메서드는 무변경.
  </action>
  <verify>
    <automated>test $(grep -c "Debug.Log(" Assets/Scripts/Player/CombatController.cs) -eq 2 && grep -n "Debug.LogError(\"\[Combat\] ExecuteDash: TARGET IS NULL" Assets/Scripts/Player/CombatController.cs && ! grep -n "RespawnedEnemyMarker\|ScoreManager.AddKillScore" Assets/Scripts/Player/CombatController.cs && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/Player/CombatController.cs` 전체에서 `Debug.Log(` 문자열이 정확히 2건만 남는다 (Update()의 "Attack released" 1건 + ExecuteWhiff()의 "Executing Whiff" 1건 — 둘 다 D-07 범위 밖이라 원문 그대로 유지)
    - `Debug.LogError("[Combat] ExecuteDash: TARGET IS NULL at start! Aborting.")`는 그대로 남아있다
    - `RespawnedEnemyMarker`, `ScoreManager.AddKillScore` 문자열이 파일에 더 이상 존재하지 않는다
    - `target.OnDashHit();` 호출은 그대로 유지된다 (호출 위치/순서 변경 없음)
  </acceptance_criteria>
  <done>DashOrWhiff()/ExecuteDash()의 디버그 로그 노이즈가 제거되고, ExecuteDash()가 더 이상 점수를 직접 적립하지 않음 — D-08의 CombatController 측 절반 완료.</done>
</task>

</tasks>

<verification>
- `Debug.Log(` 발생 횟수가 파일 전체에서 2건(D-07 범위 밖 2곳)으로 감소
- `RespawnedEnemyMarker`/`ScoreManager.AddKillScore` 참조가 CombatController.cs에서 완전히 제거됨
- `GetMouseWorldDirection()` 헬퍼가 Linear/Fan 양쪽에서 사용됨
</verification>

<success_criteria>
- D-06, D-07이 완전히 완료됨
- D-08의 CombatController 측 절반(호출 제거)이 완료됨 — 16-03이 나머지 절반(각 적의 자기 판정+점수 호출)을 완료하면 D-08 전체가 완료됨
</success_criteria>

<output>
After completion, create `.planning/phases/16-boss-room-lifecycle/16-02-SUMMARY.md`
</output>
