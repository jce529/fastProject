---
phase: 999.4-enemy-ai-enhancement-pack
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/RangedEnemy.cs
autonomous: false
requirements: [D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "원거리 적은 Chase 상태에서 플레이어가 kitingRetreatRange 안으로 들어오면 즉시 후퇴 이동을 시작한다 (D-07/D-08)"
    - "후퇴 중에도 조준선(LineRenderer)과 발사(TelegraphAndFire)는 기존과 동일하게 병행 동작한다 (D-09)"
    - "kitingRetreatRange 밖으로 벗어나면 후퇴 이동이 멈춘다 — 무한 후퇴하지 않는다"
    - "원거리 적 자체의 조준 텔레그래프(0.8초, LineRenderer 알파 페이드)는 변경 전과 동일하게 동작한다 (D-06 회귀 없음)"
  artifacts:
    - path: "Assets/Scripts/Enemy/RangedEnemy.cs"
      provides: "kitingRetreatRange/kitingSpeed 필드 + UpdateChase() 후퇴 이동 로직"
      contains: "kitingRetreatRange"
  key_links:
    - from: "RangedEnemy.UpdateChase()"
      to: "_rb.linearVelocity"
      via: "플레이어와의 거리가 kitingRetreatRange 미만이면 반대 방향 kitingSpeed 대입 (MeleeEnemy.UpdateChase()의 기존 linearVelocity 패턴과 동일)"
      pattern: "kitingSpeed"
    - from: "RangedEnemy.UpdateChase()"
      to: "RangedEnemy.TelegraphAndFire()"
      via: "기존 IsPlayerInRange(aimLineLength) && _telegraphCoroutine == null 체크를 변경 없이 그대로 유지 — 코루틴은 후퇴 이동과 독립적으로 실행됨"
      pattern: "_telegraphCoroutine"
---

<objective>
원거리 적(RangedEnemy)이 Chase 상태에서 제자리에 고정되어 있던 문제를 해결한다 — 플레이어가 `kitingRetreatRange`(≈`aimLineLength`의 50%) 안으로 들어오면 반대 방향으로 후퇴 이동을 시작하고, 기존 조준/발사(`TelegraphAndFire`)는 변경 없이 그대로 병행 동작한다.

Purpose: D-07(단순 키팅 — 근접 적 위치 미참조)/D-08(Chase 진입 즉시 발동)/D-09(조준·발사와 병행)/D-10(임계값 ≈aimLineLength*0.5f)을 만족하는 최소 추가 이동 로직. D-03의 낙사 위험 확대(원거리 적이 처음으로 이동하게 됨)는 999.4-01의 FallZoneTrigger 안전망이 담당 — 이 Plan은 별도 gap-check를 추가하지 않는다(RESEARCH.md 명시 권장).
Output: `RangedEnemy.cs`(kitingRetreatRange/kitingSpeed 필드 + UpdateChase() 후퇴 로직).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md
@Assets/Scripts/Enemy/RangedEnemy.cs
@Assets/Scripts/Enemy/MeleeEnemy.cs

<interfaces>
현재 `RangedEnemy.cs`의 관련 부분 (2026-07-10 확인, §14-22 필드 / §165-185 UpdateChase):

```csharp
[SerializeField] private float detectionRadius = 12f;
[SerializeField] private float moveSpeed       = 0f;      // D-10(Phase 3): 0 = stationary. Increase post-playtest.
[SerializeField] private float aimLineLength   = 15f;     // LineRenderer endpoint length
[SerializeField] private float patrolSpeed     = 1.5f;
[SerializeField] private float patrolHalfRange = 3f;
...

private void UpdateChase()
{
    _animator?.SetBool("isMoving", false); // 매 프레임 첫 줄 — 오늘은 이동이 전혀 없음
    if (_playerTransform == null)
    {
        FindPlayerTransform();
        if (_playerTransform == null) { _state = EnemyState.Idle; return; }
    }

    if (!IsPlayerInRange(detectionRadius))
    {
        _playerTransform = null;
        _state = EnemyState.Idle;
        return;
    }

    if (IsPlayerInRange(aimLineLength) && _telegraphCoroutine == null)
    {
        _telegraphCoroutine = StartCoroutine(TelegraphAndFire());
    }
}
```

`MeleeEnemy.cs`의 기존 Chase-state 이동 패턴 (§171-174, 키팅 이동에 그대로 참고할 확립된 패턴 — RESEARCH.md 권장: `_rb.linearVelocity`가 `MovePosition`보다 적합, `RangedEnemy`의 Rigidbody2D도 Dynamic + gravityScale 3.5로 동일):
```csharp
float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);
_rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y); // gravity의 Y 성분 보존
```

`TelegraphAndFire()`(§187-242)는 이 Plan에서 전혀 수정하지 않는다 (D-06, 999.4-03의 범위이기도 아니고 애초에 원거리 텔레그래프는 이번 phase 전체에서 미변경).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RangedEnemy 키팅 후퇴 이동 추가 (D-07, D-08, D-09, D-10)</name>
  <files>Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/RangedEnemy.cs (현재 전체 — 위 interfaces 블록의 UpdateChase와 동일)
    - Assets/Scripts/Enemy/MeleeEnemy.cs 의 UpdateChase()(§154-178) — 키팅 이동이 mirroring할 linearVelocity 패턴
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md 의 "Ranged Kiting (D-07/D-08/D-09/D-10)" 절 (움직임 API 선택 근거)
  </read_first>
  <action>
    **(A) 필드 §14-16 근처에 신규 필드 추가** (기존 `moveSpeed`/`aimLineLength` 유지, 그 아래에 추가):
    ```csharp
    [SerializeField] private float kitingRetreatRange = 7.5f; // D-10: aimLineLength(15f)의 약 50% — 이 거리보다 가까워지면 후퇴 시작
    [SerializeField] private float kitingSpeed         = 3f;  // D-07: 후퇴 이동 속도 (Claude's Discretion — chaseSpeed 4f보다 약간 느리게 시작, 플레이테스트로 조정)
    ```

    **(B) `UpdateChase()`(§165-185)를 아래로 교체** — 기존 `_animator?.SetBool("isMoving", false)` 첫 줄을 제거하고 D-08(Chase 진입 즉시 발동)/D-10(거리 임계값)/D-09(조준·발사 병행 유지) 로직을 추가:
    ```csharp
    private void UpdateChase()
    {
        if (_playerTransform == null)
        {
            FindPlayerTransform();
            if (_playerTransform == null) { _state = EnemyState.Idle; return; }
        }

        if (!IsPlayerInRange(detectionRadius))
        {
            _playerTransform = null;
            _state = EnemyState.Idle;
            return;
        }

        // D-07/D-08/D-10: Chase 진입 즉시, 거리가 kitingRetreatRange 미만이면 반대 방향으로 후퇴.
        // 근접 적 위치는 참조하지 않는 단순 거리-유지 이동(D-07 명시 범위).
        float dist = Vector2.Distance(transform.position, _playerTransform.position);
        if (dist < kitingRetreatRange)
        {
            float dirAway = Mathf.Sign(transform.position.x - _playerTransform.position.x);
            _rb.linearVelocity = new Vector2(dirAway * kitingSpeed, _rb.linearVelocity.y);
            _animator?.SetBool("isMoving", true);
        }
        else
        {
            _rb.linearVelocity = new Vector2(0f, _rb.linearVelocity.y); // 임계값 밖 — 수평 이동 정지 (무한 후퇴 방지)
            _animator?.SetBool("isMoving", false);
        }

        // D-09: 조준/발사는 위 후퇴 이동과 무관하게 매 프레임 그대로 체크 — StartCoroutine은 non-blocking이므로 병행 보장.
        if (IsPlayerInRange(aimLineLength) && _telegraphCoroutine == null)
        {
            _telegraphCoroutine = StartCoroutine(TelegraphAndFire());
        }
    }
    ```

    `TelegraphAndFire()`, `UpdateIdle()`, `FireProjectile()`, `moveSpeed`(patrol용, D-10 Phase 3) 등 다른 메서드/필드는 전혀 수정하지 않는다 (surgical change — D-06: 원거리 자체 텔레그래프 변경 없음).

    RangedEnemy에는 `FlipSprite()` 메서드가 존재하지 않는다(MeleeEnemy 전용) — 이 Plan은 신규 요청되지 않은 스프라이트 반전 기능을 추가하지 않는다(RESEARCH.md 코드 스케치에도 포함되지 않음, D-07-D-10 어디에도 명시되지 않은 범위). 시각적으로 어색하면 Task 2 플레이테스트에서 관찰 항목으로만 기록한다.
  </action>
  <verify>
    <automated>grep -n "kitingRetreatRange" Assets/Scripts/Enemy/RangedEnemy.cs && grep -n "kitingSpeed" Assets/Scripts/Enemy/RangedEnemy.cs && grep -n "_telegraphCoroutine == null" Assets/Scripts/Enemy/RangedEnemy.cs</automated>
  </verify>
  <acceptance_criteria>
    - `RangedEnemy.cs`에 `kitingRetreatRange`(기본값 7.5f)와 `kitingSpeed`(기본값 3f) `[SerializeField]` 필드가 존재한다
    - `UpdateChase()`가 `dist < kitingRetreatRange` 조건으로 후퇴 이동(`_rb.linearVelocity`)을 적용하는 분기를 포함한다
    - `UpdateChase()` 마지막에 `IsPlayerInRange(aimLineLength) && _telegraphCoroutine == null` 체크가 변경 없이 그대로 존재한다 (D-09 병행)
    - `TelegraphAndFire()` 메서드 본문은 이 태스크에서 한 글자도 변경되지 않는다 (D-06 — grep diff 0)
    - `MovePosition`이 아닌 `_rb.linearVelocity`가 후퇴 이동에 사용된다 (RESEARCH.md 권장 API)
  </acceptance_criteria>
  <done>RangedEnemy가 Chase 상태 진입 즉시 플레이어와의 거리를 감시해 kitingRetreatRange 안에서 후퇴 이동하고, 기존 조준/발사는 변경 없이 병행 동작한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 — 원거리 키팅 D-07/D-08/D-09/D-10 + D-06 회귀 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md §Implementation Decisions (D-07~D-10, D-06)
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <action>
    키팅 임계값/속도/조준-발사 병행은 물리·타이밍 기반 실시간 플레이 동작이라 자동화 불가(999.4-VALIDATION.md) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 임계값/속도 체감이 어색하면 `kitingRetreatRange`/`kitingSpeed`(둘 다 Claude's Discretion 값) 재조정 여부를 논의한다.
  </action>
  <what-built>RangedEnemy.UpdateChase()에 D-07(단순 거리-유지 후퇴)/D-08(Chase 진입 즉시 발동)/D-09(조준·발사 병행)/D-10(aimLineLength 50% 임계값) 키팅 로직 추가 — TelegraphAndFire()는 미변경(D-06).</what-built>
  <how-to-verify>
    에디터 Play 후 체크리스트:

    **[D-07/D-08/D-10 — 후퇴 발동 및 임계값]**
    1. 원거리 적의 detectionRadius 밖에서 서서히 접근한다 → detectionRadius 진입 시 Chase 상태로 전환되는지 확인 (기존 동작)
    2. 계속 접근해 kitingRetreatRange(≈7.5유닛, aimLineLength 15의 50%) 안으로 들어가면 즉시 원거리 적이 반대 방향으로 후퇴 이동을 시작하는지 확인
    3. 근접 적 위치를 신경쓰지 않는 단순 거리-유지인지 확인(근접 적이 근처에 있어도 그 뒤로 숨으려 하지 않고 그냥 플레이어 반대 방향으로만 이동)

    **[D-09 — 후퇴 중 조준/발사 병행]**
    4. 후퇴 이동 중에도 LineRenderer 조준선이 계속 플레이어를 따라가며 텔레그래프(0.8초 알파 페이드)가 정상 진행되는지 확인
    5. 후퇴 이동 중에 텔레그래프가 완료되면 정상적으로 발사(FireProjectile)되는지 확인

    **[무한 후퇴 방지]**
    6. kitingRetreatRange 밖으로 플레이어가 다시 벗어나면 후퇴 이동이 멈추는지 확인 (제자리에서 대기, 무한히 뒤로 미끄러지지 않음)

    **[D-06 — 회귀 없음]**
    7. 조준선 색상/알파 페이드 타이밍(0.8초), 발사체 궤적이 이 Plan 적용 전과 동일하게 보이는지 확인

    **[관찰 — 조치 불필요]**
    8. 후퇴 이동 중 스프라이트가 좌우 반전되지 않아 이동 방향과 시각적으로 어색해 보이는지 관찰만 하고 기록 (이번 phase 범위 밖 — 별도 백로그 후보로만 남김)
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 키팅 임계값/속도/조준-발사 병행은 실시간 플레이 관찰만 가능 (999.4-VALIDATION.md, 자동화 테스트 프레임워크 없음). 통과 기준: 체크리스트 1~7 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~7번 전부 통과 보고 수신
    - 8번(스프라이트 반전 관찰)은 통과/실패 판정 대상이 아니며 기록만 한다
    - 문제가 보고된 항목이 있다면 구체적 증상(거리, 상황)이 함께 기록된다
  </acceptance_criteria>
  <done>D-07/D-08/D-09/D-10이 실제 플레이로 검증되고, D-06(원거리 텔레그래프 미변경)이 회귀 없이 유지됨이 확인됨.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "2번: 7.5유닛보다 훨씬 가까워져야 후퇴 시작함")</resume-signal>
</task>

</tasks>

<verification>
- `RangedEnemy.cs` 컴파일 성공 (콘솔 에러 0건)
- `TelegraphAndFire()`/`IEnemy`/`ISpawnGatable` 구현부는 변경되지 않음 (grep 확인)
- 후퇴 임계값/속도/조준-발사 병행 동작 (플레이테스트 확인)
</verification>

<success_criteria>
- D-07/D-08/D-09/D-10이 실제 플레이로 검증됨
- 이 Plan은 gap-check를 추가하지 않으며(D-03/RESEARCH.md 명시), 999.4-01의 FallZoneTrigger 안전망이 늘어난 낙사 노출을 담당함을 확인
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-02-SUMMARY.md`
</output>
