---
phase: 999.4-enemy-ai-enhancement-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/MeleeEnemy.cs
  - Assets/Scripts/Environment/FallZoneTrigger.cs
autonomous: false
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "근접 적이 Room_Gap(3유닛)/Room_Fall(5.5유닛) 갭에서 추격 중 낙사하지 않는다 — 점프로 건너거나 턴어라운드한다 (D-01)"
    - "낙사한 적(근접/원거리 무관)은 파티클/사운드 없이 즉시 Destroy되며 화면 밖 좀비 상태로 남지 않는다 (D-02/D-03)"
    - "FallZoneTrigger는 Player 낙사 처리(FallDetector.OnFall)를 기존과 동일하게 수행하면서 Enemy 태그도 함께 처리한다"
  artifacts:
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "GapAhead 기반 턴어라운드 하드닝 + jumpForce 튜닝"
      contains: "GapTooWideToCross"
    - path: "Assets/Scripts/Environment/FallZoneTrigger.cs"
      provides: "Enemy 태그 사후처리 분기 (즉시 Destroy, VFX 없음)"
      contains: "CompareTag(\"Enemy\")"
  key_links:
    - from: "Assets/Scripts/Environment/FallZoneTrigger.cs"
      to: "Assets/Scripts/Enemy/MeleeEnemy.cs, Assets/Scripts/Enemy/RangedEnemy.cs"
      via: "OnTriggerEnter2D의 Enemy 태그 체크 → Destroy(other.gameObject) — 두 프리팹 모두 루트에 Enemy 태그(layer 10) 부착"
      pattern: "CompareTag\\(\"Enemy\"\\)"
    - from: "Assets/Scripts/Enemy/MeleeEnemy.cs UpdateChase()"
      to: "GapTooWideToCross()/GapAhead()"
      via: "gap이 감지되고 동시에 건너뛸 수 없을 만큼 넓으면 dirX를 반전해 턴어라운드"
      pattern: "GapTooWideToCross"
---

<objective>
근접 적(MeleeEnemy)의 기존 갭 감지(GapAhead)/점프(TryJump) 로직을 강화해 애초에 낙사하지 않도록 예방하고, `FallZoneTrigger`에 Enemy 태그 사후처리 분기를 추가해 예방을 뚫고 떨어진 적(근접+향후 원거리 키팅 포함)을 화면 밖에서 조용히 정리한다 — D-01(예방)/D-02(즉시 Destroy, VFX 없음)/D-03(근접+원거리 공통) 이중 안전망.

Purpose: 999.4-RESEARCH.md가 확인한 대로, RangedEnemy는 이번 phase(999.4-02)에서 신규 키팅 이동이 추가되면 처음으로 낙사 위험에 노출된다 — 이 Plan을 Wave 1에서 999.4-02(키팅)와 병렬로 먼저 완성해, 안전망이 키팅 도입과 동시에 마련되도록 한다.
Output: `MeleeEnemy.cs`(GapTooWideToCross + 턴어라운드 분기 + jumpForce 튜닝), `FallZoneTrigger.cs`(Enemy 분기 추가), Layer Collision Matrix 검증 기록.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md
@Assets/Scripts/Enemy/MeleeEnemy.cs
@Assets/Scripts/Environment/FallZoneTrigger.cs

<interfaces>
현재 `MeleeEnemy.cs`의 갭/점프 관련 부분 (2026-07-10 확인, §22-24 필드 / §154-178 UpdateChase / §264-298 헬퍼):

```csharp
[SerializeField] private float jumpForce     = 8f;    // 점프 초속 (Inspector 조정)
[SerializeField] private float wallCheckDist = 0.4f;  // 앞 벽 감지 거리
[SerializeField] private float gapCheckDist  = 0.6f;  // 바닥 끊김 감지 거리 (발 앞으로)

private void UpdateChase()
{
    if (_playerTransform == null) { FindPlayerTransform(); if (_playerTransform == null) { _state = EnemyState.Idle; return; } }
    float dist = Vector2.Distance(transform.position, _playerTransform.position);
    if (dist <= attackRange) { _attackCoroutine = StartCoroutine(TelegraphAndAttack()); return; }

    float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);
    _rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y);
    FlipSprite(dirX);
    TryJump(dirX);
    _animator?.SetBool("isMoving", true);
    _animator?.SetBool("isChasing", true);
}

private void TryJump(float moveDir)
{
    if (!IsGrounded()) return;
    if (WallAhead(moveDir) || GapAhead(moveDir))
        _rb.linearVelocity = new Vector2(_rb.linearVelocity.x, jumpForce);
}

private bool GapAhead(float moveDir)
{
    Vector2 origin = (Vector2)transform.position + new Vector2(moveDir * gapCheckDist, -0.5f);
    RaycastHit2D hit = Physics2D.Raycast(origin, Vector2.down, 0.5f, LayerMask.GetMask("Default", "Ground", "Platform"));
    return hit.collider == null;
}
```

RESEARCH.md 물리 추정(§"Fall Handling"): `jumpForce=8f`/`chaseSpeed=4f`일 때 체공시간 약 0.47초, 수평 이동거리 약 1.9유닛 — 실측된 `Room_Gap`(3유닛)/`Room_Fall`(5.5유닛) 갭을 클리어하지 못할 가능성이 높음. `jumpForce≈13f`면 체공시간 0.75초, 수평 이동거리 3유닛(`Room_Gap` 클리어 가능 추정치)이 나온다 — 5.5유닛 `Room_Fall`은 현실적인 점프력으로 클리어 불가능한 규모이므로 예방책이 아니라 "턴어라운드" 대상으로 다룬다(RESEARCH.md Pitfall 2 권장안).

현재 `FallZoneTrigger.cs` 전체 (2026-07-10 확인):
```csharp
public class FallZoneTrigger : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        var fallDetector = other.GetComponent<FallDetector>();
        if (fallDetector == null) return;
        fallDetector.OnFall();
    }
}
```
`MeleeEnemy`/`RangedEnemy` 프리팹 루트 모두 `m_Layer: 10`("Enemy"), `m_TagString: Enemy` — 태그 체크만으로 두 타입 모두 커버 가능(RESEARCH.md 확인 완료).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Unity 에디터 — Physics2D Layer Collision Matrix 확인 (Enemy × Default)</name>
  <files>없음 (ProjectSettings 확인 전용 — 코드 변경 없음)</files>
  <read_first>
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md 의 "Open Questions" §1 및 "Common Pitfalls" Pitfall 1
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-VALIDATION.md 의 "Mandatory pre-implementation check"
  </read_first>
  <action>
    Unity MCP가 이번 세션에 승인되지 않아 `ProjectSettings/Physics2DSettings.asset`의 비트 압축 Layer Collision Matrix를 코드로 확인할 수 없다(RESEARCH.md Open Question 1). Task 2에서 구현할 `FallZoneTrigger`의 Enemy 사후처리가 실제로 동작하려면 Enemy(layer 10) × Default(layer 0) 충돌이 활성화되어 있어야 한다 — 비활성 상태면 `OnTriggerEnter2D`가 Enemy에 대해 아무 에러 없이 조용히 호출되지 않는다.
  </action>
  <what-built>아직 코드 변경 없음 — 이 체크포인트는 Task 2(FallZoneTrigger Enemy 분기)가 실제로 작동하기 위한 전제조건을 사전 확인한다.</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서:
    1. Edit → Project Settings → Physics 2D 로 이동
    2. Layer Collision Matrix에서 "Enemy" 행과 "Default" 열의 교차 체크박스가 켜져 있는지(collide) 확인
    3. 꺼져 있다면 체크박스를 켜서 활성화한다 (다른 Default-layer 트리거/콜라이더가 의도적으로 Enemy를 무시하도록 설계된 곳이 없는지 — `FallZoneTrigger`의 KillZone 오브젝트만 Default 레이어에 있고 별다른 Enemy 회피 설계는 RESEARCH.md에서 발견되지 않음 — 안전하게 활성화 가능)
    4. 결과(원래 켜져 있었는지, 꺼져 있어서 켰는지)를 기록한다
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — Layer Collision Matrix는 Unity 에디터 Project Settings UI에서만 확인/수정 가능, ProjectSettings/Physics2DSettings.asset의 비트 압축 포맷은 코드로 신뢰성 있게 디코딩 불가 (RESEARCH.md Open Question 1)</automated>
  </verify>
  <acceptance_criteria>
    - MANUAL-ONLY: 사용자가 Enemy×Default 충돌이 활성화되어 있음(또는 활성화했음)을 확인 보고
  </acceptance_criteria>
  <done>Enemy(layer 10)×Default(layer 0) Physics2D 충돌이 활성화되어 있음이 확인됨 — Task 2의 FallZoneTrigger Enemy 분기가 실제로 트리거될 수 있는 전제조건 충족.</done>
  <resume-signal>"approved" (충돌 활성화 확인됨) 또는 "비활성 상태였으나 활성화 완료" 또는 문제 발생 시 구체적 증상 설명</resume-signal>
</task>

<task type="auto">
  <name>Task 2: MeleeEnemy 갭 하드닝(턴어라운드) + FallZoneTrigger Enemy 사후처리 추가 (D-01, D-02, D-03)</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs, Assets/Scripts/Environment/FallZoneTrigger.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/MeleeEnemy.cs (현재 전체 — 위 interfaces 블록의 UpdateChase/TryJump/GapAhead와 동일)
    - Assets/Scripts/Environment/FallZoneTrigger.cs (현재 전체 — 위 interfaces 블록과 동일)
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md 의 "Fall Handling" 및 "Code Examples" > "Minimal FallZoneTrigger extension sketch" 절
  </read_first>
  <action>
    **(A) `Assets/Scripts/Enemy/MeleeEnemy.cs` 수정:**

    1. 필드 §22 근처를 아래로 교체 (jumpForce 튜닝 + 신규 필드 추가):
    ```csharp
    [SerializeField] private float jumpForce     = 13f;   // 점프 초속 (D-01 하드닝: 8f→13f, Room_Gap 3유닛 클리어 추정치 — RESEARCH.md 물리 계산, 플레이테스트로 재조정 가능)
    [SerializeField] private float wallCheckDist = 0.4f;  // 앞 벽 감지 거리
    [SerializeField] private float gapCheckDist  = 0.6f;  // 바닥 끊김 감지 거리 (발 앞으로)
    [SerializeField] private float maxJumpableGapWidth = 3.2f; // D-01: 이 거리보다 먼 곳까지 바닥이 없으면 점프로 건널 수 없는 gap → 턴어라운드 (Room_Fall 5.5유닛을 걸러내고 Room_Gap 3유닛은 통과시키는 값)
    ```

    2. `UpdateChase()` (§154-178)를 아래로 교체 — dirX 계산 직후, 이동 적용 전에 턴어라운드 분기 삽입:
    ```csharp
    private void UpdateChase()
    {
        if (_playerTransform == null)
        {
            FindPlayerTransform();
            if (_playerTransform == null) { _state = EnemyState.Idle; return; }
        }

        float dist = Vector2.Distance(transform.position, _playerTransform.position);
        if (dist <= attackRange)
        {
            _attackCoroutine = StartCoroutine(TelegraphAndAttack());
            return;
        }

        float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);

        // D-01 하드닝 (RESEARCH.md Pitfall 2): 점프로 건널 수 없을 만큼 넓은 gap이면 뛰어들지 않고 턴어라운드
        if (IsGrounded() && GapAhead(dirX) && GapTooWideToCross(dirX))
            dirX = -dirX;

        _rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y);
        FlipSprite(dirX);
        TryJump(dirX);
        _animator?.SetBool("isMoving", true);
        _animator?.SetBool("isChasing", true);
    }
    ```

    3. `GapAhead()` 메서드(§291-298) 바로 뒤에 신규 헬퍼 메서드 추가:
    ```csharp
    /// <summary>D-01 하드닝(Pitfall 2): maxJumpableGapWidth보다 먼 지점까지도 바닥이 없으면 점프로 건널 수 없는 gap → true.</summary>
    private bool GapTooWideToCross(float moveDir)
    {
        Vector2 origin = (Vector2)transform.position + new Vector2(moveDir * maxJumpableGapWidth, -0.5f);
        RaycastHit2D hit = Physics2D.Raycast(origin, Vector2.down, 0.5f,
            LayerMask.GetMask("Default", "Ground", "Platform"));
        return hit.collider == null;
    }
    ```

    `TryJump`/`IsGrounded`/`WallAhead`/`GapAhead`/`FlipSprite`는 변경하지 않는다 (surgical change — 최소 필드/메서드 추가와 UpdateChase 삽입만).

    **(B) `Assets/Scripts/Environment/FallZoneTrigger.cs` 전체 교체:**
    ```csharp
    using UnityEngine;

    /// <summary>
    /// Attach to FallZone_Left and FallZone_Right trigger colliders.
    /// Player가 진입하면 FallDetector.OnFall()을 호출한다 (기존 동작, 변경 없음).
    /// Enemy(MeleeEnemy/RangedEnemy 공통 "Enemy" 태그)가 진입하면 D-02/D-03: 사망 이펙트 없이
    /// 즉시 Destroy — 화면 밖에서 벌어지는 낙사이므로 연출은 불필요, 좀비 상태 방지가 목적이다.
    /// </summary>
    public class FallZoneTrigger : MonoBehaviour
    {
        private void OnTriggerEnter2D(Collider2D other)
        {
            if (other.CompareTag("Player"))
            {
                var fallDetector = other.GetComponent<FallDetector>();
                if (fallDetector != null) fallDetector.OnFall();
                return;
            }

            if (other.CompareTag("Enemy"))
            {
                // D-02/D-03: MeleeEnemy/RangedEnemy 공통 — VFX/사운드 없이 즉시 제거.
                // OnDisable()이 자동 호출되어 PlayerController.OnPlayerDeath 구독 해제, 코루틴도 자동 정지됨.
                Destroy(other.gameObject);
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "maxJumpableGapWidth" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -n "GapTooWideToCross" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -n "jumpForce.*=.*13f" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -n "CompareTag(\"Enemy\")" Assets/Scripts/Environment/FallZoneTrigger.cs</automated>
  </verify>
  <acceptance_criteria>
    - `MeleeEnemy.cs`에 `maxJumpableGapWidth` 필드와 `GapTooWideToCross(float moveDir)` 메서드가 존재한다
    - `jumpForce` 기본값이 `13f`로 변경되어 있다
    - `UpdateChase()`가 `dirX` 계산 직후, 이동 적용 전에 `GapTooWideToCross`를 사용한 턴어라운드 분기를 포함한다
    - `TryJump`/`IsGrounded`/`WallAhead`/`GapAhead`/`FlipSprite` 메서드 본문은 변경되지 않는다 (surgical change)
    - `FallZoneTrigger.cs`가 `CompareTag("Enemy")` 분기를 포함하고, 기존 `CompareTag("Player")` → `FallDetector.OnFall()` 경로는 그대로 유지된다
    - `IEnemy` 인터페이스 파일은 이 태스크에서 전혀 수정되지 않는다 (grep으로 diff 없음 확인 가능)
  </acceptance_criteria>
  <done>MeleeEnemy가 건널 수 없는 gap 앞에서 턴어라운드하고, FallZoneTrigger가 Enemy 태그 진입 시 즉시 Destroy하는 사후처리 안전망을 갖춘다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 플레이테스트 — 낙사 처리 D-01/D-02/D-03 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md §Implementation Decisions (D-01, D-02, D-03)
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <action>
    갭 클리어/턴어라운드/낙사 사후처리는 물리·타이밍 기반 Unity 게임플레이 동작이라 자동화 불가(999.4-VALIDATION.md, 프로젝트 전역에 테스트 프레임워크 없음) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 `jumpForce`/`maxJumpableGapWidth`(Claude's Discretion 튜닝값) 재조정 여부를 사용자와 논의한다.
  </action>
  <what-built>MeleeEnemy의 gap-too-wide 턴어라운드 하드닝(GapTooWideToCross, jumpForce 8f→13f) + FallZoneTrigger의 Enemy 태그 사후처리(즉시 Destroy, VFX 없음) — D-01(예방)/D-02(즉시 제거)/D-03(근접+원거리 공통) 이중 안전망.</what-built>
  <how-to-verify>
    에디터 Play 후 체크리스트:

    **[D-01 — 예방: 갭 클리어 또는 턴어라운드]**
    1. `Room_Gap`(3유닛 갭)에서 근접 적을 추격 상태로 만들고 갭을 향해 이동시킨다 → 점프로 갭을 건너는지 확인
    2. `Room_Fall`(5.5유닛 갭)에서 동일하게 확인 → 점프하지 않고 턴어라운드(반대 방향으로 이동 전환)하는지 확인, 갭으로 걸어 들어가 낙사하지 않는지 확인
    3. 두 방(Room_Gap/Room_Fall)에서 각각 3회 이상 반복 관찰해 낙사가 발생하지 않는지 확인

    **[D-02/D-03 — 사후처리: 낙사 시 즉시 Destroy]**
    4. 의도적으로 근접 적을 낙사시킬 수 있는 지점(맵 경계, 강제 유도 등)을 찾아 떨어뜨린다 → 파티클/사운드 없이 즉시 사라지는지, 화면 밖에서 계속 "살아있는" 좀비 상태로 남지 않는지 확인
    5. 원거리 적도 999.4-02(키팅) 적용 후 낙사 유도 시 동일하게 즉시 Destroy되는지 확인 (999.4-02 완료 후 재확인 필요 시 이 항목만 재검증)
    6. 낙사한 적이 사라진 뒤에도 콘솔 에러가 없는지 확인

    **[회귀 확인]**
    7. Player가 기존 FallZone(낙사)에 진입할 때 기존과 동일하게 `FallDetector.OnFall()` → 텔레포트 복귀 동작이 그대로 작동하는지 확인 (Player 낙사 경로 회귀 없음)
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 갭 클리어/턴어라운드/낙사 타이밍은 물리 기반 실시간 플레이 관찰만 가능 (999.4-VALIDATION.md, 자동화 테스트 프레임워크 없음). 통과 기준: 체크리스트 1~7 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~7번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(어느 방, 어떤 조건)이 함께 기록된다
  </acceptance_criteria>
  <done>D-01/D-02/D-03이 실제 플레이로 검증됨 — 근접 적이 갭에서 낙사하지 않고, 낙사한 적(모든 타입)은 즉시 조용히 정리되며, 기존 Player 낙사 경로는 회귀 없이 유지된다.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "2번: Room_Fall에서도 여전히 낙사함")</resume-signal>
</task>

</tasks>

<verification>
- `MeleeEnemy.cs`/`FallZoneTrigger.cs` 컴파일 성공 (콘솔 에러 0건)
- `IEnemy` 인터페이스는 변경되지 않음 (grep 확인)
- Room_Gap/Room_Fall에서 낙사 없음, 낙사 사후처리는 VFX 없이 즉시 Destroy (플레이테스트 확인)
</verification>

<success_criteria>
- D-01/D-02/D-03이 실제 플레이로 검증됨
- 999.4-02(키팅)가 Wave 1에서 병렬로 작업되는 동안에도 이 Plan의 안전망(FallZoneTrigger Enemy 분기)이 함께 완성되어, 키팅 도입으로 늘어난 낙사 위험에 즉시 대응 가능한 상태가 됨
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-01-SUMMARY.md`
</output>
