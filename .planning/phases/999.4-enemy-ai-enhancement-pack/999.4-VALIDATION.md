---
phase: 999.4
slug: enemy-ai-enhancement-pack
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-10
---

# Phase 999.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — `com.unity.test-framework` 1.6.0 is installed but no EditMode/PlayMode test assembly or `[Test]`/`[UnityTest]` files exist anywhere in `Assets/` (consistent with Phase 999.2's finding — unchanged). |
| **Config file** | none |
| **Quick run command** | n/a — no automated harness |
| **Full suite command** | n/a |

This project has never introduced automated Unity tests. Phases 13, 14, and 999.2 were all verified via manual playtest checklists. This phase continues that pattern.

---

## Sampling Rate

- **After every task commit:** n/a — no automated quick-run exists
- **After every plan wave:** manual playtest checklist covering the decision table below, exercised per sub-feature (fall handling, telegraph, kiting) since they're independent
- **Before `/gsd:verify-work`:** full manual checklist pass across all 10 decisions
- **Max feedback latency:** n/a (manual-only phase)

---

## Per-Task Verification Map

No automated per-task commands exist for this phase (manual-only, see below). Task-level acceptance criteria (grep/file-existence checks on code structure) are defined per-task in PLAN.md files; end-to-end behavior is verified via the Manual-Only Verifications table below.

**Mandatory pre-implementation check (from RESEARCH.md Open Question 1):** Before implementing D-02/D-03's FallZoneTrigger extension, verify in Unity Editor (Project Settings → Physics 2D → Layer Collision Matrix) that Enemy(layer 10) × Default(layer 0) collision is enabled — Unity MCP could not confirm this programmatically this session (connection not approved). If disabled, `OnTriggerEnter2D` will silently never fire for enemies with no error logged.

---

## Wave 0 Requirements

None — no test framework exists project-wide, and continuing the manual-playtest pattern is the correct, established approach rather than a gap to fill.

*Existing infrastructure covers all phase requirements (manual playtest, per project precedent).*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|--------------------|
| Melee enemy no longer falls off edges during normal chase across gap rooms | D-01 | Physics/timing-driven Unity gameplay behavior | Chase a melee enemy across `Room_Gap` (3-unit gap) and `Room_Fall` (5.5-unit gap) repeatedly; confirm it either successfully jumps every gap or refuses to walk into an unclearable one, never falls unintentionally. |
| Fallen enemy (either type) is destroyed immediately, no VFX, off-screen | D-02/D-03 | Requires triggering an actual fall event in a live scene | Deliberately lure/force an enemy off a ledge (melee: find or force an edge case; ranged: use new kiting to walk it backward off an edge) and confirm it vanishes with no particle/sound and does not linger as a targetable "zombie". |
| Enemy↔Default Physics2D layer collision is enabled (pre-implementation check) | D-02/D-03 | Requires Unity Editor Project Settings inspection, not code-verifiable this session | Open Project Settings → Physics 2D → Layer Collision Matrix, confirm Enemy×Default checkbox is checked before/while implementing FallZoneTrigger extension. |
| Melee Telegraph duration ~0.4-0.5s, enemy keeps moving (doesn't freeze) during "!" window | D-04/D-05 | Visual/timing feel, core to game feel per PROJECT.md Core Value | Trigger melee attack telegraph, visually confirm reduced duration and continued movement/animation; confirm player can still dodge-roll to avoid the follow-up attack (shortened telegraph must still leave a real dodge window, per original Phase 3 D-06 rationale). |
| Ranged enemy's own aim telegraph (LineRenderer, 0.8s) is unchanged | D-06 | Visual regression check | Compare ranged enemy aim/fire behavior before/after this phase's changes — should be identical. |
| Ranged enemy retreats when player closes within ~50% of aimLineLength, aims/fires while retreating, stops drifting once out of range | D-07/D-08/D-09/D-10 | Physics/timing-driven positioning behavior | Approach a ranged enemy from various distances; confirm retreat begins at roughly the expected threshold, confirm aim line and projectile fire continue normally during retreat, confirm retreat halts once outside threshold (no infinite backward drift). |

---

## Validation Sign-Off

- [x] All tasks have manual-only verification mapped (no automated framework exists project-wide)
- [x] Sampling continuity: n/a — manual-only phase, no automated task chain to break
- [x] Wave 0 covers all MISSING references — none required
- [x] No watch-mode flags — n/a
- [x] Feedback latency < N/A — manual playtest only
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
