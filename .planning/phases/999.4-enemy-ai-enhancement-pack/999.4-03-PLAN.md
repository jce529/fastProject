---
phase: 999.4-enemy-ai-enhancement-pack
plan: 03
type: execute
wave: 2
depends_on: ["999.4-01"]
files_modified:
  - Assets/Scripts/Enemy/MeleeEnemy.cs
autonomous: false
requirements: [D-04, D-05, D-06]

must_haves:
  truths:
    - "근접 적의 Telegraph 지속시간이 0.8초에서 0.4~0.5초 범위로 단축된다 (D-04)"
    - "Telegraph 중 적이 완전히 멈추지 않고 느려진 속도로 플레이어를 향해 계속 이동하며, 이동 방향에 맞게 스프라이트가 반전된다 (D-05)"
    - "Telegraph 종료 후 기존 Attack(윈드업+히트박스 활성화) 흐름은 이전과 동일하게 동작한다"
    - "원거리 적(RangedEnemy)의 TelegraphAndFire()는 이 Plan에서 전혀 수정되지 않는다 (D-06)"
  artifacts:
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "per-frame 이동형 Telegraph 루프 (RangedEnemy.TelegraphAndFire()의 while(elapsed<duration) 패턴을 이동에 적용)"
      contains: "telegraphDuration"
  key_links:
    - from: "MeleeEnemy.TelegraphAndAttack()"
      to: "MeleeEnemy.FlipSprite()"
      via: "루프 매 프레임 살아있는 플레이어 위치 기준 dirX 재계산 후 FlipSprite(dirX) 호출 — 기존엔 이 코루틴에서 호출되지 않았음"
      pattern: "FlipSprite\\(dirX\\)"
---

<objective>
근접 적(MeleeEnemy)의 공격 예고(Telegraph)를 "완전 정지 + 0.8초"에서 "느려진 속도로 이동하며 예고 + 0.4~0.5초"로 재작성한다. `RangedEnemy.TelegraphAndFire()`가 이미 사용 중인 `while (elapsed < duration) { ...; yield return null; }` per-frame 루프 형태를 그대로 차용해 `TelegraphAndAttack()`의 단일 블로킹 `WaitForSecondsRealtime(0.8f)`를 대체한다.

Purpose: "! 아이콘만 뜨고 가만히 서있는" 정적인 느낌을 제거해 PROJECT.md Core Value(손맛/속도감)를 강화하되, 원본 0.8초 Telegraph의 존재 이유(플레이어가 구르기로 회피 가능한 여유)는 단축 후에도 유지되어야 한다(Phase 3 D-06 원 근거).
Output: `MeleeEnemy.cs`(telegraphDuration/telegraphSpeedMultiplier 필드 + 재작성된 TelegraphAndAttack()).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md
@.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md
@Assets/Scripts/Enemy/MeleeEnemy.cs
@Assets/Scripts/Enemy/RangedEnemy.cs

<interfaces>
현재 `MeleeEnemy.TelegraphAndAttack()` 전체 (2026-07-10 확인, §180-218 — 999.4-01의 UpdateChase/GapAhead/FallZoneTrigger 변경과는 무관한 별도 메서드이므로 999.4-01 완료 후에도 이 부분은 그대로 유지되어 있음):

```csharp
private IEnumerator TelegraphAndAttack()
{
    _state = EnemyState.Telegraph;
    _animator?.SetBool("isMoving", false);
    _animator?.SetBool("isChasing", false);

    if (_exclamationIcon != null) _exclamationIcon.enabled = true;

    yield return new WaitForSecondsRealtime(0.8f); // <- D-04 단축 대상, D-05 이동 대상

    if (!IsAlive) yield break;

    if (_exclamationIcon != null) _exclamationIcon.enabled = false;
    _state = EnemyState.Attack;
    _animator?.SetTrigger("isAttacking");

    if (attackWindupDelay > 0f)
        yield return new WaitForSecondsRealtime(attackWindupDelay);

    if (!IsAlive) yield break;

    if (_meleeHitbox != null)
    {
        _meleeHitbox.enabled = true;
        yield return new WaitForSecondsRealtime(hitboxActiveDuration);
        if (_meleeHitbox != null) _meleeHitbox.enabled = false;
    }

    _state = IsPlayerInRange(detectionRadius) ? EnemyState.Chase : EnemyState.Idle;
    _attackCoroutine = null;
}
```

`RangedEnemy.TelegraphAndFire()`의 기존 per-frame 루프 형태 (§201-217, mirror 대상 — 이 Plan에서 `RangedEnemy.cs`는 읽기 전용 참고만, 수정하지 않음):
```csharp
float elapsed = 0f;
while (elapsed < TelegraphDuration)
{
    elapsed += Time.unscaledDeltaTime; // timeScale-immune
    float alpha = Mathf.Clamp01(elapsed / TelegraphDuration);
    // ... per-frame visual update ...
    yield return null;
}
```

`MeleeEnemy.UpdateChase()`의 기존 이동 계산 패턴 (999.4-01 적용 후에도 dirX 계산 자체는 동일, §176 부근):
```csharp
float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);
_rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y);
FlipSprite(dirX);
```

RESEARCH.md Open Question 3: `isMoving=false`가 Animator Controller에서 실제로 무엇을 구동하는지 그래프를 열어보지 않아 미확인 — `isMoving=true`로 바꿔도 안전한지는 이 Plan의 Task 2 플레이테스트에서 시각 확인이 필요하다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TelegraphAndAttack() per-frame 이동형 재작성 (D-04, D-05, D-06)</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/MeleeEnemy.cs (현재 전체 — 999.4-01 완료 후 상태, TelegraphAndAttack은 위 interfaces 블록과 동일해야 함)
    - Assets/Scripts/Enemy/RangedEnemy.cs 의 TelegraphAndFire()(§187-242) — per-frame 루프 형태를 그대로 mirror
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md 의 "Melee Telegraph Rework (D-04/D-05/D-06)" 절
  </read_first>
  <action>
    **(A) 필드 추가** — `hitboxActiveDuration`/`attackWindupDelay` 필드(§18-19) 근처에 추가:
    ```csharp
    [SerializeField] private float telegraphDuration       = 0.45f; // D-04: 0.8s→0.4~0.5s 단축 (원본 근거: Phase 3 D-06 "구르기로 회피 가능한 여유" 유지)
    [SerializeField] private float telegraphSpeedMultiplier = 0.4f;  // D-05: chaseSpeed 대비 이동 속도 배율 (Claude's Discretion: 30~50% 범위 중 40%)
    ```

    **(B) `TelegraphAndAttack()`(§180-218) 전체를 아래로 교체**:
    ```csharp
    private IEnumerator TelegraphAndAttack()
    {
        _state = EnemyState.Telegraph;
        _animator?.SetBool("isMoving", true);   // D-05: 이동을 계속하므로 walk-cycle 애니메이션 유지 (기존 false에서 변경 — 미확인 시 Task 2에서 시각 검증)
        _animator?.SetBool("isChasing", false);

        if (_exclamationIcon != null) _exclamationIcon.enabled = true;

        // D-04/D-05: RangedEnemy.TelegraphAndFire()(§201-217)의 per-frame 루프 형태를 mirror —
        // 단일 블로킹 WaitForSecondsRealtime(0.8f) 대신 매 프레임 이동 + FlipSprite 갱신.
        float elapsed = 0f;
        while (elapsed < telegraphDuration)
        {
            elapsed += Time.unscaledDeltaTime; // timeScale-immune (ROADMAP Stack Constraint)

            if (!IsAlive) yield break; // 가드: 루프 도중 언제든 처치될 수 있음 (기존엔 대기 종료 후 1회만 체크)

            if (_playerTransform != null)
            {
                float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);
                _rb.linearVelocity = new Vector2(dirX * chaseSpeed * telegraphSpeedMultiplier, _rb.linearVelocity.y);
                FlipSprite(dirX); // D-05: 기존 TelegraphAndAttack()은 FlipSprite를 호출하지 않았음 — 이동 방향과 스프라이트 일치를 위해 추가
            }

            yield return null;
        }

        if (!IsAlive) yield break;

        // Telegraph 종료 — Attack 상태로 전환하며 이동 정지 (기존 Attack 단계는 정지 상태 유지, D-04/D-05는 Telegraph만 대상)
        _rb.linearVelocity = new Vector2(0f, _rb.linearVelocity.y);

        if (_exclamationIcon != null) _exclamationIcon.enabled = false;
        _state = EnemyState.Attack;
        _animator?.SetTrigger("isAttacking");

        if (attackWindupDelay > 0f)
            yield return new WaitForSecondsRealtime(attackWindupDelay);

        if (!IsAlive) yield break;

        if (_meleeHitbox != null)
        {
            _meleeHitbox.enabled = true;
            yield return new WaitForSecondsRealtime(hitboxActiveDuration);
            if (_meleeHitbox != null) _meleeHitbox.enabled = false;
        }

        _state = IsPlayerInRange(detectionRadius) ? EnemyState.Chase : EnemyState.Idle;
        _attackCoroutine = null;
    }
    ```

    Attack 단계(윈드업/히트박스 활성화) 이후의 로직은 한 글자도 바꾸지 않는다 — D-04/D-05는 Telegraph 단계에만 적용된다. `RangedEnemy.cs`는 이 태스크에서 전혀 수정하지 않는다(D-06).
  </action>
  <verify>
    <automated>grep -n "telegraphDuration" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -n "telegraphSpeedMultiplier" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -n "FlipSprite(dirX)" Assets/Scripts/Enemy/MeleeEnemy.cs && grep -c "yield return null" Assets/Scripts/Enemy/MeleeEnemy.cs</automated>
  </verify>
  <acceptance_criteria>
    - `MeleeEnemy.cs`에 `telegraphDuration`(기본값 0.45f)과 `telegraphSpeedMultiplier`(기본값 0.4f) `[SerializeField]` 필드가 존재한다
    - `TelegraphAndAttack()`이 `while (elapsed < telegraphDuration)` per-frame 루프를 포함하고, 기존 `yield return new WaitForSecondsRealtime(0.8f)` 블로킹 대기는 제거되어 있다
    - 루프 내부에서 `FlipSprite(dirX)`가 호출된다 (기존엔 이 코루틴에서 호출되지 않았음)
    - 루프 내부 시간 누적이 `Time.unscaledDeltaTime`을 사용한다 (timeScale-immune, ROADMAP Stack Constraint)
    - Attack 단계(윈드업/히트박스) 이후 로직(`attackWindupDelay`/`hitboxActiveDuration`/`_state` 복귀)은 변경되지 않는다 (surgical change)
    - `Assets/Scripts/Enemy/RangedEnemy.cs`는 이 태스크에서 전혀 수정되지 않는다 (D-06 — grep diff 0)
  </acceptance_criteria>
  <done>MeleeEnemy의 Telegraph가 0.4~0.5초로 단축되고, 정지 대신 느려진 속도로 플레이어를 향해 이동하며 스프라이트가 방향에 맞게 반전된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 — 근접 Telegraph D-04/D-05 + 애니메이터/회피 가능성 + D-06 회귀 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-CONTEXT.md §Implementation Decisions (D-04, D-05, D-06)
    - .planning/phases/999.4-enemy-ai-enhancement-pack/999.4-RESEARCH.md 의 "Open Questions" §3 (isMoving 애니메이터 그래프 미확인)
  </read_first>
  <action>
    Telegraph 타이밍/이동 체감/애니메이터 블렌딩은 시각·타이밍 기반 실시간 플레이 동작이라 자동화 불가 — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. `isMoving=true`로 변경한 것이 애니메이터에서 이상 동작(원치 않는 블렌드/전환)을 일으키면, 필요 시 별도 애니메이터 불리언 도입 여부를 사용자와 논의한다(이 Plan 범위를 벗어나면 별도 후속 작업으로 분리).
  </action>
  <what-built>MeleeEnemy.TelegraphAndAttack()을 RangedEnemy.TelegraphAndFire()와 동일한 per-frame 루프 형태로 재작성 — Telegraph 지속시간 0.8s→0.45s 단축(D-04), 루프 중 chaseSpeed*0.4 속도로 플레이어 추적 이동 + FlipSprite 반전(D-05). RangedEnemy.TelegraphAndFire()는 미변경(D-06).</what-built>
  <how-to-verify>
    에디터 Play 후 체크리스트:

    **[D-04 — 단축된 지속시간]**
    1. 근접 적의 공격 범위 안으로 접근해 Telegraph("!" 아이콘)를 트리거한다 → 이전(0.8초)보다 눈에 띄게 짧게(약 0.45초) 느껴지는지 확인

    **[D-05 — 이동하며 예고]**
    2. Telegraph 중 적이 완전히 멈추지 않고 플레이어를 향해 계속 이동하는지 확인
    3. 이동 방향이 바뀌면(플레이어가 좌우로 움직이면) 스프라이트가 그에 맞게 좌우 반전되는지 확인
    4. Telegraph 종료 후 Attack(윈드업+히트박스) 단계에서는 기존처럼 이동이 멈추는지 확인 (Telegraph만 이동, Attack은 정지 — 의도된 동작)

    **[애니메이터 확인 — RESEARCH.md Open Question 3]**
    5. Telegraph 중 walk-cycle 애니메이션이 자연스럽게 재생되는지, `isMoving=true`로 바뀐 것 때문에 원치 않는 애니메이션 전환/끊김이 발생하지 않는지 확인

    **[회피 가능성 — Phase 3 D-06 원 근거 유지]**
    6. 단축된 Telegraph 상태에서도 플레이어가 구르기(Roll)로 회피 가능한 최소한의 여유가 있는지 확인 — "전혀 반응할 수 없이 맞는다"는 느낌이면 실패로 보고

    **[D-06 — 원거리 적 회귀 없음]**
    7. 원거리 적의 조준 텔레그래프(LineRenderer, 0.8초)가 이 Plan 적용 전과 동일하게 동작하는지 확인 (이 Plan은 RangedEnemy.cs를 수정하지 않았으므로 회귀 없어야 정상)
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — Telegraph 타이밍/이동 체감/애니메이터 블렌딩은 실시간 플레이 관찰만 가능 (999.4-VALIDATION.md, 자동화 테스트 프레임워크 없음). 통과 기준: 체크리스트 1~7 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~7번 전부 통과 보고 수신
    - 5번(애니메이터)에서 이상 동작이 보고되면 구체적 증상(어떤 전환/끊김)이 기록된다
    - 6번(회피 가능성)에서 실패가 보고되면 telegraphDuration 재조정 필요 여부가 함께 기록된다
  </acceptance_criteria>
  <done>D-04/D-05가 실제 플레이로 검증되고, D-06(원거리 적 미변경)이 회귀 없이 유지됨이 확인됨 — 단축된 Telegraph도 최소한의 회피 여유를 유지한다.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "6번: 너무 빨라서 구르기로 피할 수 없음")</resume-signal>
</task>

</tasks>

<verification>
- `MeleeEnemy.cs` 컴파일 성공 (콘솔 에러 0건)
- `RangedEnemy.cs`는 이 Plan에서 전혀 수정되지 않음 (grep diff 0)
- Telegraph 단축(0.4~0.5초)/이동/스프라이트 반전/회피 가능성 (플레이테스트 확인)
</verification>

<success_criteria>
- D-04/D-05/D-06이 실제 플레이로 검증됨
- 999.4-01(낙사 처리)이 완료된 이후에 동일 파일(MeleeEnemy.cs)에 적용되어 파일 충돌 없이 순차 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-enemy-ai-enhancement-pack/999.4-03-SUMMARY.md`
</output>
