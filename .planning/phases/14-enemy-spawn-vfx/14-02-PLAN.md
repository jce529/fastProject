---
phase: 14-enemy-spawn-vfx
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - Assets/Scripts/World/EnemySpawner.cs
  - Assets/Editor/CorridorEnemySpawnerTool.cs
autonomous: true
requirements: [SPWN-01, SPWN-02]

must_haves:
  truths:
    - "EnemySpawner.Activate()는 HasActivated가 이미 true인 상태에서 재호출되면 아무 것도 하지 않는다 (D-02: 1회성 스폰)"
    - "EnemySpawner.Activate()는 _spawned.SetActive(true) 호출 이전에 ISpawnGatable.SetSpawnGate(true)를 동기 호출한다 (Pitfall 1 방지)"
    - "EnemySpawner.Activate()가 AddComponent<EnemySpawnEffect>() 후 StartCoroutine(PlaySpawnSequence(...))를 호출한다 — EnemyDeathEffect와 동일한 컨벤션"
    - "Corridor_Flat/Up/Down 3종 프리팹의 기존 EnemySpawn_0 마커에 EnemySpawner(Melee) 컴포넌트를 부착하는 에디터 도구가 코드로 존재한다 (D-03) — 실제 실행/프리팹 저장은 14-04에서 수행"
  artifacts:
    - path: "Assets/Scripts/World/EnemySpawner.cs"
      provides: "SPWN-01 2단계 Spawn/Activate 분리 + HasActivated 가드 + 스폰 VFX 위임"
      contains: "public bool HasActivated { get; private set; }"
    - path: "Assets/Editor/CorridorEnemySpawnerTool.cs"
      provides: "D-03 Corridor 마커 부착 도구 (RoomMarkerTool.cs와 동일 패턴)"
      contains: "MenuItem(\"Fast/Phase14/Add Corridor Enemy Spawners\")"
  key_links:
    - from: "Assets/Scripts/World/EnemySpawner.cs"
      to: "Assets/Scripts/Enemy/EnemySpawnEffect.cs"
      via: "AddComponent+StartCoroutine (EnemyDeathEffect 컨벤션 재사용)"
      pattern: "AddComponent<EnemySpawnEffect>\\(\\)"
    - from: "Assets/Scripts/World/EnemySpawner.cs"
      to: "Assets/Scripts/Enemy/ISpawnGatable.cs"
      via: "GetComponent<ISpawnGatable>() 후 SetActive(true) 이전 동기 호출"
      pattern: "GetComponent<ISpawnGatable>\\(\\)"
    - from: "Assets/Editor/CorridorEnemySpawnerTool.cs"
      to: "Assets/Prefabs/Corridors/{Corridor_Flat,Corridor_Up,Corridor_Down}/*.prefab"
      via: "PrefabUtility.LoadPrefabContents + AddComponent<EnemySpawner> + SaveAsPrefabAsset"
      pattern: "LoadPrefabContents"
---

<objective>
`EnemySpawner`를 D-01/D-02에 맞춰 2단계(Spawn=비활성 생성 / Activate=실제 진입 시 VFX 트리거)로 분리하고, 14-01에서 만든 `ISpawnGatable`/`EnemySpawnEffect`를 배선한다. 동시에 D-03(Corridor 스폰 동등성)을 위해 3종 Corridor 프리팹에 `EnemySpawner` 마커를 부착하는 에디터 도구를 작성한다 (RESEARCH.md Open Question 1 — 이번 Phase 범위로 확정, 코드만 작성하고 실행은 14-04에서 수행).

Purpose: EnemySpawner.Activate()가 스폰 VFX 트리거의 유일한 지점이 되도록 하고(STATE.md 제약), Corridor도 Room과 동일한 마커 기반 스폰 대상이 되도록 한다.
Output: EnemySpawner.cs(2단계 분리 완료), CorridorEnemySpawnerTool.cs(신규 에디터 도구, 미실행 상태).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-enemy-spawn-vfx/14-CONTEXT.md
@.planning/phases/14-enemy-spawn-vfx/14-RESEARCH.md
@Assets/Scripts/World/EnemySpawner.cs
@Assets/Scripts/World/DebugRoomTeleporter.cs
@Assets/Editor/RoomMarkerTool.cs
@Assets/Editor/CorridorBuilder.cs

<interfaces>
<!-- 14-01 산출물 — 탐색 불필요 -->

From Assets/Scripts/Enemy/ISpawnGatable.cs (14-01 산출물):
```csharp
public interface ISpawnGatable
{
    void SetSpawnGate(bool isSpawning);
}
```

From Assets/Scripts/Enemy/EnemySpawnEffect.cs (14-01 산출물):
```csharp
public class EnemySpawnEffect : MonoBehaviour
{
    public IEnumerator PlaySpawnSequence(GameObject portalEffectPrefab, ISpawnGatable gate);
}
```

현재 `Assets/Scripts/World/EnemySpawner.cs` 전체 (2026-07-10 확인, 변경 대상):
```csharp
public class EnemySpawner : MonoBehaviour
{
    public enum EnemyType { Melee, Ranged }
    [SerializeField] private EnemyType _type = EnemyType.Melee;
    public EnemyType Type => _type;
    private GameObject _spawned;

    public void Spawn(GameObject meleePrefab, GameObject rangedPrefab)
    {
        GameObject prefab = _type == EnemyType.Melee ? meleePrefab : rangedPrefab;
        if (prefab == null) return;
        _spawned = Instantiate(prefab, transform.position, Quaternion.identity, transform);
        _spawned.SetActive(false);
    }

    public void Activate()
    {
        if (_spawned != null)
            _spawned.SetActive(true);
    }
}
```

현재 `Assets/Scripts/World/DebugRoomTeleporter.cs`의 호출부 (line 86-90, 변경 불필요 — 시그니처 호환성 확인용):
```csharp
foreach (EnemySpawner spawner in s_lastDebugRoom.GetComponentsInChildren<EnemySpawner>(true))
{
    spawner.Spawn(_meleePrefab, _rangedPrefab);
    if (activateEnemies) spawner.Activate();
}
```
이 호출부는 `Activate()`를 인자 없이 호출한다 — 새 시그니처가 기본값 `= null`을 가지면 이 파일은 수정할 필요가 없다 (정밀 변경 원칙, files_modified에 포함하지 않음).

`Assets/Editor/RoomMarkerTool.cs` 참고 패턴 (멱등적 PrefabUtility 편집 — Corridor 도구가 그대로 모방):
```csharp
var root = PrefabUtility.LoadPrefabContents(path);
var child = root.transform.Find(childPath);
if (child.GetComponent<RoomConnector>() != null) return; // idempotent
var rc = child.gameObject.AddComponent<RoomConnector>();
PrefabUtility.SaveAsPrefabAsset(root, path);
PrefabUtility.UnloadPrefabContents(root);
```

Corridor 프리팹 경로 (CorridorBuilder.cs 확인, 모두 자식 이름 "EnemySpawn_0" 보유 — EnemySpawner 컴포넌트는 없음, `grep -rl EnemySpawner Assets/Prefabs/Corridors/` 결과 0건):
- `Assets/Prefabs/Corridors/Corridor_Flat/Corridor_Flat.prefab`
- `Assets/Prefabs/Corridors/Corridor_Up/Corridor_Up.prefab`
- `Assets/Prefabs/Corridors/Corridor_Down/Corridor_Down.prefab`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EnemySpawner 2단계 분리 — Spawn/Activate + HasActivated + ISpawnGatable/EnemySpawnEffect 배선 (D-01, D-02, SPWN-01, SPWN-02)</name>
  <files>Assets/Scripts/World/EnemySpawner.cs</files>
  <read_first>
    - Assets/Scripts/World/EnemySpawner.cs (현재 전체 — 위 interfaces 블록과 동일)
    - Assets/Scripts/Enemy/ISpawnGatable.cs (14-01 산출물)
    - Assets/Scripts/Enemy/EnemySpawnEffect.cs (14-01 산출물 — PlaySpawnSequence 시그니처)
    - Assets/Scripts/Enemy/MeleeEnemy.cs (OnDashHit()의 AddComponent+StartCoroutine 컨벤션 — line 95-97)
  </read_first>
  <action>
    `Assets/Scripts/World/EnemySpawner.cs` 전체를 아래로 교체한다:

    ```csharp
    using UnityEngine;

    /// <summary>
    /// Room/Corridor 프리팹의 각 스폰 포인트에 부착하는 마커 컴포넌트.
    /// D-01/D-02: Spawn()은 사전 생성(비활성 인스턴스) 전용 — 화면 밖 미리 생성 시점에 호출된다.
    /// Activate()는 플레이어가 실제로 이 구간에 도달했을 때만 호출되며, 스폰 VFX 트리거의 유일한
    /// 지점이다(STATE.md 제약 — Awake/OnEnable에서 트리거 금지). HasActivated로 1회성 스폰을 보장한다.
    /// 룸이 Destroy()될 때 자식인 적도 함께 제거된다.
    /// </summary>
    public class EnemySpawner : MonoBehaviour
    {
        public enum EnemyType { Melee, Ranged }

        [SerializeField] private EnemyType _type = EnemyType.Melee;
        public EnemyType Type => _type;

        private GameObject _spawned;

        /// <summary>D-02: 이미 활성화된 마커는 재호출해도 안전한 no-op — 1회성 스폰.</summary>
        public bool HasActivated { get; private set; }

        public void Spawn(GameObject meleePrefab, GameObject rangedPrefab)
        {
            GameObject prefab = _type == EnemyType.Melee ? meleePrefab : rangedPrefab;
            if (prefab == null) return;

            _spawned = Instantiate(prefab, transform.position, Quaternion.identity, transform);
            _spawned.SetActive(false);
        }

        /// <summary>
        /// D-01: 플레이어가 실제로 이 구간에 진입했을 때만 호출한다. portalEffectPrefab이 null이면
        /// 포탈 VFX 없이 걸어나오기+게이팅만 재생한다(DebugRoomTeleporter 등 호환).
        /// SPWN-02: SetActive(true) 이전에 스폰 게이트를 닫아, 같은 프레임 다른 스크립트의 Update()가
        /// 이 적을 감지/타겟팅하지 못하도록 한다 (Pitfall 1).
        /// </summary>
        public void Activate(GameObject portalEffectPrefab = null)
        {
            if (HasActivated || _spawned == null) return;
            HasActivated = true;

            var gate = _spawned.GetComponent<ISpawnGatable>();
            gate?.SetSpawnGate(true);

            _spawned.SetActive(true);

            var effect = _spawned.GetComponent<EnemySpawnEffect>();
            if (effect == null) effect = _spawned.AddComponent<EnemySpawnEffect>();
            effect.StartCoroutine(effect.PlaySpawnSequence(portalEffectPrefab, gate));
        }
    }
    ```

    `Activate(GameObject portalEffectPrefab = null)`의 기본값 덕분에 `Assets/Scripts/World/DebugRoomTeleporter.cs`의 기존 `spawner.Activate();` 호출은 수정 없이 그대로 컴파일된다 (해당 파일은 이 태스크의 files_modified에 포함하지 않는다).
  </action>
  <verify>
    <automated>grep -n "HasActivated" Assets/Scripts/World/EnemySpawner.cs && grep -n "Activate(GameObject portalEffectPrefab = null)" Assets/Scripts/World/EnemySpawner.cs && grep -n "AddComponent<EnemySpawnEffect>" Assets/Scripts/World/EnemySpawner.cs</automated>
  </verify>
  <acceptance_criteria>
    - `EnemySpawner.cs`에 `public bool HasActivated { get; private set; }`가 존재한다
    - `Activate()` 본문 첫 줄이 `if (HasActivated || _spawned == null) return;`이다
    - `gate?.SetSpawnGate(true);`가 `_spawned.SetActive(true);`보다 앞줄에 위치한다
    - `Assets/Scripts/World/DebugRoomTeleporter.cs`의 git diff가 0줄이다 (기본 매개변수로 호환)
  </acceptance_criteria>
  <done>EnemySpawner가 Spawn(사전 생성)/Activate(실제 진입 시 VFX+게이팅 트리거)로 분리 완료 — D-01/D-02/SPWN-02 충족.</done>
</task>

<task type="auto">
  <name>Task 2: CorridorEnemySpawnerTool 에디터 도구 신설 — Corridor 3종 EnemySpawner 마커 부착 (D-03)</name>
  <files>Assets/Editor/CorridorEnemySpawnerTool.cs</files>
  <read_first>
    - Assets/Editor/RoomMarkerTool.cs (멱등적 PrefabUtility 편집 패턴 — 그대로 모방)
    - Assets/Editor/CorridorBuilder.cs (Corridor 3종이 "EnemySpawn_0"라는 동일한 이름의 자식 마커를 이미 보유함을 확인 — BuildCorridor_Flat/Up/Down의 CreateSpawnPoint 호출부)
  </read_first>
  <action>
    신규 파일 `Assets/Editor/CorridorEnemySpawnerTool.cs` 생성:

    ```csharp
    using UnityEditor;
    using UnityEngine;

    /// <summary>
    /// Menu: Fast/Phase14/Add Corridor Enemy Spawners
    /// D-03: Corridor 3종(Corridor_Flat/Up/Down)의 기존 "EnemySpawn_0" 마커 GameObject에
    /// EnemySpawner(Melee) 컴포넌트를 멱등적으로 부착한다. Room 프리팹과 달리 Corridor는
    /// 이 Phase 이전까지 EnemySpawner 마커가 전무했다 (14-RESEARCH.md Open Question 1 — 이번
    /// Phase 범위로 확정). RoomMarkerTool.cs와 동일한 LoadPrefabContents/SaveAsPrefabAsset 패턴.
    /// </summary>
    public static class CorridorEnemySpawnerTool
    {
        private static readonly string[] CorridorNames =
        {
            "Corridor_Flat",
            "Corridor_Up",
            "Corridor_Down",
        };

        [MenuItem("Fast/Phase14/Add Corridor Enemy Spawners")]
        public static void AddCorridorEnemySpawners()
        {
            foreach (var corridorName in CorridorNames)
            {
                string path = $"Assets/Prefabs/Corridors/{corridorName}/{corridorName}.prefab";

                var root = PrefabUtility.LoadPrefabContents(path);
                if (root == null)
                {
                    Debug.LogError($"[CorridorEnemySpawnerTool] Prefab not found: {path}");
                    continue;
                }

                var marker = root.transform.Find("EnemySpawn_0");
                if (marker == null)
                {
                    Debug.LogWarning($"[CorridorEnemySpawnerTool] 'EnemySpawn_0' not found in '{corridorName}'");
                    PrefabUtility.UnloadPrefabContents(root);
                    continue;
                }

                if (marker.GetComponent<EnemySpawner>() == null)
                {
                    marker.gameObject.AddComponent<EnemySpawner>();
                    // _type 기본값 EnemyType.Melee 그대로 사용 — Corridor는 마커 1개 = 근접 1마리 기준 (D-04)
                }

                PrefabUtility.SaveAsPrefabAsset(root, path);
                PrefabUtility.UnloadPrefabContents(root);

                Debug.Log($"[CorridorEnemySpawnerTool] EnemySpawner(Melee) applied to {corridorName}/EnemySpawn_0");
            }

            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();

            Debug.Log("[CorridorEnemySpawnerTool] Done. 3 corridors updated with EnemySpawner markers.");
        }
    }
    ```

    이 태스크는 코드만 작성한다 — 메뉴 실행(Unity 에디터 조작)은 14-04-PLAN.md의 checkpoint:human-action 태스크에서 수행한다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase14/Add Corridor Enemy Spawners\")" Assets/Editor/CorridorEnemySpawnerTool.cs && grep -c "Corridor_Flat\|Corridor_Up\|Corridor_Down" Assets/Editor/CorridorEnemySpawnerTool.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/CorridorEnemySpawnerTool.cs`가 존재하고 `[MenuItem("Fast/Phase14/Add Corridor Enemy Spawners")]`를 포함한다
    - `CorridorNames` 배열에 정확히 3개 항목("Corridor_Flat", "Corridor_Up", "Corridor_Down")이 존재한다
    - `marker.GetComponent<EnemySpawner>() == null` 멱등성 체크가 존재한다 (재실행 안전)
    - 아직 Unity 메뉴가 실행되지 않았으므로 `Assets/Prefabs/Corridors/*/*.prefab` 3개 파일의 git diff는 이 태스크 시점에 0줄이다
  </acceptance_criteria>
  <done>Corridor 3종에 EnemySpawner 마커를 부착하는 멱등적 에디터 도구가 코드로 존재 — 실행은 14-04에서 수행.</done>
</task>

</tasks>

<verification>
- `EnemySpawner.cs` 컴파일 성공, `HasActivated`/`Activate(GameObject = null)` 시그니처 확인
- `DebugRoomTeleporter.cs`는 diff 0 (기본 매개변수 호환)
- `CorridorEnemySpawnerTool.cs` 컴파일 성공 (Unity 메뉴에 아직 노출 확인만, 실행은 14-04)
</verification>

<success_criteria>
- EnemySpawner가 D-01(사전생성/실제진입 분리)+D-02(1회성)+SPWN-02(사전 게이팅)를 모두 만족
- Corridor 스폰 동등성(D-03)을 위한 도구가 준비되어 14-03(WorldGenerator 배선)과 14-04(실행+검증)가 곧바로 이어질 수 있음
</success_criteria>

<output>
After completion, create `.planning/phases/14-enemy-spawn-vfx/14-02-SUMMARY.md`
</output>
