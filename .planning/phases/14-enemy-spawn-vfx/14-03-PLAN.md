---
phase: 14-enemy-spawn-vfx
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements: [SPWN-01, SPWN-02]

must_haves:
  truths:
    - "TrySpawnEnemies()는 더 이상 Activate()를 호출하지 않는다 — Spawn()만 수행하고 필터링된 EnemySpawner 목록을 _pendingSpawns에 저장한다"
    - "TrySpawnEnemies()는 Room뿐 아니라 Corridor GameObject에도 동일하게 호출된다 (D-03) — SpawnNextPair()/SpawnPrevPair()에서 corridor Instantiate 직후"
    - "플레이어가 새 Room에 진입하면(UpdatePlayerIndex의 노드 전환) 해당 Room의 대기 중인 EnemySpawner들이 TryActivateSection()을 통해 D-05 스태거 간격으로 순차 Activate된다"
    - "플레이어가 Corridor 자체의 진입 커넥터 임계값을 넘는 순간 해당 Corridor의 EnemySpawner가 Activate된다 — 다음 Room 진입 시점이 아니다 (Pitfall 2 해소)"
    - "새 층 시작 Room(FloorTransitionSequence Step 4)의 EnemySpawner도 명시적으로 즉시 Activate된다 — 기존 no-op 주석이 실제 호출로 대체된다 (Pitfall 5/Open Question 3 해소). 이 Activate가 성립하려면 standbyRoom(=newRoom)에 TrySpawnExitPortal() 시점부터 등록돼 있던 _pendingSpawns 엔트리가 FloorTransitionSequence() 도중 블랭킷 Clear()로 지워지지 않고 그대로 보존되어야 한다"
    - "RemoveTail()/RemoveHead()로 정리되는 Room/Corridor의 _pendingSpawns/_activatedSections 엔트리가 함께 제거된다 (메모리 정리). FloorTransitionSequence()의 옛 체인 파괴 loop도 동일하게 개별 엔트리만 제거하며, _chain에 속한 적 없는 standbyRoom/newRoom의 엔트리는 건드리지 않는다"
    - "이미 활성화된 section(_activatedSections에 등록됨)은 재진입해도 TryActivateSection이 다시 트리거하지 않는다 (D-02)"
  artifacts:
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "SPWN-01/SPWN-02 런타임 진입 감지 + 스태거 활성화 + Corridor 지원"
      contains: "private void TryActivateSection"
  key_links:
    - from: "Assets/Scripts/World/WorldGenerator.cs (UpdatePlayerIndex)"
      to: "Assets/Scripts/World/WorldGenerator.cs (TryActivateSection)"
      via: "Room 노드 전환 직후 동기 호출 (양방향)"
      pattern: "TryActivateSection\\(_playerCurrentNode\\.Value\\.room\\)"
    - from: "Assets/Scripts/World/WorldGenerator.cs (CheckCorridorEntry)"
      to: "Assets/Scripts/World/WorldGenerator.cs (TryActivateSection)"
      via: "Corridor 자체 Left/Right 커넥터 임계값 통과 시 호출 (양방향)"
      pattern: "TryActivateSection\\(nextCorridor\\)|TryActivateSection\\(currentCorridor\\)"
    - from: "Assets/Scripts/World/WorldGenerator.cs (ActivateStaggered)"
      to: "Assets/Scripts/World/EnemySpawner.cs (Activate)"
      via: "WaitForSecondsRealtime 스태거 간격으로 순차 호출"
      pattern: "spawner\\.Activate\\(_portalEffectPrefab\\)"
    - from: "Assets/Scripts/World/WorldGenerator.cs (FloorTransitionSequence 옛 체인 파괴 foreach)"
      to: "Assets/Scripts/World/WorldGenerator.cs (TryActivateSection(newRoom))"
      via: "foreach 루프는 _chain(옛 체인) 엔트리만 개별 제거 — standbyRoom(=newRoom)의 _pendingSpawns 등록은 보존되어 Step 4에서 소비된다"
      pattern: "_pendingSpawns\\.Remove\\(chainRoom\\)"
---

<objective>
`WorldGenerator`를 D-01(사전생성/실제진입 분리) 아키텍처에 맞춰 배선한다: `TrySpawnEnemies()`를 수집 전용으로 바꾸고, Room 진입(기존 `_playerCurrentIndex` 노드 전환)과 Corridor 진입(신규 임계값 체크, Pattern 3)에서 각각 대기 중인 `EnemySpawner`를 스태거 간격으로 `Activate()`한다. 새 층 시작 Room과 시작 Room도 명시적으로 즉시 활성화한다 (Pitfall 5/Open Question 3 해소).

Purpose: 14-01/14-02가 만든 계약(ISpawnGatable, EnemySpawnEffect, EnemySpawner 2단계 분리)을 실제 플레이 타이밍에 정확히 연결하는 이 Phase의 핵심 배선.
Output: WorldGenerator.cs 단일 파일의 구조적 변경 (신규 필드/메서드 추가 + 4개 호출부 수정 + 2개 정리 지점 수정).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-enemy-spawn-vfx/14-CONTEXT.md
@.planning/phases/14-enemy-spawn-vfx/14-RESEARCH.md
@Assets/Scripts/World/WorldGenerator.cs
@Assets/Scripts/World/EnemySpawner.cs
@Assets/Scripts/World/RoomConnector.cs

<interfaces>
<!-- 14-02 산출물 — 탐색 불필요 -->

From Assets/Scripts/World/EnemySpawner.cs (14-02 산출물):
```csharp
public class EnemySpawner : MonoBehaviour
{
    public enum EnemyType { Melee, Ranged }
    public EnemyType Type { get; }
    public bool HasActivated { get; }
    public void Spawn(GameObject meleePrefab, GameObject rangedPrefab);
    public void Activate(GameObject portalEffectPrefab = null); // D-01/D-02/SPWN-02 전부 내부 처리
}
```

From Assets/Scripts/World/RoomConnector.cs:
```csharp
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    public Direction direction;
}
```

현재 `Assets/Scripts/World/WorldGenerator.cs` 관련 부분 (2026-07-10 확인, 정확한 line 번호는 참고용 — 패턴으로 위치 확인):

필드 선언부 (line 44-64):
```csharp
[Header("Transition Animation (Phase 12, D-01~D-04)")]
[SerializeField] private GameObject _portalEffectPrefab;

// Runtime state
private LinkedList<(GameObject room, GameObject corridor)> _chain = new LinkedList<(GameObject, GameObject)>();
private float _currentYDrift;
private int _playerCurrentIndex;
private LinkedListNode<(GameObject room, GameObject corridor)> _playerCurrentNode;
private int _activeExitCount;
private FloorTransitionEffect _transitionEffect;
```

`Start()`의 적 스폰 호출 (line 104):
```csharp
TrySpawnEnemies(startRoom, FloorManager.CurrentFloor); // DIFF-01: 시작 룸 적 스폰
```

`SpawnNextPair()` 전체 (line 135-161):
```csharp
private void SpawnNextPair()
{
    var tailRoom = _chain.Last.Value.room;
    var tailExit = FindConnector(tailRoom, RoomConnector.Direction.Right);
    Vector3 headExitPos = tailExit != null ? tailExit.transform.position : tailRoom.transform.position;

    var corridorPrefab = SelectCorridor();
    var corridor = Instantiate(corridorPrefab, Vector3.zero, Quaternion.identity);
    AlignByEntry(corridor, headExitPos);

    var corridorExit = FindConnector(corridor, RoomConnector.Direction.Right);
    var roomEntryPos = corridorExit != null ? corridorExit.transform.position : headExitPos;

    var roomPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
    var room = Instantiate(roomPrefab, Vector3.zero, Quaternion.identity);
    AlignByEntry(room, roomEntryPos);
    TrySpawnExitPortal(room);
    TrySpawnEnemies(room, FloorManager.CurrentFloor); // DIFF-01

    _chain.AddLast((room, corridor));
}
```

`SpawnPrevPair()` 전체 (line 170-201, 좌우 대칭):
```csharp
private void SpawnPrevPair()
{
    var headRoom = _chain.First.Value.room;
    var headEntry = FindConnector(headRoom, RoomConnector.Direction.Left);
    Vector3 tailEntryPos = headEntry != null ? headEntry.transform.position : headRoom.transform.position;

    var corridorPrefab = SelectCorridor();
    var corridor = Instantiate(corridorPrefab, Vector3.zero, Quaternion.identity);
    AlignByExit(corridor, tailEntryPos);

    var corridorEntry = FindConnector(corridor, RoomConnector.Direction.Left);
    var roomExitPos = corridorEntry != null ? corridorEntry.transform.position : tailEntryPos;

    var roomPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
    var room = Instantiate(roomPrefab, Vector3.zero, Quaternion.identity);
    AlignByExit(room, roomExitPos);
    TrySpawnExitPortal(room);
    TrySpawnEnemies(room, FloorManager.CurrentFloor); // DIFF-01

    var oldFrontNode = _chain.First;
    var (oldFrontRoom, _) = oldFrontNode.Value;
    oldFrontNode.Value = (oldFrontRoom, corridor);

    _chain.AddFirst((room, null));
}
```

`RemoveTail()`/`RemoveHead()` 전체 (line 203-242):
```csharp
private void RemoveTail()
{
    var (room, corridor) = _chain.First.Value;
    ExitPortal portal = room.GetComponentInChildren<ExitPortal>(true);
    if (portal != null && portal.StandbyRoom != null)
    {
        Destroy(portal.StandbyRoom);
        _activeExitCount--;
    }
    if (corridor != null) Destroy(corridor);
    Destroy(room);
    _chain.RemoveFirst();
}

private void RemoveHead()
{
    var (room, corridor) = _chain.Last.Value;
    ExitPortal portal = room.GetComponentInChildren<ExitPortal>(true);
    if (portal != null && portal.StandbyRoom != null)
    {
        Destroy(portal.StandbyRoom);
        _activeExitCount--;
    }
    if (corridor != null) Destroy(corridor);
    Destroy(room);
    _chain.RemoveLast();
}
```

`TrySpawnExitPortal()`의 대기룸 스폰 호출 (line 356-364):
```csharp
var standbyPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
var standbyPos = new Vector3(0f, room.transform.position.y + _floorHeight, 0f);
var standbyRoom = Instantiate(standbyPrefab, standbyPos, Quaternion.identity);
TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1); // DIFF-01: 대기룸은 활성화될 미래 층 기준 난이도
standbyRoom.SetActive(false);
portal.StandbyRoom = standbyRoom;
```

`GetEnemyCount()` + 현재 `TrySpawnEnemies()` 전체 (line 372-411, 교체 대상):
```csharp
private (int melee, int ranged) GetEnemyCount(int floor)
{
    if (floor <= 5)   return (Random.Range(2, 4), Random.Range(0, 2));
    if (floor <= 10)  return (2, Random.Range(1, 3));
    return (2, Random.Range(2, 4));
}

private void TrySpawnEnemies(GameObject room, int floor)
{
    (int meleeCount, int rangedCount) = GetEnemyCount(floor);
    int meleeSpawned = 0;
    int rangedSpawned = 0;

    foreach (EnemySpawner spawner in room.GetComponentsInChildren<EnemySpawner>(true))
    {
        if (spawner.Type == EnemySpawner.EnemyType.Melee && meleeSpawned < meleeCount)
        {
            spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
            spawner.Activate();
            meleeSpawned++;
        }
        else if (spawner.Type == EnemySpawner.EnemyType.Ranged && rangedSpawned < rangedCount)
        {
            spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
            spawner.Activate();
            rangedSpawned++;
        }
    }
}
```

`Update()` 전체 (line 413-456):
```csharp
private void Update()
{
    FloorTimer.Tick();
    if (_playerTransform == null || _chain.Count == 0) return;
    UpdatePlayerIndex();

    bool chainChanged = false;
    while (_chain.Count - 1 - _playerCurrentIndex < _lookaheadCount) { SpawnNextPair(); chainChanged = true; }
    while (_playerCurrentIndex > _lookbehindCount) { RemoveTail(); _playerCurrentIndex--; chainChanged = true; }
    while (_playerCurrentIndex < _lookbehindCount) { SpawnPrevPair(); _playerCurrentIndex++; chainChanged = true; }
    while (_chain.Count - 1 - _playerCurrentIndex > _lookaheadCount) { RemoveHead(); chainChanged = true; }

    if (chainChanged) RecomputeCameraBounds();
}
```

`UpdatePlayerIndex()` 전체 (line 458-490):
```csharp
private void UpdatePlayerIndex()
{
    while (_playerCurrentNode.Next != null)
    {
        var nextEntryConnector = FindConnector(_playerCurrentNode.Next.Value.room, RoomConnector.Direction.Left);
        if (nextEntryConnector == null) break;
        if (_playerTransform.position.x > nextEntryConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Next;
            _playerCurrentIndex++;
        }
        else break;
    }

    while (_playerCurrentNode.Previous != null)
    {
        var prevExitConnector = FindConnector(_playerCurrentNode.Previous.Value.room, RoomConnector.Direction.Right);
        if (prevExitConnector == null) break;
        if (_playerTransform.position.x < prevExitConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Previous;
            _playerCurrentIndex--;
        }
        else break;
    }
}
```

`FloorTransitionSequence()`의 옛 체인 파괴 foreach + `_chain.Clear()` (line 518-534, 수정 대상 — **`_chain`에 속한 엔트리만 순회한다는 점이 이번 수정의 핵심 전제**):
```csharp
// D-07 — 기존 체인(현재 room+corridor 전부) 즉시 Destroy
// 리뷰 수정: 입장한 portal 외 다른 미사용 포탈의 대기룸도 함께 Destroy
// (RemoveTail()의 D-08 정리 패턴과 동일 — _maxExitsActive > 1일 때 고아 GameObject 누수 방지)
foreach (var (chainRoom, chainCorridor) in _chain)
{
    var orphanPortal = chainRoom.GetComponentInChildren<ExitPortal>(true);
    if (orphanPortal != null && orphanPortal != portal && orphanPortal.StandbyRoom != null)
    {
        Destroy(orphanPortal.StandbyRoom);
    }

    if (chainCorridor != null) Destroy(chainCorridor);
    Destroy(chainRoom);
}
_chain.Clear();
_activeExitCount = 0; // 옛 체인에 있던 모든 포탈(입장한 포탈 + 정리된 미사용 포탈)이 함께 사라짐
Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
```

`FloorTransitionSequence()`의 Step 4 no-op (line 536-560, 교체 대상):
```csharp
GameObject newRoom = portal.StandbyRoom;
newRoom.SetActive(true);
var newNode = _chain.AddLast((newRoom, null));
_playerCurrentIndex = 0;
_playerCurrentNode = newNode;
_currentYDrift = 0f;
FloorTimer.Reset();

// Step 2 — ExitSpawnPoint 텔레포트
var spawnPoints = newRoom.GetComponentsInChildren<ExitSpawnPoint>(true);
...
_playerTransform.position = teleportPos;

// Step 3 — 카메라 스냅
RecomputeCameraBounds();

yield return null; // Step 3.5

// Step 4 — 적 활성화: WorldGenerator는 현재 EnemySpawner.Spawn()을 호출하지 않으므로 의도적 no-op
// (Pitfall 5 — 적 스폰 배선은 EXIT-01/02/03 범위 밖. 이 단계는 구조적 자리만 유지한다.)

// EXIT (Phase 12 D-01 X1-X4)
if (_transitionEffect != null)
    yield return _transitionEffect.PlayExit(teleportPos, _portalEffectPrefab);
```

**중요:** `newRoom`(= `portal.StandbyRoom`)은 `_chain`에 속한 적이 없다 — `TrySpawnExitPortal()`에서 `Instantiate` 직후 `standbyRoom.SetActive(false)`로 비활성 보관될 뿐 `_chain.AddLast`/`AddFirst`로 체인에 등록된 적이 없기 때문이다. 하지만 그 시점에 이미 `TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1)`가 호출되어 `_pendingSpawns[standbyRoom]`에 등록이 끝나 있다. 위 foreach 루프는 `_chain`만 순회하므로 이 등록을 건드리지 않는다 — 따라서 이 등록을 지우는 유일한 방법은 (실수로) 블랭킷 `_pendingSpawns.Clear()`를 호출하는 것뿐이며, 그렇게 하면 안 된다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrySpawnEnemies 수집 전용화 + Corridor 수집 추가 + Room 진입 활성화 배선 (D-01, D-02, D-03, D-05)</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (위 interfaces 블록의 필드 선언부/Start()/SpawnNextPair()/SpawnPrevPair()/GetEnemyCount()+TrySpawnEnemies()/UpdatePlayerIndex() 전체)
    - Assets/Scripts/World/EnemySpawner.cs (Activate(GameObject portalEffectPrefab = null) 시그니처 — 14-02 산출물)
  </read_first>
  <action>
    **1. 필드 선언부** — `[Header("Transition Animation (Phase 12, D-01~D-04)")]` 블록 뒤에 신규 헤더 추가:
    ```csharp
    [Header("Enemy Spawn VFX (Phase 14, D-01~D-09)")]
    [SerializeField, Range(0.1f, 0.6f)] private float _spawnStaggerInterval = 0.3f; // D-05: 포탈 순차 배출 간격
    ```

    **2. Runtime state 블록**에 신규 필드 2개 추가 (`private FloorTransitionEffect _transitionEffect;` 바로 뒤):
    ```csharp
    private readonly HashSet<GameObject> _activatedSections = new HashSet<GameObject>();
    private readonly Dictionary<GameObject, List<EnemySpawner>> _pendingSpawns = new Dictionary<GameObject, List<EnemySpawner>>();
    ```
    (`System.Collections.Generic`은 이미 파일 상단에 `using`되어 있어 추가 using 불필요)

    **3. `GetEnemyCount()` 아래 `TrySpawnEnemies()`를 아래로 완전 교체** (기존 `spawner.Activate();` 호출 2곳을 제거하고 수집 리스트에 담는다):
    ```csharp
    /// <summary>
    /// D-01/D-03/D-04b: Room 또는 Corridor가 Instantiate된 직후 호출한다 (Room/Corridor 구분 없이
    /// 동일 처리 — D-03). section의 EnemySpawner 마커를 타입별로 필터링해 GetEnemyCount(floor)
    /// 카운트만큼 Spawn()(비활성 인스턴스 생성)만 수행하고, Activate() 호출은 TryActivateSection()으로
    /// 지연한다 (D-01: 화면 밖 사전 생성 시점에는 스폰 VFX를 재생하지 않는다).
    /// </summary>
    private void TrySpawnEnemies(GameObject section, int floor)
    {
        (int meleeCount, int rangedCount) = GetEnemyCount(floor);
        int meleeSpawned = 0;
        int rangedSpawned = 0;
        var pending = new List<EnemySpawner>();

        foreach (EnemySpawner spawner in section.GetComponentsInChildren<EnemySpawner>(true))
        {
            if (spawner.Type == EnemySpawner.EnemyType.Melee && meleeSpawned < meleeCount)
            {
                spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
                pending.Add(spawner);
                meleeSpawned++;
            }
            else if (spawner.Type == EnemySpawner.EnemyType.Ranged && rangedSpawned < rangedCount)
            {
                spawner.Spawn(_meleeEnemyPrefab, _rangedEnemyPrefab);
                pending.Add(spawner);
                rangedSpawned++;
            }
        }

        if (pending.Count > 0) _pendingSpawns[section] = pending;
    }

    /// <summary>
    /// D-01/D-02: 플레이어가 실제로 section(Room 또는 Corridor)에 진입했을 때 호출한다. 이미 트리거된
    /// section은 재진입해도 다시 재생하지 않는다(1회성 스폰). _pendingSpawns에 등록된 EnemySpawner
    /// 목록을 D-05 스태거 간격으로 순차 Activate()한다.
    /// </summary>
    private void TryActivateSection(GameObject section)
    {
        if (section == null || _activatedSections.Contains(section)) return;
        _activatedSections.Add(section);

        if (_pendingSpawns.TryGetValue(section, out var list))
        {
            _pendingSpawns.Remove(section);
            StartCoroutine(ActivateStaggered(list));
        }
    }

    /// <summary>D-05: "배열에 넣고 하나씩 배출" — 기존 DIFF-01 필터링 순서(마커 씬 계층 순회 순서)를
    /// 그대로 유지한 채, 스태거 간격만큼 실시간 대기 후 순차 Activate()한다.</summary>
    private IEnumerator ActivateStaggered(List<EnemySpawner> spawners)
    {
        foreach (var spawner in spawners)
        {
            if (spawner != null) spawner.Activate(_portalEffectPrefab);
            yield return new WaitForSecondsRealtime(_spawnStaggerInterval);
        }
    }
    ```

    **4. `Start()`** — `TrySpawnEnemies(startRoom, FloorManager.CurrentFloor); // DIFF-01: 시작 룸 적 스폰` 바로 다음 줄에 추가 (플레이어가 이미 이 룸 안에 있으므로 UpdatePlayerIndex의 노드-전환 경로를 거치지 않는다 — 즉시 활성화 필요):
    ```csharp
    TrySpawnEnemies(startRoom, FloorManager.CurrentFloor); // DIFF-01: 시작 룸 적 스폰
    TryActivateSection(startRoom); // D-01: 플레이어가 이미 위치한 시작 룸은 즉시 활성화
    ```

    **5. `SpawnNextPair()`** — corridor를 `AlignByEntry(corridor, headExitPos);` 직후 D-03 수집 대상에 추가:
    ```csharp
    var corridorPrefab = SelectCorridor();
    var corridor = Instantiate(corridorPrefab, Vector3.zero, Quaternion.identity);
    AlignByEntry(corridor, headExitPos);
    TrySpawnEnemies(corridor, FloorManager.CurrentFloor); // D-03: Corridor도 동일 처리

    var corridorExit = FindConnector(corridor, RoomConnector.Direction.Right);
    ```
    (room 쪽 `TrySpawnEnemies(room, FloorManager.CurrentFloor); // DIFF-01`는 그대로 유지 — 이미 수집 전용으로 바뀐 메서드를 그대로 호출)

    **6. `SpawnPrevPair()`** — 동일 패턴, `AlignByExit(corridor, tailEntryPos);` 직후:
    ```csharp
    var corridorPrefab = SelectCorridor();
    var corridor = Instantiate(corridorPrefab, Vector3.zero, Quaternion.identity);
    AlignByExit(corridor, tailEntryPos);
    TrySpawnEnemies(corridor, FloorManager.CurrentFloor); // D-03: Corridor도 동일 처리

    var corridorEntry = FindConnector(corridor, RoomConnector.Direction.Left);
    ```

    **7. `UpdatePlayerIndex()`** — 양방향 while 루프에 노드 전환 직후 활성화 호출 추가:
    ```csharp
    while (_playerCurrentNode.Next != null)
    {
        var nextEntryConnector = FindConnector(_playerCurrentNode.Next.Value.room, RoomConnector.Direction.Left);
        if (nextEntryConnector == null) break;
        if (_playerTransform.position.x > nextEntryConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Next;
            _playerCurrentIndex++;
            TryActivateSection(_playerCurrentNode.Value.room); // D-01: 새 Room 진입 시 활성화
        }
        else
            break;
    }

    while (_playerCurrentNode.Previous != null)
    {
        var prevExitConnector = FindConnector(_playerCurrentNode.Previous.Value.room, RoomConnector.Direction.Right);
        if (prevExitConnector == null) break;
        if (_playerTransform.position.x < prevExitConnector.transform.position.x)
        {
            _playerCurrentNode = _playerCurrentNode.Previous;
            _playerCurrentIndex--;
            TryActivateSection(_playerCurrentNode.Value.room); // D-01: 뒤로 이동해 Room 재진입 시에도 확인(이미 활성화됐으면 D-02로 스킵)
        }
        else
            break;
    }
    ```

    이 태스크에서는 Corridor 진입 감지(Pattern 3)와 FloorTransitionSequence Step 4 수정, RemoveTail/RemoveHead 정리는 다루지 않는다 — Task 2에서 처리한다.
  </action>
  <verify>
    <automated>grep -n "_activatedSections\|_pendingSpawns\|TryActivateSection\|ActivateStaggered" Assets/Scripts/World/WorldGenerator.cs | head -30 && grep -c "spawner.Activate();" Assets/Scripts/World/WorldGenerator.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spawner.Activate();"`가 0을 반환한다 (기존 즉시 Activate 호출 완전 제거)
    - `TrySpawnEnemies(corridor, FloorManager.CurrentFloor);`가 `SpawnNextPair()`와 `SpawnPrevPair()` 양쪽에 각 1회씩 존재한다 (D-03)
    - `TryActivateSection(startRoom);`이 `Start()`에 존재한다
    - `UpdatePlayerIndex()`의 forward/backward while 루프 양쪽에 `TryActivateSection(_playerCurrentNode.Value.room);` 호출이 존재한다
    - `ActivateStaggered`가 `WaitForSecondsRealtime(_spawnStaggerInterval)`를 사용한다 (timeScale 면역)
  </acceptance_criteria>
  <done>Room 진입 시점(시작 룸 포함)에 스폰이 정확히 트리거되고, Corridor도 동일한 수집 대상이 됨 — Corridor 진입 감지 자체는 Task 2에서 마무리.</done>
</task>

<task type="auto">
  <name>Task 2: Corridor 진입 감지(Pattern 3) + FloorTransitionSequence Step 4 수정 + RemoveTail/RemoveHead 정리 (D-01, D-03, Pitfall 2/5)</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (Task 1 적용 후 상태 — Update()/RemoveTail()/RemoveHead()/FloorTransitionSequence() 전체)
    - .planning/phases/14-enemy-spawn-vfx/14-RESEARCH.md §Pattern 3, §Pitfall 2, §Pitfall 5, §Open Question 3
  </read_first>
  <action>
    **1. 신규 메서드 `CheckCorridorEntry()` 추가** (`UpdatePlayerIndex()` 메서드 바로 뒤에 배치):
    ```csharp
    /// <summary>
    /// D-03/Pitfall 2: _playerCurrentIndex/UpdatePlayerIndex()는 Room 경계(다음 Room의 Left/ENT)
    /// 기준으로만 갱신되므로, Corridor 자체의 진입 시점(Room 경계보다 항상 먼저 통과하는 지점)을
    /// 감지하려면 별도의 임계값 체크가 필요하다 (Pattern 3). O(1) — 커넥터 조회 2회 + 좌표 비교뿐이라
    /// 매 프레임 호출해도 GC/성능 부담이 없다.
    /// </summary>
    private void CheckCorridorEntry()
    {
        // 전진: 다음 Room으로 이어지는 Corridor(현재 Room의 오른쪽 길)의 왼쪽(ENT) 커넥터를
        // 플레이어가 지났는지 확인 — Room 진입 판정(다음 Room의 ENT)보다 항상 먼저 통과하는 지점이다.
        if (_playerCurrentNode.Next != null)
        {
            var nextCorridor = _playerCurrentNode.Next.Value.corridor;
            if (nextCorridor != null && !_activatedSections.Contains(nextCorridor))
            {
                var corridorEntry = FindConnector(nextCorridor, RoomConnector.Direction.Left);
                if (corridorEntry != null && _playerTransform.position.x > corridorEntry.transform.position.x)
                    TryActivateSection(nextCorridor);
            }
        }

        // 후진: 현재 Room 자신의 왼쪽 Corridor(D-09: corridor = 이 room의 왼쪽 길)의 오른쪽(EXIT)
        // 커넥터를 플레이어가 아래로 지났는지 확인 — 좌우 대칭.
        var currentCorridor = _playerCurrentNode.Value.corridor;
        if (currentCorridor != null && !_activatedSections.Contains(currentCorridor))
        {
            var corridorExit = FindConnector(currentCorridor, RoomConnector.Direction.Right);
            if (corridorExit != null && _playerTransform.position.x < corridorExit.transform.position.x)
                TryActivateSection(currentCorridor);
        }
    }
    ```

    **2. `Update()`** — `UpdatePlayerIndex();` 바로 다음 줄에 호출 추가:
    ```csharp
    if (_playerTransform == null || _chain.Count == 0) return;
    UpdatePlayerIndex();
    CheckCorridorEntry(); // D-03/Pattern 3: Room보다 먼저 통과하는 Corridor 진입 임계값 체크
    ```

    **3. `RemoveTail()`** — `_chain.RemoveFirst();` 앞에 정리 코드 추가:
    ```csharp
    private void RemoveTail()
    {
        var (room, corridor) = _chain.First.Value;

        ExitPortal portal = room.GetComponentInChildren<ExitPortal>(true);
        if (portal != null && portal.StandbyRoom != null)
        {
            Destroy(portal.StandbyRoom);
            _activeExitCount--;
            Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
        }

        // Phase 14: 정리되는 section의 대기/활성 스폰 기록도 함께 제거 (참조 누수 방지)
        _activatedSections.Remove(room);
        _pendingSpawns.Remove(room);
        if (corridor != null)
        {
            _activatedSections.Remove(corridor);
            _pendingSpawns.Remove(corridor);
        }

        if (corridor != null) Destroy(corridor);
        Destroy(room);
        _chain.RemoveFirst();
    }
    ```

    **4. `RemoveHead()`** — 동일 패턴 (`_chain.RemoveLast();` 앞):
    ```csharp
    private void RemoveHead()
    {
        var (room, corridor) = _chain.Last.Value;

        ExitPortal portal = room.GetComponentInChildren<ExitPortal>(true);
        if (portal != null && portal.StandbyRoom != null)
        {
            Destroy(portal.StandbyRoom);
            _activeExitCount--;
            Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
        }

        _activatedSections.Remove(room);
        _pendingSpawns.Remove(room);
        if (corridor != null)
        {
            _activatedSections.Remove(corridor);
            _pendingSpawns.Remove(corridor);
        }

        if (corridor != null) Destroy(corridor);
        Destroy(room);
        _chain.RemoveLast();
    }
    ```

    **5. `FloorTransitionSequence()`** — 옛 체인 파괴 foreach 루프 안에서 `_activatedSections`/`_pendingSpawns`를
    엔트리별로 제거한다 (`RemoveTail()`/`RemoveHead()`와 동일 패턴). **주의 (이전 리비전에서 발견된 버그 수정):**
    `_chain.Clear()` 직후에 두 컬렉션을 통째로 `.Clear()`하면 안 된다 — `newRoom`(= `portal.StandbyRoom`)은
    `_chain`에 한 번도 속한 적이 없어 이 foreach가 순회하지 않는 대상이지만, `TrySpawnExitPortal()` 시점에
    이미 `_pendingSpawns[standbyRoom]`에 등록되어 있다. 블랭킷 `.Clear()`를 호출하면 그 등록이 아래 Step 4의
    `TryActivateSection(newRoom)`보다 먼저 지워져서 새 층 시작 Room의 적이 영원히 활성화되지 않는다
    (게다가 `TryActivateSection`은 조회 실패 여부와 무관하게 `_activatedSections.Add(section)`을 먼저
    실행하므로 재시도 경로도 없이 영구 고착된다).

    위 interfaces 블록에 인용된 기존 foreach 루프(주석 `// D-07 — 기존 체인(...) 즉시 Destroy` 바로 아래)를
    아래로 교체 (루프 안에 정리 코드 2줄만 추가, `_chain.Clear()` 뒤에는 `_activatedSections.Clear()`/
    `_pendingSpawns.Clear()`를 추가하지 않는다):
    ```csharp
    foreach (var (chainRoom, chainCorridor) in _chain)
    {
        var orphanPortal = chainRoom.GetComponentInChildren<ExitPortal>(true);
        if (orphanPortal != null && orphanPortal != portal && orphanPortal.StandbyRoom != null)
        {
            Destroy(orphanPortal.StandbyRoom);
        }

        // Phase 14: 정리되는 옛 체인 section의 대기/활성 스폰 기록만 개별 제거한다 (RemoveTail/RemoveHead와
        // 동일 패턴). standbyRoom(= 다음 Step의 newRoom)은 _chain에 속한 적이 없어 이 루프가 건드리지
        // 않으므로, 블랭킷 _activatedSections.Clear()/_pendingSpawns.Clear()는 절대 호출하지 않는다 —
        // 호출 시 standbyRoom의 사전 등록된 _pendingSpawns 엔트리까지 함께 지워져 Step 4의
        // TryActivateSection(newRoom)이 등록을 찾지 못해 새 층 시작 Room의 적이 영원히 비활성 상태로 남는다.
        _activatedSections.Remove(chainRoom);
        _pendingSpawns.Remove(chainRoom);
        if (chainCorridor != null)
        {
            _activatedSections.Remove(chainCorridor);
            _pendingSpawns.Remove(chainCorridor);
        }

        if (chainCorridor != null) Destroy(chainCorridor);
        Destroy(chainRoom);
    }
    _chain.Clear();
    _activeExitCount = 0; // 옛 체인에 있던 모든 포탈(입장한 포탈 + 정리된 미사용 포탈)이 함께 사라짐
    Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
    ```

    그리고 Step 4 부분을:
    ```csharp
    // Step 4 — 적 활성화: 새 층 시작 Room은 UpdatePlayerIndex()의 노드 전환 경로를 거치지 않으므로
    // (동일 프레임에 _playerCurrentNode를 직접 대입) 명시적으로 트리거한다 (D-01, Pitfall 5/Open Question 3 해소).
    TryActivateSection(newRoom);
    ```
    로 교체한다 (기존 "의도적 no-op" 주석 2줄을 삭제하고 위 코드로 대체).

    `newRoom`은 이미 이 메서드 앞부분에서 `GameObject newRoom = portal.StandbyRoom;`으로 선언되어 있으므로
    추가 변수 선언 불필요. `TrySpawnEnemies(standbyRoom, FloorManager.CurrentFloor + 1);`는
    `TrySpawnExitPortal()`에서 이미 호출되어 `_pendingSpawns[standbyRoom]`에 등록되어 있다 —
    `standbyRoom`이 곧 `newRoom`이며, 위 foreach 루프는 `_chain`(옛 체인)만 순회하고 `standbyRoom`은
    `_chain`에 속한 적이 없으므로 그 등록은 그대로 보존된다. 따라서 `TryActivateSection(newRoom)`이
    그 등록을 정확히 소비한다.
  </action>
  <verify>
    <automated>grep -n "CheckCorridorEntry" Assets/Scripts/World/WorldGenerator.cs && grep -n "TryActivateSection(newRoom)" Assets/Scripts/World/WorldGenerator.cs && grep -c "의도적 no-op" Assets/Scripts/World/WorldGenerator.cs && grep -c "_activatedSections.Clear()\|_pendingSpawns.Clear()" Assets/Scripts/World/WorldGenerator.cs</automated>
  </verify>
  <acceptance_criteria>
    - `CheckCorridorEntry()` 메서드가 존재하고 `Update()`에서 `UpdatePlayerIndex();` 다음 줄에 호출된다
    - `TryActivateSection(newRoom);`이 `FloorTransitionSequence()` 안에 존재하고, `"의도적 no-op"` 문자열의 grep 결과가 0건이다 (기존 플레이스홀더 주석 완전 대체)
    - `RemoveTail()`/`RemoveHead()` 양쪽에 `_activatedSections.Remove(room);`과 `_pendingSpawns.Remove(room);`이 존재한다
    - `FloorTransitionSequence()`의 옛 체인 파괴 `foreach (var (chainRoom, chainCorridor) in _chain)` 루프 안에 `_activatedSections.Remove(chainRoom);`과 `_pendingSpawns.Remove(chainRoom);`이 존재한다 (corridor 조건부 포함)
    - `_activatedSections.Clear();`/`_pendingSpawns.Clear();` 블랭킷 호출은 `WorldGenerator.cs` 전체 어디에도 존재하지 않는다 (grep -c 결과 0건) — standbyRoom(=newRoom)의 사전 등록된 `_pendingSpawns` 엔트리를 보존하기 위함이며, 이 조건이 깨지면 새 층 시작 Room의 적이 영원히 비활성화된다
  </acceptance_criteria>
  <done>Corridor 자체 진입 임계값이 Room 경계보다 먼저 감지되고, 새 층 시작 Room도 명시적으로 활성화되며, 정리되는 section의 스폰 기록이 누수 없이(그리고 standbyRoom의 미소비 등록을 훼손하지 않고) 제거됨.</done>
</task>

</tasks>

<verification>
- Unity 컴파일 에러 0건
- `grep -c "spawner.Activate();" Assets/Scripts/World/WorldGenerator.cs` == 0 (전부 `TryActivateSection`/`ActivateStaggered` 경유로 대체)
- `grep -c "_activatedSections.Clear()\|_pendingSpawns.Clear()" Assets/Scripts/World/WorldGenerator.cs` == 0 (블랭킷 Clear 금지 — standbyRoom의 사전 등록 보존)
- 4개 `TrySpawnEnemies()` 호출 지점(Start/SpawnNextPair-room/SpawnPrevPair-room/TrySpawnExitPortal-standby) + 신규 2개(SpawnNextPair-corridor/SpawnPrevPair-corridor) = 총 6개 호출 지점 존재
- 실제 동작 검증(포탈 타이밍, 감지 차단, 특히 층 전환 후 새 시작 Room의 적이 실제로 Activate되는지)은 14-04의 플레이테스트 체크포인트에서 수행 — 이 플랜 자체는 코드 배선만
</verification>

<success_criteria>
- SPWN-01(Activate 시점에만 VFX 재생)과 SPWN-02(연출 중 감지/타겟팅 차단)의 런타임 트리거 경로가 Room+Corridor 양쪽에서 완성됨
- Pitfall 2(Corridor 조기 노출), Pitfall 4(4개 호출부 누락), Pitfall 5(새 층 no-op)가 모두 코드 레벨에서 해소됨
- 새 층 시작 Room의 _pendingSpawns 등록이 FloorTransitionSequence() 도중 실수로 지워지지 않고 TryActivateSection(newRoom)까지 온전히 보존됨
- 14-04(에디터 실행+플레이테스트)로 곧바로 이어질 수 있는 상태
</success_criteria>

<output>
After completion, create `.planning/phases/14-enemy-spawn-vfx/14-03-SUMMARY.md`
</output>
