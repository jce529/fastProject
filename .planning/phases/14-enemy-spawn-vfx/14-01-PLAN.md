---
phase: 14-enemy-spawn-vfx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/ISpawnGatable.cs
  - Assets/Scripts/Enemy/MeleeEnemy.cs
  - Assets/Scripts/Enemy/RangedEnemy.cs
  - Assets/Scripts/Enemy/EnemySpawnEffect.cs
autonomous: true
requirements: [SPWN-01, SPWN-02]

must_haves:
  truths:
    - "ISpawnGatable.SetSpawnGate(true) 호출 시 MeleeEnemy/RangedEnemy의 IsAlive가 false로 전환되어 기존 CombatController.FindNearestEnemyInRange()의 !enemy.IsAlive 스킵 체크와 각 적 Update()의 if(!IsAlive) return; 가드를 그대로 통과한다"
    - "EnemySpawnEffect.PlaySpawnSequence() 완료 시 SetSpawnGate(false)가 호출되어 IsAlive가 true로 복원되고 정상 FSM/타겟팅이 재개된다"
    - "IEnemy.cs의 3-member 계약(IsAlive, OnDashHit, ClearHighlight)은 이 플랜에서 한 줄도 변경되지 않는다 (Phase 15/16 BossEnemy 통합 전제 유지)"
    - "EnemySpawnEffect는 포탈 크기를 적 SpriteRenderer.bounds 기준으로 계산한다 — Vector3.one 하드코딩이 아니다 (D-08, Pitfall 3)"
  artifacts:
    - path: "Assets/Scripts/Enemy/ISpawnGatable.cs"
      provides: "SPWN-02 게이트 브릿지 — IEnemy와 분리된 additive 인터페이스"
      contains: "void SetSpawnGate(bool isSpawning);"
    - path: "Assets/Scripts/Enemy/EnemySpawnEffect.cs"
      provides: "SPWN-01 스폰 VFX 시퀀스 (포탈 성장 → 걸어나오기+마스크 수축 동시 진행 → 포탈 축소)"
      contains: "public IEnumerator PlaySpawnSequence"
  key_links:
    - from: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      to: "Assets/Scripts/Enemy/ISpawnGatable.cs"
      via: "SetSpawnGate가 기존 private-set IsAlive 프로퍼티에 직접 대입"
      pattern: "SetSpawnGate\\(bool isSpawning\\) => IsAlive = !isSpawning;"
    - from: "Assets/Scripts/Enemy/EnemySpawnEffect.cs"
      to: "Assets/Scripts/Enemy/ISpawnGatable.cs"
      via: "시퀀스 종료 시 게이트 해제 호출"
      pattern: "gate\\?\\.SetSpawnGate\\(false\\)"
    - from: "Assets/Scripts/Enemy/EnemySpawnEffect.cs"
      to: "Assets/Scripts/World/RuntimeMaskSprite.cs"
      via: "SpriteMask 텍스처 재사용 (Phase 12/13과 동일 캐시)"
      pattern: "RuntimeMaskSprite\\.CreateMaskSprite\\(\\)"
---

<objective>
스폰 게이팅 브릿지(ISpawnGatable)와 스폰 VFX 재생 컴포넌트(EnemySpawnEffect)를 신설한다. 이 플랜은 순수 신규/추가 코드로, EnemySpawner/WorldGenerator는 아직 건드리지 않는다 — Interface-First 순서: 이후 플랜(14-02, 14-03)이 이 계약을 그대로 소비한다.

Purpose: SPWN-02(감지/타겟팅 차단)의 실제 메커니즘과 SPWN-01(포탈 연출)의 실제 비주얼을 IEnemy 계약 변경 없이 구현한다.
Output: ISpawnGatable.cs(신규), EnemySpawnEffect.cs(신규), MeleeEnemy.cs/RangedEnemy.cs에 각 1개 메서드 추가.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-enemy-spawn-vfx/14-CONTEXT.md
@.planning/phases/14-enemy-spawn-vfx/14-RESEARCH.md
@Assets/Scripts/Enemy/IEnemy.cs
@Assets/Scripts/Enemy/MeleeEnemy.cs
@Assets/Scripts/Enemy/RangedEnemy.cs
@Assets/Scripts/Enemy/EnemyDeathEffect.cs
@Assets/Scripts/World/FloorTransitionEffect.cs
@Assets/Scripts/World/RuntimeMaskSprite.cs
@Assets/Scripts/Audio/AudioManager.cs

<interfaces>
<!-- 재사용 대상 기존 API — 탐색 불필요 -->

From Assets/Scripts/Enemy/IEnemy.cs (변경 금지, 그대로 유지):
```csharp
public interface IEnemy
{
    bool IsAlive { get; }
    void OnDashHit();
    void ClearHighlight();
}
```

From Assets/Scripts/World/RuntimeMaskSprite.cs:
```csharp
public static class RuntimeMaskSprite
{
    public static Sprite CreateMaskSprite(); // 캐시된 4x4 흰색 Sprite, 반복 호출해도 재생성 없음
}
```

From Assets/Scripts/Audio/AudioManager.cs:
```csharp
public enum Sfx { PortalEnter, PortalExit, Slash, EnemyDeathGlitch }
public static void PlaySfx(Sfx id, float volume = 1f); // null 안전
```

현재 MeleeEnemy.cs 관련 부분 (line 10, 48, 100-104 — 2026-07-10 확인):
```csharp
public class MeleeEnemy : MonoBehaviour, IEnemy
{
    ...
    public bool IsAlive { get; private set; } = true;
    ...
    public void ClearHighlight()
    {
        var sr = GetComponent<SpriteRenderer>();
        if (sr != null) sr.color = Color.white;
    }
```

현재 RangedEnemy.cs 관련 부분 (line 12, 47, 106-110 — 2026-07-10 확인):
```csharp
public class RangedEnemy : MonoBehaviour, IEnemy
{
    ...
    public bool IsAlive { get; private set; } = true;
    ...
    public void ClearHighlight()
    {
        var sr = GetComponent<SpriteRenderer>();
        if (sr != null) sr.color = Color.white;
    }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ISpawnGatable 인터페이스 신설 + MeleeEnemy/RangedEnemy 구현 (SPWN-02)</name>
  <files>Assets/Scripts/Enemy/ISpawnGatable.cs, Assets/Scripts/Enemy/MeleeEnemy.cs, Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/IEnemy.cs (3-member 계약 확인 — 이 태스크에서 이 파일은 절대 수정하지 않는다)
    - Assets/Scripts/Enemy/MeleeEnemy.cs (class 선언부 line 10, IsAlive 프로퍼티 line 48, ClearHighlight() line 100-104)
    - Assets/Scripts/Enemy/RangedEnemy.cs (class 선언부 line 12, IsAlive 프로퍼티 line 47, ClearHighlight() line 106-110)
  </read_first>
  <action>
    **1. 신규 파일 `Assets/Scripts/Enemy/ISpawnGatable.cs` 생성:**
    ```csharp
    /// <summary>
    /// SPWN-02: IEnemy의 3-member 계약(IsAlive, OnDashHit, ClearHighlight)과는 별도로 존재하는
    /// additive 인터페이스 — IEnemy 자체는 변경하지 않는다 (Phase 15/16 BossEnemy 통합 전제 유지).
    /// EnemySpawnEffect가 적 타입(Melee/Ranged/향후 Boss)에 캐스팅 없이 스폰 게이트를 여닫을 수 있게 한다.
    /// </summary>
    public interface ISpawnGatable
    {
        /// <summary>true: 스폰 VFX 재생 중 — 구현체는 내부적으로 IsAlive를 false로 강제해야 한다.
        /// false: 스폰 완료 — IsAlive를 true로 복원해 정상 FSM/타겟팅을 재개한다.</summary>
        void SetSpawnGate(bool isSpawning);
    }
    ```

    **2. `Assets/Scripts/Enemy/MeleeEnemy.cs` — 2곳 수정:**

    Line 10 class 선언에 인터페이스 추가:
    ```csharp
    public class MeleeEnemy : MonoBehaviour, IEnemy, ISpawnGatable
    ```

    `ClearHighlight()` 메서드(line 100-104) 바로 뒤에 새 리전과 메서드 추가:
    ```csharp
    public void ClearHighlight()
    {
        var sr = GetComponent<SpriteRenderer>();
        if (sr != null) sr.color = Color.white;
    }

    // -- ISpawnGatable implementation (SPWN-02, IEnemy와 별도 추가 인터페이스) ------------

    public void SetSpawnGate(bool isSpawning) => IsAlive = !isSpawning;
    ```

    **3. `Assets/Scripts/Enemy/RangedEnemy.cs` — 동일 패턴 2곳 수정:**

    Line 12 class 선언:
    ```csharp
    public class RangedEnemy : MonoBehaviour, IEnemy, ISpawnGatable
    ```

    `ClearHighlight()` 메서드(line 106-110) 바로 뒤:
    ```csharp
    public void ClearHighlight()
    {
        var sr = GetComponent<SpriteRenderer>();
        if (sr != null) sr.color = Color.white;
    }

    // -- ISpawnGatable implementation (SPWN-02, IEnemy와 별도 추가 인터페이스) ------------

    public void SetSpawnGate(bool isSpawning) => IsAlive = !isSpawning;
    ```

    `IsAlive`의 setter가 `private`이므로 이 메서드는 반드시 클래스 내부에 있어야 한다(확장 메서드/외부 partial 불가). IEnemy.cs, CombatController.cs는 이 태스크에서 건드리지 않는다 — 기존 `!enemy.IsAlive` 체크가 그대로 새 게이트를 인식한다.
  </action>
  <verify>
    <automated>grep -n "ISpawnGatable" Assets/Scripts/Enemy/MeleeEnemy.cs Assets/Scripts/Enemy/RangedEnemy.cs Assets/Scripts/Enemy/ISpawnGatable.cs && grep -c "bool IsAlive { get; }" Assets/Scripts/Enemy/IEnemy.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/Enemy/ISpawnGatable.cs`가 존재하고 `void SetSpawnGate(bool isSpawning);` 시그니처를 포함한다
    - `MeleeEnemy.cs`, `RangedEnemy.cs` 둘 다 class 선언에 `, ISpawnGatable`이 추가되어 있고, `SetSpawnGate(bool isSpawning) => IsAlive = !isSpawning;` 구현이 존재한다
    - `Assets/Scripts/Enemy/IEnemy.cs`의 git diff가 0줄이다 (3-member 계약 불변)
    - `Assets/Scripts/Player/CombatController.cs`의 git diff가 0줄이다 (이 태스크에서 미수정)
  </acceptance_criteria>
  <done>MeleeEnemy/RangedEnemy가 ISpawnGatable을 구현해, 외부에서 IsAlive를 안전하게 토글할 수 있는 통로가 생겼다 — IEnemy 계약은 그대로.</done>
</task>

<task type="auto">
  <name>Task 2: EnemySpawnEffect 컴포넌트 신설 — 포탈 성장 + 걸어나오기 + 마스크 수축 + 사운드 (SPWN-01, D-06~D-09)</name>
  <files>Assets/Scripts/Enemy/EnemySpawnEffect.cs</files>
  <read_first>
    - Assets/Scripts/World/FloorTransitionEffect.cs (PlayExit() 구조 — X1 포탈성장/X2 마스크수축/X3 정리/X4 포탈페이드, ScaleTransform() 헬퍼 패턴)
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs (AddComponent+StartCoroutine 컨벤션, RuntimeMaskSprite 사용 패턴)
    - Assets/Scripts/World/RuntimeMaskSprite.cs (CreateMaskSprite 시그니처)
    - Assets/Scripts/Audio/AudioManager.cs (Sfx.PortalEnter/PlaySfx 시그니처)
    - Assets/Scripts/Enemy/ISpawnGatable.cs (Task 1 산출물 — SetSpawnGate 시그니처)
  </read_first>
  <action>
    신규 파일 `Assets/Scripts/Enemy/EnemySpawnEffect.cs` 생성 (`EnemyDeathEffect.cs`와 동일하게 `[RequireComponent(typeof(SpriteRenderer))]`, 자기 완결적 — 공용 유틸로 추출하지 않음). 아래 코드를 그대로 사용한다 (총 연출 0.4+0.5+0.3=1.2초, D-06 기준 일치):

    ```csharp
    using System.Collections;
    using UnityEngine;

    /// <summary>
    /// SPWN-01/SPWN-02/D-06/D-07/D-08/D-09: 적 스폰 시 포탈 성장 + 실제 걸어나오는 이동(마스크 수축과
    /// 동시 진행) + 포탈 축소 연출을 재생하고, 완료 즉시 ISpawnGatable 게이트를 해제한다.
    /// EnemyType(Melee/Ranged, 추후 Boss)에 종속되지 않는다 — GetComponent&lt;ISpawnGatable&gt;()로만
    /// 상호작용하며 캐스팅하지 않는다 (Phase 16 BossEnemy 재사용 전제, SPWN Success Criterion 5).
    /// EnemySpawner.Activate()에서만 AddComponent+StartCoroutine으로 호출된다 (Awake/OnEnable 트리거 금지 — STATE.md 제약).
    /// </summary>
    [RequireComponent(typeof(SpriteRenderer))]
    public class EnemySpawnEffect : MonoBehaviour
    {
        [SerializeField] private float _portalGrowDuration    = 0.4f; // D-06: FloorTransitionEffect X1과 동일 기준
        [SerializeField] private float _walkMaskDuration       = 0.5f; // D-06/D-07: 걸어나오기+마스크 수축 동시 재생 구간
        [SerializeField] private float _portalFadeDuration    = 0.3f; // D-06: FloorTransitionEffect X4와 동일 기준
        [SerializeField] private float _walkOutDistance        = 1f;   // D-07: 포탈 중심에서 걸어나오는 거리(Inspector 조정 가능)
        [SerializeField] private float _portalScaleMultiplier = 1.2f; // D-08: 적 스프라이트 크기 대비 포탈 배율

        private SpriteRenderer _sr;
        private Rigidbody2D    _rb;

        private void Awake()
        {
            _sr = GetComponent<SpriteRenderer>();
            _rb = GetComponent<Rigidbody2D>();
        }

        /// <summary>portalEffectPrefab이 null이면 포탈 비주얼 없이 걸어나오기+게이팅만 재생한다
        /// (DebugRoomTeleporter 등 포탈 프리팹을 넘기지 않는 호출자 호환).</summary>
        public IEnumerator PlaySpawnSequence(GameObject portalEffectPrefab, ISpawnGatable gate)
        {
            Vector3 restPos = transform.position; // EnemySpawner 마커 위치 — 최종 정지 위치(변경 없음)

            // D-08/Pitfall 3: Vector3.one 하드코딩 금지 — 스프라이트 실제 크기 기준으로 포탈 크기 계산
            float spriteSize = Mathf.Max(_sr.bounds.size.x, _sr.bounds.size.y);
            Vector3 targetPortalScale = Vector3.one * spriteSize * _portalScaleMultiplier;

            // 1. 포탈 성장 (D-06 — FloorTransitionEffect.PlayExit() X1과 동일 구조)
            GameObject portal = null;
            if (portalEffectPrefab != null)
            {
                portal = Instantiate(portalEffectPrefab, restPos, Quaternion.identity);
                portal.transform.localScale = Vector3.zero;
            }
            AudioManager.PlaySfx(Sfx.PortalEnter); // D-09: 일반 적 스폰음 재사용 — 신규 클립 임포트 없음
            if (portal != null)
                yield return ScaleTransform(portal.transform, Vector3.zero, targetPortalScale, _portalGrowDuration);

            // 2. D-07: 포탈 중심에서 실제로 걸어나오는 이동 — Rigidbody2D를 잠시 Kinematic으로 전환해
            // 중력/충돌 간섭 없이 startPos -> restPos로 이동시킨 뒤 원래 BodyType을 복원한다.
            RigidbodyType2D originalBodyType = RigidbodyType2D.Dynamic;
            Vector3 startPos = restPos + Vector3.left * _walkOutDistance;
            if (_rb != null)
            {
                originalBodyType = _rb.bodyType;
                _rb.bodyType = RigidbodyType2D.Kinematic;
                _rb.linearVelocity = Vector2.zero;
                _rb.position = startPos;
            }
            else
            {
                transform.position = startPos;
            }

            // SpriteMask 수축(FloorTransitionEffect와 동일한 VisibleOutsideMask 와이프 패턴)과 물리 이동을
            // 같은 시간축으로 동시 진행한다 — 이것이 D-07이 요구하는 "실제로 걸어나온다"는 느낌의 핵심.
            var maskGO = new GameObject("SpawnMask");
            var mask = maskGO.AddComponent<SpriteMask>();
            mask.sprite = RuntimeMaskSprite.CreateMaskSprite();
            _sr.maskInteraction = SpriteMaskInteraction.VisibleOutsideMask;

            float portalX = restPos.x;
            float dir = Mathf.Sign(startPos.x - portalX); // startPos가 restPos 왼쪽이므로 -1
            float startWidth = Mathf.Abs(startPos.x - portalX) + _sr.bounds.extents.x + 0.3f;

            float elapsed = 0f;
            while (elapsed < _walkMaskDuration)
            {
                elapsed += Time.unscaledDeltaTime; // 슬로우모션/HitFreeze 면역
                float t = Mathf.Clamp01(elapsed / _walkMaskDuration);

                Vector3 currentPos = Vector3.Lerp(startPos, restPos, t);
                if (_rb != null) _rb.MovePosition(currentPos);
                else transform.position = currentPos;

                float width = Mathf.Lerp(startWidth, 0f, t);
                Vector3 maskPos = currentPos;
                maskPos.x = portalX + (width * 0.5f * dir);
                maskGO.transform.position = maskPos;
                maskGO.transform.localScale = new Vector3(Mathf.Max(width, 0.001f), 20f, 1f);

                yield return null;
            }

            if (_rb != null)
            {
                _rb.position = restPos;
                _rb.bodyType = originalBodyType;
            }
            else
            {
                transform.position = restPos;
            }

            Destroy(maskGO);
            _sr.maskInteraction = SpriteMaskInteraction.None;

            // 3. 포탈 축소 후 정리 (D-06 — FloorTransitionEffect.PlayExit() X4와 동일 구조)
            if (portal != null)
            {
                yield return ScaleTransform(portal.transform, targetPortalScale, Vector3.zero, _portalFadeDuration);
                Destroy(portal);
            }

            // 4. SPWN-02: 연출 완료 즉시 게이트 해제 — 정상 FSM/타겟팅 재개
            gate?.SetSpawnGate(false);
        }

        /// <summary>공용 스케일 보간 헬퍼 — FloorTransitionEffect.ScaleTransform()과 동일 패턴(독립 사본,
        /// EnemyDeathEffect처럼 이펙트 컴포넌트가 자기 완결적이도록 공용 유틸로 추출하지 않음).</summary>
        private IEnumerator ScaleTransform(Transform t, Vector3 from, Vector3 to, float duration)
        {
            if (t == null) yield break;
            float elapsed = 0f;
            while (elapsed < duration)
            {
                if (t == null) yield break;
                elapsed += Time.unscaledDeltaTime;
                t.localScale = Vector3.Lerp(from, to, Mathf.Clamp01(elapsed / duration));
                yield return null;
            }
            if (t != null) t.localScale = to;
        }
    }
    ```

    이 파일은 EnemySpawner/WorldGenerator를 참조하지 않는다 — 14-02가 이 클래스를 소비한다.
  </action>
  <verify>
    <automated>grep -n "class EnemySpawnEffect" Assets/Scripts/Enemy/EnemySpawnEffect.cs && grep -c "Time.unscaledDeltaTime" Assets/Scripts/Enemy/EnemySpawnEffect.cs && grep -c "WaitForSeconds(" Assets/Scripts/Enemy/EnemySpawnEffect.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/Enemy/EnemySpawnEffect.cs`가 존재하고 `public IEnumerator PlaySpawnSequence(GameObject portalEffectPrefab, ISpawnGatable gate)` 시그니처를 포함한다
    - 파일 내 `Time.unscaledDeltaTime` 사용이 최소 2회 이상이다 (마스크 루프 + ScaleTransform)
    - 파일 내 `WaitForSeconds(`(Realtime 아닌 것) 사용이 0건이다 — 전부 Time.unscaledDeltaTime 기반 루프
    - `Mathf.Max(_sr.bounds.size.x, _sr.bounds.size.y)` 패턴이 존재한다 (D-08: Vector3.one 하드코딩 아님)
    - 시퀀스 마지막 줄이 `gate?.SetSpawnGate(false);`이다 (SPWN-02 게이트 해제)
  </acceptance_criteria>
  <done>EnemySpawnEffect가 독립적으로 컴파일 가능한 상태로 존재 — 포탈 성장/실제 걸어나오기/마스크 수축/포탈 축소/사운드/게이트 해제를 전부 포함.</done>
</task>

</tasks>

<verification>
- `Assets/Scripts/Enemy/ISpawnGatable.cs`, `Assets/Scripts/Enemy/EnemySpawnEffect.cs` 신규 파일 존재
- `MeleeEnemy.cs`/`RangedEnemy.cs`가 `ISpawnGatable` 구현, `IEnemy.cs`/`CombatController.cs` diff 0
- Unity 컴파일 에러 0건 (다음 플랜 14-02 실행 전 확인 권장 — 이 플랜은 신규/추가 코드만이라 기존 씬 동작에 영향 없음)
</verification>

<success_criteria>
- SPWN-01의 VFX 재생 로직과 SPWN-02의 게이팅 브릿지가 IEnemy 계약 변경 없이 완성됨
- 14-02(EnemySpawner 배선)가 즉시 소비 가능한 안정된 계약(ISpawnGatable, EnemySpawnEffect.PlaySpawnSequence)이 확보됨
</success_criteria>

<output>
After completion, create `.planning/phases/14-enemy-spawn-vfx/14-01-SUMMARY.md`
</output>
