---
phase: 10-exit-portal-floor-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/ExitSpawnPoint.cs
  - Assets/Scripts/World/ExitPortal.cs
  - Assets/Editor/ExitPortalBuilder.cs
autonomous: true
requirements:
  - EXIT-01
  - EXIT-03

must_haves:
  truths:
    - "ExitSpawnPoint는 빈 마커 컴포넌트로 Room 자식 오브젝트에 부착 가능하다 (D-01)"
    - "ExitPortal은 트리거 진입 시 자기 자신이 아닌 WorldGenerator.Instance에서 전환 코루틴을 시작시킨다 (코루틴 소유권 Pitfall 회피)"
    - "ExitPortal.StandbyRoom은 포탈 인스턴스별로 저장되어 _maxExitsActive > 1이어도 서로 덮어쓰지 않는다 (D-08)"
    - "Fast/Phase10/Build Exit Portal Prefab 메뉴 실행 시 BoxCollider2D(isTrigger)+ExitPortal 컴포넌트를 가진 프리팹이 생성된다"
  artifacts:
    - path: "Assets/Scripts/World/ExitSpawnPoint.cs"
      provides: "EXIT 포탈 스폰 후보 지점 빈 마커 컴포넌트"
    - path: "Assets/Scripts/World/ExitPortal.cs"
      provides: "트리거 콜라이더 + StandbyRoom 참조 + OnTriggerEnter2D -> WorldGenerator.Instance.EnterPortal() 호출"
      exports:
        - "public GameObject StandbyRoom { get; set; }"
        - "MonoBehaviour OnTriggerEnter2D"
    - path: "Assets/Editor/ExitPortalBuilder.cs"
      provides: "Fast/Phase10/Build Exit Portal Prefab 메뉴 -- ExitPortal.prefab 생성"
  key_links:
    - from: "ExitPortal.OnTriggerEnter2D"
      to: "WorldGenerator.Instance.EnterPortal(this)"
      via: "static Instance 싱글톤 참조"
      pattern: "WorldGenerator\\.Instance\\.EnterPortal"
---

<objective>
EXIT 포탈 시스템의 기초 컴포넌트 3종을 신규 생성한다: `ExitSpawnPoint`(포탈 스폰 후보 마커), `ExitPortal`(트리거 + 대기룸 참조), `ExitPortalBuilder`(프리팹 생성 에디터 도구). 이 Plan은 코드 계약(contracts)만 만든다 -- WorldGenerator 통합은 Plan 02, 실제 프리팹 빌드 실행과 Room 마커 배치는 Plan 03에서 진행한다.

Purpose: Plan 02(WorldGenerator 수정)와 Plan 03(에디터 수동 작업)이 참조할 타입/프리팹 빌드 도구를 먼저 확정해, 후속 Plan이 코드 탐색 없이 바로 이 파일들의 정확한 시그니처를 사용할 수 있게 한다.
Output: `ExitSpawnPoint.cs`, `ExitPortal.cs`, `ExitPortalBuilder.cs` 3개 신규 파일 (컴파일 성공, 프리팹은 아직 미생성).
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-exit-portal-floor-transition/10-CONTEXT.md
@.planning/phases/10-exit-portal-floor-transition/10-RESEARCH.md

<interfaces>
Existing project conventions this plan ports directly:

From Assets/Scripts/World/RoomEntry.cs (empty marker component pattern -- ExitSpawnPoint follows exactly):
public class RoomEntry : MonoBehaviour { }

From Assets/Scripts/World/RoomConnector.cs (OnDrawGizmos convention -- always visible in scene view):
private void OnDrawGizmos() { Gizmos.color = ...; Gizmos.DrawSphere(transform.position, 0.4f); }

From Assets/Scripts/World/RoomExit.cs (trigger tag-check convention -- ExitPortal uses the same):
private void OnTriggerEnter2D(Collider2D other) { if (!other.CompareTag("Player")) return; ... }

From Assets/Editor/CorridorBuilder.cs (SavePrefab pattern -- ExitPortalBuilder ports this exactly):
PrefabUtility.SaveAsPrefabAsset(root, assetPath); Object.DestroyImmediate(root);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ExitSpawnPoint.cs + ExitPortal.cs -- marker/trigger components</name>
  <files>Assets/Scripts/World/ExitSpawnPoint.cs, Assets/Scripts/World/ExitPortal.cs</files>
  <read_first>
    - Assets/Scripts/World/RoomEntry.cs (empty marker component pattern, source)
    - Assets/Scripts/World/RoomConnector.cs (OnDrawGizmos convention)
    - Assets/Scripts/World/RoomExit.cs (CompareTag("Player") trigger check convention -- what ExitPortal replaces)
    - Assets/Scripts/World/WorldGenerator.cs (Instance singleton will be added here in Plan 02 -- does not exist yet)
  </read_first>
  <action>
Create `Assets/Scripts/World/ExitSpawnPoint.cs` (D-01 -- same pattern as EnemySpawner.cs: empty component, child object position is the spawn candidate point):

```csharp
using UnityEngine;

/// &lt;summary&gt;
/// EXIT 포탈 스폰 후보 지점 마커. Room 프리팹 자식 오브젝트에 부착한다 (D-01).
/// WorldGenerator.TrySpawnExitPortal()이 GetComponentsInChildren&lt;ExitSpawnPoint&gt;(true)로
/// 후보를 찾아 랜덤으로 하나를 선택한다.
/// &lt;/summary&gt;
public class ExitSpawnPoint : MonoBehaviour
{
    private void OnDrawGizmos()
    {
        Gizmos.color = Color.yellow;
        Gizmos.DrawWireSphere(transform.position, 0.4f);
    }
}
```

Create `Assets/Scripts/World/ExitPortal.cs`:

```csharp
using UnityEngine;

/// &lt;summary&gt;
/// EXIT-03: WorldGenerator.TrySpawnExitPortal()이 Room 스폰 시 Instantiate하는 포탈 트리거.
/// 플레이어가 진입하면 WorldGenerator.Instance.EnterPortal(this)를 호출해 층 전환 코루틴을 시작시킨다.
///
/// CRITICAL: 코루틴은 반드시 WorldGenerator(영속 싱글톤)에서 실행되어야 한다.
/// D-07에 의해 전환 시퀀스 도중 이 포탈이 속한 room(및 이 컴포넌트 자신)이 Destroy되므로,
/// ExitPortal 자신에게서 StartCoroutine을 호출하면 Destroy 시점에 시퀀스가 즉시 중단된다
/// (Unity는 GameObject/컴포넌트가 파괴되면 그것이 소유한 모든 코루틴을 즉시 중단한다).
/// &lt;/summary&gt;
[RequireComponent(typeof(Collider2D))]
public class ExitPortal : MonoBehaviour
{
    /// &lt;summary&gt;
    /// D-08: 이 포탈에 연결된 다음 층 대기룸(비활성 상태로 미리 스폰됨).
    /// WorldGenerator.TrySpawnExitPortal()이 스폰 직후 채운다. 포탈별 저장이므로
    /// _maxExitsActive &gt; 1이어도 서로 다른 포탈의 대기룸을 덮어쓰지 않는다.
    /// &lt;/summary&gt;
    public GameObject StandbyRoom { get; set; }

    private bool _triggered;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (_triggered) return;
        if (!other.CompareTag("Player")) return;
        _triggered = true;
        WorldGenerator.Instance.EnterPortal(this);
    }

    private void OnDrawGizmos()
    {
        Gizmos.color = new Color(1f, 0.5f, 0f, 0.6f);
        var col = GetComponent&lt;BoxCollider2D&gt;();
        if (col != null)
            Gizmos.DrawWireCube(transform.position + (Vector3)col.offset, col.size);
        else
            Gizmos.DrawWireSphere(transform.position, 0.6f);
    }
}
```

Note: at this point `WorldGenerator.Instance` and `WorldGenerator.EnterPortal()` do not exist yet (added in Plan 02). A compile error after this task alone is expected and normal -- the full build only passes once Plan 02 is complete. Task 2 (ExitPortalBuilder.cs) does not reference either symbol so it is independently verifiable.
  </action>
  <verify>
    <automated>grep -n "class ExitSpawnPoint\|class ExitPortal\|StandbyRoom\|WorldGenerator.Instance.EnterPortal\|CompareTag(.Player.)" "Assets/Scripts/World/ExitSpawnPoint.cs" "Assets/Scripts/World/ExitPortal.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/World/ExitSpawnPoint.cs exists and contains "public class ExitSpawnPoint : MonoBehaviour"
    - Assets/Scripts/World/ExitPortal.cs exists and contains "public class ExitPortal : MonoBehaviour"
    - ExitPortal.cs contains "public GameObject StandbyRoom { get; set; }"
    - ExitPortal.cs's OnTriggerEnter2D checks "other.CompareTag(\"Player\")"
    - ExitPortal.cs's OnTriggerEnter2D calls "WorldGenerator.Instance.EnterPortal(this)"
    - ExitPortal.cs contains a "_triggered" guard field to prevent double-firing (Pitfall 6)
    - "[RequireComponent(typeof(Collider2D))]" attribute is present above the ExitPortal class
  </acceptance_criteria>
  <done>ExitSpawnPoint.cs and ExitPortal.cs exist. ExitPortal starts the transition from WorldGenerator.Instance rather than itself (a compile error referencing WorldGenerator.Instance/EnterPortal is expected until Plan 02 completes).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ExitPortalBuilder.cs -- ExitPortal prefab-builder editor tool</name>
  <files>Assets/Editor/ExitPortalBuilder.cs</files>
  <read_first>
    - Assets/Editor/CorridorBuilder.cs (SavePrefab pattern, source -- ported directly)
    - Assets/Scripts/World/ExitPortal.cs (Task 1 output -- AddComponent target)
  </read_first>
  <action>
Create `Assets/Editor/ExitPortalBuilder.cs`. Running the menu `Fast/Phase10/Build Exit Portal Prefab` creates `Assets/Prefabs/World/ExitPortal/ExitPortal.prefab`.

Portal collider size: BoxCollider2D `size=(1.5, 2.5)`, `offset=(0, 1.25)` -- generously larger than the Player's actual BoxCollider2D (size approx 0.33 x 1.28, see SampleScene.unity) so the player reliably triggers it while walking in. No sprite/VFX is created, per REQUIREMENTS.md Out of Scope ("포탈 이펙트/사운드") -- visual confirmation relies solely on ExitPortal.OnDrawGizmos()'s gizmo (per 10-RESEARCH.md Open Question 3 recommendation).

```csharp
using System.IO;
using UnityEditor;
using UnityEngine;

/// &lt;summary&gt;
/// Menu: Fast/Phase10/Build Exit Portal Prefab
/// ExitPortal 프리팹을 Assets/Prefabs/World/ExitPortal/ 아래에 생성한다.
/// BoxCollider2D(isTrigger) + ExitPortal 컴포넌트만 포함한다.
/// 시각 이펙트/스프라이트는 REQUIREMENTS.md Out of Scope에 따라 의도적으로 제외 --
/// 확인은 ExitPortal.OnDrawGizmos()의 Gizmo만으로 충분하다.
/// &lt;/summary&gt;
public static class ExitPortalBuilder
{
    private const string AssetPath = "Assets/Prefabs/World/ExitPortal/ExitPortal.prefab";

    [MenuItem("Fast/Phase10/Build Exit Portal Prefab")]
    public static void Run()
    {
        var root = new GameObject("ExitPortal");

        var col = root.AddComponent&lt;BoxCollider2D&gt;();
        col.isTrigger = true;
        col.size = new Vector2(1.5f, 2.5f);
        col.offset = new Vector2(0f, 1.25f);

        root.AddComponent&lt;ExitPortal&gt;();

        SavePrefab(root);
    }

    private static void SavePrefab(GameObject root)
    {
        string fullDir = Path.Combine(Application.dataPath, "Prefabs", "World", "ExitPortal");
        if (!Directory.Exists(fullDir))
            Directory.CreateDirectory(fullDir);

        if (AssetDatabase.LoadAssetAtPath&lt;GameObject&gt;(AssetPath) != null)
            AssetDatabase.DeleteAsset(AssetPath);

        AssetDatabase.Refresh();

        PrefabUtility.SaveAsPrefabAsset(root, AssetPath);
        Object.DestroyImmediate(root);

        AssetDatabase.SaveAssets();
        AssetDatabase.Refresh();

        Debug.Log($"[ExitPortalBuilder] ExitPortal built at {AssetPath}");
    }
}
```

This task only creates the script -- running the menu (actually producing the prefab) happens in Plan 03 (manual Unity Editor work).
  </action>
  <verify>
    <automated>grep -n "MenuItem(.Fast/Phase10/Build Exit Portal Prefab.)\|AddComponent<ExitPortal>\|isTrigger = true\|SaveAsPrefabAsset" "Assets/Editor/ExitPortalBuilder.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/ExitPortalBuilder.cs exists
    - Contains "[MenuItem(\"Fast/Phase10/Build Exit Portal Prefab\")]"
    - Contains "col.isTrigger = true" and "col.size = new Vector2(1.5f, 2.5f)"
    - Contains "root.AddComponent<ExitPortal>()"
    - Contains "PrefabUtility.SaveAsPrefabAsset(root, AssetPath)"
    - Contains no SpriteRenderer/particle/VFX component addition (Out of Scope check)
  </acceptance_criteria>
  <done>ExitPortalBuilder.cs exists with a menu item that will build a BoxCollider2D(isTrigger) + ExitPortal prefab at Assets/Prefabs/World/ExitPortal/ExitPortal.prefab once run inside the Unity Editor (Plan 03).</done>
</task>

</tasks>

<verification>
grep -c "class Exit" Assets/Scripts/World/ExitSpawnPoint.cs Assets/Scripts/World/ExitPortal.cs 결과 각 파일에 1개씩 존재해야 한다.
Plan 02 완료 전까지 Unity 컴파일러가 "WorldGenerator.Instance" 관련 에러를 낼 수 있음 -- 이는 정상이며 Plan 02에서 해소된다.
</verification>

<success_criteria>
- ExitSpawnPoint.cs, ExitPortal.cs, ExitPortalBuilder.cs 3개 파일이 모두 생성되었다
- ExitPortal.cs가 코루틴을 자기 자신이 아닌 WorldGenerator.Instance에서 시작시키는 구조로 작성되었다 (Pitfall 1 방지)
- ExitPortalBuilder.cs가 CorridorBuilder.cs와 동일한 SavePrefab 컨벤션을 따른다
</success_criteria>

<output>
완료 후 `.planning/phases/10-exit-portal-floor-transition/10-01-SUMMARY.md` 생성:
- 생성된 3개 파일 경로 및 각 라인 수
- ExitPortal의 코루틴 소유권 설계 (Pitfall 1 회피) 요약
- Plan 02 완료 전까지 예상되는 컴파일 에러 상태 (WorldGenerator.Instance 미정의) 명시
</output>
