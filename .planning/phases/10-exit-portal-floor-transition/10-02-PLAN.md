---
phase: 10-exit-portal-floor-transition
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements:
  - EXIT-01
  - EXIT-02
  - EXIT-03

must_haves:
  truths:
    - "_exitSpawnChance를 1.0f로 설정하면 모든 Room 스폰 시 포탈이 생성되고, 0.0f이면 생성되지 않는다 (EXIT-01)"
    - "_maxExitsActive를 1로 설정했을 때 동시 활성 포탈이 1개를 초과해 스폰되지 않는다 (EXIT-02)"
    - "_exitSpawnChance와 _maxExitsActive가 WorldGenerator 인스펙터에서 조절 가능하다 (EXIT-02)"
    - "ExitPortal.EnterPortal() 호출 시 FloorManager.CurrentFloor가 +1 증가하고 기존 체인 전체가 Destroy되며 대기룸 하나로 새 체인이 시작된다 (EXIT-03, D-07)"
    - "GEN-02 lookbehind 정리(RemoveTail)가 미사용 포탈의 대기룸을 함께 Destroy하고 _activeExitCount를 감소시킨다 (D-08)"
    - "_maxExitsActive를 1보다 크게 설정해도, FloorTransitionSequence()가 입장한 포탈 외 다른 미사용 포탈의 대기룸을 체인 파괴 시 함께 Destroy해 고아 GameObject가 남지 않는다 (D-08, 리뷰 수정)"
    - "전환 코루틴은 WorldGenerator(영속 싱글톤)에서 실행되어 옛 체인 Destroy 이후에도 계속 진행된다 (Pitfall 1)"
  artifacts:
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "포탈 스폰 롤 + 층 전환 코루틴을 포함한 완성된 WorldGenerator"
      exports:
        - "public static WorldGenerator Instance"
        - "public void EnterPortal(ExitPortal portal)"
      contains: "private void TrySpawnExitPortal(GameObject room)"
  key_links:
    - from: "WorldGenerator.Start() / SpawnNextPair()"
      to: "WorldGenerator.TrySpawnExitPortal(room)"
      via: "직접 메서드 호출"
      pattern: "TrySpawnExitPortal\\("
    - from: "WorldGenerator.EnterPortal()"
      to: "WorldGenerator.FloorTransitionSequence() 코루틴"
      via: "StartCoroutine (WorldGenerator 자신에서 실행 -- ExitPortal에서가 아님)"
      pattern: "StartCoroutine\\(FloorTransitionSequence"
    - from: "WorldGenerator.RemoveTail()"
      to: "ExitPortal.StandbyRoom"
      via: "GetComponentInChildren<ExitPortal>(true)?.StandbyRoom 조회 후 Destroy"
      pattern: "GetComponentInChildren<ExitPortal>"
    - from: "WorldGenerator.FloorTransitionSequence() 체인 teardown 루프"
      to: "ExitPortal.StandbyRoom (미입장 포탈)"
      via: "chainRoom.GetComponentInChildren<ExitPortal>(true) 조회 후 (orphanPortal != portal) 검사 및 Destroy"
      pattern: "orphanPortal != portal"
---

<objective>
`WorldGenerator.cs`에 EXIT 포탈 스폰 확률/최대 개수 로직(EXIT-01, EXIT-02)과 층 전환 코루틴(EXIT-03)을 통합한다. Phase 9의 `SpawnNextFloorStandbyRoom()` 스텁을 실제 `TrySpawnExitPortal()`로 대체하고, `ExitPortal.OnTriggerEnter2D()`가 호출하는 `EnterPortal()` public API와 6단계 전환 코루틴(`FloorTransitionSequence()`)을 추가한다.

Purpose: Plan 01에서 만든 `ExitSpawnPoint`/`ExitPortal` 컴포넌트를 실제 게임플레이 루프에 연결하는 핵심 통합 작업. `FloorSpawner.FloorTransitionSequence()`(Phase 5)의 6단계 시퀀스를 코루틴 소유권 규칙(Pitfall 1)을 지키며 이식한다.
Output: `Assets/Scripts/World/WorldGenerator.cs`가 EXIT-01/02/03 요구사항을 모두 충족하도록 완성됨. Debug.Log 3종(Wave 0 검증 요구사항) 포함.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-exit-portal-floor-transition/10-CONTEXT.md
@.planning/phases/10-exit-portal-floor-transition/10-RESEARCH.md
@.planning/phases/10-exit-portal-floor-transition/10-VALIDATION.md

<interfaces>
From Assets/Scripts/World/ExitPortal.cs (Plan 01 output -- this plan's consumer contract):
public GameObject StandbyRoom { get; set; }
private void OnTriggerEnter2D(Collider2D other) { ...; WorldGenerator.Instance.EnterPortal(this); }

From Assets/Scripts/World/ExitSpawnPoint.cs (Plan 01 output):
public class ExitSpawnPoint : MonoBehaviour { } // empty marker, position = spawn candidate

From Assets/Scripts/World/FloorManager.cs (unchanged, static):
public static class FloorManager { public static int CurrentFloor = 1; }

From Assets/Scripts/Player/PlayerController.cs (unchanged, lines 207/214):
public void LockInput()   // sets _inputLocked = true, zeroes rb velocity
public void UnlockInput() // sets _inputLocked = false

From Assets/Scripts/Player/CombatController.cs (unchanged, lines 176-185):
public void ForceExitCombatState() // ExitSlowMotion() + ExitAttackPending(); must be called BEFORE LockInput()

From Assets/Scripts/Camera/CameraBound.cs / CameraFollow.cs (unchanged):
public Bounds GetWorldBounds()          // CameraBound
public void SnapToRoom(Bounds worldBounds)  // CameraFollow
public void SnapToRoom(Vector3 worldCenter) // CameraFollow

Current Assets/Scripts/World/WorldGenerator.cs relevant excerpt (Phase 9, 183 lines -- read the full file before editing):
[Header("Next Floor Standby (Phase 10 연동)")]
[SerializeField] private float _floorHeight = 40f;
...
private GameObject _nextFloorRoom;    // D-05: 다음 층 대기룸 (체인 외부 참조) -- THIS PLAN REMOVES this field, replaced by per-portal ExitPortal.StandbyRoom (Pitfall 4)
...
public void SpawnNextFloorStandbyRoom()
{
    Debug.Log("[WorldGenerator] SpawnNextFloorStandbyRoom — stub (Phase 10에서 구현)");
} // THIS PLAN REPLACES this stub entirely with TrySpawnExitPortal()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EXIT-01/EXIT-02 -- 포탈 스폰 필드 + TrySpawnExitPortal() + RemoveTail() D-08 정리</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (수정 대상 -- 현재 183줄 전체 상태 확인 필수)
    - Assets/Scripts/World/ExitPortal.cs (StandbyRoom 프로퍼티, Plan 01 출력)
    - Assets/Scripts/World/ExitSpawnPoint.cs (GetComponentsInChildren 대상, Plan 01 출력)
  </read_first>
  <action>
`Assets/Scripts/World/WorldGenerator.cs`를 다음과 같이 수정한다.

**1. 필드 추가** -- 기존 `[Header("Next Floor Standby (Phase 10 연동)")]` 블록(`_floorHeight` 필드) 바로 뒤에 삽입:

```csharp
    [Header("Exit Portal (Phase 10)")]
    [SerializeField] private GameObject _exitPortalPrefab;
    [SerializeField, Range(0f, 1f)] private float _exitSpawnChance = 0.15f;  // EXIT-01: 기본 15%
    [SerializeField] private int _maxExitsActive = 1;                        // EXIT-02: 최대 동시 활성 개수

    [Header("References (Phase 10 추가)")]
    [SerializeField] private PlayerController _player;           // LockInput/UnlockInput 호출용
    [SerializeField] private CombatController _combatController; // ForceExitCombatState 호출용
```

**2. Runtime state 교체** -- 기존 `private GameObject _nextFloorRoom;    // D-05: 다음 층 대기룸 (체인 외부 참조)` 라인을 삭제하고 다음으로 교체 (Pitfall 4: 단일 전역 필드는 `_maxExitsActive > 1`일 때 두 번째 포탈이 첫 번째 것을 덮어쓴다 -- per-portal `ExitPortal.StandbyRoom`으로 대체):

```csharp
    private int _activeExitCount;         // D-08: 현재 활성 포탈 수
```

**3. Awake() + Instance 싱글톤 추가** -- `Start()` 메서드 바로 앞에 삽입 (WorldGenerator에는 현재 Awake()가 없음):

```csharp
    public static WorldGenerator Instance { get; private set; }

    private void Awake()
    {
        Instance = this;
    }
```

**4. Start()에 포탈 롤 추가** -- 기존 `_chain.Add((startRoom, null)); // D-09: 첫 룸은 왼쪽 Corridor 없음` 라인 바로 뒤에 추가:

```csharp
        TrySpawnExitPortal(startRoom); // EXIT-01: 시작 룸도 포탈 스폰 대상 (예외 없음)
```

**5. SpawnNextPair()에 포탈 롤 추가** -- 기존 `AlignByEntry(room, roomEntryPos);` 라인(Room 정렬 직후) 바로 뒤에 추가:

```csharp
        TrySpawnExitPortal(room);
```

**6. 스텁 메서드 전체 교체** -- 기존 `SpawnNextFloorStandbyRoom()` 메서드 전체(주석 포함)를 아래 `TrySpawnExitPortal()`로 완전히 대체한다:

```csharp
    /// <summary>
    /// EXIT-01/EXIT-02: Room 스폰 직후 호출된다. 확률 롤을 통과하고 활성 포탈 수가
    /// _maxExitsActive 미만이면, room의 ExitSpawnPoint 후보 중 하나에 포탈을 생성하고
    /// 다음 층 대기룸을 즉시(비활성 상태로) 함께 스폰해 D-08 정리 대상으로 연결한다.
    /// </summary>
    private void TrySpawnExitPortal(GameObject room)
    {
        if (_activeExitCount >= _maxExitsActive) return;
        if (Random.value > _exitSpawnChance) return;

        if (_exitPortalPrefab == null)
        {
            Debug.LogWarning("[WorldGenerator] _exitPortalPrefab is empty — Inspector에서 할당 필요");
            return;
        }

        var points = room.GetComponentsInChildren<ExitSpawnPoint>(true);
        if (points.Length == 0) return; // ExitSpawnPoint 마커 미배치 (D-03 수동 배치 대기)

        var point = points[Random.Range(0, points.Length)];
        var portalGO = Instantiate(_exitPortalPrefab, point.transform.position, Quaternion.identity, room.transform);
        var portal = portalGO.GetComponent<ExitPortal>();

        // D-04: 다음 층 대기룸을 지금 미리 스폰 — Vector3.zero 기준 Instantiate 후 X=0 고정 배치
        // (AlignByEntry는 적용 불가 — D-07이 옛 체인을 전부 파괴하므로 수평 연속성 요구가 없다)
        var standbyPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
        var standbyPos = new Vector3(0f, _chainHeadExitPos.y + _floorHeight, 0f);
        var standbyRoom = Instantiate(standbyPrefab, standbyPos, Quaternion.identity);
        standbyRoom.SetActive(false);
        portal.StandbyRoom = standbyRoom;

        _activeExitCount++;
        Debug.Log($"[WorldGenerator] Portal spawned in {room.name}");
        Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
    }
```

**7. RemoveTail() 수정** -- 기존 메서드를 아래 내용으로 교체 (D-08: 정리 대상 room이 미사용 포탈을 갖고 있으면 그 대기룸도 함께 Destroy):

```csharp
    private void RemoveTail()
    {
        var (room, corridor) = _chain[0];

        // D-08: 이 room이 보유한 포탈의 대기룸을 함께 정리 — 대기룸 메모리 누수 방지 + 포탈 스폰 기회 복원
        ExitPortal portal = room.GetComponentInChildren<ExitPortal>(true);
        if (portal != null && portal.StandbyRoom != null)
        {
            Destroy(portal.StandbyRoom);
            _activeExitCount--;
            Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");
        }

        if (corridor != null) Destroy(corridor);
        Destroy(room);
        _chain.RemoveAt(0);
    }
```

금지 사항 (기존 컨벤션 유지):
- `FindObjectsOfType` 금지 (ROADMAP Stack Constraints)
- `TrySpawnExitPortal` 내 Instantiate는 반드시 스폰 위치를 미리 계산한 `point.transform.position`/`standbyPos`로 직접 지정 — `AlignByEntry()` 호출 금지 (Pitfall — Anti-Pattern, 대기룸은 수평 연속성 대상이 아님)
- `EnemySpawner.Spawn()`/`.Activate()` 호출 추가 금지 — 적 스폰 배선은 이 Phase 범위 밖 (Pitfall 5)
  </action>
  <verify>
    <automated>grep -n "_exitSpawnChance\|_maxExitsActive\|_activeExitCount\|public static WorldGenerator Instance\|private void Awake\|TrySpawnExitPortal\|GetComponentInChildren<ExitPortal>" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "[SerializeField, Range(0f, 1f)] private float _exitSpawnChance = 0.15f;" 라인이 존재한다
    - "[SerializeField] private int _maxExitsActive = 1;" 라인이 존재한다
    - "public static WorldGenerator Instance { get; private set; }" 라인이 존재한다
    - "private void Awake()" 메서드가 존재하고 본문에 "Instance = this;"를 포함한다
    - "private void TrySpawnExitPortal(GameObject room)" 메서드가 존재한다
    - TrySpawnExitPortal 본문에 "if (_activeExitCount >= _maxExitsActive) return;"과 "if (Random.value > _exitSpawnChance) return;" 가드가 모두 존재한다
    - TrySpawnExitPortal 본문에 `Debug.Log($"[WorldGenerator] Portal spawned in {room.name}");`와 `Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");`가 존재한다 (Wave 0 요구사항)
    - "private GameObject _nextFloorRoom;" 라인이 더 이상 존재하지 않는다 (Pitfall 4 제거 확인)
    - "public void SpawnNextFloorStandbyRoom()" 메서드가 더 이상 존재하지 않는다 (스텁 완전 교체 확인)
    - RemoveTail() 본문에 "GetComponentInChildren<ExitPortal>(true)" 호출과 "_activeExitCount--"가 존재한다
    - Start() 본문에 "TrySpawnExitPortal(startRoom);" 호출이 존재한다
    - SpawnNextPair() 본문에 "TrySpawnExitPortal(room);" 호출이 존재한다
  </acceptance_criteria>
  <done>WorldGenerator가 싱글톤 Instance를 가지며, Room 스폰 시점(Start/SpawnNextPair)마다 확률 기반 포탈 스폰(EXIT-01)과 최대 활성 개수 제한(EXIT-02)을 수행하고, GEN-02 lookbehind 정리 시 미사용 포탈의 대기룸을 함께 Destroy해 D-08을 충족한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EXIT-03 -- EnterPortal() + FloorTransitionSequence() 6단계 코루틴</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (Task 1 완료 후 현재 상태 -- 삽입 위치 확인)
    - Assets/Scripts/World/FloorSpawner.cs (6단계 FloorTransitionSequence() 원본 -- 이식 대상)
    - Assets/Scripts/World/RoomEntry.cs, Assets/Scripts/Camera/CameraBound.cs, Assets/Scripts/Camera/CameraFollow.cs (ENT 텔레포트 + 카메라 스냅 참조)
  </read_first>
  <action>
**1. using 문 추가** -- 파일 최상단 `using System.Collections.Generic;` 바로 앞에 삽입:

```csharp
using System.Collections;
```

**2. EnterPortal() + FloorTransitionSequence() 추가** -- `UpdatePlayerIndex()` 메서드 뒤, 클래스 닫힘 브레이스(`}`) 바로 앞에 삽입:

```csharp
    /// <summary>
    /// EXIT-03: ExitPortal.OnTriggerEnter2D()가 호출한다. 전환 코루틴은 반드시 WorldGenerator(this)에서
    /// 실행되어야 한다 — 시퀀스 도중 옛 체인(포탈이 속한 room 포함)을 Destroy하기 때문에,
    /// ExitPortal 자신에게서 StartCoroutine을 호출하면 Destroy 시점에 코루틴이 즉시 중단된다 (Pitfall 1).
    /// </summary>
    public void EnterPortal(ExitPortal portal)
    {
        StartCoroutine(FloorTransitionSequence(portal));
    }

    private IEnumerator FloorTransitionSequence(ExitPortal portal)
    {
        Debug.Log($"[WorldGenerator] EnterPortal → Floor {FloorManager.CurrentFloor}");

        // Step 1 — 입력 잠금 (D-04 6단계 중 1단계). ForceExitCombatState는 LockInput 이전에 호출해야 한다.
        _combatController?.ForceExitCombatState();
        _player.LockInput();

        FloorManager.CurrentFloor++;

        // D-07 — 기존 체인(현재 room+corridor 전부) 즉시 Destroy
        // 리뷰 수정: 입장한 portal 외 다른 미사용 포탈의 대기룸도 함께 Destroy
        // (RemoveTail()의 D-08 정리 패턴과 동일 — _maxExitsActive > 1일 때 고아 GameObject 누수 방지)
        foreach (var (chainRoom, chainCorridor) in _chain)
        {
            var orphanPortal = chainRoom.GetComponentInChildren<ExitPortal>(true);
            if (orphanPortal != null && orphanPortal != portal && orphanPortal.StandbyRoom != null)
            {
                Destroy(orphanPortal.StandbyRoom);
            }

            if (chainCorridor != null) Destroy(chainCorridor);
            Destroy(chainRoom);
        }
        _chain.Clear();
        _activeExitCount = 0; // 옛 체인에 있던 모든 포탈(입장한 포탈 + 정리된 미사용 포탈)이 함께 사라짐
        Debug.Log($"[WorldGenerator] _activeExitCount = {_activeExitCount}");

        GameObject newRoom = portal.StandbyRoom;
        newRoom.SetActive(true);
        _chain.Add((newRoom, null));
        _playerCurrentIndex = 0;
        _currentYDrift = 0f; // 새 층은 드리프트 예산 초기화

        var exit = FindConnector(newRoom, RoomConnector.Direction.Right);
        _chainHeadExitPos = exit != null ? exit.transform.position : newRoom.transform.position;

        // Step 2 — ENT 텔레포트 (D-06: Complex_Room 6종 전부 RoomEntry 보유 — Plan 03에서 배치 완료 필요)
        RoomEntry entry = newRoom.GetComponentInChildren<RoomEntry>(true);
        Vector3 teleportPos = entry != null ? entry.transform.position : newRoom.transform.position;
        var rb = _playerTransform.GetComponent<Rigidbody2D>();
        if (rb != null) rb.linearVelocity = Vector2.zero;
        _playerTransform.position = teleportPos;

        // Step 3 — 카메라 스냅
        CameraBound cb = newRoom.GetComponentInChildren<CameraBound>(true);
        if (_cameraFollow != null)
        {
            if (cb != null) _cameraFollow.SnapToRoom(cb.GetWorldBounds());
            else _cameraFollow.SnapToRoom(teleportPos);
        }

        yield return null; // Step 3.5 — LateUpdate가 카메라 위치를 반영하도록 한 프레임 양보

        // Step 4 — 적 활성화: WorldGenerator는 현재 EnemySpawner.Spawn()을 호출하지 않으므로 의도적 no-op
        // (Pitfall 5 — 적 스폰 배선은 EXIT-01/02/03 범위 밖. 이 단계는 구조적 자리만 유지한다.)

        yield return new WaitForSecondsRealtime(0.05f); // Step 5

        _player.UnlockInput(); // Step 6
    }
```

금지 사항:
- `WaitForSeconds` 사용 금지 (반드시 `WaitForSecondsRealtime` — Time.timeScale이 슬로우모션 중일 수 있음)
- `_player.LockInput()`보다 `_combatController?.ForceExitCombatState()` 호출을 뒤에 두지 말 것 (순서 고정 — CombatController.cs:179 주석 근거)
- `GetComponentInChildren<RoomEntry>()` / `<CameraBound>()` 호출 시 `includeInactive: true` 생략 금지 (Pitfall 2 — standbyRoom은 SetActive(true) 직후이지만 방어적으로 항상 true 전달)
- 체인 teardown `foreach` 루프에서 `orphanPortal != portal` 검사 생략 금지 — 검사 없이 무조건 Destroy하면 입장한 포탈 자신의 `StandbyRoom`(이미 `newRoom`으로 활성화되어 사용 중)까지 파괴해버린다 (리뷰 이슈: cross_plan_data_contracts)
  </action>
  <verify>
    <automated>grep -n "using System.Collections;\|public void EnterPortal\|IEnumerator FloorTransitionSequence\|WaitForSecondsRealtime(0.05f)\|_player.UnlockInput()\|EnterPortal → Floor\|orphanPortal != portal" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "using System.Collections;" 라인이 파일 상단 using 블록에 존재한다
    - "public void EnterPortal(ExitPortal portal)" 메서드가 존재하고 본문이 "StartCoroutine(FloorTransitionSequence(portal));" 한 줄이다 (WorldGenerator 자신이 StartCoroutine 호출자)
    - "private IEnumerator FloorTransitionSequence(ExitPortal portal)" 메서드가 존재한다
    - 코루틴 본문에 `Debug.Log($"[WorldGenerator] EnterPortal → Floor {FloorManager.CurrentFloor}");`가 첫 실행문으로 존재한다 (Wave 0 요구사항, FloorManager.CurrentFloor++ 이전에 위치)
    - 코루틴 본문에 "FloorManager.CurrentFloor++;" 라인이 존재한다
    - 코루틴 본문에 `foreach (var (chainRoom, chainCorridor) in _chain)` 루프와 "_chain.Clear();"가 존재한다 (D-07 전체 체인 파괴)
    - 코루틴의 체인 teardown foreach 루프 본문에 "GetComponentInChildren<ExitPortal>(true)" 호출, "orphanPortal != portal" 조건, "Destroy(orphanPortal.StandbyRoom);" 호출이 모두 존재한다 (D-08: 입장하지 않은 다른 포탈의 대기룸 정리 — cross_plan_data_contracts 리뷰 이슈 수정)
    - 코루틴 본문에 "newRoom.GetComponentInChildren<RoomEntry>(true)"가 존재한다 (includeInactive: true 필수, Pitfall 2)
    - 코루틴 본문에 "yield return new WaitForSecondsRealtime(0.05f);"가 존재한다 ("yield return new WaitForSeconds(" 문자열은 존재하지 않아야 한다)
    - 코루틴 본문에 "_player.UnlockInput();"이 마지막 실행문으로 존재한다
    - "_combatController?.ForceExitCombatState();" 호출이 "_player.LockInput();" 호출보다 앞에 위치한다
  </acceptance_criteria>
  <done>WorldGenerator.EnterPortal()이 자기 자신(WorldGenerator)에서 6단계 전환 코루틴을 시작시키며, 코루틴은 FloorManager.CurrentFloor 증가 → 옛 체인 전체 Destroy(D-07, 미입장 포탈의 대기룸 정리 포함) → 대기룸 활성화 → ENT 텔레포트 → 카메라 스냅 → (적 활성화 no-op) → 입력 잠금 해제 순서로 실행된다. EXIT-01/02/03 요구사항이 코드 레벨에서 모두 충족되며, `_maxExitsActive > 1`인 경우에도 고아 StandbyRoom GameObject가 남지 않는다.</done>
</task>

</tasks>

<verification>
grep -c "private void\|public void\|private IEnumerator" Assets/Scripts/World/WorldGenerator.cs 결과가 Phase 9 대비 증가해야 한다 (Awake, TrySpawnExitPortal, EnterPortal, FloorTransitionSequence 4개 신규 추가).
Unity Editor에서 파일을 열었을 때 컴파일 에러가 0개여야 한다 (Plan 01의 ExitPortal.cs/ExitSpawnPoint.cs가 이미 존재하므로 이 시점부터 전체 빌드가 통과해야 정상).
</verification>

<success_criteria>
- WorldGenerator.cs가 컴파일 에러 없이 EXIT-01/02/03을 모두 구현한다
- TrySpawnExitPortal()이 확률 롤 + 최대 활성 개수 제한을 정확히 적용한다
- RemoveTail()이 D-08(미사용 포탈의 대기룸 정리)을 수행한다
- FloorTransitionSequence()가 WorldGenerator 자신에서 실행되어 D-07(체인 파괴) 도중에도 중단되지 않는다
- FloorTransitionSequence()가 `_maxExitsActive > 1`인 경우에도 입장하지 않은 다른 포탈의 대기룸을 정리해 고아 GameObject를 남기지 않는다 (D-08, 리뷰 수정)
- Debug.Log 3종(Portal spawned, _activeExitCount, EnterPortal → Floor)이 모두 소스에 존재한다 (10-VALIDATION.md Wave 0 요구사항)
</success_criteria>

<output>
완료 후 `.planning/phases/10-exit-portal-floor-transition/10-02-SUMMARY.md` 생성:
- 수정된 WorldGenerator.cs의 총 라인 수 변화 (Phase 9 183줄 대비)
- 추가된 메서드 목록 (Awake, TrySpawnExitPortal, EnterPortal, FloorTransitionSequence)
- 제거된 요소 (SpawnNextFloorStandbyRoom 스텁, _nextFloorRoom 필드) 및 대체 설계(D-08 per-portal StandbyRoom) 근거
- Task 1/2 verify 결과 (grep 출력 요약)
- Plan 01의 컴파일 에러(WorldGenerator.Instance 미정의)가 이 Plan 완료로 해소되었는지 확인 결과
- Task 2 chain teardown 루프의 orphan portal 정리 코드(`orphanPortal != portal` 검사) 추가 확인 결과
</output>
