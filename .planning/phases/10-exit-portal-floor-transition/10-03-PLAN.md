---
phase: 10-exit-portal-floor-transition
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
  - Assets/Prefabs/World/ExitPortal/ExitPortal.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_EdgeRun/Room_EdgeRun.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_LastStand/Room_LastStand.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_RiskCrossing/Room_RiskCrossing.prefab
  - Assets/Prefabs/Rooms/Complex_Room/Room_Vertical_Gauntlet/Room_Vertical_Gauntlet.prefab
autonomous: false
requirements:
  - EXIT-01
  - EXIT-03

must_haves:
  truths:
    - "Assets/Prefabs/World/ExitPortal/ExitPortal.prefab이 실제로 존재하고 BoxCollider2D(isTrigger)+ExitPortal 컴포넌트를 갖는다"
    - "Complex_Room 6종 전부에 ExitSpawnPoint 자식 오브젝트가 2~3개씩 존재한다 (D-01, D-02)"
    - "WorldGenerator.cs의 FloorTransitionSequence()가 RoomEntry 대신 ExitSpawnPoint 중 랜덤 1개로 텔레포트한다 (10-TRANSITION-DESIGN.md 결정)"
  artifacts:
    - path: "Assets/Prefabs/World/ExitPortal/ExitPortal.prefab"
      provides: "ExitPortalBuilder.Run() 실행 산출물 — 실제 포탈 프리팹"
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "FloorTransitionSequence()가 ExitSpawnPoint[] 중 랜덤 위치로 텔레포트 (RoomEntry 조회 제거)"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_AllInOne/Room_AllInOne.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_EdgeRun/Room_EdgeRun.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_LastStand/Room_LastStand.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_Vertical_Gauntlet/Room_Vertical_Gauntlet.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
    - path: "Assets/Prefabs/Rooms/Complex_Room/Room_RiskCrossing/Room_RiskCrossing.prefab"
      provides: "ExitSpawnPoint 2~3개 추가됨"
  key_links:
    - from: "Complex_Room 6종 자식 오브젝트"
      to: "ExitSpawnPoint 컴포넌트"
      via: "수동 AddComponent"
      pattern: "ExitSpawnPoint"
    - from: "WorldGenerator.FloorTransitionSequence()"
      to: "newRoom의 ExitSpawnPoint[]"
      via: "GetComponentsInChildren<ExitSpawnPoint>(true) + Random.Range"
      pattern: "ExitSpawnPoint"
---

<objective>
Plan 01에서 만든 `ExitPortalBuilder` 에디터 도구를 Unity Editor에서 실제로 실행해 `ExitPortal.prefab`을 생성하고, D-01/D-02/D-03 결정에 따라 Complex_Room 6종 프리팹에 `ExitSpawnPoint` 마커(룸당 2~3개)를 사용자가 Unity Editor에서 직접 수동으로 추가한다. 아울러 `WorldGenerator.FloorTransitionSequence()`의 ENT 텔레포트 로직을 `RoomEntry` 조회에서 `ExitSpawnPoint` 랜덤 선택으로 교체한다 (10-TRANSITION-DESIGN.md 결정 — RoomEntry 별도 마커가 더는 필요 없음).

Purpose: D-03이 명시적으로 자동 배치 도구를 금지한다 — 포탈 스폰 위치와 텔레포트 지점은 각 룸의 실제 지형(바닥, 낙사 구간, 장애물)을 육안으로 확인하며 배치해야 안전한 위치가 보장된다. 이 작업 없이는 Plan 02의 `TrySpawnExitPortal()`이 `points.Length == 0`으로 항상 조용히 스킵된다. 기존 계획은 ENT 텔레포트를 위해 `RoomEntry` 마커를 4개 룸에 별도로 추가해야 했으나, `ExitSpawnPoint`가 이미 바닥 위 안전한 위치이므로 이를 재사용하면 중복 마커 배치 없이 동일한 낙사 방지 효과를 얻는다 (todo `2026-07-03-complex-room-ent.md`가 지적한 허공 스폰 버그의 근본 원인 해결).
Output: 실제 `ExitPortal.prefab` 1개 + `ExitSpawnPoint` 마커가 추가된 Complex_Room 프리팹 6개 + ExitSpawnPoint 기반으로 교체된 `FloorTransitionSequence()`.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-exit-portal-floor-transition/10-CONTEXT.md
@.planning/phases/10-exit-portal-floor-transition/10-RESEARCH.md
@.planning/phases/08-room-corridor-architecture/08-EDITOR-CHECKLIST.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: FloorTransitionSequence를 ExitSpawnPoint 기반 텔레포트로 교체</name>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (FloorTransitionSequence(), 약 249~309행)
    - Assets/Scripts/World/ExitSpawnPoint.cs (Plan 01 산출물 — 컴포넌트 형태 확인)
    - .planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md (섹션 1 — 결정 근거)
  </read_first>
  <what-built>
`FloorTransitionSequence()`의 Step 2(ENT 텔레포트)가 `RoomEntry` 단일 마커를 조회하던 것을 `ExitSpawnPoint[]` 중 랜덤 1개를 선택하도록 교체한다. `RoomEntry` 조회 및 관련 using 참조는 제거하되 `RoomEntry.cs` 파일 자체는 삭제하지 않는다 (참조만 제거, 파일 보존).

변경 전:
```csharp
// Step 2 — ENT 텔레포트 (D-06: Complex_Room 6종 전부 RoomEntry 보유 — Plan 03에서 배치 완료 필요)
RoomEntry entry = newRoom.GetComponentInChildren<RoomEntry>(true);
Vector3 teleportPos = entry != null ? entry.transform.position : newRoom.transform.position;
```

변경 후:
```csharp
// Step 2 — ExitSpawnPoint 텔레포트 (RoomEntry 대체 — 포탈 스폰과 동일 마커 재사용, 10-TRANSITION-DESIGN.md)
var spawnPoints = newRoom.GetComponentsInChildren<ExitSpawnPoint>(true);
Vector3 teleportPos = spawnPoints.Length > 0
    ? spawnPoints[Random.Range(0, spawnPoints.Length)].transform.position
    : newRoom.transform.position;
```
  </what-built>
  <how-to-verify>
1. `Assets/Scripts/World/WorldGenerator.cs`에서 Step 2 코드가 위 "변경 후" 내용과 일치하는지 확인
2. 파일 내 `RoomEntry` 문자열 참조가 남아있지 않은지 grep으로 확인 (using 문, 변수 선언 등)
3. Unity 컴파일 에러 없음 (Console 확인 — 사용자에게 확인 요청 불필요, 코드 정적 검토로 충분)
  </how-to-verify>
  <acceptance_criteria>
    - WorldGenerator.cs의 FloorTransitionSequence()가 ExitSpawnPoint[]에서 Random.Range로 위치를 선택한다
    - WorldGenerator.cs에 RoomEntry 참조가 더 이상 없다
  </acceptance_criteria>
  <done>층 전환 시 ENT 텔레포트가 RoomEntry 없이 ExitSpawnPoint만으로 동작한다 — Task 3에서 6개 룸 전부에 이미 ExitSpawnPoint를 배치하므로 추가 RoomEntry 마커 배치가 불필요해진다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: ExitPortal 프리팹 빌드 실행</name>
  <read_first>
    - Assets/Editor/ExitPortalBuilder.cs (Plan 01에서 생성 — 메뉴 경로 확인)
  </read_first>
  <what-built>
Plan 01에서 `ExitPortalBuilder.cs` 에디터 스크립트가 생성되었다. 이제 Unity Editor 메뉴에서 실행해 실제 `ExitPortal.prefab`을 만든다.
  </what-built>
  <how-to-verify>
1. Unity Editor를 열고 컴파일이 완료될 때까지 기다린다 (Console에 에러가 없어야 한다 — Plan 01+02가 모두 완료되어 있어야 컴파일이 통과함)
2. 상단 메뉴에서 `Fast > Phase10 > Build Exit Portal Prefab` 클릭
3. Console 창(Window > General > Console)에서 다음 로그를 확인한다:
   - `[ExitPortalBuilder] ExitPortal built at Assets/Prefabs/World/ExitPortal/ExitPortal.prefab`
   - Error/Warning이 없어야 한다
4. Project 창에서 `Assets/Prefabs/World/ExitPortal/ExitPortal.prefab`이 존재하는지 확인한다
5. 프리팹을 더블클릭해 프리팹 편집 모드로 진입 → Inspector에서 `Box Collider 2D` (Is Trigger 체크됨, Size (1.5, 2.5))와 `Exit Portal (Script)` 컴포넌트가 부착되어 있는지 확인
  </how-to-verify>
  <resume-signal>
"완료" — 프리팹이 생성되고 컴포넌트가 확인됨. 또는 문제 설명 (컴파일 에러, 메뉴가 보이지 않음 등).
  </resume-signal>
  <acceptance_criteria>
    - Console에 "[ExitPortalBuilder] ExitPortal built at Assets/Prefabs/World/ExitPortal/ExitPortal.prefab" 로그가 출력되었다
    - Assets/Prefabs/World/ExitPortal/ExitPortal.prefab 파일이 Project 창에 존재한다
    - 프리팹의 BoxCollider2D가 Is Trigger = true, Size = (1.5, 2.5)이다
    - 프리팹에 Exit Portal (Script) 컴포넌트가 부착되어 있다
  </acceptance_criteria>
  <done>ExitPortal.prefab이 Assets/Prefabs/World/ExitPortal/ 아래에 실제로 생성되어 Plan 02의 WorldGenerator._exitPortalPrefab 필드에 연결할 수 있는 상태가 되었다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Complex_Room 6종에 ExitSpawnPoint 마커 수동 배치</name>
  <read_first>
    - Assets/Prefabs/Rooms/Complex_Room/Room_GaugeOutpost/Room_GaugeOutpost.prefab (기존 EnemySpawn 배치 예시 — 바닥 위치 참고용)
  </read_first>
  <what-built>
D-01/D-02/D-03에 따라 자동 배치 도구 없이 사용자가 직접 Unity Editor에서 각 Complex_Room 프리팹을 편집 모드로 열어 ExitSpawnPoint 마커를 추가한다. Task 1에서 텔레포트 로직이 이미 ExitSpawnPoint를 재사용하도록 바뀌었으므로 RoomEntry 마커는 더 이상 필요 없다.
  </what-built>
  <how-to-verify>

**공통 절차 (6개 프리팹 각각 반복):**

1. Project 창에서 `Assets/Prefabs/Rooms/Complex_Room/{RoomName}/{RoomName}.prefab`을 더블클릭해 프리팹 편집 모드로 진입
2. 씬 뷰에서 룸의 바닥/지형을 육안으로 확인한다 (기존 `EnemySpawns` 하위 `EnemySpawn_*` 오브젝트 위치를 참고하면 안전한 바닥 지점을 빠르게 찾을 수 있다)
3. Hierarchy에서 빈 부모 오브젝트 `ExitSpawnPoints`를 생성한다 (`EnemySpawns` 그룹과 동일한 컨벤션)
4. 그 아래 빈 자식 오브젝트 2~3개를 생성한다 (예: `ExitSpawn_0`, `ExitSpawn_1`, `ExitSpawn_2`) — 각각 바닥 위 안전한 위치(낙사 구간/장애물 회피)로 Transform Position을 이동
5. 각 `ExitSpawn_*` 오브젝트에 `Add Component > Exit Spawn Point` 부착
6. 프리팹 저장 (Ctrl+S 또는 편집 모드 종료 시 저장 프롬프트에서 저장)

**대상 6개 프리팹 전체 목록:** Room_AllInOne, Room_EdgeRun, Room_GaugeOutpost, Room_LastStand, Room_RiskCrossing, Room_Vertical_Gauntlet — 전부 동일하게 ExitSpawnPoint 2~3개만 배치한다 (RoomEntry 보유 여부와 무관하게 동일 절차).

**전체 완료 후 확인:**
7. 6개 프리팹 각각을 다시 열어 Hierarchy에 `ExitSpawnPoints` 그룹과 그 자식 2~3개가 `Exit Spawn Point (Script)` 컴포넌트와 함께 존재하는지 확인
  </how-to-verify>
  <resume-signal>
"완료" — 6개 프리팹 모두 마커 배치 및 저장 완료. 또는 어느 프리팹에서 문제가 있었는지 구체적으로 설명.
  </resume-signal>
  <acceptance_criteria>
    - Complex_Room 6종 모두에 `ExitSpawnPoints` 하위 2~3개 `ExitSpawnPoint` 컴포넌트 자식이 존재한다
    - 6개 프리팹 모두 저장되어 변경 사항이 디스크에 반영되었다
  </acceptance_criteria>
  <done>Complex_Room 6종 전부에 ExitSpawnPoint 마커가 배치되어 WorldGenerator.TrySpawnExitPortal()이 더 이상 조용히 스킵되지 않고, Task 1에서 교체된 텔레포트 로직이 동일 마커로 층 전환 시 허공 스폰 없이 동작할 수 있는 상태가 되었다.</done>
</task>

</tasks>

<verification>
6개 Complex_Room 프리팹 파일을 grep으로 스캔했을 때 각각 "ExitSpawnPoint" 문자열이 2회 이상(컴포넌트 참조 + m_Script 라인) 등장해야 한다.
WorldGenerator.cs를 grep으로 스캔했을 때 "RoomEntry" 문자열이 등장하지 않아야 한다.
</verification>

<success_criteria>
- ExitPortal.prefab이 실제로 존재하고 Plan 02의 WorldGenerator Inspector 연결에 사용 가능한 상태다
- Complex_Room 6종 전부에 ExitSpawnPoint 마커 2~3개가 존재한다 (EXIT-01 스폰 후보 겸 EXIT-03 텔레포트 대상)
- WorldGenerator.FloorTransitionSequence()가 RoomEntry 없이 ExitSpawnPoint만으로 텔레포트한다
</success_criteria>

<output>
완료 후 `.planning/phases/10-exit-portal-floor-transition/10-03-SUMMARY.md` 생성:
- ExitPortal.prefab 생성 결과 (Console 로그 확인 내용)
- WorldGenerator.cs FloorTransitionSequence 코드 변경 내용
- 6개 Complex_Room 프리팹별 추가된 ExitSpawnPoint 개수
- 배치 과정에서 발견된 문제 (있다면 — 예: 특정 룸에 안전한 바닥 지점이 부족했던 경우)
</output>
