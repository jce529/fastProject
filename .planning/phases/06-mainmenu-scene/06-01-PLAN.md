---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/MainMenuController.cs
  - Assets/Scenes/MainMenu.unity
autonomous: false
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
must_haves:
  truths:
    - "앱(에디터 Play) 실행 시 MainMenu.unity가 첫 화면으로 열린다 — SampleScene이 먼저 뜨지 않는다"
    - "Start 버튼 onClick이 MainMenuController.OnStartClicked()로 연결되고 SceneManager.LoadScene(\"AttackSelect\")를 호출한다"
    - "Quit 버튼 onClick이 MainMenuController.OnQuitClicked()로 연결되고 에디터에서 PlayMode를 종료한다"
    - "Build Settings에서 MainMenu.unity가 index 0이다"
  artifacts:
    - path: "Assets/Scripts/UI/MainMenuController.cs"
      provides: "OnStartClicked() → AttackSelect, OnQuitClicked() → 앱 종료"
    - path: "Assets/Scenes/MainMenu.unity"
      provides: "MainMenu 씬 파일 — EditorScript 재실행 후 갱신됨"
  key_links:
    - from: "MainMenuController.OnStartClicked"
      to: "AttackSelect 씬"
      via: "SceneManager.LoadScene(\"AttackSelect\")"
---

<objective>
MainMenuController.OnStartClicked()의 씬 대상을 "SampleScene"에서 "AttackSelect"로 변경하고, Unity EditorScript를 재실행해 MainMenu.unity를 최신 코드로 재빌드한다.

Purpose: MENU-02 충족 — Start 버튼이 AttackSelect 씬(Phase 7에서 구현)으로 진입하는 진입점이 된다.
Output: 수정된 MainMenuController.cs, 재생성된 MainMenu.unity (EditorScript 실행 후)
</objective>

<tasks>

<task id="1" type="auto">
  <name>MainMenuController.OnStartClicked() 수정</name>

  <read_first>
    - Assets/Scripts/UI/MainMenuController.cs — 현재 구현 확인 (OnStartClicked가 "SampleScene"을 로드 중)
    - Assets/Scripts/UI/DeathScreenController.cs — SceneManager.LoadScene() 사용 패턴 참조
  </read_first>

  <action>
Assets/Scripts/UI/MainMenuController.cs의 OnStartClicked() 메서드를 수정한다.

변경 전:
```csharp
public void OnStartClicked()
{
    SceneManager.LoadScene("SampleScene");
}
```

변경 후:
```csharp
public void OnStartClicked()
{
    SceneManager.LoadScene("AttackSelect");
}
```

OnQuitClicked(), using 선언, 클래스 구조 등 나머지는 일절 수정하지 않는다.
  </action>

  <acceptance_criteria>
    - Assets/Scripts/UI/MainMenuController.cs에 `SceneManager.LoadScene("AttackSelect")` 문자열이 존재한다
    - `SceneManager.LoadScene("SampleScene")` 문자열이 파일에 존재하지 않는다
    - `SceneManager.LoadScene("AttackSelect")` 호출이 OnStartClicked() 메서드 본문 내에 위치한다
  </acceptance_criteria>
</task>

<task id="2" type="checkpoint:human-verify" gate="blocking">
  <name>Unity Editor에서 MainMenu.unity 재빌드 및 Build Settings 검증</name>

  <read_first>
    - Assets/Editor/MainMenuSceneBuilder.cs — 씬 재생성 로직 및 Build Settings 업데이트 코드 확인
    - ProjectSettings/EditorBuildSettings.asset — 현재 Build Settings 순서 확인
  </read_first>

  <what-built>
    - MainMenuController.cs: OnStartClicked() → SceneManager.LoadScene("AttackSelect")
  </what-built>

  <how-to-verify>
    1. Unity Editor가 Task 1 코드 변경을 컴파일 완료할 때까지 대기 (하단 Progress Bar 사라질 때)
    2. 상단 메뉴 "Fast" > "Build MainMenu Scene" 클릭
    3. Assets/Scenes/MainMenu.unity 파일이 갱신(수정 날짜 변경)되는지 확인
    4. File > Build Settings 열어 MainMenu.unity가 index 0인지, SampleScene이 index 1인지 확인
    5. 에디터 Play 실행: MainMenu 씬이 첫 화면으로 열리는지 확인 (SampleScene이 뜨면 실패)
    6. Start 버튼 클릭: "Scene 'AttackSelect' couldn't be loaded" 에러(예상된 동작)가 Console에 나타나는지 확인 — 에러 메시지 자체가 MENU-02 코드 정확성을 증명함
    7. Play 재시작 후 Quit 버튼 클릭: Play 모드가 종료되는지 확인
  </how-to-verify>

  <acceptance_criteria>
    - Unity Editor Console에 컴파일 에러 없음
    - File > Build Settings에서 MainMenu.unity = index 0 확인
    - Play 실행 시 MainMenu 씬(타이틀 "Fast" + Start + Quit 버튼)이 첫 화면으로 열림
    - Start 버튼 클릭 시 Console에 "AttackSelect" 관련 씬 로드 에러(또는 성공) 로그 존재 — "SampleScene" 로드 시도면 실패
    - Quit 버튼 클릭 시 Play 모드 종료 확인
  </acceptance_criteria>

  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<verification>
- [ ] Assets/Scripts/UI/MainMenuController.cs에 SceneManager.LoadScene("AttackSelect") 존재
- [ ] Assets/Scripts/UI/MainMenuController.cs에 SceneManager.LoadScene("SampleScene") 미존재
- [ ] Unity Editor 컴파일 에러 없음
- [ ] "Fast/Build MainMenu Scene" 실행 후 Assets/Scenes/MainMenu.unity 갱신됨
- [ ] Build Settings: MainMenu.unity = index 0, SampleScene.unity = index 1
- [ ] 에디터 Play 시 MainMenu 씬이 첫 화면
- [ ] Start 버튼 → "AttackSelect" 씬 로드 시도 (에러 예상, 코드 정확성 확인)
- [ ] Quit 버튼 → Play 모드 종료
</verification>
