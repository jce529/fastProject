---
phase: 25-camera-system-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Camera/CameraFollow.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "카메라가 target을 즉시 스냅하지 않고 SmoothDamp로 부드럽게 추적한다"
    - "마우스 방향으로 화면 크기의 약 15~25%만큼 카메라가 선제 이동한다 (D-01/D-02), 리드 오프셋 자체는 독립 SmoothDamp(기본값 0=즉시반응)를 사용한다 (D-03)"
    - "SetAimLeadSuppressed(true)를 호출하면 리드 오프셋이 0으로 억제된다 (D-04 — 실제 대시 연동은 25-02에서 배선)"
    - "RequestDynamicZoom(distance, speed)를 호출하면 거리+속도 결합값에 비례해 orthographicSize가 최대 9까지 SmoothDamp로 확장되고, 줄어들 때(줌인)보다 넓어질 때(줌아웃)가 더 빠르다 (D-05/D-06/D-07)"
    - "ReleaseDynamicZoom()을 호출하면 줌 목표가 roomOrthoSize로 복귀해 줌인이 시작된다 (D-08)"
    - "target이 화면 좌우 바깥쪽 25% 구간(WorldToViewportPoint.x < 0.25 또는 > 0.75)에 진입할 때만 지수형으로 추적 SmoothTime이 줄어드는 텐션 캐치업이 개입한다 (D-09/D-10), 그 전에는 개입하지 않는다"
    - "SnapToRoom(Vector3)/SnapToRoom(Bounds)의 시그니처와 즉시 스냅 동작은 변경되지 않는다 (DebugSceneCameraBinder 회귀 방지)"
  artifacts:
    - path: "Assets/Scripts/Camera/CameraFollow.cs"
      provides: "SmoothDamp 추적 + 에임 리드 오프셋 + 텐션 캐치업 + 다이나믹 줌 파이프라인 전체"
      contains: "RequestDynamicZoom"
  key_links:
    - from: "CameraFollow.LateUpdate() 줌 SmoothDamp"
      to: "CameraFollow.LateUpdate() bounds-clamp 블록"
      via: "줌 SmoothDamp가 orthographicSize를 매 프레임 먼저 갱신한 뒤에만 clamp의 halfW/halfH 계산이 실행됨 (순서 고정, Pitfall 2)"
      pattern: "_camera\\.orthographicSize = Mathf\\.SmoothDamp"
    - from: "CameraFollow.SnapToRoom(Vector3|Bounds)"
      to: "CameraFollow._zoomTargetSize"
      via: "룸 전환 시 줌 목표를 roomOrthoSize로 즉시 리셋해 룸-to-룸 텔레포트 중 줌 크리프를 방지"
      pattern: "_zoomTargetSize = roomOrthoSize"
---

<objective>
`Assets/Scripts/Camera/CameraFollow.cs`의 현재 "즉시 스냅" LateUpdate를 SmoothDamp 기반 추적으로 재작성하고, 에임 리드 오프셋(D-01~D-04)·텐션 캐치업(D-09/D-10)·다이나믹 줌(D-05~D-08) 3개 시스템을 단일 LateUpdate 안에 25-RESEARCH.md가 권장한 고정 순서(줌 SmoothDamp → 리드+텐션+포지션 SmoothDamp → bounds clamp → shake)로 합성한다. 새 컴포넌트 타입 없이 기존 단일 파일에 필드/메서드만 추가한다 (CLAUDE.md 최소 표면적 원칙).

Purpose: 연쇄 대시 전투에서 화면 이탈 없이 속도감/타격감을 극대화하는 카메라 연출의 핵심 로직 전체를 이 Plan 하나로 완성해, 다음 Plan(25-02)이 OverclockModule.cs에서 대시 생명주기 이벤트만 배선하면 되도록 만든다.
Output: `CameraFollow.cs`에 3개 신규 public 훅(`SetAimLeadSuppressed`, `RequestDynamicZoom`, `ReleaseDynamicZoom`) + 재작성된 `LateUpdate()`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-camera-system-overhaul/25-CONTEXT.md
@.planning/phases/25-camera-system-overhaul/25-RESEARCH.md
@Assets/Scripts/Camera/CameraFollow.cs
@Assets/Scripts/Camera/CameraBound.cs
@Assets/Scripts/Debug/DebugSceneCameraBinder.cs

<interfaces>
현재 `CameraFollow.cs` 전체 (2026-08-07 확인, 96줄 — 이 Plan이 수정할 베이스라인):

```csharp
using UnityEngine;

public class CameraFollow : MonoBehaviour
{
    [SerializeField] private Transform target;
    [SerializeField] private Vector3 offset = new Vector3(0f, 1f, -10f);
    [SerializeField] private float roomOrthoSize = 7f;

    private bool _hasBounds;
    private Bounds _activeBounds;
    private Camera _camera;

    private float _shakeTimeRemaining;
    private float _shakeDurationTotal;
    private float _shakeAmplitude;

    private void Awake()
    {
        _camera = GetComponent<Camera>();
    }

    public void Shake(float duration, float amplitude) { /* unscaledDeltaTime 기반 — 변경 없음 */ }

    public void SnapToRoom(Vector3 worldCenter)
    {
        _hasBounds = false;
        transform.position = new Vector3(worldCenter.x, worldCenter.y, offset.z);
        if (_camera != null) _camera.orthographicSize = roomOrthoSize;
    }

    public void SnapToRoom(Bounds worldBounds)
    {
        _hasBounds = true;
        _activeBounds = worldBounds;
        if (_camera != null) _camera.orthographicSize = roomOrthoSize;
    }

    private void LateUpdate()
    {
        if (target == null) return;
        Vector3 desired = target.position + offset;

        if (_hasBounds && _camera != null)
        {
            float halfH = _camera.orthographicSize;
            float halfW = halfH * _camera.aspect;
            float x = _activeBounds.size.x <= halfW * 2f ? _activeBounds.center.x : Mathf.Clamp(desired.x, _activeBounds.min.x + halfW, _activeBounds.max.x - halfW);
            float y = _activeBounds.size.y <= halfH * 2f ? _activeBounds.center.y : Mathf.Clamp(desired.y, _activeBounds.min.y + halfH, _activeBounds.max.y - halfH);
            transform.position = new Vector3(x, y, offset.z);
        }
        else
        {
            transform.position = desired;
        }

        if (_shakeTimeRemaining > 0f) { /* additive shake — 변경 없음, 이 블록은 마지막에 그대로 유지 */ }
    }
}
```

`DebugSceneCameraBinder.cs`가 의존하는 정확한 계약 (변경 금지):
```csharp
_cameraFollow.SnapToRoom(bound.GetWorldBounds());   // SnapToRoom(Bounds)
_cameraFollow.SnapToRoom(_fallbackTarget.position); // SnapToRoom(Vector3)
```
두 오버로드의 시그니처(`SnapToRoom(Vector3 worldCenter)` / `SnapToRoom(Bounds worldBounds)`)와 "호출 즉시 스냅"(SmoothDamp 우회) 동작은 이 Plan 전체에서 byte-for-byte 유지되어야 한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 다이나믹 줌 시스템 추가 (D-05~D-08)</name>
  <files>Assets/Scripts/Camera/CameraFollow.cs</files>
  <read_first>
    - Assets/Scripts/Camera/CameraFollow.cs (현재 전체 — 위 interfaces 블록과 동일한 96줄 베이스라인)
    - .planning/phases/25-camera-system-overhaul/25-RESEARCH.md 의 "Pattern 4" + "Pitfall 2" 절
  </read_first>
  <action>
    **(A) 필드 추가** — `roomOrthoSize` 필드 바로 아래, `private bool _hasBounds;` 위에 삽입:
    ```csharp
    [Header("Dynamic Zoom (D-05~D-08)")]
    [SerializeField] private float maxZoomOrthoSize = 9f;       // D-06: roomOrthoSize(7) 대비 최대 확장 한계 — 룸 뷰 규격을 크게 벗어나지 않는 범위
    [SerializeField] private float zoomOutSmoothTime = 0.1f;    // D-07: 줌아웃(넓어짐) 빠르게
    [SerializeField] private float zoomInSmoothTime = 0.5f;     // D-07: 줌인(원상 복귀) 느리게
    [SerializeField] private float zoomDistanceReference = 8f;  // D-05: 이 거리(units) 이상이면 거리 팩터가 1.0으로 포화
    [SerializeField] private float zoomSpeedReference = 40f;    // D-05: 이 속도(units/sec) 이상이면 속도 팩터가 1.0으로 포화 — 대시거리(최대 10)/dashDuration(0.15s) 기준 최대 약 66u/s
    ```
    이어서 기존 `private bool _hasBounds;` 등 private 필드 블록 뒤에 추가:
    ```csharp
    private float _zoomTargetSize;
    private float _zoomVelocity;
    ```

    **(B) `Awake()` 수정** — `_camera = GetComponent<Camera>();` 다음 줄에 추가:
    ```csharp
    _zoomTargetSize = roomOrthoSize;
    ```

    **(C) 신규 public 메서드 2개 추가** — `Shake()` 메서드 다음, `SnapToRoom(Vector3)` 이전에 삽입:
    ```csharp
    /// <summary>D-05: 대시 거리+속도 결합으로 줌아웃 목표치를 계산해 요청한다.
    /// OverclockModule.Resolve()가 대시 이동(MovePosition) 완료 직후 호출한다 (25-02).
    /// Rigidbody2D.linearVelocity는 대시 중 0으로 고정되므로 사용하지 않는다 (Pitfall 1) — distance/DashDuration으로 미리 계산된 speed를 그대로 받는다.</summary>
    public void RequestDynamicZoom(float distance, float speed)
    {
        float distanceFactor = Mathf.Clamp01(distance / zoomDistanceReference);
        float speedFactor = Mathf.Clamp01(speed / zoomSpeedReference);
        float t = (distanceFactor + speedFactor) * 0.5f;
        _zoomTargetSize = Mathf.Lerp(roomOrthoSize, maxZoomOrthoSize, t);
    }

    /// <summary>D-08: 히트프리즈(OverclockModule.HitFreeze) 종료 직후 호출 — 줌 목표치를 룸 기본값으로 되돌려 줌인을 시작시킨다.</summary>
    public void ReleaseDynamicZoom()
    {
        _zoomTargetSize = roomOrthoSize;
    }
    ```

    **(D) `SnapToRoom(Vector3 worldCenter)`와 `SnapToRoom(Bounds worldBounds)` 양쪽 모두** — 기존 `if (_camera != null) _camera.orthographicSize = roomOrthoSize;` 줄 바로 앞에 한 줄 추가 (두 오버로드 각각):
    ```csharp
    _zoomTargetSize = roomOrthoSize; // 룸 전환 시 줌 목표 리셋 — 룸-to-룸 텔레포트 중 줌 크리프 방지
    ```
    두 오버로드의 기존 줄(`_hasBounds = ...;`, `if (_camera != null) _camera.orthographicSize = roomOrthoSize;`)은 그대로 유지 — 시그니처와 즉시 스냅 동작 변경 없음.

    **(E) `LateUpdate()`의 맨 첫 줄** — `if (target == null) return;` 바로 다음에 줌 SmoothDamp 블록 삽입 (이 블록은 Task 2의 포지션/리드 로직보다 반드시 먼저 실행되어야 함 — Pitfall 2: bounds-clamp가 orthographicSize를 읽으므로):
    ```csharp
    // D-05~D-08: 줌 SmoothDamp — bounds clamp가 orthographicSize를 읽으므로 반드시 먼저 실행 (Pitfall 2)
    if (_camera != null)
    {
        float zoomSmoothTime = (_zoomTargetSize > _camera.orthographicSize) ? zoomOutSmoothTime : zoomInSmoothTime;
        _camera.orthographicSize = Mathf.SmoothDamp(_camera.orthographicSize, _zoomTargetSize, ref _zoomVelocity, zoomSmoothTime, Mathf.Infinity, Time.unscaledDeltaTime);
    }
    ```
    `Time.unscaledDeltaTime`을 6번째 인자로 명시 전달 — 프로젝트 전역 컨벤션(모든 카메라 이징은 HitFreeze의 Time.timeScale=0 면역이어야 함, CLAUDE.md/STATE.md). 이 블록 뒤에 이어지는 `Vector3 desired = target.position + offset;` 이하 기존 로직은 Task 2에서 재작성한다 — 이 Task에서는 손대지 않는다.
  </action>
  <verify>
    <automated>grep -n "RequestDynamicZoom\|ReleaseDynamicZoom\|maxZoomOrthoSize = 9f\|_zoomTargetSize = roomOrthoSize\|zoomSmoothTime" Assets/Scripts/Camera/CameraFollow.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_zoomTargetSize = roomOrthoSize" Assets/Scripts/Camera/CameraFollow.cs` 결과가 정확히 3 (Awake 1회 + SnapToRoom(Vector3) 1회 + SnapToRoom(Bounds) 1회)
    - `public void RequestDynamicZoom(float distance, float speed)` 시그니처가 존재한다
    - `public void ReleaseDynamicZoom()` 시그니처가 존재한다
    - `public void SnapToRoom(Vector3 worldCenter)`와 `public void SnapToRoom(Bounds worldBounds)` 시그니처가 변경 전과 byte-for-byte 동일하다 (grep으로 원본 문자열 그대로 존재 확인)
    - `_camera.orthographicSize = Mathf.SmoothDamp(` 호출이 `LateUpdate()` 본문의 `if (target == null) return;` 직후에 위치한다
    - 줌 SmoothDamp 호출의 마지막 인자가 `Time.unscaledDeltaTime`이다
  </acceptance_criteria>
  <done>CameraFollow에 D-05~D-08 다이나믹 줌 시스템(필드+훅 메서드+LateUpdate 통합)이 추가되고, SnapToRoom 양쪽 오버로드의 공개 계약은 변경되지 않는다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 에임 리드 오프셋 + 텐션 캐치업 + SmoothDamp 포지션 추적 (D-01~D-04, D-09~D-10)</name>
  <files>Assets/Scripts/Camera/CameraFollow.cs</files>
  <read_first>
    - Assets/Scripts/Camera/CameraFollow.cs (Task 1 적용 후 상태 — 줌 SmoothDamp 블록이 LateUpdate 맨 위에 이미 존재해야 함)
    - Assets/Scripts/Player/Combat/OverclockModule.cs 의 `GetMouseWorldDirection()` (§134-141) — 동일 패턴 재사용
    - .planning/phases/25-camera-system-overhaul/25-RESEARCH.md 의 "Pattern 1/2/3" 절
  </read_first>
  <action>
    **(A) 클래스 doc 주석 갱신** — 파일 최상단 주석 블록의 다음 줄:
    ```
    /// Direct LateUpdate camera follow -- no Cinemachine, no lead-ahead (per D-11, D-12, D-13).
    ```
    을 아래로 교체 (Phase 25에서 lead-ahead가 실제로 추가되므로 stale 주석 정정):
    ```
    /// LateUpdate 기반 카메라 추적 -- no Cinemachine(25-RESEARCH.md 결론, 자체 SmoothDamp 합성 채택).
    /// SmoothDamp 부드러운 추적 + 에임 리드 오프셋(D-01~D-04) + 텐션 캐치업(D-09~D-10) + 다이나믹 줌(D-05~D-08, Task 1)을 포함한다.
    ```

    **(B) 필드 추가** — Task 1에서 추가한 `Header("Dynamic Zoom...")` 필드 블록 위(=`roomOrthoSize` 필드 바로 아래)에 삽입:
    ```csharp
    [Header("SmoothDamp Tracking (Phase 25)")]
    [SerializeField] private float positionSmoothTime = 0.12f; // 메인 카메라 추적 SmoothDamp

    [Header("Aim Lead Offset (D-01~D-04)")]
    [SerializeField, Range(0.15f, 0.25f)] private float leadIntensity = 0.20f; // D-02: 화면 절반 너비 대비 리드 오프셋 비율 (15~25%)
    [SerializeField] private float leadSmoothTime = 0f; // D-03: 리드 오프셋 전용 SmoothDamp, 기본값 0 = 즉시 반응

    [Header("Tension Catch-up (D-09~D-10)")]
    [SerializeField] private float tensionExponent = 3f;        // D-10: 지수형 가속 곡선
    [SerializeField] private float minCatchUpSmoothTime = 0.03f; // 화면 경계 최대 이탈 시 가장 빠른(작은) SmoothTime
    ```
    private 필드 블록(Task 1의 `_zoomTargetSize`/`_zoomVelocity` 근처)에 추가:
    ```csharp
    private Vector2 _smoothedLead;
    private Vector2 _leadVelocity;
    private bool _leadSuppressed;
    private Vector3 _positionVelocity;
    ```

    **(C) 신규 public/private 메서드 추가** — `ReleaseDynamicZoom()` 다음, `SnapToRoom(Vector3)` 이전에 삽입:
    ```csharp
    /// <summary>D-04: 대시 시작/종료 시 OverclockModule.Resolve()가 호출해 리드 오프셋을 켜고 끈다 (25-02에서 배선).</summary>
    public void SetAimLeadSuppressed(bool suppressed)
    {
        _leadSuppressed = suppressed;
    }

    /// <summary>D-01/D-02: 마우스 방향 계산 — OverclockModule.GetMouseWorldDirection()(§134-141)과 동일 패턴,
    /// CombatContext 의존 없이 자체 Mouse.current + 캐시된 _camera만 사용 (항상 활성화된 리드 오프셋을 위해 전투 상태와 무관하게 동작).</summary>
    private Vector2 GetMouseWorldDirection()
    {
        var mouse = UnityEngine.InputSystem.Mouse.current;
        if (mouse == null || _camera == null || target == null) return Vector2.zero;
        Vector2 mouseScreen = mouse.position.ReadValue();
        Vector3 mouseWorld = _camera.ScreenToWorldPoint(new Vector3(mouseScreen.x, mouseScreen.y, Mathf.Abs(_camera.transform.position.z)));
        Vector2 dir = (Vector2)mouseWorld - (Vector2)target.position;
        return dir.sqrMagnitude > 0.001f ? dir.normalized : Vector2.zero;
    }

    /// <summary>D-02: 리드 오프셋 최대 크기 — 화면 절반 너비(orthoSize*aspect)의 leadIntensity(15~25%) 비율.</summary>
    private float ComputeLeadMagnitude()
    {
        if (_camera == null) return 0f;
        return _camera.orthographicSize * _camera.aspect * leadIntensity;
    }

    /// <summary>D-09/D-10: target이 화면 좌우 바깥쪽 25% 구간(viewport x &lt; 0.25 또는 &gt; 0.75)에 진입할 때만
    /// 개입하는 지수형 캐치업 — 개입폭(overshoot)에 비례해 positionSmoothTime을 minCatchUpSmoothTime까지 줄인다.</summary>
    private float ComputeTensionAdjustedSmoothTime()
    {
        if (_camera == null || target == null) return positionSmoothTime;
        Vector3 vp = _camera.WorldToViewportPoint(target.position);
        float overshoot = 0f;
        if (vp.x < 0.25f) overshoot = (0.25f - vp.x) / 0.25f;
        else if (vp.x > 0.75f) overshoot = (vp.x - 0.75f) / 0.25f;
        overshoot = Mathf.Clamp01(overshoot);
        if (overshoot <= 0f) return positionSmoothTime;

        float catchUpStrength = Mathf.Pow(overshoot, tensionExponent);
        return Mathf.Lerp(positionSmoothTime, minCatchUpSmoothTime, catchUpStrength);
    }
    ```

    **(D) `LateUpdate()` 재작성** — Task 1에서 삽입한 줌 SmoothDamp 블록은 그대로 맨 위에 유지하고, 그 다음에 이어지는 기존 `Vector3 desired = target.position + offset;` 부터 `if (_shakeTimeRemaining > 0f) { ... }` 블록 시작 전까지 전체를 아래로 교체:
    ```csharp
    Vector2 leadTarget = _leadSuppressed ? Vector2.zero : GetMouseWorldDirection() * ComputeLeadMagnitude();
    _smoothedLead = Vector2.SmoothDamp(_smoothedLead, leadTarget, ref _leadVelocity, leadSmoothTime, Mathf.Infinity, Time.unscaledDeltaTime);

    Vector3 desired = target.position + offset + (Vector3)_smoothedLead;
    float effectiveSmoothTime = ComputeTensionAdjustedSmoothTime();
    transform.position = Vector3.SmoothDamp(transform.position, desired, ref _positionVelocity, effectiveSmoothTime, Mathf.Infinity, Time.unscaledDeltaTime);

    if (_hasBounds && _camera != null)
    {
        float halfH = _camera.orthographicSize;
        float halfW = halfH * _camera.aspect;

        float x = _activeBounds.size.x <= halfW * 2f
            ? _activeBounds.center.x
            : Mathf.Clamp(transform.position.x, _activeBounds.min.x + halfW, _activeBounds.max.x - halfW);

        float y = _activeBounds.size.y <= halfH * 2f
            ? _activeBounds.center.y
            : Mathf.Clamp(transform.position.y, _activeBounds.min.y + halfH, _activeBounds.max.y - halfH);

        transform.position = new Vector3(x, y, offset.z);
    }
    ```
    기존의 `else { transform.position = desired; }` 분기는 제거한다 — `Vector3.SmoothDamp` 호출이 이미 무조건 `transform.position`을 갱신하므로 `_hasBounds`가 false일 때는 그 값이 그대로 유지되면 된다. `if (_shakeTimeRemaining > 0f) { ... }` 이하 shake 블록은 한 글자도 바꾸지 않고 그대로 마지막에 유지한다 (additive, 이 Plan에서 재조정하지 않음).
  </action>
  <verify>
    <automated>grep -n "SetAimLeadSuppressed\|WorldToViewportPoint\|Mathf.Pow(overshoot, tensionExponent)\|Vector3.SmoothDamp(transform.position, desired" Assets/Scripts/Camera/CameraFollow.cs && grep -c "transform.position = desired;" Assets/Scripts/Camera/CameraFollow.cs</automated>
  </verify>
  <acceptance_criteria>
    - `public void SetAimLeadSuppressed(bool suppressed)` 시그니처가 존재한다
    - `leadSmoothTime = 0f` 필드 초기값이 존재한다 (D-03 기본값 0)
    - `Range(0.15f, 0.25f)` 어트리뷰트가 `leadIntensity` 필드에 존재한다 (D-02 범위)
    - `WorldToViewportPoint` 호출이 `ComputeTensionAdjustedSmoothTime()` 내부에 존재한다 (D-09)
    - `Mathf.Pow(overshoot, tensionExponent)` 호출이 존재한다 (D-10 지수형 곡선)
    - `0.25f`와 `0.75f` 두 임계값이 모두 존재한다 (D-09 화면 좌우 바깥쪽 25% 구간)
    - `Vector3.SmoothDamp(transform.position, desired` 호출이 존재한다 (직접 대입이 아닌 SmoothDamp 기반 포지션 추적)
    - `grep -c "transform.position = desired;" Assets/Scripts/Camera/CameraFollow.cs` 결과가 0 (기존 즉시 대입 분기 제거 확인)
    - 클래스 최상단 doc 주석에 더 이상 "no lead-ahead"라는 문구가 없다
  </acceptance_criteria>
  <done>CameraFollow가 마우스 방향 리드 오프셋(D-01~D-04)과 화면 25% 경계 기반 지수형 텐션 캐치업(D-09/D-10)을 갖춘 SmoothDamp 포지션 추적으로 전환되고, Task 1의 줌 시스템과 함께 하나의 LateUpdate 파이프라인으로 합성된다.</done>
</task>

</tasks>

<verification>
- `Assets/Scripts/Camera/CameraFollow.cs` 컴파일 성공 (Unity 콘솔 에러 0건 — Task 2 완료 후 Unity Editor로 확인)
- `SnapToRoom(Vector3)`/`SnapToRoom(Bounds)` 두 오버로드 시그니처가 Plan 시작 전과 동일함 (grep 비교)
- `DebugSceneCameraBinder.cs`는 이 Plan에서 전혀 수정되지 않음 (grep diff 0)
- LateUpdate() 실행 순서가 연구 권장 순서(줌 SmoothDamp → 리드+텐션+포지션 SmoothDamp → bounds clamp → shake)를 따름 (코드 순서 육안 확인)
</verification>

<success_criteria>
- D-01~D-10 전체가 CameraFollow.cs의 public API(SetAimLeadSuppressed/RequestDynamicZoom/ReleaseDynamicZoom) 또는 내부 로직으로 구현된다
- OverclockModule.cs 등 다른 파일은 이 Plan에서 전혀 수정되지 않는다 (25-02가 담당)
- 25-02가 즉시 참조할 수 있는 안정된 public 훅 3종이 확정된다
</success_criteria>

<output>
After completion, create `.planning/phases/25-camera-system-overhaul/25-01-SUMMARY.md`
</output>
