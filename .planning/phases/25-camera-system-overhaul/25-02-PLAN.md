---
phase: 25-camera-system-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - Assets/Scripts/Player/Combat/OverclockModule.cs
autonomous: true
requirements: [D-04, D-05, D-08]

must_haves:
  truths:
    - "대시가 시작되는 순간 CameraFollow의 리드 오프셋이 억제된다 (D-04)"
    - "대시 이동(MovePosition)이 끝나는 순간 리드 오프셋이 재개되고, 실제 대시 거리+대시 시간으로 계산된 속도가 다이나믹 줌 트리거에 전달된다 (D-04/D-05)"
    - "HitFreeze가 끝난 직후 줌이 원상 복귀를 시작한다 (D-08)"
    - "대시 속도 신호는 Rigidbody2D.linearVelocity가 아니라 dashDistance/ctx.DashDuration으로 직접 계산된다 (Pitfall 1 회피)"
  artifacts:
    - path: "Assets/Scripts/Player/Combat/OverclockModule.cs"
      provides: "대시 생명주기(시작/이동완료/히트프리즈종료) 3개 지점에서 CameraFollow 훅 호출"
      contains: "RequestDynamicZoom(dashDistance, dashSpeed)"
  key_links:
    - from: "OverclockModule.Resolve()"
      to: "CameraFollow.SetAimLeadSuppressed / RequestDynamicZoom / ReleaseDynamicZoom"
      via: "ctx.CameraFollow?. 널 체이닝 — 기존 ctx.CameraFollow?.Shake(...) 호출과 동일한 계약(카메라 없어도 안전, DebugScene/테스트 대응)"
      pattern: "ctx\\.CameraFollow\\?\\.(SetAimLeadSuppressed|RequestDynamicZoom|ReleaseDynamicZoom)"
---

<objective>
`OverclockModule.Resolve()`(F.I.O.R.A 대시 처치 코루틴)의 3개 시점 — 대시 시작, 대시 이동 완료, HitFreeze 종료 — 에 Plan 25-01이 만든 `CameraFollow`의 신규 훅 3종을 배선한다. 대시 속도는 `Rigidbody2D.linearVelocity`(대시 중 0으로 고정됨, Pitfall 1)가 아니라 `dashDistance / ctx.DashDuration`으로 직접 계산해 전달한다.

Purpose: Plan 25-01에서 만든 카메라 로직(리드 억제/다이나믹 줌)이 실제 F.I.O.R.A 전투 루프와 연결되어야 D-04/D-05/D-08이 플레이 중 실제로 발동한다.
Output: `OverclockModule.cs`의 `Resolve()`에 카메라 훅 호출 4줄 추가(계산 2줄 + 호출 4줄) — 기존 로직/시그니처는 변경 없음.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-camera-system-overhaul/25-CONTEXT.md
@.planning/phases/25-camera-system-overhaul/25-RESEARCH.md
@Assets/Scripts/Player/Combat/OverclockModule.cs
@Assets/Scripts/Player/Combat/CombatContext.cs

<interfaces>
Plan 25-01이 `CameraFollow.cs`에 추가한 신규 public API (이 Plan이 그대로 호출):
```csharp
public void SetAimLeadSuppressed(bool suppressed)          // D-04
public void RequestDynamicZoom(float distance, float speed) // D-05
public void ReleaseDynamicZoom()                             // D-08
```

`OverclockModule.Resolve()` 현재 전체 (2026-08-07 확인, §53-100 — 이 Plan이 수정할 베이스라인):
```csharp
public IEnumerator Resolve(IEnemy target, CombatContext ctx)
{
    if (target == null)
    {
        Debug.LogError("[Combat] ExecuteDash: TARGET IS NULL at start! Aborting.");
        yield break;
    }

    Vector2 startPos    = ctx.Rb.position;
    Vector2 destination = (Vector2)((MonoBehaviour)target).transform.position;
    Vector2 dirToTarget = (destination - startPos).normalized;

    // 3. 대상 방향으로 스프라이트 전환
    ctx.SpriteRenderer.flipX = destination.x < startPos.x;

    // 4. Setup visual, animation, invincibility
    ctx.Animator?.SetBool("IsDashing", true);
    ctx.Invincibility.StartInvincibility(ctx.DashDuration + 0.05f);
    if (ctx.TrailRenderer != null) ctx.TrailRenderer.emitting = true;
    ctx.Rb.linearVelocity = Vector2.zero;

    // 5. 대시 이동 (smoothstep 보간으로 가속-감속 느낌)
    float elapsed = 0f;
    while (elapsed < ctx.DashDuration)
    {
        float t = elapsed / ctx.DashDuration;
        float smooth = t * t * (3f - 2f * t); // smoothstep
        ctx.Rb.MovePosition(Vector2.Lerp(startPos, destination, smooth));
        elapsed += Time.fixedDeltaTime;
        yield return new WaitForFixedUpdate();
    }
    ctx.Rb.MovePosition(destination);

    // 6. Cleanup visual and animation
    ctx.Animator?.SetBool("IsDashing", false);
    if (ctx.TrailRenderer != null) ctx.TrailRenderer.emitting = false;

    // 6. Kill and effects
    target.OnDashHit();
    AudioManager.PlaySfx(Sfx.Slash);
    SpawnHitSpark(destination, ctx);
    ctx.CameraFollow?.Shake(ctx.CameraShakeDuration, ctx.CameraShakeAmplitude);
    yield return HitFreeze(ctx.HitFreezeDuration);

    ctx.SetAttackCooldown(ctx.PostKillLockout);
    ctx.Gauge.AddKillBonus();
}
```
`ctx.CameraFollow`는 `CombatContext` 필드(nullable) — 기존 `Shake()` 호출과 동일하게 `?.` 널 체이닝으로 호출한다 (DebugScene/테스트 등 카메라가 없는 컨텍스트에서도 안전).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 대시 생명주기 3개 지점에 카메라 훅 배선 (D-04, D-05, D-08)</name>
  <files>Assets/Scripts/Player/Combat/OverclockModule.cs</files>
  <read_first>
    - Assets/Scripts/Player/Combat/OverclockModule.cs (현재 전체 — 위 interfaces 블록과 동일한 Resolve() 베이스라인)
    - Assets/Scripts/Camera/CameraFollow.cs (Plan 25-01 적용 후 — SetAimLeadSuppressed/RequestDynamicZoom/ReleaseDynamicZoom 시그니처 확인)
    - .planning/phases/25-camera-system-overhaul/25-RESEARCH.md 의 "Pitfall 1" + "Code Examples → Dash Integration Hook" 절
  </read_first>
  <action>
    **(A) 거리/속도 계산 추가** — `Vector2 dirToTarget = (destination - startPos).normalized;` 줄 바로 다음에 삽입:
    ```csharp
    float dashDistance = Vector2.Distance(startPos, destination);
    float dashSpeed    = ctx.DashDuration > 0f ? dashDistance / ctx.DashDuration : 0f; // Pitfall 1: Rigidbody2D.linearVelocity는 대시 중 0으로 고정되므로 사용 불가
    ```

    **(B) 대시 시작 시 리드 억제 (D-04)** — `// 5. 대시 이동 (smoothstep 보간으로 가속-감속 느낌)` 주석 바로 앞, `ctx.Rb.linearVelocity = Vector2.zero;` 다음 줄에 삽입:
    ```csharp
    ctx.CameraFollow?.SetAimLeadSuppressed(true); // D-04: 대시 시작과 동시에 리드 오프셋 해제
    ```

    **(C) 대시 이동 완료 시 리드 재개 + 줌 트리거 (D-04/D-05)** — `ctx.Rb.MovePosition(destination);`(while 루프 종료 직후, `// 6. Cleanup visual and animation` 주석 이전) 다음 줄에 삽입:
    ```csharp
    ctx.CameraFollow?.SetAimLeadSuppressed(false);                    // D-04: 대시 이동 완료 즉시 리드 오프셋 재개
    ctx.CameraFollow?.RequestDynamicZoom(dashDistance, dashSpeed);    // D-05: 실제 대시 거리+속도로 줌아웃 트리거
    ```

    **(D) 히트프리즈 종료 직후 줌 복귀 (D-08)** — `yield return HitFreeze(ctx.HitFreezeDuration);` 다음 줄, `ctx.SetAttackCooldown(ctx.PostKillLockout);` 이전에 삽입:
    ```csharp
    ctx.CameraFollow?.ReleaseDynamicZoom(); // D-08: 히트프리즈 종료 직후 줌인 시작
    ```

    다른 로직(스프라이트 플립, 애니메이터, 무적, 트레일, SFX, `target.OnDashHit()`, `Shake()` 호출, `Gauge.AddKillBonus()` 등)은 한 글자도 변경하지 않는다 — 순수 추가(additive) 변경만 수행한다.
  </action>
  <verify>
    <automated>grep -n "dashDistance\|dashSpeed\|SetAimLeadSuppressed\|RequestDynamicZoom\|ReleaseDynamicZoom" Assets/Scripts/Player/Combat/OverclockModule.cs</automated>
  </verify>
  <acceptance_criteria>
    - `float dashDistance = Vector2.Distance(startPos, destination);` 줄이 존재한다
    - `float dashSpeed    = ctx.DashDuration > 0f ? dashDistance / ctx.DashDuration : 0f;` 또는 동일한 삼항식이 존재한다 (Rigidbody2D.linearVelocity를 읽지 않음 — Pitfall 1 회피)
    - `ctx.CameraFollow?.SetAimLeadSuppressed(true)` 호출이 대시 이동 while 루프 이전에 위치한다
    - `ctx.CameraFollow?.SetAimLeadSuppressed(false)` 호출이 `ctx.Rb.MovePosition(destination);`(루프 종료 후 최종 위치 고정) 다음에 위치한다
    - `ctx.CameraFollow?.RequestDynamicZoom(dashDistance, dashSpeed)` 호출이 존재하고 인자로 `dashDistance`/`dashSpeed` 변수를 그대로 전달한다
    - `ctx.CameraFollow?.ReleaseDynamicZoom()` 호출이 `yield return HitFreeze(ctx.HitFreezeDuration);` 다음, `ctx.SetAttackCooldown(...)` 이전에 위치한다
    - `grep -c "ctx.Rb.linearVelocity" Assets/Scripts/Player/Combat/OverclockModule.cs` 결과가 변경 전과 동일 (기존 `= Vector2.zero;` 대입 1곳 외에 새로운 velocity 읽기가 추가되지 않음)
    - `Whiff()`, `HitFreeze()`, `SpawnHitSpark()`, `GetMouseWorldDirection()`, `IsInAttackShape()` 메서드는 이 Plan에서 전혀 수정되지 않는다 (grep diff 0)
  </acceptance_criteria>
  <done>F.I.O.R.A 대시 처치 흐름이 진행되는 동안 CameraFollow의 리드 억제/다이나믹 줌 훅이 정확한 시점(대시 시작/이동 완료/히트프리즈 종료)에 실제 대시 거리·속도 데이터와 함께 호출된다.</done>
</task>

</tasks>

<verification>
- `Assets/Scripts/Player/Combat/OverclockModule.cs` 컴파일 성공 (Unity 콘솔 에러 0건)
- `Resolve()` 외 다른 메서드(`FindTarget`, `Whiff`, `HitFreeze`, `SpawnHitSpark`, `GetMouseWorldDirection`, `IsInAttackShape`)는 diff 0
- `ctx.CameraFollow?.` 패턴이 기존 `Shake()` 호출 포함 총 4곳에 존재 (Shake 1 + 신규 3)
</verification>

<success_criteria>
- D-04(리드 억제)/D-05(줌 트리거)/D-08(줌 복귀 타이밍)이 실제 대시 생명주기와 연결된다
- Plan 25-01의 CameraFollow.cs는 이 Plan에서 전혀 수정되지 않는다 (파일 소유권 분리 유지)
</success_criteria>

<output>
After completion, create `.planning/phases/25-camera-system-overhaul/25-02-SUMMARY.md`
</output>
