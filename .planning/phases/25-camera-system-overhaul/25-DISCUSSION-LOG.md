# Phase 25: 카메라 시스템 고도화 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-07
**Phase:** 25-camera-system-overhaul
**Areas discussed:** 에임 리드 오프셋 적용 범위/강도, 다이나믹 줌아웃 트리거/세기, 텐션 캐치업 안전장치 개입 시점

---

## 에임 리드 오프셋 적용 범위/강도

| Option | Description | Selected |
|--------|-------------|----------|
| 슬로우모션(조준) 중만 (추천) | 공격 버튼을 눌러 슬로우모션이 발동한 조준 상태에서만 마우스 방향으로 카메라가 밀림 | |
| 항상 적용 | 평상시 이동 중에도 마우스가 향하는 방향으로 카메라가 은근히 따라 움직임 | ✓ |
| 조준+대시 전환 순간만 | 손을 떼는 순간(돌진 시작)에만 짧게 마우스 방향으로 카메라가 반응 | |

**User's choice:** 항상 적용

| Option | Description | Selected |
|--------|-------------|----------|
| 미묘하게 (화면의 약 5~10%) | 은근한 긴장감. 화면 이탈 위험이 낮고 안정적인 느낌 | |
| 뚜렷하게 (화면의 약 15~25%) | 마우스 방향으로 화면이 확실히 쏠려 다음 타겟 방향을 미리 보여줌. 더 영화적이지만 어지러울 수 있음 | ✓ |
| You decide | 리서처/플래너가 대시 속도·룸 크기 등을 고려해 적정값 산출 | |

**User's choice:** 뚜렷하게 (화면의 약 15~25%)

| Option | Description | Selected |
|--------|-------------|----------|
| SmoothDamp 감쇠 커브와 동일 (추천) | 카메라 추적 자체가 SmoothDamp이므로 리드 오프셋도 같은 감쇠 곡선을 타면 전체 카메라 움직임이 하나의 느낌으로 통일됨 | |
| 즉각 반응 (마우스 따라 스냅) | 마우스가 움직이면 오프셋이 지연 없이 즉시 반영 — 더 예민하고 반응성 있는 느낌이지만 추적 움직임과 별개로 튈 수 있음 | |
| You decide | 플레이어가 다른 결정들과 일관되게 조정 | |
| **(free text)** 일단 SmoothDamp를 만들어서 인스펙터에서 별도로 조정 가능하게 제작. 기본값은 0으로 | | ✓ |

**User's choice:** 리드 오프셋 전용 SmoothDamp 파라미터를 인스펙터 노출, 기본값 0(즉시 반응)
**Notes:** 카메라 위치 추적용 SmoothDamp와는 독립된 별도 파라미터로 구현.

| Option | Description | Selected |
|--------|-------------|----------|
| 대시 시작 시 리드 해제 (추천) | 돌진이 시작되면 오프셋을 끄고 카메라가 목적지(적) 방향으로 자연스럽게 재추적. 대시 자체의 다이나믹 줌/텐션 로직과 역할이 겹치지 않음 | ✓ |
| 대시 중에도 유지 | 돌진 중에도 마우스 방향 리드가 계속 더해져 다음 타겟을 미리 화면에 보여주는 느낌이지만 줌/텐션 로직과 상호작용이 복잡해짐 | |
| You decide | 플레이어가 줌/텐션 로직과의 상호작용을 고려해 결정 | |

**User's choice:** 대시 시작 시 리드 해제 (추천)

---

## 다이나믹 줌아웃 트리거/세기

| Option | Description | Selected |
|--------|-------------|----------|
| 대시 거리 기준 (추천) | 다음 타겟까지 거리가 멀수록 그만큼 뷰 범위가 넓어짐. 로드맵의 "거리 기반" 명시에 부합 | |
| 돌진 이동 속도 기준 | 대시 순간이동 속도가 빠를수록 줌아웃 — 거리와 무관하게 속도감을 더 직접적으로 반영 | |
| 거리+속도 결합 | 둘 다 독립적으로 계산 후 결합(곱/거리 가중합) — 정교하지만 튜닝 산출이 복잡 | ✓ |

**User's choice:** 거리+속도 결합

| Option | Description | Selected |
|--------|-------------|----------|
| 약간 (7 → 약 9) | 왜곡되지 않는 보통 범위. 뷰 범위가 적은 폭으로 늘어남 | ✓ |
| 동적으로 (7 → 약 12+) | 더 영화적인 느낌을 살리기 위해 확대 폭을 크게 허용 | |
| You decide | 리서처/플레이테스트에서 적정값 산출 | |

**User's choice:** 약간 (7 → 약 9)

| Option | Description | Selected |
|--------|-------------|----------|
| 줌아웃 빨리 / 줌인 느리게 (추천) | 대시 거리가 늘어나는 순간 즉시 줌아웃하고, 복귀(줌인)는 느리게 진행되어 카메라가 급작스럽지 않게 되돌아옴 | ✓ |
| 단순 좀 대칭감 | 줌인/줌아웃에 같은 속도를 적용해 카메라 변화감을 단순화 | |
| You decide | 리서처/플레이테스트로 밸런스를 결정 | |

**User's choice:** 줌아웃 빨리 / 줌인 느리게 (추천)

| Option | Description | Selected |
|--------|-------------|----------|
| 히트프리즈 종료 직후 즉시 (추천) | 대시→히트→히트프리즈가 끝난 직후부터 카메라가 곧바로 줌인 동작을 시작 | ✓ |
| 다음 적 감지 전까지 유지 | 다음에 대시할 적이 가까이 있으면 줌아웃 상태를 유지해서 연쇄 돌진 흐름을 매끄럽게 함 | |
| You decide | 거리/속도 로직으로 자연스럽게 복귀 시점 결정 | |

**User's choice:** 히트프리즈 종료 직후 즉시 (추천)

> **참고:** 이 영역 질문을 처음 보낼 때 일부 한국어 텍스트에 인코딩 오류(글자 뭉개짐)가 있었음 — 사용자가 지적한 뒤 이후 질문부터 수정. 위 표는 뭉개진 원문 대신 의도를 복원한 정정본으로 기록.

---

## 텐션 캐치업 안전장치 개입 시점

| Option | Description | Selected |
|--------|-------------|----------|
| 항상 백그라운드로 동작 (추천) | SmoothDamp 로직 사이에 거리가 매개변수로 항상 은은하게 캐치업 텐션이 가미됨 — 별도의 온/오프 전환 없이 모드 구분 없음 | |
| 임계 거리 초과 시에만 개입 | 화면 밖으로 나갈 위험이 있는 순간에만 텐션을 걸어 안전장치처럼 동작 — 평상시엔 SmoothDamp가 그대로 움직임 | ✓ |

**User's choice:** 임계 거리 초과 시에만 개입

| Option | Description | Selected |
|--------|-------------|----------|
| 선형 (추천) | 거리에 비례해 부드럽게 감쇠 스피드가 증가 — 예측하기 쉬움, 특별한 개선점이지 않음 | |
| 지수형/가속 | 거리가 멀어질수록 감쇠 스피드가 급격하게 늘어남 — 근접 거리에서는 거의 SmoothDamp 그대로, 카메라가 거의 즉시 따라붙게 됨 | ✓ |
| You decide | 리서처/플레이테스트로 곡선 형태를 결정 | |

**User's choice:** 지수형/가속

| Option | Description | Selected |
|--------|-------------|----------|
| 화면 절반(orthographicSize 근처) 도달 전에 미리 개입 (추천) | 플레이어가 화면 밖으로 나가기 전에 여유있게 캐치업 시작 — 화면 이탈 자체를 원천 방지 | (초기 옵션 — 사용자 자유 응답으로 대체됨) |
| 화면 경계에 거의 닿을 때만 개입 | 최대한 늦게 개입해 평상시엔 카메라 지연 느낌을 최대한 유지 — 화면 이탈 위험이 살짝 더 있음 | |
| You decide | 리서처/플레이테스트로 임계값 산출 | |
| **(free text)** 25%,75%구간부터 | | ✓ (해석 필요 — 후속 확인) |

**User's choice (최종, 후속 확인 후 확정):** 화면을 좌→우 4등분(1~4구간)했을 때, 목표가 1구간(왼쪽 바깥 25%) 또는 4구간(오른쪽 바깥 25%)에 진입하는 순간부터 개입.
**Notes:** 최초 자유 응답 "25%,75%구간부터"를 orthographicSize 비율로 해석해 확인 질문을 했으나 사용자가 정정 — 실제 의도는 화면을 4등분한 구간(좌/우 바깥쪽 25%) 기준이었음. 이 과정에서 사용자가 한국어 인코딩 품질 개선을 요청함.

---

## Claude's Discretion

- 히트스탑 중 독립 카메라 셰이크 강도/스타일 재조정 여부 (이번 논의에서 미선택 영역)
- `Time.timeScale=0.01` vs 기존 `0f` (HitFreeze) 변경 여부
- `DebugSceneCameraBinder.cs` 신규 API 호환성 갱신 필요 여부

## Deferred Ideas

None — 논의가 phase 범위 내에서 유지됨.
