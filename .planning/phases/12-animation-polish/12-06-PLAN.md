---
phase: 12-animation-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Effects/AutoDestroySelf.cs
  - Assets/Editor/HitSparkBuilder.cs
  - Assets/Editor/PlayerTrailBuilder.cs
  - Assets/Scripts/Camera/CameraFollow.cs
  - Assets/Scripts/Player/CombatController.cs
autonomous: true
requirements:
  - D-07
  - D-08
  - D-10

must_haves:
  truths:
    - "처치 순간 GuardImpact 스프라이트 기반 히트 스파크가 대상 위치에 재생된다 (D-07)"
    - "처치 순간 카메라가 짧게 흔들린다 (D-08)"
    - "대시 중 강화된 TrailRenderer 잔상(그라데이션 색상, 폭 커브)이 표시된다 (D-10)"
  artifacts:
    - path: "Assets/Scripts/Effects/AutoDestroySelf.cs"
      provides: "이펙트 GameObject를 Animator 클립 길이 후 자동 Destroy하는 재사용 컴포넌트"
    - path: "Assets/Editor/HitSparkBuilder.cs"
      provides: "Fast/Phase12/Build Hit Spark Prefab 메뉴 -- SwordGuardImpact.anim 재활용한 HitSparkEffect.prefab 생성"
    - path: "Assets/Editor/PlayerTrailBuilder.cs"
      provides: "Fast/Phase12/Add Player Trail Renderer 메뉴 -- 씬의 Player GameObject에 TrailRenderer 신규 부착"
    - path: "Assets/Scripts/Camera/CameraFollow.cs"
      provides: "public void Shake(float duration, float amplitude)"
      exports:
        - "public void Shake(float duration, float amplitude)"
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "ExecuteDash()에서 히트 스파크+카메라 쉐이크 호출, Awake()에서 TrailRenderer 시각 강화"
  key_links:
    - from: "CombatController.ExecuteDash()"
      to: "CameraFollow.Shake(duration, amplitude)"
      via: "_cameraFollow?.Shake(...) 직접 호출"
      pattern: "_cameraFollow\\?\\.Shake"
    - from: "CombatController.ExecuteDash()"
      to: "Assets/Prefabs/Effects/HitSparkEffect.prefab (Plan 12-07 Inspector 연결)"
      via: "Instantiate(_hitSparkPrefab, ...)"
      pattern: "Instantiate\\(_hitSparkPrefab"
---

<objective>
D-07(히트 스파크), D-08(카메라 쉐이크), D-10(TrailRenderer 강화) 3가지 히트 임팩트 개선을 구현한다. 프로젝트 전체 검색 결과 TrailRenderer 컴포넌트가 현재 어디에도 존재하지 않음을 확인했다 (CombatController._trailRenderer는 GetComponentInChildren가 항상 null 반환) -- D-10은 "강화"가 아니라 "신규 부착 + 강화 설정"이다.

Purpose: 처치 순간 손맛(D-07/D-08)과 돌진 잔상(D-10)을 신규 아트 없이 기존 에셋(GuardImpact 스프라이트)과 런타임 코드만으로 구현한다.
Output: `AutoDestroySelf.cs`, `HitSparkBuilder.cs`, `PlayerTrailBuilder.cs` 신규 + `CameraFollow.cs`/`CombatController.cs` 수정. 프리팹 빌드 실행과 플레이테스트는 Plan 12-07.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md

<interfaces>
From Assets/Editor/ExitPortalBuilder.cs (SavePrefab MenuItem 패턴):
PrefabUtility.SaveAsPrefabAsset(root, AssetPath); Object.DestroyImmediate(root); AssetDatabase.SaveAssets(); AssetDatabase.Refresh();

From Assets/Editor/SetupPhase1Animator.cs (AnimatorController.CreateAnimatorControllerAtPath 패턴):
var controller = AnimatorController.CreateAnimatorControllerAtPath(path); controller.layers[0].stateMachine.AddState("Name").motion = clip;

From Assets/Scripts/Player/CombatController.cs (수정 대상 -- 현재 상태):
private TrailRenderer _trailRenderer; // GetComponentInChildren, 현재 항상 null
private Camera _mainCamera; // Awake()에서 Camera.main 캐시
target.OnDashHit(); ScoreManager.AddKillScore(); // ExecuteDash() 순서, 이 사이에 삽입

From Assets/Scripts/Camera/CameraFollow.cs (수정 대상): LateUpdate() 끝에서 두 분기(hasBounds/no-bounds) 이후 공통으로 Shake 오프셋 적용.

D-07 재활용 클립: Assets/DeadRevolver/PixelPrototypePlayerSprites/Art/Animations/SwordGuardImpact.anim
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AutoDestroySelf.cs + HitSparkBuilder.cs (D-07)</name>
  <files>Assets/Scripts/Effects/AutoDestroySelf.cs, Assets/Editor/HitSparkBuilder.cs</files>
  <read_first>
    - Assets/Editor/ExitPortalBuilder.cs (SavePrefab 패턴)
    - Assets/Editor/SetupPhase1Animator.cs (AnimatorController API 패턴)
  </read_first>
  <action>
Create Assets/Scripts/Effects/AutoDestroySelf.cs: MonoBehaviour with `[SerializeField] private float _fallbackSeconds = 0.3f;`. In a `private IEnumerator Start()`: get `GetComponent<Animator>()`; if found, `yield return null` then set `seconds = animator.GetCurrentAnimatorStateInfo(0).length`; else use `_fallbackSeconds`. Then `yield return new WaitForSecondsRealtime(seconds); Destroy(gameObject);`. Add XML summary: 임의 이펙트를 실시간(초) 뒤 자동 Destroy, WaitForSecondsRealtime 기반으로 HitFreeze(timeScale=0) 중에도 정상 종료.

Create Assets/Editor/HitSparkBuilder.cs. Constants: PrefabPath="Assets/Prefabs/Effects/HitSparkEffect.prefab", ControllerPath="Assets/Prefabs/Effects/HitSparkController.controller", ClipPath="Assets/DeadRevolver/PixelPrototypePlayerSprites/Art/Animations/SwordGuardImpact.anim". `[MenuItem("Fast/Phase12/Build Hit Spark Prefab")] public static void Run()`:
1. Load clip via AssetDatabase.LoadAssetAtPath<AnimationClip>(ClipPath); null -> LogError+return.
2. Ensure Assets/Prefabs/Effects folder exists (Directory.CreateDirectory + AssetDatabase.Refresh()).
3. Delete existing controller at ControllerPath if present, then `AnimatorController.CreateAnimatorControllerAtPath(ControllerPath)`; add state "GuardImpact" with `.motion = clip`.
4. `new GameObject("HitSparkEffect")` -> AddComponent<SpriteRenderer>() -> AddComponent<Animator>().runtimeAnimatorController = controller -> AddComponent<AutoDestroySelf>().
5. Delete existing prefab at PrefabPath if present, PrefabUtility.SaveAsPrefabAsset(root, PrefabPath), DestroyImmediate(root), SaveAssets/Refresh, Debug.Log($"[HitSparkBuilder] HitSparkEffect built at {PrefabPath}").

This task only creates scripts -- running the menu happens in Plan 12-07.
  </action>
  <verify>
    <automated>grep -n "class AutoDestroySelf\|WaitForSecondsRealtime" "Assets/Scripts/Effects/AutoDestroySelf.cs"</automated>
  </verify>
  <acceptance_criteria>
    - AutoDestroySelf.cs contains "public class AutoDestroySelf : MonoBehaviour" and "WaitForSecondsRealtime"
    - HitSparkBuilder.cs contains "[MenuItem(\"Fast/Phase12/Build Hit Spark Prefab\")]", "SwordGuardImpact.anim", "AnimatorController.CreateAnimatorControllerAtPath", "AddComponent<AutoDestroySelf>()", "SaveAsPrefabAsset"
    - No new sprite/PNG assets created -- reuses existing GuardImpact sprites via the existing .anim clip only
  </acceptance_criteria>
  <done>AutoDestroySelf.cs and HitSparkBuilder.cs exist. Running the menu (Plan 12-07) produces a self-cleaning HitSparkEffect.prefab.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PlayerTrailBuilder.cs -- Player에 TrailRenderer 신규 부착 (D-10 setup)</name>
  <files>Assets/Editor/PlayerTrailBuilder.cs</files>
  <read_first>
    - Assets/Editor/HUDTimerLabelBuilder.cs (FindFirstObjectByType/MarkSceneDirty 패턴)
    - Assets/Scripts/Player/CombatController.cs (line 83 -- GetComponentInChildren&lt;TrailRenderer&gt;() 확인용)
  </read_first>
  <action>
Create Assets/Editor/PlayerTrailBuilder.cs. TrailRenderer가 프로젝트 어디에도 없음을 확인했으므로 (2026-07-07), 씬의 CombatController GameObject(Player)에 직접 부착한다.

`[MenuItem("Fast/Phase12/Add Player Trail Renderer")] public static void Run()`:
1. `var combat = Object.FindFirstObjectByType<CombatController>(FindObjectsInactive.Include);` null -> LogError("[PlayerTrailBuilder] 씬에서 CombatController를 찾을 수 없습니다.") + return.
2. `var existing = combat.GetComponentInChildren<TrailRenderer>();` not null -> Log("이미 존재 -- 건너뜁니다.") + return.
3. `var trail = combat.gameObject.AddComponent<TrailRenderer>();` set `trail.time = 0.25f; trail.emitting = false; trail.minVertexDistance = 0.05f;` (CombatController.Awake()가 이후 색상/폭 커브를 추가 설정 -- Task 3).
4. `var shader = Shader.Find("Sprites/Default"); if (shader != null) trail.material = new Material(shader);` (URP 기본 Default-Line 머티리얼은 마젠타로 렌더될 수 있어 명시적으로 교체).
5. `EditorUtility.SetDirty(combat.gameObject); EditorSceneManager.MarkSceneDirty(combat.gameObject.scene);` Debug.Log("[PlayerTrailBuilder] Player GameObject에 TrailRenderer 추가 완료.").

using UnityEditor; using UnityEditor.SceneManagement; using UnityEngine; at top. This task only creates the script -- running the menu happens in Plan 12-07.
  </action>
  <verify>
    <automated>grep -n "MenuItem(.Fast/Phase12/Add Player Trail Renderer.)\|AddComponent<TrailRenderer>\|MarkSceneDirty" "Assets/Editor/PlayerTrailBuilder.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Contains "[MenuItem(\"Fast/Phase12/Add Player Trail Renderer\")]"
    - Contains "FindFirstObjectByType<CombatController>"
    - Contains "AddComponent<TrailRenderer>()"
    - Contains a guard that skips if a TrailRenderer already exists (idempotent)
    - Contains "EditorSceneManager.MarkSceneDirty"
  </acceptance_criteria>
  <done>PlayerTrailBuilder.cs exists. Running the menu (Plan 12-07) attaches TrailRenderer to the Player GameObject in SampleScene.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CameraFollow.Shake() (D-08) + CombatController 히트 스파크/쉐이크/트레일 배선 (D-07, D-10)</name>
  <files>Assets/Scripts/Camera/CameraFollow.cs, Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Camera/CameraFollow.cs (전체 -- LateUpdate() 두 분기 확인)
    - Assets/Scripts/Player/CombatController.cs (전체 -- Awake(), ExecuteDash() 확인)
  </read_first>
  <action>
CameraFollow.cs: add private fields `_shakeTimeRemaining`, `_shakeDurationTotal`, `_shakeAmplitude` (all float, default 0). Add public method:

public void Shake(float duration, float amplitude) { _shakeDurationTotal = duration; _shakeTimeRemaining = duration; _shakeAmplitude = amplitude; }

At the very end of `LateUpdate()` (after both the `if (_hasBounds && _camera != null)` and `else` branches have set `transform.position`), append this common block:

if (_shakeTimeRemaining > 0f)
{
    _shakeTimeRemaining -= Time.unscaledDeltaTime;
    float damper = Mathf.Clamp01(_shakeTimeRemaining / _shakeDurationTotal);
    Vector2 shakeOffset = Random.insideUnitCircle * _shakeAmplitude * damper;
    transform.position += new Vector3(shakeOffset.x, shakeOffset.y, 0f);
}

CombatController.cs changes:
1. Add fields: `[SerializeField] private GameObject _hitSparkPrefab;` `[SerializeField] private float _cameraShakeDuration = 0.15f;` `[SerializeField] private float _cameraShakeAmplitude = 0.2f;` `private CameraFollow _cameraFollow;`
2. In Awake(), after `_mainCamera = Camera.main;`, add: `_cameraFollow = _mainCamera != null ? _mainCamera.GetComponent<CameraFollow>() : null;`
3. In Awake(), after `_trailRenderer = GetComponentInChildren<TrailRenderer>();`, add: `if (_trailRenderer != null) ConfigureTrailVisuals(_trailRenderer);`
4. Add private method (D-10 concrete values -- Claude's Discretion per CONTEXT.md):

private void ConfigureTrailVisuals(TrailRenderer trail)
{
    trail.time = 0.25f;
    trail.widthCurve = new AnimationCurve(new Keyframe(0f, 0.4f), new Keyframe(1f, 0f));
    var grad = new Gradient();
    grad.SetKeys(
        new[] { new GradientColorKey(new Color(0.6f, 0.9f, 1f), 0f), new GradientColorKey(new Color(0.1f, 0.4f, 1f), 1f) },
        new[] { new GradientAlphaKey(0.9f, 0f), new GradientAlphaKey(0f, 1f) });
    trail.colorGradient = grad;
}

5. Add private method (D-07): `private void SpawnHitSpark(Vector2 position) { if (_hitSparkPrefab == null) return; Instantiate(_hitSparkPrefab, position, Quaternion.identity); }`
6. In `ExecuteDash(IEnemy target)`, right after `target.OnDashHit();` and BEFORE `ScoreManager.AddKillScore();`, insert:

SpawnHitSpark(destination);
_cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);

(`destination` is the existing local Vector2 already computed earlier in ExecuteDash as the target's position -- reuse it, do not recompute.)
  </action>
  <verify>
    <automated>grep -n "public void Shake" "Assets/Scripts/Camera/CameraFollow.cs"; grep -n "SpawnHitSpark(destination)\|_cameraFollow?.Shake\|ConfigureTrailVisuals\|_hitSparkPrefab" "Assets/Scripts/Player/CombatController.cs"</automated>
  </verify>
  <acceptance_criteria>
    - CameraFollow.cs contains "public void Shake(float duration, float amplitude)"
    - Shake's decay uses Time.unscaledDeltaTime (not Time.deltaTime) so it still runs during HitFreeze (Time.timeScale=0)
    - CombatController.cs contains "_cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude)" placed after "target.OnDashHit();" and before "ScoreManager.AddKillScore();"
    - CombatController.cs contains "SpawnHitSpark(destination)" in the same location
    - CombatController.Awake() calls "ConfigureTrailVisuals(_trailRenderer)" guarded by a null check
    - ConfigureTrailVisuals sets both "widthCurve" and "colorGradient" on the TrailRenderer
  </acceptance_criteria>
  <done>CameraFollow can shake on demand; CombatController spawns a hit spark and shakes the camera on every successful kill, and configures TrailRenderer visuals in Awake(). Not yet functionally verified in-editor -- Plan 12-07 builds prefabs, assigns _hitSparkPrefab in Inspector, and playtests.</done>
</task>

</tasks>

<verification>
grep -n "Shake" Assets/Scripts/Camera/CameraFollow.cs shows one public method and its usage inside LateUpdate.
grep -n "SpawnHitSpark\|_cameraFollow" Assets/Scripts/Player/CombatController.cs shows the wiring is present exactly once in ExecuteDash().
</verification>

<success_criteria>
- AutoDestroySelf.cs, HitSparkBuilder.cs, PlayerTrailBuilder.cs 모두 생성되었다 (프리팹/컴포넌트는 아직 미생성 -- Plan 12-07에서 실행)
- CameraFollow.Shake()가 Time.unscaledDeltaTime 기반으로 감쇠한다
- CombatController가 처치 시 히트 스파크+카메라 쉐이크를 호출하고, TrailRenderer 시각을 강화한다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-06-SUMMARY.md 생성:
- 3개 신규 파일 요약 + CameraFollow.cs/CombatController.cs 변경 라인 범위
- TrailRenderer가 프로젝트에 전무했다는 발견 사실과 그에 따른 대응(신규 부착) 명시
- Plan 12-07에서 필요한 수동 작업 목록 (메뉴 2개 실행, _hitSparkPrefab Inspector 연결)
</output>
