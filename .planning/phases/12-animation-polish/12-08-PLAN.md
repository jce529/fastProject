---
phase: 12-animation-polish
plan: 08
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - Assets/Scripts/Enemy/EnemyDeathEffect.cs
  - Assets/Scripts/Enemy/MeleeEnemy.cs
  - Assets/Scripts/Enemy/RangedEnemy.cs
autonomous: true
requirements:
  - D-09
  - D-11

must_haves:
  truths:
    - "적 처치 시 기존 Die 애니메이션이 먼저 재생을 마친 뒤 파티클이 재생된다 (D-09 순서)"
    - "파티클 재생과 동시에 SpriteMask가 아래에서 위로 적 스프라이트를 가리며 사라진다 (D-09)"
    - "연출이 끝나면 적 GameObject가 실제로 Destroy된다 (D-09) -- 이전에는 사망 후에도 GameObject가 영구히 씬에 남아있었다"
    - "MeleeEnemy/RangedEnemy의 공격 모션 자체(Chase/Telegraph/Attack)는 이 Plan에서 변경하지 않는다 (D-11 -- 플레이어 연출만 다루는 Phase 12 범위 확인, 사망 연출만 예외)"
  artifacts:
    - path: "Assets/Scripts/Enemy/EnemyDeathEffect.cs"
      provides: "PlayDeathSequence(Animator) 코루틴 -- Die 애니메이션 대기 -> 파티클 재생 -> SpriteMask 상승 페이드 -> Destroy"
      exports:
        - "public IEnumerator PlayDeathSequence(Animator animator)"
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "OnDashHit()이 EnemyDeathEffect를 AddComponent-or-GetComponent로 확보해 PlayDeathSequence를 시작"
    - path: "Assets/Scripts/Enemy/RangedEnemy.cs"
      provides: "OnDashHit()이 동일 패턴으로 PlayDeathSequence를 시작"
  key_links:
    - from: "EnemyDeathEffect.PlayDeathSequence"
      to: "RuntimeMaskSprite.CreateMaskSprite()"
      via: "정적 메서드 호출 (Plan 12-01 산출물 재사용, D-09 요구사항)"
      pattern: "RuntimeMaskSprite\\.CreateMaskSprite"
    - from: "MeleeEnemy.OnDashHit() / RangedEnemy.OnDashHit()"
      to: "EnemyDeathEffect.PlayDeathSequence(_animator)"
      via: "gameObject.AddComponent<EnemyDeathEffect>() 폴백 + StartCoroutine"
      pattern: "PlayDeathSequence"
---

<objective>
D-09 결정에 따라 적 처치 연출을 구현한다: 기존 Die 애니메이션 재생 -> 파티클 이펙트 재생 + SpriteMask로 스프라이트를 아래에서 위로 가리며 사라짐 -> GameObject Destroy. SpriteMask 기법은 D-01(포탈 전환, Plan 12-01)과 동일한 `RuntimeMaskSprite` 코드 패턴을 재사용한다. 파티클은 런타임에 코드로 생성해 신규 프리팹/아트가 필요 없다.

주의: 현재 `MeleeEnemy.OnDashHit()`/`RangedEnemy.OnDashHit()`는 사망한 적 GameObject를 전혀 Destroy하지 않는다 (물리/콜라이더만 끄고 영구히 씬에 잔류). 이 Plan은 그 GameObject Destroy를 신규로 추가한다.

Purpose: 처치 연출에 시각적 완결성을 더하고, 사망한 적이 씬에 무한정 쌓이는 문제(모바일 메모리 관리, CLAUDE.md)를 함께 해소한다.
Output: `EnemyDeathEffect.cs` 신규 파일 + `MeleeEnemy.cs`/`RangedEnemy.cs`의 `OnDashHit()` 배선. EnemyDeathEffect는 `gameObject.AddComponent`로 런타임 부착되므로 프리팹 수정이나 Inspector 연결이 필요 없다 -- Plan 12-09는 순수 플레이테스트만 진행한다.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md

<interfaces>
From Assets/Scripts/World/RuntimeMaskSprite.cs (Plan 12-01 output -- exact signature this plan calls):
public static Sprite CreateMaskSprite()

From Assets/Scripts/Enemy/MeleeEnemy.cs (현재 OnDashHit(), 수정 대상):
public void OnDashHit() { if (!IsAlive) return; IsAlive = false; ... foreach (var c in GetComponents<Collider2D>()) c.enabled = false; _animator?.SetBool("isDead", true); }

From Assets/Scripts/Enemy/RangedEnemy.cs (현재 OnDashHit(), 동일 구조):
public void OnDashHit() { if (!IsAlive) return; IsAlive = false; ... _animator?.SetBool("isDead", true); }

Animator 파라미터 확인 (Assets/Animations/Enemies/MeleeEnemyAnimator.controller, RangedEnemyAnimator.controller):
lowerCamelCase 컨벤션 (isDead, isMoving, isChasing, isAttacking) -- 플레이어 계열 PascalCase와 다름, 이미 SetBool("isDead", true)로 정상 호출 중 (변경 불필요)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EnemyDeathEffect.cs -- Die 대기 -> 파티클 -> SpriteMask 상승 페이드 -> Destroy (D-09)</name>
  <files>Assets/Scripts/Enemy/EnemyDeathEffect.cs</files>
  <read_first>
    - Assets/Scripts/World/RuntimeMaskSprite.cs (Plan 12-01 산출물 -- CreateMaskSprite() 호출 대상)
    - Assets/Scripts/Enemy/MeleeEnemy.cs (Animator 파라미터 "isDead" 사용 컨벤션 확인)
  </read_first>
  <action>
Create Assets/Scripts/Enemy/EnemyDeathEffect.cs. `[RequireComponent(typeof(SpriteRenderer))]`.

Fields:
[SerializeField] private float _maskRiseDuration = 0.6f; // Claude's discretion
[SerializeField] private Color _particleColor = new Color(1f, 0.3f, 0.1f); // Claude's discretion
private SpriteRenderer _sr; // set in Awake() via GetComponent<SpriteRenderer>()

public IEnumerator PlayDeathSequence(Animator animator) 구현:
1. Die 애니메이션 재생 완료 대기: if (animator != null) { yield return null; (한 프레임 대기 -- isDead bool 전환이 이 프레임에 반영되도록) float dieLength = animator.GetCurrentAnimatorStateInfo(0).length; yield return new WaitForSecondsRealtime(dieLength); }
2. 파티클 재생: SpawnDeathParticles() 호출 (아래 정의)
3. SpriteMask 아래->위 페이드: new GameObject("DeathMask")에 SpriteMask 추가, mask.sprite = RuntimeMaskSprite.CreateMaskSprite(); _sr.maskInteraction = SpriteMaskInteraction.VisibleOutsideMask; 초기 위치는 transform.position + Vector3.down * _sr.bounds.extents.y (스프라이트 맨 아래). float height = _sr.bounds.size.y; elapsed 0 -> _maskRiseDuration 루프(Time.unscaledDeltaTime): maskHeight = Lerp(0, height, t); maskGO.transform.position = transform.position + Vector3.down * (_sr.bounds.extents.y - maskHeight * 0.5f); maskGO.transform.localScale = new Vector3(20f, Mathf.Max(maskHeight, 0.001f), 1f); yield return null.
4. Destroy(maskGO); Destroy(gameObject);

private void SpawnDeathParticles() 구현 (런타임 생성, 신규 아트/프리팹 불필요):
var psGO = new GameObject("DeathParticles"); psGO.transform.position = transform.position;
var ps = psGO.AddComponent<ParticleSystem>();
var main = ps.main; main.startColor = _particleColor; main.startLifetime = 0.5f; main.startSpeed = 3f; main.startSize = 0.15f; main.loop = false; main.stopAction = ParticleSystemStopAction.Destroy;
var emission = ps.emission; emission.rateOverTime = 0f; emission.SetBursts(new[] { new ParticleSystem.Burst(0f, 12) });
var shape = ps.shape; shape.shapeType = ParticleSystemShapeType.Circle; shape.radius = 0.3f;
ps.Play();
(main.stopAction = Destroy와 loop = false 조합으로 파티클이 모두 소멸하면 psGO가 자동으로 Destroy되므로 별도 정리 코드가 필요 없다.)

Add XML summary above the class: "D-09: 적 사망 연출 -- 기존 Die 애니메이션 재생 -> 파티클 재생 + SpriteMask로 아래에서 위로 가리며 사라짐 -> Destroy. SpriteMask 생성 패턴은 FloorTransitionEffect(D-01)와 동일한 RuntimeMaskSprite.CreateMaskSprite()를 재사용한다. MeleeEnemy/RangedEnemy가 OnDashHit()에서 AddComponent 후 StartCoroutine(PlayDeathSequence(...))로 호출한다."
  </action>
  <verify>
    <automated>grep -n "class EnemyDeathEffect\|public IEnumerator PlayDeathSequence\|RuntimeMaskSprite.CreateMaskSprite\|ParticleSystemStopAction.Destroy\|Time.unscaledDeltaTime" "Assets/Scripts/Enemy/EnemyDeathEffect.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs exists and contains "public class EnemyDeathEffect : MonoBehaviour"
    - Contains "[RequireComponent(typeof(SpriteRenderer))]"
    - Contains "public IEnumerator PlayDeathSequence(Animator animator)"
    - Contains "RuntimeMaskSprite.CreateMaskSprite()"
    - Contains "ParticleSystemStopAction.Destroy" and "main.loop = false" (self-cleaning particle, no manual Destroy needed for the particle GameObject)
    - Contains "Destroy(gameObject);" as the final step of PlayDeathSequence
    - All mask timing uses "Time.unscaledDeltaTime" (slow-mo/HitFreeze immune)
  </acceptance_criteria>
  <done>EnemyDeathEffect.cs exists with PlayDeathSequence() implementing Die-wait -> particle -> bottom-up SpriteMask fade -> Destroy. Not yet called by anything (Task 2 wires MeleeEnemy/RangedEnemy).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MeleeEnemy.cs + RangedEnemy.cs -- OnDashHit()에서 사망 연출 시작 (D-09)</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs, Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs (Task 1 산출물 -- PlayDeathSequence 정확한 시그니처)
    - Assets/Scripts/Enemy/MeleeEnemy.cs (전체 OnDashHit(), 라인 81-94)
    - Assets/Scripts/Enemy/RangedEnemy.cs (전체 OnDashHit(), 라인 88-100)
  </read_first>
  <action>
In both MeleeEnemy.cs and RangedEnemy.cs, at the end of `OnDashHit()` (after the existing `_animator?.SetBool("isDead", true);` line, which stays unchanged), append:

var deathEffect = GetComponent<EnemyDeathEffect>();
if (deathEffect == null) deathEffect = gameObject.AddComponent<EnemyDeathEffect>();
StartCoroutine(deathEffect.PlayDeathSequence(_animator));

Do not modify any other part of OnDashHit() (physics freeze, collider disable, IsAlive=false, coroutine stop -- all unchanged, CLAUDE.md surgical-change principle). Do not modify any other method in either file (Chase/Telegraph/Attack FSM logic stays untouched per D-11).
  </action>
  <verify>
    <automated>grep -n "GetComponent<EnemyDeathEffect>\|AddComponent<EnemyDeathEffect>\|StartCoroutine(deathEffect.PlayDeathSequence" "Assets/Scripts/Enemy/MeleeEnemy.cs" "Assets/Scripts/Enemy/RangedEnemy.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Both MeleeEnemy.cs and RangedEnemy.cs's OnDashHit() contain "gameObject.AddComponent<EnemyDeathEffect>()" guarded by a GetComponent null-check
    - Both contain "StartCoroutine(deathEffect.PlayDeathSequence(_animator));" as the last statement in OnDashHit()
    - The existing "_animator?.SetBool(\"isDead\", true);" line is unchanged and still executes before the new lines
    - No other method in MeleeEnemy.cs or RangedEnemy.cs (UpdateIdle, UpdateChase, TelegraphAndAttack/TelegraphAndFire, FireProjectile, etc.) was modified -- confirms D-11 scope boundary (enemy attack motions untouched)
  </acceptance_criteria>
  <done>Killing a MeleeEnemy or RangedEnemy now plays the full Die-wait -> particle -> SpriteMask fade -> Destroy sequence, and the enemy GameObject is actually removed from the scene afterward (previously it stayed forever as a disabled corpse).</done>
</task>

</tasks>

<verification>
grep -c "AddComponent<EnemyDeathEffect>" Assets/Scripts/Enemy/MeleeEnemy.cs Assets/Scripts/Enemy/RangedEnemy.cs returns 1 in each file.
Unity 컴파일 성공 -- EnemyDeathEffect와 RuntimeMaskSprite 타입 참조가 정상 해석되어야 한다 (Plan 12-01 완료 전제).
</verification>

<success_criteria>
- EnemyDeathEffect.cs가 생성되어 RuntimeMaskSprite를 재사용한다
- MeleeEnemy/RangedEnemy 모두 OnDashHit() 마지막에 사망 연출을 시작한다
- 프리팹 수정이나 Inspector 연결 없이(AddComponent 런타임 부착) 즉시 동작 가능한 상태다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-08-SUMMARY.md 생성:
- EnemyDeathEffect.cs 생성 내용 요약 (파티클 설정값, 마스크 상승 지속시간)
- MeleeEnemy.cs/RangedEnemy.cs 변경 라인 요약
- 사망한 적 GameObject가 이제 실제로 Destroy된다는 동작 변경 사실 명시 (이전: 영구 잔류)
</output>
