---
phase: 12-animation-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - Assets/Editor/PortalEffectBuilder.cs
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "FloorTransitionSequence()의 입장 연출은 옛 체인(포탈 포함)이 Destroy되기 전에 재생을 마친다 (E1-E4가 F1-F7보다 먼저 실행)"
    - "FloorTransitionSequence()의 퇴장 연출은 플레이어 텔레포트+카메라 스냅이 끝난 뒤에 재생된다 (X1-X4가 텔레포트 이후 실행)"
    - "PortalEffect.prefab은 Collider가 없는 순수 비주얼 프리팹이며 기존 ExitPortal 스프라이트를 재활용한다 (D-02)"
  artifacts:
    - path: "Assets/Editor/PortalEffectBuilder.cs"
      provides: "Fast/Phase12/Build Portal Effect Prefab 메뉴 -- PortalEffect.prefab 생성 (SpriteRenderer만, Collider 없음)"
      contains: "MenuItem(\"Fast/Phase12/Build Portal Effect Prefab\")"
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "_portalEffectPrefab 필드 + FloorTransitionSequence()가 FloorTransitionEffect.PlayEntry/PlayExit를 호출하도록 재작성"
      contains: "_transitionEffect.PlayEntry"
  key_links:
    - from: "WorldGenerator.Start()"
      to: "FloorTransitionEffect (Plplayer GameObject에 GetComponent-or-AddComponent)"
      via: "_playerTransform.gameObject.AddComponent<FloorTransitionEffect>() 폴백"
      pattern: "AddComponent<FloorTransitionEffect>"
    - from: "WorldGenerator.FloorTransitionSequence()"
      to: "FloorTransitionEffect.PlayEntry(portal.transform) / PlayExit(teleportPos, _portalEffectPrefab)"
      via: "yield return"
      pattern: "_transitionEffect\\.Play(Entry|Exit)"
---

<objective>
Plan 12-01에서 만든 `FloorTransitionEffect`를 실제로 사용하도록 `WorldGenerator.cs`의 `FloorTransitionSequence()`를 재작성하고, D-02 결정에 따라 기존 ExitPortal 스프라이트를 재활용하는 `PortalEffect.prefab` 빌더 에디터 도구를 만든다. 이 Plan은 코드만 작성한다 -- 실제 메뉴 실행과 Inspector 연결, 플레이테스트는 Plan 12-03에서 진행한다.

Purpose: 10-TRANSITION-DESIGN.md의 전체 시퀀스(E1-E4 입장 -> F1-F7 층 전환 -> X1-X4 퇴장)를 실제 코드 흐름에 반영해, 기존의 단순한 `WaitForSecondsRealtime(0.05f)` 플레이스홀더를 실제 연출 재생으로 교체한다.
Output: `PortalEffectBuilder.cs` 신규 파일 (프리팹은 아직 미생성), `WorldGenerator.cs`의 `FloorTransitionSequence()` 재작성 완료 (컴파일 성공, 실행은 Plan 12-03).
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md
@.planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md

<interfaces>
From Assets/Scripts/World/FloorTransitionEffect.cs (Plan 12-01 output -- exact signatures this plan calls):
public IEnumerator PlayEntry(Transform portal)
public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)

From Assets/Editor/ExitPortalBuilder.cs (SavePrefab MenuItem pattern -- PortalEffectBuilder ports this exactly):
private static void SavePrefab(GameObject root) { ... PrefabUtility.SaveAsPrefabAsset(root, AssetPath); Object.DestroyImmediate(root); AssetDatabase.SaveAssets(); AssetDatabase.Refresh(); }

Portal sprite source confirmed via ExitPortal.prefab's SpriteRenderer (D-02 reuse target):
Assets/Prefabs/Portal/Portal_100x100px1.png (guid d955c863ad568e34bb88bce0967fddbe, Single sprite mode -- AssetDatabase.LoadAssetAtPath<Sprite>(path) returns it directly)

From Assets/Scripts/World/WorldGenerator.cs (current FloorTransitionSequence(), lines 490-548 -- this plan's edit target):
public void EnterPortal(ExitPortal portal) { StartCoroutine(FloorTransitionSequence(portal)); }
private IEnumerator FloorTransitionSequence(ExitPortal portal) { ... FloorManager.CurrentFloor++; foreach chain Destroy; GameObject newRoom = portal.StandbyRoom; ... teleportPos ...; RecomputeCameraBounds(); yield return null; yield return new WaitForSecondsRealtime(0.05f); _player.UnlockInput(); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PortalEffectBuilder.cs -- PortalEffect 프리팹 생성 에디터 도구 (D-02)</name>
  <files>Assets/Editor/PortalEffectBuilder.cs</files>
  <read_first>
    - Assets/Editor/ExitPortalBuilder.cs (SavePrefab 패턴, source -- 그대로 이식)
    - Assets/Prefabs/World/ExitPortal/ExitPortal.prefab (line 99 -- m_Sprite guid d955c863ad568e34bb88bce0967fddbe 확인용, 재활용 대상)
  </read_first>
  <action>
Create Assets/Editor/PortalEffectBuilder.cs. Running the menu Fast/Phase12/Build Portal Effect Prefab creates Assets/Prefabs/World/PortalEffect/PortalEffect.prefab.

Implementation:
- private const string AssetPath = "Assets/Prefabs/World/PortalEffect/PortalEffect.prefab";
- private const string SpritePath = "Assets/Prefabs/Portal/Portal_100x100px1.png";
- [MenuItem("Fast/Phase12/Build Portal Effect Prefab")] public static void Run():
  1. var sprite = AssetDatabase.LoadAssetAtPath<Sprite>(SpritePath); if null, Debug.LogError and return.
  2. var root = new GameObject("PortalEffect"); var sr = root.AddComponent<SpriteRenderer>(); sr.sprite = sprite;
  3. Deliberately do NOT add any Collider2D component -- D-01 explicitly requires this prefab to be visual-only (no trigger, no physics).
  4. Call SavePrefab(root) using the exact same pattern as ExitPortalBuilder.SavePrefab: create Assets/Prefabs/World/PortalEffect/ directory via Directory.CreateDirectory(Path.Combine(Application.dataPath, "Prefabs", "World", "PortalEffect")) if missing, delete existing asset at AssetPath if present, PrefabUtility.SaveAsPrefabAsset(root, AssetPath), Object.DestroyImmediate(root), AssetDatabase.SaveAssets(), AssetDatabase.Refresh(), Debug.Log($"[PortalEffectBuilder] PortalEffect built at {AssetPath}").

Add an XML summary above the class: "Menu: Fast/Phase12/Build Portal Effect Prefab -- D-02: 신규 아트 없이 기존 ExitPortal 스프라이트(Portal_100x100px1.png)를 재활용해 퇴장 연출용 PortalEffect 프리팹을 생성한다. Collider 없음 -- 비주얼 전용(10-TRANSITION-DESIGN.md)."

This task only creates the script -- running the menu happens in Plan 12-03 (checkpoint).
  </action>
  <verify>
    <automated>grep -n "MenuItem(.Fast/Phase12/Build Portal Effect Prefab.)\|Portal_100x100px1.png\|AddComponent<SpriteRenderer>\|SaveAsPrefabAsset\|Collider" "Assets/Editor/PortalEffectBuilder.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/PortalEffectBuilder.cs exists
    - Contains "[MenuItem(\"Fast/Phase12/Build Portal Effect Prefab\")]"
    - Contains "Assets/Prefabs/Portal/Portal_100x100px1.png"
    - Contains "root.AddComponent<SpriteRenderer>()"
    - Contains "PrefabUtility.SaveAsPrefabAsset(root, AssetPath)"
    - Does NOT contain "AddComponent<BoxCollider2D>" or "AddComponent<CircleCollider2D>" or any Collider2D addition (visual-only per D-01/D-02)
  </acceptance_criteria>
  <done>PortalEffectBuilder.cs exists with a menu item that will build a Collider-less SpriteRenderer-only prefab at Assets/Prefabs/World/PortalEffect/PortalEffect.prefab once run inside the Unity Editor (Plan 12-03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WorldGenerator.cs -- FloorTransitionSequence를 FloorTransitionEffect 호출로 재작성</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (전체, 특히 Start() 라인 69-121, FloorTransitionSequence() 라인 490-548)
    - Assets/Scripts/World/FloorTransitionEffect.cs (Plan 12-01 산출물 -- PlayEntry/PlayExit 정확한 시그니처)
    - .planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md (전체 시퀀스 순서: ENTRY -> FLOOR SETUP -> EXIT)
  </read_first>
  <action>
1. Add a new serialized field under a new header, placed after the existing "Exit Portal (Phase 10)" header block:

[Header("Transition Animation (Phase 12, D-01~D-04)")]
[SerializeField] private GameObject _portalEffectPrefab;

Add a runtime field: private FloorTransitionEffect _transitionEffect;

2. In Start(), immediately after the existing `if (_playerTransform != null) { ... }` block that does the start-room teleport (around line 87-96), add:

if (_playerTransform != null)
{
    _transitionEffect = _playerTransform.GetComponent<FloorTransitionEffect>();
    if (_transitionEffect == null)
        _transitionEffect = _playerTransform.gameObject.AddComponent<FloorTransitionEffect>();
}

This auto-attaches FloorTransitionEffect to the Player GameObject at runtime if it is not already present in the scene -- avoids requiring a manual Unity Editor "Add Component" step (Player already has a SpriteRenderer, satisfying FloorTransitionEffect's RequireComponent).

3. Rewrite FloorTransitionSequence(ExitPortal portal) to insert the ENTRY call before the old-chain destroy block, and the EXIT call after the camera snap. The existing structure (Step 1 lock input, chain destroy loop, new room activation, Step 2 teleport, Step 3 camera snap) stays unchanged -- only insert two new blocks and replace the final WaitForSecondsRealtime(0.05f):

private IEnumerator FloorTransitionSequence(ExitPortal portal)
{
    Debug.Log($"[WorldGenerator] EnterPortal -> Floor {FloorManager.CurrentFloor}");
    ScoreManager.AddTimeBonus(FloorTimer.RemainingSeconds);

    // Step 1 -- lock input (unchanged)
    _combatController?.ForceExitCombatState();
    _player.LockInput();

    // ENTRY (D-01 E1-E4) -- portal is still alive here, destroyed right after this call
    if (_transitionEffect != null)
        yield return _transitionEffect.PlayEntry(portal.transform);

    FloorManager.CurrentFloor++;

    // [existing chain-destroy foreach loop -- KEEP UNCHANGED, do not modify]
    // [existing _chain.Clear(); _activeExitCount = 0; -- KEEP UNCHANGED]
    // [existing newRoom activation, _chain.AddLast, _playerCurrentIndex/_playerCurrentNode/_currentYDrift/FloorTimer.Reset() -- KEEP UNCHANGED]

    // Step 2 -- teleport (unchanged, keep exact existing spawnPoints/teleportPos/rb logic)
    // ... existing code ...
    Vector3 teleportPos = /* existing computed value */;
    // ... existing _playerTransform.position = teleportPos; ...

    // Step 3 -- camera snap (unchanged)
    RecomputeCameraBounds();

    yield return null; // Step 3.5

    // EXIT (D-01 X1-X4) -- replaces the old WaitForSecondsRealtime(0.05f) placeholder
    if (_transitionEffect != null)
        yield return _transitionEffect.PlayExit(teleportPos, _portalEffectPrefab);
    else
        yield return new WaitForSecondsRealtime(0.05f); // fallback if effect component missing

    _player.UnlockInput(); // Step 6
}

Important: preserve every existing line inside the chain-destroy foreach loop, the newRoom setup block, and the Step 2 teleport block verbatim -- only the ENTRY call insertion point (before `FloorManager.CurrentFloor++`) and the EXIT call replacing the final `WaitForSecondsRealtime(0.05f)` are new. Do not alter EnterPortal(ExitPortal portal) itself.
  </action>
  <verify>
    <automated>grep -n "_portalEffectPrefab\|_transitionEffect\|PlayEntry(portal.transform)\|PlayExit(teleportPos, _portalEffectPrefab)\|AddComponent<FloorTransitionEffect>" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - WorldGenerator.cs contains "[SerializeField] private GameObject _portalEffectPrefab;"
    - WorldGenerator.cs contains "private FloorTransitionEffect _transitionEffect;"
    - Start() contains "AddComponent<FloorTransitionEffect>()" as a fallback when GetComponent returns null
    - FloorTransitionSequence() calls "_transitionEffect.PlayEntry(portal.transform)" BEFORE the line "FloorManager.CurrentFloor++"
    - FloorTransitionSequence() calls "_transitionEffect.PlayExit(teleportPos, _portalEffectPrefab)" AFTER "RecomputeCameraBounds()"
    - The chain-destroy foreach loop, newRoom activation block, and Step 2 teleport block are byte-identical to the pre-edit version (no unrelated changes -- CLAUDE.md surgical-change principle)
    - EnterPortal(ExitPortal portal) method signature and body are unchanged
  </acceptance_criteria>
  <done>WorldGenerator.FloorTransitionSequence() now plays the full 10-TRANSITION-DESIGN.md entry/exit SpriteMask sequence via FloorTransitionEffect instead of the old fixed 0.05s wait. Compiles successfully. Not yet functionally verified in-editor (Plan 12-03 builds the PortalEffect prefab, assigns _portalEffectPrefab in Inspector, and playtests).</done>
</task>

</tasks>

<verification>
grep -n "PlayEntry\|PlayExit" Assets/Scripts/World/WorldGenerator.cs shows exactly one PlayEntry call and one PlayExit call, both inside FloorTransitionSequence().
Unity 컴파일 성공 -- FloorTransitionEffect 타입 참조가 정상 해석되어야 한다 (Plan 12-01 완료 후 실행 전제).
</verification>

<success_criteria>
- PortalEffectBuilder.cs가 생성되었다 (프리팹은 아직 미생성 -- Plan 12-03에서 실행)
- WorldGenerator.FloorTransitionSequence()가 ENTRY/EXIT 연출을 올바른 순서로 호출한다
- 기존 F1-F7(층 전환 핵심 로직)이 한 줄도 변경되지 않았다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-02-SUMMARY.md 생성:
- PortalEffectBuilder.cs 생성 내용 요약
- WorldGenerator.cs에서 변경된 정확한 라인 범위와 삽입 위치
- Plan 12-03에서 필요한 수동 작업 목록 (메뉴 실행, Inspector _portalEffectPrefab 연결)
</output>
