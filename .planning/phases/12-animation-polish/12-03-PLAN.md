---
phase: 12-animation-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - Assets/Prefabs/World/PortalEffect/PortalEffect.prefab
  - Assets/Scenes/SampleScene.unity
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "Assets/Prefabs/World/PortalEffect/PortalEffect.prefab이 실제로 존재하고 SpriteRenderer만 가진 Collider 없는 프리팹이다"
    - "SampleScene의 WorldGenerator._portalEffectPrefab에 PortalEffect.prefab이 연결되어 있다"
    - "포탈 진입 시 플레이어가 SpriteMask에 가려지며 사라지고, 새 층에서 포탈 이펙트가 성장한 뒤 플레이어가 걸어나오듯 나타난다 (D-01 실제 재생 확인)"
  artifacts:
    - path: "Assets/Prefabs/World/PortalEffect/PortalEffect.prefab"
      provides: "PortalEffectBuilder.Run() 실행 산출물 -- 실제 포탈 이펙트 프리팹"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "WorldGenerator._portalEffectPrefab 연결된 씬"
  key_links:
    - from: "SampleScene WorldGenerator GameObject"
      to: "Assets/Prefabs/World/PortalEffect/PortalEffect.prefab"
      via: "Inspector _portalEffectPrefab 필드 드래그 연결"
      pattern: "PortalEffect"
---

<objective>
Plan 12-02에서 만든 `PortalEffectBuilder` 에디터 도구를 Unity Editor에서 실제로 실행해 `PortalEffect.prefab`을 생성하고, `WorldGenerator._portalEffectPrefab` Inspector 필드에 연결한 뒤, D-01~D-04 포탈 입장/퇴장 연출을 플레이테스트로 확인한다.

Purpose: Plan 01~02에서 완성된 코드가 실제 씬에서 의도대로 재생되는지 검증하는 최종 게이트. Phase 10의 10-03/10-04 플랜과 동일한 검증 스타일(에디터 도구 실행 checkpoint -> Inspector 연결 -> 플레이테스트 checkpoint)을 따른다.
Output: PortalEffect.prefab 실제 생성 + WorldGenerator Inspector 연결 완료 + D-01~D-04 성공 기준 플레이테스트 확인.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: PortalEffect 프리팹 빌드 실행 + WorldGenerator Inspector 연결</name>
  <read_first>
    - Assets/Editor/PortalEffectBuilder.cs (Plan 12-02에서 생성 -- 메뉴 경로 확인)
    - Assets/Scripts/World/WorldGenerator.cs (Plan 12-02 산출물 -- _portalEffectPrefab 필드 확인)
  </read_first>
  <what-built>
Plan 12-02에서 `PortalEffectBuilder.cs` 에디터 스크립트와 `WorldGenerator._portalEffectPrefab` 필드가 준비되었다. 이제 Unity Editor에서 메뉴를 실행해 실제 프리팹을 만들고 Inspector에 연결한다.
  </what-built>
  <how-to-verify>
1. `Assets/Scenes/SampleScene.unity`를 연다. 컴파일이 완료될 때까지 기다린다 (Console에 에러가 없어야 한다 -- Plan 12-01/12-02가 모두 완료되어 있어야 정상)
2. 상단 메뉴에서 `Fast > Phase12 > Build Portal Effect Prefab` 클릭
3. Console에서 `[PortalEffectBuilder] PortalEffect built at Assets/Prefabs/World/PortalEffect/PortalEffect.prefab` 로그를 확인한다 (Error 없어야 함)
4. Project 창에서 `Assets/Prefabs/World/PortalEffect/PortalEffect.prefab`이 존재하는지 확인하고, 더블클릭해 SpriteRenderer만 있고 Collider 컴포넌트가 없는지 확인한다
5. Hierarchy에서 `WorldGenerator` GameObject를 선택한다
6. Inspector의 `Transition Animation (Phase 12, D-01~D-04)` 헤더 아래 `_portalEffectPrefab` 필드에 방금 만든 `PortalEffect.prefab`을 드래그해 연결한다
7. 기존 Phase 10/11 필드(`_exitPortalPrefab`, `_meleeEnemyPrefab`, `_rangedEnemyPrefab` 등)는 변경하지 않는다
8. `Ctrl+S`로 씬 저장
  </how-to-verify>
  <resume-signal>"완료" -- 프리팹 생성 확인 + WorldGenerator._portalEffectPrefab 연결 확인. 또는 문제 설명 (컴파일 에러, 메뉴 없음 등).</resume-signal>
  <acceptance_criteria>
    - Console에 "[PortalEffectBuilder] PortalEffect built at Assets/Prefabs/World/PortalEffect/PortalEffect.prefab" 로그가 출력되었다
    - PortalEffect.prefab이 SpriteRenderer 컴포넌트만 가지고 Collider2D 컴포넌트가 없다
    - WorldGenerator Inspector의 _portalEffectPrefab에 PortalEffect.prefab이 연결되어 있다
    - 기존 WorldGenerator 필드가 그대로 유지되어 있다
    - Console에 컴파일 에러가 없다
  </acceptance_criteria>
  <done>PortalEffect.prefab이 실제로 존재하고 WorldGenerator에 연결되어 다음 플레이테스트(Task 2)를 진행할 준비가 되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 -- D-01~D-04 포탈 전환 연출 확인</name>
  <read_first>
    - .planning/phases/12-animation-polish/12-CONTEXT.md (D-01~D-04 결정 재확인)
  </read_first>
  <what-built>
FloorTransitionSequence()가 이제 FloorTransitionEffect.PlayEntry()/PlayExit()를 호출해 플레이어가 포탈 안으로 사라지고(SpriteMask 입장), 층 전환 후 포탈 이펙트가 성장하며 플레이어가 걸어나오는(SpriteMask 퇴장) 연출을 재생한다.
  </what-built>
  <how-to-verify>
1. Unity Editor Play 모드를 시작한다
2. WorldGenerator Inspector에서 `_exitSpawnChance`를 일시적으로 1.0f로 올려 포탈이 즉시 스폰되도록 한다 (테스트 편의 -- 테스트 후 0.15f로 되돌릴 것)
3. 가까운 EXIT 포탈로 걸어 들어간다
4. **E1-E4 확인**: 포탈에 닿는 순간 플레이어 스프라이트가 포탈 경계선 방향으로 점점 가려지며 사라지는지 확인한다 (약 0.4초). 이어서 포탈 자체가 눈에 띄게 작아지는지 확인한다 (약 0.3초)
5. **F1-F7 확인**: 화면이 새 층으로 전환되고 (기존처럼) 즉시 확인 가능해야 한다
6. **X1-X4 확인**: 새 위치에서 포탈 이펙트 스프라이트가 0에서 커지며 나타나는지 확인한다 (약 0.4초). 곧이어 플레이어 스프라이트가 그 위치에서 점진적으로 드러나며(가려진 영역이 줄어들며) 나타나는지 확인한다 (약 0.5초). 마지막으로 포탈 이펙트가 줄어들며 사라지는지 확인한다 (약 0.3초)
7. 전체 시퀀스 동안 입력이 잠겨 있다가(LockInput) 연출이 끝나면 즉시 조작 가능한지 확인한다 (UnlockInput)
8. 5회 반복해 포탈 진입 시마다 매번 연출이 일관되게 재생되는지 확인한다
9. Console에 "[WorldGenerator]" 관련 LogError가 없는지 확인한다
10. 테스트 종료 후 `_exitSpawnChance`를 0.15f로 되돌리고 씬을 저장하지 않거나(변경 취소), 되돌린 값으로 저장한다
  </how-to-verify>
  <resume-signal>
"D-01~D-04: 통과" -- 입장/퇴장 연출이 모두 자연스럽게 재생됨. 또는 미통과 항목과 관찰된 현상 설명 (예: "입장 마스크가 순간적으로 사라짐 -- 애니메이션 안 보임").
  </resume-signal>
  <acceptance_criteria>
    - 포탈 진입 시 플레이어가 점진적으로(순간적이지 않게) 사라진다
    - 포탈 자체가 진입 직후 눈에 띄게 축소된다
    - 새 층에서 포탈 이펙트가 성장하며 나타난다
    - 플레이어가 점진적으로 다시 나타난다 (순간적으로 팝인하지 않음)
    - 포탈 이펙트가 최종적으로 축소되며 사라진다 (씬에 잔류 GameObject가 남지 않음)
    - 전체 시퀀스 동안 플레이어 입력이 잠겨 있다가 종료 즉시 정상 조작 가능하다
    - Console에 LogError가 없다
  </acceptance_criteria>
  <done>D-01(SpriteMask 입장/퇴장), D-02(PortalEffect 스프라이트 재활용), D-03(지속시간), D-04(기존 Animator 미사용 유지)가 모두 플레이테스트로 확인되었다.</done>
</task>

</tasks>

<verification>
Task 1의 Console 로그와 Inspector 연결 상태, Task 2의 5회 반복 플레이테스트 결과를 통해 D-01~D-04를 모두 확인한다.
</verification>

<success_criteria>
- PortalEffect.prefab이 실제로 존재하고 WorldGenerator에 연결되어 있다
- 포탈 입장/퇴장 SpriteMask 연출이 플레이테스트에서 자연스럽게 재생됨을 확인했다
- Console에 컴파일/런타임 오류가 없다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-03-SUMMARY.md 생성:
- Task 1 결과 (프리팹 생성 + Inspector 연결)
- Task 2 결과 (D-01~D-04 각 항목 통과 여부 및 관찰 내용)
- 발견된 문제 및 해결 방법 (있다면)
</output>
