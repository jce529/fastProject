---
phase: 12-animation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/RuntimeMaskSprite.cs
  - Assets/Scripts/World/FloorTransitionEffect.cs
autonomous: true
requirements:
  - D-01
  - D-03
  - D-04

must_haves:
  truths:
    - "포탈 진입 시 플레이어 스프라이트가 SpriteMask에 의해 포탈 경계선 너머로 점진적으로 사라진다 (D-01 Entry)"
    - "새 층 진입 시 플레이어 스프라이트가 SpriteMask 수축에 의해 포탈에서 걸어나오듯 점진적으로 나타난다 (D-01 Exit)"
    - "모든 연출 코루틴은 Time.unscaledDeltaTime만 사용해 슬로우모션/HitFreeze(timeScale=0) 중에도 정상 진행된다"
  artifacts:
    - path: "Assets/Scripts/World/RuntimeMaskSprite.cs"
      provides: "런타임 4x4 흰색 마스크 스프라이트 생성 정적 유틸 (캐시 포함) — 10-TRANSITION-DESIGN.md CreateMaskSprite() 이식"
      exports:
        - "public static Sprite CreateMaskSprite()"
    - path: "Assets/Scripts/World/FloorTransitionEffect.cs"
      provides: "PlayEntry()/PlayExit() 코루틴 — WorldGenerator가 호출할 진입/퇴장 SpriteMask 애니메이션 계약"
      exports:
        - "public IEnumerator PlayEntry(Transform portal)"
        - "public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)"
  key_links:
    - from: "FloorTransitionEffect.PlayEntry/PlayExit"
      to: "RuntimeMaskSprite.CreateMaskSprite()"
      via: "정적 메서드 호출 (SpriteMask.sprite 할당)"
      pattern: "RuntimeMaskSprite\\.CreateMaskSprite"
---

<objective>
10-TRANSITION-DESIGN.md에 설계됐지만 미구현이던 포탈 입장/퇴장 SpriteMask 연출을 코드로 구현한다. D-01 결정에 따라 `WorldGenerator.cs`에 직접 넣지 않고 신규 컴포넌트 `FloorTransitionEffect`로 분리한다 — WorldGenerator는 다음 Plan(12-02)에서 이 컴포넌트의 `PlayEntry()`/`PlayExit()`만 호출한다. 이 Plan은 코드 계약만 만든다: Player GameObject에 부착될 컴포넌트와 그것이 의존하는 마스크 스프라이트 유틸리티.

Purpose: Plan 12-02(WorldGenerator 배선)가 참조할 정확한 메서드 시그니처를 먼저 확정해, 후속 Plan이 코드 탐색 없이 바로 사용할 수 있게 한다. 적 사망 연출(D-09, Plan 12-08)도 동일 마스크 유틸을 재사용하므로 여기서 공용 정적 클래스로 분리한다.
Output: `RuntimeMaskSprite.cs`, `FloorTransitionEffect.cs` 2개 신규 파일 (컴파일 성공, 아직 아무것도 호출하지 않음 — WorldGenerator 배선은 Plan 12-02).
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md
@.planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md

<interfaces>
Existing project conventions this plan ports directly:

From 10-TRANSITION-DESIGN.md (원본 CreateMaskSprite 스니펫 -- RuntimeMaskSprite.cs가 그대로 이식):
public static Sprite CreateMaskSprite -- Texture2D(4,4) 전부 흰색, Sprite.Create(tex, Rect(0,0,4,4), pivot(0.5,0.5), pixelsPerUnit=4)

From 10-TRANSITION-DESIGN.md (SpriteMask 방향 로직 -- FloorTransitionEffect.PlayEntry/PlayExit가 그대로 이식):
direction = player.position.x > portalX ? +1 : -1
maskCenter.x = portalX + (maskWidth * 0.5f * direction)
scale.x: 0 -> Abs(player.x - portalX) + playerSpriteHalfWidth

From Assets/Scripts/World/WorldGenerator.cs (기존 코루틴 컨벤션 -- 동일 패턴 사용):
yield return new WaitForSecondsRealtime(0.05f);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RuntimeMaskSprite.cs -- 공용 SpriteMask 스프라이트 생성 유틸</name>
  <files>Assets/Scripts/World/RuntimeMaskSprite.cs</files>
  <read_first>
    - .planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md (CreateMaskSprite 원본 스니펫, 섹션 "SpriteMask 스프라이트")
  </read_first>
  <action>
Create Assets/Scripts/World/RuntimeMaskSprite.cs with this exact content:

using UnityEngine;

public static class RuntimeMaskSprite
{
    private static Sprite _cached;

    public static Sprite CreateMaskSprite()
    {
        if (_cached != null) return _cached;

        var tex = new Texture2D(4, 4);
        var px = new Color[16];
        for (int i = 0; i < 16; i++) px[i] = Color.white;
        tex.SetPixels(px);
        tex.Apply();
        _cached = Sprite.Create(tex, new Rect(0, 0, 4, 4), new Vector2(0.5f, 0.5f), 4f);
        return _cached;
    }
}

Add an XML summary comment above the class explaining: D-01/D-09 -- SpriteMask 런타임 생성 유틸리티, FloorTransitionEffect(D-01)와 EnemyDeathEffect(D-09, Plan 12-08)가 공유, 캐시로 모바일 GC 압박 감소.
  </action>
  <verify>
    <automated>grep -n "class RuntimeMaskSprite\|public static Sprite CreateMaskSprite\|_cached" "Assets/Scripts/World/RuntimeMaskSprite.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/World/RuntimeMaskSprite.cs exists and contains "public static class RuntimeMaskSprite"
    - Contains "public static Sprite CreateMaskSprite()"
    - Contains a "_cached" field guard (returns cached sprite on repeat calls, no per-call Texture2D allocation)
  </acceptance_criteria>
  <done>RuntimeMaskSprite.CreateMaskSprite() exists and is ready to be called by FloorTransitionEffect (this plan, Task 2) and EnemyDeathEffect (Plan 12-08).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FloorTransitionEffect.cs -- 포탈 입장/퇴장 SpriteMask 애니메이션 컴포넌트</name>
  <files>Assets/Scripts/World/FloorTransitionEffect.cs</files>
  <read_first>
    - Assets/Scripts/World/RuntimeMaskSprite.cs (Task 1 산출물 -- CreateMaskSprite() 호출 대상)
    - .planning/phases/10-exit-portal-floor-transition/10-TRANSITION-DESIGN.md (전체 시퀀스 E1-E4, X1-X4, 지속시간 표)
    - Assets/Scripts/Player/CombatController.cs (line 82 -- GetComponent&lt;SpriteRenderer&gt; 직접 사용 컨벤션 확인용)
  </read_first>
  <action>
Create Assets/Scripts/World/FloorTransitionEffect.cs. Player GameObject에 부착한다 (SpriteRenderer 필요 -- RequireComponent). D-03 지속시간 값을 그대로 사용한다: 입장 마스크 0.4s, 포탈 수축 0.3s, 퇴장 포탈 성장 0.4s, 퇴장 마스크 0.5s, 포탈 페이드 0.3s.

Required members (exact signatures):
- [RequireComponent(typeof(SpriteRenderer))] public class FloorTransitionEffect : MonoBehaviour
- private SpriteRenderer _sr; set in Awake() via GetComponent<SpriteRenderer>()
- [SerializeField] private float _entryMaskDuration = 0.4f;
- [SerializeField] private float _portalShrinkDuration = 0.3f;
- [SerializeField] private float _exitPortalGrowDuration = 0.4f;
- [SerializeField] private float _exitMaskDuration = 0.5f;
- [SerializeField] private float _portalFadeDuration = 0.3f;
- public IEnumerator PlayEntry(Transform portal)
- public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)
- private IEnumerator ScaleTransform(Transform t, Vector3 from, Vector3 to, float duration) -- shared Lerp helper using Time.unscaledDeltaTime

PlayEntry(Transform portal) implementation (E1-E4):
1. Compute portalX = portal.position.x; dir = transform.position.x > portalX ? 1f : -1f; targetWidth = Mathf.Abs(transform.position.x - portalX) + _sr.bounds.extents.x.
2. Create a new GameObject "EntryMask", AddComponent&lt;SpriteMask&gt;(), assign mask.sprite = RuntimeMaskSprite.CreateMaskSprite(). Set _sr.maskInteraction = SpriteMaskInteraction.VisibleOutsideMask.
3. E2: loop elapsed 0 -> _entryMaskDuration using Time.unscaledDeltaTime; each frame compute t = Clamp01(elapsed/_entryMaskDuration), width = Lerp(0, targetWidth, t); set maskGO.transform.position.x = portalX + (width*0.5f*dir), keep y/z from transform.position; set maskGO.transform.localScale = new Vector3(Mathf.Max(width,0.001f), 20f, 1f); yield return null.
4. E3: yield return ScaleTransform(portal, Vector3.one, Vector3.zero, _portalShrinkDuration) -- shrinks the live ExitPortal instance itself.
5. E4: _sr.enabled = false; Destroy(maskGO).

PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab) implementation (X1-X4):
1. X1: if portalEffectPrefab != null, Instantiate(portalEffectPrefab, spawnWorldPos, Quaternion.identity), set its localScale = Vector3.zero, yield return ScaleTransform(instance.transform, Vector3.zero, Vector3.one, _exitPortalGrowDuration). Keep the instance reference (portalEffect) for X4 -- if prefab is null, skip entirely (portalEffect stays null).
2. portalX = spawnWorldPos.x; dir = transform.position.x > portalX ? 1f : -1f; startWidth = Mathf.Abs(transform.position.x - portalX) + _sr.bounds.extents.x + 2f (extra 2f margin to guarantee full coverage at start).
3. Create GameObject "ExitMask" + SpriteMask with RuntimeMaskSprite.CreateMaskSprite(). Set _sr.maskInteraction = SpriteMaskInteraction.VisibleOutsideMask; _sr.enabled = true.
4. X2: loop elapsed 0 -> _exitMaskDuration using Time.unscaledDeltaTime; width = Lerp(startWidth, 0f, t); same position/scale update pattern as PlayEntry but shrinking toward the portal.
5. X3: Destroy(maskGO); _sr.maskInteraction = SpriteMaskInteraction.None.
6. X4: if portalEffect != null, yield return ScaleTransform(portalEffect.transform, Vector3.one, Vector3.zero, _portalFadeDuration), then Destroy(portalEffect).

Note: ScaleTransform must guard `if (t == null) yield break;` at the start (defensive -- portal or portalEffect could already be destroyed by an external system).
  </action>
  <verify>
    <automated>grep -n "class FloorTransitionEffect\|public IEnumerator PlayEntry\|public IEnumerator PlayExit\|RuntimeMaskSprite.CreateMaskSprite\|Time.unscaledDeltaTime\|RequireComponent(typeof(SpriteRenderer))" "Assets/Scripts/World/FloorTransitionEffect.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/World/FloorTransitionEffect.cs exists and contains "public class FloorTransitionEffect : MonoBehaviour"
    - Contains "[RequireComponent(typeof(SpriteRenderer))]"
    - Contains "public IEnumerator PlayEntry(Transform portal)"
    - Contains "public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)"
    - Contains "RuntimeMaskSprite.CreateMaskSprite()" at least twice (PlayEntry and PlayExit each create a mask)
    - Contains "SpriteMaskInteraction.VisibleOutsideMask" and "SpriteMaskInteraction.None"
    - All timing loops use "Time.unscaledDeltaTime" (no bare Time.deltaTime) -- slow-mo/HitFreeze immune per STATE.md constraint
    - No compile-time reference to WorldGenerator (this file is a self-contained contract -- wiring happens in Plan 12-02)
  </acceptance_criteria>
  <done>FloorTransitionEffect.cs exists with PlayEntry()/PlayExit() coroutines implementing the full E1-E4/X1-X4 sequence from 10-TRANSITION-DESIGN.md using the D-03 duration values. Not yet called by anything (Plan 12-02 wires WorldGenerator to it).</done>
</task>

</tasks>

<verification>
grep -c "IEnumerator" Assets/Scripts/World/FloorTransitionEffect.cs returns 3 (PlayEntry, PlayExit, ScaleTransform).
Unity 컴파일 성공 -- 두 파일 모두 기존 코드에 대한 참조가 없으므로 (독립 신규 컴포넌트) 컴파일 에러가 발생하지 않아야 한다.
</verification>

<success_criteria>
- RuntimeMaskSprite.cs, FloorTransitionEffect.cs 2개 파일이 모두 생성되었다
- FloorTransitionEffect가 RuntimeMaskSprite를 재사용해 마스크 스프라이트를 생성한다 (중복 코드 없음)
- 모든 연출 타이밍이 Time.unscaledDeltaTime 기반이라 슬로우모션/HitFreeze 중에도 정상 진행된다
- D-03 지속시간 값(0.4/0.3/0.4/0.5/0.3)이 코드에 정확히 반영되었다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-01-SUMMARY.md 생성:
- 생성된 2개 파일 경로 및 각 라인 수
- PlayEntry()/PlayExit() 시그니처 및 Plan 12-02가 호출해야 할 정확한 방식 요약
- RuntimeMaskSprite 캐싱 전략 요약 (Plan 12-08 EnemyDeathEffect도 재사용 예정임을 명시)
</output>
