---
phase: 12-animation-polish
plan: 09
type: execute
wave: 3
depends_on:
  - 12-08
files_modified: []
autonomous: false
requirements:
  - D-09
  - D-11

must_haves:
  truths:
    - "적 처치 시 Die 애니메이션 -> 파티클 -> SpriteMask 상승 페이드 -> Destroy 순서가 실제로 눈으로 확인된다 (D-09)"
    - "적 GameObject가 연출 종료 후 Hierarchy에서 실제로 사라진다 (더 이상 영구 잔류하지 않음)"
    - "MeleeEnemy/RangedEnemy의 기존 전투 동작(추격, 텔레그래프, 공격)은 이 Phase로 인해 전혀 달라지지 않았다 (D-11)"
---

<objective>
Plan 12-08에서 구현한 적 사망 연출(`EnemyDeathEffect`)이 프리팹 수정이나 Inspector 연결 없이(런타임 `AddComponent`) 실제 플레이 상황에서 의도대로 재생되는지 플레이테스트로 확인한다. 이 Plan은 코드/에셋 변경이 전혀 없다 -- 순수 검증 게이트다.

Purpose: EnemyDeathEffect가 씬/프리팹을 건드리지 않고 코드만으로 완결되는 설계이므로, 실제 실행 결과가 설계 의도(Die 재생 -> 파티클 -> 마스크 상승 -> Destroy)와 일치하는지 확인이 필요하다.
Output: D-09/D-11 성공 기준 플레이테스트 확인 완료.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 플레이테스트 -- D-09 적 사망 연출 + D-11 범위 확인</name>
  <read_first>
    - .planning/phases/12-animation-polish/12-CONTEXT.md (D-09, D-11 재확인)
  </read_first>
  <what-built>
MeleeEnemy/RangedEnemy의 OnDashHit()이 이제 EnemyDeathEffect를 런타임으로 부착하고 Die 애니메이션 대기 -> 파티클 재생 -> SpriteMask로 스프라이트를 아래에서 위로 가리며 사라짐 -> GameObject Destroy 시퀀스를 재생한다.
  </what-built>
  <how-to-verify>
1. Unity Editor를 열고 컴파일 에러가 없는지 확인한다 (Plan 12-01, 12-08 완료 전제 -- 별도 메뉴 실행이나 Inspector 연결이 필요 없다는 점이 이 Plan의 특징이다)
2. Play 모드 시작. MeleeEnemy를 공격해 처치한다
3. **D-09 순서 확인**: 처치 즉시 기존 Die 애니메이션이 먼저 완주하는지 확인한다. 애니메이션이 끝난 직후 파티클(주황빛 계열, 12개 파편)이 터지는지 확인한다. 파티클과 거의 동시에 스프라이트가 아래에서부터 위로 점점 가려지며 사라지는지 확인한다 (약 0.6초)
4. 연출이 끝난 후 Hierarchy에서 해당 MeleeEnemy GameObject가 실제로 사라졌는지(Destroy) 확인한다 -- 이전에는 사망 후에도 비활성 콜라이더 상태로 영구히 남아있었다
5. RangedEnemy도 동일하게 처치해 같은 시퀀스가 재생되는지 확인한다
6. **D-11 확인**: 처치 전 MeleeEnemy의 추격/텔레그래프("!" 아이콘)/공격 동작과 RangedEnemy의 조준선/발사 동작이 Phase 11 이전과 동일하게 작동하는지 확인한다 (이 Phase가 공격 모션 자체를 바꾸지 않았음을 재확인)
7. 연속으로 3~4마리를 처치해 매번 시퀀스가 일관되게 재생되고 GameObject가 정리되는지 확인한다
8. Console에 관련 LogError가 없는지 확인한다
  </how-to-verify>
  <resume-signal>"D-09: 통과, D-11: 통과" 또는 미통과 항목과 관찰된 현상 설명 (예: "파티클이 보이지 않음", "GameObject가 사라지지 않음").</resume-signal>
  <acceptance_criteria>
    - Die 애니메이션이 완주된 후에 파티클이 재생된다 (동시에 시작하지 않음)
    - SpriteMask가 아래에서 위로 스프라이트를 가려 사라지게 만든다
    - 연출 종료 후 적 GameObject가 Hierarchy에서 실제로 사라진다
    - MeleeEnemy/RangedEnemy 모두 동일하게 동작한다
    - 기존 추격/텔레그래프/공격 동작이 이 Phase로 인해 변경되지 않았다
    - Console에 LogError가 없다
  </acceptance_criteria>
  <done>D-09(적 사망 연출: Die -> 파티클 -> SpriteMask 페이드 -> Destroy)와 D-11(적 공격 모션 무변경 범위 확인)이 모두 플레이테스트로 확인되었다.</done>
</task>

</tasks>

<verification>
연속 3~4회 처치 플레이테스트 결과를 통해 D-09/D-11을 확인한다. 코드/에셋 변경이 없으므로 정적 grep 검증은 Plan 12-08에서 이미 완료되었다.
</verification>

<success_criteria>
- 적 사망 연출이 설계된 순서(Die -> 파티클 -> 마스크 페이드 -> Destroy)대로 재생된다
- 사망한 적 GameObject가 더 이상 영구 잔류하지 않는다
- 기존 적 전투 동작(D-11 범위)이 그대로 유지된다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-09-SUMMARY.md 생성:
- D-09/D-11 각 항목 통과 여부 및 관찰 내용
- 발견된 문제 및 해결 방법 (있다면, 예: _maskRiseDuration/_particleColor 튜닝 필요 여부)
- Phase 12 전체 완료 여부 선언 (12-01~12-09 모든 Plan 통과 확인 시)
</output>
