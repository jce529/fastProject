---
phase: 12-animation-polish
plan: 07
type: execute
wave: 2
depends_on:
  - 12-06
files_modified:
  - Assets/Prefabs/Effects/HitSparkEffect.prefab
  - Assets/Prefabs/Effects/HitSparkController.controller
  - Assets/Scenes/SampleScene.unity
autonomous: false
requirements:
  - D-07
  - D-08
  - D-10

must_haves:
  truths:
    - "처치 순간 GuardImpact 히트 스파크가 실제로 재생된다 (D-07)"
    - "처치 순간 카메라가 실제로 짧게 흔들린다 (D-08)"
    - "대시 중 강화된 색상/폭 그라데이션 잔상이 실제로 보인다 (D-10)"
  artifacts:
    - path: "Assets/Prefabs/Effects/HitSparkEffect.prefab"
      provides: "HitSparkBuilder.Run() 실행 산출물"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "CombatController._hitSparkPrefab 연결 + Player GameObject에 TrailRenderer 부착된 씬"
  key_links:
    - from: "SampleScene CombatController GameObject"
      to: "Assets/Prefabs/Effects/HitSparkEffect.prefab"
      via: "Inspector _hitSparkPrefab 필드 드래그 연결"
      pattern: "HitSparkEffect"
---

<objective>
Plan 12-06에서 만든 `HitSparkBuilder`/`PlayerTrailBuilder` 에디터 도구를 Unity Editor에서 실제로 실행해 `HitSparkEffect.prefab`을 생성하고 Player에 `TrailRenderer`를 부착한 뒤, `CombatController._hitSparkPrefab`을 Inspector에서 연결하고 D-07/D-08/D-10을 플레이테스트로 확인한다.

Purpose: Plan 12-06에서 완성된 코드가 실제 씬에서 의도대로 재생되는지 검증하는 최종 게이트.
Output: HitSparkEffect.prefab 실제 생성 + Player TrailRenderer 부착 + CombatController Inspector 연결 완료 + D-07/D-08/D-10 플레이테스트 확인.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-animation-polish/12-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 에디터 도구 2종 실행 + CombatController Inspector 연결</name>
  <read_first>
    - Assets/Editor/HitSparkBuilder.cs, Assets/Editor/PlayerTrailBuilder.cs (Plan 12-06 산출물 -- 메뉴 경로 확인)
    - Assets/Scripts/Player/CombatController.cs (Plan 12-06 산출물 -- _hitSparkPrefab 필드 확인)
  </read_first>
  <what-built>
Plan 12-06에서 `HitSparkBuilder.cs`, `PlayerTrailBuilder.cs` 에디터 스크립트와 `CombatController._hitSparkPrefab` 필드가 준비되었다. 이제 Unity Editor에서 두 메뉴를 실행하고 Inspector를 연결한다.
  </what-built>
  <how-to-verify>
1. `Assets/Scenes/SampleScene.unity`를 연다. 컴파일이 완료될 때까지 기다린다 (Console에 에러 없어야 함 -- Plan 12-06 완료 전제)
2. 상단 메뉴에서 `Fast > Phase12 > Build Hit Spark Prefab` 클릭 -> Console에 `[HitSparkBuilder] HitSparkEffect built at Assets/Prefabs/Effects/HitSparkEffect.prefab` 확인
3. 상단 메뉴에서 `Fast > Phase12 > Add Player Trail Renderer` 클릭 -> Console에 `[PlayerTrailBuilder] Player GameObject에 TrailRenderer 추가 완료.` 확인
4. Hierarchy에서 Player GameObject를 선택 -> Inspector에 `Trail Renderer` 컴포넌트가 추가되어 있는지 확인 (Time=0.25, Material이 마젠타가 아닌 정상 셰이더인지 확인)
5. 같은 Player GameObject의 `Combat Controller` 컴포넌트에서 `_hitSparkPrefab` 필드에 `Assets/Prefabs/Effects/HitSparkEffect.prefab`을 드래그해 연결한다
6. `Ctrl+S`로 씬 저장
  </how-to-verify>
  <resume-signal>"완료" -- 두 메뉴 실행 확인 + Inspector 연결 확인. 또는 문제 설명.</resume-signal>
  <acceptance_criteria>
    - Console에 HitSparkBuilder, PlayerTrailBuilder 완료 로그가 모두 출력되었다
    - Player GameObject에 TrailRenderer 컴포넌트가 존재하고 마젠타(누락 셰이더)가 아닌 정상 머티리얼이 적용되어 있다
    - CombatController._hitSparkPrefab에 HitSparkEffect.prefab이 연결되어 있다
    - Console에 컴파일 에러가 없다
  </acceptance_criteria>
  <done>HitSparkEffect.prefab이 실제로 존재하고, Player에 TrailRenderer가 부착되었으며, CombatController가 이를 참조할 준비가 되어 다음 플레이테스트(Task 2)를 진행할 수 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 -- D-07/D-08/D-10 히트 임팩트 확인</name>
  <read_first>
    - .planning/phases/12-animation-polish/12-CONTEXT.md (D-07, D-08, D-10 재확인)
  </read_first>
  <what-built>
CombatController.ExecuteDash()가 처치 성공 시 히트 스파크를 스폰하고 카메라를 흔들며, 대시 중 강화된 TrailRenderer 잔상을 표시한다.
  </what-built>
  <how-to-verify>
1. Play 모드 시작. 적을 향해 공격을 발동해 처치한다
2. **D-07 확인**: 처치 순간 대상 위치에 짧은 스파크(칼날 부딪힘 이펙트, 약 0.1~0.2초)가 재생되는지 확인한다
3. **D-08 확인**: 처치 순간 화면(카메라)이 짧게 흔들리는지 확인한다 -- 너무 약하거나 과해 보이면 CombatController Inspector의 `_cameraShakeDuration`/`_cameraShakeAmplitude` 값을 조정할 수 있음을 메모한다
4. **D-10 확인**: 대시(돌진) 중 플레이어 뒤로 밝은 하늘색에서 짙은 파란색으로 옅어지는 잔상이 표시되는지 확인한다 (이전에는 TrailRenderer 자체가 없어 잔상이 전혀 없었음)
5. 연속으로 3~5회 적을 처치해 매번 스파크+쉐이크+잔상이 일관되게 재생되는지 확인한다
6. Console에 관련 LogError가 없는지 확인한다
  </how-to-verify>
  <resume-signal>"D-07: 통과, D-08: 통과, D-10: 통과" 또는 미통과 항목과 관찰된 현상 설명 (예: "쉐이크가 거의 느껴지지 않음 -- amplitude 상향 필요").</resume-signal>
  <acceptance_criteria>
    - 처치 순간 히트 스파크가 대상 위치에서 재생된다
    - 처치 순간 카메라가 눈에 띄게(그러나 과하지 않게) 흔들린다
    - 대시 중 그라데이션 잔상이 표시된다
    - 연속 처치에서도 매번 일관되게 재생된다
    - Console에 LogError가 없다
  </acceptance_criteria>
  <done>D-07(히트 스파크), D-08(카메라 쉐이크), D-10(TrailRenderer 강화)이 모두 플레이테스트로 확인되었다.</done>
</task>

</tasks>

<verification>
Task 1의 Console 로그와 Inspector 연결 상태, Task 2의 연속 처치 플레이테스트 결과를 통해 D-07/D-08/D-10을 모두 확인한다.
</verification>

<success_criteria>
- HitSparkEffect.prefab이 실제로 존재하고 CombatController에 연결되어 있다
- Player에 TrailRenderer가 부착되어 강화된 잔상이 재생된다
- 카메라 쉐이크가 처치 시 실제로 발동한다
</success_criteria>

<output>
완료 후 .planning/phases/12-animation-polish/12-07-SUMMARY.md 생성:
- Task 1 결과 (프리팹 생성 + TrailRenderer 부착 + Inspector 연결)
- Task 2 결과 (D-07/D-08/D-10 각 항목 통과 여부 및 관찰 내용, 필요 시 조정된 튜닝 값)
- 발견된 문제 및 해결 방법 (있다면)
</output>
