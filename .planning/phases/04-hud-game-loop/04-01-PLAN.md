---
plan: "04-01"
phase: "04"
title: "FloorManager + HUDController + Canvas HUD Build (UI-01)"
wave: 1
depends_on: []
files_modified:
  - "Assets/Scripts/World/FloorManager.cs"
  - "Assets/Scripts/UI/HUDController.cs"
  - "Assets/Scenes/SampleScene.unity"
requirements:
  - "UI-01"
autonomous: false
must_haves:
  - "FloorManager static class exists with public static int CurrentFloor = 1"
  - "HUDController.Update() calls SetText('Floor {0}', FloorManager.CurrentFloor) — no string allocation"
  - "HUDController.Update() sets _gaugeFill.fillAmount = _gauge.Value"
  - "HUDController dirty-checks AttackType before calling SetText — no TMP re-render every frame"
  - "Canvas: Screen Space Overlay, CanvasScaler Scale With Screen Size 1920x1080 Match 0.5"
  - "HUDPanel always active — FloorGroup (top-left), GaugeGroup (top-center), AttackTypeGroup (top-right)"
  - "GaugeFill Image: Type=Filled, FillMethod=Horizontal, FillOrigin=Left"
  - "TMP Essential Resources imported before Play Mode (no magenta error quads)"
  - "All HUDController SerializeField fields wired in Inspector"
---

<objective>
Implement the Phase 4 HUD: a persistent overlay showing floor counter, time-stop gauge, and attack type. Delivers UI-01 entirely.

Three code files are created (FloorManager, HUDController). The Canvas hierarchy is built manually in the Unity Editor per the UI-SPEC.md canvas contract. This plan has no dependencies — it can start immediately.

Purpose: Player always sees game state. Validates that uGUI + TMP pipeline works before Plan 04-02 adds death screen.
Output: FloorManager.cs (new), HUDController.cs (new), SampleScene updated with Canvas + HUDPanel hierarchy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/새 폴더/Fast/.planning/STATE.md
@D:/새 폴더/Fast/.planning/phases/04-hud-game-loop/04-RESEARCH.md
@D:/새 폴더/Fast/.planning/phases/04-hud-game-loop/04-UI-SPEC.md
</context>

<tasks>

<task id="04-01-T1" title="Create FloorManager static class">
  <read_first>
    - Assets/Scripts/UI/AttackTypeSelector.cs — established pattern for a minimal static data provider (public static Selected property, no MonoBehaviour needed for data-only class)
  </read_first>
  <action>
Create new file `Assets/Scripts/World/FloorManager.cs`:

```csharp
public static class FloorManager
{
    public static int CurrentFloor = 1;
}
```

No namespace. No MonoBehaviour. No MonoBehaviour is needed — static class is sufficient for a single int.
Phase 4 scope: always Floor 1 (single-room prototype). DeathScreenController (Plan 04-02) resets to 1 on restart. v2 FloorProgressor will increment this on floor transition.
  </action>
  <acceptance_criteria>
    - File exists at Assets/Scripts/World/FloorManager.cs
    - `public static class FloorManager` declared
    - `public static int CurrentFloor = 1` field present
    - No MonoBehaviour, no namespace
    - Project compiles (no red Console errors)
  </acceptance_criteria>
</task>

<task id="04-01-T2" title="Create HUDController MonoBehaviour">
  <read_first>
    - Assets/Scripts/Player/ChronoChronoGaugeController.cs — confirms `public float Value { get; private set; }` (line 17)
    - Assets/Scripts/UI/AttackTypeSelector.cs — confirms `public static AttackType Selected { get; private set; }` (line 14), `public enum AttackType { Linear, Fan }` (line 3)
    - Assets/Scripts/Player/PlayerController.cs — not directly used by HUD but context for timeScale behavior (HUDController runs in Update, no unscaled time needed for read-only polling)
  </read_first>
  <action>
Create new file `Assets/Scripts/UI/HUDController.cs`:

```csharp
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class HUDController : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI _floorLabel;
    [SerializeField] private Image           _gaugeFill;
    [SerializeField] private TextMeshProUGUI _attackTypeLabel;
    [SerializeField] private ChronoChronoGaugeController _gauge;

    private AttackType _lastType = (AttackType)(-1);

    private void Update()
    {
        _floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
        _gaugeFill.fillAmount = _gauge.Value;

        AttackType t = AttackTypeSelector.Selected;
        if (t != _lastType)
        {
            _lastType = t;
            _attackTypeLabel.SetText(t == AttackType.Linear ? "LINEAR" : "FAN");
        }
    }
}
```

**Why `(AttackType)(-1)` init:** Forces the dirty-check to fire on the very first Update() frame, ensuring the label is set before the player sees the HUD. -1 is not a valid AttackType value so it always differs from the real Selected value.

**Why poll not event:** ChronoGaugeController has no OnValueChanged. AttackTypeSelector.SetType fires rarely. Polling is the established codebase pattern (CombatController). Negligible per-frame cost at 60fps.

**Why `SetText("{0}", value)` not interpolation:** `$"Floor {FloorManager.CurrentFloor}"` allocates a new string every frame → GC pressure. `SetText("{0}", intValue)` uses TMP's internal char buffer — zero allocation per call (verified in TMP_Text.cs lines 2493-2496 in PackageCache).
  </action>
  <acceptance_criteria>
    - File exists at Assets/Scripts/UI/HUDController.cs
    - `[SerializeField] private TextMeshProUGUI _floorLabel` present
    - `[SerializeField] private Image _gaugeFill` present
    - `[SerializeField] private TextMeshProUGUI _attackTypeLabel` present
    - `[SerializeField] private ChronoGaugeController _gauge` present
    - `_floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor)` in Update()
    - `_gaugeFill.fillAmount = _gauge.Value` in Update()
    - AttackType dirty-check `if (t != _lastType)` present
    - `_attackTypeLabel.SetText(t == AttackType.Linear ? "LINEAR" : "FAN")` inside the if-block
    - No `string.Format`, no `$"..."` interpolation anywhere in Update()
    - Project compiles with no errors
  </acceptance_criteria>
</task>

<task id="04-01-T3" title="[HUMAN] Import TMP Essential Resources + build Canvas HUD hierarchy">
  <note>This task requires Unity Editor interaction. Claude cannot perform editor GUI actions. Steps must be executed by the human developer.</note>
  <action>
**Step 0 — Import TMP Essential Resources (REQUIRED FIRST):**

Open Unity Editor. Menu: `Window > TextMeshPro > Import TMP Essential Resources`
Wait for import to complete. Verify `Assets/TextMesh Pro/` folder now exists in Project window.
This is mandatory — without it, all TextMeshProUGUI components render magenta error quads.

---

**Step 1 — Create Canvas:**

In Hierarchy window, right-click → UI → Canvas.

Set Canvas properties in Inspector:
- Render Mode: `Screen Space - Overlay`
- Sort Order: `0`

Add CanvasScaler (auto-added):
- UI Scale Mode: `Scale With Screen Size`
- Reference Resolution: `1920 x 1080`
- Match Width Or Height: `0.5`

Add GraphicRaycaster (auto-added — keep defaults).

Rename GameObject to `Canvas`.

---

**Step 2 — Create HUDPanel:**

Right-click Canvas → Create Empty. Rename to `HUDPanel`.

RectTransform: anchor min=(0,0), max=(1,1) — stretch full. Offsets all zero.
No Image component. HUDPanel is a layout container only, always active.

---

**Step 3 — Create FloorGroup (top-left):**

Right-click HUDPanel → Create Empty. Rename to `FloorGroup`.

RectTransform:
- Anchor preset: Top-Left (min=(0,1), max=(0,1))
- Pivot: (0, 1)
- Pos X: 24, Pos Y: -24
- Width: auto (Content Size Fitter recommended) or set manually to 160, Height: 44

Add Image component:
- Color: (0, 0, 0, 0.55) — R=0 G=0 B=0 A=140

Right-click FloorGroup → UI → Text - TextMeshPro. Rename to `FloorLabel`.
TextMeshProUGUI settings:
- Text: `Floor 1`
- Font Size: `28`
- Font Style: Bold
- Color: White (1,1,1,1)
- Alignment: Middle Left
- RectTransform: anchored inside FloorGroup with 8px padding all sides

---

**Step 4 — Create GaugeGroup (top-center):**

Right-click HUDPanel → Create Empty. Rename to `GaugeGroup`.

RectTransform:
- Anchor preset: Top-Center (min=(0.5,1), max=(0.5,1))
- Pivot: (0.5, 1)
- Pos X: 0, Pos Y: -24
- Width: 216, Height: 32

Add Image component:
- Color: (0, 0, 0, 0.55)

Right-click GaugeGroup → UI → Image. Rename to `GaugeTrack`.
Image settings:
- Color: (0.133, 0.133, 0.133, 1) — hex #222222
- RectTransform: Width 200, Height 16, centered in GaugeGroup

Right-click GaugeGroup → UI → Image. Rename to `GaugeFill`.
Image settings:
- Image Type: `Filled`
- Fill Method: `Horizontal`
- Fill Origin: `Left`
- Fill Amount: `1` (starts full)
- Color: White (1,1,1,1)
- RectTransform: same size and position as GaugeTrack (Width 200, Height 16)

---

**Step 5 — Create AttackTypeGroup (top-right):**

Right-click HUDPanel → Create Empty. Rename to `AttackTypeGroup`.

RectTransform:
- Anchor preset: Top-Right (min=(1,1), max=(1,1))
- Pivot: (1, 1)
- Pos X: -24, Pos Y: -24
- Width: 140, Height: 44

Add Image component:
- Color: (0, 0, 0, 0.55)

Right-click AttackTypeGroup → UI → Text - TextMeshPro. Rename to `AttackTypeLabel`.
TextMeshProUGUI settings:
- Text: `LINEAR`
- Font Size: `28`
- Font Style: Bold
- Color: White (1,1,1,1)
- Alignment: Middle Right
- RectTransform: anchored inside AttackTypeGroup with 8px padding

---

**Step 6 — Attach and wire HUDController:**

Select HUDPanel in Hierarchy. In Inspector, Add Component → HUDController.

Wire SerializeField fields:
- `_floorLabel` → FloorLabel (TextMeshProUGUI)
- `_gaugeFill` → GaugeFill (Image)
- `_attackTypeLabel` → AttackTypeLabel (TextMeshProUGUI)
- `_gauge` → Player GameObject's ChronoGaugeController component

Save scene (Ctrl+S).

---

**Step 7 — Verify in Play Mode:**

Enter Play Mode. Check:
- [ ] "Floor 1" label visible top-left with semi-transparent backing
- [ ] Gauge fill bar visible top-center, starts full
- [ ] "LINEAR" label visible top-right
- [ ] Hold Attack button — gauge bar drains in real-time
- [ ] No magenta error quads in Game view
- [ ] No TMP errors in Console
  </action>
  <acceptance_criteria>
    - `Assets/TextMesh Pro/` folder exists (TMP import confirmed)
    - Canvas at scene root with Screen Space Overlay + CanvasScaler 1920x1080 Match 0.5
    - HUDPanel as Canvas child, always active
    - FloorGroup anchored top-left at (24, -24), has Image backing (0,0,0,0.55), contains FloorLabel TMP text "Floor 1"
    - GaugeGroup anchored top-center at (0, -24), contains GaugeTrack (#222222) and GaugeFill (Filled/Horizontal/Left)
    - AttackTypeGroup anchored top-right at (-24, -24), contains AttackTypeLabel TMP text "LINEAR"
    - HUDController component on HUDPanel with all 4 SerializeField fields wired
    - Play Mode: all 3 HUD elements visible, no magenta quads
    - Play Mode: gauge fill bar visually drains when Attack button held
    - Play Mode: attack type label reads "LINEAR" (default)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `Assets/Scripts/World/FloorManager.cs` exists — `public static class FloorManager` with `public static int CurrentFloor = 1`
2. `Assets/Scripts/UI/HUDController.cs` exists — `SetText("Floor {0}", ...)` and `fillAmount = _gauge.Value` in Update(), dirty-check on AttackType
3. `Assets/TextMesh Pro/` folder exists (TMP Essential Resources imported)
4. Enter Play Mode — zero magenta TMP error quads in Game view
5. "Floor 1" label visible in top-left area of screen
6. Gauge fill bar visible in top-center, starts full, drains when Attack held
7. "LINEAR" (or "FAN") label visible in top-right area
8. No NullReferenceException in Console during Play Mode
</verification>

<success_criteria>
- FloorManager.cs: static class, CurrentFloor = 1, no MonoBehaviour
- HUDController.cs: Update() polls all three sources with zero string allocation
- Canvas hierarchy: Screen Space Overlay, CanvasScaler 1920x1080 Match 0.5, HUDPanel with 3 groups
- TMP Essential Resources imported: no magenta quads in Play Mode
- All 3 HUD elements visible and live-updating in Play Mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-hud-game-loop/04-01-SUMMARY.md`
</output>
