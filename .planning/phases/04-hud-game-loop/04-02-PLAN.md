---
plan: "04-02"
phase: "04"
title: "DeathScreenController + Restart Flow (UI-02)"
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "Assets/Scripts/UI/DeathScreenController.cs"
  - "Assets/Scenes/SampleScene.unity"
requirements:
  - "UI-02"
autonomous: false
must_haves:
  - "DeathScreenController subscribes PlayerController.OnPlayerDeath in OnEnable, unsubscribes in OnDisable"
  - "HandleDeath: _deathPanel.SetActive(true), Time.timeScale = 0f, Time.fixedDeltaTime = 0f"
  - "RestartGame: Time.timeScale = 1f, Time.fixedDeltaTime = 0.02f, FloorManager.CurrentFloor = 1, SceneManager.LoadScene(0)"
  - "Time.timeScale and Time.fixedDeltaTime restored as a pair BEFORE SceneManager.LoadScene call"
  - "DeathPanel starts inactive in the scene (SetActive false in Inspector)"
  - "DeathOverlay: full-screen Image, color #000000 alpha 0.70"
  - "RestartButton: Button 240x56px centered, Normal=#FFFFFF, Highlighted=#CCCCCC, Pressed=#999999"
  - "RestartLabel: TextMeshProUGUI size 32 Bold, color #000000, text 'RESTART'"
  - "5 consecutive die-restart cycles complete without dev intervention or errors"
---

<objective>
Implement the death screen and restart flow. When the player dies, the world freezes and a centered Restart button appears over a dark overlay. Tapping restart restores time and reloads the scene from Floor 1. Delivers UI-02 entirely.

Depends on Plan 04-01 (Canvas with HUDPanel must exist; TMP Essential Resources must be imported).

Purpose: Closes the game loop — player can now die and restart without developer intervention.
Output: DeathScreenController.cs (new), SampleScene updated with DeathPanel hierarchy and wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/새 폴더/Fast/.planning/STATE.md
@D:/새 폴더/Fast/.planning/phases/04-hud-game-loop/04-RESEARCH.md
@D:/새 폴더/Fast/.planning/phases/04-hud-game-loop/04-UI-SPEC.md
</context>

<tasks>

<task id="04-02-T1" title="Create DeathScreenController MonoBehaviour">
  <read_first>
    - Assets/Scripts/Player/PlayerController.cs — confirm `public static event Action OnPlayerDeath` (line 49) and `public static void TriggerDeath()` (line 51)
    - Assets/Scripts/Player/PlayerDeathHandler.cs — confirm OnEnable/OnDisable subscription pattern; Phase 4 DeathScreenController coexists with this handler (both subscribe to the same event independently)
    - Assets/Scripts/World/FloorManager.cs — confirm `public static int CurrentFloor = 1` (created in Plan 04-01)
  </read_first>
  <action>
Create new file `Assets/Scripts/UI/DeathScreenController.cs`:

```csharp
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

public class DeathScreenController : MonoBehaviour
{
    [SerializeField] private GameObject _deathPanel;
    [SerializeField] private Button     _restartButton;

    private void OnEnable()
    {
        PlayerController.OnPlayerDeath += HandleDeath;
        _restartButton.onClick.AddListener(RestartGame);
    }

    private void OnDisable()
    {
        PlayerController.OnPlayerDeath -= HandleDeath;
        _restartButton.onClick.RemoveListener(RestartGame);
    }

    private void HandleDeath()
    {
        _deathPanel.SetActive(true);
        Time.timeScale      = 0f;
        Time.fixedDeltaTime = 0f;
    }

    private void RestartGame()
    {
        Time.timeScale         = 1f;
        Time.fixedDeltaTime    = 0.02f;
        FloorManager.CurrentFloor = 1;
        SceneManager.LoadScene(0);
    }
}
```

**Why OnEnable/OnDisable (not Start/Awake):** `PlayerController.OnPlayerDeath` is a static event — it persists across Play Mode cycles when domain reload is disabled. Subscribing in Start() would cause double-subscription after the second Play. OnEnable/OnDisable is the established pattern from PlayerDeathHandler.cs.

**Why `Time.timeScale = 1f` BEFORE `SceneManager.LoadScene(0)`:** If timeScale is 0f at the moment of scene load, the reloaded scene starts frozen — physics and coroutines never advance. The pair (timeScale + fixedDeltaTime) must always be restored together before calling LoadScene. Source: RESEARCH.md Pitfall 2.

**Why `FloorManager.CurrentFloor = 1` before LoadScene:** Static fields survive scene loads unless domain reload is active (which is off in builds). Explicit reset is mandatory for correctness in build targets. In editor with domain reload on, the reset is redundant but harmless.

**Why no confirmation dialog:** Restart is the only action on the death screen. Confirmation would be an anti-pattern. Source: REQUIREMENTS.md UI-02 "nothing else required to understand what to do."
  </action>
  <acceptance_criteria>
    - File exists at Assets/Scripts/UI/DeathScreenController.cs
    - `[SerializeField] private GameObject _deathPanel` present
    - `[SerializeField] private Button _restartButton` present
    - `PlayerController.OnPlayerDeath += HandleDeath` in OnEnable
    - `PlayerController.OnPlayerDeath -= HandleDeath` in OnDisable
    - `_restartButton.onClick.AddListener(RestartGame)` in OnEnable
    - `_restartButton.onClick.RemoveListener(RestartGame)` in OnDisable
    - `_deathPanel.SetActive(true)` in HandleDeath
    - `Time.timeScale = 0f` and `Time.fixedDeltaTime = 0f` in HandleDeath (set as a pair)
    - `Time.timeScale = 1f` and `Time.fixedDeltaTime = 0.02f` in RestartGame (set as a pair, BEFORE LoadScene)
    - `FloorManager.CurrentFloor = 1` in RestartGame before LoadScene
    - `SceneManager.LoadScene(0)` in RestartGame
    - Project compiles with no errors
  </acceptance_criteria>
</task>

<task id="04-02-T2" title="[HUMAN] Build DeathPanel hierarchy + wire DeathScreenController">
  <note>This task requires Unity Editor interaction. Claude cannot perform editor GUI actions. Steps must be executed by the human developer. Plan 04-01 must be complete (Canvas exists, TMP imported).</note>
  <action>
**Step 1 — Create DeathPanel under Canvas:**

In Hierarchy, right-click Canvas → Create Empty. Rename to `DeathPanel`.

In Inspector, UNCHECK the active checkbox next to the GameObject name — DeathPanel must start inactive.

RectTransform: Anchor preset stretch-full (min=(0,0), max=(1,1)), all offsets zero.

---

**Step 2 — Create DeathOverlay (full-screen dark background):**

Right-click DeathPanel → UI → Image. Rename to `DeathOverlay`.

Image settings:
- Color: (0, 0, 0, 0.70) — R=0, G=0, B=0, A=179
- RectTransform: anchor min=(0,0), max=(1,1) — stretch full. All offsets zero.

This is the dark overlay that dims the frozen world behind the restart button.

---

**Step 3 — Create RestartButton:**

Right-click DeathPanel → UI → Button - TextMeshPro. Rename to `RestartButton`.

Button component settings:
- Transition: Color Tint
- Normal Color: #FFFFFF (1,1,1,1)
- Highlighted Color: #CCCCCC (0.8,0.8,0.8,1)
- Pressed Color: #999999 (0.6,0.6,0.6,1)
- Disabled Color: #888888 at alpha 0.5
- Navigation: None

Image component (attached to Button):
- Color: #FFFFFF (1,1,1,1)

RectTransform:
- Anchor preset: Middle-Center (min=(0.5,0.5), max=(0.5,0.5))
- Pivot: (0.5, 0.5)
- Pos X: 0, Pos Y: 0
- Width: 240, Height: 56

---

**Step 4 — Configure RestartLabel (TMP text child of RestartButton):**

Expand RestartButton — there should be a Text (TMP) child. Rename to `RestartLabel`.

TextMeshProUGUI settings:
- Text: `RESTART`
- Font Size: `32`
- Font Style: Bold
- Color: (0, 0, 0, 1) — Black
- Alignment: Center Middle
- RectTransform: stretch-fill parent (anchor all corners to parent corners, all offsets zero)

---

**Step 5 — Attach DeathScreenController:**

Select Canvas in Hierarchy (or create a new empty child named `UIManager`).
In Inspector, Add Component → DeathScreenController.

Wire SerializeField fields:
- `_deathPanel` → DeathPanel GameObject
- `_restartButton` → RestartButton Button component (drag RestartButton GameObject onto the Button field)

Save scene (Ctrl+S).

---

**Step 6 — Verify the full game loop in Play Mode:**

Enter Play Mode. Perform these checks:
- [ ] DeathPanel is NOT visible at game start (starts inactive — confirm in Hierarchy)
- [ ] Walk player into a MeleeEnemy or RangedEnemy — death event fires
- [ ] DeathPanel appears: dark overlay covers screen, "RESTART" button visible in center
- [ ] Game world is frozen (Time.timeScale = 0, physics stopped)
- [ ] HUDPanel remains visible behind the overlay (HUD is not hidden)
- [ ] Tap/click RESTART button — scene reloads
- [ ] After reload: gauge bar full, "Floor 1" label, HUD visible, DeathPanel hidden
- [ ] Repeat 5 times: die → restart → die → restart (5 full cycles, no dev intervention, no console errors)
  </action>
  <acceptance_criteria>
    - DeathPanel is a child of Canvas, starts inactive (unchecked in Inspector)
    - DeathOverlay is a child of DeathPanel: Image, color (0,0,0,0.70), stretch-full anchor
    - RestartButton is a child of DeathPanel: Button, 240x56px, centered, ColorBlock Normal=#FFFFFF Highlighted=#CCCCCC Pressed=#999999
    - RestartLabel is a child of RestartButton: TextMeshProUGUI, size 32, Bold, color Black, text "RESTART"
    - DeathScreenController attached in scene with _deathPanel and _restartButton wired
    - Play Mode: DeathPanel invisible at start
    - Play Mode: death event shows DeathPanel + freezes time (physics stops)
    - Play Mode: Restart button click reloads scene — HUD resets to Floor 1, gauge full
    - Play Mode: 5 consecutive die-restart cycles complete without errors or dev intervention
    - Console: no NullReferenceException, no TMP errors across 5 cycles
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `Assets/Scripts/UI/DeathScreenController.cs` exists — OnEnable/OnDisable pattern, time-restore before LoadScene, FloorManager.CurrentFloor reset
2. Enter Play Mode — DeathPanel not visible at start
3. Walk into enemy — DeathPanel appears with dark overlay + RESTART button. World freezes.
4. Click RESTART — scene reloads. HUD shows "Floor 1", gauge full. No console errors.
5. Run 5 consecutive die-restart cycles — no dev intervention required, no errors
6. grep `Time.timeScale = 1f` in DeathScreenController.cs — appears BEFORE SceneManager.LoadScene(0)
7. grep `OnPlayerDeath -= HandleDeath` in DeathScreenController.cs — unsubscribe present in OnDisable
</verification>

<success_criteria>
- DeathScreenController.cs: OnEnable/OnDisable subscription pattern, time pair restored before LoadScene, FloorManager reset
- DeathPanel: starts inactive, DeathOverlay covers full screen, RestartButton centered 240x56px
- RestartLabel: "RESTART", size 32, Bold, Black
- 5 consecutive die-restart cycles: zero errors, zero dev intervention
- Phase 4 HUD always visible throughout all cycles
</success_criteria>

<output>
After completion, create `.planning/phases/04-hud-game-loop/04-02-SUMMARY.md`
</output>
