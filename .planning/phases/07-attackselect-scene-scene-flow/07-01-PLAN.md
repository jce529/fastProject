---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/AttackSelectController.cs
  - Assets/Scripts/UI/DeathScreenController.cs
autonomous: true
requirements:
  - ATKS-01
  - ATKS-02
  - FLOW-01
must_haves:
  truths:
    - "Assets/Scripts/UI/AttackSelectController.cs 파일이 존재하고 OnLinearClicked() / OnFanClicked() 메서드를 포함한다"
    - "OnLinearClicked()는 AttackTypeSelector.SetType(AttackType.Linear) 후 SceneManager.LoadScene(\"SampleScene\")을 호출한다"
    - "OnFanClicked()는 AttackTypeSelector.SetType(AttackType.Fan) 후 SceneManager.LoadScene(\"SampleScene\")을 호출한다"
    - "DeathScreenController.RestartGame()이 SceneManager.LoadScene(\"AttackSelect\")를 호출한다 — SceneManager.LoadScene(0) 아님"
  artifacts:
    - path: "Assets/Scripts/UI/AttackSelectController.cs"
      provides: "AttackSelect 씬 버튼 핸들러 — SetType() 호출 후 SampleScene 로드"
    - path: "Assets/Scripts/UI/DeathScreenController.cs"
      provides: "사망 후 AttackSelect 복귀 — LoadScene(0) → LoadScene(\"AttackSelect\")"
  key_links:
    - from: "AttackSelectController.OnLinearClicked"
      to: "AttackTypeSelector.Selected = Linear, SampleScene 로드"
      via: "SetType(AttackType.Linear) + SceneManager.LoadScene(\"SampleScene\")"
    - from: "AttackSelectController.OnFanClicked"
      to: "AttackTypeSelector.Selected = Fan, SampleScene 로드"
      via: "SetType(AttackType.Fan) + SceneManager.LoadScene(\"SampleScene\")"
    - from: "DeathScreenController.RestartGame"
      to: "AttackSelect 씬 복귀"
      via: "SceneManager.LoadScene(\"AttackSelect\")"
---

<objective>
AttackSelectController.cs를 신규 생성하고 DeathScreenController.cs의 씬 로드 타겟을 수정한다.

Purpose: ATKS-01/02 충족 — LINEAR/FAN 버튼 클릭 시 타입 설정 + SampleScene 로드. FLOW-01 충족 — 사망 후 AttackSelect 복귀.
Output: AttackSelectController.cs (신규), DeathScreenController.cs (수정)
</objective>

<tasks>

<task id="1" type="auto">
  <name>AttackSelectController.cs 신규 생성</name>

  <read_first>
    - Assets/Scripts/UI/MainMenuController.cs — 동일 패턴 참조 (OnStartClicked 구조)
    - Assets/Scripts/UI/AttackTypeSelector.cs — SetType() 시그니처 확인 (public static void SetType(AttackType type))
  </read_first>

  <action>
Assets/Scripts/UI/AttackSelectController.cs 파일을 새로 생성한다.

파일 내용 (정확히 아래와 같이 작성):
```csharp
using UnityEngine;
using UnityEngine.SceneManagement;

public class AttackSelectController : MonoBehaviour
{
    public void OnLinearClicked()
    {
        AttackTypeSelector.SetType(AttackType.Linear);
        SceneManager.LoadScene("SampleScene");
    }

    public void OnFanClicked()
    {
        AttackTypeSelector.SetType(AttackType.Fan);
        SceneManager.LoadScene("SampleScene");
    }
}
```

MainMenuController.cs와 동일한 구조: public void 메서드, using UnityEngine.SceneManagement.
#if UNITY_EDITOR 블록 불필요 (Quit 없음).
  </action>

  <verify>
    <automated>
    grep -n "OnLinearClicked\|OnFanClicked" "Assets/Scripts/UI/AttackSelectController.cs" && grep -n "SetType(AttackType.Linear)\|SetType(AttackType.Fan)" "Assets/Scripts/UI/AttackSelectController.cs" && grep -c "LoadScene" "Assets/Scripts/UI/AttackSelectController.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/UI/AttackSelectController.cs 파일이 존재한다
    - 파일에 `public void OnLinearClicked()` 문자열이 존재한다
    - 파일에 `public void OnFanClicked()` 문자열이 존재한다
    - 파일에 `AttackTypeSelector.SetType(AttackType.Linear)` 문자열이 존재한다
    - 파일에 `AttackTypeSelector.SetType(AttackType.Fan)` 문자열이 존재한다
    - 파일에 `SceneManager.LoadScene("SampleScene")` 문자열이 2회 존재한다 (Linear/Fan 각 1회)
    - 파일에 `using UnityEngine.SceneManagement;` 선언이 존재한다
  </acceptance_criteria>
  <done>AttackSelectController.cs가 생성되어 LINEAR/FAN 버튼 클릭 시 AttackType을 설정하고 SampleScene을 로드한다.</done>
</task>

<task id="2" type="auto">
  <name>DeathScreenController.cs — RestartGame() 씬 로드 타겟 변경</name>

  <read_first>
    - Assets/Scripts/UI/DeathScreenController.cs — 현재 RestartGame() 구현 확인 (SceneManager.LoadScene(0) 존재 여부)
  </read_first>

  <action>
Assets/Scripts/UI/DeathScreenController.cs의 RestartGame() 메서드에서 씬 로드 호출을 변경한다.

변경 전:
```csharp
SceneManager.LoadScene(0);
```

변경 후:
```csharp
SceneManager.LoadScene("AttackSelect");
```

RestartGame() 내의 다른 코드(Time.timeScale = 1f, Time.fixedDeltaTime = 0.02f, FloorManager.CurrentFloor = 1)는 그대로 유지한다.
나머지 메서드(HandleDeath, OnEnable, OnDisable)는 일절 수정하지 않는다.
  </action>

  <verify>
    <automated>
    grep -n "LoadScene" "Assets/Scripts/UI/DeathScreenController.cs" && grep -n "FloorManager.CurrentFloor" "Assets/Scripts/UI/DeathScreenController.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/UI/DeathScreenController.cs에 `SceneManager.LoadScene("AttackSelect")` 문자열이 존재한다
    - `SceneManager.LoadScene(0)` 문자열이 파일에 존재하지 않는다
    - `FloorManager.CurrentFloor = 1` 코드가 RestartGame() 내에 여전히 존재한다
    - `Time.timeScale = 1f` 코드가 RestartGame() 내에 여전히 존재한다
  </acceptance_criteria>
  <done>DeathScreenController.RestartGame()이 AttackSelect 씬으로 복귀하도록 수정되어 FLOW-01이 코드 수준에서 완성된다.</done>
</task>

</tasks>

<verification>
- [ ] Assets/Scripts/UI/AttackSelectController.cs 파일 존재
- [ ] AttackSelectController에 OnLinearClicked() / OnFanClicked() 메서드 존재
- [ ] 각 메서드가 SetType() + LoadScene("SampleScene") 호출
- [ ] DeathScreenController.cs에 SceneManager.LoadScene("AttackSelect") 존재
- [ ] DeathScreenController.cs에 SceneManager.LoadScene(0) 미존재
- [ ] FloorManager.CurrentFloor = 1 코드 유지
- [ ] Unity Editor 컴파일 에러 없음
</verification>
