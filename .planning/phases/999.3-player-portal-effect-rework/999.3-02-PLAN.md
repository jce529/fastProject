---
phase: 999.3-player-portal-effect-rework
plan: 02
type: execute
wave: 2
depends_on: ["999.3-01"]
files_modified:
  - Assets/Scripts/World/FloorTransitionEffect.cs
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "포탈 진입 시 플레이어 Transform이 포탈 중심으로 실제로 이동하며(기존엔 SpriteMask만 움직였음), vortexMaterial이 할당되어 있으면 소용돌이 셰이더 이펙트가 함께 재생된다 (D-01/D-02/D-03)"
    - "PlayEntry()는 더 이상 SpriteMask/EntryMask GameObject를 생성하지 않는다 (D-03: 기존 마스크 페이드 완전 대체)"
    - "새 층 진입 시 IsDashing 애니메이터 bool이 true→false로 토글되며, 플레이어가 spawnWorldPos 기준 순수 수직 arc(사인 곡선)로 도약한 뒤 정확히 spawnWorldPos에 착지한다 — 수평 이동은 없다 (D-04/D-05)"
    - "PlayExit()는 더 이상 SpriteMask/ExitMask GameObject를 생성하지 않지만, 기존 포탈 VFX 프리팹(X1 성장/X4 페이드)은 배경 요소로 그대로 재생된다 (D-06, Open Question 1 기본값: KEEP)"
    - "PlayEntry()/PlayExit() 양쪽 모두 AudioManager.PlaySfx(Sfx.PortalEnter/PortalExit) 호출이 각 메서드 시작부에 그대로 남아있다 (D-07)"
    - "WorldGenerator.FloorTransitionSequence()의 ENTRY 호출부가 새 PlayEntry(Transform, Material) 시그니처에 맞춰 _portalVortexMaterial을 전달한다 — PlayExit() 호출 시그니처는 변경되지 않는다"
  artifacts:
    - path: "Assets/Scripts/World/FloorTransitionEffect.cs"
      provides: "재작성된 포탈 진입(소용돌이 흡입)/퇴장(수직 도약) 연출"
      contains: "public IEnumerator PlayEntry(Transform portal, Material vortexMaterial)"
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "_portalVortexMaterial 필드 + PlayEntry 호출부 배선"
      contains: "_portalVortexMaterial"
  key_links:
    - from: "Assets/Scripts/World/WorldGenerator.cs"
      to: "Assets/Scripts/World/FloorTransitionEffect.cs"
      via: "FloorTransitionSequence()의 ENTRY 단계 호출"
      pattern: "PlayEntry\\(portal\\.transform, _portalVortexMaterial\\)"
    - from: "Assets/Scripts/World/FloorTransitionEffect.cs"
      to: "FastPlayerAnimator.controller Dash 상태"
      via: "SetBool(\"IsDashing\", ...) — 기존 CombatController.ExecuteDash()와 동일 파라미터 재사용"
      pattern: "SetBool\\(\"IsDashing\""
    - from: "Assets/Scripts/World/FloorTransitionEffect.cs"
      to: "Assets/Shaders/PortalVortex.shader (999.3-01 산출물)"
      via: "SpriteRenderer.material = new Material(vortexMaterial) — 인스턴스 카피, sharedMaterial 금지"
      pattern: "new Material\\(vortexMaterial\\)"
---

<objective>
`FloorTransitionEffect.cs`의 `PlayEntry()`/`PlayExit()`를 완전히 재작성한다. `PlayEntry()`는 기존 SpriteMask 경계선 페이드(E1-E4)를 999.3-01에서 검증한 `PortalVortex` 셰이더 오버레이 + 플레이어 Transform 실제 이동으로 교체한다(D-01/D-02/D-03). `PlayExit()`는 기존 마스크 수축(X2/X3)을 `CombatController.ExecuteDash()`가 이미 쓰는 `IsDashing` 애니메이터 상태를 재사용한 수직 도약(arc)으로 교체하되, 포탈 배경 VFX(X1 성장/X4 페이드)는 그대로 유지한다(D-04/D-05/D-06, Open Question 1 기본값).

Purpose: "SpriteMask만 움직이고 플레이어 Transform 자체는 고정돼 있다"는 이 페이즈의 근본 불만(999.3-CONTEXT.md `<domain>`)을 해소한다 — 이제 두 연출 모두 플레이어 Transform이 실제로 이동한다.
Output: 재작성된 `FloorTransitionEffect.cs`(PlayEntry/PlayExit 전체 교체, 호출 시그니처는 PlayExit만 그대로 유지), `WorldGenerator.cs`의 최소 배선 변경(1개 필드 + 1개 호출부).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.3-player-portal-effect-rework/999.3-CONTEXT.md
@.planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md
@Assets/Scripts/World/FloorTransitionEffect.cs
@Assets/Scripts/World/WorldGenerator.cs
@Assets/Scripts/World/RuntimeMaskSprite.cs
@Assets/Scripts/Player/CombatController.cs
@Assets/Scripts/Player/RollController.cs
@Assets/Scripts/Audio/AudioManager.cs

<interfaces>
<!-- 재사용 대상 기존 API — 탐색 불필요. 이 태스크에서 변경하지 않는다. -->

From Assets/Scripts/World/RuntimeMaskSprite.cs (변경 금지):
```csharp
public static class RuntimeMaskSprite
{
    public static Sprite CreateMaskSprite(); // 캐시된 4x4 흰색 Sprite — 새 vortex 오버레이의 SpriteRenderer.sprite(UV 0..1 매핑용)로도 재사용
}
```

From Assets/Scripts/Audio/AudioManager.cs (변경 금지):
```csharp
public enum Sfx { PortalEnter, PortalExit, Slash, EnemyDeathGlitch }
public static void PlaySfx(Sfx id, float volume = 1f); // null 안전
```

From Assets/Player/Resource/Animation/FastPlayerAnimator.controller (999.3-RESEARCH.md 직접 검증 — 변경 금지):
```yaml
# Dash 상태: IsDashing은 bool(트리거 아님). Enter/Exit 트랜지션 모두 TransitionDuration=0, HasExitTime=0.
# => true인 동안 계속 Dash 포즈를 유지하고, false가 되는 즉시 빠져나온다 — 커스텀 길이의 도약에 안전하게 재사용 가능.
```

From Assets/Scripts/Player/CombatController.cs (ExecuteDash 발췌, 변경하지 않음 — IsDashing 재사용 패턴 참고용):
```csharp
_animator?.SetBool("IsDashing", true);
...
_rb.MovePosition(...); // FixedUpdate 루프
...
_animator?.SetBool("IsDashing", false);
```
(주의: ExecuteDash는 `WaitForFixedUpdate`+`Time.fixedDeltaTime` 패턴을 쓰지만, 이는 `ExitSlowMotion()`이 먼저 `Time.timeScale=1`을 보장하기 때문에만 안전하다. FloorTransitionEffect는 그런 보장이 없으므로 절대 이 패턴을 복사하지 않는다 — 아래 Task들은 기존 `FloorTransitionEffect`/`RollController`와 동일한 `Time.unscaledDeltaTime` + `yield return null` 루프를 사용한다.)

Current Assets/Scripts/World/FloorTransitionEffect.cs 전체(127줄, 그대로 아래 재현 — Task 1/2가 이 파일을 순차적으로 수정한다):
```csharp
using System.Collections;
using UnityEngine;

[RequireComponent(typeof(SpriteRenderer))]
public class FloorTransitionEffect : MonoBehaviour
{
    private SpriteRenderer _sr;

    [SerializeField] private float _entryMaskDuration = 0.4f;
    [SerializeField] private float _portalShrinkDuration = 0.3f;
    [SerializeField] private float _exitPortalGrowDuration = 0.4f;
    [SerializeField] private float _exitMaskDuration = 0.5f;
    [SerializeField] private float _portalFadeDuration = 0.3f;

    private void Awake()
    {
        _sr = GetComponent<SpriteRenderer>();
    }

    public IEnumerator PlayEntry(Transform portal) { /* E1-E4 — Task 1이 전체 교체 */ }

    public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab) { /* X1-X4 — Task 2가 X2/X3만 교체, X1/X4는 유지 */ }

    private IEnumerator ScaleTransform(Transform t, Vector3 from, Vector3 to, float duration) { /* 공용 헬퍼 — 변경하지 않음, X1/X4가 계속 사용 */ }
}
```

Current WorldGenerator.cs 관련 라인 (변경 대상 정확한 위치):
```csharp
// 49-50줄 (Header 필드 블록)
[Header("Transition Animation (Phase 12, D-01~D-04)")]
[SerializeField] private GameObject _portalEffectPrefab;

// 605-606줄 (FloorTransitionSequence ENTRY 호출부)
if (_transitionEffect != null)
    yield return _transitionEffect.PlayEntry(portal.transform);

// 668-669줄 (FloorTransitionSequence EXIT 호출부 — 이 태스크에서 변경하지 않음, PlayExit 시그니처 그대로 유지)
if (_transitionEffect != null)
    yield return _transitionEffect.PlayExit(teleportPos, _portalEffectPrefab);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PlayEntry() 소용돌이 흡입 재작성 + WorldGenerator 배선 (D-01/D-02/D-03/D-07)</name>
  <files>Assets/Scripts/World/FloorTransitionEffect.cs, Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/FloorTransitionEffect.cs (전체 127줄 — 특히 1-59줄: usings/필드/Awake/PlayEntry)
    - Assets/Scripts/World/WorldGenerator.cs (49-50줄 Header 필드 블록, 132-138줄 _transitionEffect AddComponent, 604-608줄 ENTRY 호출부)
    - Assets/Scripts/World/RuntimeMaskSprite.cs (CreateMaskSprite 시그니처)
    - .planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md (Pitfall 3 — Renderer.material 인스턴스 카피 필수, Pitfall 5 — spawnWorldPos/portal.position은 이미 정확한 좌표이므로 추가 오프셋 계산 불필요)
  </read_first>
  <action>
    **1. `Assets/Scripts/World/FloorTransitionEffect.cs` — 필드 블록 교체 (15-19줄):**

    변경 전:
    ```csharp
    [SerializeField] private float _entryMaskDuration = 0.4f;
    [SerializeField] private float _portalShrinkDuration = 0.3f;
    [SerializeField] private float _exitPortalGrowDuration = 0.4f;
    [SerializeField] private float _exitMaskDuration = 0.5f;
    [SerializeField] private float _portalFadeDuration = 0.3f;
    ```

    변경 후 (마지막 3줄 `_exitPortalGrowDuration`/`_exitMaskDuration`/`_portalFadeDuration`은 Task 2가 아직 사용 중이므로 이 태스크에서는 값을 그대로 둔 채 유지한다 — `_exitMaskDuration`은 Task 2가 제거할 예정):
    ```csharp
    [Header("Entry Vortex (Phase 999.3 D-01~D-03)")]
    [SerializeField] private Material _vortexMaterial;          // PortalVortex.mat — WorldGenerator가 PlayEntry() 호출 시 전달 (999.3-01 산출물)
    [SerializeField] private float _entryVortexDuration = 0.4f; // D-08: 기존 E1-E4 총합(~0.4s)과 동일 기준 유지
    [SerializeField] private float _vortexWorldRadius = 4f;     // 소용돌이가 덮는 월드 반경 — 플레이어+주변 타일 포함

    [SerializeField] private float _exitPortalGrowDuration = 0.4f;
    [SerializeField] private float _exitMaskDuration = 0.5f;    // NOTE: Plan 999.3-02 Task 2가 이 필드를 leap 필드로 교체 예정 — 이 태스크에서는 값만 유지
    [SerializeField] private float _portalFadeDuration = 0.3f;
    ```

    클래스 상단(필드 선언부 바로 뒤, `private SpriteRenderer _sr;` 다음 줄)에 아래 2개 필드와 2개 static readonly 프로퍼티 ID 추가:
    ```csharp
    private Rigidbody2D _rb;
    private Animator _animator;

    private static readonly int UnscaledTimeId = Shader.PropertyToID("_UnscaledTime");
    private static readonly int EffectAlphaId  = Shader.PropertyToID("_EffectAlpha");
    ```

    **2. `Awake()` 교체 (21-24줄):**

    변경 전:
    ```csharp
    private void Awake()
    {
        _sr = GetComponent<SpriteRenderer>();
    }
    ```

    변경 후:
    ```csharp
    private void Awake()
    {
        _sr = GetComponent<SpriteRenderer>();
        _rb = GetComponent<Rigidbody2D>();       // D-05: 흡입/도약 이동에 사용 — Player 루트에 이미 존재(CombatController RequireComponent)
        _animator = GetComponent<Animator>();    // D-04: IsDashing 재사용에 사용
    }
    ```

    **3. `PlayEntry(Transform portal)` 메서드 전체를 아래로 교체 (기존 26-59줄, E1-E4 전체 삭제 — SpriteMask/EntryMask GameObject 생성 코드 완전 제거):**
    ```csharp
    /// <summary>D-01/D-02/D-03: 포탈 진입 시 URP 셰이더 기반 소용돌이 흡입 이펙트를 재생하고, 플레이어
    /// Transform 자체도 포탈 중심으로 실제 이동한다(기존 SpriteMask 페이드를 완전히 대체 — 겹쳐 쓰지 않음).
    /// vortexMaterial은 WorldGenerator가 호출 시 전달한다(null이면 이동만 재생, 셰이더 오버레이는 생략).</summary>
    public IEnumerator PlayEntry(Transform portal, Material vortexMaterial)
    {
        AudioManager.PlaySfx(Sfx.PortalEnter); // SFX-02/D-07: 그대로 재사용 — 새 이펙트 시작과 동시

        Vector3 startPos = transform.position;

        GameObject vortexGO = null;
        Material vortexMat = null;
        if (vortexMaterial != null)
        {
            vortexGO = new GameObject("EntryVortex");
            vortexGO.transform.position = portal.position;
            vortexGO.transform.localScale = new Vector3(_vortexWorldRadius * 2f, _vortexWorldRadius * 2f, 1f);
            var vortexSr = vortexGO.AddComponent<SpriteRenderer>();
            vortexSr.sprite = RuntimeMaskSprite.CreateMaskSprite(); // 기존 12-01/14-01 공용 4x4 흰 스프라이트 재사용 — UV 0..1 매핑용
            vortexSr.sortingLayerName = "PortalVFX"; // 999.3-01에서 신설한 레이어 — Default보다 항상 위에 렌더되어야 그랩 텍스처를 왜곡해 덮어씌울 수 있다
            vortexMat = new Material(vortexMaterial); // Pitfall 3: 인스턴스 카피 — sharedMaterial 절대 금지(플레이어+전 Room Tilemap이 공유하는 기본 머티리얼과 무관하게 항상 신규 인스턴스)
            vortexSr.material = vortexMat;
        }

        if (_rb != null) _rb.linearVelocity = Vector2.zero;

        float elapsed = 0f;
        while (elapsed < _entryVortexDuration)
        {
            elapsed += Time.unscaledDeltaTime; // 슬로우모션/HitFreeze 면역
            float t = Mathf.Clamp01(elapsed / _entryVortexDuration);

            if (vortexMat != null)
            {
                // Pitfall 1: Shader Graph/_Time 대신 반드시 수동으로 Time.unscaledTime을 매 프레임 주입
                vortexMat.SetFloat(UnscaledTimeId, Time.unscaledTime);
                vortexMat.SetFloat(EffectAlphaId, Mathf.Sin(t * Mathf.PI)); // 0→1→0 — 등장과 동시에 옅어지며 사라짐
            }

            // 핵심 불만 해소(999.3-CONTEXT.md <domain>): 플레이어 Transform이 실제로 포탈 중심을 향해
            // 이동한다 — 기존엔 SpriteMask만 움직이고 Transform은 고정이었다.
            Vector3 pos = Vector3.Lerp(startPos, portal.position, t * t); // ease-in
            if (_rb != null) _rb.MovePosition(pos);
            else transform.position = pos;

            yield return null;
        }

        if (_rb != null) _rb.MovePosition(portal.position);
        else transform.position = portal.position;

        _sr.enabled = false; // 기존 E4와 동일 목적 — 완전 비가시 전환(새 층 텔레포트 전까지)
        if (vortexGO != null) Destroy(vortexGO);
    }
    ```
    (구버전 E3의 "포탈 자체 수축"은 이 태스크에서 재현하지 않는다 — 999.3-CONTEXT.md D-03이 "PlayEntry() 전체"의 완전 대체를 명시하므로, 포탈 GameObject는 소용돌이 재생 중 그대로 있다가 `WorldGenerator.FloorTransitionSequence()`가 옛 체인을 Destroy하는 시점에 함께 정리된다 — 그 시점은 이미 새 층 전환 시퀀스 중이라 급격한 사라짐이 체감되지 않는다.)

    `PlayExit()`와 `ScaleTransform()` 메서드는 이 태스크에서 **한 글자도 건드리지 않는다** — Task 2의 영역이다.

    **4. `Assets/Scripts/World/WorldGenerator.cs` — 2곳 수정:**

    Header 필드 블록(49-50줄) 변경 전:
    ```csharp
    [Header("Transition Animation (Phase 12, D-01~D-04)")]
    [SerializeField] private GameObject _portalEffectPrefab;
    ```
    변경 후:
    ```csharp
    [Header("Transition Animation (Phase 12, D-01~D-04 / Phase 999.3 D-01~D-08)")]
    [SerializeField] private GameObject _portalEffectPrefab;
    [SerializeField] private Material _portalVortexMaterial; // Phase 999.3 D-01~D-03: PortalVortex.mat — Inspector 할당은 999.3-03에서 SampleScene에 배선
    ```

    ENTRY 호출부(604-608줄 부근) 변경 전:
    ```csharp
        if (_transitionEffect != null)
            yield return _transitionEffect.PlayEntry(portal.transform);
    ```
    변경 후:
    ```csharp
        if (_transitionEffect != null)
            yield return _transitionEffect.PlayEntry(portal.transform, _portalVortexMaterial);
    ```

    EXIT 호출부(`PlayExit(teleportPos, _portalEffectPrefab)`)는 시그니처가 바뀌지 않으므로 이 태스크에서 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -n "PlayEntry(Transform portal, Material vortexMaterial)" Assets/Scripts/World/FloorTransitionEffect.cs && grep -c "\"EntryMask\"" Assets/Scripts/World/FloorTransitionEffect.cs && grep -n "PlayEntry(portal.transform, _portalVortexMaterial)" Assets/Scripts/World/WorldGenerator.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/World/FloorTransitionEffect.cs`에 `public IEnumerator PlayEntry(Transform portal, Material vortexMaterial)` 시그니처가 존재한다
    - `"EntryMask"` 문자열이 파일에서 완전히 사라졌다 (grep 매치 0건 — 옛 SpriteMask 생성 코드 제거 확인)
    - `_entryMaskDuration`, `_portalShrinkDuration` 필드가 파일에서 완전히 사라졌다 (고아 필드 제거)
    - `_vortexMaterial`, `_entryVortexDuration`, `_vortexWorldRadius`, `_rb`, `_animator` 필드가 모두 존재한다
    - `Assets/Scripts/World/WorldGenerator.cs`에 `_portalVortexMaterial` 필드 선언과 `PlayEntry(portal.transform, _portalVortexMaterial)` 호출부가 각 1회씩 존재한다
    - `Assets/Scripts/World/WorldGenerator.cs`의 `PlayExit(teleportPos, _portalEffectPrefab)` 호출부는 이 태스크 전후로 문자 그대로 동일하다 (git diff에 해당 줄 없음)
  </acceptance_criteria>
  <done>포탈 진입 시 플레이어가 실제로 포탈 중심으로 이동하며, vortexMaterial이 배선되면 소용돌이 셰이더가 함께 재생된다. PlayExit()는 아직 옛 마스크 로직 그대로다(Task 2에서 교체).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PlayExit() 수직 도약 재작성 (D-04/D-05/D-06/D-07)</name>
  <files>Assets/Scripts/World/FloorTransitionEffect.cs</files>
  <read_first>
    - Assets/Scripts/World/FloorTransitionEffect.cs (Task 1 완료 후 상태 — 특히 PlayExit() 메서드, ScaleTransform() 헬퍼)
    - Assets/Scripts/Player/CombatController.cs (ExecuteDash의 IsDashing SetBool 순서 — 재확인용, 이 파일은 수정하지 않음)
    - Assets/Player/Resource/Animation/FastPlayerAnimator.controller (IsDashing bool 컨디션 확인 — 이 파일은 수정하지 않음)
    - .planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md (Pitfall 4 — IsDashing 동시성은 LockInput()이 이미 보장하므로 재확인만, Pitfall 5 — spawnWorldPos는 이미 정확한 착지 좌표)
  </read_first>
  <action>
    **1. 필드 블록 마지막 3줄 교체 (Task 1이 남긴 상태 기준):**

    변경 전:
    ```csharp
    [SerializeField] private float _exitPortalGrowDuration = 0.4f;
    [SerializeField] private float _exitMaskDuration = 0.5f;    // NOTE: Plan 999.3-02 Task 2가 이 필드를 leap 필드로 교체 예정 — 이 태스크에서는 값만 유지
    [SerializeField] private float _portalFadeDuration = 0.3f;
    ```

    변경 후:
    ```csharp
    [Header("Exit Portal Backdrop — X1/X4 존치 (Open Question 1 기본값: KEEP)")]
    [SerializeField] private float _exitPortalGrowDuration = 0.4f;
    [SerializeField] private float _portalFadeDuration = 0.3f;

    [Header("Exit Leap (Phase 999.3 D-04~D-06)")]
    [SerializeField] private float _exitLeapDuration = 0.45f; // D-08: 기존 X1-X4 총합(~0.4~0.5s)과 동일 기준 유지
    [SerializeField] private float _exitLeapHeight = 2.5f;    // 포탈에서 머리 위로 튀어오르는 높이(월드 유닛)
    ```

    **2. `PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)` 메서드 전체를 아래로 교체 (기존 X2/X3 SpriteMask 로직 완전 제거, X1/X4는 그대로 유지 — 호출 시그니처는 변경하지 않는다, WorldGenerator.cs 호출부도 그대로):**
    ```csharp
    /// <summary>D-04/D-05/D-06: 새 층 진입 시 기존 IsDashing 애니메이터 대시 모션을 재사용해 포탈에서
    /// 머리 위로 짧게 도약(수직 arc)한 뒤 원래 spawnWorldPos에 정확히 착지한다. 기존 SpriteMask
    /// 수축(X2/X3)은 완전히 대체됨 — X1(포탈 성장)/X4(포탈 페이드)는 Open Question 1 기본값(KEEP)에
    /// 따라 배경 요소로 존치한다.</summary>
    public IEnumerator PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)
    {
        AudioManager.PlaySfx(Sfx.PortalExit); // SFX-02/D-07: 그대로 재사용

        // X1(존치): 퇴장 포탈 이펙트 성장 — 도약과 동시 진행(병렬, StartCoroutine — 대기하지 않음)
        GameObject portalEffect = null;
        if (portalEffectPrefab != null)
        {
            portalEffect = Instantiate(portalEffectPrefab, spawnWorldPos, Quaternion.identity);
            portalEffect.transform.localScale = Vector3.zero;
            StartCoroutine(ScaleTransform(portalEffect.transform, Vector3.zero, Vector3.one, _exitPortalGrowDuration));
        }

        // D-04/D-05/D-06: X2/X3 마스크 로직을 수직 도약으로 완전히 대체
        _sr.enabled = true;
        if (_animator == null) _animator = GetComponent<Animator>(); // Awake에서 이미 캐시되지만 방어적으로 재확인
        _animator?.SetBool("IsDashing", true);
        if (_rb != null) _rb.linearVelocity = Vector2.zero;

        // Pitfall 5: spawnWorldPos는 이미 WorldGenerator.FloorTransitionSequence Step 2에서 하드
        // 텔레포트된 현재 위치와 동일하다 — 수평 이동 없이 순수 수직 오프셋만 적용한다.
        float elapsed = 0f;
        while (elapsed < _exitLeapDuration)
        {
            elapsed += Time.unscaledDeltaTime; // 슬로우모션/HitFreeze 면역
            float t = Mathf.Clamp01(elapsed / _exitLeapDuration);
            float height = _exitLeapHeight * Mathf.Sin(t * Mathf.PI); // 0 -> 정점 -> 0
            Vector3 pos = spawnWorldPos + Vector3.up * height;
            if (_rb != null) _rb.MovePosition(pos);
            else transform.position = pos;
            yield return null;
        }

        if (_rb != null) _rb.MovePosition(spawnWorldPos);
        else transform.position = spawnWorldPos;
        _animator?.SetBool("IsDashing", false);

        // X4(존치): 퇴장 포탈 이펙트 페이드 아웃 후 정리
        if (portalEffect != null)
        {
            yield return ScaleTransform(portalEffect.transform, Vector3.one, Vector3.zero, _portalFadeDuration);
            Destroy(portalEffect);
        }
    }
    ```

    `ScaleTransform()` 헬퍼는 X1/X4가 계속 사용하므로 이 태스크에서 변경하지 않는다. `PlayEntry()`는 Task 1에서 이미 완료되었으므로 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -c "SetBool(\"IsDashing\"" Assets/Scripts/World/FloorTransitionEffect.cs && grep -c "\"ExitMask\"" Assets/Scripts/World/FloorTransitionEffect.cs && grep -n "_exitLeapDuration\|_exitLeapHeight" Assets/Scripts/World/FloorTransitionEffect.cs</automated>
  </verify>
  <acceptance_criteria>
    - `SetBool("IsDashing"` 호출이 정확히 2회 존재한다 (true 진입, false 종료)
    - `"ExitMask"` 문자열이 파일에서 완전히 사라졌다 (grep 매치 0건 — 옛 SpriteMask 생성 코드 제거 확인)
    - `_exitMaskDuration` 필드가 파일에서 완전히 사라졌다 (고아 필드 제거)
    - `_exitLeapDuration`, `_exitLeapHeight` 필드가 모두 존재한다
    - `ScaleTransform(portalEffect.transform` 호출이 정확히 2회 존재한다 (X1 성장 + X4 페이드 — 배경 유지 확인)
    - `PlayExit(Vector3 spawnWorldPos, GameObject portalEffectPrefab)` 시그니처는 이 태스크 전후로 문자 그대로 동일하다
  </acceptance_criteria>
  <done>새 층 진입 시 IsDashing 포즈로 수직 도약 후 spawnWorldPos에 정확히 착지하며, 포탈 배경 VFX(X1/X4)는 그대로 유지된다. PlayEntry()/PlayExit() 양쪽 모두 재작성이 완료되어 파일 전체가 컴파일 가능한 최종 상태다.</done>
</task>

</tasks>

<verification>
- `Assets/Scripts/World/FloorTransitionEffect.cs`에서 `SpriteMask`/`EntryMask`/`ExitMask` 관련 코드가 완전히 제거됨 (D-03/D-06)
- `PlayEntry`/`PlayExit` 양쪽 모두 `AudioManager.PlaySfx` 호출이 그대로 남아있음 (D-07)
- `WorldGenerator.cs`의 `PlayExit` 호출부는 변경 없음, `PlayEntry` 호출부만 `_portalVortexMaterial` 전달로 갱신됨
- Unity 컴파일 에러 0건 (999.3-03 실행 전 확인 권장 — 이 플랜은 두 태스크 모두 동일 파일을 순차 편집하므로 각 태스크 종료 시점마다 컴파일 가능한 상태를 유지해야 한다)
</verification>

<success_criteria>
- D-01~D-08 전체가 코드 레벨에서 구현 완료됨 — 남은 것은 실제 씬 배선(999.3-03)과 육안 플레이테스트뿐
- FloorTransitionEffect.cs의 두 메서드 모두 플레이어 Transform을 실제로 이동시킴 (999.3-CONTEXT.md 핵심 불만 해소)
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-player-portal-effect-rework/999.3-02-SUMMARY.md`
</output>
