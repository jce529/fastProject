---
phase: 999.3-player-portal-effect-rework
plan: 02
subsystem: gameplay-vfx
tags: [unity, shader-graph-adjacent-hlsl, rigidbody2d, animator, floor-transition, portal-vfx]

# Dependency graph
requires:
  - phase: 999.3-player-portal-effect-rework (plan 01)
    provides: "Assets/Shaders/PortalVortex.shader, Assets/Materials/PortalVortex.mat, Assets/Scripts/Effects/PortalVortexDriver.cs (grab-pass swirl distortion, validated in Play mode)"
provides:
  - "FloorTransitionEffect.PlayEntry(Transform, Material) — vortex shader overlay + real Rigidbody2D player movement toward portal center, replacing SpriteMask boundary fade (E1-E4)"
  - "FloorTransitionEffect.PlayExit(Vector3, GameObject) — vertical leap arc reusing CombatController's IsDashing animator bool, replacing SpriteMask shrink (X2/X3), while keeping portal backdrop VFX (X1 grow / X4 fade)"
  - "WorldGenerator._portalVortexMaterial field wired into the ENTRY call site (Inspector assignment deferred to 999.3-03)"
affects: [999.3-03-scene-wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FloorTransitionEffect now caches Rigidbody2D/Animator in Awake() and drives player Transform via _rb.MovePosition() during transition coroutines, matching the project convention already used by CombatController/RollController"
    - "IsDashing animator bool (0-duration transitions, no exit time) reused outside CombatController.ExecuteDash() as a generic 'dash pose for N seconds' primitive, driven by Time.unscaledDeltaTime instead of the fixed-update pattern ExecuteDash uses (documented as unsafe to copy — FloorTransitionEffect has no ExitSlowMotion() timeScale=1 guarantee)"

key-files:
  created: []
  modified:
    - Assets/Scripts/World/FloorTransitionEffect.cs
    - Assets/Scripts/World/WorldGenerator.cs

key-decisions:
  - "PlayEntry() signature changed to accept Material vortexMaterial parameter; PlayExit() signature left untouched per plan (D-08 constraint) — only WorldGenerator's ENTRY call site needed updating"
  - "Old E3 'portal shrinks' beat has no direct replacement — poral GameObject stays alive during the vortex playback and is destroyed later when WorldGenerator tears down the old chain, which happens off-screen during the same transition sequence"
  - "X1 (portal grow) now runs in parallel with the leap via StartCoroutine instead of being awaited first, since the leap and portal growth are visually independent effects that no longer need to be sequenced by a shared blocking mask timeline"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

# Metrics
duration: 6min
completed: 2026-07-15
---

# Phase 999.3 Plan 02: FloorTransitionEffect Rewrite Summary

**Replaced SpriteMask-based portal entry/exit fades with real Rigidbody2D player movement — vortex shader suction on entry, IsDashing-driven vertical leap on exit.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-15T01:27:32Z
- **Completed:** 2026-07-15T01:33:23Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `PlayEntry(Transform, Material)` now moves the player's actual Transform toward the portal center via `Rigidbody2D.MovePosition`, while an instanced `PortalVortex.mat` copy (from 999.3-01) renders a swirl overlay on a dedicated `PortalVFX` sorting layer — the SpriteMask/`EntryMask` GameObject approach is gone entirely
- `PlayExit(Vector3, GameObject)` now reuses the existing `IsDashing` animator bool (already used by `CombatController.ExecuteDash()`) to play a vertical sine-arc leap that lands exactly at `spawnWorldPos`, replacing the SpriteMask/`ExitMask` shrink — portal backdrop VFX (grow/fade) is kept as background dressing per Open Question 1 default (KEEP)
- `WorldGenerator` gained a `_portalVortexMaterial` field wired into the ENTRY call site; the EXIT call site and `PlayExit` signature are untouched, confirmed via `git diff`

## Task Commits

Each task was committed atomically:

1. **Task 1: PlayEntry() 소용돌이 흡입 재작성 + WorldGenerator 배선** - `968afeb` (feat)
2. **Task 2: PlayExit() 수직 도약 재작성** - `f23d3c0` (feat)

**Doc fix (Rule 1 auto-fix, folded in as its own commit):** `445e9ff` (docs — stale class-level comment)

_No plan-metadata-only "docs: complete plan" commit yet — this SUMMARY/STATE/ROADMAP update will be committed as the final metadata commit._

## Files Created/Modified
- `Assets/Scripts/World/FloorTransitionEffect.cs` - `PlayEntry`/`PlayExit` fully rewritten; `_rb`/`_animator` fields added, cached in `Awake()`; old `_entryMaskDuration`/`_portalShrinkDuration`/`_exitMaskDuration` fields removed; `ScaleTransform()` helper untouched
- `Assets/Scripts/World/WorldGenerator.cs` - Added `_portalVortexMaterial` `[SerializeField]`; ENTRY call site updated to `PlayEntry(portal.transform, _portalVortexMaterial)`; EXIT call site unchanged

## Decisions Made
- Kept `PlayExit`'s public signature identical to avoid touching `WorldGenerator`'s EXIT call site at all (plan constraint D-08/interfaces section)
- X1 (portal grow) changed from an awaited coroutine to a fire-and-forget `StartCoroutine` so it visually overlaps the leap instead of blocking it — matches the plan's explicit example code
- Updated the class-level `<summary>` doc comment (Rule 1 — stale documentation bug) since it still described the removed SpriteMask design after both methods were rewritten

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug/Docs] Stale class-level doc comment describing removed SpriteMask design**
- **Found during:** Post-Task-2 review
- **Issue:** `FloorTransitionEffect`'s class summary still read "포탈 입장/퇴장 SpriteMask 애니메이션 컴포넌트" and referenced "Plan 12-02" after both `PlayEntry`/`PlayExit` were fully rewritten to no longer use SpriteMask at all — misleading for future readers
- **Fix:** Updated the summary to describe the new vortex-suction/IsDashing-leap design and reference this plan (999.3-02)
- **Files modified:** Assets/Scripts/World/FloorTransitionEffect.cs
- **Verification:** `grep -n "SpriteMask" Assets/Scripts/World/FloorTransitionEffect.cs` shows only descriptive comparison comments ("기존 SpriteMask 페이드를 완전히 대체" etc.), no live SpriteMask code or stale component description remains
- **Committed in:** 445e9ff

---

**Total deviations:** 1 auto-fixed (1 doc/bug)
**Impact on plan:** Documentation-only fix directly tied to the two rewritten methods. No scope creep, no behavior change.

## Issues Encountered
- Initial edit accidentally merged Task 2's field-block changes (`_exitLeapDuration`/`_exitLeapHeight`) into the Task 1 commit scope, which would have left `PlayExit()` referencing the now-removed `_exitMaskDuration` field (a compile error) at the Task 1 commit boundary. Caught before committing — reverted the field block to the exact Task 1 end-state (kept `_exitMaskDuration` with the plan's NOTE comment) so Task 1's commit is independently compilable, then applied Task 2's field/method rewrite as a separate commit as originally planned.

## User Setup Required

None - no external service configuration required. `_portalVortexMaterial` Inspector assignment on the SampleScene Player is explicitly deferred to plan 999.3-03 per this plan's scope.

## Next Phase Readiness
- D-01~D-08 fully implemented at the code level; `FloorTransitionEffect.cs` compiles against the same public method signatures WorldGenerator already calls (verified via grep, not a live Unity compile — Unity Editor was not launched during this plan)
- 999.3-03 needs to: wire `_portalVortexMaterial` (WorldGenerator) to the `PortalVortex.mat` asset in SampleScene's Inspector, then run an actual Play-mode checkpoint to verify the swirl entry + vertical leap exit visually, since no Unity compile/Play-mode check was performed in this plan (per plan's `autonomous: true` / `type="auto"` tasks — no checkpoint was defined for this plan)

---
*Phase: 999.3-player-portal-effect-rework*
*Completed: 2026-07-15*

## Self-Check: PASSED

All created/modified files confirmed present on disk; all 3 task/deviation commit hashes (968afeb, f23d3c0, 445e9ff) confirmed in git log.
