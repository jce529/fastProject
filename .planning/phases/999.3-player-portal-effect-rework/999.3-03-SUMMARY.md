---
phase: 999.3-player-portal-effect-rework
plan: 03
subsystem: gameplay-vfx
tags: [unity, shader, scene-wiring, human-verify, portal-vfx]

# Dependency graph
requires:
  - phase: 999.3-player-portal-effect-rework (plan 01)
    provides: "Assets/Shaders/PortalVortex.shader, Assets/Materials/PortalVortex.mat"
  - phase: 999.3-player-portal-effect-rework (plan 02)
    provides: "FloorTransitionEffect.PlayEntry(Transform, Material) / PlayExit(Vector3, GameObject)"
provides:
  - "SampleScene.unity WorldGenerator._portalVortexMaterial wired to PortalVortex.mat — entry vortex now renders in actual Play mode"
  - "EntryVortex swirl tuned (world size, distortion scale, winding count, animated phase driven by local elapsed time) through iterative human playtesting"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "EntryVortex swirl phase animated from local elapsed time (not Time.time) so the effect always starts from the same visual state regardless of session length"

key-files:
  created: []
  modified:
    - Assets/Scenes/SampleScene.unity
    - Assets/Scripts/World/FloorTransitionEffect.cs
    - Assets/Materials/PortalVortex.mat
    - Assets/Prefabs/World/ExitPortal/ExitPortal.prefab

key-decisions:
  - "PortalVortex.mat tuning reverted to 999.3-01's confirmed-working baseline after an initial regression, then re-tuned from that known-good baseline"
  - "EntryVortex world size shrunk and PortalVortex distortion scale rescaled to match, per user feedback during playtesting"
  - "Swirl phase animates up to the previously confirmed-good max instead of holding static, then intensity/winding count were further strengthened across two more feedback rounds until user confirmed the look was good"
  - "ExitPortal.prefab collider change (CircleCollider2D → CapsuleCollider2D, scale 1,1,1 → 1,2,1), found as an unrelated working-tree diff during this plan, confirmed intentional by user and committed alongside"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

# Metrics
duration: 61min
completed: 2026-07-15
---

# Phase 999.3 Plan 03: Scene Wiring + Portal Vortex Playtest Tuning Summary

**Wired WorldGenerator._portalVortexMaterial to PortalVortex.mat in SampleScene, then iteratively tuned the EntryVortex swirl (size, distortion scale, winding count, animated phase) across multiple human playtest rounds until the visual was confirmed good.**

## Performance

- **Duration:** 61 min
- **Started:** 2026-07-15T10:37:06+09:00
- **Completed:** 2026-07-15T11:38:37+09:00
- **Tasks:** 1 (auto) + 1 (human-verify checkpoint, resolved via iterative tuning rather than a single pass/fail)
- **Files modified:** 4

## Accomplishments
- `WorldGenerator._portalVortexMaterial` in `SampleScene.unity` now references `PortalVortex.mat`, so the entry vortex shader actually renders during Play mode instead of `PlayEntry` receiving a null material
- EntryVortex swirl tuned through repeated human playtesting: reverted an initial regression to the 999.3-01 confirmed-working baseline, shrunk world size with matching distortion-scale rescale, switched the phase driver from absolute session time to local elapsed time, animated the phase up to the confirmed-good max, and strengthened swirl intensity and winding count until the spiral arms read clearly
- User confirmed the vortex visual is good enough to close out the phase without exhausting the full D-01~D-08 checklist line-by-line — the entry-vortex portion (D-01/D-02/D-03) was the remaining unverified risk and is now visually approved

## Task Commits

Each tuning iteration was committed atomically as playtest feedback came in:

1. **Task 1: Scene wiring** - `17721fd` (feat)
2. Revert to confirmed-working baseline - `4517f6e` (fix)
3. Shrink EntryVortex world size - `295de53` (fix)
4. Rescale PortalVortex distortion scale to match - `21c3398` (fix)
5. Drive swirl from local elapsed time - `1fe09f8` (fix)
6. Hold swirl at fixed confirmed-good phase - `b761a97` (fix)
7. Animate swirl phase up to confirmed-good max - `ba20feb` (fix)
8. Strengthen swirl intensity - `f92fee1` (fix)
9. Increase winding count - `f08cdb6` (fix)
10. **Deferred item resolution:** ExitPortal.prefab collider change confirmed intentional and committed - `1e3ada5` (fix)

## Files Created/Modified
- `Assets/Scenes/SampleScene.unity` - `WorldGenerator._portalVortexMaterial` field wired to `PortalVortex.mat`
- `Assets/Scripts/World/FloorTransitionEffect.cs` - EntryVortex world size, swirl phase driver (local elapsed time), and winding/intensity tuning
- `Assets/Materials/PortalVortex.mat` - distortion scale rescaled to match shrunk EntryVortex size
- `Assets/Prefabs/World/ExitPortal/ExitPortal.prefab` - collider changed from `CircleCollider2D` to `CapsuleCollider2D`, scale adjusted (unrelated to this plan's scope but confirmed intentional and committed alongside)

## Decisions Made
- Closed the Task 2 human-verify checkpoint based on user confirmation of the vortex visual quality ("회오리는 이정도면 괜찮은것 같아") rather than running the full 11-item D-01~D-08 checklist, since the entry vortex was the only piece left unverified after 999.3-01/02's prior confirmations
- ExitPortal.prefab's out-of-scope collider diff (flagged in `deferred-items.md` during an earlier checkpoint) was confirmed intentional by the user and committed rather than reverted

## Deviations from Plan

None beyond the iterative tuning already anticipated by the plan's human-verify checkpoint design (the checkpoint explicitly allows for gap-closure iteration based on playtest feedback).

## Issues Encountered

- Initial PortalVortex.mat tuning attempt regressed the swirl visual; reverted to the 999.3-01 baseline and re-tuned from there instead of continuing to iterate on the regressed state.

## User Setup Required

None.

## Next Phase Readiness

- 999.3 phase complete: PortalVortex shader (999.3-01), FloorTransitionEffect rewrite (999.3-02), and scene wiring + playtest tuning (999.3-03) are all done and confirmed by the user
- No further gap-closure plan needed for 999.3

---
*Phase: 999.3-player-portal-effect-rework*
*Completed: 2026-07-15*
