---
phase: 999.3-player-portal-effect-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ProjectSettings/TagManager.asset
  - Assets/Settings/Renderer2D.asset
  - Assets/Shaders/PortalVortex.shader
  - Assets/Shaders/PortalVortex.shader.meta
  - Assets/Materials/PortalVortex.mat
  - Assets/Materials/PortalVortex.mat.meta
autonomous: false
requirements: [D-01, D-02, D-08]

must_haves:
  truths:
    - "Renderer2D.asset의 Camera Sorting Layer Texture가 활성화되어 있고, Default보다 렌더 순서가 위인 PortalVFX 소팅 레이어가 존재한다"
    - "PortalVortex.mat을 부착한 SpriteRenderer를 PortalVFX 레이어에 놓고 Play 모드에서 확인하면, 뒤에 렌더링된 Default 레이어 타일/스프라이트가 소용돌이 형태로 왜곡되어 보인다 (사람이 직접 확인)"
  artifacts:
    - path: "Assets/Shaders/PortalVortex.shader"
      provides: "D-01 소용돌이 UV 왜곡 + URP 2D Camera Sorting Layer Texture 그랩 샘플링 셰이더"
      contains: "_CameraSortingLayerTexture"
    - path: "Assets/Materials/PortalVortex.mat"
      provides: "PortalVortex.shader의 인스턴스 소스 머티리얼 — FloorTransitionEffect가 Renderer.material로 복사해 사용"
      contains: "guid: 5f2a8c3d9b1e4a6f8c0d2e4f6a8b0c2d"
    - path: "Assets/Settings/Renderer2D.asset"
      provides: "프로젝트 전역 Camera Sorting Layer Texture 캡처 설정 (Foremost Sorting Layer = Default)"
      contains: "m_UseCameraSortingLayersTexture: 1"
  key_links:
    - from: "Assets/Shaders/PortalVortex.shader"
      to: "URP 2D Renderer Camera Sorting Layer Texture (Renderer2D.asset)"
      via: "TEXTURE2D(_CameraSortingLayerTexture) 선언 + Renderer2D.asset의 m_UseCameraSortingLayersTexture=1/m_CameraSortingLayersTextureBound=0"
      pattern: "_CameraSortingLayerTexture"
---

<objective>
이 백로그 페이즈(999.3)의 Wave 0 위험 감소 스파이크: 프로젝트 최초의 그랩패스 기반 셰이더(`PortalVortex.shader`)와 그 전제 조건인 프로젝트 전역 렌더러 설정(Camera Sorting Layer Texture + 신규 소팅 레이어)을 구축하고, Play 모드에서 실제로 환경(타일)이 왜곡되어 보이는지 사람이 직접 확인한다. RESEARCH.md Open Question 2가 명시적으로 권장하는 "전체 FloorTransitionEffect 재작성 전에 먼저 스파이크로 검증" 절차다.

Purpose: D-01(셰이더 기반 소용돌이)/D-02(환경까지 흡입) 결정의 핵심 기술(_CameraSortingLayerTexture)이 이 프로젝트의 URP 6000.3.11f1 + Android ARM64 설정에서 실제로 작동하는지, 셰이더 컴파일 에러 없이 확인한다. 이 스파이크가 통과해야 999.3-02(FloorTransitionEffect 전체 재작성)를 진행할 근거가 생긴다.
Output: `PortalVortex.shader`/`PortalVortex.mat`(신규 에셋), `Renderer2D.asset`/`TagManager.asset`(프로젝트 전역 설정 변경), 사람의 Play 모드 검증 승인.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.3-player-portal-effect-rework/999.3-CONTEXT.md
@.planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md
@Assets/Shaders/SpriteWhite.shader
@Assets/Materials/Ladder_White.mat
@Assets/Settings/Renderer2D.asset
@ProjectSettings/TagManager.asset

<interfaces>
<!-- 이 프로젝트에 이미 존재하는 hand-written ShaderLab/HLSL 셰이더 -- 최초 셰이더가 아니다.
     PortalVortex.shader는 이 파일과 동일한 구조(HLSLPROGRAM, Universal2D LightMode, SRP Batcher용
     CBUFFER)를 따른다. 그대로 템플릿으로 사용한다. -->

From Assets/Shaders/SpriteWhite.shader (전체, 62줄 — 그대로 구조 참고):
```hlsl
Shader "Custom/SpriteWhite"
{
    Properties
    {
        _MainTex ("Sprite Texture", 2D) = "white" {}
    }
    SubShader
    {
        Tags
        {
            "Queue" = "Transparent"
            "RenderType" = "Transparent"
            "RenderPipeline" = "UniversalPipeline"
        }

        Blend SrcAlpha OneMinusSrcAlpha
        Cull Off
        ZWrite Off

        Pass
        {
            Name "SpriteWhite"
            Tags { "LightMode" = "Universal2D" }

            HLSLPROGRAM
            #pragma vertex vert
            #pragma fragment frag

            #include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl"

            struct Attributes { float4 positionOS : POSITION; float2 uv : TEXCOORD0; };
            struct Varyings { float4 positionHCS : SV_POSITION; float2 uv : TEXCOORD0; };

            TEXTURE2D(_MainTex);
            SAMPLER(sampler_MainTex);

            Varyings vert(Attributes IN)
            {
                Varyings OUT;
                OUT.positionHCS = TransformObjectToHClip(IN.positionOS.xyz);
                OUT.uv = IN.uv;
                return OUT;
            }

            half4 frag(Varyings IN) : SV_Target
            {
                half alpha = SAMPLE_TEXTURE2D(_MainTex, sampler_MainTex, IN.uv).a;
                return half4(1.0, 1.0, 1.0, alpha);
            }
            ENDHLSL
        }
    }
}
```

From Assets/Shaders/SpriteWhite.shader.meta (guid 형식 그대로 재사용 — ShaderImporter):
```
fileFormatVersion: 2
guid: 8cc5f95cf92756f43a996ffcb83f4413
ShaderImporter:
  externalObjects: {}
  defaultTextures: []
  nonModifiableTextures: []
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```

From Assets/Materials/Ladder_White.mat (m_Shader guid로 셰이더 참조 — 그대로 구조 참고):
```yaml
%YAML 1.1
%TAG !u! tag:unity3d.com,2011:
--- !u!21 &2100000
Material:
  serializedVersion: 8
  m_ObjectHideFlags: 0
  m_CorrespondingSourceObject: {fileID: 0}
  m_PrefabInstance: {fileID: 0}
  m_PrefabAsset: {fileID: 0}
  m_Name: Ladder_White
  m_Shader: {fileID: 4800000, guid: 8cc5f95cf92756f43a996ffcb83f4413, type: 3}
  m_Parent: {fileID: 0}
  m_ModifiedSerializedProperties: 0
  m_ValidKeywords: []
  m_InvalidKeywords: []
  m_LightmapFlags: 4
  m_EnableInstancingVariants: 0
  m_DoubleSidedGI: 0
  m_CustomRenderQueue: 3000
  stringTagMap:
    RenderType: Transparent
  disabledShaderPasses: []
  m_LockedProperties:
  m_SavedProperties:
    serializedVersion: 3
    m_TexEnvs:
    - _MainTex:
        m_Texture: {fileID: 0}
        m_Scale: {x: 1, y: 1}
        m_Offset: {x: 0, y: 0}
    m_Ints: []
    m_Floats: []
    m_Colors: []
  m_BuildTextureStacks: []
  m_AllowLocking: 1
```

Current Assets/Settings/Renderer2D.asset (lines 46-49 — 이 태스크의 수정 대상):
```yaml
  m_UseDepthStencilBuffer: 1
  m_UseCameraSortingLayersTexture: 0
  m_CameraSortingLayersTextureBound: -1
  m_CameraSortingLayerDownsamplingMethod: 0
```

Current ProjectSettings/TagManager.asset (lines 43-46 — 이 태스크의 수정 대상, 프로젝트 전체에 소팅 레이어가 "Default" 1개뿐임을 확인):
```yaml
  m_SortingLayers:
  - name: Default
    uniqueID: 0
    locked: 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PortalVFX 소팅 레이어 신설 + Renderer2D Camera Sorting Layer Texture 활성화</name>
  <files>ProjectSettings/TagManager.asset, Assets/Settings/Renderer2D.asset</files>
  <read_first>
    - ProjectSettings/TagManager.asset (m_SortingLayers 블록, 43-46줄 — 현재 "Default" 1개만 존재)
    - Assets/Settings/Renderer2D.asset (46-49줄 — m_UseCameraSortingLayersTexture/m_CameraSortingLayersTextureBound/m_CameraSortingLayerDownsamplingMethod)
    - .planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md (Pitfall 2 — 이 설정은 프로젝트 전역이며 매 프레임 캡처 비용이 발생함을 인지)
  </read_first>
  <action>
    이 프로젝트에는 소팅 레이어가 "Default" 1개뿐이다(모든 Room의 Tilemap도 m_SortingLayer: 0). URP 2D의 Camera Sorting Layer Texture 기능은 "Foremost Sorting Layer"까지의 내용을 캡처해 그 위(=리스트상 뒤쪽) 레이어에서 샘플링하는 방식이므로, 소용돌이 오버레이를 그릴 새 레이어가 반드시 필요하다.

    **1. `ProjectSettings/TagManager.asset` — `m_SortingLayers` 블록에 새 레이어 1개를 Default 뒤에 추가 (uniqueID는 기존에 없는 값 1 사용):**

    변경 전(43-46줄):
    ```yaml
      m_SortingLayers:
      - name: Default
        uniqueID: 0
        locked: 0
    ```

    변경 후:
    ```yaml
      m_SortingLayers:
      - name: Default
        uniqueID: 0
        locked: 0
      - name: PortalVFX
        uniqueID: 1
        locked: 0
    ```
    (리스트 순서 = 렌더 순서이므로 PortalVFX가 Default보다 항상 위에 그려진다. `m_RenderingLayers` 블록은 건드리지 않는다 — Rendering Layers는 라이팅용 별개 개념.)

    **2. `Assets/Settings/Renderer2D.asset` — Camera Sorting Layer Texture 활성화, Foremost Sorting Layer = Default(uniqueID 0), 다운샘플링 2x Bilinear:**

    변경 전(46-49줄):
    ```yaml
      m_UseDepthStencilBuffer: 1
      m_UseCameraSortingLayersTexture: 0
      m_CameraSortingLayersTextureBound: -1
      m_CameraSortingLayerDownsamplingMethod: 0
    ```

    변경 후:
    ```yaml
      m_UseDepthStencilBuffer: 1
      m_UseCameraSortingLayersTexture: 1
      m_CameraSortingLayersTextureBound: 0
      m_CameraSortingLayerDownsamplingMethod: 1
    ```
    (`m_CameraSortingLayersTextureBound: 0` = Default 레이어의 uniqueID. `m_CameraSortingLayerDownsamplingMethod: 1`은 "2x Bilinear" 추정값 — RESEARCH.md 권장 다운샘플링. **정확한 enum 매핑은 Unity Editor에서 검증되지 않았으므로, Task 3 체크포인트에서 Project Settings > Graphics > Universal Render Pipeline Asset(2D Renderer) Inspector를 열어 "Foremost Sorting Layer"가 실제로 "Default"로, 다운샘플링 드롭다운이 "2x Bilinear"로 표시되는지 확인하고, 다르면 Inspector에서 직접 고쳐서 이 파일에 반영한다.**)
  </action>
  <verify>
    <automated>grep -n "name: PortalVFX" ProjectSettings/TagManager.asset && grep -n "m_UseCameraSortingLayersTexture: 1" Assets/Settings/Renderer2D.asset</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectSettings/TagManager.asset`에 `- name: PortalVFX` / `uniqueID: 1` 항목이 `Default` 항목 바로 뒤에 존재한다
    - `Assets/Settings/Renderer2D.asset`의 `m_UseCameraSortingLayersTexture`가 `1`이고 `m_CameraSortingLayersTextureBound`가 `0`이다
    - 기존 `m_SortingLayers`의 `Default` 항목(uniqueID: 0)은 한 글자도 변경되지 않았다
  </acceptance_criteria>
  <done>PortalVFX 소팅 레이어가 존재하고, URP 2D Renderer가 Default 레이어까지의 카메라 출력을 매 프레임 캡처해 `_CameraSortingLayerTexture`로 노출한다.</done>
</task>

<task type="auto">
  <name>Task 2: PortalVortex.shader + PortalVortex.mat 신규 작성 (D-01/D-02)</name>
  <files>Assets/Shaders/PortalVortex.shader, Assets/Shaders/PortalVortex.shader.meta, Assets/Materials/PortalVortex.mat, Assets/Materials/PortalVortex.mat.meta</files>
  <read_first>
    - Assets/Shaders/SpriteWhite.shader (구조 템플릿 — context의 &lt;interfaces&gt; 참고)
    - Assets/Materials/Ladder_White.mat (머티리얼 YAML 구조 템플릿)
    - .planning/phases/999.3-player-portal-effect-rework/999.3-RESEARCH.md (Pattern 1 소용돌이 HLSL 예시, Pattern 2 SRP Batcher CBUFFER 규칙, Pitfall 1 — Shader Graph Time 노드/`_Time` 대신 반드시 수동 `_UnscaledTime` 사용)
  </read_first>
  <action>
    **1. `Assets/Shaders/PortalVortex.shader` 신규 생성 — 아래 코드를 그대로 사용:**
    ```hlsl
    Shader "Custom/PortalVortex"
    {
        Properties
        {
            _SwirlStrength ("Swirl Strength", Float) = 6.0
            _Radius ("Effect Radius (local UV)", Float) = 0.5
            _PullStrength ("Pull Strength", Float) = 0.15
            _UnscaledTime ("Unscaled Time", Float) = 0.0
            _EffectAlpha ("Effect Alpha", Range(0,1)) = 1.0
        }
        SubShader
        {
            Tags
            {
                "Queue" = "Transparent"
                "RenderType" = "Transparent"
                "RenderPipeline" = "UniversalPipeline"
            }

            Blend SrcAlpha OneMinusSrcAlpha
            Cull Off
            ZWrite Off

            Pass
            {
                Name "PortalVortex"
                Tags { "LightMode" = "Universal2D" }

                HLSLPROGRAM
                #pragma vertex vert
                #pragma fragment frag

                #include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl"

                struct Attributes
                {
                    float4 positionOS : POSITION;
                    float2 uv        : TEXCOORD0;
                };

                struct Varyings
                {
                    float4 positionHCS : SV_POSITION;
                    float2 uv          : TEXCOORD0;
                    float4 screenPos   : TEXCOORD1;
                };

                TEXTURE2D(_CameraSortingLayerTexture);
                SAMPLER(sampler_CameraSortingLayerTexture);

                CBUFFER_START(UnityPerMaterial)
                    float _SwirlStrength;
                    float _Radius;
                    float _PullStrength;
                    float _UnscaledTime;
                    float _EffectAlpha;
                CBUFFER_END

                Varyings vert(Attributes IN)
                {
                    Varyings OUT;
                    OUT.positionHCS = TransformObjectToHClip(IN.positionOS.xyz);
                    OUT.uv = IN.uv;
                    OUT.screenPos = ComputeScreenPos(OUT.positionHCS);
                    return OUT;
                }

                half4 frag(Varyings IN) : SV_Target
                {
                    float2 screenUV = IN.screenPos.xy / IN.screenPos.w;
                    float2 toCenter = IN.uv - 0.5;
                    float dist = length(toCenter);
                    float falloff = saturate(1.0 - dist / _Radius);

                    float angle = _SwirlStrength * falloff * falloff * _UnscaledTime;
                    float s = sin(angle);
                    float c = cos(angle);
                    float2 swirled = float2(
                        toCenter.x * c - toCenter.y * s,
                        toCenter.x * s + toCenter.y * c);

                    float2 pullOffset = (swirled - toCenter) - toCenter * falloff * _PullStrength;
                    float2 distortedUV = screenUV + pullOffset;

                    half4 col = SAMPLE_TEXTURE2D(_CameraSortingLayerTexture, sampler_CameraSortingLayerTexture, distortedUV);
                    half edgeMask = smoothstep(_Radius, _Radius * 0.4, dist);
                    col.a *= edgeMask * _EffectAlpha;
                    return col;
                }
                ENDHLSL
            }
        }
    }
    ```
    (`_UnscaledTime`은 Plan 999.3-02에서 `Time.unscaledTime`을 매 프레임 C#에서 주입한다 — Pitfall 1: 이 셰이더 자체에는 시간을 스스로 진행시키는 로직이 없다. `_CameraSortingLayerTexture`/`sampler_CameraSortingLayerTexture`는 URP 2D Renderer가 Task 1 설정에 따라 전역으로 바인딩하므로 Properties 블록에 선언하지 않는다.)

    **2. `Assets/Shaders/PortalVortex.shader.meta` 신규 생성:**
    ```
    fileFormatVersion: 2
    guid: 5f2a8c3d9b1e4a6f8c0d2e4f6a8b0c2d
    ShaderImporter:
      externalObjects: {}
      defaultTextures: []
      nonModifiableTextures: []
      userData: 
      assetBundleName: 
      assetBundleVariant: 
    ```

    **3. `Assets/Materials/PortalVortex.mat` 신규 생성:**
    ```yaml
    %YAML 1.1
    %TAG !u! tag:unity3d.com,2011:
    --- !u!21 &2100000
    Material:
      serializedVersion: 8
      m_ObjectHideFlags: 0
      m_CorrespondingSourceObject: {fileID: 0}
      m_PrefabInstance: {fileID: 0}
      m_PrefabAsset: {fileID: 0}
      m_Name: PortalVortex
      m_Shader: {fileID: 4800000, guid: 5f2a8c3d9b1e4a6f8c0d2e4f6a8b0c2d, type: 3}
      m_Parent: {fileID: 0}
      m_ModifiedSerializedProperties: 0
      m_ValidKeywords: []
      m_InvalidKeywords: []
      m_LightmapFlags: 4
      m_EnableInstancingVariants: 0
      m_DoubleSidedGI: 0
      m_CustomRenderQueue: 3000
      stringTagMap:
        RenderType: Transparent
      disabledShaderPasses: []
      m_LockedProperties:
      m_SavedProperties:
        serializedVersion: 3
        m_TexEnvs: []
        m_Ints: []
        m_Floats:
        - _SwirlStrength: 6
        - _Radius: 0.5
        - _PullStrength: 0.15
        - _UnscaledTime: 0
        - _EffectAlpha: 1
        m_Colors: []
      m_BuildTextureStacks: []
      m_AllowLocking: 1
    ```

    **4. `Assets/Materials/PortalVortex.mat.meta` 신규 생성:**
    ```
    fileFormatVersion: 2
    guid: c7d4e1f28a3b4c5d6e7f8091a2b3c4d5
    NativeFormatImporter:
      externalObjects: {}
      mainObjectFileID: 0
      userData: 
      assetBundleName: 
      assetBundleVariant: 
    ```
  </action>
  <verify>
    <automated>grep -c "_CameraSortingLayerTexture" Assets/Shaders/PortalVortex.shader && grep -n "guid: 5f2a8c3d9b1e4a6f8c0d2e4f6a8b0c2d" Assets/Materials/PortalVortex.mat</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Shaders/PortalVortex.shader`가 존재하고 `_CameraSortingLayerTexture`, `CBUFFER_START(UnityPerMaterial)`, `"LightMode" = "Universal2D"`를 포함한다
    - `Assets/Shaders/PortalVortex.shader.meta`가 존재하고 guid가 `5f2a8c3d9b1e4a6f8c0d2e4f6a8b0c2d`이다
    - `Assets/Materials/PortalVortex.mat`이 존재하고 `m_Shader`가 위 shader guid를 참조하며, `_SwirlStrength`/`_Radius`/`_PullStrength`/`_UnscaledTime`/`_EffectAlpha` 5개 float 프로퍼티를 모두 포함한다
    - `Assets/Materials/PortalVortex.mat.meta`가 존재하고 guid가 `c7d4e1f28a3b4c5d6e7f8091a2b3c4d5`이다
  </acceptance_criteria>
  <done>PortalVortex.shader/.mat이 프로젝트에 존재하며, Unity가 임포트해도(다음 Editor 세션에서) 컴파일 에러 없이 로드된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Unity Editor Play 모드 검증 — 소용돌이 그랩패스 스파이크</name>
  <files>없음 (검증 전용 — 파일 변경 없음, 테스트용 GameObject는 저장하지 않고 삭제)</files>
  <read_first>
    - Assets/Shaders/PortalVortex.shader (Task 2 산출물)
    - Assets/Settings/Renderer2D.asset (Task 1 산출물 — Foremost Sorting Layer 설정)
  </read_first>
  <action>
    Unity 에디터 내부 Play 모드 조작(임시 GameObject 생성, 머티리얼 드래그, 육안 확인)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 왜곡이 보이지 않거나 컴파일 에러가 있으면 Task 1의 enum 추정값(m_CameraSortingLayerDownsamplingMethod)이나 Task 2의 셰이더 코드를 재검토해 수정한다.
  </action>
  <what-built>
    Task 1에서 프로젝트 전역 렌더러 설정(PortalVFX 소팅 레이어 신설 + Renderer2D의 Camera Sorting Layer Texture 활성화)을, Task 2에서 그 설정을 소비하는 첫 커스텀 그랩패스 셰이더(PortalVortex.shader/.mat)를 만들었다. 아직 FloorTransitionEffect.cs는 건드리지 않았다 — 이 체크포인트는 셰이더 기술 자체가 이 프로젝트의 URP 6000.3.11f1 설정에서 실제로 작동하는지만 확인하는 스파이크다(RESEARCH.md Open Question 2).
  </what-built>
  <how-to-verify>
    1. Unity Editor에서 프로젝트를 열어 TagManager/Renderer2D 변경사항이 임포트되며 Console에 에러가 없는지 확인한다.
    2. Project Settings > Graphics > (URP Renderer2D 에셋) Inspector를 열어 "Foremost Sorting Layer"가 "Default"로 표시되는지 확인한다. 다르면 드롭다운에서 "Default"로 직접 선택하고, 다운샘플링 드롭다운이 "2x Bilinear"인지 확인해 필요시 맞춘다 (Task 1의 enum 추정값 보정).
    3. `Assets/Scenes/SampleScene.unity`를 Play 모드로 실행한다 (씬의 `_exitSpawnChance: 1`이라 EXIT 포탈이 바로 스폰된다).
    4. Play 모드를 일시정지(Pause)한 상태에서 Hierarchy에 빈 GameObject를 하나 생성하고(`GameObject > Create Empty`), `SpriteRenderer` 컴포넌트를 추가한다.
    5. 그 SpriteRenderer의 Sprite 필드에 아무 기존 스프라이트(예: `Assets/Prefabs/Portal/Portal_100x100px1.png`)를 드래그하고, Material 필드에 `Assets/Materials/PortalVortex.mat`을 드래그한다.
    6. Sorting Layer 드롭다운에서 "PortalVFX"가 목록에 보이는지 확인하고 선택한다.
    7. 이 GameObject를 플레이어 근처 타일이 보이는 위치로 옮기고 Scale을 (5,5,1) 정도로 키운다.
    8. Play 모드를 재생(Resume)해 Game 뷰에서 확인: 뒤쪽 타일/스프라이트가 소용돌이 형태로 왜곡되어 보이는가? (셰이더의 `_UnscaledTime`이 기본값 0으로 고정돼 있어 회전은 정적이어도 무방 — 지금은 "왜곡이 실제로 걸리는지"만 확인한다.)
    9. Console에 셰이더 컴파일 에러/경고("Shader error", "SRP Batcher" 관련 경고 등)가 없는지 확인한다.
    10. 확인 후 테스트용 GameObject는 삭제한다(저장 불필요 — 이 스파이크는 씬을 변경하지 않는다).
  </how-to-verify>
  <resume-signal>"approved" 또는 문제 설명 (예: "왜곡이 안 보임 — Foremost Sorting Layer 재확인 필요", "PortalVFX 레이어가 드롭다운에 없음", "셰이더 컴파일 에러: ...")</resume-signal>
</task>

</tasks>

<verification>
- `ProjectSettings/TagManager.asset`에 PortalVFX 소팅 레이어 존재, `Assets/Settings/Renderer2D.asset`의 Camera Sorting Layer Texture 활성화
- `Assets/Shaders/PortalVortex.shader`/`Assets/Materials/PortalVortex.mat` 신규 존재, guid 상호 참조 일치
- 사람이 Play 모드에서 실제 환경 왜곡을 육안으로 확인 (D-01/D-02 기술 검증 완료)
</verification>

<success_criteria>
- 999.3-02(FloorTransitionEffect 전체 재작성)가 검증된 셰이더/렌더러 설정 위에서 진행될 수 있는 상태가 됨
- 이 스파이크 단계에서 SampleScene.unity/FloorTransitionEffect.cs는 전혀 수정되지 않음 — 순수 인프라 준비 단계
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-player-portal-effect-rework/999.3-01-SUMMARY.md`
</output>
