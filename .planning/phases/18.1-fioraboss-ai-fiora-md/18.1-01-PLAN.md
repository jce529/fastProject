---
phase: 18.1-fioraboss-ai-fiora-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/Boss/FioraBoss.cs
  - Assets/Prefabs/Enemies/BossEnemy.prefab
autonomous: false
requirements: [BOSS-03, BOSS-04, BOSS-05, BOSS-06]

must_haves:
  truths:
    - "보스는 돌진 방향을 플레이어 위치가 아니라 룸 경계(RoomConnector Left/Right) 기준으로 결정한다 — 더 이상 플레이어를 쫓아가지 않는다"
    - "보스는 한 사이클 동안 _hitCount 티어별로 랜덤하게 정해진 N회(0~1: 3~5회 / 2~4: 4~6회 / 5~6: 5~7회) 돌진 패스를 반복한 뒤 빈틈(Vulnerable) 상태에 진입한다"
    - "돌진 속도는 동일한 티어 기준으로 16 / 20 / 24 units/sec로 가속된다"
    - "돌진 중 히트박스는 각 패스 전체 구간 동안 활성화되어 플레이어가 패스 도중 어디서든 닿으면 즉사한다 (기존 windup 후 0.2초 펄스 방식 폐기)"
    - "예고(느낌표 아이콘)는 사이클당 첫 돌진 패스 이전에만 표시되고, 이후 연속 돌진 사이에는 표시되지 않는다"
    - "벽(룸 경계) 도달 시 0.1~0.2초 짧은 안반품 후 방향이 전환된다 — 즉시 튕기지 않는다"
    - "빈틈 상태에서만 대시 공격 대상이 되고(BOSS-03 회귀 없음), 정확히 7회 피격 시에만 처치되며(BOSS-04), 피격 진행률 UI는 어디에도 없고(BOSS-05), 처치 시 점수 보너스가 지급된다(BOSS-06) — 모두 기존과 동일하게 유지"
  artifacts:
    - path: "Assets/Scripts/Enemy/Boss/FioraBoss.cs"
      provides: "룸 경계 기반 연속 돌진(Dash) 패턴 루프로 전면 재작성된 PatternLoop() + CacheRoomBounds()/GetDashTier() 헬퍼"
      contains: "private void CacheRoomBounds"
    - path: "Assets/Prefabs/Enemies/BossEnemy.prefab"
      provides: "Rigidbody2D.Interpolate 모드 수정 (m_Interpolate: 0 에서 1로) — 새 돌진 속도(16~24 u/s)에서 시각적 stepping 방지"
      contains: "m_Interpolate: 1"
  key_links:
    - from: "Assets/Scripts/Enemy/Boss/FioraBoss.cs (CacheRoomBounds)"
      to: "Assets/Scripts/World/RoomConnector.cs"
      via: "transform.parent.GetComponentsInChildren<RoomConnector>(true)로 Left/Right 마커 X좌표 조회 (WorldGenerator.FindConnector()와 동일 컨벤션)"
      pattern: "GetComponentsInChildren<RoomConnector>"
    - from: "Assets/Scripts/Enemy/Boss/FioraBoss.cs (PatternLoop 돌진 루프)"
      to: "Assets/Scripts/Enemy/Boss/BossEnemyBase.cs (IsAlive)"
      via: "돌진 시퀀스 전체 동안 IsAlive = false 유지 — CombatController의 기존 !IsAlive 스킵 체크 재사용"
      pattern: "IsAlive = false"
    - from: "Assets/Scripts/Enemy/Boss/FioraBoss.cs (OnDashHit, 변경 없음)"
      to: "Assets/Scripts/Player/Combat/OverclockModule.cs"
      via: "target.OnDashHit() 호출부 변경 없음 — _isDefeated만 가드, IsAlive는 절대 참조하지 않는 레이스 컨디션 방지(Pitfall 2) 유지"
      pattern: "if (_isDefeated) return;"
---

<objective>
`FioraBoss.PatternLoop()`(Phase 15가 만든 범용 Telegraph→Attack→Vulnerable 단일 루프)를 `Assets/Prefabs/Enemies/FIORA.md`의 보스 패턴 스펙 — "맵 끝에서 끝으로 연속 돌진 공격을 이어가다가, 랜덤 타이밍에 숨을 고르는 딜레이가 발생, 이 순간이 유일한 타격 기회" — 에 맞춰 룸 경계 기반 연속 돌진(Dash) 패턴으로 전면 재작성한다. BOSS-03/04/05/06(빈틈 타겟팅/7회 피격/진행률 비노출/점수 보너스)의 판정 로직과 `BossEnemyBase`의 defeat-guard/사망시퀀스/스폰게이팅/하이라이트 plumbing은 그대로 유지되며 전혀 변경하지 않는다 — 이번 Phase는 `PatternLoop()` 내부의 이동/히트박스 시퀀스만 교체한다.

또한 18.1-RESEARCH.md Open Question 1(Pitfall 4)에서 발견된 `BossEnemy.prefab`의 `Rigidbody2D.Interpolate` 미설정(project-wide constraint 위반)을 이번 Phase에 번들로 수정한다 — 새 돌진 속도(16~24 units/sec)가 이 기존 갭을 처음으로 시각적으로 눈에 띄게 만들기 때문이다(연구 권고안 채택, 명시적 결정).

Purpose: FIORA.md가 설명하는 "예측(prediction)" 전투 철학 — 플레이어가 보스의 돌진 경로를 읽고 회피해야 하는 압박감 — 을 실제 보스 AI에 구현해, Phase 15의 제자리 근접 공격 컨셉과 스토리 문서(FIORA.md) 사이의 불일치를 해소한다.
Output: 재작성된 `FioraBoss.cs` (돌진 패턴 루프 + 룸 경계 조회 헬퍼), 수정된 `BossEnemy.prefab` (Rigidbody2D Interpolate).
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-fioraboss-ai-fiora-md/18.1-CONTEXT.md
@.planning/phases/18.1-fioraboss-ai-fiora-md/18.1-RESEARCH.md
@.planning/phases/18.1-fioraboss-ai-fiora-md/18.1-VALIDATION.md

<interfaces>
BossEnemyBase 계약 (변경 없음, FioraBoss가 상속) — Assets/Scripts/Enemy/Boss/BossEnemyBase.cs:
```csharp
public abstract class BossEnemyBase : MonoBehaviour, IEnemy, ISpawnGatable
{
    protected Rigidbody2D _rb;
    protected Animator _animator;
    protected SpriteRenderer _sr;
    protected Transform _playerTransform;   // HitReactionAndReset()의 넉백 방향 계산에 여전히 필요 - 삭제 금지 (Pitfall 5)
    protected Color _baseColor;
    protected bool _isDefeated;             // OnDashHit()가 유일하게 가드하는 플래그 - IsAlive는 절대 참조 안 함 (Pitfall 2)
    public bool IsAlive { get; protected set; } = true; // "빈틈 여부"로 오버로드, 생존 여부 아님
    protected abstract IEnumerator PatternLoop();
    protected abstract Color GetHighlightColor();
    // Die(), OnPlayerDied(), SetSpawnGate() 등은 변경 없이 그대로 상속 - 이번 Phase가 건드리지 않음
}
```

RoomConnector 계약 — Assets/Scripts/World/RoomConnector.cs:
```csharp
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    public Direction direction;
}
```

Room_BossFsmTest.prefab 실측 경계 (Assets/Editor/RoomBossFsmTestBuilder.cs:69-75):
Door/ENT(Left) x=-14, Door/EXIT(Right) x=14 — 28유닛 span. 보스 인스턴스는 룸 root의 직계 자식으로 배치됨(RoomBossFsmTestBuilder.cs:88, DebugSceneBuilder.cs:72도 동일) — `transform.parent.GetComponentsInChildren<RoomConnector>(true)`로 정확히 이 룸의 ENT/EXIT만 조회된다(씬 전체 검색 금지 — 인접 룸까지 걸릴 수 있음).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FioraBoss.PatternLoop() 룸 경계 기반 연속 돌진 패턴으로 재작성 (D-01~D-08)</name>
  <files>Assets/Scripts/Enemy/Boss/FioraBoss.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/Boss/FioraBoss.cs (현재 상태 전체 — 교체 대상 라인 확인)
    - Assets/Scripts/Enemy/Boss/BossEnemyBase.cs (IsAlive/_isDefeated/_rb/_sr/_animator/_playerTransform가 protected 상속 멤버임을 확인 — 새 코드에서 재선언 금지)
    - Assets/Scripts/World/RoomConnector.cs (Direction.Left/Right 값 확인)
    - Assets/Editor/RoomBossFsmTestBuilder.cs (Room_BossFsmTest 실측 경계 x:-14~14, 보스가 룸 root 직계 자식으로 배치되는 근거 확인)
    - Assets/Prefabs/Enemies/FIORA.md (보스 패턴 원문 한 줄 스펙)
  </read_first>
  <action>
    1) 클래스 상단 XML 문서 주석 갱신 — 첫 문장 "BOSS-03/04/05/06 (Phase 15): 예고(Telegraph)→공격(Attack)→빈틈(Vulnerable) 단일 패턴 루프를 반복하는"(FioraBoss.cs:5, 화살표 `→` 문자 그대로 매칭할 것 — "에서"/"으로" 조사가 아님)을 다음으로 교체 (이후 문장들은 그대로 유지):
    "BOSS-03/04/05/06 (Phase 15) + Phase 18.1(FIORA.md 재설계): 룸 양끝을 왕복하는 연속 돌진(Dash)을 N회 반복한 뒤 빈틈(Vulnerable) 상태에 진입하는 패턴 루프를 반복하는"

    2) 필드 블록 교체 — 현재 "Telegraph and Attack (D-01, D-04)" 섹션(moveSpeed, telegraphSpeedMultiplier, telegraphDuration, attackWindupDelay, hitboxActiveDuration 5개 필드 + _exclamationIcon/_meleeHitbox)을 다음으로 완전히 교체한다 (_exclamationIcon/_meleeHitbox는 그대로 유지, 나머지 5개는 새 필드로 대체):
    ```csharp
    // -- Dash pattern (D-01~D-08, Phase 18.1 FIORA.md 재설계) --------------------------
    [SerializeField] private float baseDashSpeed = 16f;               // D-05: _hitCount 0~1 구간 기준 속도 (units/sec)
    private const float DashSpeedTier2Multiplier = 1.25f;              // D-03: _hitCount 2~4 구간 -> 20 u/s
    private const float DashSpeedTier3Multiplier = 1.5f;               // D-03: _hitCount 5~6 구간 -> 24 u/s
    [SerializeField] private float firstDashTelegraphDuration = 0.25f; // D-07: 0.2~0.3초, 사이클당 첫 돌진에만
    private const float WallDwellMin = 0.1f;                           // D-06
    private const float WallDwellMax = 0.2f;                           // D-06
    [SerializeField] private SpriteRenderer _exclamationIcon;          // Child SpriteRenderer - 15-03 프리팹 빌더가 할당 (그대로 유지)
    [SerializeField] private Collider2D     _meleeHitbox;              // Child Trigger Collider2D - 15-03 프리팹 빌더가 할당 (그대로 유지)

    // -- Room bounds cache (D-04) ------------------------------------------------------
    private float _leftBoundX;
    private float _rightBoundX;
    private bool  _boundsResolved;
    ```

    3) BossState enum + 상태 필드 기본값 교체 — 두 줄을 함께 교체한다 (Telegraph+Attack을 Dash 하나로 합침 — GetHighlightColor()는 Vulnerable 여부만 분기하므로 영향 없음). enum 선언 줄만 바꾸고 바로 다음 줄을 그대로 두면, 존재하지 않는 `BossState.Telegraph`를 참조하게 되어 컴파일 에러(CS0117)가 발생하므로 반드시 두 줄을 함께 교체할 것:

    변경 전 (FioraBoss.cs:50-51):
    ```csharp
    private enum BossState { Telegraph, Attack, Vulnerable, HitReaction, Dead }
    private BossState _state = BossState.Telegraph;
    ```

    변경 후:
    ```csharp
    private enum BossState { Dash, Vulnerable, HitReaction, Dead }
    private BossState _state = BossState.Dash;
    ```

    4) PatternLoop() 바로 위에 두 private 헬퍼 메서드 신규 추가:
    ```csharp
    private void CacheRoomBounds()
    {
        if (_boundsResolved) return;
        var searchRoot = transform.parent != null ? transform.parent : transform;
        foreach (RoomConnector rc in searchRoot.GetComponentsInChildren<RoomConnector>(true))
        {
            if (rc.direction == RoomConnector.Direction.Left)  _leftBoundX  = rc.transform.position.x;
            if (rc.direction == RoomConnector.Direction.Right) _rightBoundX = rc.transform.position.x;
        }
        _boundsResolved = true;
    }

    private void GetDashTier(int hitCount, out int minDashes, out int maxDashes, out float speed)
    {
        if (hitCount <= 1)      { minDashes = 3; maxDashes = 5; speed = baseDashSpeed; }                           // D-02 초반
        else if (hitCount <= 4) { minDashes = 4; maxDashes = 6; speed = baseDashSpeed * DashSpeedTier2Multiplier; } // D-02 중반
        else                    { minDashes = 5; maxDashes = 7; speed = baseDashSpeed * DashSpeedTier3Multiplier; } // D-02 막판 (5~6)
    }
    ```

    5) PatternLoop() 전체 몸체를 다음으로 교체 (시그니처 `protected override IEnumerator PatternLoop()`는 그대로, while(true) 안쪽 전체를 교체 — Vulnerable 블록은 기존 로직을 그대로 옮긴 것이며 값/타이밍 변경 없음):
    ```csharp
    protected override IEnumerator PatternLoop()
    {
        CacheRoomBounds();

        while (true)
        {
            // ---- Dash: 룸 경계 왕복 연속 돌진, N회 (D-01/D-02), 속도 티어 (D-03/D-05) ----
            GetDashTier(_hitCount, out int minDashes, out int maxDashes, out float dashSpeed);
            int dashCount = Random.Range(minDashes, maxDashes + 1); // int 오버로드는 max 미포함 - +1로 양 끝 포함

            _state = BossState.Dash;
            IsAlive = false; // 타겟 불가 - 돌진 시퀀스 전체 동안 유지 (BOSS-03)

            float midX = (_leftBoundX + _rightBoundX) * 0.5f;
            float dirX = transform.position.x <= midX ? 1f : -1f; // 룸 기하학 기준 - 플레이어 위치를 절대 참조하지 않는다 (D-04)

            for (int pass = 0; pass < dashCount; pass++)
            {
                // D-07: 이 사이클의 첫 돌진 패스에만 짧은 예고
                if (pass == 0)
                {
                    if (_exclamationIcon != null) _exclamationIcon.enabled = true;
                    float teleElapsed = 0f;
                    while (teleElapsed < firstDashTelegraphDuration)
                    {
                        teleElapsed += Time.unscaledDeltaTime;
                        if (_isDefeated) yield break;
                        yield return null;
                    }
                    if (_exclamationIcon != null) _exclamationIcon.enabled = false;
                    if (_isDefeated) yield break;
                }

                // ---- 돌진 패스: 전체 구간 히트박스 지속 활성화 (D-08) ----
                FlipSprite(dirX);
                _animator?.SetBool("isMoving", true);
                if (_meleeHitbox != null) _meleeHitbox.enabled = true;

                float targetX = dirX > 0f ? _rightBoundX : _leftBoundX;
                while ((dirX > 0f && transform.position.x < targetX) ||
                       (dirX < 0f && transform.position.x > targetX))
                {
                    _rb.linearVelocity = new Vector2(dirX * dashSpeed, _rb.linearVelocity.y);
                    if (_isDefeated) yield break;
                    yield return null;
                }

                // 벽 도착 - 오버슈트 스냅(고속 티어에서 물리 스텝 오버슈트 방지) + 정지 + 히트박스 비활성화
                _rb.position = new Vector2(targetX, _rb.position.y);
                _rb.linearVelocity = new Vector2(0f, _rb.linearVelocity.y);
                if (_meleeHitbox != null) _meleeHitbox.enabled = false;
                _animator?.SetBool("isMoving", false);
                if (_isDefeated) yield break;

                // D-06: 0.1~0.2초 안반품 후 방향 전환
                float dwell = Random.Range(WallDwellMin, WallDwellMax);
                float dwellElapsed = 0f;
                while (dwellElapsed < dwell)
                {
                    dwellElapsed += Time.unscaledDeltaTime;
                    if (_isDefeated) yield break;
                    yield return null;
                }
                dirX = -dirX;
            }

            // ---- Vulnerable: 정지 + 색상 변화, 기존 그대로 유지 (D-locked, 변경 없음) ----
            _state = BossState.Vulnerable;
            IsAlive = true; // 타겟 가능 (BOSS-03)
            _rb.linearVelocity = Vector2.zero;
            _sr.color = vulnerableTintColor;
            _animator?.SetBool("isMoving", false);

            float vulnerableElapsed = 0f;
            while (vulnerableElapsed < vulnerableDuration)
            {
                vulnerableElapsed += Time.unscaledDeltaTime;
                if (_isDefeated) yield break;
                yield return null;
            }

            // 빈틈 창이 히트 없이 종료 - 다시 Dash로 (BOSS-04: 패턴 반복)
            IsAlive = false;
            _sr.color = _baseColor;
        }
    }
    ```

    참고 — 손대지 않는 부분(명시적 확인): OnDashHit(), HitReactionAndReset(), OnTriggerEnter2D(), GetHighlightColor(), FlipSprite(), Awake(), OnPlayerDiedCleanup(), RequiredHits/_hitCount, vulnerableDuration/vulnerableTintColor, hitFlashColor/hitFlashDuration/knockbackForce/resetPauseDuration, 사망 연출 필드(bossMaskRiseDuration 등) — 전부 그대로 둔다. _playerTransform은 HitReactionAndReset()의 넉백 방향 계산에 여전히 쓰이므로 필드 자체(BossEnemyBase 소유)를 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -c "BossState.Telegraph\|BossState.Attack\|enum BossState { Telegraph" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -n "enum BossState { Dash, Vulnerable, HitReaction, Dead }" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -n "_state = BossState.Dash;" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -c "CacheRoomBounds" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -c "GetDashTier" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -c "moveSpeed\|telegraphSpeedMultiplier\|telegraphDuration\b\|attackWindupDelay\|hitboxActiveDuration" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -n "GetComponentsInChildren<RoomConnector>" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -c "_playerTransform" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -n "baseDashSpeed = 16f" Assets/Scripts/Enemy/Boss/FioraBoss.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BossState.Telegraph\|BossState.Attack\|enum BossState { Telegraph"` returns 0 (구 상태값 완전 제거 — enum 선언과 `_state` 기본값 초기화 모두 포함)
    - `grep -n "enum BossState { Dash, Vulnerable, HitReaction, Dead }"` returns 1 match
    - `grep -n "_state = BossState.Dash;"` returns 1 match (필드 기본값도 함께 교체되었는지 확인 — 누락 시 CS0117 컴파일 에러)
    - `grep -c "CacheRoomBounds"` returns 2 (선언 1 + PatternLoop() 내 호출 1)
    - `grep -c "GetDashTier"` returns 2 (선언 1 + PatternLoop() 내 호출 1)
    - `grep -c "moveSpeed\|telegraphSpeedMultiplier\|telegraphDuration\b\|attackWindupDelay\|hitboxActiveDuration"` returns 0 (구 필드 5개 완전 제거 — firstDashTelegraphDuration은 대문자 T라 매치 안 됨, case-sensitive 확인됨)
    - `grep -n "GetComponentsInChildren<RoomConnector>"` returns 1 match (씬 전체 검색 FindObjectsByType<RoomConnector> 사용 금지 확인 겸함)
    - `grep -c "_playerTransform"` returns 1 or more (Pitfall 5 — HitReactionAndReset()에서 여전히 참조되어야 함, 삭제되지 않았음 확인)
    - `grep -n "baseDashSpeed = 16f"` returns 1 match (D-05 기본 속도값 확인)
    - Unity Editor 콘솔에 컴파일 에러 0건 (수동 확인 — 자동 빌드 파이프라인 없음, VALIDATION.md 확정 컨벤션)
  </acceptance_criteria>
  <done>FioraBoss.PatternLoop()가 룸 경계 기반 연속 돌진 패턴(D-01~D-08)으로 완전히 교체되었고, 구 Telegraph/Attack 코드와 관련 필드(enum 선언 및 `_state` 기본값 초기화 포함)가 남김없이 제거되었으며, BossEnemyBase/IEnemy/ISpawnGatable 계약과 BOSS-03~06 판정 로직은 전혀 변경되지 않았다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BossEnemy.prefab Rigidbody2D Interpolate 필드 수정 (RESEARCH.md Pitfall 4, 명시적 번들 결정)</name>
  <files>Assets/Prefabs/Enemies/BossEnemy.prefab</files>
  <read_first>
    - Assets/Prefabs/Enemies/BossEnemy.prefab (Rigidbody2D 블록 — 현재 m_Interpolate: 0 확인, m_GameObject: {fileID: 7269809630814702082})
    - .planning/phases/18.1-fioraboss-ai-fiora-md/18.1-RESEARCH.md "Pitfall 4" 섹션 (수정 근거)
  </read_first>
  <action>
    Assets/Prefabs/Enemies/BossEnemy.prefab의 Rigidbody2D 컴포넌트 블록(`--- !u!50 &2761367018759252734` 부터 다음 `---` 구분자 이전까지, m_GameObject: {fileID: 7269809630814702082}) 안에서 정확히 한 줄만 수정한다:

    변경 전: `  m_Interpolate: 0`
    변경 후: `  m_Interpolate: 1`

    (Unity Rigidbody2D.Interpolate enum: 0=None, 1=Interpolate, 2=Extrapolate — STATE.md의 전역 제약 "Rigidbody2D: Continuous collision detection + Interpolate mode"를 이 프리팹에도 맞춘다.)

    같은 블록의 m_CollisionDetection: 1 (이미 Continuous, 정상)이나 다른 어떤 필드도 건드리지 않는다.

    명시적 범위 결정 — MeleeEnemy.prefab / RangedEnemy.prefab은 건드리지 않는다: 두 프리팹도 동일하게 m_Interpolate: 0이지만(grep으로 확인됨), 이번 Phase는 오직 F.I.O.R.A 보스의 이동 속도만 16~24 units/sec로 대폭 상승시키므로 이 갭이 시각적으로 새롭게 두드러지는 대상은 BossEnemy.prefab뿐이다. MeleeEnemy/RangedEnemy의 이동 속도는 이번 Phase에서 전혀 변경되지 않으므로, 두 프리팹까지 함께 고치는 것은 CLAUDE.md의 "정밀한 변경(현재 Phase 범위 밖 건드리지 않기)" 원칙 위반이다 — 별도 후속 항목으로만 인지하고 이 Phase에서는 손대지 않는다.
  </action>
  <verify>
    <automated>grep -n "m_Interpolate" Assets/Prefabs/Enemies/BossEnemy.prefab; grep -c "m_Interpolate: 0" Assets/Prefabs/Enemies/MeleeEnemy.prefab Assets/Prefabs/Enemies/RangedEnemy.prefab; grep -n "m_CollisionDetection" Assets/Prefabs/Enemies/BossEnemy.prefab</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "m_Interpolate" Assets/Prefabs/Enemies/BossEnemy.prefab` shows "m_Interpolate: 1" (0이 아님)
    - `grep -c "m_Interpolate: 0" Assets/Prefabs/Enemies/MeleeEnemy.prefab` returns 1 (변경 없음 확인)
    - `grep -c "m_Interpolate: 0" Assets/Prefabs/Enemies/RangedEnemy.prefab` returns 1 (변경 없음 확인)
    - `grep -n "m_CollisionDetection" Assets/Prefabs/Enemies/BossEnemy.prefab` still shows "m_CollisionDetection: 1" (Continuous 유지, 회귀 없음)
    - `git diff --stat -- Assets/Prefabs/Enemies/` shows only BossEnemy.prefab as changed
  </acceptance_criteria>
  <done>BossEnemy.prefab의 Rigidbody2D.Interpolate가 Interpolate(1)로 설정되어 새 돌진 속도(16-24 u/s)에서 시각적 stepping 아티팩트가 발생하지 않는다. MeleeEnemy.prefab/RangedEnemy.prefab은 변경되지 않았다(범위 밖, Pitfall 4 근거로 명시적으로 제외).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FIORA.md 돌진 패턴 + Interpolate 수정 수동 플레이테스트 (D-01~D-08, BOSS-03~06 회귀 확인)</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/18.1-fioraboss-ai-fiora-md/18.1-VALIDATION.md (Manual-Only Verifications 표 전체)
    - .planning/phases/18.1-fioraboss-ai-fiora-md/18.1-CONTEXT.md (D-01~D-08 locked decisions)
  </read_first>
  <action>
    자동화 테스트 프레임워크가 없어(프로젝트 전역 확정 컨벤션, VALIDATION.md) 돌진 패턴/타이밍/회귀는 실제 Play로만 확인 가능하다 — 사용자에게 아래 how-to-verify 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 어느 D-번호가 실패했는지 특정한 뒤 FioraBoss.cs의 해당 로직(CacheRoomBounds/GetDashTier/PatternLoop 돌진 루프)을 재검토해 수정한다.
  </action>
  <what-built>FioraBoss.PatternLoop()가 룸 경계 기반 연속 돌진 패턴(D-01~D-08)으로 전면 교체되었고, BossEnemy.prefab의 Rigidbody2D Interpolate 필드가 수정되었다.</what-built>
  <how-to-verify>
    Assets/Scenes/DebugScene.unity를 열고 Play (Room_BossFsmTest + FioraBoss 고정좌표 배치, WorldGenerator 흐름과 무관):
    1. Play 시작 직후 FioraBoss가 첫 돌진 예고(느낌표 아이콘, 약 0.25초) 후 왼쪽 또는 오른쪽 벽을 향해 돌진하는지 확인 — 플레이어를 쫓아가지 않고 곧장 한쪽 벽으로 향하는지(D-04) 확인
    2. hitCount=0 상태(처치 전 첫 사이클)에서 빈틈(Vulnerable) 전환 전까지의 돌진 패스 횟수를 세어 3~5회 범위에 드는지 확인(최소 3회 관찰 권장)
    3. 벽간 이동(28유닛, Room_BossFsmTest 기준) 1회 패스 시간이 약 1.75초 근방(±약간)인지 스톱워치로 확인 (D-05 tier1, 16 u/s)
    4. 벽 도달 시 짧게(0.1~0.2초) 멈췄다가 방향을 바꾸는지 확인(즉시 튕기지 않음, D-06) — 오버슈트/떨림 없이 벽 위치에서 정확히 멈추는지도 함께 확인
    5. 2번째 돌진 패스부터는 느낌표 예고가 뜨지 않는지 확인 (D-07)
    6. 돌진 경로 중간(첫 패스가 아닌 이후 패스)에 서서 플레이어가 닿으면 즉사하는지 확인 (D-08 — 패스 시작 부분만이 아니라 중간에서도 반드시 확인)
    7. N회 돌진 종료 후 보스가 정지하고 색상이 바뀌는 빈틈(Vulnerable) 상태로 전환되는지, 이 상태에서만 대시 공격 대상이 되는지 확인 (BOSS-03 회귀 없음)
    8. 빈틈 상태에서 대시 공격 성공 시 패턴이 처음(새 예고+돌진)부터 재시작되는지 확인 — 1~6회차 반복
    9. 2회 피격(hitCount=2) 이후 재관찰: 돌진 횟수 4~6회, 속도 체감 상승(약 1.4초/패스) 확인 (D-02/D-03/D-05 tier2, 20 u/s)
    10. 5회 피격(hitCount=5) 이후 재관찰: 돌진 횟수 5~7회, 속도 체감 추가 상승(약 1.17초/패스) 확인 (tier3, 24 u/s)
    11. 정확히 7회째 피격에서 처치(사망 연출+카메라 쉐이크+점수 보너스) 발생, 그 이전에는 처치되지 않는지 확인 (BOSS-04 회귀 없음)
    12. 화면 어디에도 피격 횟수/진행률 UI가 없는지 확인 (BOSS-05 회귀 없음)
    13. 돌진 애니메이션이 이전보다 부드럽게 보이는지, 특히 tier2/tier3 속도에서 stepping 아티팩트가 눈에 띄게 줄었는지 확인 (Task 2 Interpolate 수정 검증)
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 돌진 패턴/타이밍/방향/BOSS-03~06 회귀는 시각 판정 및 실측 타이밍이 필요해 자동화 불가 (18.1-VALIDATION.md, 프로젝트 전역 확정 컨벤션). 통과 기준: 사용자 리포트에서 체크리스트 1~13 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~13번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 몇 번째 항목에서, 어떤 동작이 예상과 달랐는지(D-번호 기준) 구체적으로 기록된다
  </acceptance_criteria>
  <done>FIORA.md 보스 패턴 스펙(맵 끝에서 끝으로 연속 돌진 + 랜덤 타이밍 빈틈)이 D-01~D-08 그대로 구현되었고, BOSS-03/04/05/06 판정 로직에 회귀가 없음이 수동 플레이테스트로 확인됨.</done>
  <resume-signal>모두 통과하면 "approved", 문제가 있으면 몇 번 항목이 실패했는지와 증상을 구체적으로 설명해 주세요.</resume-signal>
</task>

</tasks>

<verification>
- FioraBoss.cs의 PatternLoop()가 CacheRoomBounds()/GetDashTier()를 사용하는 룸 경계 기반 돌진 루프로 완전히 교체되었고, 구 Telegraph/Attack 관련 코드/필드(enum 선언 및 `_state` 기본값 초기화 포함)가 남아있지 않다.
- 돌진 방향 결정 로직이 _playerTransform을 참조하지 않는다 (PatternLoop 내부에서만 — HitReactionAndReset()의 참조는 유지).
- BossEnemy.prefab의 Rigidbody2D.Interpolate가 1(Interpolate)이고, MeleeEnemy.prefab/RangedEnemy.prefab은 변경되지 않았다.
- BossEnemyBase/IEnemy/ISpawnGatable 계약, OnDashHit()의 _isDefeated 전용 가드(Pitfall 2), BOSS-04(7회 피격 처치)/BOSS-05(진행률 비노출)/BOSS-06(점수 보너스)이 전혀 변경되지 않았다.
- Task 3 수동 플레이테스트 체크리스트 1~13 전부 통과.
</verification>

<success_criteria>
FioraBoss가 FIORA.md의 보스 패턴 스펙("맵 끝에서 끝으로 연속 돌진 공격을 이어가다가, 랜덤 타이밍에 숨을 고르는 딜레이가 발생 — 이 순간이 유일한 타격 기회")대로 동작하며, BOSS-03/04/05/06의 기존 판정 로직에 회귀가 없고, BossEnemy.prefab의 물리 보간 설정이 새 돌진 속도에 맞게 수정되었다.
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-fioraboss-ai-fiora-md/18.1-01-SUMMARY.md`
</output>
