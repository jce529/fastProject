---
phase: 15-fsm
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
supersedes: ["15-05"]
files_modified:
  - Assets/Editor/RoomBossFsmTestBuilder.cs
  - Assets/Editor/BossFsmTestPoolSwapTool.cs
  - Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab
  - Assets/Scenes/SampleScene.unity
autonomous: false
requirements: [BOSS-03, BOSS-04, BOSS-05, BOSS-06]

must_haves:
  truths:
    - "에디터에서 Play를 누르기만 하면(걸어서 텔레포터를 찾아갈 필요 없이) 첫 룸이 항상 Room_BossFsmTest로 스폰된다 (D-13)"
    - "Room_BossFsmTest.prefab에 다른 13개 룸과 동일 컨벤션의 Door/ENT(Left)/Door/EXIT(Right) RoomConnector 마커가 있어 WorldGenerator의 AlignByEntry()/AlignByExit()/FindConnector()가 경고 없이 정상 정렬한다"
    - "Room_BossFsmTest가 WorldGenerator를 통해 정상 스폰될 때 보스(BossEnemy)가 별도 배선 없이 자동으로 함께 등장한다 — EnemySpawner/WorldGenerator의 Melee/Ranged 전용 EnemyType 계약은 전혀 변경되지 않는다"
    - "SampleScene의 WorldGenerator._roomPrefabs는 테스트 기간 동안 Room_BossFsmTest.prefab 1개만 담도록 스왑되며, 원본 6개 룸 풀은 하드코딩된 GUID로 언제든 정확히 복원 가능하다(가역적 임시 교체 — 영구 변경 아님)"
    - "SampleScene의 WorldGenerator._roomPrefabs가 스왑된 상태에서 Play를 누르면, _lookaheadCount/_lookbehindCount가 0으로 강제되어 Room_BossFsmTest가 정확히 1개만 스폰되고 그 안의 BossEnemy 인스턴스도 정확히 1개다 — 룸 풀에 프리팹이 1개뿐인 상태에서 lookahead 2 + lookbehind 2로 인해 동일 룸/보스가 5개 동시 스폰되는 것을 방지한다(gsd-plan-checker blocker 이슈 1 수정)"
    - "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab은 이 플랜 전후로 git diff 0이다 — 전혀 열리거나 수정되지 않는다"
    - "BossFsmTest_Teleporter(SampleScene 독립 GameObject, D-11 산출물)와 DebugRoomTeleporter._bossPrefab 배선은 삭제되지 않고 그대로 남는다 — 이 플랜은 사용 안 함(unused)으로 만들 뿐이다"
    - "새 진입 경로(WorldGenerator 풀 스왑)를 통해 Room_BossFsmTest 안에서 BOSS-03/04/05/06 플레이테스트 체크리스트(15-05 Task 4와 동일 내용, 격리 확인 항목 확장)가 전부 통과한다"
  artifacts:
    - path: "Assets/Editor/RoomBossFsmTestBuilder.cs"
      provides: "Room_BossFsmTest.prefab 생성 도구 확장 — Door/ENT(Left)/Door/EXIT(Right) RoomConnector 마커 + ExitSpawnPoint 마커 + BossEnemy.prefab nested instance 추가"
      contains: "RoomConnector.Direction.Left"
    - path: "Assets/Editor/BossFsmTestPoolSwapTool.cs"
      provides: "SampleScene의 WorldGenerator._roomPrefabs를 Room_BossFsmTest 1개로/_lookaheadCount·_lookbehindCount를 0으로 임시 스왑하거나 원본(6개 풀, 2/2)으로 복원하는 신규 에디터 도구 — 원본 배열은 GUID 상수로 코드에 기록"
      contains: "MenuItem(\"Fast/Phase15/Swap WorldGenerator Pool To BossFsmTest Only\")"
    - path: "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab"
      provides: "RoomConnector 마커 + ExitSpawnPoint 마커 + nested BossEnemy 인스턴스가 포함된 재생성된 테스트 룸"
      contains: "Door"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "WorldGenerator._roomPrefabs가 Room_BossFsmTest 1개로, _lookaheadCount/_lookbehindCount가 0으로 스왑된 상태로 저장됨"
      contains: "_roomPrefabs:"
  key_links:
    - from: "Assets/Editor/RoomBossFsmTestBuilder.cs"
      to: "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab"
      via: "PrefabUtility.SaveAsPrefabAsset(root, PrefabPath) — Door/ENT/EXIT + ExitSpawnPoint + nested BossEnemy 포함"
      pattern: "InstantiatePrefab"
    - from: "Assets/Editor/BossFsmTestPoolSwapTool.cs"
      to: "Assets/Scenes/SampleScene.unity (WorldGenerator._roomPrefabs)"
      via: "SerializedObject/SerializedProperty 배열 교체 + EditorSceneManager.SaveScene"
      pattern: "_roomPrefabs"
    - from: "Assets/Editor/BossFsmTestPoolSwapTool.cs"
      to: "Assets/Scenes/SampleScene.unity (WorldGenerator._lookaheadCount/_lookbehindCount)"
      via: "SerializedObject/SerializedProperty 필드 0 강제(Swap) / 2·2 복원(Restore)"
      pattern: "_lookaheadCount"
    - from: "Assets/Scripts/World/WorldGenerator.cs (FindConnector/AlignByEntry/AlignByExit)"
      to: "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab (Door/ENT, Door/EXIT)"
      via: "GetComponentsInChildren<RoomConnector>(true)"
      pattern: "RoomConnector"
---

<objective>
D-13(2026-07-17, 사용자 피드백)은 D-11 RE-RESOLVED가 만든 `BossFsmTest_Teleporter`를 걸어서 찾아가는 방식이 테스트하기 번거롭다는 이유로 재차 교체를 확정했다. 이 플랜은 보스 FSM 격리 테스트의 **진입 방식**을 텔레포터 워킹에서 **`WorldGenerator`의 라이브 룸 풀(`_roomPrefabs`) 임시 스왑**으로 바꾼다 — 에디터에서 Play만 누르면 매번 확실히 Room_BossFsmTest에서 시작하게 만든다.

15-05-PLAN.md와의 관계(부분 대체): 15-05 Task 1(`RoomBossFsmTestBuilder.cs` 신설, 커밋 a220a39)은 그대로 유지하고 **확장**한다(다시 만들지 않음). 15-05 Task 2/3(`BossEnemyPrefabBuilder.cs`의 텔레포터 재배선 + 그 Unity 실행)은 이 플랜이 사실상 대체한다 — 텔레포터 배선 자체는 삭제하지 않지만(D-13 명시), 앞으로의 표준 테스트 경로는 텔레포터가 아니라 풀 스왑이 된다. 15-05 Task 4의 11개 항목 플레이테스트 체크리스트 **내용**은 그대로 재사용하되, 진입 방법만 바꾸고 격리 확인 항목을 확장해 이 플랜의 Task 4로 다시 실행한다.

**이 플랜이 명시적으로 닫는 갭 (D-13 텍스트에 없던 추가 설계 결정, 이 플랜의 재량):** D-13은 룸 풀 스왑과 RoomConnector 추가만 언급하지만, 이것만으로는 보스가 실제로 등장하지 않는다 — 기존 D-11 경로는 `DebugRoomTeleporter.TeleportToRoom()`이 `_bossPrefab`을 텔레포트 시점에 직접 `Instantiate()`했지만, `WorldGenerator`의 정상 룸 스폰 경로(`Start()`/`SpawnNextPair()`)는 `EnemySpawner`(Melee/Ranged 전용) 마커만 처리하고 보스를 스폰할 방법이 없다. 이 플랜은 **`BossEnemy.prefab`을 `Room_BossFsmTest.prefab` 자체에 nested prefab instance로 미리 심어** 이 갭을 닫는다 — `WorldGenerator`/`EnemySpawner`의 계약을 전혀 건드리지 않는 가장 단순한 해법이며, Phase 16/17이 설계할 "진짜" 보스 룸 스폰 확률/게이팅 시스템과는 무관한 순수 테스트 편의 장치다.

**명시적 결정 — `_roomPrefabs` 복원 시점:** 이 플랜은 `_roomPrefabs`를 원본 6개 룸으로 **복원하지 않은 채로 완료된다.** 복원용 메뉴 커맨드(`Fast/Phase15/Restore WorldGenerator Original Room Pool`)는 Task 2에서 코드로 제공되지만, 실행은 이 플랜의 태스크에 포함되지 않는다 — 사용자가 보스 FSM 테스트를 마치고 일반 플레이테스트로 돌아갈 준비가 되었을 때 수동으로 실행하는 후속 작업이다. Phase 17(WorldGenerator 통합, BOSS-08)이 어차피 보스 룸의 실제 확률적 스폰 방식을 다시 설계할 것이므로, 이 임시 스왑을 이 플랜 안에서 되돌리는 것은 불필요한 범위 확장이다.

**명시적 결정 — gsd-plan-checker blocker 수정(2026-07-17 revision): `_lookaheadCount`/`_lookbehindCount` 동시 강제:** 체커가 SampleScene.unity의 `WorldGenerator._lookaheadCount`/`_lookbehindCount`가 둘 다 `2`로 설정되어 있음을 확인했다 — 룸 풀이 Room_BossFsmTest 하나로 스왑된 상태에서 이 값들이 그대로 `2`로 남아있으면 `WorldGenerator.Start()`가 시작 룸 1개 + `SpawnNextPair()`×2(lookahead) + `SpawnPrevPair()`×2(lookbehind) = 동일 프리팹을 5번 스폰하고, 각 인스턴스 안의 nested BossEnemy도 5마리 동시에 등장해 격리 테스트가 깨진다(거리 게이팅 없이 전원이 즉시 Telegraph를 시작해 플레이어를 추격/원샷킬하고, 7회 히트 카운트도 어느 보스가 맞았는지 신뢰할 수 없게 된다). `BossFsmTestPoolSwapTool.SwapToBossFsmTestOnly()`는 이제 `_lookaheadCount`/`_lookbehindCount`도 `0`으로 함께 강제해 시작 룸 하나만 스폰되게 하고, `RestoreOriginalPool()`은 원래 값(`2`/`2`, SampleScene.unity에서 확인됨)으로 함께 되돌린다. 이 수정은 체커 경고 이슈 3(코리도어 스폰)도 부수적으로 자동 해결한다 — `SpawnNextPair()`/`SpawnPrevPair()` 자체가 전혀 호출되지 않으므로 Corridor가 생성될 경로가 없다(Task 4에서 재확인).

**명시적 결정 — 체커 경고 이슈 2 수정: `ExitSpawnPoint` 마커 추가:** `WorldGenerator.Start()`는 시작 룸에 `ExitSpawnPoint`가 하나도 없으면 플레이어를 `Vector3.zero`(바닥 높이)로 텔레포트한다 — `RoomEntry`의 `(0,1,0)`과 다른 폴백 경로라 시각적으로 오해를 살 수 있다. 문서화만으로 넘기는 대신(비용이 낮으므로) Task 1이 `RoomEntry`와 동일 위치에 `ExitSpawnPoint` 컴포넌트를 함께 추가해 이 폴백 자체를 회피한다.

Purpose: 보스 FSM(15-01/15-02) 검증을, 텔레포터를 걸어서 찾아가는 마찰 없이 Play 버튼 한 번으로 즉시 반복 가능하게 만든다.
Output: RoomBossFsmTestBuilder.cs(확장 — RoomConnector + ExitSpawnPoint + nested boss), BossFsmTestPoolSwapTool.cs(신규 — 풀+lookahead/lookbehind 스왑/복원 도구), Room_BossFsmTest.prefab(재생성), SampleScene.unity(_roomPrefabs/_lookaheadCount/_lookbehindCount 스왑됨), 플레이테스트 결과.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fsm/15-CONTEXT.md
@.planning/phases/15-fsm/15-RESEARCH.md
@.planning/phases/15-fsm/15-05-SUMMARY.md
@Assets/Editor/RoomBossFsmTestBuilder.cs
@Assets/Editor/RoomMarkerTool.cs
@Assets/Editor/BossEnemyPrefabBuilder.cs
@Assets/Editor/PlayerTrailBuilder.cs
@Assets/Scripts/World/RoomConnector.cs
@Assets/Scripts/World/WorldGenerator.cs
@Assets/Scripts/World/ExitSpawnPoint.cs

<interfaces>
<!-- 기존 코드 계약 — 탐색 불필요, 아래 시그니처를 그대로 사용한다 -->

RoomConnector.cs (전체, 변경 없음):
```csharp
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    [SerializeField] public Direction direction;       // public 필드 — SerializedObject 불필요, 직접 대입 가능
    [SerializeField] public GameObject connectedObject; // WorldGenerator가 채움, Room 프리팹 단계에서는 null로 둠
}
```

ExitSpawnPoint.cs (전체, 변경 없음 — 필드 없는 순수 마커 컴포넌트, `AddComponent<ExitSpawnPoint>()`만으로 충분):
```csharp
public class ExitSpawnPoint : MonoBehaviour
{
    private void OnDrawGizmos() { /* Gizmos만, 런타임 로직 없음 */ }
}
```

Assets/Editor/RoomMarkerTool.cs의 기존 14개 룸 컨벤션 (참고용 — Room_BossFsmTest는 매번 새로 생성되는 프리팹이므로 LoadPrefabContents로 여는 이 패턴을 그대로 호출하지 않고, RoomBossFsmTestBuilder.Build() 안에 인라인으로 직접 생성한다):
```csharp
var child = root.transform.Find("Door/ENT"); // "/" 경로로 중첩 탐색
var rc = child.gameObject.AddComponent<RoomConnector>();
rc.direction = RoomConnector.Direction.Left; // 직접 필드 대입 (public 필드)
```

Assets/Editor/RoomBossFsmTestBuilder.cs 현재 전체 상태 (15-05 Task 1 산출물, 커밋 a220a39 — 이 플랜의 Task 1이 확장할 기준선. 바닥 span은 x:-14~14, y:0. RoomEntry는 (0,1,0)):
```csharp
using UnityEditor;
using UnityEngine;
using UnityEngine.Tilemaps;

public static class RoomBossFsmTestBuilder
{
    private const string RoomName      = "Room_BossFsmTest";
    private const string PrefabPath    = "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab";
    private const string SolidTilePath = "Assets/Tiles/Tile_Solid.asset";

    [MenuItem("Fast/Phase15/Build Room_BossFsmTest")]
    public static void Build()
    {
        var tile = AssetDatabase.LoadAssetAtPath<Tile>(SolidTilePath);
        if (tile == null) { Debug.LogError("..."); return; }

        var root = new GameObject(RoomName);

        var gridGO = new GameObject("Grid");
        gridGO.transform.SetParent(root.transform, false);
        gridGO.AddComponent<Grid>().cellSize = Vector3.one;

        var solidGO = new GameObject("Tilemap_Solid");
        solidGO.transform.SetParent(gridGO.transform, false);
        solidGO.layer = 9;
        var tilemap = solidGO.AddComponent<Tilemap>();
        solidGO.AddComponent<TilemapRenderer>();
        solidGO.AddComponent<TilemapCollider2D>();

        for (int x = -14; x <= 14; x++)
            tilemap.SetTile(new Vector3Int(x, 0, 0), tile);

        var entryGO = new GameObject("RoomEntry");
        entryGO.transform.SetParent(root.transform);
        entryGO.transform.localPosition = new Vector3(0f, 1f, 0f);
        entryGO.AddComponent<RoomEntry>(); // 이 주석 줄은 Task 1이 갱신한다 — 더 이상 "유일한 마커"가 아님

        string dir = "Assets/Prefabs/Rooms/Room_BossFsmTest";
        if (!AssetDatabase.IsValidFolder(dir))
            AssetDatabase.CreateFolder("Assets/Prefabs/Rooms", "Room_BossFsmTest");

        if (AssetDatabase.LoadAssetAtPath<GameObject>(PrefabPath) != null)
            AssetDatabase.DeleteAsset(PrefabPath);

        PrefabUtility.SaveAsPrefabAsset(root, PrefabPath);
        Object.DestroyImmediate(root);

        AssetDatabase.SaveAssets();
        AssetDatabase.Refresh();
        Debug.Log($"[RoomBossFsmTestBuilder] Created {PrefabPath} — RoomEntry(0,1,0) + flat floor(x:-14~14, y:0) only.");
    }
}
```
(이 플랜의 Task 1은 "string dir = ..." 저장 블록 직전에 Door/ENT+Door/EXIT+ExitSpawnPoint+nested BossEnemy 생성 코드를 삽입한다.)

WorldGenerator.cs 관련 발췌 (변경 없음, 그대로 재사용):
```csharp
[SerializeField] private GameObject[] _roomPrefabs;         // private — Task 2는 SerializedObject로만 접근 가능
[SerializeField] private int _lookaheadCount  = 2;           // GEN-01: 앞 N개 유지, private — SerializedObject 필요
[SerializeField] private int _lookbehindCount = 2;            // GEN-02: 뒤 N개 유지, private — SerializedObject 필요

private RoomConnector FindConnector(GameObject go, RoomConnector.Direction direction)
{
    foreach (RoomConnector rc in go.GetComponentsInChildren<RoomConnector>(true))
        if (rc.direction == direction) return rc;
    return null;
}
// RecomputeCameraBounds()는 CameraBound 컴포넌트가 없으면 그냥 스킵한다(에러 없음) — D-13이 CameraBound
// 추가를 생략한 근거. TrySpawnEnemies()도 EnemySpawner가 없는 섹션에서는 그냥 아무것도 하지 않는다(에러 없음).

// Start() 발췌 — checker 이슈 1/2의 근거:
// var startRoomPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];  // 풀이 1개면 항상 Room_BossFsmTest
// var startSpawnPoints = startRoom.GetComponentsInChildren<ExitSpawnPoint>(true);
// startTeleportPos = startSpawnPoints.Length > 0 ? ... : Vector3.zero;       // 이슈 2: ExitSpawnPoint 없으면 바닥(0,0,0) 폴백
// for (int i = 0; i < _lookaheadCount; i++) SpawnNextPair();                 // 이슈 1: 2면 풀이 1개뿐이라 동일 룸 2개 추가 스폰
// for (int i = 0; i < _lookbehindCount; i++) SpawnPrevPair();                // 이슈 1: 2면 룸 2개 추가 스폰 — 합계 5개 룸, 5마리 보스
```

Assets/Scenes/SampleScene.unity 현재 확인된 WorldGenerator 상태 (2026-07-17 grep 확인 — GameObject fileID 1344439147, MonoBehaviour fileID 1344439150):
```yaml
_roomPrefabs:
- {fileID: 2564265763709976335, guid: c0f6fea8d7a78ce43ac944ec03d891dc, type: 3}
- {fileID: 7737809153690431726, guid: ed2d93b531268124382b9403fce5ec7b, type: 3}
- {fileID: 3114153540849030163, guid: 89dc91aa481422a498c78a73e7f6a4d2, type: 3}
- {fileID: 6386340677169604563, guid: 844b014bfac621a44b5ab3524880a63a, type: 3}
- {fileID: 8226199497095424007, guid: 0aa738cd2b8fc6a45be52c4403bc2e80, type: 3}
- {fileID: 4532260002168211988, guid: b6ff0323bb78a7b4ba8eaebdaa67a684, type: 3}
_lookaheadCount: 2
_lookbehindCount: 2
```
(이 6개 GUID가 Task 2의 복원용 상수 배열의 정확한 소스이며, `2`/`2`가 Task 2의 lookahead/lookbehind 복원 상수 소스다 — 순서/값 그대로 보존해야 한다.)

BossEnemyPrefabBuilder.cs 확인된 상수 (변경 없음, 참조만):
```csharp
private const string TargetPrefabPath = "Assets/Prefabs/Enemies/BossEnemy.prefab"; // 이미 디스크에 존재 확인됨(2026-07-16 사용자 Task 3 실행 결과, 아직 미커밋)
```

Assets/Editor/PlayerTrailBuilder.cs / BossEnemyPrefabBuilder.cs 컨벤션 ("열려있는 씬에 스크립트로 오브젝트를 추가/수정" — Task 2가 그대로 따른다):
```csharp
var activeScene = EditorSceneManager.GetActiveScene();
if (activeScene.name != "SampleScene") { Debug.LogError("..."); return; }
var target = Object.FindFirstObjectByType<SomeComponent>(FindObjectsInactive.Include);
var so = new SerializedObject(target);
so.FindProperty("someField").objectReferenceValue = value;
so.ApplyModifiedProperties();
EditorUtility.SetDirty(target);
EditorSceneManager.MarkSceneDirty(target.gameObject.scene);
EditorSceneManager.SaveScene(target.gameObject.scene);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomBossFsmTestBuilder.cs 확장 — RoomConnector 마커 + ExitSpawnPoint 마커 + nested BossEnemy 인스턴스 (D-13 + 체커 이슈 2)</name>
  <files>Assets/Editor/RoomBossFsmTestBuilder.cs</files>
  <read_first>
    - Assets/Editor/RoomBossFsmTestBuilder.cs (현재 전체 — 위 interfaces 블록에 이미 인용됨, 확장 대상)
    - Assets/Editor/RoomMarkerTool.cs (Door/ENT+Door/EXIT 네이밍 컨벤션, RoomConnector.direction 직접 대입 패턴)
    - Assets/Editor/BossEnemyPrefabBuilder.cs (BossEnemy.prefab 경로 상수 `TargetPrefabPath` 값 확인용)
    - Assets/Scripts/World/RoomConnector.cs (컴포넌트 계약 — public 필드, SerializedObject 불필요)
    - Assets/Scripts/World/ExitSpawnPoint.cs (필드 없는 순수 마커 — AddComponent만으로 충분)
  </read_first>
  <action>
    `Assets/Editor/RoomBossFsmTestBuilder.cs`의 `Build()` 메서드를 다음과 같이 확장한다. 기존 Grid/Tilemap_Solid/RoomEntry 생성 로직과 파일 저장 로직(`string dir = ...`부터 끝까지)은 **한 글자도 바꾸지 않는다** — 오직 `entryGO.AddComponent<RoomEntry>();` 줄과 `string dir = "Assets/Prefabs/Rooms/Room_BossFsmTest";` 줄 사이에 새 코드를 삽입한다.

    1) 클래스 상단에 신규 const 추가 (`SolidTilePath` 바로 아래):
    ```csharp
    private const string BossPrefabPath = "Assets/Prefabs/Enemies/BossEnemy.prefab"; // D-13: 이 룸 자체에 nested prefab으로 미리 심어둠
    ```

    2) `entryGO.AddComponent<RoomEntry>();` 줄의 주석을 다음으로 교체 (더 이상 "유일한 마커"가 아니므로):
    ```csharp
    entryGO.AddComponent<RoomEntry>(); // 진입 지점 마커
    ```

    3) 위 줄 바로 다음 줄에 `ExitSpawnPoint` 마커 추가 코드를 삽입한다 (gsd-plan-checker 경고 이슈 2 수정):
    ```csharp
    // gsd-plan-checker 이슈 2 (revision, 2026-07-17): WorldGenerator.Start()는 시작 룸에 ExitSpawnPoint가
    // 하나도 없으면 플레이어를 Vector3.zero(바닥 높이, RoomEntry(0,1,0)보다 낮음)로 텔레포트한다.
    // Room_BossFsmTest는 이 폴백 경로를 타지 않도록 RoomEntry와 동일 GameObject/위치에 ExitSpawnPoint를
    // 함께 심는다 (필드 없는 순수 마커라 컴포넌트만 추가하면 충분 — 포탈 스폰 후보로도 쓰이지만
    // _exitSpawnChance 롤에 당첨되지 않는 한 포탈은 생기지 않으므로 부작용 없음).
    entryGO.AddComponent<ExitSpawnPoint>();
    ```

    4) 저장 블록(`string dir = ...`) 직전에 아래 코드를 그대로 삽입:
    ```csharp
    // D-13 (2026-07-17): Door/ENT(Left)+Door/EXIT(Right) RoomConnector — 다른 13개 룸과 동일 컨벤션
    // (Assets/Editor/RoomMarkerTool.cs 참고). WorldGenerator.FindConnector()/AlignByEntry()/AlignByExit()가
    // 이 마커로 체인 정렬한다. 바닥 span(x:-14~14) 양 끝, RoomEntry와 동일 y=1.
    var doorGO = new GameObject("Door");
    doorGO.transform.SetParent(root.transform, false);

    var entConnGO = new GameObject("ENT");
    entConnGO.transform.SetParent(doorGO.transform, false);
    entConnGO.transform.localPosition = new Vector3(-14f, 1f, 0f);
    entConnGO.AddComponent<RoomConnector>().direction = RoomConnector.Direction.Left;

    var exitConnGO = new GameObject("EXIT");
    exitConnGO.transform.SetParent(doorGO.transform, false);
    exitConnGO.transform.localPosition = new Vector3(14f, 1f, 0f);
    exitConnGO.AddComponent<RoomConnector>().direction = RoomConnector.Direction.Right;

    // D-13: WorldGenerator._roomPrefabs가 이 룸 하나로 스왑됐을 때 Play만 눌러도 보스가 실제로 등장하도록,
    // BossEnemy.prefab을 nested prefab instance로 미리 심어둔다 — 기존 DebugRoomTeleporter._bossPrefab의
    // "텔레포트 시점 즉시 Instantiate" 동작을 prefab-build 시점으로 이전한 것. WorldGenerator/EnemySpawner의
    // Melee/Ranged 전용 EnemyType 계약은 전혀 건드리지 않는다(D-13 취지, 정밀한 변경).
    var bossPrefab = AssetDatabase.LoadAssetAtPath<GameObject>(BossPrefabPath);
    if (bossPrefab == null)
    {
        Debug.LogError($"[RoomBossFsmTestBuilder] {BossPrefabPath} not found — run 'Fast/Phase15/Build BossEnemy Prefab' first.");
        Object.DestroyImmediate(root);
        return;
    }
    var bossInstance = (GameObject)PrefabUtility.InstantiatePrefab(bossPrefab, root.transform);
    bossInstance.transform.localPosition = new Vector3(6f, 1f, 0f); // RoomEntry(0,1,0) 기준 우측 6유닛 — 기존 DebugRoomTeleporter의 Vector3.right*6f 오프셋과 동일 컨벤션
    ```

    5) 마지막 `Debug.Log` 줄을 다음으로 교체:
    ```csharp
    Debug.Log($"[RoomBossFsmTestBuilder] Created {PrefabPath} — RoomEntry(0,1,0) + ExitSpawnPoint(0,1,0) + flat floor(x:-14~14, y:0) + Door/ENT(Left)/EXIT(Right) RoomConnectors + nested BossEnemy instance(6,1,0).");
    ```

    6) 클래스 상단 문서 주석(`/// <summary>` 블록) 끝에 한 줄 추가:
    ```csharp
    /// D-13 (2026-07-17): Door/ENT/EXIT RoomConnector + ExitSpawnPoint + nested BossEnemy 인스턴스 추가 —
    /// WorldGenerator _roomPrefabs 풀 스왑(BossFsmTestPoolSwapTool.cs)을 통해 Play만 눌러도 보스전이
    /// 시작되도록 지원한다. ExitSpawnPoint는 gsd-plan-checker 이슈 2 수정(Vector3.zero 폴백 회피)이다.
    ```

    이 태스크는 코드만 수정한다 — 메뉴 재실행(Room_BossFsmTest.prefab 재생성)은 이 플랜의 Task 3(checkpoint:human-action)에서 수행한다. 기존 디스크상의 `Room_BossFsmTest.prefab`(15-05 Task 3 산출물, RoomConnector/보스 없음)은 이 태스크 시점에 갱신되지 않는다.
  </action>
  <verify>
    <automated>grep -n "RoomConnector.Direction.Left" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "RoomConnector.Direction.Right" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "InstantiatePrefab" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "BossPrefabPath" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "AddComponent&lt;ExitSpawnPoint&gt;()" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "new Vector3(-14f, 1f, 0f)" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "new Vector3(14f, 1f, 0f)" Assets/Editor/RoomBossFsmTestBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Build()`가 기존 Tilemap/RoomEntry 생성 로직과 프리팹 저장 로직을 그대로 유지한다(git diff로 삽입/치환 구간 외 변경 없음 확인)
    - `Door/ENT`(Left)와 `Door/EXIT`(Right) 두 개의 `RoomConnector`가 각각 `x:-14`와 `x:14`, `y:1`에 생성된다
    - `RoomEntry`와 동일한 GameObject(`entryGO`)에 `ExitSpawnPoint` 컴포넌트가 추가되어 `WorldGenerator.Start()`의 `Vector3.zero` 폴백 텔레포트가 발생하지 않는다(체커 경고 이슈 2 수정)
    - `BossPrefabPath`("Assets/Prefabs/Enemies/BossEnemy.prefab")가 `PrefabUtility.InstantiatePrefab()`으로 nested instance로 심어진다 — `Object.Instantiate`(연결 끊긴 클론)가 아니다
    - BossEnemy.prefab이 없으면 `Debug.LogError` 후 `root`를 정리(`DestroyImmediate`)하고 조용히 반환한다 — 예외를 던지지 않는다
    - `EnemySpawner`/`KillZone`/`CameraBound` 등 다른 룸 기믹 컴포넌트는 여전히 추가되지 않는다
  </acceptance_criteria>
  <done>RoomBossFsmTestBuilder.cs가 RoomConnector 마커, ExitSpawnPoint 마커, nested BossEnemy 인스턴스를 생성하도록 확장됨 — 실행(프리팹 재생성)은 Task 3에서 수행.</done>
</task>

<task type="auto">
  <name>Task 2: BossFsmTestPoolSwapTool.cs — WorldGenerator._roomPrefabs + _lookaheadCount/_lookbehindCount 임시 스왑/복원 도구 신설 (D-13 + 체커 blocker 이슈 1)</name>
  <files>Assets/Editor/BossFsmTestPoolSwapTool.cs</files>
  <read_first>
    - Assets/Editor/BossEnemyPrefabBuilder.cs (SerializedObject + 활성 씬 확인 + EditorSceneManager 저장 컨벤션 전체)
    - Assets/Editor/PlayerTrailBuilder.cs (FindFirstObjectByType + SetDirty + MarkSceneDirty 컨벤션)
    - Assets/Scripts/World/WorldGenerator.cs (14행 `[SerializeField] private GameObject[] _roomPrefabs;`, 26~27행 `_lookaheadCount`/`_lookbehindCount` — 모두 private 필드이므로 SerializedProperty로만 접근 가능함을 확인)
  </read_first>
  <action>
    신규 파일 `Assets/Editor/BossFsmTestPoolSwapTool.cs` 생성:

    ```csharp
    using UnityEditor;
    using UnityEditor.SceneManagement;
    using UnityEngine;

    /// <summary>
    /// Menu: Fast/Phase15/Swap WorldGenerator Pool To BossFsmTest Only,
    ///       Fast/Phase15/Restore WorldGenerator Original Room Pool
    /// D-13 (2026-07-17): BossFsmTest_Teleporter를 걸어서 찾아가는 방식이 번거롭다는 사용자 피드백으로,
    /// SampleScene의 WorldGenerator._roomPrefabs 풀 자체를 Room_BossFsmTest.prefab 하나만 담도록 임시
    /// 교체한다 — Play만 누르면 첫 룸이 항상 Room_BossFsmTest로 스폰된다(WorldGenerator.Start() 참고).
    /// 원본 6개 룸 풀은 이 파일에 하드코딩된 GUID로 언제든 복원 가능 — 영구 변경이 아니라 테스트 기간
    /// 동안의 가역적 스왑이다. 이 플랜은 복원을 자동 실행하지 않는다 — 사용자가 보스 FSM 테스트를 마치고
    /// 일반 플레이테스트로 돌아갈 때 수동으로 Restore 메뉴를 실행하는 것이 후속 작업이다.
    /// gsd-plan-checker blocker 이슈 1 (revision, 2026-07-17): 룸 풀이 1개뿐인 상태에서
    /// _lookaheadCount/_lookbehindCount가 원래 값(2/2)으로 남아있으면 Start()가 동일 룸(및 그 안의
    /// nested BossEnemy)을 5번 스폰해버린다 — Swap은 이 두 필드도 0으로 강제하고, Restore는 2/2로 되돌린다.
    /// </summary>
    public static class BossFsmTestPoolSwapTool
    {
        private const string TargetSceneName     = "SampleScene";
        private const string BossFsmTestRoomPath = "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab";

        // 2026-07-17 기준 SampleScene.unity WorldGenerator._roomPrefabs 원본 6개 룸 풀(D-13 스왑 이전) —
        // 순서 그대로 보존. GUIDToAssetPath로 로드하면 각 프리팹의 루트 GameObject를 정확히 가리킨다.
        private static readonly string[] OriginalRoomPrefabGuids =
        {
            "c0f6fea8d7a78ce43ac944ec03d891dc", // fileID 2564265763709976335
            "ed2d93b531268124382b9403fce5ec7b", // fileID 7737809153690431726
            "89dc91aa481422a498c78a73e7f6a4d2", // fileID 3114153540849030163
            "844b014bfac621a44b5ab3524880a63a", // fileID 6386340677169604563
            "0aa738cd2b8fc6a45be52c4403bc2e80", // fileID 8226199497095424007
            "b6ff0323bb78a7b4ba8eaebdaa67a684", // fileID 4532260002168211988
        };

        // 2026-07-17 기준 SampleScene.unity WorldGenerator._lookaheadCount/_lookbehindCount 원본 값
        // (gsd-plan-checker blocker 이슈 1 수정 — Restore가 되돌릴 정확한 소스).
        private const int OriginalLookaheadCount  = 2;
        private const int OriginalLookbehindCount = 2;

        [MenuItem("Fast/Phase15/Swap WorldGenerator Pool To BossFsmTest Only")]
        public static void SwapToBossFsmTestOnly()
        {
            var wg = GetWorldGeneratorInActiveScene();
            if (wg == null) return;

            var testRoomPrefab = AssetDatabase.LoadAssetAtPath<GameObject>(BossFsmTestRoomPath);
            if (testRoomPrefab == null)
            {
                Debug.LogError($"[BossFsmTestPoolSwapTool] {BossFsmTestRoomPath} not found — run 'Fast/Phase15/Build Room_BossFsmTest' first.");
                return;
            }

            LogCurrentPool(wg, "Swap 직전");

            var so = new SerializedObject(wg);
            var prop = so.FindProperty("_roomPrefabs");
            prop.arraySize = 1;
            prop.GetArrayElementAtIndex(0).objectReferenceValue = testRoomPrefab;

            // gsd-plan-checker blocker 이슈 1 수정: 룸 풀이 1개뿐인 상태에서 lookahead/lookbehind가
            // 2/2로 남아있으면 Start()가 시작 룸 1개 + SpawnNextPair()x2 + SpawnPrevPair()x2 =
            // 동일 프리팹을 5번 스폰하고, 그 안의 nested BossEnemy도 5마리 동시에 등장한다(거리 게이팅
            // 없이 전원이 즉시 Telegraph를 시작). Play만 누르면 격리된 단일 보스 룸만 남도록 0으로 강제한다.
            var lookaheadProp = so.FindProperty("_lookaheadCount");
            var lookbehindProp = so.FindProperty("_lookbehindCount");
            lookaheadProp.intValue = 0;
            lookbehindProp.intValue = 0;

            so.ApplyModifiedProperties();

            SaveActiveScene(wg);
            Debug.Log($"[BossFsmTestPoolSwapTool] _roomPrefabs swapped to 1-entry pool ({BossFsmTestRoomPath}), _lookaheadCount/_lookbehindCount forced to 0 (single-boss isolation). Run 'Fast/Phase15/Restore WorldGenerator Original Room Pool' when done testing.");
        }

        [MenuItem("Fast/Phase15/Restore WorldGenerator Original Room Pool")]
        public static void RestoreOriginalPool()
        {
            var wg = GetWorldGeneratorInActiveScene();
            if (wg == null) return;

            var so = new SerializedObject(wg);
            var prop = so.FindProperty("_roomPrefabs");
            prop.arraySize = OriginalRoomPrefabGuids.Length;

            for (int i = 0; i < OriginalRoomPrefabGuids.Length; i++)
            {
                string path = AssetDatabase.GUIDToAssetPath(OriginalRoomPrefabGuids[i]);
                var prefab = string.IsNullOrEmpty(path) ? null : AssetDatabase.LoadAssetAtPath<GameObject>(path);
                if (prefab == null)
                {
                    Debug.LogError($"[BossFsmTestPoolSwapTool] Could not resolve original room prefab #{i} (guid {OriginalRoomPrefabGuids[i]}) — restore aborted, _roomPrefabs left unchanged.");
                    return;
                }
                prop.GetArrayElementAtIndex(i).objectReferenceValue = prefab;
            }

            // gsd-plan-checker blocker 이슈 1 수정: Swap이 0으로 강제한 lookahead/lookbehind를
            // 원래 값(2026-07-17 SampleScene.unity 확인)으로 되돌린다.
            so.FindProperty("_lookaheadCount").intValue = OriginalLookaheadCount;
            so.FindProperty("_lookbehindCount").intValue = OriginalLookbehindCount;

            so.ApplyModifiedProperties();
            SaveActiveScene(wg);
            Debug.Log($"[BossFsmTestPoolSwapTool] _roomPrefabs restored to original {OriginalRoomPrefabGuids.Length}-room pool, _lookaheadCount/_lookbehindCount restored to {OriginalLookaheadCount}/{OriginalLookbehindCount}.");
        }

        private static WorldGenerator GetWorldGeneratorInActiveScene()
        {
            var activeScene = EditorSceneManager.GetActiveScene();
            if (activeScene.name != TargetSceneName)
            {
                Debug.LogError($"[BossFsmTestPoolSwapTool] '{TargetSceneName}'이 열려있지 않습니다 (현재: '{activeScene.name}'). Assets/Scenes/SampleScene.unity를 열고 다시 실행하세요.");
                return null;
            }

            var wg = Object.FindFirstObjectByType<WorldGenerator>(FindObjectsInactive.Include);
            if (wg == null)
            {
                Debug.LogError("[BossFsmTestPoolSwapTool] WorldGenerator not found in active scene.");
                return null;
            }
            return wg;
        }

        private static void LogCurrentPool(WorldGenerator wg, string label)
        {
            var so = new SerializedObject(wg);
            var prop = so.FindProperty("_roomPrefabs");
            var names = new System.Text.StringBuilder();
            for (int i = 0; i < prop.arraySize; i++)
            {
                var obj = prop.GetArrayElementAtIndex(i).objectReferenceValue;
                names.Append(obj != null ? obj.name : "null");
                if (i < prop.arraySize - 1) names.Append(", ");
            }
            Debug.Log($"[BossFsmTestPoolSwapTool] {label} _roomPrefabs ({prop.arraySize}): {names}");
        }

        private static void SaveActiveScene(WorldGenerator wg)
        {
            EditorUtility.SetDirty(wg);
            EditorSceneManager.MarkSceneDirty(wg.gameObject.scene);
            EditorSceneManager.SaveScene(wg.gameObject.scene);
        }
    }
    ```

    멱등성: `SwapToBossFsmTestOnly()`를 여러 번 실행해도 배열은 계속 1개 엔트리로 유지되고 lookahead/lookbehind도 계속 0으로 유지된다(재대입은 안전). `RestoreOriginalPool()`도 몇 번을 실행해도 항상 동일한 6개 GUID + 2/2로 되돌린다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase15/Swap WorldGenerator Pool To BossFsmTest Only\")" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "MenuItem(\"Fast/Phase15/Restore WorldGenerator Original Room Pool\")" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "c0f6fea8d7a78ce43ac944ec03d891dc" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "b6ff0323bb78a7b4ba8eaebdaa67a684" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -c "FindProperty(\"_roomPrefabs\")" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "_lookaheadCount" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "_lookbehindCount" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "OriginalLookaheadCount  = 2" Assets/Editor/BossFsmTestPoolSwapTool.cs && grep -n "OriginalLookbehindCount = 2" Assets/Editor/BossFsmTestPoolSwapTool.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/BossFsmTestPoolSwapTool.cs`가 존재하고 두 개의 `[MenuItem]`(Swap, Restore)을 포함한다
    - `OriginalRoomPrefabGuids` 배열이 정확히 6개의 GUID를 SampleScene.unity에서 확인된 순서 그대로 담고 있다
    - `_roomPrefabs`/`_lookaheadCount`/`_lookbehindCount` 모두 `SerializedObject`/`SerializedProperty`로만 접근한다 — private 필드에 리플렉션이나 다른 우회 방법을 쓰지 않는다
    - `SwapToBossFsmTestOnly()`는 `_lookaheadCount`/`_lookbehindCount`를 `0`으로 강제하고, `RestoreOriginalPool()`은 이를 각각 원래 값 `2`/`2`로 복원한다(체커 blocker 이슈 1 수정)
    - `SwapToBossFsmTestOnly()`/`RestoreOriginalPool()` 둘 다 활성 씬이 `SampleScene`이 아니면 `Debug.LogError` 후 조용히 반환한다
    - 두 메서드 모두 `EditorUtility.SetDirty` + `EditorSceneManager.MarkSceneDirty` + `EditorSceneManager.SaveScene`으로 변경사항을 씬에 저장한다
  </acceptance_criteria>
  <done>BossFsmTestPoolSwapTool.cs가 WorldGenerator._roomPrefabs와 _lookaheadCount/_lookbehindCount를 함께 Room_BossFsmTest 전용 1개 배열+0/0으로 스왑하거나 원본 6개+2/2로 복원하는 두 메뉴 커맨드로 존재함 — 실행은 Task 3에서 수행(Swap만), Restore 실행은 이 플랜 범위 밖(후속 수동 작업).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Unity 에디터 — 컴파일 확인 + Room_BossFsmTest 재생성 + WorldGenerator 풀/lookahead/lookbehind 스왑 실행</name>
  <files>Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab, Assets/Scenes/SampleScene.unity</files>
  <read_first>
    - Assets/Editor/RoomBossFsmTestBuilder.cs (Task 1 산출물 — 실행할 메뉴 경로와 새로 추가된 Door/ENT/EXIT/ExitSpawnPoint/보스 확인)
    - Assets/Editor/BossFsmTestPoolSwapTool.cs (Task 2 산출물 — 실행할 메뉴 경로와 풀/lookahead/lookbehind 스왑 로직 확인)
  </read_first>
  <action>
    Unity 에디터 내부 조작(메뉴 실행)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 컴파일 에러가 보고되면 Task 1/2 코드의 문법/시그니처 불일치를 수정한 뒤 재확인을 요청한다.
  </action>
  <what-built>RoomBossFsmTestBuilder.cs(Task 1, RoomConnector+ExitSpawnPoint+nested boss 확장) + BossFsmTestPoolSwapTool.cs(Task 2, 신규 풀+lookahead/lookbehind 스왑 도구) — Unity 에디터 안에서의 마지막 조립 단계만 남음. BossEnemy.prefab은 이미 존재하므로 재생성 불필요(존재 여부만 확인)</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서 `Assets/Scenes/SampleScene.unity`를 열어둔 상태로 순서대로:
    1. **컴파일 확인**: Console에 컴파일 에러 0건 (경고는 허용)
    2. **BossEnemy.prefab 존재 확인**: `Assets/Prefabs/Enemies/BossEnemy.prefab`이 Project 창에 이미 존재하는지 확인 — 존재하면 재실행 불필요
    3. **Room_BossFsmTest 재생성**: 메뉴 Fast → Phase15 → Build Room_BossFsmTest 실행 → Console에 "ExitSpawnPoint(0,1,0) + ... + Door/ENT(Left)/EXIT(Right) RoomConnectors + nested BossEnemy instance(6,1,0)" 문구가 포함된 로그 확인, 에러 0건
    4. **프리팹 구조 확인**: `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab`을 선택해 Hierarchy 미리보기에서 `Grid/Tilemap_Solid`, `RoomEntry`(Inspector에 `RoomEntry` + `ExitSpawnPoint` 컴포넌트 둘 다 붙어있는지 확인 — 체커 이슈 2 수정), `Door/ENT`, `Door/EXIT`, `BossEnemy`(nested, 1.6배 확대+붉은 색조) 오브젝트가 모두 존재하는지 확인 — 다른 적/기믹 오브젝트는 없어야 한다
    5. **WorldGenerator 풀/lookahead/lookbehind 스왑**: SampleScene이 활성 씬인 상태에서 메뉴 Fast → Phase15 → Swap WorldGenerator Pool To BossFsmTest Only 실행 → Console에 "Swap 직전 _roomPrefabs (6): ..." 로그와 "_roomPrefabs swapped to 1-entry pool ..., _lookaheadCount/_lookbehindCount forced to 0 ..." 로그 둘 다 확인
    6. **Inspector 확인**: Hierarchy에서 WorldGenerator GameObject 선택 → Inspector의 `Room Prefabs` 배열이 `Size = 1`, 요소 0이 `Room_BossFsmTest`인지 확인. 같은 Inspector에서 `Lookahead Count`/`Lookbehind Count`가 둘 다 `0`인지 함께 확인(체커 blocker 이슈 1 수정)
    7. **Play 테스트**: Play 버튼을 눌러 플레이어가 자동으로 Room_BossFsmTest에 스폰되는지 확인 — 바닥이 허공에 떠 있거나 어긋나지 않고(RoomConnector 정렬 정상), 플레이어가 바닥 아래로 떨어지거나 허공에 뜨지 않고 RoomEntry 높이(y=1) 근처에서 시작하며(ExitSpawnPoint 정상 동작), 보스(BossEnemy, 크게+붉은색)가 우측 근처에 등장하는지 확인, Console 에러 0건
    8. **정확히 1개 확인** (체커 blocker 이슈 1 수정 검증): Play 중 Hierarchy 검색창에 "Room_BossFsmTest"를 입력해 결과가 정확히 1개(Clone)인지 확인, 이어서 "BossEnemy"를 입력해 결과가 정확히 1개(Clone)인지 확인 — 5개씩 나타나면 안 된다
    9. **레거시/Room_Debug 무변경 확인**: 터미널에서 `git status`로 `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`이 변경 목록에 없는지 확인 (diff 0 — 이 태스크에서 이 파일은 전혀 열리거나 수정되지 않아야 한다)
    10. **재실행 안전성 확인**: 3번과 5번 메뉴를 각각 한 번 더 실행 — Room_BossFsmTest.prefab이 동일 구조로 재생성되고, `_roomPrefabs`가 여전히 1개 요소, `_lookaheadCount`/`_lookbehindCount`가 여전히 둘 다 `0`인지 확인 (중복/누적 없음)
    11. **스왑 상태 유지 확인**: `_roomPrefabs`/`_lookaheadCount`/`_lookbehindCount`를 아직 복원하지 않는다 — 다음 태스크(Task 4 플레이테스트)가 이 스왑된 상태를 그대로 사용한다. `Fast/Phase15/Restore WorldGenerator Original Room Pool`은 이번 체크포인트에서 실행하지 않는다
  </how-to-verify>
  <verify>
    <automated>test -f "Assets/Prefabs/Enemies/BossEnemy.prefab" && test -f "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab" && grep -c "m_Name: ENT" "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab" && grep -c "m_Name: EXIT" "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab" && test -z "$(git diff --stat -- Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab)" && awk '/_roomPrefabs:/{flag=1;next}/_corridorFlat:/{flag=0}flag' "Assets/Scenes/SampleScene.unity" | grep -c "^  -" && grep -n "_lookaheadCount: 0" Assets/Scenes/SampleScene.unity && grep -n "_lookbehindCount: 0" Assets/Scenes/SampleScene.unity</automated>
  </verify>
  <acceptance_criteria>
    - Console 컴파일 에러 0건 (사용자 확인)
    - `Room_BossFsmTest.prefab`이 `Door/ENT`, `Door/EXIT`, `ExitSpawnPoint`(RoomEntry에 병합), nested `BossEnemy` 인스턴스를 포함한 구조로 재생성되었다
    - `Assets/Scenes/SampleScene.unity`의 `WorldGenerator._roomPrefabs`가 정확히 1개 요소(`Room_BossFsmTest`)를 담고 있다 (Inspector + awk 카운트 둘 다 1)
    - `_lookaheadCount`/`_lookbehindCount`가 Swap 후 Inspector와 SampleScene.unity 원본 데이터 둘 다에서 `0`으로 확인된다(체커 blocker 이슈 1 수정)
    - `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`은 이 태스크 전후로 git diff 0이다
    - Play 모드에서 플레이어가 Room_BossFsmTest에 자동 스폰되고, 보스가 등장하며, 바닥/커넥터 정렬에 시각적 이상이 없다 (사용자 확인)
    - Play 시점에 Hierarchy에 `Room_BossFsmTest`(Clone) 인스턴스가 정확히 1개, 그 안의 `BossEnemy` 인스턴스가 정확히 1개만 존재한다(체커 blocker 이슈 1 수정 확인)
    - 두 메뉴 모두 재실행해도 결과가 안정적이다(중복 생성/누적 없음) (사용자 확인)
  </acceptance_criteria>
  <done>Room_BossFsmTest.prefab이 RoomConnector+ExitSpawnPoint+nested boss를 포함한 상태로 재생성되고, SampleScene의 WorldGenerator._roomPrefabs가 이 룸 하나로, _lookaheadCount/_lookbehindCount가 0/0으로 스왑되어 Play만 누르면 정확히 1개의 격리된 보스전이 즉시 시작 가능한 상태 — Room_Debug.prefab은 전혀 건드려지지 않음.</done>
  <resume-signal>"approved" 입력 또는 문제 항목 설명 (예: "컴파일 에러 2건" 또는 "바닥이 허공에 떠 있음" 또는 "보스가 등장하지 않음" 또는 "보스가 5마리 등장함" 또는 "Room_Debug.prefab에 diff가 생김")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 플레이테스트 — WorldGenerator 풀 스왑 경로로 Room_BossFsmTest 안에서 BOSS-03/04/05/06 검증 (격리 확인 항목 확장)</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 15 Success Criteria 1-5
    - .planning/phases/15-fsm/15-CONTEXT.md §Implementation Decisions (D-01~D-13)
  </read_first>
  <action>
    빈틈 타겟팅 타이밍, 7회 처치 임계값, 점수 자기 상쇄는 인간 시각 판정만 가능(15-RESEARCH.md Validation Architecture, 자동화 테스트 프레임워크 없음) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 이 체크리스트는 15-05-PLAN.md Task 4와 동일한 내용이되, 진입 방법이 "BossFsmTest_Teleporter로 걸어가기"에서 "Play 버튼(WorldGenerator가 Room_BossFsmTest를 첫 룸으로 자동 스폰)"으로 바뀌고, 체커 blocker/경고 이슈 수정을 검증하는 항목(정확히 1개 보스, ExitSpawnPoint 텔레포트, 코리도어 미스폰)이 추가되었다. 문제가 보고되면 원인 파일(BossEnemy.cs 튜닝값, RoomConnector/ExitSpawnPoint 위치 등)을 특정해 후속 수정 여부를 사용자와 논의한다.
  </action>
  <what-built>보스 FSM 전체(15-01/15-02: 예고→공격→빈틈 패턴 루프, 레이스 안전 피격 등록, D-12 점수 자기 상쇄, D-08 처치 연출 연장) + WorldGenerator 풀/lookahead/lookbehind 스왑을 통한 마찰 없는(Play 버튼 한 번) 격리 스폰 테스트 환경(D-13 + 체커 blocker/경고 이슈 수정)</what-built>
  <how-to-verify>
    에디터 Play 후(텔레포터로 걸어갈 필요 없이 시작하자마자 Room_BossFsmTest 안에 있음), 아래 체크리스트:

    **[BOSS-03 — 빈틈 타겟팅]**
    1. 보스가 예고(이동+근접 공격) 중일 때 공격 버튼을 눌러 슬로우모션으로 조준 — 보스가 빨간 하이라이트/타겟 후보로 잡히지 않는다
    2. 보스가 빈틈(정지+노란색 변화) 상태로 전환되는 순간, 같은 방식으로 조준하면 보스가 빨간 하이라이트로 잡히고 돌진 대상이 된다
    3. 이 Telegraph→Attack→Vulnerable 루프가 최소 5회 반복되는 동안 위 1/2번 패턴이 일관되게 유지된다

    **[BOSS-04 — 7회 처치 + 패턴 리셋]**
    4. 빈틈 상태에서 돌진 공격을 성공시킨다 — 보스가 색상 플래시+넉백 반응을 보이고, 짧은 공백 후 패턴이 예고(Telegraph)부터 다시 시작된다
    5. 이 과정을 1~6번째 히트까지 반복 — 매번 보스가 생존하고 패턴이 리셋된다 (Destroy되지 않는다)
    6. 7번째 히트를 성공시킨다 — 보스가 사망 연출(Die 애니메이션 → 파티클 → SpriteMask 페이드 → 카메라 쉐이크)을 재생하고 최종적으로 Destroy된다

    **[BOSS-05 — 진행률 비노출]**
    7. 1~7번째 히트 전 과정 동안 화면 어디에도 피격 횟수/진행률을 나타내는 UI 텍스트가 나타나지 않는다

    **[BOSS-06 — 점수 보너스 (D-12 포함)]**
    8. HUD 점수 표시를 보면서 1~6번째 히트 각각의 직후 점수가 변하지 않는지 확인한다 (+100이 잠깐 보였다가 사라지는 것도 실패 — 순간적으로도 변하면 안 됨)
    9. 7번째 히트(처치) 직후 점수가 한 번에 크게 상승한다 (일반 적 처치(+100)보다 눈에 띄게 큰 폭)

    **[일반 안정성 + 새 진입 경로 확인]**
    10. 전체 7회 히트 사이클 동안 콘솔 에러 0건, 보스가 벽에 끼거나 화면 밖으로 사라지는 등의 비정상 동작 없음, RoomConnector 정렬로 인한 바닥 어긋남/틈 없음
    11. Room_BossFsmTest 안에는 보스 외에 다른 적(근접/원거리)이 전혀 등장하지 않는다 — 순수 격리 전투 확인. 그리고 Play를 누른 즉시(텔레포터를 찾아 걸어갈 필요 없이) Room_BossFsmTest 안에서 시작했는지 확인(D-13 핵심 목표)

    **[체커 수정 검증 — 격리/폴백 확인 (revision, 2026-07-17)]**
    12. **정확히 1개의 BossEnemy만 존재** (체커 blocker 이슈 1): Play 중 Hierarchy 검색창에 "BossEnemy"를 입력해 결과가 정확히 1개인지 확인, 이어서 "Room_BossFsmTest"를 입력해 결과가 정확히 1개인지 확인 — `_lookaheadCount`/`_lookbehindCount`를 0으로 강제해 5개 동시 스폰을 방지한 수정이 실제로 작동하는지 검증
    13. **ExitSpawnPoint 텔레포트 확인** (체커 경고 이슈 2): 시작 시 플레이어가 허공에 뜨거나 바닥 아래로 꺼지지 않고 RoomEntry 높이(y=1) 근처에서 정상 스폰되는지 확인 — Room_BossFsmTest에 ExitSpawnPoint 마커를 추가해 Vector3.zero 폴백을 회피한 수정(15-06 Task 1)이 실제로 작동하는지 검증
    14. **코리도어 미스폰 확인** (체커 경고 이슈 3): Room_BossFsmTest 좌우로 Corridor(연결 통로)가 전혀 생성되지 않는지 확인 — `_lookaheadCount`/`_lookbehindCount`가 0으로 강제되어 SpawnNextPair()/SpawnPrevPair() 자체가 호출되지 않으므로 자동으로 해결되어야 한다. 만약 그래도 Corridor가 보이면 몇 번인지와 함께 보고한다
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 빈틈 타겟팅/7회 처치/점수 자기 상쇄/격리 인스턴스 개수/진입 마찰 개선은 시각 판정만 가능 (15-RESEARCH.md Validation Architecture 정당화, 이 프로젝트에 자동화 테스트 프레임워크 없음). 통과 기준: 사용자 리포트에서 체크리스트 1~14 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~14번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(몇 번째 히트, 어떤 동작이 예상과 달랐는지)이 함께 기록된다
    - 11번 항목을 통해 "Play만 누르면 즉시 Room_BossFsmTest에서 시작"(D-13 핵심 목표)이 실제로 체감되었음이 확인된다
    - 12~14번 항목을 통해 체커 blocker(이슈 1)/경고(이슈 2, 3) 수정이 실제 플레이에서 확인된다
  </acceptance_criteria>
  <done>Phase 15 성공 기준 5개(BOSS-03, BOSS-04, BOSS-05, BOSS-06, D-12 포함)가 WorldGenerator 풀/lookahead/lookbehind 스왑을 통한 마찰 없는 격리 진입 경로에서 플레이테스트로 통과 확인. `_roomPrefabs`/`_lookaheadCount`/`_lookbehindCount`는 스왑된 상태로 남아있으며, 복원은 이 플랜 범위 밖의 후속 수동 작업이다(objective 참고).</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "8번: 3번째 히트 후 점수가 잠깐 +100 올라갔다가 내려감" 또는 "12번: BossEnemy가 5개 검색됨" 또는 "13번: 플레이어가 바닥 아래로 떨어짐")</resume-signal>
</task>

</tasks>

<verification>
- `Assets/Editor/RoomBossFsmTestBuilder.cs`, `Assets/Editor/BossFsmTestPoolSwapTool.cs` 컴파일 성공
- `Assets/Scripts/Enemy/BossEnemy.cs`, `Assets/Scripts/World/ScoreManager.cs`, `Assets/Scripts/World/WorldGenerator.cs`, `Assets/Scripts/World/DebugRoomTeleporter.cs`, `Assets/Editor/BossEnemyPrefabBuilder.cs`의 git diff 0줄 (이 플랜은 진입 경로 재배선 전용 — FSM/점수/사망연출/WorldGenerator 로직 코드는 15-01/15-02/15-05에서 이미 완료, 재변경 없음. `_roomPrefabs`/`_lookaheadCount`/`_lookbehindCount` 값 자체의 변경은 코드가 아니라 SampleScene.unity 데이터에서만 발생한다)
- `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`의 git diff 0줄 — 이 플랜은 Room_Debug.prefab을 전혀 열거나 수정하지 않는다
- `Assets/Scenes/SampleScene.unity`의 `WorldGenerator._roomPrefabs`가 `Room_BossFsmTest.prefab` 1개 요소로 스왑되어 있다
- `Assets/Scenes/SampleScene.unity`의 `WorldGenerator._lookaheadCount`/`_lookbehindCount`가 `0`으로 강제되어 있다(스왑 직후 기준) — 룸 풀이 1개뿐인 상태에서 lookahead/lookbehind가 2/2로 남아있으면 동일 룸+보스가 5개 동시 스폰되는 체커 blocker(이슈 1)를 방지한다. `Fast/Phase15/Restore WorldGenerator Original Room Pool` 실행 시 `2`/`2`로, `_roomPrefabs`는 원본 6개로 함께 복원된다
- `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab`에 `Door/ENT`(Left)/`Door/EXIT`(Right) RoomConnector + ExitSpawnPoint 마커 + nested BossEnemy 인스턴스가 존재한다
- Phase 15 ROADMAP Success Criteria 1-5 전항목 새 진입 경로(WorldGenerator 풀/lookahead/lookbehind 스왑)로 플레이테스트 통과
</verification>

<success_criteria>
- 에디터에서 Play 버튼만 누르면(텔레포터를 걸어서 찾아갈 필요 없이) Room_BossFsmTest에서 정확히 1개의 격리된 보스전이 즉시 시작된다 (D-13 핵심 목표 + 체커 blocker 이슈 1 수정)
- BOSS-03, BOSS-04, BOSS-05, BOSS-06이 이 새 진입 경로에서 실제 플레이로 검증됨
- D-12(비치명타 점수 자기 상쇄)가 시각적으로 재확인됨 — 점수 HUD가 1~6회 히트 동안 전혀 흔들리지 않음
- Play 시점에 Room_BossFsmTest 및 BossEnemy 인스턴스가 각각 정확히 1개씩만 존재한다(체커 blocker 이슈 1 수정 확인)
- `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`은 git diff 0으로 완전히 무관하게 유지됨
- `WorldGenerator._roomPrefabs`(1개)와 `_lookaheadCount`/`_lookbehindCount`(둘 다 `0`)는 이 플랜 완료 시점에 의도적으로 스왑/강제된 상태로 남는다 — 원본 6개 룸 풀 및 `2`/`2`로의 복원은 `Fast/Phase15/Restore WorldGenerator Original Room Pool` 메뉴를 통한 명시적 후속 수동 작업이며 이 플랜의 완료 조건이 아니다
- `BossFsmTest_Teleporter`/`DebugRoomTeleporter` 배선은 삭제되지 않고 그대로 남아있다(사용 안 함)
- 15-05-PLAN.md는 부분 대체(Task 2/3 대체, Task 1/4 내용 재사용)된 상태로 남고, 이 플랜(15-06)이 Phase 15의 최종 실행 플랜이다
- `/gsd:verify-work 15` 진입 가능 상태
</success_criteria>

<output>
After completion, create `.planning/phases/15-fsm/15-06-SUMMARY.md` (플레이테스트 체크리스트 결과 + 발견된 이슈 + 대응 여부 + `_roomPrefabs`/`_lookaheadCount`/`_lookbehindCount` 스왑 상태 유지 확정 + 복원 방법 안내 포함)
</output>
