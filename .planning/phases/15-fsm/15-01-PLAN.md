---
phase: 15-fsm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/ScoreManager.cs
  - Assets/Scripts/Enemy/EnemyDeathEffect.cs
autonomous: true
requirements: [BOSS-04, BOSS-06]

must_haves:
  truths:
    - "ScoreManager.AddBossKillScore()를 호출하면 Score가 BossKillScore(750)만큼 증가한다 (BOSS-06/D-09)"
    - "ScoreManager.SubtractScore(int)를 호출하면 Score가 인자만큼 감소한다 — D-12(BossEnemy 비치명타 자기 상쇄)가 소비할 유틸"
    - "EnemyDeathEffect.ConfigureIntensity(maskRiseDuration, particleColor, particleBurstCount)를 호출한 뒤 PlayDeathSequence()를 실행하면 전달된 3개 값으로 사망 연출이 재생된다 (D-08 보스 전용 연장의 기반)"
    - "MeleeEnemy.cs/RangedEnemy.cs의 기존 EnemyDeathEffect 호출부(AddComponent 후 바로 StartCoroutine)는 이 플랜에서 한 줄도 변경되지 않는다 — ConfigureIntensity()를 호출하지 않으면 기존 기본값(_maskRiseDuration=0.6f, _particleColor, _particleBurstCount=12)이 그대로 유지된다"
  artifacts:
    - path: "Assets/Scripts/World/ScoreManager.cs"
      provides: "BossKillScore 상수 + AddBossKillScore()/SubtractScore() 정적 메서드"
      contains: "public const int BossKillScore"
    - path: "Assets/Scripts/Enemy/EnemyDeathEffect.cs"
      provides: "ConfigureIntensity() 추가 메서드 + _particleBurstCount 필드(기본값 12)"
      contains: "public void ConfigureIntensity"
  key_links:
    - from: "Assets/Scripts/Enemy/BossEnemy.cs (15-02 산출물, 아직 미존재)"
      to: "Assets/Scripts/World/ScoreManager.cs"
      via: "OnDashHit()/Die()에서 AddBossKillScore()/SubtractScore() 정적 호출"
      pattern: "ScoreManager\\.(AddBossKillScore|SubtractScore)"
    - from: "Assets/Scripts/Enemy/BossEnemy.cs (15-02 산출물, 아직 미존재)"
      to: "Assets/Scripts/Enemy/EnemyDeathEffect.cs"
      via: "AddComponent 직후 ConfigureIntensity() 호출 후 PlayDeathSequence() 시작 (MeleeEnemy 컨벤션 + 신규 1줄)"
      pattern: "ConfigureIntensity\\("
---

<objective>
보스가 15-02에서 소비할 두 가지 기반 유틸을 먼저 준비한다: (1) `ScoreManager`에 보스 처치 보너스(D-09)와 비치명타 자기 상쇄(D-12)를 위한 신규 정적 메서드, (2) `EnemyDeathEffect`에 보스 전용으로 사망 연출 강도를 확장할 수 있는 additive 설정 메서드. 둘 다 기존 호출부(CombatController, MeleeEnemy, RangedEnemy)에 diff 0을 유지하는 순수 추가 변경이다.

Purpose: BossEnemy.cs(15-02)가 컴파일 시점에 바로 참조할 수 있는 안정된 계약을 먼저 만들어, Interface-First 순서로 다음 플랜의 스캐빈저 헌트를 방지한다.
Output: ScoreManager.cs(+3 멤버), EnemyDeathEffect.cs(+1 필드, +1 메서드).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fsm/15-CONTEXT.md
@.planning/phases/15-fsm/15-RESEARCH.md
@Assets/Scripts/World/ScoreManager.cs
@Assets/Scripts/Enemy/EnemyDeathEffect.cs
@Assets/Scripts/Player/CombatController.cs

<interfaces>
<!-- CombatController.ExecuteDash()의 관련 호출 순서 — 15-02가 상쇄해야 할 정확한 지점(D-12/Pitfall 4).
     이 플랜에서는 CombatController를 변경하지 않는다 — 읽기 전용 참조. -->

From Assets/Scripts/Player/CombatController.cs:306-312 (읽기 전용, 변경 금지):
```csharp
bool isRespawnKill = ((MonoBehaviour)target).GetComponent<RespawnedEnemyMarker>() != null;
target.OnDashHit();                       // BossEnemy.OnDashHit()이 여기서 호출됨
AudioManager.PlaySfx(Sfx.Slash);
SpawnHitSpark(destination);               // D-06 요구사항의 "기존 히트스파크"가 여기서 이미 재생됨
_cameraFollow?.Shake(_cameraShakeDuration, _cameraShakeAmplitude);
ScoreManager.AddKillScore(isRespawnKill);  // target이 BossEnemy여도 무조건 호출됨 — 비치명타 6회 전부 +100
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreManager 보스 점수 유틸 추가 (BOSS-06, D-09, D-12)</name>
  <files>Assets/Scripts/World/ScoreManager.cs</files>
  <read_first>
    - Assets/Scripts/World/ScoreManager.cs (현재 전체 — AddKillScore()/AddTimeBonus() 패턴 확인)
  </read_first>
  <action>
    `Assets/Scripts/World/ScoreManager.cs`에 다음을 추가한다.

    1) `KillScore` 상수 선언 바로 아래(기존 상수 그룹 안)에 추가:
    ```csharp
    public const int BossKillScore = 750; // D-09: KillScore(100) 대비 유의미하게 큰 값, 500~1000 권장 범위 내
    ```

    2) `AddKillScore()` 메서드 바로 아래에 두 개의 신규 정적 메서드 추가:
    ```csharp
    /// <summary>BOSS-06/D-09: 보스 처치(7회째 피격) 순간 CombatController와 무관하게 BossEnemy.Die()가 직접 호출한다.</summary>
    public static void AddBossKillScore()
    {
        Score += BossKillScore;
    }

    /// <summary>
    /// D-12: BossEnemy.OnDashHit()의 비치명타(1~6회)가 CombatController.ExecuteDash()의
    /// 무조건적인 AddKillScore(false) 호출(+100)을 즉시 상쇄하기 위해 사용한다.
    /// CombatController/IEnemy 계약은 변경하지 않는다 — BossEnemy 내부에서만 소비.
    /// </summary>
    public static void SubtractScore(int amount)
    {
        Score -= amount;
    }
    ```

    기존 `KillScore`/`AddKillScore()`/`AddTimeBonus()`/`StartRoomTimer()`/`AddRoomClearBonus()`는 한 글자도 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "public const int BossKillScore" Assets/Scripts/World/ScoreManager.cs && grep -n "public static void AddBossKillScore" Assets/Scripts/World/ScoreManager.cs && grep -n "public static void SubtractScore" Assets/Scripts/World/ScoreManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/World/ScoreManager.cs`에 `public const int BossKillScore = 750;`가 존재한다
    - `public static void AddBossKillScore()` 본문이 `Score += BossKillScore;` 한 줄이다
    - `public static void SubtractScore(int amount)` 본문이 `Score -= amount;` 한 줄이다
    - 기존 `AddKillScore(bool isRespawn = false)` 시그니처와 본문은 diff 0이다
  </acceptance_criteria>
  <done>ScoreManager가 보스 처치 보너스(D-09)와 비치명타 자기 상쇄(D-12)에 필요한 API를 제공 — CombatController/IEnemy 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: EnemyDeathEffect 보스 전용 강도 설정 메서드 추가 (D-08)</name>
  <files>Assets/Scripts/Enemy/EnemyDeathEffect.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/EnemyDeathEffect.cs (현재 전체 — `_maskRiseDuration`/`_particleColor` 필드와 `SpawnDeathParticles()`의 하드코딩된 `Burst(0f, 12)` 확인)
    - Assets/Scripts/Enemy/MeleeEnemy.cs:98-100 (`AddComponent<EnemyDeathEffect>()` 후 `StartCoroutine(PlayDeathSequence(...))` 호출 컨벤션 — 이 컨벤션은 변경하지 않음)
  </read_first>
  <action>
    `Assets/Scripts/Enemy/EnemyDeathEffect.cs`를 다음과 같이 수정한다.

    1) 필드 선언부에 `_particleBurstCount` 추가 (기존 `_maskRiseDuration`/`_particleColor` 바로 아래):
    ```csharp
    [SerializeField] private int _particleBurstCount = 12; // 기본값 12 — MeleeEnemy/RangedEnemy 기존 동작과 동일 유지
    ```

    2) `SpawnDeathParticles()` 내부의 하드코딩된 버스트 라인을 필드 참조로 교체:
    ```csharp
    // 변경 전: emission.SetBursts(new[] { new ParticleSystem.Burst(0f, 12) });
    // 변경 후:
    emission.SetBursts(new[] { new ParticleSystem.Burst(0f, (short)_particleBurstCount) });
    ```

    3) `SpawnDeathParticles()` 메서드 위(또는 `PlayDeathSequence()` 바로 위)에 공개 메서드 추가:
    ```csharp
    /// <summary>
    /// D-08: 이 컴포넌트는 AddComponent()로 매번 새로 붙기 때문에 Inspector 프리팹 값이 존재하지 않는다
    /// (EnemyDeathEffect.cs 상단 doc-comment 참조). BossEnemy.Die()가 AddComponent 직후,
    /// StartCoroutine(PlayDeathSequence(...)) 호출 전에 호출해 보스 전용 강도로 재정의한다.
    /// MeleeEnemy/RangedEnemy는 이 메서드를 호출하지 않으므로 기존 기본값 그대로 동작한다.
    /// </summary>
    public void ConfigureIntensity(float maskRiseDuration, Color particleColor, int particleBurstCount)
    {
        _maskRiseDuration = maskRiseDuration;
        _particleColor = particleColor;
        _particleBurstCount = particleBurstCount;
    }
    ```

    `PlayDeathSequence()`/`SpawnDeathParticles()`의 나머지 로직(애니메이션 대기, SpriteMask 페이드, Destroy 순서)은 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -n "_particleBurstCount" Assets/Scripts/Enemy/EnemyDeathEffect.cs && grep -n "public void ConfigureIntensity" Assets/Scripts/Enemy/EnemyDeathEffect.cs && grep -n "(short)_particleBurstCount" Assets/Scripts/Enemy/EnemyDeathEffect.cs</automated>
  </verify>
  <acceptance_criteria>
    - `[SerializeField] private int _particleBurstCount = 12;` 필드가 존재한다
    - `SpawnDeathParticles()` 내부의 `ParticleSystem.Burst` 생성자가 `(short)_particleBurstCount`를 사용한다 (하드코딩된 `12` 리터럴이 더 이상 없다)
    - `public void ConfigureIntensity(float maskRiseDuration, Color particleColor, int particleBurstCount)`가 존재하고 본문이 3개 필드에 각각 대입한다
    - `Assets/Scripts/Enemy/MeleeEnemy.cs`, `Assets/Scripts/Enemy/RangedEnemy.cs`의 git diff가 0줄이다 (호출부 무변경)
  </acceptance_criteria>
  <done>EnemyDeathEffect가 보스 전용 강도(마스크 상승 시간/파티클 색상/버스트 수)를 코드로 재정의할 수 있는 additive API를 제공 — 기존 두 적 타입의 사망 연출은 완전히 동일하게 유지.</done>
</task>

</tasks>

<verification>
- `ScoreManager.cs`, `EnemyDeathEffect.cs` 컴파일 성공 (문법 오류 없음)
- `AddKillScore()`, `MeleeEnemy.cs`, `RangedEnemy.cs` 무변경 확인 (grep diff 0)
</verification>

<success_criteria>
- 15-02(BossEnemy.cs)가 즉시 참조 가능한 `ScoreManager.AddBossKillScore()`/`SubtractScore()`, `EnemyDeathEffect.ConfigureIntensity()` API가 준비됨
- 기존 두 적 타입(MeleeEnemy/RangedEnemy)의 점수/사망 연출 동작에는 어떤 회귀도 없음
</success_criteria>

<output>
After completion, create `.planning/phases/15-fsm/15-01-SUMMARY.md`
</output>
