---
phase: 15-fsm
plan: 05
type: execute
wave: 4
depends_on: ["15-03"]
supersedes: ["15-04"]
files_modified:
  - Assets/Editor/RoomBossFsmTestBuilder.cs
  - Assets/Editor/BossEnemyPrefabBuilder.cs
  - Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab
  - Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab
autonomous: false
requirements: [BOSS-03, BOSS-04, BOSS-05, BOSS-06]

must_haves:
  truths:
    - "Assets/Prefabs/Rooms/Room_BossFsmTest.prefab이 신규 독립 프리팹으로 존재하며, RoomEntry 마커 하나 + 평평한 바닥(Tilemap) 외에 기존 적 스폰 마커/기믹이 전혀 없다 (D-11 RE-RESOLVED)"
    - "Room_BossFsmTest.prefab은 Room_Debug.prefab을 참조하거나 그 안에 중첩되지 않는다 — 완전히 독립된 에셋 파일이라 Phase 16의 Room_Debug/구형 Room_*.prefab 삭제가 실행돼도 영향받지 않는다"
    - "BossEnemyPrefabBuilder.cs의 배선 대상은 더 이상 Room_Debug의 모든 DebugRoomTeleporter가 아니라, Room_Debug 안의 전용 신규 teleporter 자식 오브젝트 하나(BossFsmTest) — 이 인스턴스의 targetRoomPrefab은 Room_BossFsmTest.prefab을, _bossPrefab은 BossEnemy.prefab을 가리킨다"
    - "Room_Debug.prefab 안의 기존 14종 레거시 DebugRoomTeleporter(Combat/Chase/Fall/Stair/Recover/Sniper/Cross/Chain/Hunt/Ladder/Dodge/Gap/Mixed/LadderDanger)의 targetRoomPrefab/offsetX/offsetY/_meleePrefab/_rangedPrefab은 이 플랜 실행 전후로 diff 0이다 (정밀한 변경 — 레거시 테스트 경로 보존)"
    - "BossEnemy FSM 자체(15-02) 및 EnemyDeathEffect/ScoreManager 확장(15-01)은 이 플랜에서 한 줄도 변경되지 않는다 — 이 플랜은 순수 리와이어링(rewiring)만 다룬다"
    - "새 BossFsmTest teleporter를 통해 진입하면, 15-04가 정의했던 것과 동일한 BOSS-03/04/05/06 플레이테스트 체크리스트 항목이 Room_BossFsmTest 안에서 전부 통과한다"
  artifacts:
    - path: "Assets/Editor/RoomBossFsmTestBuilder.cs"
      provides: "Room_BossFsmTest.prefab(RoomEntry+평평한 바닥) 생성 에디터 도구 — Assets/Editor/RoomCreator.cs의 NewRoom/AddEntry/PRow 컨벤션 재사용"
      contains: "MenuItem(\"Fast/Phase15/Build Room_BossFsmTest\")"
    - path: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      provides: "재배선 도구 — RoomDebugPath 상수 제거, BossFsmTestRoomPath 신규 추가, WireBossIntoRoomDebug()를 WireBossIntoBossFsmTestRoom()으로 교체(전용 teleporter 1개만 find-or-create)"
      contains: "MenuItem(\"Fast/Phase15/Wire Boss Into BossFsmTest Room\")"
    - path: "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab"
      provides: "보스 FSM 격리 테스트 전용 목적지 룸 — Room_Debug와 완전히 독립"
      contains: "RoomEntry"
    - path: "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"
      provides: "신규 전용 'BossFsmTest' teleporter 자식 오브젝트 1개 추가 — 기존 14종은 무변경"
      contains: "_bossPrefab:"
  key_links:
    - from: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      to: "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab"
      via: "WireBossIntoBossFsmTestRoom()이 BossFsmTest teleporter의 targetRoomPrefab에 배선"
      pattern: "BossFsmTestRoomPath"
    - from: "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab (BossFsmTest teleporter)"
      to: "Assets/Prefabs/Enemies/BossEnemy.prefab"
      via: "DebugRoomTeleporter._bossPrefab 직렬화 참조"
      pattern: "_bossPrefab:"
    - from: "Assets/Editor/RoomBossFsmTestBuilder.cs"
      to: "Assets/Scripts/World/RoomEntry.cs"
      via: "AddComponent<RoomEntry>()"
      pattern: "AddComponent<RoomEntry>"
---

<objective>
D-11이 2026-07-15에 재논의(RE-RESOLVED)되어, 보스 FSM 격리 테스트 환경의 목적지 룸이 (삭제 예정인) `Room_Debug.prefab`이 아니라 신규 독립 프리팹 `Room_BossFsmTest.prefab`이어야 하는 것으로 확정됐다. 15-03은 이 재논의 이전에 작성되어 `Room_Debug.prefab` 자체를 배선 대상으로 코딩했다 — 아직 실행(멱등적 메뉴 커맨드)되지 않았으므로 되돌릴 배선은 없다(`Room_Debug.prefab`의 `_bossPrefab:` 카운트는 현재 0). 이 플랜은 **되돌리기가 아니라 리다이렉트**다: 실행되기 전에 배선 목적지 자체를 바꾼다.

이 플랜은 **15-04-PLAN.md를 대체(supersede)한다.** 15-04는 여전히 파일로 존재하지만 상단에 SUPERSEDED 배너가 추가되어 더 이상 실행되지 않는다 — 15-04의 두 체크포인트 태스크(에디터 도구 실행 + 플레이테스트)는 이 플랜의 Task 3/4로 대상만 `Room_BossFsmTest`로 교체되어 그대로 이어진다.

범위는 사용자가 명시적으로 확정한 **리와이어링 전용**이다: BossEnemy FSM(15-02), ScoreManager/EnemyDeathEffect 확장(15-01)은 전혀 건드리지 않는다. Phase 16의 Room_Debug/구형 Room_*.prefab 삭제 작업도 이 플랜 범위 밖이다(레거시 14종 teleporter는 무변경).

Purpose: Phase 16이 Room_Debug.prefab과 구형 Room_*.prefab 14종을 삭제해도 Phase 15의 보스 FSM 격리 테스트 환경이 살아남도록, 목적지 룸을 Room_Debug와 완전히 독립된 자산으로 재배치한다.
Output: Room_BossFsmTest.prefab(신규 목적지 룸), RoomBossFsmTestBuilder.cs(신규 빌더), BossEnemyPrefabBuilder.cs(재배선 로직 교체), Room_Debug.prefab(전용 teleporter 1개 추가 — 레거시 14종 무변경), 플레이테스트 결과.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fsm/15-CONTEXT.md
@.planning/phases/15-fsm/15-RESEARCH.md
@.planning/phases/15-fsm/15-03-SUMMARY.md
@Assets/Editor/RoomCreator.cs
@Assets/Editor/BossEnemyPrefabBuilder.cs
@Assets/Scripts/World/DebugRoomTeleporter.cs
@Assets/Scripts/World/RoomEntry.cs

<interfaces>
<!-- 기존 코드 계약 — 탐색 불필요, 아래 시그니처를 그대로 사용한다 -->

DebugRoomTeleporter.cs (15-03 산출물, 변경 없음 — 그대로 재사용):
```csharp
[SerializeField] private GameObject targetRoomPrefab;  // 언더스코어 없음 — 텔레포트 목적지 룸
[SerializeField] private float      offsetX = 30f;
[SerializeField] private float      offsetY = 30f;
[SerializeField] private bool       activateEnemies = false;
[SerializeField] private GameObject _meleePrefab;
[SerializeField] private GameObject _rangedPrefab;
[SerializeField] private GameObject _bossPrefab;        // D-11 — 보스 스폰 목적지

private void TeleportToRoom()
{
    ...
    s_lastDebugRoom = Instantiate(targetRoomPrefab, s_nextSpawnPos, Quaternion.identity);
    ...
    RoomEntry entry = s_lastDebugRoom.GetComponentInChildren<RoomEntry>(true);
    Vector3 entryPos = entry != null ? entry.transform.position : s_lastDebugRoom.transform.position + Vector3.up * 2f;
    if (_bossPrefab != null)
    {
        Vector3 bossSpawnPos = entryPos + Vector3.right * 6f;
        Instantiate(_bossPrefab, bossSpawnPos, Quaternion.identity, s_lastDebugRoom.transform);
    }
    ...
}
```

RoomEntry.cs (전체, 변경 없음):
```csharp
public class RoomEntry : MonoBehaviour { }
```

Assets/Editor/RoomCreator.cs 재사용 컨벤션 (전체 탐색 불필요 — 아래 3개 헬퍼 패턴만 그대로 복제해서 쓴다, private이라 직접 호출은 불가하므로 RoomBossFsmTestBuilder.cs 안에 동일 패턴을 인라인으로 재작성):
```csharp
// NewRoom(): Grid + Tilemap_Solid(layer 9 Platform) + TilemapCollider2D
var gridGO = new GameObject("Grid");
gridGO.transform.SetParent(root.transform, false);
gridGO.AddComponent<Grid>().cellSize = Vector3.one;
var solidGO = new GameObject("Tilemap_Solid");
solidGO.transform.SetParent(gridGO.transform, false);
solidGO.layer = 9; // Platform
var tilemap = solidGO.AddComponent<Tilemap>();
solidGO.AddComponent<TilemapRenderer>();
solidGO.AddComponent<TilemapCollider2D>();

// PRow(): 바닥 한 줄 채우기
for (int x = x0; x <= x1; x++) tm.SetTile(new Vector3Int(x, y, 0), tile);

// AddEntry(): RoomEntry 마커
var go = new GameObject("RoomEntry");
go.transform.SetParent(root.transform);
go.transform.localPosition = localPos;
go.AddComponent<RoomEntry>();

// EnsureSolidTile()과 동일한 공유 타일 자산 — 이미 디스크에 존재하므로 재생성하지 말고 LoadAssetAtPath로만 로드:
// "Assets/Tiles/Tile_Solid.asset" (Fast/RoomCreator가 이미 생성해둠, 확인됨)
```

Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab 현재 구조 (grep 확인 — 14개 레거시 DebugRoomTeleporter 자식, 각기 다른 targetRoomPrefab):
```
Room_Debug (root)
├── Combat / Chase / Fall / Stair / Recover / Sniper / Cross / Chain / Hunt / Ladder / Dodge / Gap / Mixed / LadderDanger
│     (각각 Transform + SpriteRenderer(초록 1x1) + BoxCollider2D(isTrigger, size 1x1) + DebugRoomTeleporter)
│     targetRoomPrefab: 각기 다른 구형 Room_*.prefab, offsetX=30, offsetY=30, activateEnemies=0
├── Door / ENT / EXIT / RoomExit / CameraBound / Debug Door / Ground(1) / Ground / Platforms / KillZone (비-teleporter 오브젝트)
```
Room_Debug.prefab의 `_bossPrefab:` 매치 개수는 현재 0 (2026-07-15 확인) — 되돌릴 배선이 없다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomBossFsmTestBuilder.cs — Room_BossFsmTest.prefab 생성 도구 (D-11 RE-RESOLVED)</name>
  <files>Assets/Editor/RoomBossFsmTestBuilder.cs</files>
  <read_first>
    - Assets/Editor/RoomCreator.cs (NewRoom/PRow/AddEntry/EnsureSolidTile/Save 헬퍼 패턴 전체 — 아래 action의 기반)
    - Assets/Scripts/World/RoomEntry.cs (컴포넌트 계약 확인 — 필드 없음, AddComponent만 하면 됨)
  </read_first>
  <action>
    신규 파일 `Assets/Editor/RoomBossFsmTestBuilder.cs` 생성:

    ```csharp
    using UnityEditor;
    using UnityEngine;
    using UnityEngine.Tilemaps;

    /// <summary>
    /// Menu: Fast/Phase15/Build Room_BossFsmTest
    /// D-11 (RE-RESOLVED 2026-07-15): Room_Debug.prefab은 Phase 16 레거시 정리에서 삭제될 예정이므로,
    /// 보스 FSM 격리 테스트 전용 목적지 룸을 Room_Debug와 완전히 독립된 신규 프리팹으로 생성한다.
    /// RoomEntry 마커 하나 + 평평한 바닥(Tilemap)만 포함 — 기존 적 스폰 마커/기믹은 전혀 없음(D-11 명시).
    /// Assets/Editor/RoomCreator.cs의 NewRoom/PRow/AddEntry/EnsureSolidTile 컨벤션을 그대로 따른다
    /// (RoomCreator의 헬퍼는 private static이라 직접 호출 불가 — 동일 패턴을 이 파일 안에 인라인으로 재작성).
    /// </summary>
    public static class RoomBossFsmTestBuilder
    {
        private const string RoomName      = "Room_BossFsmTest";
        private const string PrefabPath    = "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab";
        private const string SolidTilePath = "Assets/Tiles/Tile_Solid.asset"; // RoomCreator.cs가 이미 생성해둔 공유 자산 — 재생성 금지

        [MenuItem("Fast/Phase15/Build Room_BossFsmTest")]
        public static void Build()
        {
            var tile = AssetDatabase.LoadAssetAtPath<Tile>(SolidTilePath);
            if (tile == null)
            {
                Debug.LogError($"[RoomBossFsmTestBuilder] {SolidTilePath} not found — Fast/RoomCreator/Build All Complex Rooms를 먼저 실행해 공유 타일 자산을 생성하세요.");
                return;
            }

            var root = new GameObject(RoomName);

            var gridGO = new GameObject("Grid");
            gridGO.transform.SetParent(root.transform, false);
            gridGO.AddComponent<Grid>().cellSize = Vector3.one;

            var solidGO = new GameObject("Tilemap_Solid");
            solidGO.transform.SetParent(gridGO.transform, false);
            solidGO.layer = 9; // Platform
            var tilemap = solidGO.AddComponent<Tilemap>();
            solidGO.AddComponent<TilemapRenderer>();
            solidGO.AddComponent<TilemapCollider2D>();

            for (int x = -14; x <= 14; x++) // 평평한 바닥 29타일 (D-11: 바닥만, 벽/장애물 없음)
                tilemap.SetTile(new Vector3Int(x, 0, 0), tile);

            var entryGO = new GameObject("RoomEntry");
            entryGO.transform.SetParent(root.transform);
            entryGO.transform.localPosition = new Vector3(0f, 1f, 0f);
            entryGO.AddComponent<RoomEntry>(); // D-11: 유일한 마커 — EnemySpawner/RoomConnector/KillZone 등 일절 없음

            string dir = "Assets/Prefabs/Rooms/Room_BossFsmTest";
            if (!AssetDatabase.IsValidFolder(dir))
                AssetDatabase.CreateFolder("Assets/Prefabs/Rooms", "Room_BossFsmTest");

            if (AssetDatabase.LoadAssetAtPath<GameObject>(PrefabPath) != null)
                AssetDatabase.DeleteAsset(PrefabPath); // 멱등적 재실행 — 기존 자산 덮어쓰기

            PrefabUtility.SaveAsPrefabAsset(root, PrefabPath);
            Object.DestroyImmediate(root);

            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();
            Debug.Log($"[RoomBossFsmTestBuilder] Created {PrefabPath} — RoomEntry(0,1,0) + flat floor(x:-14~14, y:0) only.");
        }
    }
    ```

    이 태스크는 코드만 작성한다 — 메뉴 실행(Unity 에디터 조작)은 이 플랜의 Task 3(checkpoint:human-action)에서 수행한다. `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab`은 이 태스크 시점에는 아직 존재하지 않는다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase15/Build Room_BossFsmTest\")" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "AddComponent<RoomEntry>" Assets/Editor/RoomBossFsmTestBuilder.cs && grep -n "SetTile(new Vector3Int(x, 0, 0), tile)" Assets/Editor/RoomBossFsmTestBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/RoomBossFsmTestBuilder.cs`가 존재하고 `[MenuItem("Fast/Phase15/Build Room_BossFsmTest")]`를 포함한다
    - `Build()`가 `EnemySpawner`/`RoomConnector`/`FallZoneTrigger`/`KillZone` 등 어떤 적/기믹 관련 컴포넌트도 추가하지 않는다 — `AddComponent<RoomEntry>()` 단 한 번만 호출된다
    - 바닥은 `Tilemap_Solid`에 `x:-14~14, y:0` 범위로만 채워진다(벽/천장/사다리 없음)
    - 공유 타일 자산(`Assets/Tiles/Tile_Solid.asset`)은 `LoadAssetAtPath`로만 로드되고 재생성되지 않는다
    - 아직 Unity 메뉴가 실행되지 않았으므로 `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab` 파일은 이 태스크 시점에 존재하지 않는다
  </acceptance_criteria>
  <done>Room_BossFsmTest.prefab 생성을 위한 멱등적 에디터 도구가 코드로 존재 — 실행은 Task 3에서 수행.</done>
</task>

<task type="auto">
  <name>Task 2: BossEnemyPrefabBuilder.cs — 배선 대상을 Room_BossFsmTest로 재배선 (D-11 RE-RESOLVED)</name>
  <files>Assets/Editor/BossEnemyPrefabBuilder.cs</files>
  <read_first>
    - Assets/Editor/BossEnemyPrefabBuilder.cs (현재 전체 — `RoomDebugPath` 상수, `WireBossIntoRoomDebug()` 전체 로직. `BuildBossEnemyPrefab()`/`SourcePrefabPath`/`TargetPrefabPath`/`BossScale`/`BossTint`는 절대 건드리지 않는다 — BossEnemy.prefab은 이미 디스크에 생성되어 있고 재실행해도 안전해야 한다)
  </read_first>
  <action>
    `Assets/Editor/BossEnemyPrefabBuilder.cs`를 다음과 같이 수정한다. `BuildBossEnemyPrefab()` 메서드와 `SourcePrefabPath`/`TargetPrefabPath`/`BossScale`/`BossTint`는 **한 글자도 변경하지 않는다** — 이미 실행되어 `Assets/Prefabs/Enemies/BossEnemy.prefab`이 존재하는 상태이므로 재실행 시 diff 0이어야 한다.

    1) 클래스 상단 문서 주석의 D-11 문단을 다음으로 교체:
    ```csharp
    /// D-11 (RE-RESOLVED 2026-07-15): 생성된 프리팹을 Room_Debug.prefab 안의 전용 신규 teleporter
    /// 자식 오브젝트("BossFsmTest") 하나에만 배선한다 — 목적지는 (삭제 예정인) Room_Debug 자신이 아니라
    /// 독립 프리팹 Room_BossFsmTest.prefab이다. 기존 14종 레거시 teleporter(Combat/Chase/... )는 건드리지 않는다.
    ```

    2) `private const string RoomDebugPath = "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab";` 줄을 다음 세 줄로 교체:
    ```csharp
    private const string TestHubRoomPath      = "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"; // 재명명 — 이제 배선 "목적지"가 아니라 teleporter 트리거를 담는 "호스트" 룸일 뿐
    private const string BossFsmTestRoomPath  = "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab";
    private const string BossFsmTestTeleporterName = "BossFsmTest";
    ```

    3) 기존 `WireBossIntoRoomDebug()` 메서드 전체(`[MenuItem("Fast/Phase15/Wire Boss Into Room Debug")]`부터 닫는 중괄호까지)를 다음으로 완전히 교체:
    ```csharp
    [MenuItem("Fast/Phase15/Wire Boss Into BossFsmTest Room")]
    public static void WireBossIntoBossFsmTestRoom()
    {
        var bossPrefab = AssetDatabase.LoadAssetAtPath<GameObject>(TargetPrefabPath);
        if (bossPrefab == null)
        {
            Debug.LogError($"[BossEnemyPrefabBuilder] {TargetPrefabPath} not found — run 'Build BossEnemy Prefab' first.");
            return;
        }

        var testRoomPrefab = AssetDatabase.LoadAssetAtPath<GameObject>(BossFsmTestRoomPath);
        if (testRoomPrefab == null)
        {
            Debug.LogError($"[BossEnemyPrefabBuilder] {BossFsmTestRoomPath} not found — run 'Fast/Phase15/Build Room_BossFsmTest' first.");
            return;
        }

        var root = PrefabUtility.LoadPrefabContents(TestHubRoomPath);
        if (root == null)
        {
            Debug.LogError($"[BossEnemyPrefabBuilder] Prefab not found: {TestHubRoomPath}");
            return;
        }

        // 기존 14종 레거시 teleporter(Combat/Chase/Fall/...)는 전혀 건드리지 않는다 —
        // 전용 신규 teleporter 하나만 find-or-create 한다 (D-11 RE-RESOLVED).
        var existing = root.GetComponentsInChildren<DebugRoomTeleporter>(true);
        DebugRoomTeleporter target = null;
        foreach (var t in existing)
        {
            if (t.gameObject.name == BossFsmTestTeleporterName) { target = t; break; }
        }

        if (target == null)
        {
            if (existing.Length == 0)
            {
                Debug.LogError($"[BossEnemyPrefabBuilder] No existing DebugRoomTeleporter found in {TestHubRoomPath} to clone from.");
                PrefabUtility.UnloadPrefabContents(root);
                return;
            }

            var template = existing[0].gameObject; // 레이아웃/컴포넌트 구조만 복제 — 어떤 레거시 인스턴스든 무방
            var clone = Object.Instantiate(template, template.transform.parent);
            clone.name = BossFsmTestTeleporterName;
            clone.transform.localPosition = template.transform.localPosition + new Vector3(0f, 20f, 0f); // 기존 14종과 겹치지 않는 전용 위치
            target = clone.GetComponent<DebugRoomTeleporter>();
        }

        var so = new SerializedObject(target);
        so.FindProperty("targetRoomPrefab").objectReferenceValue = testRoomPrefab;
        so.FindProperty("_bossPrefab").objectReferenceValue      = bossPrefab;
        so.ApplyModifiedProperties();

        PrefabUtility.SaveAsPrefabAsset(root, TestHubRoomPath);
        PrefabUtility.UnloadPrefabContents(root);

        AssetDatabase.SaveAssets();
        AssetDatabase.Refresh();
        Debug.Log($"[BossEnemyPrefabBuilder] Wired dedicated '{BossFsmTestTeleporterName}' teleporter → targetRoomPrefab={BossFsmTestRoomPath}, _bossPrefab={TargetPrefabPath}. Existing 14 legacy teleporters untouched.");
    }
    ```

    재실행 시 멱등성: `target`을 이름으로 먼저 찾으므로, 두 번째 실행부터는 clone을 새로 만들지 않고 기존 `BossFsmTest` 인스턴스의 필드만 재대입한다(동일 참조 재대입이라 `ApplyModifiedProperties()`가 안전하게 no-op에 가깝게 동작).
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase15/Wire Boss Into BossFsmTest Room\")" Assets/Editor/BossEnemyPrefabBuilder.cs && grep -n "BossFsmTestRoomPath" Assets/Editor/BossEnemyPrefabBuilder.cs && grep -n "BossFsmTestTeleporterName" Assets/Editor/BossEnemyPrefabBuilder.cs && ! grep -n "RoomDebugPath\b" Assets/Editor/BossEnemyPrefabBuilder.cs && ! grep -n "WireBossIntoRoomDebug\b" Assets/Editor/BossEnemyPrefabBuilder.cs && grep -n "AddComponent<BossEnemy>" Assets/Editor/BossEnemyPrefabBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/BossEnemyPrefabBuilder.cs`에 더 이상 `RoomDebugPath` 식별자가 존재하지 않는다(완전히 `TestHubRoomPath`/`BossFsmTestRoomPath`로 대체)
    - `WireBossIntoRoomDebug()`는 더 이상 존재하지 않고 `WireBossIntoBossFsmTestRoom()`으로 대체되었다
    - `WireBossIntoBossFsmTestRoom()`은 `GetComponentsInChildren<DebugRoomTeleporter>(true)`로 찾은 인스턴스 중 이름이 `"BossFsmTest"`인 것 하나만 대상으로 하고, 없으면 기존 인스턴스 중 하나를 복제해 새로 만든다(전체 14개를 순회하며 전부 배선하지 않는다)
    - `BuildBossEnemyPrefab()` 메서드 본문은 이 태스크 전후로 diff 0이다(git diff로 확인 가능)
    - `SourcePrefabPath`/`TargetPrefabPath`/`BossScale`/`BossTint` 상수는 값과 이름 모두 변경되지 않는다
  </acceptance_criteria>
  <done>BossEnemyPrefabBuilder.cs가 Room_Debug 전체가 아닌 전용 BossFsmTest teleporter 하나만 Room_BossFsmTest.prefab에 배선하도록 재작성됨 — 실행은 Task 3에서 수행.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Unity 에디터 — 컴파일 확인 + Room_BossFsmTest 생성 + 전용 teleporter 배선 실행</name>
  <files>Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab, Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab</files>
  <read_first>
    - Assets/Editor/RoomBossFsmTestBuilder.cs (Task 1 산출물 — 실행할 메뉴 경로 확인)
    - Assets/Editor/BossEnemyPrefabBuilder.cs (Task 2 산출물 — 실행할 메뉴 경로와 배선 로직 확인)
  </read_first>
  <action>
    Unity 에디터 내부 조작(메뉴 실행)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 컴파일 에러가 보고되면 Task 1/2 코드의 문법/시그니처 불일치를 수정한 뒤 재확인을 요청한다.
  </action>
  <what-built>RoomBossFsmTestBuilder.cs(Task 1) + BossEnemyPrefabBuilder.cs 재배선 로직(Task 2) — Unity 에디터 안에서의 마지막 조립 단계만 남음. BossEnemy.prefab 자체는 이미 생성 완료되어 있으므로 재실행 불필요(존재 여부만 확인)</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서 프로젝트를 열고 순서대로:
    1. **컴파일 확인**: Console에 컴파일 에러 0건 (경고는 허용)
    2. **BossEnemy.prefab 존재 확인**: `Assets/Prefabs/Enemies/BossEnemy.prefab`이 이미 존재하는지 Project 창에서 확인 — 존재하면 재실행 불필요. 만약 없다면 메뉴 Fast → Phase15 → Build BossEnemy Prefab을 먼저 실행
    3. **Room_BossFsmTest 생성**: 메뉴 Fast → Phase15 → Build Room_BossFsmTest 실행 → Console에 "[RoomBossFsmTestBuilder] Created Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab ..." 로그 확인, 에러 0건
    4. **프리팹 확인**: `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab` 선택 → Hierarchy 미리보기에서 `Grid/Tilemap_Solid`(평평한 바닥)와 `RoomEntry` 오브젝트만 존재하고 다른 적/기믹 오브젝트가 없음을 확인
    5. **전용 teleporter 배선**: 메뉴 Fast → Phase15 → Wire Boss Into BossFsmTest Room 실행 → Console에 "[BossEnemyPrefabBuilder] Wired dedicated 'BossFsmTest' teleporter → targetRoomPrefab=... _bossPrefab=... Existing 14 legacy teleporters untouched." 로그 확인
    6. **레거시 무변경 확인**: `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`을 열어 Combat/Chase/Fall/Stair/Recover/Sniper/Cross/Chain/Hunt/Ladder/Dodge/Gap/Mixed/LadderDanger 중 아무거나 하나 선택 → `targetRoomPrefab`이 여전히 원래의 구형 Room_*.prefab을 가리키고 `_bossPrefab`은 비어있음을 확인 (전용 teleporter만 배선됨)
    7. **재실행 안전성 확인**: 5번 메뉴를 한 번 더 실행 — 새 teleporter가 중복 생성되지 않고 기존 "BossFsmTest" 오브젝트의 필드만 재대입되는지 확인 (Hierarchy에서 "BossFsmTest" 오브젝트가 정확히 1개인지 확인)
  </how-to-verify>
  <verify>
    <automated>test -f "Assets/Prefabs/Enemies/BossEnemy.prefab" && test -f "Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab" && grep -c "_bossPrefab:" "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"</automated>
  </verify>
  <acceptance_criteria>
    - Console 컴파일 에러 0건 (사용자 확인)
    - `Assets/Prefabs/Rooms/Room_BossFsmTest/Room_BossFsmTest.prefab` 파일이 존재한다
    - `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`에서 `grep -c "_bossPrefab:"`가 정확히 1을 반환한다 (전용 teleporter 1개만 배선 — 14개 전체가 아님)
    - Room_Debug.prefab 안 "BossFsmTest"라는 이름의 GameObject가 정확히 1개 존재한다 (사용자 확인 — Hierarchy)
    - 기존 14종 레거시 teleporter의 targetRoomPrefab은 이 태스크 전후로 변경되지 않았다 (사용자 확인)
  </acceptance_criteria>
  <done>Room_BossFsmTest.prefab이 실제 디스크 자산으로 존재하고, Room_Debug.prefab 안 전용 teleporter 1개만 그 프리팹+BossEnemy.prefab으로 배선 완료 — D-11 RE-RESOLVED 격리 테스트 환경이 실제로 동작 가능한 상태.</done>
  <resume-signal>"approved" 입력 또는 문제 항목 설명 (예: "컴파일 에러 2건" 또는 "BossFsmTest 오브젝트가 2개 생성됨")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 플레이테스트 — Room_BossFsmTest 안에서 BOSS-03/04/05/06 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 15 Success Criteria 1-5
    - .planning/phases/15-fsm/15-CONTEXT.md §Implementation Decisions (D-01~D-12)
  </read_first>
  <action>
    빈틈 타겟팅 타이밍, 7회 처치 임계값, 점수 자기 상쇄는 인간 시각 판정만 가능(15-RESEARCH.md Validation Architecture, 자동화 테스트 프레임워크 없음) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 원인 파일(BossEnemy.cs 튜닝값 등)을 특정해 후속 수정 여부를 사용자와 논의한다. 이 체크리스트는 15-04-PLAN.md Task 2와 동일하되, 진입 경로만 Room_Debug의 신규 "BossFsmTest" teleporter → Room_BossFsmTest로 바뀐다.
  </action>
  <what-built>보스 FSM 전체(15-01/15-02: 예고→공격→빈틈 패턴 루프, 레이스 안전 피격 등록, D-12 점수 자기 상쇄, D-08 처치 연출 연장) + Room_BossFsmTest(RoomEntry+평평한 바닥, 다른 적/기믹 없음)를 통한 격리 스폰 테스트 환경</what-built>
  <how-to-verify>
    에디터 Play 후, Room_Debug의 "BossFsmTest" teleporter(위쪽 화살표 UpArrow)로 Room_BossFsmTest에 진입한 뒤 체크리스트:

    **[BOSS-03 — 빈틈 타겟팅]**
    1. 보스가 예고(이동+근접 공격) 중일 때 공격 버튼을 눌러 슬로우모션으로 조준 — 보스가 빨간 하이라이트/타겟 후보로 잡히지 않는다
    2. 보스가 빈틈(정지+노란색 변화) 상태로 전환되는 순간, 같은 방식으로 조준하면 보스가 빨간 하이라이트로 잡히고 돌진 대상이 된다
    3. 이 Telegraph→Attack→Vulnerable 루프가 최소 5회 반복되는 동안 위 1/2번 패턴이 일관되게 유지된다

    **[BOSS-04 — 7회 처치 + 패턴 리셋]**
    4. 빈틈 상태에서 돌진 공격을 성공시킨다 — 보스가 색상 플래시+넉백 반응을 보이고, 짧은 공백 후 패턴이 예고(Telegraph)부터 다시 시작된다
    5. 이 과정을 1~6번째 히트까지 반복 — 매번 보스가 생존하고 패턴이 리셋된다 (Destroy되지 않는다)
    6. 7번째 히트를 성공시킨다 — 보스가 사망 연출(Die 애니메이션 → 파티클 → SpriteMask 페이드 → 카메라 쉐이크)을 재생하고 최종적으로 Destroy된다

    **[BOSS-05 — 진행률 비노출]**
    7. 1~7번째 히트 전 과정 동안 화면 어디에도 피격 횟수/진행률을 나타내는 UI 텍스트가 나타나지 않는다

    **[BOSS-06 — 점수 보너스 (D-12 포함)]**
    8. HUD 점수 표시를 보면서 1~6번째 히트 각각의 직후 점수가 변하지 않는지 확인한다 (+100이 잠깐 보였다가 사라지는 것도 실패 — 순간적으로도 변하면 안 됨)
    9. 7번째 히트(처치) 직후 점수가 한 번에 크게 상승한다 (일반 적 처치(+100)보다 눈에 띄게 큰 폭)

    **[일반 안정성 + 격리 환경 확인]**
    10. 전체 7회 히트 사이클 동안 콘솔 에러 0건, 보스가 벽에 끼거나 화면 밖으로 사라지는 등의 비정상 동작 없음
    11. Room_BossFsmTest 안에는 보스 외에 다른 적(근접/원거리)이 전혀 등장하지 않는다 — 순수 격리 전투 확인 (D-11 RE-RESOLVED 요구사항)
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 빈틈 타겟팅/7회 처치/점수 자기 상쇄는 시각 판정만 가능 (15-RESEARCH.md Validation Architecture 정당화, 이 프로젝트에 자동화 테스트 프레임워크 없음). 통과 기준: 사용자 리포트에서 체크리스트 1~11 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~11번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(몇 번째 히트, 어떤 동작이 예상과 달랐는지)이 함께 기록된다
  </acceptance_criteria>
  <done>Phase 15 성공 기준 5개(BOSS-03, BOSS-04, BOSS-05, BOSS-06, D-12 포함)가 Room_BossFsmTest(Room_Debug와 독립된 격리 환경)에서 플레이테스트로 통과 확인.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "8번: 3번째 히트 후 점수가 잠깐 +100 올라갔다가 내려감")</resume-signal>
</task>

</tasks>

<verification>
- `Assets/Editor/RoomBossFsmTestBuilder.cs`, `Assets/Editor/BossEnemyPrefabBuilder.cs` 컴파일 성공
- `Assets/Scripts/Enemy/BossEnemy.cs`, `Assets/Scripts/Enemy/EnemyDeathEffect.cs`, `Assets/Scripts/World/ScoreManager.cs`, `Assets/Scripts/World/DebugRoomTeleporter.cs`의 git diff 0줄 (이 플랜은 리와이어링 전용 — FSM/점수/사망연출 코드는 15-01/15-02/15-03에서 이미 완료, 재변경 없음)
- Room_Debug.prefab 내 레거시 14종 teleporter의 targetRoomPrefab/offsetX/offsetY/_meleePrefab/_rangedPrefab이 이 플랜 실행 전후로 diff 0
- Phase 15 ROADMAP Success Criteria 1-5 전항목 Room_BossFsmTest 안에서 플레이테스트 통과
</verification>

<success_criteria>
- Room_BossFsmTest.prefab이 Room_Debug와 완전히 독립된 자산으로 존재하며 Phase 16의 향후 Room_Debug 삭제에 영향받지 않는다
- BOSS-03, BOSS-04, BOSS-05, BOSS-06이 Room_BossFsmTest 안에서 실제 플레이로 검증됨
- D-12(비치명타 점수 자기 상쇄)가 시각적으로 확인됨 — 점수 HUD가 1~6회 히트 동안 전혀 흔들리지 않음
- 15-04-PLAN.md는 SUPERSEDED 상태로 남고, 이 플랜(15-05)이 Phase 15의 최종 실행 플랜이다
- `/gsd:verify-work 15` 진입 가능 상태
</success_criteria>

<output>
After completion, create `.planning/phases/15-fsm/15-05-SUMMARY.md` (플레이테스트 체크리스트 결과 + 발견된 이슈 + 대응 여부 + 15-04 대체 확정 포함)
</output>
