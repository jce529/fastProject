---
phase: 15-fsm
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - Assets/Prefabs/Enemies/BossEnemy.prefab
  - Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab
autonomous: false
requirements: [BOSS-03, BOSS-04, BOSS-05, BOSS-06]

must_haves:
  truths:
    - "보스는 예고→빈틈 루프를 반복하며, 빈틈 상태가 아닐 때는 플레이어의 돌진 공격 대상으로 선택되지 않는다 (BOSS-03, ROADMAP SC1)"
    - "빈틈 상태에서 플레이어가 돌진 공격을 성공시키면 보스가 즉시 피격 판정을 받고, 공격 패턴이 처음부터 다시 시작된다 (BOSS-03/04, ROADMAP SC2)"
    - "보스는 정확히 7회 피격 시 처치(사망 연출 재생 + Destroy)되며, 6회까지는 처치되지 않는다 (BOSS-04, ROADMAP SC3)"
    - "보스 처치 진행률(피격 횟수)을 나타내는 UI/텍스트 요소가 화면 어디에도 존재하지 않는다 (BOSS-05, ROADMAP SC4)"
    - "보스 처치 시 ScoreManager에 일반 적보다 큰 점수 보너스가 즉시 누적되고, 6회의 비치명타는 순 점수 변화가 0이다 (BOSS-06, ROADMAP SC5, D-12)"
  artifacts:
    - path: "Assets/Prefabs/Enemies/BossEnemy.prefab"
      provides: "실제 스폰 가능한 보스 프리팹 자산 (BossEnemy 컴포넌트 부착, 1.6배 확대, 붉은색 색조)"
      contains: "BossEnemy"
    - path: "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"
      provides: "모든 DebugRoomTeleporter 인스턴스에 _bossPrefab 배선 완료"
      contains: "_bossPrefab"
  key_links:
    - from: "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"
      to: "Assets/Prefabs/Enemies/BossEnemy.prefab"
      via: "DebugRoomTeleporter._bossPrefab 직렬화 참조"
      pattern: "_bossPrefab:"
---

<objective>
15-03에서 작성한 두 에디터 도구(`Fast/Phase15/Build BossEnemy Prefab`, `Fast/Phase15/Wire Boss Into Room Debug`)를 Unity 에디터에서 실제로 실행해 `BossEnemy.prefab` 자산을 생성하고 `Room_Debug.prefab`에 배선한 뒤, Phase 15 전체(15-01~15-03)의 코드가 실제 플레이에서 BOSS-03/04/05/06을 만족하는지 플레이테스트로 검증한다.

Purpose: 이 Phase의 최종 통합·검수 게이트. 빈틈 타겟팅 타이밍, 7회 처치 임계값, 점수 자기 상쇄(D-12)는 인간 시각 판정이 유일한 검증 수단 (15-VALIDATION.md — 이 프로젝트에 자동화 테스트 프레임워크 없음, Phase 13/14와 동일한 manual-only 컨벤션).
Output: BossEnemy.prefab(신규 자산), Room_Debug.prefab(배선 완료), Phase 15 성공 기준 5개 통과 기록.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fsm/15-CONTEXT.md
@.planning/phases/15-fsm/15-VALIDATION.md
@Assets/Editor/BossEnemyPrefabBuilder.cs
@Assets/Scripts/Enemy/BossEnemy.cs
@Assets/Scripts/World/DebugRoomTeleporter.cs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Unity 에디터 — 컴파일 확인 + BossEnemy 프리팹 생성/배선 도구 실행</name>
  <files>Assets/Prefabs/Enemies/BossEnemy.prefab, Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab</files>
  <read_first>
    - Assets/Editor/BossEnemyPrefabBuilder.cs (실행할 두 메뉴 경로와 대상 프리팹 확인)
  </read_first>
  <action>
    Unity 에디터 내부 조작(메뉴 실행)은 자동화 불가 — 사용자에게 아래 how-to-verify 절차를 안내하고 결과를 대기한다. 컴파일 에러가 보고되면 15-01/15-02/15-03에서 작성한 코드의 문법/시그니처 불일치를 수정한 뒤 재확인을 요청한다.
  </action>
  <what-built>ScoreManager/EnemyDeathEffect 확장(15-01), BossEnemy.cs FSM 전체(15-02), BossEnemyPrefabBuilder+DebugRoomTeleporter 배선 코드(15-03) — Unity 에디터 안에서의 마지막 조립 단계만 남음</what-built>
  <how-to-verify>
    Unity 에디터(6000.3.11f1)에서 프로젝트를 열고 순서대로:
    1. **컴파일 확인**: Console에 컴파일 에러 0건 (경고는 허용)
    2. **BossEnemy 프리팹 생성**: 메뉴 Fast → Phase15 → Build BossEnemy Prefab 실행 → Console에 "[BossEnemyPrefabBuilder] Created Assets/Prefabs/Enemies/BossEnemy.prefab ..." 로그 확인, 에러 0건
    3. **프리팹 확인**: `Assets/Prefabs/Enemies/BossEnemy.prefab` 선택 → Inspector에서 `BossEnemy` 컴포넌트가 부착되어 있고 `MeleeEnemy` 컴포넌트는 없음을 확인, Scene 뷰 미리보기에서 원본 대비 확대+붉은 색조 확인
    4. **Room_Debug 배선**: 메뉴 Fast → Phase15 → Wire Boss Into Room Debug 실행 → Console에 "[BossEnemyPrefabBuilder] Wired _bossPrefab into N DebugRoomTeleporter instance(s) ..." 로그 확인 (N > 0)
    5. **재실행 안전성 확인**: 두 메뉴를 각각 한 번 더 실행 — 중복 프리팹/중복 배선 없이 동일하게 동작하는지 확인 (멱등성)
  </how-to-verify>
  <verify>
    <automated>grep -l "BossEnemy" "Assets/Prefabs/Enemies/BossEnemy.prefab" && grep -l "_bossPrefab" "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"</automated>
  </verify>
  <acceptance_criteria>
    - Console 컴파일 에러 0건 (사용자 확인)
    - `Assets/Prefabs/Enemies/BossEnemy.prefab` 파일이 존재하고 `grep -l "BossEnemy"`가 매치를 반환한다
    - `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab`에서 `grep -c "_bossPrefab:"`가 1 이상을 반환한다 (최소 1개 인스턴스 배선)
    - 메뉴 재실행 시 경고/에러 없이 멱등적으로 동작한다 (사용자 확인)
  </acceptance_criteria>
  <done>BossEnemy.prefab이 실제 디스크 자산으로 존재하고 Room_Debug.prefab의 DebugRoomTeleporter 인스턴스에 배선 완료 — D-11 격리 테스트 환경이 실제로 동작 가능한 상태.</done>
  <resume-signal>"approved" 입력 또는 문제 항목 설명 (예: "컴파일 에러 2건" 또는 "ExclamationIcon/MeleeHitbox not found 에러 발생")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 플레이테스트 — BOSS-03/04/05/06 + Phase 15 성공 기준 5개 검증</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 15 Success Criteria 1-5
    - .planning/phases/15-fsm/15-CONTEXT.md §Implementation Decisions (D-01~D-12)
    - .planning/phases/15-fsm/15-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    빈틈 타겟팅 타이밍, 7회 처치 임계값, 점수 자기 상쇄는 인간 시각 판정만 가능(15-VALIDATION.md, 자동화 테스트 프레임워크 없음) — 사용자에게 아래 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 원인 파일(BossEnemy.cs 튜닝값 등)을 특정해 후속 수정 여부를 사용자와 논의한다.
  </action>
  <what-built>보스 FSM 전체(예고→공격→빈틈 패턴 루프, 레이스 안전 피격 등록, D-12 점수 자기 상쇄, D-08 처치 연출 연장) + Room_Debug를 통한 즉시 스폰 테스트 환경</what-built>
  <how-to-verify>
    에디터 Play 후, Room_Debug의 위쪽 화살표(UpArrow) 텔레포터로 보스가 배선된 방에 진입한 뒤 체크리스트:

    **[BOSS-03 — 빈틈 타겟팅]**
    1. 보스가 예고(이동+근접 공격) 중일 때 공격 버튼을 눌러 슬로우모션으로 조준 — 보스가 빨간 하이라이트/타겟 후보로 잡히지 않는다
    2. 보스가 빈틈(정지+노란색 변화) 상태로 전환되는 순간, 같은 방식으로 조준하면 보스가 빨간 하이라이트로 잡히고 돌진 대상이 된다
    3. 이 Telegraph→Attack→Vulnerable 루프가 최소 5회 반복되는 동안 위 1/2번 패턴이 일관되게 유지된다

    **[BOSS-04 — 7회 처치 + 패턴 리셋]**
    4. 빈틈 상태에서 돌진 공격을 성공시킨다 — 보스가 색상 플래시+넉백 반응을 보이고, 짧은 공백 후 패턴이 예고(Telegraph)부터 다시 시작된다
    5. 이 과정을 1~6번째 히트까지 반복 — 매번 보스가 생존하고 패턴이 리셋된다 (Destroy되지 않는다)
    6. 7번째 히트를 성공시킨다 — 보스가 사망 연출(Die 애니메이션 → 파티클 → SpriteMask 페이드 → 카메라 쉐이크)을 재생하고 최종적으로 Destroy된다

    **[BOSS-05 — 진행률 비노출]**
    7. 1~7번째 히트 전 과정 동안 화면 어디에도 피격 횟수/진행률을 나타내는 UI 텍스트가 나타나지 않는다

    **[BOSS-06 — 점수 보너스 (D-12 포함)]**
    8. HUD 점수 표시를 보면서 1~6번째 히트 각각의 직후 점수가 변하지 않는지 확인한다 (+100이 잠깐 보였다가 사라지는 것도 실패 — 순간적으로도 변하면 안 됨)
    9. 7번째 히트(처치) 직후 점수가 한 번에 크게 상승한다 (일반 적 처치(+100)보다 눈에 띄게 큰 폭)

    **[일반 안정성]**
    10. 전체 7회 히트 사이클 동안 콘솔 에러 0건, 보스가 벽에 끼거나 화면 밖으로 사라지는 등의 비정상 동작 없음
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 빈틈 타겟팅/7회 처치/점수 자기 상쇄는 시각 판정만 가능 (15-VALIDATION.md 정당화, 이 프로젝트에 자동화 테스트 프레임워크 없음). 통과 기준: 사용자 리포트에서 체크리스트 1~10 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~10번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 구체적 증상(몇 번째 히트, 어떤 동작이 예상과 달랐는지)이 함께 기록된다
  </acceptance_criteria>
  <done>Phase 15 성공 기준 5개(BOSS-03, BOSS-04, BOSS-05, BOSS-06, D-12 포함) 전부 플레이테스트로 통과 확인.</done>
  <resume-signal>체크리스트 결과 보고 — 전부 통과면 "approved", 문제가 있으면 번호별로 기술 (예: "8번: 3번째 히트 후 점수가 잠깐 +100 올라갔다가 내려감")</resume-signal>
</task>

</tasks>

<verification>
- Phase 15 ROADMAP Success Criteria 1-5 전항목 플레이테스트 통과
- `Assets/Prefabs/Enemies/BossEnemy.prefab` 존재 + `Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab` 배선 확인 (grep 확인)
- Console 에러 0건 (전체 7회 히트 사이클 기준)
</verification>

<success_criteria>
- BOSS-03, BOSS-04, BOSS-05, BOSS-06이 실제 플레이로 검증됨
- D-12(비치명타 점수 자기 상쇄)가 시각적으로 확인됨 — 점수 HUD가 1~6회 히트 동안 전혀 흔들리지 않음
- `/gsd:verify-work 15` 진입 가능 상태
</success_criteria>

<output>
After completion, create `.planning/phases/15-fsm/15-04-SUMMARY.md` (플레이테스트 체크리스트 결과 + 발견된 이슈 + 대응 여부 포함)
</output>
