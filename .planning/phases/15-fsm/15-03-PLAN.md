---
phase: 15-fsm
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - Assets/Editor/BossEnemyPrefabBuilder.cs
  - Assets/Scripts/World/DebugRoomTeleporter.cs
autonomous: true
requirements: [BOSS-03, BOSS-04, BOSS-05, BOSS-06]

must_haves:
  truths:
    - "BossEnemyPrefabBuilder의 'Build BossEnemy Prefab' 메뉴를 실행하면 MeleeEnemy.prefab의 구조(Rigidbody2D/CapsuleCollider2D/SpriteRenderer/Animator/ExclamationIcon/MeleeHitbox)를 복제하되 MeleeEnemy 컴포넌트 대신 BossEnemy 컴포넌트가 부착된 Assets/Prefabs/Enemies/BossEnemy.prefab이 생성된다 (D-10, D-11)"
    - "생성된 BossEnemy.prefab은 원본 대비 1.6배 확대된 localScale과 진한 붉은색(0.7, 0.15, 0.2) SpriteRenderer.color를 가진다 — 신규 아트 없이 시각적으로 구분된다 (D-10)"
    - "BossEnemyPrefabBuilder의 'Wire Boss Into Room Debug' 메뉴를 실행하면 Room_Debug.prefab 내 모든 DebugRoomTeleporter 인스턴스의 _bossPrefab 필드가 BossEnemy.prefab을 참조하도록 배선된다 (D-11)"
    - "DebugRoomTeleporter.TeleportToRoom()은 _bossPrefab이 할당돼 있으면 새로 생성된 룸의 진입 지점 근처에 보스를 직접 Instantiate한다 — EnemySpawner를 거치지 않는다 (D-11, RESEARCH.md Open Question 2 권장안)"
    - "Assets/Scripts/Enemy/MeleeEnemy.cs는 이 플랜에서 한 줄도 변경되지 않는다 (정밀한 변경 — 원본 프리팹 소스로만 참조됨)"
  artifacts:
    - path: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      provides: "BossEnemy.prefab 생성 + Room_Debug.prefab 배선 에디터 도구 2종"
      contains: "MenuItem(\"Fast/Phase15/Build BossEnemy Prefab\")"
    - path: "Assets/Scripts/World/DebugRoomTeleporter.cs"
      provides: "_bossPrefab 필드 + TeleportToRoom() 보스 Instantiate 배선"
      contains: "_bossPrefab"
  key_links:
    - from: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      to: "Assets/Scripts/Enemy/BossEnemy.cs (15-02 산출물)"
      via: "PrefabUtility.LoadPrefabContents로 복제한 GameObject에 AddComponent<BossEnemy>()"
      pattern: "AddComponent<BossEnemy>"
    - from: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      to: "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab"
      via: "SerializedObject로 모든 DebugRoomTeleporter 인스턴스의 _bossPrefab 프로퍼티 배선"
      pattern: "FindProperty\\(\"_bossPrefab\"\\)"
    - from: "Assets/Scripts/World/DebugRoomTeleporter.cs"
      to: "Assets/Prefabs/Enemies/BossEnemy.prefab"
      via: "TeleportToRoom() 내 Instantiate(_bossPrefab, ...)"
      pattern: "Instantiate\\(_bossPrefab"
---

<objective>
D-11이 요구하는 격리 테스트 환경을 완성한다: (1) 신규 아트 없이 `MeleeEnemy.prefab` 구조를 복제해 크기/색조만 변형한 `BossEnemy.prefab`을 생성하는 에디터 도구, (2) 그 프리팹을 `Room_Debug.prefab`의 모든 `DebugRoomTeleporter` 인스턴스에 배선하는 에디터 도구, (3) `DebugRoomTeleporter.TeleportToRoom()`이 실제로 보스를 스폰하도록 하는 코드 변경. 이 플랜은 코드만 작성한다 — 메뉴 실행(Unity 에디터 조작)은 15-04에서 수행한다.

Purpose: 15-02의 BossEnemy FSM을 실제 Play 모드에서 눈으로 검증할 수 있는 유일한 경로를 만든다 — Phase 16/17의 보스 룸 콘텐츠나 WorldGenerator 통합 없이도 즉시 스폰/테스트 가능.
Output: Assets/Editor/BossEnemyPrefabBuilder.cs(신규), Assets/Scripts/World/DebugRoomTeleporter.cs(+1 필드, +1 블록). 프리팹 자산 생성/배선 자체는 15-04에서 실행.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fsm/15-CONTEXT.md
@.planning/phases/15-fsm/15-RESEARCH.md
@Assets/Editor/ExclamationIconBuilder.cs
@Assets/Editor/CorridorEnemySpawnerTool.cs
@Assets/Scripts/World/DebugRoomTeleporter.cs
@Assets/Prefabs/Enemies/MeleeEnemy.prefab

<interfaces>
<!-- 15-02 산출물 — 탐색 불필요, BossEnemyPrefabBuilder가 그대로 참조한다 -->

From Assets/Scripts/Enemy/BossEnemy.cs (15-02 산출물, SerializeField 이름 정확히 일치):
```csharp
public class BossEnemy : MonoBehaviour, IEnemy, ISpawnGatable
{
    [SerializeField] private SpriteRenderer _exclamationIcon; // MeleeEnemy.prefab의 "ExclamationIcon" 자식에서 그대로 재사용
    [SerializeField] private Collider2D     _meleeHitbox;      // MeleeEnemy.prefab의 "MeleeHitbox" 자식에서 그대로 재사용
    public void SetSpawnGate(bool isSpawning); // ISpawnGatable
}
```

MeleeEnemy.prefab 자식 계층 (Assets/Prefabs/Enemies/MeleeEnemy.prefab 실제 확인, BossEnemyPrefabBuilder가 Find()로 참조):
```
MeleeEnemy (root)               — Rigidbody2D, CapsuleCollider2D, SpriteRenderer, Animator, MeleeEnemy(제거 대상)
├── ExclamationIcon             — SpriteRenderer (m_Color: 노란색, "!" 스프라이트)
└── MeleeHitbox                 — BoxCollider2D (m_IsTrigger: 1)
```

현재 `Assets/Scripts/World/DebugRoomTeleporter.cs`의 관련 필드/메서드 (변경 대상 확인용, 전체는 read_first로 재확인):
```csharp
[Header("Enemy Prefabs")]
[SerializeField] private GameObject _meleePrefab;
[SerializeField] private GameObject _rangedPrefab;
...
private void TeleportToRoom()
{
    ...
    RoomEntry entry = s_lastDebugRoom.GetComponentInChildren<RoomEntry>(true);
    Vector3 entryPos = entry != null
        ? entry.transform.position
        : s_lastDebugRoom.transform.position + Vector3.up * 2f;

    _player.LockInput();
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BossEnemyPrefabBuilder.cs — BossEnemy.prefab 생성 + Room_Debug 배선 도구 (D-10, D-11)</name>
  <files>Assets/Editor/BossEnemyPrefabBuilder.cs</files>
  <read_first>
    - Assets/Editor/ExclamationIconBuilder.cs (LoadPrefabContents 없이 프리팹 필드만 수정하는 패턴, SaveAsPrefabAsset 컨벤션)
    - Assets/Editor/CorridorEnemySpawnerTool.cs (LoadPrefabContents+AddComponent+SaveAsPrefabAsset+멱등성 체크 패턴)
    - Assets/Prefabs/Enemies/MeleeEnemy.prefab (자식 GameObject 이름 "ExclamationIcon"/"MeleeHitbox" 확인 — 위 interfaces 블록과 동일)
  </read_first>
  <action>
    신규 파일 `Assets/Editor/BossEnemyPrefabBuilder.cs` 생성:

    ```csharp
    using UnityEditor;
    using UnityEngine;

    /// <summary>
    /// Menu: Fast/Phase15/Build BossEnemy Prefab, Fast/Phase15/Wire Boss Into Room Debug
    /// D-10: 신규 아트 없이 MeleeEnemy.prefab 구조(Rigidbody2D/CapsuleCollider2D/SpriteRenderer/Animator/
    /// ExclamationIcon/MeleeHitbox)를 복제하되 MeleeEnemy 컴포넌트를 BossEnemy로 교체하고, 크기 1.6배 확대 +
    /// 진한 붉은색 색조로 "보스처럼 보이게" 변형한다. 기존 MeleeEnemy.prefab은 건드리지 않는다(정밀한 변경).
    /// D-11: 생성된 프리팹을 Room_Debug.prefab의 모든 DebugRoomTeleporter 인스턴스 _bossPrefab 필드에 배선한다.
    /// </summary>
    public static class BossEnemyPrefabBuilder
    {
        private const string SourcePrefabPath = "Assets/Prefabs/Enemies/MeleeEnemy.prefab";
        private const string TargetPrefabPath = "Assets/Prefabs/Enemies/BossEnemy.prefab";
        private const string RoomDebugPath    = "Assets/Prefabs/Rooms/Room_Debug/Room_Debug.prefab";
        private static readonly Vector3 BossScale = new Vector3(1.6f, 1.6f, 1f);   // D-10: 크기 확대
        private static readonly Color   BossTint  = new Color(0.7f, 0.15f, 0.2f);  // D-10: 진한 붉은색 색조

        [MenuItem("Fast/Phase15/Build BossEnemy Prefab")]
        public static void BuildBossEnemyPrefab()
        {
            var source = PrefabUtility.LoadPrefabContents(SourcePrefabPath);
            if (source == null)
            {
                Debug.LogError($"[BossEnemyPrefabBuilder] Source prefab not found: {SourcePrefabPath}");
                return;
            }

            var clone = Object.Instantiate(source);
            clone.name = "BossEnemy";
            PrefabUtility.UnloadPrefabContents(source);

            var oldMelee = clone.GetComponent<MeleeEnemy>();
            if (oldMelee != null) Object.DestroyImmediate(oldMelee);

            var boss = clone.AddComponent<BossEnemy>();

            var exclamation = clone.transform.Find("ExclamationIcon")?.GetComponent<SpriteRenderer>();
            var hitbox      = clone.transform.Find("MeleeHitbox")?.GetComponent<Collider2D>();
            if (exclamation == null || hitbox == null)
            {
                Debug.LogError("[BossEnemyPrefabBuilder] ExclamationIcon/MeleeHitbox child not found on cloned MeleeEnemy structure.");
                Object.DestroyImmediate(clone);
                return;
            }

            var so = new SerializedObject(boss);
            so.FindProperty("_exclamationIcon").objectReferenceValue = exclamation;
            so.FindProperty("_meleeHitbox").objectReferenceValue     = hitbox;
            so.ApplyModifiedProperties();

            clone.transform.localScale = BossScale; // D-10: 크기 확대
            var sr = clone.GetComponent<SpriteRenderer>();
            if (sr != null) sr.color = BossTint;    // D-10: 색조 변형 — BossEnemy.Awake()가 _baseColor로 캡처

            PrefabUtility.SaveAsPrefabAsset(clone, TargetPrefabPath);
            Object.DestroyImmediate(clone);

            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();
            Debug.Log($"[BossEnemyPrefabBuilder] Created {TargetPrefabPath} — scale {BossScale}, tint {BossTint}.");
        }

        [MenuItem("Fast/Phase15/Wire Boss Into Room Debug")]
        public static void WireBossIntoRoomDebug()
        {
            var bossPrefab = AssetDatabase.LoadAssetAtPath<GameObject>(TargetPrefabPath);
            if (bossPrefab == null)
            {
                Debug.LogError($"[BossEnemyPrefabBuilder] {TargetPrefabPath} not found — run 'Build BossEnemy Prefab' first.");
                return;
            }

            var root = PrefabUtility.LoadPrefabContents(RoomDebugPath);
            if (root == null)
            {
                Debug.LogError($"[BossEnemyPrefabBuilder] Prefab not found: {RoomDebugPath}");
                return;
            }

            int wired = 0;
            foreach (var teleporter in root.GetComponentsInChildren<DebugRoomTeleporter>(true))
            {
                var so = new SerializedObject(teleporter);
                var prop = so.FindProperty("_bossPrefab");
                if (prop.objectReferenceValue == bossPrefab) continue; // 멱등성 — 이미 배선됨
                prop.objectReferenceValue = bossPrefab;
                so.ApplyModifiedProperties();
                wired++;
            }

            PrefabUtility.SaveAsPrefabAsset(root, RoomDebugPath);
            PrefabUtility.UnloadPrefabContents(root);

            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();
            Debug.Log($"[BossEnemyPrefabBuilder] Wired _bossPrefab into {wired} DebugRoomTeleporter instance(s) in Room_Debug.prefab.");
        }
    }
    ```

    이 태스크는 코드만 작성한다 — 메뉴 실행(Unity 에디터 조작)은 15-04-PLAN.md의 checkpoint:human-action 태스크에서 수행한다.
  </action>
  <verify>
    <automated>grep -n "MenuItem(\"Fast/Phase15/Build BossEnemy Prefab\")" Assets/Editor/BossEnemyPrefabBuilder.cs && grep -n "MenuItem(\"Fast/Phase15/Wire Boss Into Room Debug\")" Assets/Editor/BossEnemyPrefabBuilder.cs && grep -n "AddComponent<BossEnemy>" Assets/Editor/BossEnemyPrefabBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Editor/BossEnemyPrefabBuilder.cs`가 존재하고 두 개의 `[MenuItem]`("Fast/Phase15/Build BossEnemy Prefab", "Fast/Phase15/Wire Boss Into Room Debug")를 포함한다
    - `BuildBossEnemyPrefab()`이 `clone.GetComponent<MeleeEnemy>()`를 제거한 뒤 `clone.AddComponent<BossEnemy>()`를 호출한다
    - `WireBossIntoRoomDebug()`에 `if (prop.objectReferenceValue == bossPrefab) continue;` 멱등성 체크가 존재한다 (재실행 안전)
    - `clone.transform.localScale = BossScale;`와 `sr.color = BossTint;`가 존재한다 (D-10)
    - 아직 Unity 메뉴가 실행되지 않았으므로 `Assets/Prefabs/Enemies/BossEnemy.prefab` 파일은 이 태스크 시점에 존재하지 않는다
  </acceptance_criteria>
  <done>BossEnemy.prefab 생성 및 Room_Debug 배선을 위한 멱등적 에디터 도구가 코드로 존재 — 실행은 15-04에서 수행.</done>
</task>

<task type="auto">
  <name>Task 2: DebugRoomTeleporter._bossPrefab 필드 + TeleportToRoom() 보스 스폰 배선 (D-11)</name>
  <files>Assets/Scripts/World/DebugRoomTeleporter.cs</files>
  <read_first>
    - Assets/Scripts/World/DebugRoomTeleporter.cs (현재 전체 — `_meleePrefab`/`_rangedPrefab` 필드 패턴 17-19행, `TeleportToRoom()` 73-114행 전체 흐름)
  </read_first>
  <action>
    `Assets/Scripts/World/DebugRoomTeleporter.cs`를 다음과 같이 수정한다.

    1) `[Header("Enemy Prefabs")]` 블록 바로 아래에 신규 Header+필드 추가:
    ```csharp
    [Header("Boss (D-11)")]
    [SerializeField] private GameObject _bossPrefab;
    ```

    2) `TeleportToRoom()` 내부, `Vector3 entryPos = ...` 계산 직후 `_player.LockInput();` 호출 이전에 다음 블록 삽입:
    ```csharp
    if (_bossPrefab != null)
    {
        Vector3 bossSpawnPos = entryPos + Vector3.right * 6f; // D-11: 진입 지점에서 우측 6유닛 — 즉시 시야 확보 (격리 테스트)
        Instantiate(_bossPrefab, bossSpawnPos, Quaternion.identity, s_lastDebugRoom.transform);
    }
    ```
    `s_lastDebugRoom.transform`을 부모로 지정해, 다음 텔레포트 시 `if (s_lastDebugRoom != null) Destroy(s_lastDebugRoom);`에 의해 보스도 함께 정리되도록 한다(기존 EnemySpawner 스폰 개체와 동일한 생명주기).

    보스는 `EnemySpawner`를 거치지 않고 직접 `Instantiate()`되므로 `SetSpawnGate()`가 호출되지 않는다 — `BossEnemy`의 기본 `IsAlive = true` + `Start()`의 `if (IsAlive && ...)` 가드(15-02 산출물)에 의해 패턴 루프가 즉시 시작된다. 이 태스크에서는 `ISpawnGatable` 관련 호출을 추가하지 않는다(RESEARCH.md Open Question 2 권장안 — Phase 16에서 EnemySpawner 경유로 교체 예정).
  </action>
  <verify>
    <automated>grep -n "_bossPrefab" Assets/Scripts/World/DebugRoomTeleporter.cs && grep -n "Instantiate(_bossPrefab" Assets/Scripts/World/DebugRoomTeleporter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `[SerializeField] private GameObject _bossPrefab;`가 존재한다
    - `TeleportToRoom()` 내부에 `if (_bossPrefab != null)` 블록이 존재하고, `Instantiate(_bossPrefab, bossSpawnPos, Quaternion.identity, s_lastDebugRoom.transform);`을 호출한다
    - 이 블록은 `Vector3 entryPos = ...` 대입 이후, `_player.LockInput();` 호출 이전에 위치한다
    - 기존 `_meleePrefab`/`_rangedPrefab` 필드와 `EnemySpawner` 루프 관련 코드는 diff 0이다
  </acceptance_criteria>
  <done>DebugRoomTeleporter가 _bossPrefab 필드를 통해 보스를 직접 스폰할 수 있게 됨 — 기존 Melee/Ranged 스폰 경로는 완전히 그대로 유지.</done>
</task>

</tasks>

<verification>
- `Assets/Editor/BossEnemyPrefabBuilder.cs`, `Assets/Scripts/World/DebugRoomTeleporter.cs` 컴파일 성공
- `Assets/Scripts/Enemy/MeleeEnemy.cs`의 git diff 0줄 (원본 프리팹 소스로만 참조, 코드 변경 없음)
</verification>

<success_criteria>
- 15-04가 즉시 두 메뉴("Build BossEnemy Prefab" → "Wire Boss Into Room Debug")를 순서대로 실행해 실제 프리팹 자산과 씬 배선을 완성할 수 있는 상태
- D-11(격리 테스트 환경)이 코드 레벨에서 완비됨
</success_criteria>

<output>
After completion, create `.planning/phases/15-fsm/15-03-SUMMARY.md`
</output>
