---
phase: 05-procedural-map-infinite-stages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/PlayerController.cs
  - Assets/Scripts/Player/CombatController.cs
  - Assets/Scripts/World/FloorSpawner.cs
  - Assets/Scripts/World/RoomExit.cs
  - ProjectSettings/TagManager.asset
autonomous: true
requirements:
  - FLOOR-01
  - FLOOR-02
  - FLOOR-03
  - FLOOR-04

must_haves:
  truths:
    - "출구 트리거에 플레이어가 닿으면 6단계 전환 시퀀스가 발동된다"
    - "전환 시퀀스 중 플레이어가 이동, 점프, 공격 입력을 할 수 없다"
    - "전환 완료 후 플레이어가 새 층 위에 순간이동되어 있다"
    - "전환 완료 후 이전 층 GameObject가 씬에서 사라진다"
    - "새 층에 스폰된 적들은 전환 4단계 전까지 비활성 상태다"
    - "FloorManager.CurrentFloor가 전환 시마다 정확히 1씩 증가한다"
  artifacts:
    - path: "Assets/Scripts/World/FloorSpawner.cs"
      provides: "층 스폰 및 전환 시퀀스 MonoBehaviour"
      exports: ["FloorSpawner", "AdvanceFloor()", "SpawnRoom()", "FloorTransitionSequence()"]
    - path: "Assets/Scripts/World/RoomExit.cs"
      provides: "출구 Trigger 감지 — RoomExit 자식 오브젝트용"
      exports: ["RoomExit"]
    - path: "Assets/Scripts/Player/PlayerController.cs"
      provides: "입력 잠금 기능 추가"
      contains: "_inputLocked"
  key_links:
    - from: "Assets/Scripts/World/RoomExit.cs"
      to: "FloorSpawner.AdvanceFloor()"
      via: "OnTriggerEnter2D + CompareTag(Player)"
      pattern: "FloorSpawner.Instance.*AdvanceFloor"
    - from: "Assets/Scripts/World/FloorSpawner.cs"
      to: "PlayerController._inputLocked"
      via: "[SerializeField] private PlayerController _player"
      pattern: "_player.LockInput"
    - from: "Assets/Scripts/World/FloorSpawner.cs"
      to: "IEnemy.SetActive(true)"
      via: "GetComponentsInChildren<IEnemy>(true)"
      pattern: "includeInactive.*true"
---

<objective>
FloorSpawner MonoBehaviour, RoomExit 트리거, PlayerController 입력 잠금을 구현하여 6단계 층 전환 시퀀스의 코어를 완성한다.

Purpose: FLOOR-01(Room 프리팹 선택 로직 — SelectNextRoom, SpawnRoom, _roomPool), FLOOR-02(층 전환 시퀀스), FLOOR-03(적 비활성화), FLOOR-04(이전 층 파괴)의 C# 로직을 구현한다. 이 플랜이 완료된 후 플랜 02에서 Room 프리팹을 제작하고 FloorSpawner Inspector를 연결하면 플레이 가능한 상태가 된다.

Output: FloorSpawner.cs, RoomExit.cs (신규), PlayerController.cs + CombatController.cs (수정), EnemySpawnPoint 태그 (TagManager)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-procedural-map-infinite-stages/05-CONTEXT.md
@.planning/phases/05-procedural-map-infinite-stages/05-RESEARCH.md

<interfaces>
<!-- 아래 인터페이스는 실행자가 직접 읽어야 하는 기존 파일의 핵심 시그니처다. -->

From Assets/Scripts/World/FloorManager.cs:
```csharp
public static class FloorManager
{
    public static int CurrentFloor = 1;
}
```

From Assets/Scripts/Player/PlayerController.cs (현재 상태 — 수정 대상):
```csharp
public class PlayerController : MonoBehaviour
{
    private Rigidbody2D _rb;
    private bool _isGrounded;
    private bool _jumpHeld;
    // 현재 _inputLocked 필드 없음 — 이번 플랜에서 추가

    // FixedUpdate (현재):
    private void FixedUpdate()
    {
        CheckGround();
        ApplyMovement();  // 잠금 가드 추가 필요
    }

    // OnJumpPerformed (현재):
    private void OnJumpPerformed(InputAction.CallbackContext ctx)
    {
        if (!_isGrounded) return;  // _inputLocked 체크 추가 필요
        // ...
    }

    public bool IsGrounded => _isGrounded;
    public float MoveSpeed => moveSpeed;
    // InputLocked 프로퍼티 없음 — 이번 플랜에서 추가
}
```

From Assets/Scripts/Player/CombatController.cs (현재 상태 — 수정 대상):
```csharp
public class CombatController : MonoBehaviour
{
    private void Update()
    {
        if (_isBusy) return;
        var input = InputManager.Instance;
        // ... 입력 잠금 체크 없음 — 이번 플랜에서 추가
    }
    // _player (PlayerController) 참조 없음 — [SerializeField] 추가 필요
}
```

From Assets/Scripts/Enemy/IEnemy.cs:
```csharp
public interface IEnemy
{
    bool IsAlive { get; }
    void OnDashHit();
    void ClearHighlight();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayerController 입력 잠금 + CombatController 가드 추가</name>
  <files>Assets/Scripts/Player/PlayerController.cs, Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/PlayerController.cs (수정 대상 — 현재 상태 확인 필수)
    - Assets/Scripts/Player/CombatController.cs (수정 대상 — 현재 상태 확인 필수)
  </read_first>
  <action>
PlayerController.cs에 다음을 추가한다 (기존 코드 변경 없음 — 추가만):

1. `private bool _jumpHeld;` 필드 바로 아래에 다음 필드 추가:
   ```csharp
   private bool _inputLocked;
   ```

2. `public float MoveSpeed => moveSpeed;` 다음 줄에 다음 프로퍼티 추가:
   ```csharp
   /// <summary>FloorSpawner가 층 전환 시퀀스 중 읽는다. CombatController가 Update 진입 차단에 사용.</summary>
   public bool InputLocked => _inputLocked;
   ```

3. 같은 위치(public 프로퍼티 영역) 바로 아래에 다음 메서드 2개 추가:
   ```csharp
   /// <summary>층 전환 시퀀스 1단계 — 이동/점프 입력 잠금, 속도 즉시 0.</summary>
   public void LockInput()
   {
       _inputLocked = true;
       _rb.linearVelocity = Vector2.zero;
   }

   /// <summary>층 전환 시퀀스 6단계 — 입력 잠금 해제.</summary>
   public void UnlockInput() => _inputLocked = false;
   ```

4. `FixedUpdate()` 내부를 다음으로 교체:
   ```csharp
   private void FixedUpdate()
   {
       CheckGround();
       if (!_inputLocked) ApplyMovement();
   }
   ```

5. `OnJumpPerformed()` 내부 첫 줄을 다음으로 교체:
   ```csharp
   private void OnJumpPerformed(InputAction.CallbackContext ctx)
   {
       if (_inputLocked || !_isGrounded) return;
       _rb.linearVelocity = new Vector2(_rb.linearVelocity.x, jumpForce);
       _jumpHeld = true;
   }
   ```

CombatController.cs에 다음을 추가한다:

1. 기존 컴포넌트 레퍼런스 영역(private Rigidbody2D _rb; 등이 있는 부분) 아래에 다음 필드 추가:
   ```csharp
   [SerializeField] private PlayerController _player;
   ```

2. `Update()` 메서드 맨 첫 줄(기존 `if (_isBusy) return;` 바로 앞)에 다음 가드 추가:
   ```csharp
   if (_player != null && _player.InputLocked) return;
   ```
   최종 Update() 시작부:
   ```csharp
   private void Update()
   {
       if (_player != null && _player.InputLocked) return;
       if (_isBusy) return;
       // ... 기존 코드 유지
   ```
  </action>
  <verify>
    <automated>
    grep -n "_inputLocked" "D:/새 폴더/Fast/Assets/Scripts/Player/PlayerController.cs" && grep -n "LockInput\|UnlockInput\|InputLocked" "D:/새 폴더/Fast/Assets/Scripts/Player/PlayerController.cs" && grep -n "InputLocked" "D:/새 폴더/Fast/Assets/Scripts/Player/CombatController.cs" && grep -n "_player" "D:/새 폴더/Fast/Assets/Scripts/Player/CombatController.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - PlayerController.cs에 `private bool _inputLocked;` 필드가 존재한다
    - PlayerController.cs에 `public bool InputLocked => _inputLocked;` 프로퍼티가 존재한다
    - PlayerController.cs에 `public void LockInput()` 메서드가 존재하고 내부에 `_rb.linearVelocity = Vector2.zero;`가 있다
    - PlayerController.cs에 `public void UnlockInput()` 메서드가 존재한다
    - PlayerController.cs의 FixedUpdate에 `if (!_inputLocked) ApplyMovement();` 패턴이 존재한다
    - PlayerController.cs의 OnJumpPerformed에 `if (_inputLocked || !_isGrounded) return;` 줄이 존재한다
    - CombatController.cs에 `[SerializeField] private PlayerController _player;` 필드가 존재한다
    - CombatController.cs의 Update() 내부에 `_player.InputLocked` 참조가 존재한다
  </acceptance_criteria>
  <done>PlayerController가 입력 잠금/해제 API를 제공하고, CombatController가 전환 시퀀스 중 슬로우모션 진입을 차단한다.</done>
</task>

<task type="auto">
  <name>Task 2: EnemySpawnPoint 태그 추가 + FloorSpawner.cs + RoomExit.cs 생성</name>
  <files>ProjectSettings/TagManager.asset, Assets/Scripts/World/FloorSpawner.cs, Assets/Scripts/World/RoomExit.cs</files>
  <read_first>
    - ProjectSettings/TagManager.asset (태그 목록 확인 — EnemySpawnPoint 중복 방지)
    - Assets/Scripts/World/FloorManager.cs (FloorManager.CurrentFloor 필드 확인)
    - Assets/Scripts/Enemy/IEnemy.cs (ActivateEnemies에서 사용하는 인터페이스 확인)
    - Assets/Scripts/Player/PlayerController.cs (Task 1 완료 후 — LockInput/UnlockInput 시그니처 확인)
  </read_first>
  <action>
**Step A: TagManager.asset에 EnemySpawnPoint 태그 추가**

ProjectSettings/TagManager.asset을 열어 `tags:` 배열 끝에 다음 항목을 추가한다:
```yaml
  - EnemySpawnPoint
```
이미 존재한다면 추가하지 않는다. TagManager.asset은 YAML 형식이다.

**Step B: Assets/Scripts/World/RoomExit.cs 생성**

```csharp
using UnityEngine;

/// <summary>
/// D-01 (05-CONTEXT.md): 각 Room 프리팹의 위쪽에 자식 오브젝트로 배치되는 출구 트리거.
/// 플레이어가 Trigger Collider2D에 닿으면 FloorSpawner.AdvanceFloor()를 호출한다.
/// 이중 발동은 FloorSpawner._transitioning 플래그가 차단한다 (Pitfall 1 — RESEARCH.md).
/// </summary>
[RequireComponent(typeof(Collider2D))]
public class RoomExit : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        FloorSpawner.Instance?.AdvanceFloor();
    }
}
```

**Step C: Assets/Scripts/World/FloorSpawner.cs 생성**

```csharp
using System.Collections;
using UnityEngine;

/// <summary>
/// Phase 5 — 층 스폰 및 전환 시퀀스 MonoBehaviour.
/// FloorManager(static data)와 분리된 씬 싱글톤 패턴 (04-01 FloorManager 패턴 유지).
///
/// 요구사항:
///   FLOOR-01: _floor1RoomPrefab(고정) + _roomPool(가중치 랜덤) 으로 층 프리팹 선택
///   FLOOR-02: AdvanceFloor() 호출 → FloorTransitionSequence() 6단계 코루틴
///   FLOOR-03: SpawnRoom() 내 적 Instantiate 직후 SetActive(false); ActivateEnemies()에서 true
///   FLOOR-04: 플레이어 순간이동 후 이전 층 Destroy()
///
/// Inspector 연결 필요:
///   _floor1RoomPrefab — Room_Combat 프리팹 (D-05: 1층 고정)
///   _roomPool         — 2층 이후 랜덤 선택 Room 프리팹 배열 (D-03: 4~5개)
///   _playerTransform  — Player GameObject의 Transform
///   _player           — PlayerController 컴포넌트
///   _meleeEnemyPrefab — MeleeEnemy 프리팹
///   _rangedEnemyPrefab— RangedEnemy 프리팹
///   _roomHeight       — 18 (Claude's discretion — 실제 Room 높이 측정 후 조정)
/// </summary>
public class FloorSpawner : MonoBehaviour
{
    // -- Inspector fields -------------------------------------------------------
    [SerializeField] private GameObject      _floor1RoomPrefab;   // D-05: 1층 고정 Room
    [SerializeField] private GameObject[]    _roomPool;            // D-03: 2층+ 랜덤 풀 (4~5개)
    [SerializeField] private Transform       _playerTransform;
    [SerializeField] private PlayerController _player;             // Pitfall 7: Inspector 연결, static 아님
    [SerializeField] private GameObject      _meleeEnemyPrefab;
    [SerializeField] private GameObject      _rangedEnemyPrefab;
    [SerializeField] private float           _roomHeight = 18f;    // Claude's discretion

    // -- Runtime state ----------------------------------------------------------
    private GameObject _currentRoom;
    private bool       _transitioning;

    // -- Singleton (씬 내 단일 인스턴스 보장) ----------------------------------
    public static FloorSpawner Instance { get; private set; }

    // -- Unity lifecycle --------------------------------------------------------

    private void Awake()
    {
        Instance = this;
        // D-05: 1층은 항상 고정 Room 스폰 (적 없음 — GetEnemyCount(1) returns (0,0))
        _currentRoom = SpawnRoom(_floor1RoomPrefab, 1);
    }

    // -- Public API (RoomExit.OnTriggerEnter2D가 호출) -------------------------

    /// <summary>
    /// 층 전환을 시작한다. _transitioning 플래그가 이중 호출을 방지한다 (Pitfall 1).
    /// </summary>
    public void AdvanceFloor()
    {
        if (_transitioning) return;
        StartCoroutine(FloorTransitionSequence());
    }

    // -- 전환 시퀀스 (FLOOR-02, D-08 6단계) -----------------------------------

    /// <summary>
    /// FLOOR-02: 6단계 층 전환 시퀀스.
    /// 모든 yield는 WaitForSecondsRealtime 사용 — Time.timeScale이 0일 때도 진행됨 (Pitfall 3).
    /// </summary>
    private IEnumerator FloorTransitionSequence()
    {
        _transitioning = true;

        // [Step 1] 조작 불가 — 이동·점프·공격 입력 차단
        _player.LockInput();

        // FloorManager 증가 및 다음 층 스폰 (적은 SetActive(false) 상태 — FLOOR-03)
        FloorManager.CurrentFloor++;
        GameObject nextRoom = SpawnRoom(SelectNextRoom(), FloorManager.CurrentFloor);

        // [Step 2] 순간이동 — 플레이어를 새 층 바닥 위 2유닛으로 즉시 이동
        // Pitfall 4: 위치는 Instantiate 시 이미 설정됨 — Enemy Awake가 정확한 위치로 실행됨
        float newY = (FloorManager.CurrentFloor - 1) * _roomHeight + 2f;
        var rb = _playerTransform.GetComponent<Rigidbody2D>();
        if (rb != null) rb.linearVelocity = Vector2.zero;
        _playerTransform.position = new Vector3(
            _playerTransform.position.x,
            newY,
            0f
        );

        // [Step 3] 카메라 Y스냅 — CameraFollow.LateUpdate가 target.position을 매 프레임 추적.
        // 플레이어 순간이동으로 자동 완성됨. 한 프레임 양보해 LateUpdate 실행 허용.
        yield return null;

        // [Step 4] 가림막 해제 — 새 층의 비활성 적들 활성화 (FLOOR-03)
        // Pitfall 2: includeInactive: true 필수 — SetActive(false)인 적도 반환
        ActivateEnemies(nextRoom);

        // [Step 5] 짧은 대기 — FSM이 첫 Update 사이클을 마칠 시간 (적이 플레이어 인식 시작)
        yield return new WaitForSecondsRealtime(0.05f);

        // [Step 6] 조작 재개
        _player.UnlockInput();

        // 이전 층 파괴 (FLOOR-04, D-09) — 모바일 메모리: 현재 층만 씬에 유지
        if (_currentRoom != null) Destroy(_currentRoom);
        _currentRoom = nextRoom;

        _transitioning = false;
    }

    // -- 내부 헬퍼 --------------------------------------------------------------

    /// <summary>
    /// 2층 이상에서 랜덤 Room 프리팹을 선택한다.
    /// 배열에 같은 프리팹을 여러 번 등록하면 가중치 효과 (Claude's discretion).
    /// </summary>
    private GameObject SelectNextRoom()
    {
        if (_roomPool == null || _roomPool.Length == 0) return _floor1RoomPrefab;
        return _roomPool[Random.Range(0, _roomPool.Length)];
    }

    /// <summary>
    /// Room 프리팹을 올바른 Y 위치에 Instantiate하고 적 스폰 포인트에서 적을 생성한다.
    /// 생성된 적은 SetActive(false) 상태로 유지 — ActivateEnemies()가 활성화함 (FLOOR-03).
    /// </summary>
    private GameObject SpawnRoom(GameObject prefab, int floor)
    {
        // Y = (floor - 1) * roomHeight  →  1층 = Y0, 2층 = Y18, 3층 = Y36, ...
        Vector3 roomOrigin = new Vector3(0f, (floor - 1) * _roomHeight, 0f);
        GameObject room = Instantiate(prefab, roomOrigin, Quaternion.identity);

        // EnemySpawnPoint 태그가 붙은 자식들에 적 Instantiate
        (int meleeCount, int rangedCount) = GetEnemyCount(floor);
        int spawnIndex = 0;

        foreach (Transform child in room.GetComponentsInChildren<Transform>(true))
        {
            if (!child.CompareTag("EnemySpawnPoint")) continue;

            GameObject enemyPrefab;
            if (spawnIndex < meleeCount)
                enemyPrefab = _meleeEnemyPrefab;
            else if (spawnIndex < meleeCount + rangedCount)
                enemyPrefab = _rangedEnemyPrefab;
            else
                break; // 스폰 포인트가 더 있어도 난이도 테이블 초과 시 중단

            if (enemyPrefab == null) { spawnIndex++; continue; }

            // Pitfall 4: Instantiate(prefab, position, rotation) — Awake가 올바른 위치로 실행됨
            GameObject enemy = Instantiate(enemyPrefab, child.position, Quaternion.identity);
            enemy.SetActive(false); // FLOOR-03: 전환 4단계까지 비활성
            spawnIndex++;
        }

        return room;
    }

    /// <summary>
    /// FLOOR-03: 전환 4단계에서 호출. Room 내 모든 IEnemy를 활성화한다.
    /// Pitfall 2: GetComponentsInChildren에 true(includeInactive) 필수 전달.
    /// </summary>
    private void ActivateEnemies(GameObject room)
    {
        // IEnemy를 직접 GetComponentsInChildren으로 가져올 수 없으므로 MonoBehaviour 경유
        foreach (MonoBehaviour mb in room.GetComponentsInChildren<MonoBehaviour>(true))
        {
            if (mb is IEnemy)
                mb.gameObject.SetActive(true);
        }
    }

    /// <summary>
    /// D-07: 층 번호 기반 적 수 난이도 테이블.
    /// 1층: 적 없음 (튜토리얼).
    /// 1~5층: 근접 위주 / 6~10층: 혼합 / 11층+: 원거리 비율 확대.
    /// </summary>
    private (int melee, int ranged) GetEnemyCount(int floor)
    {
        if (floor == 1)   return (0, 0);
        if (floor <= 5)   return (Random.Range(2, 4), Random.Range(0, 2));
        if (floor <= 10)  return (2, Random.Range(1, 3));
        return (2, Random.Range(2, 4));
    }
}
```
  </action>
  <verify>
    <automated>
    grep -n "EnemySpawnPoint" "D:/새 폴더/Fast/ProjectSettings/TagManager.asset" && grep -n "AdvanceFloor\|FloorTransitionSequence\|WaitForSecondsRealtime" "D:/새 폴더/Fast/Assets/Scripts/World/FloorSpawner.cs" && grep -n "includeInactive\|GetComponentsInChildren" "D:/새 폴더/Fast/Assets/Scripts/World/FloorSpawner.cs" && grep -n "OnTriggerEnter2D\|CompareTag" "D:/새 폴더/Fast/Assets/Scripts/World/RoomExit.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - ProjectSettings/TagManager.asset에 `EnemySpawnPoint` 문자열이 존재한다
    - Assets/Scripts/World/FloorSpawner.cs 파일이 존재한다
    - FloorSpawner.cs에 `private IEnumerator FloorTransitionSequence()` 메서드가 존재한다
    - FloorSpawner.cs에 `WaitForSecondsRealtime` 사용이 존재한다 (`WaitForSeconds`가 아님)
    - FloorSpawner.cs에 `GetComponentsInChildren<MonoBehaviour>(true)` 호출이 존재한다 (true 인수 필수)
    - FloorSpawner.cs에 `enemy.SetActive(false)` 코드가 SpawnRoom() 내부에 존재한다
    - FloorSpawner.cs에 `Destroy(_currentRoom)` 코드가 존재한다 (FLOOR-04)
    - FloorSpawner.cs에 `if (_transitioning) return;` 이중 발동 가드가 존재한다
    - FloorSpawner.cs의 FloorTransitionSequence() 코루틴 마지막 줄에 `_transitioning = false;`가 존재한다 — 없으면 첫 전환 후 FloorSpawner가 영구 잠금됨
    - Assets/Scripts/World/RoomExit.cs 파일이 존재한다
    - RoomExit.cs에 `CompareTag("Player")` 체크가 OnTriggerEnter2D 내부에 존재한다
    - RoomExit.cs에 `[RequireComponent(typeof(Collider2D))]` 어트리뷰트가 존재한다
  </acceptance_criteria>
  <done>FloorSpawner와 RoomExit C# 코드가 완성되고, EnemySpawnPoint 태그가 TagManager에 등록되어 Unity Editor에서 Room 프리팹 조립에 즉시 사용할 수 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1: PlayerController에 _inputLocked 필드, LockInput()/UnlockInput()/InputLocked 추가. CombatController.Update()에 _player.InputLocked 가드 추가.
    Task 2: FloorSpawner.cs 신규 생성 (6단계 전환 시퀀스, D-07 난이도 테이블). RoomExit.cs 신규 생성. EnemySpawnPoint 태그 추가.
  </what-built>
  <how-to-verify>
    Unity Editor에서 컴파일 에러 없음을 확인한다:

    1. Unity Editor를 열고 Console 창을 확인한다 (Window > General > Console)
    2. 에러 메시지 없이 컴파일이 완료되는지 확인한다
    3. Project 창에서 Assets/Scripts/World/FloorSpawner.cs, RoomExit.cs 두 파일이 존재하는지 확인한다
    4. Edit > Project Settings > Tags and Layers 열어 Tags 목록에 "EnemySpawnPoint"가 있는지 확인한다
    5. Player GameObject의 Inspector에서 CombatController 컴포넌트에 "_player" 필드(PlayerController 타입)가 노출되는지 확인한다

    **컴파일 에러 발생 시:** Console의 에러 메시지를 그대로 복사해서 답변에 포함한다.
  </how-to-verify>
  <resume-signal>컴파일 에러 없음 확인 후 "approved" 입력. 에러가 있으면 에러 메시지 붙여넣기.</resume-signal>
</task>

</tasks>

<verification>
플랜 01 전체 완료 기준:

1. `grep -rn "_inputLocked" Assets/Scripts/Player/PlayerController.cs` — 결과 있음
2. `grep -rn "WaitForSecondsRealtime" Assets/Scripts/World/FloorSpawner.cs` — 결과 있음
3. `grep -rn "includeInactive\|GetComponentsInChildren.*true" Assets/Scripts/World/FloorSpawner.cs` — 결과 있음
4. `grep -rn "SetActive(false)" Assets/Scripts/World/FloorSpawner.cs` — SpawnRoom 내부에 존재
5. `grep -n "_transitioning = false" Assets/Scripts/World/FloorSpawner.cs` — FloorTransitionSequence 마지막에 존재
6. Unity Editor 컴파일 에러 없음 (checkpoint 통과)
</verification>

<success_criteria>
- PlayerController가 LockInput()/UnlockInput()/InputLocked API를 제공한다
- CombatController가 층 전환 시퀀스 중 슬로우모션 진입을 차단한다
- FloorSpawner가 씬에 배치 가능한 MonoBehaviour로 존재한다
- FloorTransitionSequence()가 WaitForSecondsRealtime만 사용한다 (timeScale=0 안전)
- ActivateEnemies()가 includeInactive:true로 비활성 적을 정확히 찾는다
- RoomExit.cs가 FloorSpawner.Instance.AdvanceFloor()를 호출한다
- Unity Editor 컴파일 에러 없음
</success_criteria>

<output>
완료 후 `.planning/phases/05-procedural-map-infinite-stages/05-01-SUMMARY.md`를 생성한다.
</output>
