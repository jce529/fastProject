---
phase: 09-infinite-gen-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/WorldGenerator.cs
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03

must_haves:
  truths:
    - "WorldGenerator.cs가 컴파일 오류 없이 존재하고 MonoBehaviour를 상속한다"
    - "Start()가 호출되면 체인에 시작룸 1개 + lookahead 2쌍(= 총 GO 5개)이 생성된다"
    - "SpawnNextPair()가 호출될 때마다 SelectCorridor()의 Y drift 제약이 Corridor 선택에 반영된다 (GEN-03)"
    - "Update()가 매 프레임 UpdatePlayerIndex()→ GEN-01 스폰 루프 → GEN-02 정리 루프 순서로 실행된다"
    - "RemoveTail() 직후 _playerCurrentIndex--가 항상 쌍으로 실행된다 (Pitfall 4 방지)"
  artifacts:
    - path: "Assets/Scripts/World/WorldGenerator.cs"
      provides: "플레이어 위치 기반 무한 Room+Corridor 체인 생성/정리 MonoBehaviour"
      exports:
        - "public void SpawnNextFloorStandbyRoom()"
      contains: "private List<(GameObject room, GameObject corridor)> _chain"
  key_links:
    - from: "WorldGenerator.Update()"
      to: "WorldGenerator.SpawnNextPair()"
      via: "while (_chain.Count - 1 - _playerCurrentIndex < _lookaheadCount)"
      pattern: "_lookaheadCount"
    - from: "WorldGenerator.Update()"
      to: "WorldGenerator.RemoveTail()"
      via: "while (_playerCurrentIndex > _lookbehindCount)"
      pattern: "_playerCurrentIndex--"
    - from: "WorldGenerator.SpawnNextPair()"
      to: "WorldGenerator.AlignByEntry()"
      via: "Instantiate(..., Vector3.zero, ...) 직후 호출"
      pattern: "Instantiate.*Vector3.zero"
    - from: "WorldGenerator.SelectCorridor()"
      to: "_currentYDrift"
      via: "_corridorUp+4 / _corridorDown-4 누적"
      pattern: "_currentYDrift [+\\-]= 4f"
---

<objective>
Assets/Scripts/World/WorldGenerator.cs를 신규 생성한다. Start()에서 초기 체인을 구성하고, Update()에서 플레이어 위치 기반으로 앞 2개 Room+Corridor를 미리 스폰(GEN-01)하고 뒤 2개 초과분을 Destroy(GEN-02)하며, SelectCorridor()에서 Y drift 제약을 적용한 Corridor 3종 랜덤 선택(GEN-03)을 구현한다.

Purpose: Phase 9의 핵심 런타임 MonoBehaviour. TestWorldGenerator의 정적 1회 배치를 동적 무한 체인으로 확장하며, Phase 8에서 검증된 AlignByEntry/FindConnector 패턴을 직접 이식한다.
Output: Assets/Scripts/World/WorldGenerator.cs (신규, 약 160줄)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-infinite-gen-cleanup/09-CONTEXT.md
@.planning/phases/09-infinite-gen-cleanup/09-RESEARCH.md

<interfaces>
<!-- Phase 8 산출물에서 추출 — WorldGenerator가 이식하는 패턴들 -->

From Assets/Scripts/World/TestWorldGenerator.cs (Phase 8 검증 완료):
```csharp
// AlignByEntry — Instantiate(Vector3.zero) 직후에만 유효
private void AlignByEntry(GameObject go, Vector3 targetWorldPos)
{
    RoomConnector entry = FindConnector(go, RoomConnector.Direction.Left);
    if (entry == null) { go.transform.position = targetWorldPos; return; }
    go.transform.position = targetWorldPos - entry.transform.position;
}

private RoomConnector FindConnector(GameObject go, RoomConnector.Direction direction)
{
    foreach (RoomConnector rc in go.GetComponentsInChildren<RoomConnector>(true))
        if (rc.direction == direction) return rc;
    return null;
}
```

From Assets/Scripts/World/RoomConnector.cs:
```csharp
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    [SerializeField] public Direction direction;
    [SerializeField] public GameObject connectedObject;
}
```

From Assets/Scripts/Camera/CameraFollow.cs:
```csharp
public void SnapToRoom(Vector3 worldCenter)  // _hasBounds = false, 자유 추적
public void SnapToRoom(Bounds worldBounds)   // _hasBounds = true, bounds 클램프
```

// Context: D-09 체인 구조
// chain[0] = (StartRoom, null)      — 첫 룸, 왼쪽 Corridor 없음
// chain[1] = (Room2, Corridor1)     — Corridor1이 StartRoom과 Room2 사이
// chain[N] = (RoomN+1, CorridorN)   — CorridorN이 RoomN과 RoomN+1 사이
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorldGenerator.cs — 클래스 선언 + Start() + 모든 헬퍼 메서드</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/TestWorldGenerator.cs (AlignByEntry + FindConnector 패턴 — 이식 원본)
    - Assets/Scripts/World/RoomConnector.cs (Direction enum + GetComponentsInChildren 패턴)
    - Assets/Scripts/Camera/CameraFollow.cs (SnapToRoom(Vector3) 시그니처 — Pitfall 7 방지)
  </read_first>
  <action>
Assets/Scripts/World/WorldGenerator.cs를 신규 생성한다. Update()와 UpdatePlayerIndex()는 Task 2에서 추가하므로 이 Task에서는 제외한다.

파일 전체 내용 (Update/UpdatePlayerIndex 제외):

```csharp
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Phase 9: 플레이어 위치 기반 수평 Room+Corridor 무한 체인 생성/정리.
/// SampleScene에서 FloorSpawner/TestWorldGenerator 대신 배치한다.
/// D-08: FloorSpawner.cs는 Phase 9에서 수정하지 않는다.
/// D-09: 체인 List에서 corridor = 해당 room의 왼쪽(ENT 방향) 길.
/// </summary>
public class WorldGenerator : MonoBehaviour
{
    [Header("Room Pool")]
    [SerializeField] private GameObject[] _roomPrefabs;

    [Header("Corridor Prefabs")]
    [SerializeField] private GameObject _corridorFlat;
    [SerializeField] private GameObject _corridorUp;
    [SerializeField] private GameObject _corridorDown;

    [Header("References")]
    [SerializeField] private Transform _playerTransform;
    [SerializeField] private CameraFollow _cameraFollow;  // Pitfall 7: SnapToRoom 초기화용

    [Header("Chain Settings")]
    [SerializeField] private int _lookaheadCount  = 2;   // GEN-01: 앞 N개 유지
    [SerializeField] private int _lookbehindCount = 2;   // GEN-02: 뒤 N개 유지

    [Header("Y Drift Bounds")]
    [SerializeField] private float _minYDrift = -12f;    // D-02
    [SerializeField] private float _maxYDrift = +12f;    // D-02

    [Header("Next Floor Standby (Phase 10 연동)")]
    [SerializeField] private float _floorHeight = 40f;   // D-04: nextFloorBaseY 계산용

    // Runtime state
    private List<(GameObject room, GameObject corridor)> _chain
        = new List<(GameObject, GameObject)>();
    private float _currentYDrift;         // D-01: 누적 Y 변위
    private Vector3 _chainHeadExitPos;    // 다음 Corridor ENT 스폰 기준점
    private int _playerCurrentIndex;      // 플레이어가 현재 위치한 체인 인덱스
    private GameObject _nextFloorRoom;    // D-05: 다음 층 대기룸 (체인 외부 참조)

    private void Start()
    {
        if (_roomPrefabs == null || _roomPrefabs.Length == 0)
        {
            Debug.LogError("[WorldGenerator] _roomPrefabs is empty — Inspector에서 할당 필요");
            return;
        }

        // 1. 시작 룸 스폰 — 반드시 Vector3.zero 기준 (Pitfall 2: AlignByEntry 공식 성립 조건)
        var startRoomPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
        var startRoom = Instantiate(startRoomPrefab, Vector3.zero, Quaternion.identity);
        _chain.Add((startRoom, null)); // D-09: 첫 룸은 왼쪽 Corridor 없음

        // 시작 룸 EXIT 위치 → 첫 Corridor 스폰 기준점
        var startExit = FindConnector(startRoom, RoomConnector.Direction.Right);
        _chainHeadExitPos = startExit != null ? startExit.transform.position : Vector3.zero;

        // 2. 초기 lookahead 스폰 (GEN-01: 시작 시 앞 2쌍 미리 생성)
        for (int i = 0; i < _lookaheadCount; i++)
            SpawnNextPair();

        // 3. CameraFollow bounds 초기화 (Pitfall 7: FloorSpawner가 설정한 _hasBounds 잔류 방지)
        if (_cameraFollow != null)
            _cameraFollow.SnapToRoom(Vector3.zero);
    }

    private void SpawnNextPair()
    {
        // Corridor 선택 및 스폰 (GEN-03: 3종 랜덤, D-01~D-03: Y drift 제약)
        var corridorPrefab = SelectCorridor();
        var corridor = Instantiate(corridorPrefab, Vector3.zero, Quaternion.identity);
        AlignByEntry(corridor, _chainHeadExitPos);

        // Corridor EXIT → Room ENT 스폰 기준점
        var corridorExit = FindConnector(corridor, RoomConnector.Direction.Right);
        var roomEntryPos = corridorExit != null ? corridorExit.transform.position : _chainHeadExitPos;

        // Room 스폰 (GEN-03: 룸 풀 랜덤 선택)
        var roomPrefab = _roomPrefabs[Random.Range(0, _roomPrefabs.Length)];
        var room = Instantiate(roomPrefab, Vector3.zero, Quaternion.identity);
        AlignByEntry(room, roomEntryPos);

        // 다음 스폰 기준점 업데이트 (Room EXIT 위치)
        var roomExit = FindConnector(room, RoomConnector.Direction.Right);
        _chainHeadExitPos = roomExit != null ? roomExit.transform.position : roomEntryPos;

        // D-09: corridor = 이 room의 왼쪽 길로 체인 등록
        _chain.Add((room, corridor));
    }

    private void RemoveTail()
    {
        var (room, corridor) = _chain[0];
        if (corridor != null) Destroy(corridor);
        Destroy(room);
        _chain.RemoveAt(0);
    }

    private GameObject SelectCorridor()
    {
        // D-01~D-03: 현재 Y drift 범위 내 유효 후보만 선택
        var candidates = new List<GameObject>(3);
        if (_currentYDrift < _maxYDrift) candidates.Add(_corridorUp);     // +4 여유 있을 때만
        candidates.Add(_corridorFlat);                                      // 항상 허용
        if (_currentYDrift > _minYDrift) candidates.Add(_corridorDown);    // -4 여유 있을 때만

        var chosen = candidates[Random.Range(0, candidates.Count)];

        // D-01: 선택된 Corridor에 따라 drift 누적
        if      (chosen == _corridorUp)   _currentYDrift += 4f;
        else if (chosen == _corridorDown) _currentYDrift -= 4f;

        return chosen;
    }

    private void AlignByEntry(GameObject go, Vector3 targetWorldPos)
    {
        // Source: TestWorldGenerator.cs — Phase 8에서 검증된 패턴
        // CRITICAL: Instantiate(prefab, Vector3.zero, Quaternion.identity) 직후에만 호출해야 함 (Pitfall 2)
        RoomConnector entry = FindConnector(go, RoomConnector.Direction.Left);
        if (entry == null)
        {
            Debug.LogWarning($"[WorldGenerator] {go.name} has no Left RoomConnector — placed at {targetWorldPos}");
            go.transform.position = targetWorldPos;
            return;
        }
        // entry.transform.position == 루트 원점 기준 로컬 오프셋 (root가 Vector3.zero이므로)
        go.transform.position = targetWorldPos - entry.transform.position;
    }

    private RoomConnector FindConnector(GameObject go, RoomConnector.Direction direction)
    {
        foreach (RoomConnector rc in go.GetComponentsInChildren<RoomConnector>(true))
        {
            if (rc.direction == direction) return rc;
        }
        return null;
    }

    /// <summary>
    /// Phase 10에서 ExitPortal.OnTriggerEnter2D()가 호출한다.
    /// D-04: _chainHeadExitPos.y + _floorHeight 위치에 룸을 비활성(SetActive false) 스폰.
    /// D-06: Phase 9에서는 스텁만 구현. 실제 로직은 Phase 10 범위.
    /// </summary>
    public void SpawnNextFloorStandbyRoom()
    {
        Debug.Log("[WorldGenerator] SpawnNextFloorStandbyRoom — stub (Phase 10에서 구현)");
    }
}
```

금지 사항:
- CorridorBuilder.Run() 호출 금지 (에디터 전용 — 런타임 컴파일 불가, Pitfall 3)
- FindObjectsOfType 금지 (ROADMAP Stack Constraints)
- FloorSpawner.cs 수정 금지 (D-08)
- Instantiate 시 Vector3.zero 외 위치 사용 금지 (AlignByEntry 공식 파괴, Pitfall 2)
  </action>
  <verify>
    <automated>grep -n "private List.*room.*corridor\|private void SpawnNextPair\|private GameObject SelectCorridor\|private void RemoveTail\|private void AlignByEntry\|public void SpawnNextFloorStandbyRoom\|_currentYDrift.*4f" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/World/WorldGenerator.cs 파일이 존재한다
    - "private List<(GameObject room, GameObject corridor)> _chain" 라인이 존재한다
    - "private void SpawnNextPair()" 메서드가 존재한다
    - "private GameObject SelectCorridor()" 메서드가 존재한다
    - "private void RemoveTail()" 메서드가 존재한다
    - "private void AlignByEntry" 메서드가 존재한다
    - "private RoomConnector FindConnector" 메서드가 존재한다
    - "public void SpawnNextFloorStandbyRoom()" 메서드가 존재한다
    - "_currentYDrift += 4f"와 "_currentYDrift -= 4f" 두 라인이 모두 존재한다 (D-01 drift 누적)
    - "Instantiate(startRoomPrefab, Vector3.zero, Quaternion.identity)" 패턴이 존재한다 (Pitfall 2 준수)
    - "_cameraFollow.SnapToRoom(Vector3.zero)" 호출이 Start() 내에 존재한다 (Pitfall 7 방지)
    - Update() 메서드는 아직 존재하지 않는다 (Task 2에서 추가)
  </acceptance_criteria>
  <done>WorldGenerator.cs가 생성되었고, Start()가 초기 체인(시작룸 + lookahead 2쌍)을 구성하며, SelectCorridor()가 Y drift 제약을 적용한 3종 랜덤 선택을 구현하고, SpawnNextFloorStandbyRoom()이 Phase 10 연동용 스텁으로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: WorldGenerator.cs — Update() + UpdatePlayerIndex() 추가</name>
  <files>Assets/Scripts/World/WorldGenerator.cs</files>
  <read_first>
    - Assets/Scripts/World/WorldGenerator.cs (Task 1에서 생성된 현재 상태 — 삽입 위치 확인)
  </read_first>
  <action>
Assets/Scripts/World/WorldGenerator.cs의 클래스 닫힘 브레이스(`}`) 바로 앞에 Update()와 UpdatePlayerIndex() 메서드를 삽입한다. SpawnNextFloorStandbyRoom() 메서드 뒤, 클래스 닫힘 브레이스 앞이 삽입 위치다.

삽입할 메서드 2개 (정확한 코드):

```csharp
    private void Update()
    {
        if (_playerTransform == null || _chain.Count == 0) return;
        UpdatePlayerIndex();

        // GEN-01: 플레이어 앞 _lookaheadCount개 Room+Corridor 보장
        while (_chain.Count - 1 - _playerCurrentIndex < _lookaheadCount)
            SpawnNextPair();

        // GEN-02: 플레이어 뒤 _lookbehindCount개 초과 시 tail 정리
        // Pitfall 4: RemoveTail 후 _playerCurrentIndex-- 반드시 쌍으로 실행
        while (_playerCurrentIndex > _lookbehindCount)
        {
            RemoveTail();
            _playerCurrentIndex--;
        }
    }

    private void UpdatePlayerIndex()
    {
        // 플레이어 X > 현재 룸의 Door/EXIT X → 다음 룸 인덱스로 진행
        for (int i = _playerCurrentIndex; i < _chain.Count - 1; i++)
        {
            var exitConnector = FindConnector(_chain[i].room, RoomConnector.Direction.Right);
            if (exitConnector == null) break;
            if (_playerTransform.position.x > exitConnector.transform.position.x)
                _playerCurrentIndex = i + 1;
            else
                break;
        }
    }
```

삽입 후 파일 마지막 부분이 다음 순서여야 한다:
1. SpawnNextFloorStandbyRoom() { ... }
2. Update() { ... }         ← 신규
3. UpdatePlayerIndex() { ... } ← 신규
4. }  // class WorldGenerator 닫힘

인라인 삽입 시 들여쓰기는 클래스 멤버 수준(4 spaces)을 유지한다.
  </action>
  <verify>
    <automated>grep -n "private void Update\|private void UpdatePlayerIndex\|_playerCurrentIndex--\|SpawnNextPair\|RemoveTail\|_lookaheadCount\|_lookbehindCount" "Assets/Scripts/World/WorldGenerator.cs"</automated>
  </verify>
  <acceptance_criteria>
    - "private void Update()" 메서드가 WorldGenerator.cs에 존재한다
    - "private void UpdatePlayerIndex()" 메서드가 WorldGenerator.cs에 존재한다
    - "_chain.Count - 1 - _playerCurrentIndex < _lookaheadCount" 조건식이 존재한다 (GEN-01 while loop)
    - "_playerCurrentIndex > _lookbehindCount" 조건식이 존재한다 (GEN-02 while loop)
    - "_playerCurrentIndex--" 라인이 RemoveTail() 호출 직후 존재한다 (Pitfall 4 방지)
    - UpdatePlayerIndex() 내에 "exitConnector.transform.position.x" 참조가 존재한다
    - "if (_playerTransform == null || _chain.Count == 0) return;" 가드가 Update() 첫 줄에 존재한다
  </acceptance_criteria>
  <done>WorldGenerator.cs가 Update() 폴링 로직을 포함하여 완성되었다. Update()는 플레이어 위치 기반으로 GEN-01(앞 2개 스폰)과 GEN-02(뒤 초과분 Destroy)를 매 프레임 실행한다. 파일이 컴파일 오류 없이 완결된 클래스 구조를 갖는다.</done>
</task>

</tasks>

<verification>
grep -c "private void" "Assets/Scripts/World/WorldGenerator.cs" 결과가 7 이상이어야 한다 (Start, SpawnNextPair, RemoveTail, SelectCorridor, AlignByEntry, FindConnector, Update, UpdatePlayerIndex).
grep "Vector3.zero, Quaternion.identity" "Assets/Scripts/World/WorldGenerator.cs" 결과가 3줄 이어야 한다 (startRoom, corridor, room 3번 Instantiate).
</verification>

<success_criteria>
- Assets/Scripts/World/WorldGenerator.cs가 약 160줄 이상으로 생성되었다
- 모든 [SerializeField] Inspector 필드가 올바른 [Header] 그룹 아래 존재한다
- Start()가 초기 체인을 구성하고, Update()가 GEN-01/GEN-02 폴링을 수행한다
- SelectCorridor()가 _currentYDrift 기반 Y drift 제약(D-01~D-03)을 정확히 구현한다
- Unity에서 해당 파일을 열었을 때 컴파일 오류가 0개이다
</success_criteria>

<output>
완료 후 `.planning/phases/09-infinite-gen-cleanup/09-02-SUMMARY.md` 생성:
- 생성된 파일 경로 및 총 라인 수
- 구현된 메서드 목록
- 중요 설계 결정 (AlignByEntry 이식, Y drift 공식, chain 자료구조)
- Task 1/2 verify 결과 (grep 출력 요약)
</output>
