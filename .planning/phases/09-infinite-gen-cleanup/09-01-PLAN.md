---
phase: 09-infinite-gen-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Editor/RoomMarkerTool.cs
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03

must_haves:
  truths:
    - "RoomMarkerTool 실행 후 14개 룸 모두의 Door/ENT에 RoomConnector(Left)가 존재한다"
    - "RoomMarkerTool 실행 후 14개 룸 모두의 Door/EXIT에 RoomConnector(Right)가 존재한다"
    - "WorldGenerator.FindConnector(room, Direction.Right)가 null을 반환하지 않아 모든 룸이 원점에 쌓이는 현상이 차단된다"
  artifacts:
    - path: "Assets/Editor/RoomMarkerTool.cs"
      provides: "14개 룸 × 양방향(Left+Right) RoomConnector 멱등 부착 에디터 도구"
      contains: "Room_Chain, Door/EXIT, Direction.Right"
  key_links:
    - from: "Assets/Editor/RoomMarkerTool.cs"
      to: "Assets/Prefabs/Rooms/*/[Room].prefab Door/EXIT"
      via: "PrefabUtility.LoadPrefabContents + AddComponent<RoomConnector>"
      pattern: "AddConnector.*Door/EXIT.*Direction.Right"
---

<objective>
RoomMarkerTool.cs를 업데이트하여 전체 14개 Room 프리팹의 Door/ENT(Left)와 Door/EXIT(Right) 양방향 RoomConnector를 멱등적으로 부착한다.

Purpose: 현재 RoomMarkerTool은 4개 룸의 Door/ENT(Left)만 처리한다. WorldGenerator.FindConnector(room, Direction.Right)가 null을 반환하지 않으려면 모든 룸의 Door/EXIT에 Direction.Right 커넥터가 반드시 존재해야 한다 (RESEARCH.md Pitfall 1).
Output: 업데이트된 Assets/Editor/RoomMarkerTool.cs — 에디터 메뉴 Fast/Phase9/Add Room Connectors로 실행 가능.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-infinite-gen-cleanup/09-CONTEXT.md
@.planning/phases/09-infinite-gen-cleanup/09-RESEARCH.md

<interfaces>
<!-- RoomConnector.cs에서 추출 — AddConnector 함수가 참조하는 타입 -->

From Assets/Scripts/World/RoomConnector.cs:
```csharp
public class RoomConnector : MonoBehaviour
{
    public enum Direction { Left, Right }
    [SerializeField] public Direction direction;
    [SerializeField] public GameObject connectedObject;
}
```

From Assets/Editor/RoomMarkerTool.cs (현재 상태):
```csharp
// 현재 커버: 4개 룸, Door/ENT(Left)만
private static readonly string[] RoomNames = {
    "Room_Combat", "Room_Fall", "Room_Gap", "Room_Stair",
};

[MenuItem("Fast/Phase8/Add Room Connectors")]
public static void AddRoomConnectors() {
    foreach (var roomName in RoomNames) {
        // ...
        AddConnector(root, "Door/ENT", RoomConnector.Direction.Left);
        // Door/EXIT Right 없음 — Phase 9에서 추가
        // ...
    }
}

private static void AddConnector(GameObject root, string childPath, RoomConnector.Direction dir) {
    var child = root.transform.Find(childPath);
    if (child == null) { Debug.LogWarning(...); return; }
    if (child.GetComponent<RoomConnector>() != null) return; // idempotent
    var rc = child.gameObject.AddComponent<RoomConnector>();
    rc.direction = dir;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomMarkerTool.cs — 14개 룸 + Door/EXIT Right 커넥터 추가</name>
  <files>Assets/Editor/RoomMarkerTool.cs</files>
  <read_first>
    - Assets/Editor/RoomMarkerTool.cs (현재 상태 확인 — 무엇을 교체하는지 파악)
    - Assets/Scripts/World/RoomConnector.cs (Direction.Left / Direction.Right enum 확인)
  </read_first>
  <action>
Assets/Editor/RoomMarkerTool.cs를 아래 내용으로 완전히 교체한다.

변경 핵심:
1. RoomNames 배열 확장: 기존 4개 → 14개 전체 (아래 목록)
2. 메뉴 경로 변경: "Fast/Phase8/Add Room Connectors" → "Fast/Phase9/Add Room Connectors"
3. AddRoomConnectors() 루프에 Door/EXIT Right 커넥터 추가 라인 삽입
4. 주석/로그 업데이트: Phase 9 및 양방향 부착 명시

교체 후 파일 전체 내용:

```csharp
using UnityEditor;
using UnityEngine;

/// <summary>
/// Menu: Fast/Phase9/Add Room Connectors
/// 전체 14개 Room 프리팹의 Door/ENT 자식에 RoomConnector(Left)를,
/// Door/EXIT 자식에 RoomConnector(Right)를 멱등적으로 부착한다.
/// Phase 9 WorldGenerator.FindConnector()가 체인 정렬에 사용한다.
/// </summary>
public static class RoomMarkerTool
{
    private static readonly string[] RoomNames =
    {
        "Room_Combat",
        "Room_Hunt",
        "Room_Ladder",
        "Room_LadderDanger",
        "Room_Gap",
        "Room_Fall",
        "Room_Sniper",
        "Room_Stair",
        "Room_Crossroad",
        "Room_Chase",
        "Room_Dodge",
        "Room_Chain",
        "Room_Recovery",
        "Room_Mixed",
    };

    [MenuItem("Fast/Phase9/Add Room Connectors")]
    public static void AddRoomConnectors()
    {
        foreach (var roomName in RoomNames)
        {
            string path = $"Assets/Prefabs/Rooms/{roomName}/{roomName}.prefab";

            var root = PrefabUtility.LoadPrefabContents(path);
            if (root == null)
            {
                Debug.LogError($"[RoomMarkerTool] Prefab not found: {path}");
                continue;
            }

            AddConnector(root, "Door/ENT",  RoomConnector.Direction.Left);
            AddConnector(root, "Door/EXIT", RoomConnector.Direction.Right);

            PrefabUtility.SaveAsPrefabAsset(root, path);
            PrefabUtility.UnloadPrefabContents(root);

            Debug.Log($"[RoomMarkerTool] ENT(Left)+EXIT(Right) applied to {roomName}");
        }

        AssetDatabase.SaveAssets();
        AssetDatabase.Refresh();

        Debug.Log("[RoomMarkerTool] Done. 14 rooms updated with bidirectional RoomConnectors.");
    }

    private static void AddConnector(GameObject root, string childPath, RoomConnector.Direction dir)
    {
        var child = root.transform.Find(childPath); // "/" 경로 지원 (예: "Door/ENT")
        if (child == null)
        {
            Debug.LogWarning($"[RoomMarkerTool] '{childPath}' not found in '{root.name}'");
            return;
        }

        if (child.GetComponent<RoomConnector>() != null)
            return; // Already attached — idempotent

        var rc = child.gameObject.AddComponent<RoomConnector>();
        rc.direction = dir;
    }
}
```

주의: Room_Stair는 Phase 9 _roomPrefabs 풀에서 제외되지만(Pitfall 5: ENT/EXIT ΔY 불일치),
RoomMarkerTool은 여전히 커넥터를 부착한다. 풀 제외는 Phase 9-03 SampleScene 설정에서 처리한다.
  </action>
  <verify>
    <automated>grep -n "Room_Chain\|Room_Recovery\|Room_Mixed\|Door/EXIT\|Direction.Right\|Phase9" "Assets/Editor/RoomMarkerTool.cs"</automated>
  </verify>
  <acceptance_criteria>
    - RoomMarkerTool.cs에 "Room_Chain" 문자열이 존재한다
    - RoomMarkerTool.cs에 "Room_Recovery" 문자열이 존재한다
    - RoomMarkerTool.cs에 "Room_Mixed" 문자열이 존재한다
    - RoomMarkerTool.cs에 "Door/EXIT" 문자열이 존재한다
    - RoomMarkerTool.cs에 "Direction.Right" 문자열이 존재한다
    - RoomMarkerTool.cs에 "Fast/Phase9/Add Room Connectors" MenuItem이 존재한다
    - RoomNames 배열에 14개 항목이 존재한다 (grep "Room_" 결과 14줄)
    - AddConnector(root, "Door/ENT", ...)와 AddConnector(root, "Door/EXIT", ...) 두 라인이 모두 존재한다
  </acceptance_criteria>
  <done>RoomMarkerTool.cs가 14개 룸 × 양방향(Left+Right) 커넥터 부착 로직을 포함하며, Unity Editor에서 Fast/Phase9/Add Room Connectors 메뉴 실행 시 Console에 "Done. 14 rooms updated" 로그가 출력된다.</done>
</task>

</tasks>

<verification>
grep -c "Room_" "Assets/Editor/RoomMarkerTool.cs" 결과가 14 이상이어야 한다.
grep "AddConnector.*Door/EXIT.*Direction.Right" "Assets/Editor/RoomMarkerTool.cs" 결과가 1줄 이상이어야 한다.
</verification>

<success_criteria>
- Assets/Editor/RoomMarkerTool.cs에 14개 RoomNames 항목이 모두 존재한다
- Door/ENT(Left) + Door/EXIT(Right) 양방향 AddConnector 호출이 루프 안에 존재한다
- Unity Editor에서 Fast/Phase9/Add Room Connectors 실행 시 컴파일 오류 없이 동작한다
- 실행 후 Room_Combat.prefab Inspector의 Door/EXIT 자식에 RoomConnector(Right)가 부착된다
</success_criteria>

<output>
완료 후 `.planning/phases/09-infinite-gen-cleanup/09-01-SUMMARY.md` 생성:
- 무엇을 변경했는지 (교체된 파일, 변경 내용 요약)
- 실행 결과 (Unity Console 로그 확인 여부)
- 다음 플랜에서 필요한 정보 (RoomConnector가 부착되었음을 확인)
</output>
