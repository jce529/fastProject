---
phase: 18-shared-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Progression/BossUnlockManager.cs
  - Assets/Scripts/Enemy/Boss/BossEnemyBase.cs
  - Assets/Scripts/Enemy/Boss/FioraBoss.cs (renamed from Assets/Scripts/Enemy/BossEnemy.cs)
  - Assets/Scripts/Enemy/Boss/FioraBoss.cs.meta (renamed from Assets/Scripts/Enemy/BossEnemy.cs.meta — GUID cb839023c498e514cab6bb76ab11cde9 preserved)
  - Assets/Editor/BossEnemyPrefabBuilder.cs
autonomous: false
requirements: [INFRA-03, UNLOCK-01]

must_haves:
  truths:
    - "BossUnlockManager.Unlock/IsUnlocked API가 존재하며 PlayerPrefs에 즉시 Save()된다"
    - "DeathScreenController.RestartGame()은 BossUnlockManager를 전혀 참조하지 않는다 (해금 상태는 리셋 스윕에서 구조적으로 격리)"
    - "FioraBoss가 BossEnemyBase를 상속하며 defeat-guard(_isDefeated)/사망시퀀스(Die)/스폰게이팅(SetSpawnGate)/피격하이라이트(ClearHighlight)를 재작성 없이 사용한다"
    - "FioraBoss를 7회 피격으로 처치하면 pattern이 1~6회차에는 처음부터 재시작되고, 7회째에 사망 연출+점수 보너스+BossUnlockManager.Unlock 호출이 발생한다"
    - "BossEnemy.prefab의 기존 컴포넌트 참조(GUID cb839023c498e514cab6bb76ab11cde9)가 FioraBoss.cs로도 깨지지 않고 그대로 해석된다 (파일 rename, 삭제+재생성 아님)"
  artifacts:
    - path: "Assets/Scripts/Progression/BossUnlockManager.cs"
      provides: "PlayerPrefs 기반 보스 해금 영구 저장소 (첫 disk persistence 사용)"
      contains: "public static class BossUnlockManager"
    - path: "Assets/Scripts/Enemy/Boss/BossEnemyBase.cs"
      provides: "보스 공통 plumbing (defeat-guard/사망시퀀스/스폰게이팅/하이라이트), EnemyBase와 별개 형제 클래스"
      contains: "abstract class BossEnemyBase"
    - path: "Assets/Scripts/Enemy/Boss/FioraBoss.cs"
      provides: "F.I.O.R.A 전용 Telegraph→Attack→Vulnerable 패턴 루프 (BossEnemy.cs에서 rename)"
      contains: "class FioraBoss : BossEnemyBase"
    - path: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      provides: "BossEnemy.prefab 빌더 — AddComponent 대상이 FioraBoss로 갱신됨"
      contains: "AddComponent<FioraBoss>"
  key_links:
    - from: "Assets/Scripts/Enemy/Boss/FioraBoss.cs"
      to: "Assets/Scripts/Progression/BossUnlockManager.cs"
      via: "Die() 시퀀스에서 BossUnlockManager.Unlock(BossId) 호출"
      pattern: "BossUnlockManager\\.Unlock"
    - from: "Assets/Editor/BossEnemyPrefabBuilder.cs"
      to: "Assets/Scripts/Enemy/Boss/FioraBoss.cs"
      via: "AddComponent<FioraBoss>() — 기존 AddComponent<BossEnemy>() 대체"
      pattern: "AddComponent<FioraBoss>"
    - from: "Assets/Scripts/UI/DeathScreenController.cs"
      to: "Assets/Scripts/Progression/BossUnlockManager.cs"
      via: "참조 없음 — 의도적 격리 (Pitfall 6)"
      pattern: "NEGATIVE: BossUnlockManager"
---

<objective>
INFRA-03 + UNLOCK-01: 기존 `BossEnemy.cs`(Phase 15, 유일한 보스 구현체)에서 보스 범용 plumbing(defeat-guard/사망시퀀스/스폰게이팅/하이라이트)을 `BossEnemyBase`로 추출하고, F.I.O.R.A 고유 패턴 루프는 `FioraBoss : BossEnemyBase`로 남긴다(D-03 — boss id `"Fiora"`). 동시에 이 프로젝트 최초의 디스크 영속 저장소인 `BossUnlockManager`(PlayerPrefs 기반)를 신설하고, 보스 처치 순간 이를 호출해 UNLOCK-01("앱 재시작 후에도 해금 상태 유지")을 만족시킨다.

Purpose: Phase 19-23에서 SAMURAI/DeadEye/MAX/NOVA가 `BossEnemyBase`를 상속해 defeat-guard/사망시퀀스/스폰게이팅/하이라이트를 재작성 없이 즉시 사용하게 하는 것이 목적이다. 2번째 보스가 생기기 전인 지금이 이 추출을 깔끔하게 할 수 있는 마지막 시점(18-RESEARCH.md Pitfall 2).
Output: `Assets/Scripts/Enemy/Boss/BossEnemyBase.cs`(신규) + `FioraBoss.cs`(BossEnemy.cs에서 rename), `Assets/Scripts/Progression/BossUnlockManager.cs`(신규), `BossEnemyPrefabBuilder.cs` 갱신.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-shared-infra/18-CONTEXT.md
@.planning/phases/18-shared-infra/18-RESEARCH.md
@.planning/phases/18-shared-infra/18-VALIDATION.md

<interfaces>
BossEnemyBase로 추출 (범용 plumbing, 현재 Assets/Scripts/Enemy/BossEnemy.cs 302 lines 기준):
- `_isDefeated` 필드 + 불변식: `OnDashHit()`은 오직 `_isDefeated`만 가드하고 `IsAlive`는 절대 참조하지 않는다 (IsAlive는 "빈틈 여부"로 오버로드됨 — line 195-208 참고)
- `IsAlive { get; private set; } = true` — base에서는 `protected set`
- `Awake()`의 범용 부분: `_rb`/`_animator`/`_sr` 캐싱, `_baseColor = _sr.color`, `_playerTransform` 탐색, `_cameraFollow` 캐싱 (line 69-84, `_meleeHitbox`/`_exclamationIcon` 비활성화는 FioraBoss 전용이므로 제외)
- `OnEnable()`/`OnDisable()` — `PlayerController.OnPlayerDeath` 구독/해제 (line 96-104)
- `Start()`의 패턴 부트스트랩 로직 (line 86-94)
- `SetSpawnGate(bool isSpawning)` (line 108-113)
- `OnPlayerDied()`의 "코루틴 정지 + cleanup 훅 호출" 뼈대 (line 117-123 — `_exclamationIcon`/`_meleeHitbox` 비활성화는 FioraBoss 전용 cleanup 훅으로)
- `Die()` 시퀀스의 범용 부분 (line 256-274 — `_state = BossState.Dead`는 FioraBoss 전용이므로 제외): rb 정지, 콜라이더 비활성화, animator isDead, EnemyDeathEffect 추가+ConfigureIntensity+PlayDeathSequence, cameraFollow.Shake, ScoreManager.AddBossKillScore() + 신규 BossUnlockManager.Unlock(bossId) 호출

FioraBoss(rename)에 남는 것 (F.I.O.R.A 전용):
- `BossState` enum, `_state` 필드, `PatternLoop()` 전체(line 127-191), `HitReactionAndReset()`(line 223-252), `RequiredHits = 7`/`_hitCount`, 관련 튜너블(`moveSpeed`/`telegraphDuration`/`vulnerableDuration`/`hitFlashColor`/`knockbackForce`/`resetPauseDuration`/`bossMaskRiseDuration`/`bossParticleColor`/`bossParticleBurstCount`/`bossDeathShakeDuration`/`bossDeathShakeAmplitude`), `_exclamationIcon`/`_meleeHitbox` 필드, `OnTriggerEnter2D`(line 278-285), `OnDashHit()`(line 195-219 — hit counter 로직 자체는 보스마다 다르므로 abstract로 base에 선언하지 않고 FioraBoss가 구현), `ClearHighlight()`의 빈틈 색상 분기(line 289-293).

IEnemy/ISpawnGatable 계약 (변경 금지):
Assets/Scripts/Enemy/IEnemy.cs: `bool IsAlive { get; } / void OnDashHit(); / void ClearHighlight();`
Assets/Scripts/Enemy/ISpawnGatable.cs: `void SetSpawnGate(bool isSpawning);`

기존 data-only static class 컨벤션 (BossUnlockManager가 따를 패턴):
Assets/Scripts/World/ScoreManager.cs: `public static class ScoreManager { public static int Score { get; private set; } ... }`

절대 건드리면 안 되는 리셋 스윕 경계 (Pitfall 6), Assets/Scripts/UI/DeathScreenController.cs:29-36:
```csharp
private void RestartGame()
{
    Time.timeScale         = 1f;
    Time.fixedDeltaTime    = 0.02f;
    FloorManager.CurrentFloor = 1;
    ScoreManager.Reset();
    SceneManager.LoadScene("AttackSelect");
}
```
BossUnlockManager는 이 메서드에서 절대 참조되지 않는다 — reset() 메서드 자체를 노출하지 않는다.

EnemyDeathEffect.cs 공개 API (Die()가 재사용):
```csharp
public IEnumerator PlayDeathSequence(Animator animator);
public void ConfigureIntensity(float maskRiseDuration, Color particleColor, int particleBurstCount);
```

BossEnemyPrefabBuilder.cs 수정 대상 (Assets/Editor/BossEnemyPrefabBuilder.cs:42):
```csharp
var boss = clone.AddComponent<BossEnemy>(); // → AddComponent<FioraBoss>()로 교체
```
`Assets/Editor/RoomBossFsmTestBuilder.cs`, `Assets/Editor/BossFsmTestPoolSwapTool.cs`는 `"Assets/Prefabs/Enemies/BossEnemy.prefab"` 경로 문자열만 참조하고 C# 타입 참조는 없음(grep으로 확인됨) — 수정 불필요, 건드리지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BossUnlockManager 신설</name>
  <files>Assets/Scripts/Progression/BossUnlockManager.cs</files>
  <read_first>
    - Assets/Scripts/World/ScoreManager.cs (data-only static class 컨벤션 확인)
    - Assets/Scripts/UI/DeathScreenController.cs (RestartGame()이 무엇을 리셋하는지, BossUnlockManager가 절대 여기 포함되면 안 됨을 확인)
  </read_first>
  <action>
    `Assets/Scripts/Progression/` 폴더를 신설하고 `BossUnlockManager.cs`를 작성한다:
    ```csharp
    using System.Collections.Generic;
    using UnityEngine;

    public static class BossUnlockManager
    {
        private const string PrefsKeyPrefix = "boss_unlock_";
        private static readonly Dictionary<string, bool> _cache = new Dictionary<string, bool>();

        public static bool IsUnlocked(string bossId)
        {
            if (_cache.TryGetValue(bossId, out var cached)) return cached;
            bool value = PlayerPrefs.GetInt(PrefsKeyPrefix + bossId, 0) == 1;
            _cache[bossId] = value;
            return value;
        }

        public static void Unlock(string bossId)
        {
            PlayerPrefs.SetInt(PrefsKeyPrefix + bossId, 1);
            PlayerPrefs.Save();
            _cache[bossId] = true;
        }
    }
    ```
    Reset/Clear 메서드는 절대 추가하지 않는다 — 이것이 구조적 격리(Pitfall 6)의 핵심이다.
  </action>
  <verify>
    <automated>grep -n "public static class BossUnlockManager" Assets/Scripts/Progression/BossUnlockManager.cs; grep -c "Unlock\|IsUnlocked" Assets/Scripts/Progression/BossUnlockManager.cs; grep -ic "reset\|clear" Assets/Scripts/Progression/BossUnlockManager.cs; grep -c "BossUnlockManager" Assets/Scripts/UI/DeathScreenController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public static class BossUnlockManager" Assets/Scripts/Progression/BossUnlockManager.cs` → 1개 매치
    - `grep -c "Unlock\|IsUnlocked" Assets/Scripts/Progression/BossUnlockManager.cs` → 합산 2 이상
    - `grep -ic "reset\|clear" Assets/Scripts/Progression/BossUnlockManager.cs` → 0 매치 (리셋 메서드 미노출 확인)
    - `grep -c "BossUnlockManager" Assets/Scripts/UI/DeathScreenController.cs` → 0 매치 (아직 참조 안 함 — 이 파일은 이번 태스크에서 건드리지 않는다)
  </acceptance_criteria>
  <done>BossUnlockManager.cs가 PlayerPrefs 기반 Unlock/IsUnlocked API만 노출하며 리셋 메서드가 없다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BossEnemyBase 추출 + BossEnemy.cs → FioraBoss.cs rename + Editor 도구 갱신</name>
  <files>Assets/Scripts/Enemy/Boss/BossEnemyBase.cs, Assets/Scripts/Enemy/Boss/FioraBoss.cs, Assets/Scripts/Enemy/Boss/FioraBoss.cs.meta, Assets/Editor/BossEnemyPrefabBuilder.cs</files>
  <read_first>
    - Assets/Scripts/Enemy/BossEnemy.cs (전체 — 추출 원본, 위 interfaces 블록의 line 번호 근거)
    - Assets/Scripts/Enemy/EnemyBase.cs (동일 프로젝트의 "최소 공통분모만 추출" 선례 — BossEnemyBase도 동일 철학)
    - Assets/Scripts/Enemy/IEnemy.cs, Assets/Scripts/Enemy/ISpawnGatable.cs (계약 변경 금지 확인)
    - Assets/Editor/BossEnemyPrefabBuilder.cs (line 42의 하드코딩된 타입 참조 확인)
    - `git status` 실행 결과 확인 — Assets/Prefabs/Enemies/BossEnemy.prefab, Assets/Prefabs/Rooms/Room_BossFsmTest/, Assets/Editor/RoomBossFsmTestBuilder.cs, Assets/Editor/BossFsmTestPoolSwapTool.cs는 Phase 15(v3.1, parked)의 미커밋 상태다 — 이번 태스크와 무관하니 정리/삭제/커밋하지 않는다. 단 BossEnemy.cs/BossEnemy.cs.meta 자체는 이미 git에 커밋된 추적 파일이다(git status에 나타나지 않음) — 이 두 파일만 이번 태스크의 rename 대상이다.
  </read_first>
  <action>
    **1) GUID 보존 필수 — 파일을 삭제 후 재생성하지 말고 반드시 `git mv`로 rename한다:**
    ```
    mkdir -p Assets/Scripts/Enemy/Boss
    git mv Assets/Scripts/Enemy/BossEnemy.cs Assets/Scripts/Enemy/Boss/FioraBoss.cs
    git mv Assets/Scripts/Enemy/BossEnemy.cs.meta Assets/Scripts/Enemy/Boss/FioraBoss.cs.meta
    ```
    이렇게 하면 `Assets/Prefabs/Enemies/BossEnemy.prefab`이 참조하는 GUID `cb839023c498e514cab6bb76ab11cde9`(FioraBoss.cs.meta 안의 `guid:` 값)가 그대로 보존되어 프리팹의 "missing script" 없이 새 클래스로 해석된다.

    **2) `Assets/Scripts/Enemy/Boss/BossEnemyBase.cs` 신규 작성** (`EnemyBase`를 상속하지 않는 독립 형제 클래스 — `IsAlive`가 "빈틈 여부"로 오버로드되어 `EnemyBase`의 생존/사망 시맨틱과 구조적으로 다르기 때문):
    ```csharp
    using System.Collections;
    using UnityEngine;

    public abstract class BossEnemyBase : MonoBehaviour, IEnemy, ISpawnGatable
    {
        protected Rigidbody2D _rb;
        protected Animator _animator;
        protected SpriteRenderer _sr;
        protected Transform _playerTransform;
        protected CameraFollow _cameraFollow;
        protected Coroutine _patternCoroutine;
        protected Color _baseColor;
        protected bool _isDefeated;

        public bool IsAlive { get; protected set; } = true;

        protected virtual void Awake()
        {
            _rb = GetComponent<Rigidbody2D>();
            _animator = GetComponent<Animator>();
            _sr = GetComponent<SpriteRenderer>();
            _baseColor = _sr.color;
            var player = FindFirstObjectByType<PlayerController>();
            _playerTransform = player != null ? player.transform : null;
            var cam = Camera.main;
            _cameraFollow = cam != null ? cam.GetComponent<CameraFollow>() : null;
        }

        protected virtual void Start()
        {
            if (IsAlive && _patternCoroutine == null && !_isDefeated)
                _patternCoroutine = StartCoroutine(PatternLoop());
        }

        protected virtual void OnEnable() { PlayerController.OnPlayerDeath += OnPlayerDied; }
        protected virtual void OnDisable() { PlayerController.OnPlayerDeath -= OnPlayerDied; }

        public void SetSpawnGate(bool isSpawning)
        {
            IsAlive = !isSpawning;
            if (!isSpawning && _patternCoroutine == null && !_isDefeated)
                _patternCoroutine = StartCoroutine(PatternLoop());
        }

        private void OnPlayerDied()
        {
            if (_patternCoroutine != null) { StopCoroutine(_patternCoroutine); _patternCoroutine = null; }
            OnPlayerDiedCleanup();
        }

        public void ClearHighlight()
        {
            if (_sr == null) return;
            _sr.color = GetHighlightColor();
        }

        protected abstract IEnumerator PatternLoop();
        protected abstract void OnPlayerDiedCleanup();
        protected abstract Color GetHighlightColor();

        protected virtual void Die(float maskRiseDuration, Color particleColor, int particleBurstCount,
            float shakeDuration, float shakeAmplitude, string bossId)
        {
            _isDefeated = true;
            IsAlive = false;
            if (_patternCoroutine != null) { StopCoroutine(_patternCoroutine); _patternCoroutine = null; }
            if (_rb != null) { _rb.linearVelocity = Vector2.zero; _rb.bodyType = RigidbodyType2D.Static; }
            foreach (var c in GetComponents<Collider2D>()) c.enabled = false;
            _animator?.SetBool("isDead", true);

            var deathEffect = GetComponent<EnemyDeathEffect>();
            if (deathEffect == null) deathEffect = gameObject.AddComponent<EnemyDeathEffect>();
            deathEffect.ConfigureIntensity(maskRiseDuration, particleColor, particleBurstCount);
            StartCoroutine(deathEffect.PlayDeathSequence(_animator));

            _cameraFollow?.Shake(shakeDuration, shakeAmplitude);
            ScoreManager.AddBossKillScore();
            BossUnlockManager.Unlock(bossId);
        }

        public abstract void OnDashHit();
    }
    ```

    **3) `Assets/Scripts/Enemy/Boss/FioraBoss.cs` 수정** — rename 직후 파일 내용을 다음과 같이 고친다:
    - 클래스 선언: `public class BossEnemy : MonoBehaviour, IEnemy, ISpawnGatable` → `public class FioraBoss : BossEnemyBase` (D-03: boss id 상수 `public const string BossId = "Fiora";` 추가)
    - 제거: `_isDefeated` 필드, `IsAlive` 프로퍼티 선언, `_rb`/`_animator`/`_sr`/`_playerTransform`/`_cameraFollow`/`_patternCoroutine`/`_baseColor` 필드 선언(전부 base의 protected 멤버 재사용), `Awake()`의 범용 캐싱 부분(단, `_meleeHitbox`/`_exclamationIcon` 초기 비활성화는 `override Awake()`에 남기고 `base.Awake()`를 먼저 호출), `OnEnable`/`OnDisable`(base 그대로 사용), `SetSpawnGate`(base 그대로 사용)
    - `Start()`: F.I.O.R.A 전용 추가 로직이 없으므로 override 자체를 삭제(base.Start()가 그대로 상속되어 동작)
    - `OnPlayerDied()` → `protected override void OnPlayerDiedCleanup() { if (_exclamationIcon != null) _exclamationIcon.enabled = false; if (_meleeHitbox != null) _meleeHitbox.enabled = false; _animator?.SetBool("isMoving", false); }`
    - `PatternLoop()`: `protected override IEnumerator PatternLoop()` — 몸체는 그대로, `IsAlive`/`_isDefeated`/`_rb`/`_sr`/`_animator`/`_playerTransform`은 base의 protected 멤버이므로 그대로 컴파일된다.
    - `OnDashHit()`: `public override void OnDashHit()` — 기존 몸체 그대로 두되, `_isDefeated = true; IsAlive = false; Die();` 세 줄을 `Die(bossMaskRiseDuration, bossParticleColor, bossParticleBurstCount, bossDeathShakeDuration, bossDeathShakeAmplitude, BossId);` 한 줄로 교체(base.Die가 _isDefeated/IsAlive 설정과 점수+해금까지 전부 수행).
    - 기존 `Die()` 메서드 전체 삭제(base로 이관됨).
    - `ClearHighlight()` → `protected override Color GetHighlightColor() => (_state == BossState.Vulnerable) ? vulnerableTintColor : _baseColor;` (public ClearHighlight() 자체는 base가 구현하므로 FioraBoss에서 삭제)
    - `HitReactionAndReset()`, `BossState` enum, `_state` 필드, `RequiredHits`/`_hitCount`, `OnTriggerEnter2D`, 모든 F.I.O.R.A 전용 튜너블 필드는 그대로 유지.

    **4) `Assets/Editor/BossEnemyPrefabBuilder.cs` 수정**: line 42 `var boss = clone.AddComponent<BossEnemy>();` → `var boss = clone.AddComponent<FioraBoss>();`. 이 파일의 다른 부분(경로 상수, 프리팹 빌드 로직)은 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -n "cb839023c498e514cab6bb76ab11cde9" Assets/Scripts/Enemy/Boss/FioraBoss.cs.meta; grep -rc "class BossEnemy\b" Assets/Scripts/; grep -n "class FioraBoss : BossEnemyBase" Assets/Scripts/Enemy/Boss/FioraBoss.cs; grep -n "abstract class BossEnemyBase" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs; grep -c ": EnemyBase" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs; grep -n "AddComponent<FioraBoss>" Assets/Editor/BossEnemyPrefabBuilder.cs; grep -c "AddComponent<BossEnemy>" Assets/Editor/BossEnemyPrefabBuilder.cs; grep -n "BossUnlockManager.Unlock" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cb839023c498e514cab6bb76ab11cde9" Assets/Scripts/Enemy/Boss/FioraBoss.cs.meta` → 1개 매치 (GUID 보존 확인)
    - `grep -rc "class BossEnemy\b" Assets/Scripts/` → 0 (구 클래스명 완전 제거)
    - `grep -n "class FioraBoss : BossEnemyBase" Assets/Scripts/Enemy/Boss/FioraBoss.cs` → 1개 매치
    - `grep -n "abstract class BossEnemyBase" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs` → 1개 매치, `grep -c ": EnemyBase" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs` → 0 (EnemyBase를 상속하지 않는 형제 클래스 확인)
    - `grep -n "AddComponent<FioraBoss>" Assets/Editor/BossEnemyPrefabBuilder.cs` → 1개 매치, `grep -c "AddComponent<BossEnemy>" Assets/Editor/BossEnemyPrefabBuilder.cs` → 0
    - `grep -n "BossUnlockManager.Unlock" Assets/Scripts/Enemy/Boss/BossEnemyBase.cs` → 1개 매치
    - `git status --porcelain -- Assets/Editor/RoomBossFsmTestBuilder.cs Assets/Editor/BossFsmTestPoolSwapTool.cs` → 빈 출력 (건드리지 않았음 확인)
    - Unity Editor를 열어 콘솔에 컴파일 에러가 0건임을 수동 확인 (D-04 — 자동 빌드 파이프라인 없음)
  </acceptance_criteria>
  <done>BossEnemyBase.cs가 범용 plumbing만 담고 있고, FioraBoss.cs(rename)가 F.I.O.R.A 전용 패턴 루프만 담고 있으며, 프리팹 GUID가 보존되고, BossEnemyPrefabBuilder.cs가 새 타입명으로 컴파일된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: INFRA-03 + UNLOCK-01 수동 플레이테스트 (D-04)</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/18-shared-infra/18-VALIDATION.md (Manual-Only Verifications 표, INFRA-03/UNLOCK-01 행)
    - .planning/ROADMAP.md §Phase 18 Success Criteria #2, #3
  </read_first>
  <action>
    defeat-guard/패턴 리셋/사망 연출/PlayerPrefs 영속성은 자동화 테스트 프레임워크가 없어(D-04) 실제 Play로만 확인 가능하다 — 사용자에게 아래 how-to-verify 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 BossEnemyBase.cs/FioraBoss.cs의 어느 훅(PatternLoop/Die/GetHighlightColor/OnPlayerDiedCleanup)이 원본 BossEnemy.cs 로직과 달라졌는지 diff를 재검토해 수정한다.
  </action>
  <what-built>BossEnemyBase/FioraBoss 추출과 BossUnlockManager가 완료되었다 — Room_BossFsmTest(Phase 15 격리 테스트 룸)에서 실제로 보스를 처치해 defeat-guard/사망시퀀스/스폰게이팅/하이라이트가 재작성 없이 동작하는지, PlayerPrefs 해금 플래그가 앱 재시작 후에도 유지되는지 확인이 필요하다.</what-built>
  <how-to-verify>
    Unity Editor에서 (Phase 15 잔여 테스트 도구를 그대로 사용):
    1. 컴파일 에러 없이 Play 진입 가능한지 확인.
    2. `Fast/Phase15/Build BossEnemy Prefab` → `Fast/Phase15/Wire Boss Into BossFsmTest Room` 메뉴가 여전히 정상 실행되는지 확인(FioraBoss로 AddComponent해도 동일하게 동작해야 함).
    3. `BossFsmTest_Teleporter`를 통해 Room_BossFsmTest에 진입, FioraBoss를 7회 피격으로 처치 — 1~6회차는 패턴이 처음(Telegraph)부터 재시작되는지, 빈틈(Vulnerable) 상태가 아닐 때는 대시 타겟이 되지 않는지 확인.
    4. 7회째 피격에서 사망 연출(파티클+마스크 페이드)과 카메라 쉐이크, ScoreManager 보스 보너스 점수 가산이 재생되는지 확인.
    5. 처치 직후 `Debug.Log(BossUnlockManager.IsUnlocked("Fiora"))`를 임시로 추가하거나 Unity Editor의 PlayerPrefs를 직접 확인(Windows: 레지스트리 `HKCU\Software\DefaultCompany\Fast`)해 `boss_unlock_Fiora` 값이 1로 기록됐는지 확인.
    6. Play 모드를 완전히 정지했다가 다시 시작 — 해금 플래그가 여전히 유지되는지 재확인(앱 재시작 시뮬레이션).
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — defeat-guard/패턴 리셋/사망 연출/PlayerPrefs 영속성은 시각 판정 및 프로세스 재시작이 필요해 자동화 불가 (18-RESEARCH.md Validation Architecture, D-04 확정). 통과 기준: 사용자 리포트에서 체크리스트 1~6 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~6번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 몇 번째 히트에서, 어떤 동작이 예상과 달랐는지 구체적으로 기록된다
  </acceptance_criteria>
  <done>ROADMAP.md Phase 18 Success Criteria #2, #3이 수동 플레이테스트로 통과 확인됨.</done>
  <resume-signal>모두 통과하면 "approved", 문제가 있으면 어느 항목이 실패했는지 구체적으로 설명해 주세요.</resume-signal>
</task>

</tasks>

<verification>
- BossEnemyBase.cs가 EnemyBase를 상속하지 않는 독립 abstract 클래스로 존재하고 IEnemy/ISpawnGatable을 구현한다.
- FioraBoss.cs(rename)가 BossEnemyBase를 상속하며 F.I.O.R.A 전용 패턴 루프만 담고 있다.
- BossEnemy.prefab의 GUID 참조가 깨지지 않았다(FioraBoss.cs.meta의 guid 값 확인).
- BossUnlockManager.cs가 DeathScreenController.RestartGame()에서 참조되지 않는다.
- Task 3 수동 플레이테스트 체크리스트 전부 통과.
</verification>

<success_criteria>
ROADMAP.md Phase 18 Success Criteria #2, #3 — "BossEnemyBase를 상속한 신규 보스 클래스가 defeat-guard/사망 시퀀스/스폰 게이팅/피격 하이라이트를 별도 재작성 없이 즉시 사용할 수 있다"와 "보스를 격파하면 PlayerPrefs에 해금 플래그가 기록되고, 앱을 완전히 재시작해도 해금 상태가 유지된다"가 수동 플레이테스트로 확인됨.
</success_criteria>

<output>
After completion, create `.planning/phases/18-shared-infra/18-02-SUMMARY.md`
</output>
