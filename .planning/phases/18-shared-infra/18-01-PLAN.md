---
phase: 18-shared-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/Combat/IPlayerCombatModule.cs
  - Assets/Scripts/Player/Combat/CombatContext.cs
  - Assets/Scripts/Player/Combat/OverclockModule.cs
  - Assets/Scripts/Player/CombatController.cs
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "Attack 버튼을 홀드하면 슬로우모션+범위표시가 마이그레이션 이전과 동일하게 동작한다"
    - "Attack 버튼을 릴리즈했을 때 범위 내 가장 가까운 적에게 즉시 대시해 원샷 처치한다 (마이그레이션 이전과 동일)"
    - "범위 내 적이 없는 상태로 릴리즈하면 헛치기 애니메이션과 postKillLockout보다 긴 락아웃이 발동한다"
    - "게이지가 소진되면 슬로우모션은 자동 종료되지만 릴리즈 시 대시는 여전히 가능하다"
    - "CombatController는 타겟팅(FindTarget)과 처치/헛치기 판정(Resolve/Whiff)을 IPlayerCombatModule 인터페이스를 통해서만 수행하고, 두 로직을 자체 private 메서드로 더 이상 보유하지 않는다"
  artifacts:
    - path: "Assets/Scripts/Player/Combat/IPlayerCombatModule.cs"
      provides: "전투 모듈 계약 (FindTarget/Resolve/Whiff)"
      contains: "interface IPlayerCombatModule"
    - path: "Assets/Scripts/Player/Combat/CombatContext.cs"
      provides: "모듈이 필요로 하는 공유 참조/튜너블/콜백을 담는 순수 C# 클래스"
      contains: "public class CombatContext"
    - path: "Assets/Scripts/Player/Combat/OverclockModule.cs"
      provides: "기존 Overclock(F.I.O.R.A) 로직의 verbatim 이관 — IPlayerCombatModule 최초/유일 구현체"
      contains: "class OverclockModule : IPlayerCombatModule"
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "_activeModule 필드를 통해 OverclockModule을 host하는 모듈화된 컨트롤러"
      contains: "IPlayerCombatModule _activeModule"
  key_links:
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/Player/Combat/OverclockModule.cs"
      via: "_activeModule 필드 (FindTarget/Resolve/Whiff 호출)"
      pattern: "_activeModule\\.(FindTarget|Resolve|Whiff)"
    - from: "Assets/Scripts/Player/CombatController.cs"
      to: "Assets/Scripts/Player/Combat/CombatContext.cs"
      via: "_ctx 필드 생성 및 모듈 호출 시 전달"
      pattern: "new CombatContext"
---

<objective>
INFRA-01: `CombatController`에 하드코딩되어 있던 기존 Overclock(F.I.O.R.A) 전투 로직(타겟팅 + 대시처치/헛치기 판정)을 `IPlayerCombatModule` 인터페이스 뒤로 무손상 이관한다. 슬로우모션 lifecycle, 게이지, `_isBusy` 락아웃, 입력 폴링은 `CombatController`에 그대로 남아 host 역할을 하고, `OverclockModule`이 이 인터페이스의 최초(이자 이번 Phase에서는 유일한) 구현체가 된다.

Purpose: Phase 19-23에서 SAMURAI/DeadEye/MAX/NOVA 각각의 전투 모듈을 `CombatController`의 핵심 루프를 더 이상 건드리지 않고 추가할 수 있는 기반을 마련한다. D-04(18-CONTEXT.md)에 따라 이번 마이그레이션은 로직 변경 없는 verbatim move이며, 회귀 검증은 수동 플레이테스트로만 진행한다.
Output: `Assets/Scripts/Player/Combat/` 신규 폴더에 `IPlayerCombatModule.cs`/`CombatContext.cs`/`OverclockModule.cs` 3개 파일, `CombatController.cs`는 host로 축소.
</objective>

<execution_context>
@D:\새 폴더\Fast\.claude\get-shit-done\workflows\execute-plan.md
@D:\새 폴더\Fast\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-shared-infra/18-CONTEXT.md
@.planning/phases/18-shared-infra/18-RESEARCH.md
@.planning/phases/18-shared-infra/18-VALIDATION.md

<interfaces>
<!-- CombatController.cs 현재 전체 구조 (Assets/Scripts/Player/CombatController.cs, 435 lines) —
     아래 line 번호는 이번 세션에 직접 읽은 현재 소스 기준. 이관 대상을 정확히 식별하기 위해 인용한다. -->

이관 대상 (verbatim move, OverclockModule.cs로 이동 후 CombatController.cs에서 삭제):
- `ExecuteDash(IEnemy target)` — lines 251-301 (단, line 260의 `ExitSlowMotion();` 호출은 host에 남기고 DashOrWhiff()가 모듈 호출 직전에 대신 호출한다 — 이미 Update()의 release 분기(line 162-163)에서 한 번 호출되므로 사실상 안전한 idempotent no-op 재확인이다)
- `ExecuteWhiff()` — lines 303-310 (line 309의 `whiffLockout` 필드 사용 — CombatContext에 `WhiffLockout`으로 이관)
- `HitFreeze(float realSeconds)` — lines 312-322 (OverclockModule 내부 private 메서드로 verbatim 이동 — `StartCoroutine()` 없이 `yield return HitFreeze(...)`로 직접 호출 가능, Unity 코루틴 엔진은 중첩 IEnumerator를 자동으로 끝까지 구동한다)
- `SpawnHitSpark(Vector2 position)` — lines 338-343
- `GetMouseWorldDirection(Vector2 origin)` — lines 351-358 (D-02: `Mouse.current` 사용 방식 자체는 절대 변경하지 않는다 — 위치만 이동)
- `FindNearestEnemyInRange()` — lines 360-402
- `IsInAttackShape(...)` — lines 425-434

CombatController.cs에 그대로 남는 것 (host, 변경 없음):
- `Update()` 루프 (lines 108-167) — 단, line 144 `FindNearestEnemyInRange()` 호출과 line 237 fallback 호출은 `_activeModule.FindTarget(...)`로 교체
- `EnterSlowMotion()`/`ExitSlowMotion()`/`ExitAttackPending()`/`ForceExitCombatState()` (lines 175-225) — 완전히 그대로
- `DashOrWhiff(IEnemy cachedTarget)` (lines 229-249) — 아래 action의 정확한 교체 지시 참고
- `UpdateHighlight(IEnemy nearest)` (lines 408-418) — 그대로 (모듈-무관 프레젠테이션)
- `ConfigureTrailVisuals(TrailRenderer trail)` (lines 326-336) — 그대로, Awake()에서 호출
- 모든 `[SerializeField]` 튜너블 필드 (lines 26-44, `whiffLockout` 포함) — Inspector 노출은 CombatController에 유지, CombatContext 생성 시 값만 복사

<!-- IEnemy 계약 (변경 금지) -->
Assets/Scripts/Enemy/IEnemy.cs:
```csharp
public interface IEnemy
{
    bool IsAlive { get; }
    void OnDashHit();
    void ClearHighlight();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: IPlayerCombatModule + CombatContext 계약 정의</name>
  <files>Assets/Scripts/Player/Combat/IPlayerCombatModule.cs, Assets/Scripts/Player/Combat/CombatContext.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (전체 — 위 interfaces 블록의 line 번호 근거 확인, 특히 line 30 `whiffLockout` 필드와 line 309 사용처)
    - Assets/Scripts/Enemy/IEnemy.cs (FindTarget이 반환하는 타입, 변경 금지 계약 확인)
  </read_first>
  <action>
    `Assets/Scripts/Player/Combat/` 폴더를 신설하고 다음 두 파일을 작성한다 (아직 구현체는 만들지 않음 — 순수 계약 정의).

    `IPlayerCombatModule.cs`:
    ```csharp
    using System.Collections;

    public interface IPlayerCombatModule
    {
        IEnemy FindTarget(UnityEngine.Vector2 origin, CombatContext ctx);
        IEnumerator Resolve(IEnemy target, CombatContext ctx);
        IEnumerator Whiff(CombatContext ctx);
    }
    ```

    `CombatContext.cs`:
    ```csharp
    using UnityEngine;

    public class CombatContext
    {
        public Rigidbody2D Rb;
        public SpriteRenderer SpriteRenderer;
        public Animator Animator;
        public TrailRenderer TrailRenderer;
        public InvincibilityHandler Invincibility;
        public CameraFollow CameraFollow;
        public ChronoGaugeController Gauge;
        public Camera MainCamera;

        public GameObject HitSparkPrefab;
        public float DashDuration;
        public float HitFreezeDuration;
        public float PostKillLockout;
        public float WhiffLockout;
        public float CameraShakeDuration;
        public float CameraShakeAmplitude;
        public float SearchRadius;
        public float FanRadius;
        public float FanHalfAngleDeg;
        public float LinearHalfAngleDeg;

        public ContactFilter2D EnemyFilter;
        public Collider2D[] HitBuffer;
        public int ObstacleMask;

        public System.Action<float> SetAttackCooldown;
    }
    ```

    `WhiffLockout`은 `OverclockModule.Whiff()`가 `ExecuteWhiff()`(CombatController.cs line 303-310)의 `WaitForSecondsRealtime(whiffLockout)` 호출을 verbatim 이관할 때 필요한 필드다 — 반드시 포함한다.

    Reset/Clear 메서드나 추가 로직은 넣지 않는다 — 순수 데이터 홀더다.
  </action>
  <verify>
    <automated>grep -c "IEnemy FindTarget\|IEnumerator Resolve\|IEnumerator Whiff" Assets/Scripts/Player/Combat/IPlayerCombatModule.cs; grep -n "SetAttackCooldown" Assets/Scripts/Player/Combat/CombatContext.cs; grep -n "WhiffLockout" Assets/Scripts/Player/Combat/CombatContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/Player/Combat/IPlayerCombatModule.cs` 파일이 존재하고 `interface IPlayerCombatModule`을 선언하며 정확히 3개 멤버(`FindTarget`, `Resolve`, `Whiff`)를 갖는다 — grep 카운트 3
    - `Assets/Scripts/Player/Combat/CombatContext.cs` 파일이 존재하고 `public class CombatContext`를 선언하며 `System.Action<float> SetAttackCooldown` 필드와 `public float WhiffLockout` 필드를 포함한다 (Task 2의 `OverclockModule.Whiff()`가 이 필드에 의존한다)
    - 이 시점에는 아직 어떤 구현체도 없으므로 CombatController.cs는 수정하지 않는다 — `git diff --stat Assets/Scripts/Player/CombatController.cs` → 빈 출력
  </acceptance_criteria>
  <done>두 계약 파일이 정확한 시그니처로 존재하고(WhiffLockout 필드 포함), 기존 CombatController.cs는 아직 무변경 상태다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OverclockModule 구현 + CombatController를 host로 재배선</name>
  <files>Assets/Scripts/Player/Combat/OverclockModule.cs, Assets/Scripts/Player/CombatController.cs</files>
  <read_first>
    - Assets/Scripts/Player/CombatController.cs (전체 — 이관 대상 정확한 코드 확인, 특히 lines 251-434 및 line 30의 `whiffLockout` 필드 선언)
    - Assets/Scripts/Player/Combat/IPlayerCombatModule.cs, CombatContext.cs (Task 1 산출물 — 이 시그니처를 그대로 구현, `WhiffLockout` 필드 포함 확인)
  </read_first>
  <action>
    **1) `OverclockModule.cs` 신규 작성** — `IPlayerCombatModule`을 구현하는 순수 C# 클래스(MonoBehaviour 아님):
    - `FindTarget(Vector2 origin, CombatContext ctx)`: 기존 `FindNearestEnemyInRange()`(line 360-402) + `IsInAttackShape(...)`(line 425-434) + `GetMouseWorldDirection(origin)`(line 351-358, private helper로 그대로 이동, `ctx.MainCamera` 사용)를 로직 변경 없이 이 메서드 안으로 옮긴다. `searchRadius`/`fanRadius`/`fanHalfAngleDeg`/`linearHalfAngleDeg`/`_enemyFilter`/`_hitBuffer`/`_obstacleMask`는 전부 `ctx.SearchRadius`/`ctx.FanRadius`/`ctx.FanHalfAngleDeg`/`ctx.LinearHalfAngleDeg`/`ctx.EnemyFilter`/`ctx.HitBuffer`/`ctx.ObstacleMask`로 치환. `AttackTypeSelector.Selected` 참조는 그대로 유지(정적 클래스, ctx 불필요).
    - `Resolve(IEnemy target, CombatContext ctx)`: 기존 `ExecuteDash(IEnemy target)`(line 251-301)의 몸체를 verbatim 이동하되 **line 260의 `ExitSlowMotion();` 호출은 제외**(host가 호출 전에 이미 처리). `_rb`/`_spriteRenderer`/`_animator`/`_invincibilityHandler`/`_trailRenderer`/`_cameraFollow`/`_gauge`는 각각 `ctx.Rb`/`ctx.SpriteRenderer`/`ctx.Animator`/`ctx.Invincibility`/`ctx.TrailRenderer`/`ctx.CameraFollow`/`ctx.Gauge`로 치환. `dashDuration`/`hitFreezeDuration`/`postKillLockout`/`_cameraShakeDuration`/`_cameraShakeAmplitude`/`_hitSparkPrefab`도 각각 `ctx.DashDuration` 등으로 치환. 마지막의 `_attackCooldown = postKillLockout;`은 `ctx.SetAttackCooldown(ctx.PostKillLockout);`으로, `_gauge.AddKillBonus();`는 `ctx.Gauge.AddKillBonus();`로 치환. `SpawnHitSpark(destination)`(line 339-343)과 `HitFreeze(hitFreezeDuration)`(line 312-322, `yield return StartCoroutine(...)` → `yield return HitFreeze(ctx.HitFreezeDuration);`로 변경 — StartCoroutine 없이 직접 nested IEnumerator로 호출, Unity 코루틴 엔진이 자동으로 끝까지 구동한다)는 OverclockModule의 private 메서드로 그대로 이동.
    - `Whiff(CombatContext ctx)`: 기존 `ExecuteWhiff()`(line 303-310) 몸체를 verbatim 이동, `_animator` → `ctx.Animator`, `whiffLockout` → `ctx.WhiffLockout`(line 309의 `WaitForSecondsRealtime(whiffLockout)` → `WaitForSecondsRealtime(ctx.WhiffLockout)`).

    **2) `CombatController.cs` 수정**:
    - 신규 필드 추가: `private IPlayerCombatModule _activeModule; private CombatContext _ctx;`
    - `Awake()` 끝부분(기존 컴포넌트 캐싱 이후)에 `_activeModule = new OverclockModule();`과 `_ctx = new CombatContext { ... };`를 추가 — CombatContext의 모든 필드에 Awake()에서 이미 캐시된 값(`_rb`, `_spriteRenderer`, `_animator`, `_trailRenderer`, `_invincibilityHandler`, `_cameraFollow`, `_gauge`, `_mainCamera`, `_hitSparkPrefab`, `dashDuration`, `hitFreezeDuration`, `postKillLockout`, `whiffLockout`, `_cameraShakeDuration`, `_cameraShakeAmplitude`, `searchRadius`, `fanRadius`, `fanHalfAngleDeg`, `linearHalfAngleDeg`, `_enemyFilter`, `_hitBuffer`, `_obstacleMask`)를 대입하고(`_ctx.WhiffLockout = whiffLockout;` 포함), `SetAttackCooldown = sec => _attackCooldown = sec`를 람다로 대입.
    - `Update()` line 144: `UpdateHighlight(FindNearestEnemyInRange());` → `UpdateHighlight(_activeModule.FindTarget((Vector2)transform.position, _ctx));`
    - `DashOrWhiff(IEnemy cachedTarget)`(line 229-249): line 237의 fallback `FindNearestEnemyInRange()` → `_activeModule.FindTarget((Vector2)transform.position, _ctx)`로 교체. line 240의 `yield return ExecuteDash(target);` 직전에 `ExitSlowMotion();`을 명시적으로 호출한 뒤 `yield return _activeModule.Resolve(target, _ctx);`로 교체. line 244의 `yield return ExecuteWhiff();` → `yield return _activeModule.Whiff(_ctx);`로 교체.
    - 이관 완료된 다음 private 메서드를 CombatController.cs에서 완전히 삭제: `ExecuteDash`, `ExecuteWhiff`, `HitFreeze`, `SpawnHitSpark`, `GetMouseWorldDirection`, `FindNearestEnemyInRange`, `IsInAttackShape`.
    - `ConfigureTrailVisuals`, `UpdateHighlight`, `EnterSlowMotion`, `ExitSlowMotion`, `ExitAttackPending`, `ForceExitCombatState`, `Update()`의 나머지 로직은 그대로 둔다.
    - `whiffLockout` `[SerializeField]` 필드 자체는 CombatController.cs에서 삭제하지 않는다 — Inspector 튜닝 값의 원본이며 Awake()에서 `_ctx.WhiffLockout`으로 복사되는 소스다.

    **주의 — `_activeModule.` 호출 지점은 총 4곳이다** (아래 acceptance_criteria 참고): `Update()`의 `FindTarget` 1곳 + `DashOrWhiff()`의 fallback `FindTarget` 1곳 + `Resolve` 1곳 + `Whiff` 1곳. 캐싱해서 3곳으로 줄이려 하지 말 것 — `Update()`의 하이라이트 갱신용 조회와 `DashOrWhiff()`의 release 시점 fallback 조회는 서로 다른 시점(호버 중 vs 릴리즈 확정 순간)에 실행되므로 결과를 공유할 수 없다.
  </action>
  <verify>
    <automated>grep -n "class OverclockModule" Assets/Scripts/Player/Combat/OverclockModule.cs; grep -n "ctx.WhiffLockout" Assets/Scripts/Player/Combat/OverclockModule.cs; grep -c "ExecuteDash\|ExecuteWhiff(\|FindNearestEnemyInRange\|IsInAttackShape\|GetMouseWorldDirection" Assets/Scripts/Player/CombatController.cs; grep -c "_activeModule\." Assets/Scripts/Player/CombatController.cs; grep -n "new CombatContext" Assets/Scripts/Player/CombatController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class OverclockModule" Assets/Scripts/Player/Combat/OverclockModule.cs` → `class OverclockModule : IPlayerCombatModule` 매치
    - `grep -n "ctx.WhiffLockout" Assets/Scripts/Player/Combat/OverclockModule.cs` → 1개 이상 매치 (Whiff()가 whiffLockout을 ctx 경유로 사용)
    - `grep -c "ExecuteDash\|ExecuteWhiff(\|FindNearestEnemyInRange\|IsInAttackShape\|GetMouseWorldDirection" Assets/Scripts/Player/CombatController.cs` → 0 (전부 이관되어 CombatController.cs에는 흔적 없음)
    - `grep -c "_activeModule\." Assets/Scripts/Player/CombatController.cs` → 4 (`Update()`의 FindTarget 1곳, `DashOrWhiff()`의 fallback FindTarget 1곳, Resolve 1곳, Whiff 1곳 — 총 4개 호출 지점)
    - `grep -n "new CombatContext" Assets/Scripts/Player/CombatController.cs` → 1개 매치 (Awake 내부)
    - Unity Editor를 열어 콘솔에 컴파일 에러가 0건임을 수동 확인 (D-04 — 자동 빌드 파이프라인 없음)
  </acceptance_criteria>
  <done>OverclockModule.cs가 기존 로직을 손실 없이 담고 있고(whiffLockout 포함), CombatController.cs는 _activeModule/_ctx를 통해서만 타겟팅/처치를 수행하며 컴파일 에러가 없다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: INFRA-01 회귀 없음 수동 플레이테스트 (D-04)</name>
  <files>없음 (검증 전용 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/18-shared-infra/18-VALIDATION.md (Manual-Only Verifications 표, INFRA-01 행)
    - .planning/ROADMAP.md §Phase 18 Success Criteria #1
  </read_first>
  <action>
    슬로우모션 홀드/릴리즈 타이밍, 대시 이동감, 헛치기 페널티 체감은 자동화 테스트 프레임워크가 없어(D-04) 시각/조작 판정만 가능하다 — 사용자에게 아래 how-to-verify 체크리스트 플레이테스트를 요청하고 결과를 수집한다. 문제가 보고되면 OverclockModule.cs/CombatContext.cs의 어느 필드/치환이 원본 CombatController.cs 로직과 달라졌는지 diff를 재검토해 수정한다.
  </action>
  <what-built>CombatController의 Overclock 전투 로직이 IPlayerCombatModule/OverclockModule로 이관되었다 — 자동 테스트 없이 실제 플레이로 기존 동작과 100% 동일한지 확인이 필요하다.</what-built>
  <how-to-verify>
    SampleScene을 Play한 뒤 다음을 순서대로 확인한다 (18-VALIDATION.md Manual-Only Verifications 표 그대로):
    1. Attack 버튼을 홀드 — 슬로우모션과 범위 표시(Linear/Fan)가 마이그레이션 이전과 동일하게 나타나는지 확인.
    2. 적이 범위 안에 있는 상태로 릴리즈 — 즉시 대시하여 원샷 처치되는지, 히트스파크/카메라 쉐이크/힛프리즈가 재생되는지 확인.
    3. 적이 범위 밖인 상태로 릴리즈 — 헛치기 애니메이션이 재생되고 처치 시보다 명백히 긴 락아웃이 걸리는지 확인.
    4. Attack을 홀드한 채 게이지가 완전히 소진될 때까지 대기 — 슬로우모션은 자동 종료되지만 그 상태에서 릴리즈하면 여전히 대시가 발동하는지 확인.
    5. Unity 콘솔에 컴파일 에러/런타임 예외가 없는지 확인.
  </how-to-verify>
  <verify>
    <automated>MANUAL-ONLY — 슬로우모션/대시/헛치기 체감은 시각·조작 판정만 가능 (18-RESEARCH.md Validation Architecture, D-04 확정). 통과 기준: 사용자 리포트에서 체크리스트 1~5 전부 통과</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트 1~5번 전부 통과 보고 수신
    - 문제가 보고된 항목이 있다면 어느 단계(홀드/릴리즈/헛치기/게이지소진)에서 실패했는지 구체적으로 기록된다
  </acceptance_criteria>
  <done>ROADMAP.md Phase 18 Success Criteria #1이 수동 플레이테스트로 통과 확인됨.</done>
  <resume-signal>모두 통과하면 "approved", 문제가 있으면 어느 체크리스트 항목이 실패했는지 구체적으로 설명해 주세요.</resume-signal>
</task>

</tasks>

<verification>
- CombatController.cs가 `ExecuteDash`/`ExecuteWhiff`/`FindNearestEnemyInRange`/`IsInAttackShape`/`GetMouseWorldDirection`/`HitFreeze`/`SpawnHitSpark`를 더 이상 직접 소유하지 않고 `_activeModule`을 통해서만 위임한다.
- OverclockModule.cs가 IPlayerCombatModule을 구현하며 위 로직을 손실 없이 담고 있다(whiffLockout 포함).
- Task 3 수동 플레이테스트 체크리스트 5개 항목 전부 통과.
</verification>

<success_criteria>
ROADMAP.md Phase 18 Success Criteria #1 — "기존 F.I.O.R.A(Overclock) 전투 흐름이 IPlayerCombatModule 마이그레이션 이후에도 회귀 없이 동일하게 동작한다"가 수동 플레이테스트로 확인됨.
</success_criteria>

<output>
After completion, create `.planning/phases/18-shared-infra/18-01-SUMMARY.md`
</output>
