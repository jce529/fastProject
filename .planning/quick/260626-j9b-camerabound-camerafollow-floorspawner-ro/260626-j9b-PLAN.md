---
phase: quick-260626-j9b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Camera/CameraBound.cs
  - Assets/Scripts/Camera/CameraFollow.cs
  - Assets/Scripts/World/FloorSpawner.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "CameraBound 컴포넌트를 Room 자식 오브젝트에 부착하면 씬 뷰에서 시안색 와이어박스가 표시된다"
    - "CameraFollow.SnapToRoom(Bounds)를 호출하면 orthographicSize가 Bounds에 맞게 자동 계산된다"
    - "CameraBound가 있는 Room으로 전환 시 카메라가 Bounds 기준으로 고정되고, 없는 Room은 기존 fallback 동작을 유지한다"
  artifacts:
    - path: Assets/Scripts/Camera/CameraBound.cs
      provides: "룸 카메라 뷰 영역 정의 컴포넌트"
      exports: [CameraBound, GetWorldBounds]
    - path: Assets/Scripts/Camera/CameraFollow.cs
      provides: "Bounds 오버로드 SnapToRoom"
      exports: [SnapToRoom(Bounds)]
    - path: Assets/Scripts/World/FloorSpawner.cs
      provides: "CameraBound 우선/fallback 스냅 헬퍼"
  key_links:
    - from: FloorSpawner.SnapCameraToRoom
      to: CameraBound.GetWorldBounds
      via: room.GetComponentInChildren<CameraBound>()
    - from: CameraFollow.SnapToRoom(Bounds)
      to: _camera.orthographicSize
      via: Mathf.Max(orthoH, orthoW)
---

<objective>
각 Room 프리팹에 붙이는 CameraBound 컴포넌트를 신규 생성하고, CameraFollow와 FloorSpawner를 수정해 룸 전환 시 CameraBound 크기 기반으로 카메라 orthographicSize를 자동 계산한다.

Purpose: Inspector에서 roomOrthoSize 수치를 룸마다 직접 조정하는 대신, Room 프리팹 자체가 자신의 카메라 뷰를 정의하게 해 유지보수성을 높인다.
Output: CameraBound.cs (신규), CameraFollow.cs (오버로드 추가), FloorSpawner.cs (헬퍼 + 호출부 교체)
</objective>

<context>
@Assets/Scripts/Camera/CameraFollow.cs
@Assets/Scripts/World/FloorSpawner.cs

<interfaces>
<!-- CameraFollow 현재 퍼블릭 API -->
public void SnapToRoom(Vector3 worldCenter)   // 기존 — fallback으로 유지
private bool _roomMode;
private Camera _camera;
[SerializeField] private Vector3 offset;      // offset.z = -10f (카메라 깊이)
[SerializeField] private float roomOrthoSize; // 기존 Inspector 필드 — 오버로드 미사용 시 그대로 동작

<!-- FloorSpawner SnapToRoom 호출 2곳 -->
// Awake():
_cameraFollow?.SnapToRoom(new Vector3(0f, _roomCameraOffsetY, 0f));

// FloorTransitionSequence():
float newRoomBaseY = (FloorManager.CurrentFloor - 1) * _roomHeight;
_cameraFollow?.SnapToRoom(new Vector3(0f, newRoomBaseY + _roomCameraOffsetY, 0f));
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraBound.cs 신규 생성 + CameraFollow.cs SnapToRoom(Bounds) 오버로드 추가</name>
  <files>Assets/Scripts/Camera/CameraBound.cs, Assets/Scripts/Camera/CameraFollow.cs</files>
  <action>
**CameraBound.cs 신규 생성** (`Assets/Scripts/Camera/CameraBound.cs`):

```csharp
using UnityEngine;

/// <summary>
/// Room 프리팹 자식 오브젝트에 부착해 카메라 뷰 영역을 정의한다.
/// CameraFollow.SnapToRoom(Bounds) 가 이 값을 읽어 orthographicSize를 자동 계산한다.
/// BoxCollider2D 불필요 — 물리 개입 없음.
/// </summary>
public class CameraBound : MonoBehaviour
{
    [SerializeField] private Vector2 _size = new Vector2(20f, 12f);

    /// <summary>
    /// 월드 좌표 기준 카메라 뷰 Bounds를 반환한다.
    /// center = transform.position, size = _size.
    /// </summary>
    public Bounds GetWorldBounds()
    {
        return new Bounds(transform.position, new Vector3(_size.x, _size.y, 0f));
    }

    private void OnDrawGizmos()
    {
        Gizmos.color = Color.cyan;
        Gizmos.DrawWireCube(transform.position, new Vector3(_size.x, _size.y, 0f));
    }
}
```

**CameraFollow.cs 수정** — 기존 파일에 `SnapToRoom(Bounds)` 오버로드를 `SnapToRoom(Vector3)` 바로 아래에 추가:

```csharp
/// <summary>
/// 카메라를 worldBounds에 맞게 고정한다. orthographicSize를 Bounds에서 자동 계산한다.
/// CameraBound.GetWorldBounds() 결과를 전달받는다.
/// </summary>
public void SnapToRoom(Bounds worldBounds)
{
    _roomMode = true;
    transform.position = new Vector3(worldBounds.center.x, worldBounds.center.y, offset.z);
    float aspect = (_camera != null) ? _camera.aspect : (16f / 9f);
    float orthoH = worldBounds.size.y / 2f;
    float orthoW = worldBounds.size.x / (2f * aspect);
    if (_camera != null) _camera.orthographicSize = Mathf.Max(orthoH, orthoW);
}
```

기존 `SnapToRoom(Vector3 worldCenter)` 메서드와 `roomOrthoSize` Inspector 필드는 삭제하지 않는다 — CameraBound 없는 Room의 fallback 경로에서 계속 사용된다.
  </action>
  <verify>Unity Editor에서 컴파일 오류 없음. Scene 뷰에서 CameraBound를 붙인 GameObject 선택 시 시안색 와이어박스 표시됨.</verify>
  <done>CameraBound.cs 존재, GetWorldBounds() 반환값이 transform.position + _size 기반 Bounds. CameraFollow에 Bounds 오버로드 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: FloorSpawner.cs SnapCameraToRoom 헬퍼 추가 + 기존 호출 2곳 교체</name>
  <files>Assets/Scripts/World/FloorSpawner.cs</files>
  <action>
**헬퍼 메서드 추가** — `FloorSpawner.cs` 내부 헬퍼 영역(SelectNextRoom 위)에 추가:

```csharp
/// <summary>
/// Room에 CameraBound가 있으면 Bounds 기반 SnapToRoom, 없으면 fallbackCenter 기반으로 고정한다.
/// Awake()와 FloorTransitionSequence() 두 곳에서 공유한다.
/// </summary>
private void SnapCameraToRoom(GameObject room, Vector3 fallbackCenter)
{
    if (_cameraFollow == null) return;
    CameraBound cb = room.GetComponentInChildren<CameraBound>();
    if (cb != null)
        _cameraFollow.SnapToRoom(cb.GetWorldBounds());
    else
        _cameraFollow.SnapToRoom(fallbackCenter);
}
```

**Awake() 교체** (54번째 줄):
```csharp
// 기존:
_cameraFollow?.SnapToRoom(new Vector3(0f, _roomCameraOffsetY, 0f));
// 교체:
SnapCameraToRoom(_currentRoom, new Vector3(0f, _roomCameraOffsetY, 0f));
```

**FloorTransitionSequence() 교체** (106번째 줄):
```csharp
// 기존:
_cameraFollow?.SnapToRoom(new Vector3(0f, newRoomBaseY + _roomCameraOffsetY, 0f));
// 교체:
SnapCameraToRoom(nextRoom, new Vector3(0f, newRoomBaseY + _roomCameraOffsetY, 0f));
```

`_cameraFollow` null 체크는 헬퍼 내부에서 처리하므로 호출부에서 `?.` 연산자 제거. `_roomCameraOffsetY` Inspector 필드와 fallback 로직은 유지 — CameraBound 없는 기존 Room 프리팹과 하위 호환.
  </action>
  <verify>Unity Editor 컴파일 오류 없음. Play 모드 진입 시 Console에 NullReferenceException 없음. CameraBound가 없는 Room에서 기존 동작(fallback Y 오프셋 기반 스냅) 유지됨.</verify>
  <done>FloorSpawner에 SnapCameraToRoom 헬퍼 존재. Awake + FloorTransitionSequence 두 곳의 직접 SnapToRoom 호출이 헬퍼 호출로 교체됨. CameraBound 있는 룸 = Bounds 자동 계산, 없는 룸 = 기존 fallback.</done>
</task>

</tasks>

<verification>
1. Unity Editor 열기 → Console 탭에 컴파일 에러 없음
2. Hierarchy에서 빈 GameObject 생성 → CameraBound 컴포넌트 추가 → Scene 뷰에서 시안색 와이어박스 확인
3. Play 모드 진입 → 1층 스폰 시 카메라가 룸에 고정됨 (기존 동작 유지 확인)
4. Room 프리팹에 CameraBound 자식 추가 후 Play → 층 전환 시 해당 Room의 Bounds 기반 카메라 크기 적용 확인
</verification>

<success_criteria>
- CameraBound.cs 신규 파일 존재, OnDrawGizmos 시안색 와이어박스
- CameraFollow에 SnapToRoom(Bounds) 오버로드 추가, 기존 SnapToRoom(Vector3) 유지
- FloorSpawner Awake + FloorTransitionSequence 두 호출이 SnapCameraToRoom 헬퍼로 통일됨
- CameraBound 없는 기존 Room에서 fallback 동작 유지 — 기존 씬 무결성 보장
</success_criteria>

<output>
After completion, create `.planning/quick/260626-j9b-camerabound-camerafollow-floorspawner-ro/260626-j9b-SUMMARY.md`
</output>
