---
id: 260701-j76
type: quick
status: complete
date: "2026-07-01"
files_modified:
  - Assets/Editor/RoomCreator.cs
commits:
  - 08ad0d3
---

# Quick Task 260701-j76: RoomCreator 3-way patch

**One-liner:** SpawnMarker() + EnsureLadderTile() (갈색) + AddLadderTilemap 단일열/상하단플랫폼 — 3가지 결함 수정.

## Changes Applied

### Change 1: SpawnMarker() helper

`Marker()` 바로 아래에 `SpawnMarker()` 추가.
`SerializedObject`로 `_type` private 필드를 에디터 생성 시 설정.
6개 빌더의 적 스폰 `Marker()` 호출 29개 전부 `SpawnMarker(type)` 으로 교체:
- Melee 이름 (Melee/Chain/Chase/LargeBoss/BossChase) → `EnemyType.Melee`
- Ranged 이름 (Ranged/Wave/BossWave) → `EnemyType.Ranged`

### Change 2: EnsureLadderTile()

`EnsureSolidTile()` 아래 추가.
`Assets/Tiles/Tile_Ladder.asset` 생성, color = `new Color(0.76f, 0.60f, 0.42f)` (갈색).
기존 흰색 타일 버그 해결.

### Change 3: AddLadderTilemap 시그니처 변경

기존 `(root, tile, x0, x1, y0, y1)` → 새 `(root, solidTm, solidTile, x, y0, y1)`.
- 단일 x열로 사다리 타일 배치 (다중 열 → 단일 열)
- `solidTm.SetTile(x, y0-1)` — 하단 바닥 막음
- `Tilemap_LadderTop` + `PlatformEffector2D(useOneWay=true, surfaceArc=170)` — 상단 one-way 플랫폼
- 두 호출 사이트 업데이트: VerticalGauntlet x=0, AllInOne x=-9

## Verification

Unity Editor에서 컴파일 후 확인 항목:
- `Fast > RoomCreator > Build All Complex Rooms` → Console "복합 Room 6종 생성 완료"
- `Room_LastStand` 프리팹 > EnemySpawns > BossChaseSpawn: EnemySpawner(Type=Melee)
- `Room_LastStand` 프리팹 > EnemySpawns > BossWaveSpawn: EnemySpawner(Type=Ranged)
- `Assets/Tiles/Tile_Ladder.asset` 갈색 확인
- `Room_Vertical_Gauntlet` Grid 하위 Tilemap_Ladder(isTrigger, tag=Ladder) + Tilemap_LadderTop(PlatformEffector2D)

## Deviations from Plan

None — plan executed exactly as written.

Note: `RoomCreator.cs` was untracked in the main repo and absent from the worktree.
Copied from main repo into worktree before editing (standard worktree isolation pattern).

## Self-Check: PASSED

- [x] `Assets/Editor/RoomCreator.cs` exists in worktree
- [x] Commit `08ad0d3` exists (`git log --oneline` confirmed)
- [x] All 3 changes applied in single atomic commit
