---
phase: quick
plan: 260616-vo4
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/HUDController.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Play Mode 콘솔에 _gauge / _gaugeFill null 여부가 즉시 출력된다"
    - "Update()에서 _gauge.Value가 실제로 호출될 때 값이 로그에 찍힌다"
  artifacts:
    - path: Assets/Scripts/UI/HUDController.cs
      provides: "Start() 및 Update() 진단 로그"
  key_links:
    - from: HUDController.Start()
      to: ChronoGaugeController
      via: _gauge 직렬화 레퍼런스 + FindFirstObjectByType 폴백
      pattern: "Debug.Log.*_gauge"
---

<objective>
HUDController.cs의 Start()와 Update()에 최소한의 임시 진단 로그를 추가한다.

Purpose: GaugeController→ChronoGaugeController 리네임 후 씬 저장 미흡으로 _gauge 직렬화 레퍼런스가 null일 가능성을 Play Mode 콘솔에서 즉시 확인한다.
Output: 수정된 HUDController.cs — 원인 파악 후 진단 코드는 제거 예정인 임시 코드.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HUDController Start/Update에 null 체크 진단 로그 추가</name>
  <files>Assets/Scripts/UI/HUDController.cs</files>
  <action>
HUDController.cs의 Start()와 Update()를 아래와 같이 수정한다.

Start() — 기존 FindFirstObjectByType 폴백 직후에 로그 추가:

```csharp
private void Start()
{
    if (_gauge == null)
        _gauge = FindFirstObjectByType<ChronoGaugeController>();

    // [DIAG] 임시 진단 로그 — 원인 파악 후 제거
    Debug.Log($"[HUD] Start: _gauge={(_gauge != null ? "OK" : "NULL")}, _gaugeFill={(_gaugeFill != null ? "OK" : "NULL")}, _floorLabel={(_floorLabel != null ? "OK" : "NULL")}, _attackTypeLabel={(_attackTypeLabel != null ? "OK" : "NULL")}");
}
```

Update() — _gauge null 가드 안쪽에 값 추적 로그 추가 (매 프레임 출력은 과도하므로 프레임 카운트 30 단위로 제한):

```csharp
private void Update()
{
    _floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
    if (_gauge != null)
    {
        _gaugeFill.fillAmount = _gauge.Value;
        // [DIAG] 임시 진단 로그 — 0.5초(30프레임)마다 한 번
        if (Time.frameCount % 30 == 0)
            Debug.Log($"[HUD] Update: _gauge.Value={_gauge.Value:F3}, fillAmount={_gaugeFill.fillAmount:F3}");
    }

    AttackType t = AttackTypeSelector.Selected;
    if (t != _lastType)
    {
        _lastType = t;
        _attackTypeLabel.SetText(t == AttackType.Linear ? "LINEAR" : "FAN");
    }
}
```

주의:
- `// [DIAG]` 주석을 반드시 붙여 임시 코드임을 명시한다.
- 기존 로직(FindFirstObjectByType 폴백, null 가드, _lastType dirty-check)은 일체 변경하지 않는다.
- 들여쓰기·포맷은 기존 파일 스타일(4스페이스)을 따른다.
  </action>
  <verify>
    <automated>파일이 컴파일 에러 없이 저장됐는지 확인: Assets/Scripts/UI/HUDController.cs에 "DIAG" 문자열이 2개 이상 포함돼 있어야 한다. Play Mode 진입 후 콘솔에 "[HUD] Start:" 로그가 출력되면 성공.</automated>
  </verify>
  <done>
    - Play Mode 직후 콘솔에 "[HUD] Start: _gauge=OK or NULL" 형태의 로그가 출력된다.
    - _gauge가 정상이면 약 0.5초마다 "[HUD] Update: _gauge.Value=X.XXX" 로그가 찍힌다.
    - _gauge가 NULL이면 Update 로그는 출력되지 않아 null임을 즉시 확인할 수 있다.
  </done>
</task>

</tasks>

<verification>
Unity Editor에서 Play Mode 진입 → Console 창 확인:
1. "[HUD] Start:" 로그 1줄 — _gauge / _gaugeFill / _floorLabel / _attackTypeLabel 중 NULL인 항목 식별
2. _gauge=OK이면 약 30프레임마다 "[HUD] Update:" 로그 반복 출력
3. _gauge=NULL이면 Update 로그 없음 → 직렬화 레퍼런스 깨짐 확정, 씬 재저장 또는 Inspector 재연결 필요
</verification>

<success_criteria>
Play Mode 콘솔만으로 _gauge null 여부와 gauge.Value 반영 여부를 즉시 판단할 수 있다.
</success_criteria>

<output>
완료 후 .planning/quick/260616-vo4-hudcontroller-start-gauge-null/260616-vo4-SUMMARY.md 작성
</output>
