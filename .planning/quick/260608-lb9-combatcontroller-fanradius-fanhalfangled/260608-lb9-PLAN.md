---
phase: quick-260608-lb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/RangeDisplay.cs
autonomous: true
requirements: [ATCK-02]

must_haves:
  truths:
    - "RangeDisplay의 fanRadius, fanHalfAngleDeg, linearLength(searchRadius)가 Inspector에 노출되지 않는다"
    - "CombatController의 fanRadius, fanHalfAngleDeg, searchRadius가 유일한 Inspector 직렬화 필드이다"
    - "RangeDisplay가 그리는 부채꼴 반지름/각도와 CombatController의 실제 판정 반지름/각도가 항상 동일하다"
  artifacts:
    - path: "Assets/Scripts/Player/RangeDisplay.cs"
      provides: "CombatController에서 값을 읽는 표시 컴포넌트"
      contains: "_combat.fanRadius, _combat.fanHalfAngleDeg, _combat.searchRadius"
    - path: "Assets/Scripts/Player/CombatController.cs"
      provides: "단일 진실 소스 — fanRadius, fanHalfAngleDeg, searchRadius SerializeField 유지"
  key_links:
    - from: "RangeDisplay.Awake()"
      to: "CombatController (parent)"
      via: "GetComponentInParent<CombatController>()"
      pattern: "_combat = GetComponentInParent<CombatController>"
    - from: "RangeDisplay.UpdateFanDisplay()"
      to: "_combat.FanRadius / _combat.FanHalfAngleDeg"
      via: "public property getter"
    - from: "RangeDisplay.UpdateCircleDisplay()"
      to: "_combat.SearchRadius / _combat.FanRadius"
      via: "public property getter"
---

<objective>
CombatController의 fanRadius, fanHalfAngleDeg, searchRadius를 단일 진실 소스(Single Source of Truth)로 삼아
RangeDisplay의 중복 SerializeField를 제거하고 CombatController 참조로 교체한다.

Purpose: 실제 적 탐지 범위(CombatController)와 시각적 표시 범위(RangeDisplay)가 항상 동기화됨을 보장한다.
Output: RangeDisplay.cs — 중복 3개 필드 제거, CombatController 프로퍼티 읽기로 교체
</objective>

<execution_context>
@C:/Users/MSI/Projeect_A.E/fastProject/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Assets/Scripts/Player/CombatController.cs
@Assets/Scripts/Player/RangeDisplay.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: CombatController에 public 프로퍼티 3개 추가</name>
  <files>Assets/Scripts/Player/CombatController.cs</files>
  <action>
CombatController의 SerializeField 블록 아래(Awake 위)에 public 읽기 전용 프로퍼티 3개를 추가한다.
기존 [SerializeField] private 변수는 그대로 유지한다(이름/값 변경 없음).

추가할 프로퍼티:
```csharp
// -- Range display accessors (RangeDisplay reads these — single source of truth) ----
public float FanRadius       => fanRadius;
public float FanHalfAngleDeg => fanHalfAngleDeg;
public float SearchRadius    => searchRadius;
```

위치: `private readonly Collider2D[] _hitBuffer` 선언 직전 빈 줄 뒤.
변경 최소화: 기존 코드 한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "public float FanRadius\|public float FanHalfAngleDeg\|public float SearchRadius" "Assets/Scripts/Player/CombatController.cs"</automated>
  </verify>
  <done>CombatController.cs에 FanRadius, FanHalfAngleDeg, SearchRadius 프로퍼티가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: RangeDisplay에서 중복 SerializeField 제거 및 CombatController 참조로 교체</name>
  <files>Assets/Scripts/Player/RangeDisplay.cs</files>
  <action>
아래 변경을 정확히 적용한다. 다른 코드는 건드리지 않는다.

**[제거] 중복 SerializeField 3개 삭제:**
```csharp
// 삭제 대상:
[SerializeField] private float linearLength = 10f;
[SerializeField] private float fanRadius       = 4f;
[SerializeField] private float fanHalfAngleDeg = 35f;
```

**[추가] CombatController 참조 필드 (private, 비직렬화):**
```csharp
// -- CombatController reference (single source of truth for range values) ----------
private CombatController _combat;
```
위치: `private SpriteRenderer _playerSprite;` 선언 직전.

**[수정] Awake() — _combat 초기화 추가:**
기존 Awake() 본문 맨 앞에 한 줄 추가:
```csharp
_combat = GetComponentInParent<CombatController>();
```

**[수정] UpdateLinearDisplay() — linearLength → _combat.SearchRadius:**
```csharp
// 변경 전:
_leftBeam.SetPosition(1, origin + dir * linearLength);
// 변경 후:
_leftBeam.SetPosition(1, origin + dir * _combat.SearchRadius);
```

**[수정] UpdateFanDisplay() — fanRadius, fanHalfAngleDeg → _combat 프로퍼티:**
```csharp
// 변경 전:
float angle = (baseAngle - fanHalfAngleDeg + t * fanHalfAngleDeg * 2f) * Mathf.Deg2Rad;
Vector2 pt  = origin + new Vector2(Mathf.Cos(angle), Mathf.Sin(angle)) * fanRadius;
// 변경 후:
float angle = (baseAngle - _combat.FanHalfAngleDeg + t * _combat.FanHalfAngleDeg * 2f) * Mathf.Deg2Rad;
Vector2 pt  = origin + new Vector2(Mathf.Cos(angle), Mathf.Sin(angle)) * _combat.FanRadius;
```

**[수정] UpdateCircleDisplay() — linearLength, fanRadius → _combat 프로퍼티:**
```csharp
// 변경 전:
float radius = AttackTypeSelector.Selected == AttackType.Linear ? linearLength : fanRadius;
// 변경 후:
float radius = AttackTypeSelector.Selected == AttackType.Linear ? _combat.SearchRadius : _combat.FanRadius;
```

완료 후 파일 전체에서 `linearLength`, `fanRadius`, `fanHalfAngleDeg` 텍스트가 남아 있으면 안 된다 (주석 포함).
  </action>
  <verify>
    <automated>grep -n "linearLength\|private float fanRadius\|private float fanHalfAngleDeg\|\[SerializeField\].*fanRadius\|\[SerializeField\].*linearLength\|\[SerializeField\].*fanHalfAngleDeg" "Assets/Scripts/Player/RangeDisplay.cs"</automated>
  </verify>
  <done>
- grep 결과가 0줄 (중복 필드가 완전히 제거됨)
- _combat.SearchRadius, _combat.FanRadius, _combat.FanHalfAngleDeg 참조가 RangeDisplay.cs에 존재함
- Unity 컴파일 에러 없음 (Console에 CS 에러 없음)
  </done>
</task>

</tasks>

<verification>
1. Unity Editor Console에 컴파일 에러 없음
2. Inspector에서 Player > RangeDisplay 컴포넌트에 linearLength, fanRadius, fanHalfAngleDeg 필드가 보이지 않음
3. Inspector에서 Player > CombatController 컴포넌트에만 Fan Radius, Fan Half Angle Deg, Search Radius가 노출됨
4. Play Mode 진입 후 공격 버튼 홀드 시 부채꼴/빔 표시가 정상 작동함
5. CombatController의 fanRadius 값을 Inspector에서 변경하면 Play Mode에서 표시 범위가 즉시 반영됨
</verification>

<success_criteria>
- RangeDisplay.cs에서 linearLength, fanRadius, fanHalfAngleDeg SerializeField가 완전히 제거됨
- CombatController.cs에 FanRadius, FanHalfAngleDeg, SearchRadius public 프로퍼티가 추가됨
- RangeDisplay가 _combat = GetComponentInParent<CombatController>()로 참조를 획득함
- Unity 컴파일 에러 없음
- 실제 판정 범위와 표시 범위가 단일 Inspector 값(CombatController)에 의해 결정됨
</success_criteria>

<output>
완료 후 `.planning/quick/260608-lb9-combatcontroller-fanradius-fanhalfangled/260608-lb9-SUMMARY.md` 생성
</output>
