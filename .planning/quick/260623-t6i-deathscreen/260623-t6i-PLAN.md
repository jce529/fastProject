---
phase: quick-260623-t6i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scenes/SampleScene.unity
autonomous: true
requirements: [QUICK-260623-t6i]
must_haves:
  truths:
    - "DeathScreen의 버튼 텍스트가 '메인 메뉴'로 표시된다"
    - "버튼 기능(LoadScene(0))은 변경 없이 그대로 동작한다"
  artifacts:
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "RestartButton > RestartLabel TMP 텍스트 업데이트"
      contains: "m_text: 메인 메뉴"
  key_links:
    - from: "Assets/Scenes/SampleScene.unity"
      to: "RestartLabel (fileID: 318715502)"
      via: "YAML 직접 편집 — m_text 필드"
      pattern: "m_text: 메인 메뉴"
---

<objective>
DeathScreen의 재시작 버튼 텍스트를 실제 동작과 일치하도록 수정한다.

Purpose: 버튼은 LoadScene(0)으로 메인 메뉴로 이동하지만 레이블은 "RESTART"로 표시되어 있다. 텍스트를 "메인 메뉴"로 변경해 사용자 혼란을 없앤다.
Output: SampleScene.unity의 RestartLabel TMP 텍스트가 "메인 메뉴"로 업데이트됨
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- 관련 구조 요약 -->
<!-- SampleScene.unity 내 DeathScreen UI 계층:
     DeathScreenController (MonoBehaviour, fileID: ~980)
       └── _deathPanel (fileID: 293336001)
             └── RestartButton (fileID: 385590840, Button 컴포넌트)
                   └── RestartLabel (fileID: 318715502, GameObject)
                         └── TextMeshProUGUI — m_text: RESTART  ← 이것을 수정
     DeathScreenController.RestartGame() → SceneManager.LoadScene(0) → 메인 메뉴
     별도 DeathScreen 프리팹 없음; UI가 씬에 직접 있음
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: RestartLabel 텍스트를 "메인 메뉴"로 변경</name>
  <files>Assets/Scenes/SampleScene.unity</files>
  <action>
    Assets/Scenes/SampleScene.unity 파일을 열고 line 873의 YAML 값을 수정한다.

    변경 전:
      m_text: RESTART

    변경 후:
      m_text: 메인 메뉴

    해당 항목은 fileID 318715504 (MonoBehaviour, TextMeshProUGUI)이며
    GameObject "RestartLabel" (fileID 318715502)에 속한다.
    이 GameObject는 "RestartButton" (fileID 385590840)의 자식이다.

    주의: m_text 필드는 파일에 딱 하나만 존재하지 않는다.
    "Floor 1", "LINEAR" 텍스트도 있으므로 fileID 318715504 블록 안의
    m_text 만 수정할 것. line 873이 정확한 위치이다.
  </action>
  <verify>
    <automated>grep -n "m_text: 메인 메뉴" "Assets/Scenes/SampleScene.unity"</automated>
  </verify>
  <done>"메인 메뉴" 문자열이 SampleScene.unity에 존재하고, "RESTART" 텍스트는 더 이상 파일에 없다</done>
</task>

</tasks>

<verification>
grep -c "RESTART" Assets/Scenes/SampleScene.unity  → 0 이어야 함
grep -c "메인 메뉴" Assets/Scenes/SampleScene.unity → 1 이어야 함
Unity Editor에서 씬을 열면 DeathScreen 버튼에 "메인 메뉴"가 표시됨
</verification>

<success_criteria>
- SampleScene.unity에 "m_text: 메인 메뉴" 존재
- SampleScene.unity에 "RESTART" 미존재
- DeathScreenController.cs 변경 없음 (버튼 로직 유지)
</success_criteria>

<output>
완료 후 `.planning/quick/260623-t6i-deathscreen/260623-t6i-SUMMARY.md` 생성
</output>
