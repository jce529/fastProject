---
phase: quick-260626-kz4-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/ScoreManager.cs
  - Assets/Scripts/Player/CombatController.cs
  - Assets/Scripts/World/RoomExit.cs
  - Assets/Scripts/World/FloorSpawner.cs
  - Assets/Scripts/UI/DeathScreenController.cs
  - Assets/Scripts/UI/HUDController.cs
autonomous: false
requirements: []

must_haves:
  truths:
    - "적을 대시 처치하면 점수가 100점씩 올라간다"
    - "방 출구에 도달하면 클리어 속도에 따른 보너스 점수가 더해진다"
    - "HUD에 현재 점수가 실시간으로 표시된다"
    - "재시작(RestartGame) 시 점수가 0으로 초기화된다"
  artifacts:
    - path: "Assets/Scripts/World/ScoreManager.cs"
      provides: "점수 상태 + 타이머 (static class, FloorManager 패턴)"
  key_links:
    - from: "CombatController.ExecuteDash()"
      to: "ScoreManager.AddKillScore()"
      via: "target.OnDashHit() 직후 호출"
    - from: "RoomExit.OnTriggerEnter2D()"
      to: "ScoreManager.AddRoomClearBonus()"
      via: "AdvanceFloor() 직전 호출"
    - from: "FloorSpawner.ActivateEnemies()"
      to: "ScoreManager.StartRoomTimer()"
      via: "적 활성화 완료 직후 호출"
    - from: "HUDController.Update()"
      to: "ScoreManager.Score"
      via: "_scoreLabel.SetText(\"{0}\", ScoreManager.Score)"
---

<objective>
적 처치 점수(+100/처치) + 방 클리어 속도 보너스(+300/+150/+50) 시스템을 구현하고 HUD에 표시한다.

Purpose: 핵심 전투 메카닉(대시 처치)과 방 클리어 속도를 동시에 보상하는 최소한의 피드백 루프. 플레이테스트 시 세션 품질 지표로 활용.
Output: ScoreManager.cs (신규) + 5개 파일 최소 수정 + HUD 점수 레이블 연결
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 계약 — 탐색 없이 바로 사용 가능 -->

FloorManager (Assets/Scripts/World/FloorManager.cs):
```csharp
// ScoreManager가 따를 패턴 — data-only static class, no scene lifecycle
public static class FloorManager
{
    public static int CurrentFloor = 1;
}
```

CombatController.ExecuteDash() 수정 위치 (line 304):
```csharp
// 6. Kill and effects
target.OnDashHit();           // ← 이 줄 바로 다음에 ScoreManager.AddKillScore(); 추가
yield return StartCoroutine(HitFreeze(hitFreezeDuration));
```

RoomExit.OnTriggerEnter2D() 수정 위치:
```csharp
private void OnTriggerEnter2D(Collider2D other)
{
    if (!other.CompareTag("Player")) return;
    // ← 여기에 ScoreManager.AddRoomClearBonus(); 추가
    FloorSpawner.Instance?.AdvanceFloor();
}
```

FloorSpawner.ActivateEnemies() 수정 위치:
```csharp
private void ActivateEnemies(GameObject room)
{
    foreach (MonoBehaviour mb in room.GetComponentsInChildren<MonoBehaviour>(true))
    {
        if (mb is IEnemy)
            mb.gameObject.SetActive(true);
    }
    // ← 여기에 ScoreManager.StartRoomTimer(); 추가
}
```

DeathScreenController.RestartGame() 수정 위치:
```csharp
private void RestartGame()
{
    Time.timeScale         = 1f;
    Time.fixedDeltaTime    = 0.02f;
    FloorManager.CurrentFloor = 1;
    // ← 여기에 ScoreManager.Reset(); 추가
    SceneManager.LoadScene("AttackSelect");
}
```

HUDController (Assets/Scripts/UI/HUDController.cs):
```csharp
// 기존 패턴 — 이 패턴 그대로 적용
[SerializeField] private TextMeshProUGUI _floorLabel;
// _scoreLabel 동일 방식으로 추가

// Update()에서 기존 패턴:
_floorLabel.SetText("Floor {0}", FloorManager.CurrentFloor);
// 아래도 동일 방식으로 추가:
_scoreLabel?.SetText("{0}", ScoreManager.Score);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreManager 생성 + 5개 파일 훅 연결</name>
  <files>
    Assets/Scripts/World/ScoreManager.cs,
    Assets/Scripts/Player/CombatController.cs,
    Assets/Scripts/World/RoomExit.cs,
    Assets/Scripts/World/FloorSpawner.cs,
    Assets/Scripts/UI/DeathScreenController.cs,
    Assets/Scripts/UI/HUDController.cs
  </files>
  <action>
**1. Assets/Scripts/World/ScoreManager.cs 신규 생성** (FloorManager 패턴 — data-only static class):

```csharp
using UnityEngine;

/// <summary>
/// 점수 관리 정적 클래스. FloorManager 패턴 준수 (data-only, no scene lifecycle).
/// 적 처치 시 +100점, 방 클리어 속도에 따른 보너스 (+300/+150/+50).
/// 타이머: Time.unscaledTime 기반 — timeScale 변화(슬로우모션, 히트프리즈)에 면역.
/// </summary>
public static class ScoreManager
{
    // -- 보너스 기준 (Inspector 없음 — 프로토타입 상수로 충분) --------------------
    public const int   KillScore        = 100;
    public const int   FastClearBonus   = 300;
    public const int   NormalClearBonus = 150;
    public const int   SlowClearBonus   = 50;
    public const float FastClearTime    = 10f; // 초 이내 클리어 시 FastClearBonus
    public const float NormalClearTime  = 25f; // 초 이내 클리어 시 NormalClearBonus

    // -- 상태 -----------------------------------------------------------------------
    public static int Score { get; private set; }
    private static float _roomStartTime;

    // -- Public API ----------------------------------------------------------------

    /// <summary>씬 재로드 없이 점수 초기화. DeathScreenController.RestartGame()에서 호출.</summary>
    public static void Reset()
    {
        Score = 0;
        _roomStartTime = Time.unscaledTime;
    }

    /// <summary>CombatController.ExecuteDash() — target.OnDashHit() 직후 호출.</summary>
    public static void AddKillScore()
    {
        Score += KillScore;
    }

    /// <summary>방 입장 타이머 시작. FloorSpawner.ActivateEnemies() 완료 직후 호출.</summary>
    public static void StartRoomTimer()
    {
        _roomStartTime = Time.unscaledTime;
    }

    /// <summary>
    /// 방 클리어 보너스 계산 및 추가. RoomExit.OnTriggerEnter2D()에서 AdvanceFloor() 직전 호출.
    /// elapsed = unscaledTime 기반 — 슬로우모션 중 출구 진입 시에도 실제 경과 시간으로 계산.
    /// </summary>
    public static void AddRoomClearBonus()
    {
        float elapsed = Time.unscaledTime - _roomStartTime;
        if      (elapsed <= FastClearTime)   Score += FastClearBonus;
        else if (elapsed <= NormalClearTime) Score += NormalClearBonus;
        else                                 Score += SlowClearBonus;
    }
}
```

**2. CombatController.cs 수정** — `ExecuteDash()` 내 `target.OnDashHit();` 직후 1줄 추가:
```csharp
target.OnDashHit();
ScoreManager.AddKillScore();  // ← 추가
yield return StartCoroutine(HitFreeze(hitFreezeDuration));
```

**3. RoomExit.cs 수정** — `OnTriggerEnter2D()` 내 `FloorSpawner.Instance?.AdvanceFloor();` 직전 1줄 추가:
```csharp
if (!other.CompareTag("Player")) return;
ScoreManager.AddRoomClearBonus();     // ← 추가
FloorSpawner.Instance?.AdvanceFloor();
```

**4. FloorSpawner.cs 수정** — `ActivateEnemies()` 메서드 끝에 1줄 추가:
```csharp
private void ActivateEnemies(GameObject room)
{
    foreach (MonoBehaviour mb in room.GetComponentsInChildren<MonoBehaviour>(true))
    {
        if (mb is IEnemy)
            mb.gameObject.SetActive(true);
    }
    ScoreManager.StartRoomTimer();  // ← 추가 (적 활성화 완료 시점 = 방 입장 기준)
}
```

**5. DeathScreenController.cs 수정** — `RestartGame()` 내 `SceneManager.LoadScene()` 직전 1줄 추가:
```csharp
FloorManager.CurrentFloor = 1;
ScoreManager.Reset();               // ← 추가
SceneManager.LoadScene("AttackSelect");
```

**6. HUDController.cs 수정** — 필드 추가 + Update 1줄 추가:
```csharp
// 기존 필드 아래에 추가:
[SerializeField] private TextMeshProUGUI _scoreLabel;

// Update() 내 _floorLabel.SetText(...) 직후에 추가:
_scoreLabel?.SetText("{0}", ScoreManager.Score);
```

**금지**: 기존 코드 리팩터링, 임의 로그 추가, 서술한 6개 변경 외 추가 수정 없음.
  </action>
  <verify>
    <automated>MISSING — Unity 컴파일 오류는 Unity Editor Console에서 확인. 자동화 불가.</automated>
  </verify>
  <done>
    - ScoreManager.cs 파일이 Assets/Scripts/World/ 에 존재한다
    - Unity Editor Console에 컴파일 오류 0개
    - 6개 파일 모두 수정 완료 (ScoreManager.cs 신규 포함)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Unity Editor에서 HUD Score 레이블 생성 및 연결</name>
  <what-built>HUDController._scoreLabel 직렬화 필드가 준비됨. Unity Editor에서 TextMeshProUGUI GameObject를 추가하고 연결해야 HUD에 점수가 표시된다.</what-built>
  <how-to-verify>
**[Editor 작업 — Claude 불가, 사용자 수행]**

1. Unity Editor에서 SampleScene을 연다
2. Hierarchy에서 HUD Canvas 하위의 기존 레이블(FloorLabel, AttackTypeLabel 등) 구조를 확인한다
3. Canvas 하위에 새 TextMeshPro - Text (UI) GameObject를 추가한다 (이름: "ScoreLabel")
4. ScoreLabel의 RectTransform을 원하는 위치로 배치한다 (예: 우상단, FloorLabel 아래)
5. 기본 텍스트를 "0"으로, 폰트 크기는 기존 레이블과 동일하게 설정한다
6. HUDController 컴포넌트의 Inspector에서 "Score Label" 슬롯에 ScoreLabel GameObject를 드래그한다
7. Play 모드 진입 후 적을 처치하면 점수가 100씩 올라가는지 확인한다
8. 방 출구(사다리 최상단 등)에 닿으면 보너스 점수(+300/+150/+50)가 더해지는지 확인한다
9. 사망 후 재시작 시 점수가 0으로 초기화되는지 확인한다

**예상 결과:** HUD에 숫자가 보이고, 처치마다 100씩 증가하며, 방 클리어 시 점프 증가한다.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견한 문제 설명 입력</resume-signal>
</task>

</tasks>

<verification>
- Unity Console 컴파일 오류 0개
- 플레이 중 적 처치 시 HUD 점수 +100 확인
- 방 출구 진입 시 보너스 점수 확인 (빠를수록 높음)
- 사망 → 재시작 후 점수 0 확인
</verification>

<success_criteria>
- ScoreManager.cs 신규 생성, 6개 파일 컴파일 오류 없음
- 플레이테스터가 처치/클리어 행동과 점수 증가를 즉각 연관할 수 있음
- 재시작 후 점수 상태가 오염되지 않음 (0에서 재시작)
</success_criteria>

<output>
완료 후 `.planning/quick/260626-kz4-hud/260626-kz4-SUMMARY.md` 작성.
</output>
