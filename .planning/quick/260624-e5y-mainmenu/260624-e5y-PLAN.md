---
quick: 260624-e5y
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/GameBootstrapper.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "어느 씬에서 Play를 눌러도 첫 씬은 항상 MainMenu다"
    - "이미 MainMenu 씬에서 실행하면 다시 로드하지 않는다"
  artifacts:
    - path: "Assets/Scripts/World/GameBootstrapper.cs"
      provides: "RuntimeInitializeOnLoadMethod 부트스트랩 — 씬 이동 전 MainMenu로 리디렉션"
  key_links:
    - from: "GameBootstrapper.EnsureMainMenu()"
      to: "MainMenu 씬"
      via: "SceneManager.LoadScene(\"MainMenu\")"
      pattern: "SceneManager\\.LoadScene.*MainMenu"
---

<objective>
어느 씬에서 Play를 눌러도 항상 MainMenu 씬으로 먼저 이동하는 부트스트랩 스크립트 추가.

Purpose: 개발 중 SampleScene 등에서 Play를 눌러도 정상 게임 플로우(MainMenu → AttackSelect → SampleScene)가 보장된다.
Output: Assets/Scripts/World/GameBootstrapper.cs — MonoBehaviour 없이 동작하는 static bootstrapper.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Build Settings (confirmed):
  index 0: Assets/Scenes/MainMenu.unity    (씬 이름: "MainMenu")
  index 1: Assets/Scenes/AttackSelect.unity
  index 2: Assets/Scenes/SampleScene.unity

기존 World 스크립트 패턴:
  Assets/Scripts/World/FloorManager.cs  — static class, no MonoBehaviour
  Assets/Scripts/World/RoomEntry.cs     — MonoBehaviour
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameBootstrapper.cs 생성</name>
  <files>Assets/Scripts/World/GameBootstrapper.cs</files>
  <action>
Assets/Scripts/World/GameBootstrapper.cs 를 새로 만든다. MonoBehaviour 상속 없이 static 클래스로 작성한다.

구현 내용:
- `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]` 어트리뷰트를 붙인 static void EnsureMainMenu() 메서드 하나만 작성
- 메서드 내부: `SceneManager.GetActiveScene().name`이 "MainMenu"가 아닐 경우에만 `SceneManager.LoadScene("MainMenu")` 호출
- using 문: `UnityEngine`, `UnityEngine.SceneManagement`
- 클래스 레벨 주석: // 어느 씬에서 Play해도 항상 MainMenu로 부트스트랩

금지사항:
- MonoBehaviour 상속 금지 — scene object가 아닌 코드로만 동작해야 함
- Awake/Start/Update 등 생명주기 메서드 금지
- BeforeSceneLoad 이외의 RuntimeInitializeLoadType 사용 금지 (AfterSceneLoad는 씬이 이미 로드된 후라 의미 없음)

완성 코드 예시:
```csharp
using UnityEngine;
using UnityEngine.SceneManagement;

// 어느 씬에서 Play해도 항상 MainMenu로 부트스트랩
public static class GameBootstrapper
{
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void EnsureMainMenu()
    {
        if (SceneManager.GetActiveScene().name != "MainMenu")
            SceneManager.LoadScene("MainMenu");
    }
}
```
  </action>
  <verify>
파일 Assets/Scripts/World/GameBootstrapper.cs 존재 확인.
Unity Editor에서 SampleScene을 열고 Play → Console에 오류 없이 MainMenu 씬으로 이동하는지 확인.
  </verify>
  <done>
- GameBootstrapper.cs 파일 존재
- SampleScene에서 Play 시 MainMenu로 자동 이동
- MainMenu에서 Play 시 추가 리로드 없이 그대로 유지
  </done>
</task>

</tasks>

<verification>
Unity Editor: SampleScene 열고 Play → MainMenu 씬으로 자동 전환 확인.
Unity Editor: MainMenu 씬 열고 Play → MainMenu에 그대로 머무는지 확인 (무한루프 없음).
Console 창에 오류/경고 없음.
</verification>

<success_criteria>
- [ ] GameBootstrapper.cs 파일 생성됨
- [ ] SampleScene Play → MainMenu 자동 이동
- [ ] MainMenu Play → 씬 재로드 없이 정상 유지
- [ ] Console 오류 없음
</success_criteria>

<output>
완료 후 .planning/quick/260624-e5y-mainmenu/260624-e5y-SUMMARY.md 생성.
</output>
