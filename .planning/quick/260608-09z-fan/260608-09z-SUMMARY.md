---
quick_id: 260608-09z
status: complete
date: 2026-06-08
---

# Quick Task 260608-09z: Summary

## What Was Done

Fan 모드의 부채꼴 방향을 이동방향(flipX) 기준에서 마우스 방향 기준으로 통일.

### RangeDisplay.cs — UpdateFanDisplay()

기존:
```csharp
Vector2 facing    = (_playerSprite != null && _playerSprite.flipX) ? Vector2.left : Vector2.right;
float   baseAngle = Mathf.Atan2(facing.y, facing.x) * Mathf.Rad2Deg;
```

변경 후: Linear와 동일하게 `Mouse.current → ScreenToWorldPoint → dir → Atan2(dir.y, dir.x)` 패턴 적용.

### CombatController.cs — FindNearestEnemyInRange() Fan 브랜치

기존:
```csharp
attackDir = _spriteRenderer.flipX ? Vector2.left : Vector2.right;
```

변경 후: Linear 브랜치와 동일한 마우스 좌표 계산 패턴으로 교체.

## Result

Fan 모드에서 부채꼴 표시(RangeDisplay)와 실제 히트 판정(CombatController) 모두 마우스 방향 기준으로 동작.
