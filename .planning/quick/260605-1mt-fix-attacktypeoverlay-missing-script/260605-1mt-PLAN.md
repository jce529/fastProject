---
phase: quick-260605-1mt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scenes/SampleScene.unity
  - Assets/Scripts/UI/AttackTypeSelector.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "AttackTypeOverlay GameObject에서 Missing Script 경고가 사라진다"
    - "AttackTypeSelector.cs가 컴파일 에러 없이 빌드된다"
    - "PC(마우스)와 모바일(터치) 모두 Pointer.current로 입력을 읽는다"
  artifacts:
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "CanvasScaler/GraphicRaycaster GUID 교정"
    - path: "Assets/Scripts/UI/AttackTypeSelector.cs"
      provides: "New Input System Pointer API 사용"
  key_links:
    - from: "AttackTypeSelector.cs"
      to: "UnityEngine.InputSystem.Pointer.current"
      via: "using UnityEngine.InputSystem; + Pointer.current?.position.ReadValue()"
---

<objective>
SampleScene의 AttackTypeOverlay에 붙은 Missing Script 2개를 제거하고,
AttackTypeSelector.cs의 레거시 Input API를 New Input System으로 교체한다.

Purpose: Unity 에디터 콘솔 경고 제거 및 Android 빌드에서 입력이 올바르게 동작하도록 보장.
Output: 수정된 SampleScene.unity (GUID 2개 교정) + 수정된 AttackTypeSelector.cs (Pointer API)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SampleScene.unity — CanvasScaler / GraphicRaycaster GUID 교정</name>
  <files>Assets/Scenes/SampleScene.unity</files>
  <action>
    Assets/Scenes/SampleScene.unity 를 텍스트 편집기로 열어 아래 두 줄을 정확히 교체한다.

    Line 174 — CanvasScaler:
      BEFORE: guid: 0cd44c1031e13a943bb63994233966ae
      AFTER:  guid: 0cd44c1031e13a943bb63640046fad76

    Line 186 — GraphicRaycaster:
      BEFORE: guid: dc42784cf147fd54f92e4ed82eed3e91
      AFTER:  guid: dc42784cf147c0c48a680349fa168899

    변경 전후 나머지 줄(fileID, type 등)은 건드리지 않는다.
    파일은 YAML 이진 혼합 포맷이므로 BOM이나 줄 끝 문자(CRLF/LF)를 바꾸지 않는다.
  </action>
  <verify>
    grep으로 두 올바른 GUID가 존재하고, 잘못된 GUID가 없는지 확인:
      grep "0cd44c1031e13a943bb63640046fad76" Assets/Scenes/SampleScene.unity  → 1줄 출력
      grep "dc42784cf147c0c48a680349fa168899" Assets/Scenes/SampleScene.unity  → 1줄 출력
      grep "0cd44c1031e13a943bb63994233966ae" Assets/Scenes/SampleScene.unity  → 0줄 (없어야 함)
      grep "dc42784cf147fd54f92e4ed82eed3e91" Assets/Scenes/SampleScene.unity  → 0줄 (없어야 함)
  </verify>
  <done>
    SampleScene.unity에 잘못된 GUID가 존재하지 않고,
    올바른 CanvasScaler/GraphicRaycaster GUID가 각각 정확히 1회 등장한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: AttackTypeSelector.cs — 레거시 Input API → New Input System Pointer</name>
  <files>Assets/Scripts/UI/AttackTypeSelector.cs</files>
  <action>
    현재 파일 상단에 `using UnityEngine;` 만 있다. 아래 using을 추가한다:
      using UnityEngine.InputSystem;

    Update() 메서드 내 lines 29-31:
      BEFORE:
        Vector2 pos = Input.touchCount > 0
            ? Input.GetTouch(0).position
            : (Vector2)Input.mousePosition;

      AFTER:
        Vector2 pos = Pointer.current?.position.ReadValue() ?? Vector2.zero;
        if (pos == Vector2.zero) return;

    주석("Works on both PC (mouse) and mobile (first touch)")은 그대로 유지한다.
    나머지 코드(RectTransformUtility 호출, Select, RefreshHighlights)는 변경하지 않는다.

    결과적으로 파일은 다음 구조가 된다:
      using UnityEngine;
      using UnityEngine.InputSystem;

      public enum AttackType { ... }

      // ... 주석 ...
      public class AttackTypeSelector : MonoBehaviour
      {
          ...
          private void Update()
          {
              if (zoneRect == null) return;

              Vector2 pos = Pointer.current?.position.ReadValue() ?? Vector2.zero;
              if (pos == Vector2.zero) return;

              if (!RectTransformUtility.RectangleContainsScreenPoint(...)) return;
              ...
          }
          ...
      }
  </action>
  <verify>
    파일 내 레거시 API가 남아 있지 않은지 확인:
      grep "Input.touchCount\|Input.GetTouch\|Input.mousePosition" Assets/Scripts/UI/AttackTypeSelector.cs  → 0줄

    Pointer API와 using이 존재하는지 확인:
      grep "Pointer.current" Assets/Scripts/UI/AttackTypeSelector.cs  → 1줄
      grep "using UnityEngine.InputSystem" Assets/Scripts/UI/AttackTypeSelector.cs  → 1줄

    Unity Editor에서 재컴파일 후 Console에 CS 에러 없음 (에디터에서 직접 확인).
  </verify>
  <done>
    AttackTypeSelector.cs가 레거시 Input API를 사용하지 않고,
    Pointer.current?.position.ReadValue()로 마우스/터치를 통합 처리하며,
    컴파일 에러 없이 빌드된다.
  </done>
</task>

</tasks>

<verification>
1. SampleScene을 Unity 에디터에서 열어 AttackTypeOverlay 오브젝트의 Inspector에
   Missing Script 경고가 표시되지 않는지 확인한다.
2. Console 창에 CS 컴파일 에러 없음을 확인한다.
3. Play Mode 진입 후 마우스를 AttackTypeOverlay 존 위로 이동시키면
   Linear/Fan 하이라이트가 전환되는지 확인한다.
</verification>

<success_criteria>
- SampleScene.unity의 AttackTypeOverlay에 Missing Script가 0개
- AttackTypeSelector.cs 컴파일 에러 0개
- 레거시 Input API 참조 0개 (grep 확인)
- 올바른 GUID 2개가 씬 파일에 존재
</success_criteria>

<output>
완료 후 .planning/quick/260605-1mt-fix-attacktypeoverlay-missing-script/ 디렉터리에
260605-1mt-SUMMARY.md 를 생성한다.
</output>
