---
phase: quick-260624-t4e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/MeleeEnemy.cs
  - Assets/Scripts/Enemy/RangedEnemy.cs
autonomous: true
requirements: [ENMY-01, ENMY-02]
must_haves:
  truths:
    - "MeleeEnemy 히트박스는 공격 애니메이션 트리거 후 attackWindupDelay 만큼 지연되어 활성화된다"
    - "RangedEnemy 발사체는 발사 애니메이션 트리거 후 attackWindupDelay 만큼 지연되어 생성된다"
    - "두 딜레이 값 모두 Inspector에서 조정 가능하다"
  artifacts:
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "attackWindupDelay SerializeField + TelegraphAndAttack 코루틴 내 지연 삽입"
      contains: "attackWindupDelay"
    - path: "Assets/Scripts/Enemy/RangedEnemy.cs"
      provides: "attackWindupDelay SerializeField + TelegraphAndFire 코루틴 내 지연 삽입"
      contains: "attackWindupDelay"
  key_links:
    - from: "MeleeEnemy.TelegraphAndAttack()"
      to: "_meleeHitbox.enabled = true"
      via: "WaitForSecondsRealtime(attackWindupDelay)"
      pattern: "attackWindupDelay.*meleeHitbox"
    - from: "RangedEnemy.TelegraphAndFire()"
      to: "FireProjectile()"
      via: "WaitForSecondsRealtime(attackWindupDelay)"
      pattern: "attackWindupDelay.*FireProjectile"
---

<objective>
근접/원거리 적 공격의 hitbox/발사 타이밍을 애니메이션 windup과 동기화한다.

Purpose: 현재 공격 애니메이션 트리거와 히트박스 활성화/발사체 생성이 같은 프레임에 발생해, 시각적으로 windup 중인데도 플레이어가 피해를 입는다. attackWindupDelay를 삽입해 애니메이션 실제 타격 프레임에 맞게 조정 가능하도록 한다.
Output: MeleeEnemy.cs, RangedEnemy.cs — 각각 attackWindupDelay 필드 + 코루틴 내 지연 1줄 추가
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeleeEnemy — attackWindupDelay 필드 추가 및 히트박스 지연 적용</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs</files>
  <action>
두 가지 변경만 수행한다. 다른 로직은 건드리지 않는다.

1. 기존 `hitboxActiveDuration` SerializeField 바로 아래에 필드 추가:
   ```csharp
   [SerializeField] private float attackWindupDelay = 0.1f; // 애니메이션 windup 이후 히트박스 활성화까지 대기 (Inspector 조정)
   ```

2. `TelegraphAndAttack()` 코루틴에서 `_animator?.SetTrigger("isAttacking");` 다음 줄에 삽입:
   ```csharp
   if (attackWindupDelay > 0f)
       yield return new WaitForSecondsRealtime(attackWindupDelay);
   ```
   이 yield는 `if (_meleeHitbox != null)` 블록보다 앞에 위치해야 한다.

삽입 후 코루틴 순서:
  1. `_animator?.SetTrigger("isAttacking");`
  2. `if (attackWindupDelay > 0f) yield return new WaitForSecondsRealtime(attackWindupDelay);`  ← 신규
  3. `if (_meleeHitbox != null) { _meleeHitbox.enabled = true; ... }`

주의: IsAlive 가드(line 185)는 TelegraphAndAttack 최상단에 이미 있다. windup delay 이후에도 IsAlive를 한 번 더 체크해야 한다 — windup 중에 플레이어가 처치할 수 있기 때문이다. _meleeHitbox 블록 진입 전에 `if (!IsAlive) yield break;` 추가.
  </action>
  <verify>
    <automated>파일 저장 후 Unity Editor에서 컴파일 오류 없음 확인. `grep -n "attackWindupDelay" "Assets/Scripts/Enemy/MeleeEnemy.cs"` 결과 2줄 이상 (선언 + 사용)</automated>
  </verify>
  <done>MeleeEnemy Inspector에 "Attack Windup Delay" 필드가 노출되고, 히트박스가 애니메이션 트리거 후 해당 시간만큼 지연되어 활성화된다. 기본값 0.1f.</done>
</task>

<task type="auto">
  <name>Task 2: RangedEnemy — attackWindupDelay 필드 추가 및 발사 지연 적용</name>
  <files>Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <action>
두 가지 변경만 수행한다. 다른 로직은 건드리지 않는다.

1. 기존 `[SerializeField] private Transform firePoint;` 바로 아래에 필드 추가:
   ```csharp
   [SerializeField] private float attackWindupDelay = 0.1f; // 발사 애니메이션 트리거 이후 실제 발사까지 대기 (Inspector 조정)
   ```

2. `TelegraphAndFire()` 코루틴에서 aim line 숨기기(`_aimLine.enabled = false`) 이후, `_animator?.SetTrigger("isAttacking");` 다음 줄에 삽입:
   ```csharp
   if (attackWindupDelay > 0f)
       yield return new WaitForSecondsRealtime(attackWindupDelay);
   ```
   이 yield는 `FireProjectile(aimDir, origin);` 호출보다 앞에 위치해야 한다.

삽입 후 코루틴 순서 (line 218 이후):
  1. `if (_aimLine != null) _aimLine.enabled = false;`
  2. `_animator?.SetTrigger("isAttacking");`
  3. `if (attackWindupDelay > 0f) yield return new WaitForSecondsRealtime(attackWindupDelay);`  ← 신규
  4. `FireProjectile(aimDir, origin);`

주의: windup 중에 적이 대시로 처치될 수 있으므로, yield 이후 `if (!IsAlive) yield break;` 추가 (기존 IsAlive 가드는 telegraph 완료 직후에 있으므로, 여기서 별도로 한 번 더 체크).
  </action>
  <verify>
    <automated>파일 저장 후 Unity Editor에서 컴파일 오류 없음 확인. `grep -n "attackWindupDelay" "Assets/Scripts/Enemy/RangedEnemy.cs"` 결과 2줄 이상 (선언 + 사용)</automated>
  </verify>
  <done>RangedEnemy Inspector에 "Attack Windup Delay" 필드가 노출되고, 발사체가 애니메이션 트리거 후 해당 시간만큼 지연되어 생성된다. 기본값 0.1f.</done>
</task>

</tasks>

<verification>
Unity Editor Play Mode에서 확인:
1. MeleeEnemy가 플레이어에게 접근 → 느낌표 → 공격 애니메이션 시작 후 약 0.1s 뒤 히트박스 활성화
2. RangedEnemy 조준선 페이드인 완료 → 발사 애니메이션 시작 후 약 0.1s 뒤 발사체 생성
3. Inspector에서 값 변경(예: 0.3f) 시 즉시 반영 확인
4. 공격 windup 중 플레이어 대시로 처치 시 hitbox/발사체 생성되지 않음 (IsAlive 가드)
</verification>

<success_criteria>
- MeleeEnemy.cs: attackWindupDelay 필드 + 코루틴 내 WaitForSecondsRealtime(attackWindupDelay) + IsAlive 재체크
- RangedEnemy.cs: attackWindupDelay 필드 + 코루틴 내 WaitForSecondsRealtime(attackWindupDelay) + IsAlive 재체크
- 두 파일 모두 컴파일 에러 없음
- 값 조정 후 체감상 애니메이션 타격 프레임과 실제 피해 타이밍이 일치
</success_criteria>

<output>
완료 후 `.planning/quick/260624-t4e-windup-delay/260624-t4e-SUMMARY.md` 생성
</output>
