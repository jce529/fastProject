---
phase: quick-260624-q65
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Enemy/MeleeEnemy.cs
autonomous: true
requirements: [ENMY-01]

must_haves:
  truths:
    - "MeleeEnemy가 플랫폼 위를 중력에 맞춰 수평으로 이동한다 (대각선 직진 없음)"
    - "앞에 벽이 있으면 점프한다"
    - "앞 바닥이 끊기면 점프한다"
    - "공중에 있을 때는 점프를 시도하지 않는다"
    - "Patrol도 동일한 velocity X 방식으로 동작한다"
  artifacts:
    - path: "Assets/Scripts/Enemy/MeleeEnemy.cs"
      provides: "velocity-X 기반 이동 + WallCheck/GapCheck/GroundCheck 점프 로직"
      exports: []
  key_links:
    - from: "UpdateChase()"
      to: "_rb.linearVelocity"
      via: "x-only assignment, y untouched"
      pattern: "linearVelocity = new Vector2.*linearVelocity\\.y"
    - from: "TryJump()"
      to: "_rb.linearVelocity"
      via: "jumpForce on y"
      pattern: "new Vector2.*jumpForce"
---

<objective>
MeleeEnemy 이동을 MovePosition(XY 합산) → linearVelocity.x 단독 제어로 전환하고,
벽 감지/바닥 끊김 감지 시 점프하는 로직을 추가한다.

Purpose: MovePosition에 XY 합산 벡터를 쓰면 중력을 무시하고 대각선 직진한다.
         velocity.x만 건드리면 Y는 Unity 2D 물리에 맡길 수 있다.
Output: MeleeEnemy.cs — 수정된 단일 파일
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Assets/Scripts/Enemy/MeleeEnemy.cs

<interfaces>
<!-- 현재 MeleeEnemy.cs 핵심 구조 (executor 참조용) -->

// Rigidbody2D 필드
private Rigidbody2D _rb;   // Awake에서 캐시됨

// 현재 이동 패턴 (수정 전)
// UpdateIdle():  _rb.MovePosition(new Vector2(newX, _rb.position.y));
// UpdateChase(): Vector2 dir = normalized XY; _rb.MovePosition(_rb.position + dir * chaseSpeed * Time.deltaTime);

// 수정 후 목표 패턴
// UpdateIdle():  _rb.linearVelocity = new Vector2(_patrolDir * patrolSpeed, _rb.linearVelocity.y);
// UpdateChase(): float dirX = Mathf.Sign(player.x - pos.x);
//                _rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y);
//                TryJump(dirX);

// TryJump 목표 시그니처
// private void TryJump(float moveDir)
//   IsGrounded() → true여야 점프 가능
//   WallAhead(moveDir) OR GapAhead(moveDir) → 조건 시 _rb.linearVelocity = new Vector2(_rb.linearVelocity.x, jumpForce)

// Inspector 추가 필드
[SerializeField] private float jumpForce       = 8f;
[SerializeField] private float wallCheckDist   = 0.4f;
[SerializeField] private float gapCheckDist    = 0.6f;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: velocity X 이동 전환 + WallCheck/GapCheck/GroundCheck 점프 로직</name>
  <files>Assets/Scripts/Enemy/MeleeEnemy.cs</files>
  <action>
아래 변경을 MeleeEnemy.cs에 적용한다. **변경되지 않는 코드는 그대로 유지한다.**

### 1) Inspector 필드 추가 (기존 SerializeField 블록 끝에 추가)
```csharp
[SerializeField] private float jumpForce     = 8f;    // 점프 초속 (Inspector 조정)
[SerializeField] private float wallCheckDist = 0.4f;  // 앞 벽 감지 거리
[SerializeField] private float gapCheckDist  = 0.6f;  // 바닥 끊김 감지 거리 (발 앞으로)
```

### 2) UpdateIdle() 수정 — MovePosition → velocity X

기존:
```csharp
float newX = transform.position.x + _patrolDir * patrolSpeed * Time.deltaTime;
if (Mathf.Abs(newX - _spawnPosition.x) >= patrolHalfRange)
    _patrolDir *= -1f;
_rb.MovePosition(new Vector2(newX, _rb.position.y));
```

교체:
```csharp
if (Mathf.Abs(transform.position.x - _spawnPosition.x) >= patrolHalfRange)
    _patrolDir *= -1f;
_rb.linearVelocity = new Vector2(_patrolDir * patrolSpeed, _rb.linearVelocity.y);
FlipSprite(_patrolDir);
```

### 3) UpdateChase() 수정 — MovePosition → velocity X + TryJump 호출

기존:
```csharp
Vector2 dir = ((Vector2)_playerTransform.position - _rb.position).normalized;
_rb.MovePosition(_rb.position + dir * chaseSpeed * Time.deltaTime);
```

교체:
```csharp
float dirX = Mathf.Sign(_playerTransform.position.x - transform.position.x);
_rb.linearVelocity = new Vector2(dirX * chaseSpeed, _rb.linearVelocity.y);
FlipSprite(dirX);
TryJump(dirX);
```

### 4) 헬퍼 메서드 3개 추가 (기존 Helpers 영역 끝에 추가)

```csharp
/// <summary>땅에 있을 때만 점프. 앞에 벽 또는 바닥 끊김 감지 시 발동.</summary>
private void TryJump(float moveDir)
{
    if (!IsGrounded()) return;
    if (WallAhead(moveDir) || GapAhead(moveDir))
        _rb.linearVelocity = new Vector2(_rb.linearVelocity.x, jumpForce);
}

/// <summary>발 아래 짧은 Raycast — 땅 위에 있으면 true.</summary>
private bool IsGrounded()
{
    // 발 위치: 콜라이더 하단 기준 (0.1f 여유)
    Vector2 origin = (Vector2)transform.position + Vector2.down * 0.55f;
    RaycastHit2D hit = Physics2D.Raycast(origin, Vector2.down, 0.15f,
        LayerMask.GetMask("Default", "Ground", "Platform"));
    return hit.collider != null;
}

/// <summary>이동 방향 앞 벽 Raycast — 충돌체 있으면 true.</summary>
private bool WallAhead(float moveDir)
{
    Vector2 origin = (Vector2)transform.position;
    RaycastHit2D hit = Physics2D.Raycast(origin, new Vector2(moveDir, 0f), wallCheckDist,
        LayerMask.GetMask("Default", "Ground", "Platform"));
    return hit.collider != null;
}

/// <summary>이동 방향 발 앞 아래 Raycast — 바닥 없으면 gap → true.</summary>
private bool GapAhead(float moveDir)
{
    // 발 앞 수평으로 gapCheckDist 이동한 지점에서 아래로 Ray
    Vector2 origin = (Vector2)transform.position + new Vector2(moveDir * gapCheckDist, -0.5f);
    RaycastHit2D hit = Physics2D.Raycast(origin, Vector2.down, 0.5f,
        LayerMask.GetMask("Default", "Ground", "Platform"));
    return hit.collider == null;
}

/// <summary>이동 방향에 따라 스프라이트 좌우 반전.</summary>
private void FlipSprite(float dirX)
{
    if (dirX == 0f) return;
    var sr = GetComponent<SpriteRenderer>();
    if (sr != null) sr.flipX = dirX < 0f;
}
```

### 주의사항
- `IsGrounded`, `WallAhead`, `GapAhead` 의 LayerMask 문자열은 프로젝트 레이어명과 일치해야 한다.
  현재 프로젝트에서 플랫폼/바닥 레이어가 "Default"뿐이면 `LayerMask.GetMask("Default")`만 사용해도 무방.
  레이어명이 확실하지 않으면 `~LayerMask.GetMask("PlayerHurtbox", "PlayerInvincible")` (비트 NOT으로 해당 레이어만 제외) 방식도 가능.
- `FlipSprite`에서 `GetComponent<SpriteRenderer>()`는 Update 빈도가 낮은 호출이 아니므로, 
  Awake에서 `_sr` 필드로 캐시 후 사용하는 것이 더 좋다. 기존에 캐시 필드가 없으면 Awake 블록에 `_sr = GetComponent<SpriteRenderer>();` 추가하고 FlipSprite 내부를 `_sr?.flipX = dirX < 0f;` 로 교체.
- 기존 UpdateIdle()의 순찰 범위 로직(`_spawnPosition` 기반)은 그대로 유지 — 변경 전과 동일한 패턴으로 동작.
- TelegraphAndAttack() 코루틴, OnDashHit(), OnTriggerEnter2D() 등 나머지 코드는 건드리지 않는다.
  </action>
  <verify>
    <automated>
      1. Unity Editor에서 Play — MeleeEnemy가 플랫폼 위를 수평으로 순찰하고 플레이어를 수평으로 추적하는지 확인 (대각선 직진 없음).
      2. 플레이어를 벽 너머에 위치시켜 MeleeEnemy가 벽 앞에서 점프하는지 확인.
      3. 플레이어를 낭떠러지 건너편에 위치시켜 MeleeEnemy가 갭 앞에서 점프하는지 확인.
      4. Console에 컴파일 에러 없음 확인.
    </automated>
  </verify>
  <done>
    - MeleeEnemy가 중력을 받으며 수평으로 이동 (Y 속도 보존)
    - 벽 앞 또는 바닥 끊김 앞에서 점프
    - 공중에서는 추가 점프 없음
    - 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
Play Mode에서 MeleeEnemy를 포함한 룸에서:
- 적이 플랫폼 위에서 중력의 영향을 받으며 수평으로 이동
- 플레이어 추적 시 대각선 직진 없이 같은 플랫폼 레벨 유지
- 벽/갭 앞에서 자동 점프
- Console 에러 없음
</verification>

<success_criteria>
MeleeEnemy.linearVelocity.x 방식으로 이동하며, IsGrounded + WallAhead/GapAhead 조건 시 jumpForce로 Y velocity 인가. 기존 FSM, Telegraph, Attack, OnDashHit 동작 변경 없음.
</success_criteria>

<output>
After completion, create `.planning/quick/260624-q65-meleeenemy-moveposition-velocity-x/260624-q65-SUMMARY.md`
</output>
