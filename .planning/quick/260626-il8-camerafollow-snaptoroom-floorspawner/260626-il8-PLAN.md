---
phase: quick-260626-il8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Camera/CameraFollow.cs
  - Assets/Scripts/World/FloorSpawner.cs
autonomous: true
requirements: [FLOOR-02]

must_haves:
  truths:
    - "게임 시작 시(1층 스폰) 카메라가 룸 전체가 보이는 위치에 고정된다"
    - "층 전환 시 카메라가 새 룸 중심으로 즉시 이동한다"
    - "카메라 고정 중 플레이어 이동이 카메라 위치에 영향을 주지 않는다"
  artifacts:
    - path: "Assets/Scripts/Camera/CameraFollow.cs"
      provides: "SnapToRoom(Vector3) 메서드 + _roomMode 플래그"
      exports: ["SnapToRoom"]
    - path: "Assets/Scripts/World/FloorSpawner.cs"
      provides: "_cameraFollow 연결 + Awake/FloorTransitionSequence에서 SnapToRoom 호출"
  key_links:
    - from: "Assets/Scripts/World/FloorSpawner.cs"
      to: "Assets/Scripts/Camera/CameraFollow.cs"
      via: "_cameraFollow.SnapToRoom(worldCenter)"
      pattern: "_cameraFollow\\.SnapToRoom"
---

<objective>
CameraFollow에 SnapToRoom 모드를 추가하고, FloorSpawner가 룸 스폰/전환 시점에 카메라를 룸 중심으로 고정시킨다.

Purpose: 룸 전체가 보이는 고정 뷰로 전환해 플레이어가 전체 레이아웃을 한눈에 파악하도록 한다.
Output: CameraFollow.SnapToRoom() 메서드, FloorSpawner의 _cameraFollow 연결 및 호출 지점 2곳.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260626-il8-camerafollow-snaptoroom-floorspawner/260626-il8-PLAN.md

<interfaces>
<!-- 기존 CameraFollow (변경 전) -->
From Assets/Scripts/Camera/CameraFollow.cs:
```csharp
// SerializeField: Transform target, Vector3 offset = (0,1,-10)
// LateUpdate: transform.position = target.position + offset
// 변경 없는 필드는 그대로 유지
```

<!-- FloorSpawner 핵심 구조 -->
From Assets/Scripts/World/FloorSpawner.cs:
```csharp
// _roomHeight = 18f
// Awake(): _currentRoom = SpawnRoom(_floor1RoomPrefab, 1); ActivateEnemies(_currentRoom);
// FloorTransitionSequence() Step 2 직후: _playerTransform.position = teleportPos;
//   → 여기 바로 다음에 SnapToRoom 호출 삽입
// Step 3: yield return null  (주석: LateUpdate 실행 허용)
// FloorManager.CurrentFloor — 전환 후 새 층 번호
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraFollow — SnapToRoom 모드 추가</name>
  <files>Assets/Scripts/Camera/CameraFollow.cs</files>
  <action>
CameraFollow.cs를 다음과 같이 수정한다. 기존 target/offset SerializeField는 유지.

추가할 필드:
- `[SerializeField] private float roomOrthoSize = 7f;` — Inspector에서 조정 가능한 룸 뷰 orthographicSize (기본 7f: 16:9 기준 너비 22 units 수용)
- `private bool _roomMode;` — true이면 플레이어 추적 비활성
- `private Camera _camera;` — GetComponent 캐시

Awake() 추가:
```csharp
private void Awake()
{
    _camera = GetComponent<Camera>();
}
```

SnapToRoom() 메서드 추가 (public):
```csharp
/// <summary>
/// 카메라를 룸 중심(worldCenter)으로 고정한다. LateUpdate 플레이어 추적을 중단한다.
/// FloorSpawner가 룸 스폰/전환 시 호출한다.
/// </summary>
public void SnapToRoom(Vector3 worldCenter)
{
    _roomMode = true;
    transform.position = new Vector3(worldCenter.x, worldCenter.y, offset.z);
    if (_camera != null) _camera.orthographicSize = roomOrthoSize;
}
```

LateUpdate() 수정:
```csharp
private void LateUpdate()
{
    if (_roomMode) return;          // 룸 고정 중 — 플레이어 추적 건너뜀
    if (target == null) return;
    transform.position = target.position + offset;
}
```
  </action>
  <verify>Unity Editor 콘솔에 컴파일 오류 없음. CameraFollow Inspector에 Room Ortho Size 필드(기본 7) 노출 확인.</verify>
  <done>CameraFollow.SnapToRoom(Vector3)가 호출되면 카메라가 worldCenter 위치로 이동하고, 이후 LateUpdate에서 플레이어 위치를 따라가지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: FloorSpawner — _cameraFollow 연결 + SnapToRoom 호출 2곳</name>
  <files>Assets/Scripts/World/FloorSpawner.cs</files>
  <action>
FloorSpawner.cs에 다음을 추가한다.

1. Inspector 필드 2개 추가 (기존 _roomHeight 필드 바로 아래):
```csharp
[SerializeField] private CameraFollow _cameraFollow;          // Main Camera에 부착된 CameraFollow
[SerializeField] private float        _roomCameraOffsetY = 6f; // 룸 원점에서 카메라 중심까지 Y 오프셋
```

2. Awake() 맨 끝에 1층 카메라 스냅 추가:
```csharp
// 1층 스폰 직후 카메라를 룸 중심으로 고정
_cameraFollow?.SnapToRoom(new Vector3(0f, _roomCameraOffsetY, 0f));
```
기존 Awake 구조:
```
Instance = this;
_currentRoom = SpawnRoom(_floor1RoomPrefab, 1);
ActivateEnemies(_currentRoom);
// ← 여기에 삽입
```

3. FloorTransitionSequence()의 Step 2 플레이어 순간이동 직후, Step 3 `yield return null` 바로 앞에 삽입:
```csharp
// 카메라를 새 룸 중심으로 즉시 스냅 (Step 3 yield return null 전)
float newRoomBaseY = (FloorManager.CurrentFloor - 1) * _roomHeight;
_cameraFollow?.SnapToRoom(new Vector3(0f, newRoomBaseY + _roomCameraOffsetY, 0f));
```
Step 3 주석도 갱신:
```csharp
// [Step 3] 카메라 스냅 완료 — LateUpdate가 실행되도록 한 프레임 양보
yield return null;
```

Unity Editor Inspector에서 FloorSpawner 컴포넌트의 Camera Follow 슬롯에 Main Camera를 연결해야 한다 (코드 변경 후 수동 연결 필요).
  </action>
  <verify>Unity Editor 콘솔에 컴파일 오류 없음. FloorSpawner Inspector에 Camera Follow 슬롯과 Room Camera Offset Y(기본 6) 노출 확인.</verify>
  <done>
- FloorSpawner Inspector에 _cameraFollow 슬롯이 노출된다
- Awake에서 1층 스폰 후 _cameraFollow?.SnapToRoom이 호출된다
- FloorTransitionSequence Step 2 직후 새 룸 중심으로 SnapToRoom이 호출된다
- 플레이 시 카메라가 룸 전체를 보이는 위치에 고정된다
  </done>
</task>

</tasks>

<verification>
1. Unity Editor 열기 → 컴파일 오류 없음 확인
2. SampleScene에서 FloorSpawner GameObject 선택 → Inspector에서 Camera Follow 슬롯에 Main Camera 드래그 연결
3. Play 모드 진입 → 카메라가 Room_Combat 전체가 보이도록 고정 확인 (플레이어 이동해도 카메라 안 움직임)
4. EXIT 트리거 진입 → 층 전환 시 카메라가 새 룸 중심으로 즉시 이동 확인
5. Room Ortho Size(기본 7) 조정해 룸 크기가 뷰에 맞는지 확인
</verification>

<success_criteria>
- CameraFollow.SnapToRoom(Vector3)가 존재하고, 호출 시 _roomMode=true로 카메라 고정
- FloorSpawner에 _cameraFollow 슬롯 노출 + 2곳(Awake, FloorTransitionSequence)에서 SnapToRoom 호출
- 플레이 테스트 시 룸 전체가 카메라 뷰에 들어오며 고정됨
</success_criteria>

<output>
After completion, create `.planning/quick/260626-il8-camerafollow-snaptoroom-floorspawner/260626-il8-SUMMARY.md`
</output>
