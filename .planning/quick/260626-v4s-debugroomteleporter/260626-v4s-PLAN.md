---
phase: quick-260626-v4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/DebugRoomTeleporter.cs
autonomous: true
requirements: [DEBUG-LOG]
must_haves:
  truths:
    - "Console에 트리거 진입/퇴장 로그가 출력된다"
    - "위 방향키 누를 때 _playerInZone 상태가 콘솔에 출력된다"
  artifacts:
    - path: "Assets/Scripts/World/DebugRoomTeleporter.cs"
      provides: "진단용 Debug.Log 3곳 추가"
  key_links:
    - from: "OnTriggerEnter/Exit2D"
      to: "_playerInZone"
      via: "Debug.Log 출력"
      pattern: "Debug\\.Log.*playerInZone"
---

<objective>
DebugRoomTeleporter 텔레포트가 동작하지 않는 원인을 Console 로그로 진단할 수 있도록
진단용 Debug.Log를 세 곳에 추가한다.

Purpose: 어느 단계에서 실패하는지 — 트리거 감지 / 키 입력 / 조건 불충족 — 파악
Output: 수정된 DebugRoomTeleporter.cs (로직 변경 없음, 로그만 추가)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@Assets/Scripts/World/DebugRoomTeleporter.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 진단 로그 3곳 추가</name>
  <files>Assets/Scripts/World/DebugRoomTeleporter.cs</files>
  <action>
아래 세 곳에 Debug.Log를 추가한다. 기존 로직·포맷·주석은 일절 변경하지 않는다.

**1. OnTriggerEnter2D** — 진입 감지 확인
```csharp
private void OnTriggerEnter2D(Collider2D other)
{
    Debug.Log($"[DebugTeleport] TriggerEnter: {other.name} (tag={other.tag})");
    if (other.GetComponentInParent<PlayerController>() != null)
    {
        _playerInZone = true;
        Debug.Log("[DebugTeleport] _playerInZone = true");
    }
}
```

**2. OnTriggerExit2D** — 퇴장 감지 확인
```csharp
private void OnTriggerExit2D(Collider2D other)
{
    Debug.Log($"[DebugTeleport] TriggerExit: {other.name} (tag={other.tag})");
    if (other.GetComponentInParent<PlayerController>() != null)
    {
        _playerInZone = false;
        Debug.Log("[DebugTeleport] _playerInZone = false");
    }
}
```

**3. Update()** — 위 방향키 눌렸을 때 상태 출력
```csharp
private void Update()
{
    if (Keyboard.current == null) return;

    if (Keyboard.current.upArrowKey.wasPressedThisFrame)
    {
        Debug.Log($"[DebugTeleport] UpArrow pressed | _playerInZone={_playerInZone} | Keyboard.current={Keyboard.current != null}");
    }

    if (!_playerInZone || Keyboard.current == null) return;

    if (Keyboard.current.upArrowKey.wasPressedThisFrame)
    {
        TeleportToRoom();
    }
}
```

주의: Update()의 upArrowKey 검사가 두 번 실행된다 — 첫 번째는 키 눌림 여부를 무조건 로그로 남기고,
두 번째는 기존 `!_playerInZone` 가드 이후에 실제 텔레포트 실행. TeleportToRoom()은 중복 호출되지 않는다.
  </action>
  <verify>
Unity Editor에서 Play 모드 진입 후 텔레포터 트리거 콜라이더 안에 플레이어를 이동시키고
위 방향키를 누른다. Console 창에 아래 세 가지 패턴이 모두 출력되는지 확인:
- `[DebugTeleport] TriggerEnter: ...`
- `[DebugTeleport] _playerInZone = true`
- `[DebugTeleport] UpArrow pressed | _playerInZone=...`
  </verify>
  <done>
Console에서 트리거 진입/퇴장과 위 방향키 입력 시 _playerInZone 상태가 출력된다.
어느 단계에서 로그가 끊기는지로 실패 원인(트리거 미감지 vs 키 미감지 vs 조건 불충족)을 특정할 수 있다.
  </done>
</task>

</tasks>

<verification>
- [ ] OnTriggerEnter2D: `[DebugTeleport] TriggerEnter:` 로그 출력
- [ ] OnTriggerEnter2D: PlayerController 감지 시 `_playerInZone = true` 로그 출력
- [ ] OnTriggerExit2D: `[DebugTeleport] TriggerExit:` 로그 출력
- [ ] Update(): 위 방향키 누를 때 `_playerInZone` 상태 포함 로그 출력
- [ ] 기존 TeleportToRoom() 로직 변경 없음
</verification>

<success_criteria>
Play 모드에서 Console에 세 카테고리 로그가 모두 출력되어,
텔레포트 실패 원인을 트리거/키입력/조건 세 단계 중 어느 한 곳으로 좁힐 수 있다.
</success_criteria>

<output>
완료 후 `.planning/quick/260626-v4s-debugroomteleporter/260626-v4s-SUMMARY.md` 작성
</output>
