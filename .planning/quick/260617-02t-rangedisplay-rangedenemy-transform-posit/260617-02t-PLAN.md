---
phase: quick-260617-02t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/RangeDisplay.cs
  - Assets/Scripts/Enemy/RangedEnemy.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "RangeDisplay 세 가지 표시 모드(Linear, Fan, Circle) 모두 transform.position이 아닌 가슴 오프셋 기준점에서 시작한다"
    - "RangedEnemy 텔레그래프 조준선이 transform.position이 아닌 firePoint 위치(없으면 fallback)에서 시작한다"
  artifacts:
    - path: "Assets/Scripts/Player/RangeDisplay.cs"
      provides: "chestOffset SerializeField + 3개 메서드 origin 갱신"
    - path: "Assets/Scripts/Enemy/RangedEnemy.cs"
      provides: "TelegraphAndFire origin을 firePoint 기준으로 교체"
  key_links:
    - from: "RangeDisplay.UpdateLinearDisplay / UpdateFanDisplay / UpdateCircleDisplay"
      to: "chestOffset 필드"
      via: "(Vector2)transform.position + chestOffset"
    - from: "RangedEnemy.TelegraphAndFire"
      to: "firePoint"
      via: "firePoint != null ? (Vector2)firePoint.position : (Vector2)transform.position"
---

<objective>
공격 사거리 표시와 적 텔레그래프 조준선의 시작점을 각각 플레이어 가슴 오프셋, firePoint 기준으로 변경한다.

Purpose: 범위 표시가 발 위치가 아닌 공격이 실제로 나가는 위치에서 그려져야 시각적 피드백이 정확하다.
Output: RangeDisplay.cs(+chestOffset 필드, origin 3곳 수정), RangedEnemy.cs(TelegraphAndFire origin 1곳 수정)
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RangeDisplay — chestOffset 필드 추가 및 origin 3곳 교체</name>
  <files>Assets/Scripts/Player/RangeDisplay.cs</files>
  <action>
다음 두 가지 변경만 수행한다. 다른 코드는 절대 건드리지 않는다.

1. 기존 `[SerializeField] private float lineWidth` 선언 바로 아래(12번째 줄 근처)에 새 필드를 추가:
   ```csharp
   [SerializeField] private Vector2 chestOffset = new Vector2(0f, 0.3f);
   ```

2. 세 메서드에서 origin 할당을 교체:
   - UpdateLinearDisplay() line 94: `Vector2 origin = transform.position;`
     → `Vector2 origin = (Vector2)transform.position + chestOffset;`
   - UpdateFanDisplay() line 120: `Vector2 origin = transform.position;`
     → `Vector2 origin = (Vector2)transform.position + chestOffset;`
   - UpdateCircleDisplay() line 152: `Vector2 origin = transform.position;`
     → `Vector2 origin = (Vector2)transform.position + chestOffset;`

주의: mouseDir 계산의 `- origin` 부분은 이미 올바르게 origin을 참조하고 있으므로 별도 수정 불필요.
  </action>
  <verify>
    <automated>grep -n "chestOffset" "Assets/Scripts/Player/RangeDisplay.cs" | wc -l</automated>
  </verify>
  <done>
    - chestOffset 필드가 SerializeField로 선언됨 (기본값 0f, 0.3f)
    - UpdateLinearDisplay, UpdateFanDisplay, UpdateCircleDisplay 세 곳 모두 `(Vector2)transform.position + chestOffset` 형태로 교체됨
    - 코드 컴파일 오류 없음 (다른 코드 변경 없음)
  </done>
</task>

<task type="auto">
  <name>Task 2: RangedEnemy — TelegraphAndFire origin을 firePoint 기준으로 교체</name>
  <files>Assets/Scripts/Enemy/RangedEnemy.cs</files>
  <action>
TelegraphAndFire() 코루틴의 origin 할당 1줄만 교체한다.

현재 (line 165):
```csharp
Vector2 origin    = (Vector2)transform.position; 
```

교체 후:
```csharp
Vector2 origin = firePoint != null ? (Vector2)firePoint.position : (Vector2)transform.position;
```

바로 위 주석 (line 164: `// Visual change: telegraph line starts from monster center, but projectile fires from firePoint`)도
실제 동작에 맞게 업데이트:
```csharp
// Telegraph line starts from firePoint (if assigned), matching where the projectile actually fires from.
```

다른 코드는 변경하지 않는다. fireOrigin 계산(line 204)은 이미 동일 패턴으로 올바르게 작성되어 있으므로 그대로 둔다.
  </action>
  <verify>
    <automated>grep -n "firePoint != null" "Assets/Scripts/Enemy/RangedEnemy.cs" | wc -l</automated>
  </verify>
  <done>
    - TelegraphAndFire의 origin이 `firePoint != null ? (Vector2)firePoint.position : (Vector2)transform.position` 형태임
    - fireOrigin 할당(line 204)은 변경되지 않음 (이미 동일 패턴)
    - 코드 컴파일 오류 없음
  </done>
</task>

</tasks>

<verification>
grep -n "chestOffset" Assets/Scripts/Player/RangeDisplay.cs
grep -n "firePoint != null" Assets/Scripts/Enemy/RangedEnemy.cs

예상 결과:
- RangeDisplay.cs: chestOffset이 4줄(필드 선언 1 + origin 할당 3)에서 나타남
- RangedEnemy.cs: `firePoint != null` 패턴이 2줄(origin line 165, fireOrigin line 204)에서 나타남
</verification>

<success_criteria>
- RangeDisplay.cs: chestOffset SerializeField 추가, UpdateLinearDisplay / UpdateFanDisplay / UpdateCircleDisplay 모두 offset 적용
- RangedEnemy.cs: TelegraphAndFire origin이 firePoint 우선, fallback transform.position
- Unity Editor에서 Inspector에 "Chest Offset" 슬라이더 노출 — 플레이테스트 중 조정 가능
- 씬 재컴파일 오류 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260617-02t-rangedisplay-rangedenemy-transform-posit/260617-02t-SUMMARY.md`
</output>
