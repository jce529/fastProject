---
phase: quick-260617-02t
plan: 01
subsystem: combat-visuals
tags: [range-display, ranged-enemy, origin-fix, inspector-tunable]
dependency_graph:
  requires: []
  provides: [chest-offset-origin, firepoint-telegraph-origin]
  affects: [RangeDisplay, RangedEnemy]
tech_stack:
  added: []
  patterns: ["(Vector2)transform.position + offset", "firePoint != null ? firePoint.position : transform.position"]
key_files:
  modified:
    - Assets/Scripts/Player/RangeDisplay.cs
    - Assets/Scripts/Enemy/RangedEnemy.cs
decisions:
  - "chestOffset 기본값 (0f, 0.3f) — Inspector에서 플레이테스트 중 조정 가능"
metrics:
  duration: "~5min"
  completed: "2026-06-17"
  tasks: 2
  files: 2
---

# Quick Task 260617-02t: RangeDisplay/RangedEnemy transform.position origin 수정 Summary

**One-liner:** chestOffset SerializeField로 RangeDisplay 3개 메서드 origin을 가슴 기준으로 올리고, RangedEnemy 텔레그래프 조준선 시작점을 firePoint 위치로 교체.

---

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | RangeDisplay chestOffset 필드 추가 + origin 3곳 교체 | 0e779c5 | Assets/Scripts/Player/RangeDisplay.cs |
| 2 | RangedEnemy TelegraphAndFire origin firePoint 기준 교체 | f9b8827 | Assets/Scripts/Enemy/RangedEnemy.cs |

---

## Changes Made

### Task 1 — RangeDisplay.cs

- `[SerializeField] private Vector2 chestOffset = new Vector2(0f, 0.3f);` 추가 (lineWidth 필드 바로 아래)
- `UpdateLinearDisplay()`, `UpdateFanDisplay()`, `UpdateCircleDisplay()` 세 메서드에서 `Vector2 origin = transform.position;` → `Vector2 origin = (Vector2)transform.position + chestOffset;` 교체
- Unity Inspector에 "Chest Offset" 슬라이더 노출 — 플레이테스트 중 조정 가능

### Task 2 — RangedEnemy.cs

- `TelegraphAndFire()` 내 `origin` 할당을 `firePoint != null ? (Vector2)firePoint.position : (Vector2)transform.position` 로 교체 (주석도 실제 동작 설명으로 업데이트)
- `fireOrigin` 할당(line 219)은 이미 동일 패턴으로 올바르게 작성되어 있어 변경 없음

---

## Verification

```
RangeDisplay.cs:
  line 18: [SerializeField] private Vector2 chestOffset = new Vector2(0f, 0.3f);
  line 95: Vector2 origin = (Vector2)transform.position + chestOffset;
  line 121: Vector2 origin = (Vector2)transform.position + chestOffset;
  line 153: Vector2 origin = (Vector2)transform.position + chestOffset;

RangedEnemy.cs:
  line 167: Vector2 origin = firePoint != null ? (Vector2)firePoint.position : (Vector2)transform.position;
  line 219: Vector3 spawnPos = firePoint != null ? firePoint.position : transform.position;
```

---

## Deviations from Plan

**Task 2 상황:** 메인 브랜치에 이미 `firePoint != null` 패턴이 origin 할당에 적용된 커밋이 존재했음. 워크트리 머지 후 functional 변경은 이미 반영된 상태였고, 플랜이 지정한 주석 문구만 추가로 교체함.

- 동작 변경: 없음 (이미 올바른 코드였음)
- 주석 업데이트: "Lock aim direction at telegraph start..." → "Telegraph line starts from firePoint (if assigned), matching where the projectile actually fires from."

---

## Known Stubs

None.

---

## Self-Check: PASSED

- [x] `Assets/Scripts/Player/RangeDisplay.cs` — chestOffset 4줄 확인
- [x] `Assets/Scripts/Enemy/RangedEnemy.cs` — firePoint != null 2줄 확인
- [x] commit 0e779c5 존재
- [x] commit f9b8827 존재
