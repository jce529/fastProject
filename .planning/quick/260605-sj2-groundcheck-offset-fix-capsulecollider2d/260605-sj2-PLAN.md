---
phase: quick
plan: 260605-sj2
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Player/PlayerController.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Player standing on the platform registers IsGrounded = true"
    - "Idle/Walk/Run animation plays when grounded, not the falling animation"
  artifacts:
    - path: Assets/Scripts/Player/PlayerController.cs
      provides: "Fixed CheckGround origin offset"
      contains: "Vector2.down * 0.05f"
  key_links:
    - from: "PlayerController.CheckGround()"
      to: "Platform collider"
      via: "Physics2D.OverlapCircle at transform.position - 0.05f"
      pattern: "Vector2\\.down \\* 0\\.05f"
---

<objective>
Fix `CheckGround()` so the overlap-circle origin lands inside the platform collider when the player is standing on it.

Purpose: With CapsuleCollider2D whose bottom edge sits exactly at the pivot (offset.y == half_height), the old offset of 0.51f drops the sample point 0.51 units below the pivot — far below the platform surface at only ~0.16 units clearance. Changing to 0.05f places the check just barely below the pivot, directly inside the platform top surface.

Output: One changed constant in PlayerController.cs. IsGrounded becomes true while standing; JumpFall animation exits correctly.
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@Assets/Scripts/Player/PlayerController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ground-check offset constant</name>
  <files>Assets/Scripts/Player/PlayerController.cs</files>
  <action>
    In `CheckGround()` (line 108), change the offset constant from `0.51f` to `0.05f`:

    Before:
      Vector2 origin = (Vector2)_transform.position + Vector2.down * 0.51f;

    After:
      Vector2 origin = (Vector2)_transform.position + Vector2.down * 0.05f;

    Root cause: CapsuleCollider2D has offset.y == half_height (0.6408), so the capsule bottom is exactly at the pivot. 0.51f sends the sample point to transform.position.y - 0.51, which is 0.35 units below the capsule bottom and misses the platform entirely. 0.05f keeps the sample point just inside the platform surface when the player is standing on it. No other changes.
  </action>
  <verify>
    <automated>
      Open Unity Editor, enter Play mode, confirm player stands on the platform without falling through. In the Inspector, confirm _isGrounded shows true while standing. If an Animator is wired: idle/walk animation plays instead of JumpFall.
    </automated>
  </verify>
  <done>Player stands on the platform, IsGrounded is true, falling animation no longer plays while grounded.</done>
</task>

</tasks>

<verification>
In Play mode: player spawns on platform, IsGrounded = true visible in Inspector, idle animation active. Jump works and IsGrounded returns to true on landing.
</verification>

<success_criteria>
- `Vector2.down * 0.05f` present in CheckGround()
- No other lines in PlayerController.cs changed
- IsGrounded = true while player is on the platform
</success_criteria>

<output>
After completion, create `.planning/quick/260605-sj2-groundcheck-offset-fix-capsulecollider2d/260605-sj2-SUMMARY.md`
</output>
