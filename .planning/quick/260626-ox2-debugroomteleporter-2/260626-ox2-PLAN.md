---
phase: quick-260626-ox2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/World/DebugRoomTeleporter.cs
autonomous: true
requirements: [DEBUG-TELEPORTER-FIX]

must_haves:
  truths:
    - "다른 텔레포터로 전환해도 이전 방이 파괴된 뒤 새 방이 스폰된다"
    - "텔레포트 직후 카메라가 새 방을 따라간다"
  artifacts:
    - path: "Assets/Scripts/World/DebugRoomTeleporter.cs"
      provides: "static 공유 상태 + 카메라 스냅"
      contains: "s_lastDebugRoom, s_nextSpawnPos, CameraFollow"
  key_links:
    - from: "DebugRoomTeleporter.TeleportToRoom()"
      to: "CameraFollow.SnapToRoom()"
      via: "Camera.main.GetComponent<CameraFollow>()"
    - from: "DebugRoomTeleporter (모든 인스턴스)"
      to: "s_lastDebugRoom / s_nextSpawnPos"
      via: "static 필드 공유"
---

<objective>
DebugRoomTeleporter 버그 2개를 단일 파일 수정으로 수정한다.

Purpose: 14개 텔레포터 사이를 전환할 때 방이 겹치거나 카메라가 Room_Debug에 고정되는 문제를 제거해 디버그 워크플로우를 복원한다.
Output: DebugRoomTeleporter.cs — static 공유 상태 + TeleportToRoom() 내 카메라 스냅 추가
</objective>

<execution_context>
@D:/새 폴더/Fast/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Fast/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- CameraFollow (Assets/Scripts/Camera/CameraFollow.cs) -->
public void SnapToRoom(Vector3 worldCenter);   // CameraBound 없는 룸 — 중심점으로 스냅
public void SnapToRoom(Bounds worldBounds);    // CameraBound 있는 룸 — Bounds 클램프 모드

<!-- CameraBound (Assets/Scripts/Camera/CameraBound.cs) -->
public Bounds GetWorldBounds();  // transform.position 기준 월드 Bounds 반환

<!-- FloorSpawner.SnapCameraToRoom() 패턴 (참조용) -->
CameraBound cb = room.GetComponentInChildren<CameraBound>();
if (cb != null)
    _cameraFollow.SnapToRoom(cb.GetWorldBounds());
else
    _cameraFollow.SnapToRoom(fallbackCenter);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DebugRoomTeleporter — static 공유 상태 + 카메라 스냅</name>
  <files>Assets/Scripts/World/DebugRoomTeleporter.cs</files>
  <action>
**버그 1 수정 (카메라 미업데이트):**
TeleportToRoom() 내 플레이어 이동(_playerTransform.position = entryPos) 직후에
Camera.main에서 CameraFollow를 가져와 FloorSpawner.SnapCameraToRoom() 동일 패턴으로 스냅한다.

```csharp
CameraFollow cam = Camera.main != null ? Camera.main.GetComponent<CameraFollow>() : null;
if (cam != null)
{
    CameraBound cb = s_lastDebugRoom.GetComponentInChildren<CameraBound>();
    if (cb != null)
        cam.SnapToRoom(cb.GetWorldBounds());
    else
        cam.SnapToRoom(entryPos);
}
```

**버그 2 수정 (방 겹침):**
인스턴스 필드 2개를 static으로 교체한다.

제거:
```csharp
private GameObject _debugRoom;
private Vector3    _nextSpawnPos = Vector3.zero;
```

추가 (클래스 상단):
```csharp
private static GameObject s_lastDebugRoom;
private static Vector3    s_nextSpawnPos = Vector3.zero;
```

TeleportToRoom() 내 모든 `_debugRoom` → `s_lastDebugRoom`, `_nextSpawnPos` → `s_nextSpawnPos`로 치환한다.
(코드 4줄: Destroy 체크, Instantiate 결과 대입, += 누적, GetComponentInChildren 인자)
  </action>
  <verify>
    <automated>Unity Editor에서 컴파일 오류 없음 확인 (Console 창 오류 0). 플레이 모드에서 텔레포터 A → 텔레포터 B 순서로 Enter: B 텔레포트 시 A방이 Destroy되고 카메라가 새 방으로 이동하는지 확인.</automated>
  </verify>
  <done>
- 컴파일 오류 없음
- 텔레포터를 전환해도 이전 방이 정리되고 새 방만 존재
- 텔레포트 후 카메라가 새 방을 따라감 (Room_Debug에 묶이지 않음)
  </done>
</task>

</tasks>

<verification>
1. Unity Console에 컴파일 오류 없음
2. 플레이 모드 — 텔레포터 A로 진입 후 Enter: 방 스폰 + 카메라 이동 정상
3. 플레이 모드 — 텔레포터 B로 진입 후 Enter: A방 Destroy + 새 방 스폰 + 카메라 이동 정상
4. 같은 텔레포터를 2회 연속 사용해도 이전 방 정리 후 재스폰 정상
</verification>

<success_criteria>
- DebugRoomTeleporter.cs 수정 완료, static 필드 2개 + 카메라 스냅 로직 추가
- 14개 텔레포터 어떤 순서로 사용해도 방 겹침 없음
- 텔레포트 직후 카메라가 목적 방의 CameraBound 또는 entryPos를 기준으로 즉시 스냅
</success_criteria>

<output>
After completion, create `.planning/quick/260626-ox2-debugroomteleporter-2/260626-ox2-SUMMARY.md`
</output>
