---
phase: quick-260607-ku4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/AttackTypeSelector.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "AttackTypeSelector.Selected is updated when player enters AttackTypeZone even when AttackTypeSelector MonoBehaviour is not in the scene"
  artifacts:
    - path: "Assets/Scripts/UI/AttackTypeSelector.cs"
      provides: "Fixed SetType — Selected updates unconditionally, RefreshHighlights only called when _instance is present"
  key_links:
    - from: "AttackTypeZone.OnTriggerEnter2D"
      to: "AttackTypeSelector.Selected"
      via: "AttackTypeSelector.SetType()"
      pattern: "AttackTypeSelector\\.SetType"
---

<objective>
Fix the guard clause in AttackTypeSelector.SetType that blocks the Selected static field from being updated when no UI MonoBehaviour instance is present in the scene.

Purpose: AttackTypeZone triggers call SetType on player entry. The early return on _instance == null prevents Selected from ever changing when the AttackTypeSelector GameObject is absent from the scene.

Output: One-line fix — remove the null-instance guard from SetType and use null-conditional operator on RefreshHighlights call instead.
</objective>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SetType null-instance guard in AttackTypeSelector</name>
  <files>Assets/Scripts/UI/AttackTypeSelector.cs</files>
  <action>
In SetType, replace the two-guard + direct call pattern with the following:

BEFORE:
```csharp
public static void SetType(AttackType type)
{
    if (_instance == null) return;
    if (Selected == type) return;
    Selected = type;
    _instance.RefreshHighlights();
}
```

AFTER:
```csharp
public static void SetType(AttackType type)
{
    if (Selected == type) return;
    Selected = type;
    _instance?.RefreshHighlights();
}
```

Remove only the `if (_instance == null) return;` line. Keep the duplicate-type early return. Change `_instance.RefreshHighlights()` to `_instance?.RefreshHighlights()`. No other changes to the file.
  </action>
  <verify>
    <automated>Open Assets/Scripts/UI/AttackTypeSelector.cs and confirm: (1) no line reads `if (_instance == null) return;` inside SetType, (2) the call reads `_instance?.RefreshHighlights();`</automated>
  </verify>
  <done>SetType updates Selected regardless of _instance presence. When _instance is null the null-conditional silently skips RefreshHighlights. When _instance exists RefreshHighlights is called as before.</done>
</task>

</tasks>

<verification>
Play in editor: walk player into AttackTypeZone_Fan trigger. AttackTypeDebugOverlay (already on Player) should immediately display "Fan". Walk out and into AttackTypeZone_Linear — should display "Linear". No AttackTypeSelector GameObject needed in the scene.
</verification>

<success_criteria>
AttackTypeSelector.Selected reflects the current zone type at all times, regardless of whether the AttackTypeSelector UI MonoBehaviour is present in the scene.
</success_criteria>

<output>
After completion, update .planning/STATE.md Quick Tasks Completed table with entry for 260607-ku4.
</output>
