using UnityEngine;

/// <summary>
/// 각 Room 프리팹에 배치하는 ENT(진입점) 마커.
/// 이 컴포넌트가 붙은 자식 오브젝트의 위치로 플레이어가 텔레포트된다.
/// 에디터에서 자식 오브젝트를 이동해 진입점을 자유롭게 조정할 수 있다.
/// </summary>
public class RoomEntry : MonoBehaviour { }
